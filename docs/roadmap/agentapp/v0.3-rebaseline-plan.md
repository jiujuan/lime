# Agent App v0.3 / 内容工厂 Rebaseline 计划

更新时间：2026-05-15

## 一句话结论

`docs/roadmap/agentapp` 已升级到 Agent App v0.3 与 `内容工厂`事实源；P4-R Rebaseline 已把客户端实验岛、fixture、demo、workflow runtime、Lab UI 和测试对齐到 `content-factory-app / content_scenario_planning / content_factory_demo / content_table / content_scenarios`。P5-P16 已继续把 Cloud bootstrap、schema coverage、schema gate、setup resolver、setup state store、installed state snapshot、local persistence adapter、package cache / verify / rollback、runtime package loader、entry runtime guard、Lab install / launch flow、Agent App Lab 专用 GUI smoke 与最小 Agent App Manager 补齐；P17 Gate 审计、P17.0 Formal Entry Contract、P17.1 Formal route / nav / copy hardening、P17.2.1 Source state model、P17.2.2 Install review descriptor、P17.2.3 Registration hardening 与 P17.2.4a Cloud release descriptor / verification gate、P17.2.4b-1 acquisition seam / verified cache source、P17.2.4b-2 packageUrl fetch / staging / manifest extraction 与 P17.2.5 public schema / reference CLI / standard example package cross-check 已完成，P17.3 lifecycle / cleanup contract 与 P17.4 runtime surface production hardening 已完成，当前进入 P17.5 formal entry GUI smoke。上游标准新增 public schema、reference CLI、release metadata、runtime model 和 overlay resolver 后，P17.3 已把 source / install 已验证的 package code 与 overlay、secret binding、setup state、cleanup evidence 分离；P17.4 已补 Host Bridge 与完整 GUI smoke 证据。

## 升级后的事实源

| 事实源 | 关键变化 | 对实施计划的影响 |
|---|---|---|
| `README.md` | 标准从 v0.2 升级到 v0.3，标杆 App 改为 `内容工厂`。 | Host profile、manifest normalize、readiness 与测试夹具要支持 v0.3。 |
| `content-factory-app.md` | 产品从“内容工程化案例”升级为可安装垂直 App：项目首页、知识库、内容场景规划、内容工厂、策略分析、PPT / 报告、数据复盘。 | P4 仍只做最小闭环，但命名和数据对象必须贴近完整 App。 |
| `p4-content-factory-demo.md` | P4.1 接口改为 `runContentFactoryDemo()`，入口改为 `content_scenario_planning`。 | 旧 `runContentEngineeringDemo()` 不能继续作为 current 主实现。 |
| `p4-workflow-runtime.md` | P4.2 workflow definition 改为 `content_factory_demo`。 | runtime 已有雏形，但需要改到新 workflow key 和 storage/artifact/evidence kind。 |
| 当前工作区 | 旧 SceneApps 主路径正在被清理。 | Agent App 不应复活 `sceneapp_*` 命令或旧 SceneApps 页面；继续保持 `src/features/agent-app/` 实验岛。 |
| `/Users/coso/Documents/dev/ai/limecloud/agentapp/docs/zh/client-implementation/*` | 新增 discovery / installation、projection、readiness、runtime、安全、overlay、typed SDK 宿主实现指南。 | P17.2.4a 已把 Cloud metadata 落成 release descriptor；P17.2.4b-1 已把 acquisition seam / verified cache source 接入安装主链；P17.2.4b-2 已补 packageUrl fetch / staging；P17.2.5 已以 schema / reference CLI 做机械 cross-check，后续 P17.3 与 P17.4 已完成，当前进入 P17.5 formal entry GUI smoke。 |
| `/Users/coso/Documents/dev/ai/limecloud/agentapp/docs/public/schemas/*` 与 `bin/agentapp-ref.mjs` | 标准开始提供 public JSON Schema 与 reference CLI。 | 客户端不能只靠内部 fixture；需要和 `content-factory-app` 标准示例包做投影 / readiness 对照。 |

## P4-R 执行状态

| 阶段 | 当前状态 | 证据 / 下一步 |
|---|---|---|
| P4-R0 计划与事实源收口 | 已完成 | `README.md`、`implementation-plan.md` 与本文都已把 P5 前置 gate 调整为 v0.3 / 内容工厂 rebaseline。 |
| P4-R1 v0.3 manifest / fixture / readiness | 已完成 | `content-factory-app.json` 成为 current fixture；manifest normalize、projection、readiness 和 host capability profile 已支持 v0.3。 |
| P4-R2 内容工厂 demo rebaseline | 已完成 | current API 是 `runContentFactoryDemo()`；storage / artifact / evidence key 收口到 `content_scenarios/*`、`content_table`、`content_factory_demo`。 |
| P4-R3 WorkflowRuntimeHost rebaseline | 已完成 | workflow key 收口到 `content_factory_demo`；trace step 使用内容工厂 current 语义。 |
| P4-R4 Lab UI / i18n / tests rebaseline | 已完成 | `AgentAppLabPage`、导出 API 与五语言 `agent.json` 已切到 内容工厂。 |
| P4-R5 Gate 恢复与旧 SceneApp 不复活 | 已完成 | `test:contracts`、`governance:legacy-report` 与 `LIME_VITEST_BATCH_SIZE=8 npm run verify:local` 已通过；未复活 `sceneapp_*` Rust 命令或旧 SceneApps 页面。 |
| P5 Cloud Bootstrap | 已通过 | P4-R5 与 P5.0-P5.5 已通过；不让 Cloud 运行默认 Agent。详见 [p5-cloud-bootstrap.md](./p5-cloud-bootstrap.md)。 |
| P6 Schema Coverage | 已通过 | v0.3 projection / readiness schema coverage 已补到客户端实验岛。详见 [p6-schema-coverage.md](./p6-schema-coverage.md)。 |
| P7 Schema Gate | 已通过 | 本地 schema / snapshot gate 已覆盖 projection / readiness 结构与 setup issue 字段。详见 [p7-schema-gate.md](./p7-schema-gate.md)。 |
| P8 Setup Resolver | 已通过 | `AgentAppSetupState` 与 `needs-setup` readiness 语义已落地。详见 [p8-setup-resolver.md](./p8-setup-resolver.md)。 |
| P9 Setup State Store | 已通过 | 本地 setup state store、readiness 输入和 cleanup preview 已集成。详见 [p9-setup-state-store.md](./p9-setup-state-store.md)。 |
| P10 Installed State | 已通过 | Installed state snapshot、in-memory store、cleanup preview 与导出入口已集成。详见 [p10-installed-state-persistence.md](./p10-installed-state-persistence.md)。 |
| P11 Local Persistence Adapter | 已通过 | P10 installed state 已从 in-memory 推进到本地可恢复 adapter；未做下载器、市场页或正式主导航。详见 [p11-local-persistence-adapter.md](./p11-local-persistence-adapter.md)。 |
| P12 Package Cache / Verify / Rollback | 已通过 | Package cache、hash verify、cached fallback、upgrade staging 和 rollback 已落地；未执行 package JS。详见 [p12-package-cache-verify-rollback.md](./p12-package-cache-verify-rollback.md)。 |
| P13 Runtime Package Loader / UI Bundle Loader | 已通过 | 已从 verified package cache 读取 runtime descriptor / UI bundle descriptor，并继续通过 UI Host 和 policy gate。详见 [p13-runtime-package-loader.md](./p13-runtime-package-loader.md)。 |
| P14 Entry Runtime Guard / Permission Prompt | 已通过 | 合并 entry readiness、permission、setup state、package verification 和 runtime policy，并接入 Lab run / mount 前置路径。详见 [p14-entry-runtime-guard-permission-prompt.md](./p14-entry-runtime-guard-permission-prompt.md)。 |
| P15 Lab Install / Launch Flow | 已通过 | 已串联 install review、verified cache、installed state、permission prompt、runtime launch 和 cleanup preview。详见 [p15-lab-install-launch-flow.md](./p15-lab-install-launch-flow.md)。 |
| P15-H GUI Smoke / Cleanup Rehearsal | 已通过专用验证 | 已补 Agent App Lab 专用 smoke、summary / screenshot 证据和 cleanup rehearsal 口径。详见 [p15-h-gui-smoke-cleanup-rehearsal.md](./p15-h-gui-smoke-cleanup-rehearsal.md)。 |
| P16 Agent App Manager / Product Entry Gate | 已通过最小实现 | 已在实验岛内验证 App 管理面、entry launcher、disable / uninstall preview 和 cleanup evidence。详见 [p16-agent-app-manager-product-entry-gate.md](./p16-agent-app-manager-product-entry-gate.md)。 |
| P16-H Multi-app repository / lifecycle hardening | P17.2.1-P17.2.5 已完成，下一刀 P17.3 lifecycle / cleanup contract hardening | 详见 [p16-h-multi-app-repository-lifecycle-hardening.md](./p16-h-multi-app-repository-lifecycle-hardening.md)、[p17-formal-entry-contract.md](./p17-formal-entry-contract.md) 与 [p17-source-install-contract-hardening.md](./p17-source-install-contract-hardening.md)；继续硬化 lifecycle / cleanup、overlay / setup / secret binding 和 evidence 边界，不直接发布 marketplace 或 Cloud 管理台。 |

## P4-R5 验证记录

更新时间：2026-05-15

| 校验 | 结果 | 说明 |
|---|---|---|
| `rg` legacy audit | 通过 | `src/features/agent-app` 与五语言 `agent.json` 中不再出现 `contentEngineering`、`scene_exhaustion`、`content_engineering_*`、`shenlan-content-engineering` 等旧 current 关键字。 |
| `rg` boundary audit | 通过 | `src/features/agent-app` 没有 `safeInvoke` / `invoke`、Tauri 注册、mock priority、raw Worker 等越界入口。 |
| `npm run test:contracts` | 通过 | 命令契约、Harness 契约、modality contracts 与 cleanup report contract 均通过。 |
| `npm run governance:legacy-report` | 通过 | 边界违规为 0；`rust-sceneapp-command-module-surface` 继续保持 `dead / 零引用`。 |
| `LIME_VITEST_BATCH_SIZE=8 npm run verify:local` | 通过 | 覆盖 `verify:app-version`、`lint`、`typecheck`、105 批 Vitest、`test:contracts`、`cargo test --manifest-path src-tauri/Cargo.toml` 与 `verify:gui-smoke`。 |

门禁修复：首轮 `verify:local` 暴露 `useServiceSkills.test.tsx` 隐式依赖未声明的 `get_automation_jobs` Tauri mock；已改为测试内显式 mock `getAutomationJobs()`，避免把 service skills 测试耦合到全局 browser mock。

补充清理：Desktop 端已继续下线 SceneApp 首页推荐卡片、Workspace 启动 Hook、launch bridge / launcher / launch draft 旧链路，以及自动化详情页对 `planSceneAppLaunch` 等 retired API 的运行时调用；历史 `sceneapp_execution_summary` 仍仅作为只读回看与结果复盘参考保留。治理目录册新增 `sceneapp-active-launch-surface` / `sceneapp-active-launch-frontend-surface`，防止后续把独立 SceneApp 启动面接回 Agent Chat 首页或 Workspace。

继续清障：Desktop 端已删除旧 `src/lib/sceneapp/catalog.ts` catalog 编译 / 存储投影，并从 retired API 网关和产品 view model 中移除 `listSceneAppCatalog`、`SceneAppCatalog`、目录统计卡与目录卡片 helper；Agent App 后续作为应用目录与运行装配的唯一 current 事实源，历史 `sceneapp_execution_summary` 只读链不受影响。

## 新实施顺序

```text
P4-R0~P4-R5 v0.3 / 内容工厂 Rebaseline（已通过）
→ P5.0 v0.3 标准差距收口（已通过）
→ P5.1 Cloud Bootstrap payload DTO / parser / validator（已通过）
→ P5.2 Bootstrap payload 本地 package source adapter（已通过）
→ P5.3 Tenant enablement + local readiness 合并判断（已通过）
→ P5.4 hash mismatch / disable / offline regression tests（已通过）
→ P5.5 schema / reference CLI cross-check（已通过）
→ P6 v0.3 projection / readiness schema coverage（已通过）
→ P7 schema validator / reference snapshot gate（已通过）
→ P8 setup resolver / needs-setup semantics（已通过）
→ P9 installed setup state store（已通过）
→ P10 installed app state / persistence（已通过）
→ P11 local persistence adapter（已通过）
→ P12 package cache / verify / rollback（已通过）
→ P13 runtime package loader / UI bundle loader（已通过）
→ P14 entry runtime guard / permission prompt（已通过）
→ P15 lab install / launch flow（已通过）
→ P15-H GUI smoke / cleanup rehearsal hardening（已通过专用验证）
→ P16 Agent App Manager / Product Entry Gate（已通过最小实现）
→ P16-H.0 Multi-app repository / lifecycle hardening 计划收口（已通过）
→ P16-H.1 Repository-backed multi-app list（已完成最小实现）
→ P16-H.2 Selected app launcher + persisted lifecycle（已完成最小实现）
→ P16-H.3 Cleanup rehearsal evidence export（已完成最小实现）
→ P16-H.4 Residual audit（已完成最小实现）
→ P16-H.5 Agent App Lab GUI smoke + flag-off regression（已完成最小实现）
→ P17 正式入口前 Gate 审计（已完成）
→ P17.0 Formal Entry Contract（已完成计划收口）
→ P17.1 Formal route / nav / copy hardening（已完成最小实现） → P17.2.1 Source state model（已完成） → P17.2.2 Install review descriptor（已完成） → P17.2.3 Registration hardening（已完成最小实现）
→ P17.2.4a Cloud release descriptor / verification gate（已完成） → P17.2.4b-1 Cloud package acquisition seam / verified cache source（已完成） → P17.2.4b-2 packageUrl fetch / staging（已完成） → P17.2.5 Schema / reference CLI / example package cross-check（已完成）
→ P17.3 Lifecycle / cleanup contract hardening（已完成）
→ P17.4 Runtime surface production hardening（已完成，含 P17.4.5 GUI smoke）
→ P17.5 Formal entry GUI smoke（下一刀）
```

## 执行边界

1. 本计划只更新 Lime Desktop 客户端，不把 Cloud / LimeCore 职责写进 `lime/docs/roadmap/agentapp`。
2. `/Users/coso/Documents/dev/ai/limecloud/agentapp` 作为 v0.3 标准事实源，约束 schema、capability、entry kind、runtime package、release descriptor、overlay、readiness、reference CLI 和 provenance。
3. `内容工厂`的客户端 P4 current 命名以本目录为准：`content-factory-app`、`content_scenario_planning`、`content_factory_demo`、`content_table`、`content_scenarios/*`。
4. 如果上游标准示例和客户端 P4 demo 出现业务 key 差异，先保持客户端单一事实源，不在源码里混用两套 key。
5. P4-R 只做 rebaseline，不新增完整 SaaS、市场页、正式主导航或 raw worker sandbox。

## 文件级落点

| 阶段 | 主要改动 | 文件边界 |
|---|---|---|
| P4-R1 | v0.3 manifest、fixture、readiness、capability profile。 | `types.ts`、`manifest/*`、`fixtures/content-factory-app.json`、`projection/*`、`readiness/*`、`install/installedAppPreview.ts`。 |
| P4-R2 | demo API 与业务对象改名，storage / artifact / evidence kind 收口。 | `runtime/contentFactoryDemo.ts`、`runtime/contentFactoryDemo.test.ts`、`adapters/*`、`sdk/*`。 |
| P4-R3 | workflow definition 和 trace step 改到内容工厂 current key。 | `runtime/workflowRuntimeHost.ts`、`runtime/runtimePolicy.ts`、`runtime/workflowRuntimeCapabilityProfile.ts`。 |
| P4-R4 | Lab UI、五语言文案、导出 API 和 UI 测试改名。 | `ui/AgentAppLabPage.tsx`、`ui/AgentAppLabPage.test.tsx`、`index.ts`、`src/i18n/resources/*/agent.json`。 |
| P4-R5 | 验证 gate、dangling 引用和旧 SceneApp 清障。 | `src/features/agent-app/*`、`src-tauri/*` 中仍引用旧 SceneApp 的位置。 |

## P4-R0：计划与事实源收口

目标：先避免继续沿旧命名堆实现。

交付：

- `README.md` 的当前执行顺序改成“先 rebaseline，再 P5”。
- `implementation-plan.md` 明确 P4-R 是进入 P5 的前置 gate。
- 所有 current 文档以 `内容工厂` 为主，不再把 `AI 内容工程化` 作为 current 名称。

验收：

```bash
rg -n "AI 内容工程化|contentEngineering|ContentEngineering|content_engineering|scene_exhaustion|scenes/" docs/roadmap/agentapp
```

允许结果：只出现在历史说明或明确标注为 legacy / pre-v0.3 的段落；current 计划不能再出现。

## P4-R1：v0.3 Manifest / Fixture / Readiness

目标：让 App Host 读取 v0.3 package，而不是继续伪装 v0.2。

交付：

- `fixtures/content-factory-app.json` 替换旧 `content-engineering-app.json`。
- manifest 支持 `manifestVersion: 0.3.0`，normalize 后保留 `"0.3"`。
- `appId = content-factory-app`，storage namespace 同名。
- entries 至少包含：`dashboard`、`knowledge`、`content_scenario_planning`、`content_factory`、`content_strategist`。
- capability range 与 host profile 对齐 `^0.3.0`，不再靠旧 `^0.1.0` 过关。

验收：

```bash
npm run test -- \
  src/features/agent-app/manifest/parseManifest.test.ts \
  src/features/agent-app/projection/projectApp.test.ts \
  src/features/agent-app/readiness/checkReadiness.test.ts \
  src/features/agent-app/featureFlag.test.ts
```

## P4-R2：内容工厂 Demo Rebaseline

目标：把 P4.1 业务闭环从旧“内容工程化样板”改成 `内容工厂` 最小闭环。

交付：

- `runtime/contentFactoryDemo.ts` / `contentFactoryDemo.test.ts` 成为 current 主文件。
- 导出 `runContentFactoryDemo()`、`ContentFactoryProjectRecord`、`ContentFactoryScenarioRecord`、`ContentFactoryAssetRecord`。
- 默认入口改为 `content_scenario_planning`。
- storage 写入：`projects/*`、`knowledge-bindings/*`、`content_scenarios/*`、`content-assets/*`。
- Artifact kind 改为 `content_table`。
- Evidence kind 改为 `content_factory_demo`。
- 旧 `contentEngineering*` 不保留 compat，实验岛无人依赖时直接删除。

验收：

```bash
npm run test -- \
  src/features/agent-app/runtime/contentFactoryDemo.test.ts \
  src/features/agent-app/adapters/AdapterCapabilityHost.test.ts \
  src/features/agent-app/sdk/MockCapabilityHost.test.ts
```

## P4-R3：Workflow Runtime Rebaseline

目标：保留已完成的受控 DSL runner，但把 workflow definition 对齐新内容工厂对象。

交付：

- `WorkflowRuntimeHost` 继续只允许白名单 step。
- `runContentFactoryDemo({ workflowRuntime })` 执行 `workflowKey = content_factory_demo`。
- workflow trace 中出现 `store-project`、`search-knowledge`、`store-content-scenarios`、`store-content-assets`、`create-content-table`、`record-content-evidence`。
- policy 仍固定阻断 raw worker、外部代码、网络、文件系统。

验收：

```bash
npm run test -- \
  src/features/agent-app/runtime/workflowRuntimeHost.test.ts \
  src/features/agent-app/runtime/contentFactoryDemo.test.ts
```

## P4-R4：Lab UI / i18n / Index Export Rebaseline

目标：用户可见层全部切到 内容工厂。

交付：

- `AgentAppLabPage` 按 `ContentFactoryDemoResult` 展示项目、内容场景、内容资产、Artifact、Evidence、workflow trace。
- 五语言 `agent.json` 更新：`zh-CN / zh-TW / en-US / ja-JP / ko-KR`。
- `src/features/agent-app/index.ts` 导出 content factory current API。
- UI 测试不再查询 `scene_exhaustion` 或 `content_engineering_demo`。

验收：

```bash
npm run test -- \
  src/features/agent-app/ui/AgentAppLabPage.test.tsx \
  src/i18n/__tests__/translation-coverage.test.ts \
  src/i18n/__tests__/loadNamespace.test.ts \
  src/i18n/__tests__/types.test.ts
```

## P4-R5：Gate 恢复与旧 SceneApp 不复活

目标：把升级后的 Agent App 与当前 SceneApp 清理方向一起收口，不用修旧产品来绕过失败。

交付：

- `src/features/agent-app` 不包含 Tauri `invoke`、不新增 command。
- 不恢复已删除的 `src-tauri/src/sceneapp/*`、`src/components/sceneapps/SceneAppsPage.tsx` 等旧 SceneApp 主路径。
- 如果 Rust 仍引用 `crate::sceneapp`，优先删除或改到当前事实源，而不是重建旧模块。

验收：

```bash
rg -n "safeInvoke|invoke\(|tauri::|generate_handler|mockPriorityCommands|defaultMocks|new Worker|Worker\(" src/features/agent-app || true
npm run test:contracts
LIME_VITEST_BATCH_SIZE=8 npm run verify:local
```

## 后续进入门槛

P4-R 已通过，并且 P5-P16 已按实验岛边界继续推进。当前进入 P16-H 时仍必须保持以下边界：

1. current 代码和文档都只承认 Agent App v0.3 / 内容工厂。
2. `smoke:agent-app-lab` 与最新 `verify:gui-smoke` 均通过，summary / screenshot / cleanup preview 可复核；后续若全局 smoke 再受外部 provider/model 影响，必须单独记录。
3. 内容工厂 P4.1 / P4.2 定向测试通过。
4. delete-data 能清理 content factory demo 的 storage / artifact / evidence / task。
5. 没有复活旧 SceneApp runtime 或旧 `sceneapp_*` command。

P14 已经完成 entry runtime guard / permission prompt 的当前实现，P15 已把这些能力串成 Lab-only 安装启动闭环，P15-H 已补专用 GUI smoke，P16 已补最小 App Manager；P17 Gate 审计、P17.0 Formal Entry Contract 与 P17.1 Formal route / nav / copy hardening 已完成，下一刀先做 P17.2 Source / install contract hardening；不把 App 接进 marketplace、真实 delete-data 或 Cloud 管理台，Cloud 不运行默认 Agent。

P14 不做：

1. 不做 Agent App 市场页、审核流、支付或企业控制台。
2. 不把 App entry 接入正式主导航。
3. 不让 Cloud / LimeCore 运行默认 Agent Runtime、渲染 App UI 或管理本地 App storage。
4. 不新增第二套安装、readiness、persistence 或 cleanup 链路。
5. 不做 raw worker sandbox、任意 package JS 执行或完整行业内容系统。
6. P16-H 必须继续复用 P14 guard，不允许为了 App Manager、演示、smoke 或后续正式入口而绕过授权 gate。
