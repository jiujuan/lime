# P18 Completion Audit / 协作防打架记录

更新时间：2026-05-19 14:42

状态：P18.1-P18.6 当前交付候选已具备证据；P18.7-A/B/C/D Host first-cut、P18.7-F 内容工厂真实闭环、20:49 run-scenarios 连续 GUI materialization、22:13 run-strategy cross-project fix、22:19 run-review 通过证据，以及 22:33 五动作 full-flow action gates 已落地；P18.7-E 已新增 Host-managed fixture connector mutation proof、Cloud Overlay outbox adapter first-cut、local worker delivery receipt first-cut、Host-managed webhook external delivery first-cut、metadata/output/trusted connector JSON 多路径 evidence projection、focused live runtime smoke harness，并接通 Host Bridge -> `lime.agent.startTask` handoff。当前剩余主线不是 P18.5 owner handoff，也不是重复跑内容工厂按钮 smoke，而是 P18.7-E 真实 Connector OAuth / raw secret material delivery adapter、生产外部平台真实 delivery 与产品级 GUI 送达证明；外部 package / Lime 工作区 dirty 仍需要 owner 提交边界收口。

## P18.7 追加审计口径

2026-05-16 追加：本审计原本判定的是 P18 typed SDK gate，而不是“Lime 所有能力已完成抽象和后端接线”。用户明确要求继续完善到整体目标后，完成口径改为 [P18.7 Full Lime Capability Surface](./p18-7-full-lime-capability-surface.md)：全量 capability catalog、v0.6 reference 兼容、Host discovery、AgentRuntime resource 能力、Tool/Integration 能力和内容工厂真实闭环都要逐项有证据。当前 P18.7-A/B/C/D/F 已具备 first-cut 或产品闭环证据；P18.7-E 已具备 Host-managed fixture mutation/evidence proof、Cloud Overlay outbox adapter first-cut、local worker delivery receipt first-cut、Host-managed webhook external delivery first-cut、metadata/output/trusted connector JSON 多路径 evidence projection、focused live runtime smoke harness 与 Host Bridge handoff，但仍剩真实 Connector OAuth / raw secret material delivery adapter、生产外部平台真实 delivery 与产品级 GUI 送达证明。

### 2026-05-19 13:49 当前 prompt-to-artifact completion checklist

本段用于避免把“测试通过 / local webhook receipt / 内容工厂 green”误判为整体目标完成；只有所有 `missing / pending` 项关闭后，才能声明 `完成整体目标`。

| 交付项 / 明确要求 | 当前证据 | 审计结论 |
| --- | --- | --- |
| Agent App 在 Host iframe 内通过统一 `lime.*` capability surface 消费 Host 能力。 | `p18-7-full-lime-capability-surface.md`、`capability-sdk.md`、`capabilityCatalog.ts`、SDK / dispatcher / Host Bridge 回归。 | covered first-cut；不是每个后端 mutation 都已生产化。 |
| `manifestVersion: 0.5/0.6` reference compatibility 与 readiness cross-check。 | `parseManifest` / `normalizeManifest` / `referenceCliCrossCheck` 回归与 P18.7-B 文档。 | covered；accepted divergences 仍按文档退出。 |
| App / Lime 主 App / AgentRuntime / ToolRuntime owner boundary 清晰。 | `app-surface-runtime.md`、`agent_app_runtime_cmd` facade 拆分、Rust runtime / connector tests、`npm run test:contracts` 历史通过记录。 | covered；继续实现必须保持 facade + 子模块单责。 |
| P18.7-F 内容工厂五动作真实业务闭环。 | Yunwu final / metrics summary、MiniMax tail-cleanup summary、Agent Apps smoke summary、14:11 MiniMax delivery-confirm next-round summary。 | green with caveats；`sameIframeContext=false` 与 failedRequests 噪声不阻塞业务闭环，next-round confirm / review materialization 已由 14:11 证据补齐。 |
| next-round confirm / review materialization 稳定闭环。 | 14:11 `content-factory-review-confirm-next-round-minimax-delivery-confirm-20260519-codex-summary.json` 覆盖 `run-review` + `run-production-next-round`；第1轮复盘显示 `样本量240条 / 看完率32%`，第2轮内容达到 `20/20`。 | covered with caveats；`sameIframeContext=false`、`failedRequests=6`，但 action gates、workspace patch、Skill、usage/cost、no console errors 均 green。 |
| P18.7-E ToolRuntime connector owner binding / evidence projection。 | `cloud_overlay_outbox.rs`、`readiness.rs`、connector Rust tests、focused smoke replay / live local webhook evidence。 | covered first-cut；仅证明 Host-managed outbox/evidence/readiness boundary。 |
| webhook/local worker receipt 不能被升级为生产平台 delivery。 | `productionDelivery.proofLevel`、`productionPlatformDelivered=false`、`nextRequired=production_connector_delivery_adapter`，以及 connector smoke `--expect-production-platform-delivered`。 | covered guard；这是防伪完成，不是生产送达。 |
| 真实 Connector OAuth handshake。 | 当前无 Notion/Slack/Feishu/OAuth secret 来源，未观察到真实 OAuth handshake evidence。 | missing。 |
| raw secret material delivery adapter。 | 只有 Host-managed lease/readiness/红线断言，未实现或验证 raw secret material adapter。 | missing。 |
| Notion/Slack/Feishu 等生产平台真实 delivery。 | local webhook / remote webhook receipt 只属于 host-managed webhook proof。 | missing；不能用 2xx webhook 或 local worker receipt 替代。 |
| 产品级 GUI evidence 证明生产平台送达。 | 无生产平台送达截图 / receipt / GUI evidence。 | missing。 |
| 并行大写集 owner 收口。 | 当前未见 active `agent-apps-content-factory-flow`，但工作树仍存在多处并行修改与 v2 evidence。 | pending；需要 owner 最终收口验证。 |

实时判定：**整体目标尚未完成，不应调用 goal complete**。P18.7-F next-round confirm 已有新的 green 证据；下一刀应转向真实 OAuth / secret / production connector 凭证、P18.7-E production delivery adapter 与 GUI 送达证明，不能再把 local webhook 或内容工厂业务闭环当成生产连接器送达。

### 2026-05-17 20:49 当前状态完成审计

本段是面向“和隔壁进程一起完成整体目标”的实时完成审计，不替代下方历史 P18 审计。

| 明确交付要求 | 当前 artifact / evidence | 审计结论 |
| --- | --- | --- |
| 全量 `lime.*` capability surface 有单一事实源，并通过 SDK / Host profile 暴露给 Agent App。 | `docs/roadmap/agentapp/p18-7-full-lime-capability-surface.md`、`src/features/agent-app/sdk/capabilityCatalog.ts`、`capabilityDispatcher.test.ts`、`capabilityAdapters.test.ts`、`publicSdkSurface.test.ts`。 | P18.7-A/C first-cut 已有测试和文档证据；不等于后端所有 mutation 已实现。 |
| `manifestVersion: 0.6.0` / layered manifest / reference CLI cross-check 兼容。 | `referenceCliCrossCheck.test.ts`、`parseManifest.test.ts`、`normalizeManifest` / `checkReadiness` 相关回归。 | P18.7-B 已完成当前兼容口径；v0.6 新增 discovery / policy 深字段仍按 accepted divergences 退出。 |
| AgentRuntime resource projection：models / usage / skills / memory / context / tasks 来自 Host / AgentRuntime，不由 App 猜测。 | `capabilityDispatcher.test.ts`、`agentRuntimeCapabilityHost.test.ts`、`agentRuntimeProcess.test.ts`、`p18-7-parallel-validation.md`。 | P18.7-D first-cut 已完成；写入 / runtime enable / 自动 tool 注入仍明确不开放。 |
| Tool / Integration 受控执行 gate：App 只能发 intent，实际执行、policy、进度、evidence 属于 Lime Host / ToolRuntime。 | `src/features/agent-app/runtime/capabilityDispatcher.ts`、`src-tauri/src/commands/aster_agent_cmd/tool_runtime/agent_app_tool_execution.rs`、`src-tauri/src/commands/aster_agent_cmd/tool_runtime/connector_tools.rs`、`src-tauri/src/services/runtime_evidence_projection_service.rs`、`p18-7-e-toolruntime-execution-gate-plan.md`；2026-05-17 21:15 `cargo test --manifest-path "src-tauri/Cargo.toml" -p lime agent_app_connector --lib` 8 tests passed；2026-05-17 21:31 `npm test -- "src/features/agent-app/runtime/capabilityDispatcher.test.ts"` 18 tests passed、`npm run typecheck` 与 `npm run test:contracts` 通过；2026-05-17 21:55 `collects_structured_connector_evidence_refs` / `thread_read_should_project_tool_calls_for_profile_consumers` / `agent_app_connector` / `agent_app_runtime` 定向 Rust 测试与标准 `cargo check` 通过；2026-05-17 23:17 `thread_read_should_project_cloud_overlay_outbox_evidence_for_connector_tool_calls` 与 `test_agent_app_runtime_task_events_project_connector_outbox_evidence` 通过，覆盖 Cloud Overlay outbox evidence 进入 `threadRead` 与 Agent App task events；2026-05-18 00:00 `collects_evidence_refs_from_bounded_tool_output_metadata` 通过，并产出 read-only replay `.lime/qc/gui-evidence/agent-apps/p18-7-e-runtime-outbox-output-metadata-projection-20260518-summary.json`，覆盖 live ToolRuntime output bounded metadata block 内的 outbox evidence refs 可进入 `threadRead` evidence summary / Agent App task events；2026-05-18 00:08 `scripts/agent-app-connector-outbox-smoke.mjs` `node --check` 通过，并以 `--mode replay` 生成 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-replay-20260518-summary.json`，把该复核变成可重复 focused smoke；2026-05-18 00:13 继续增强 live 模式自动 provider/model 选择，并生成 replay 证据 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-replay-auto-provider-20260518-summary.json`；2026-05-18 00:22 跑通 live 证据 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-live-auto-provider-final-20260518-summary.json`，自动 provider/model 为 `deepseek/deepseek-v4-flash`，`toolCallCount=1`，同一个 outbox evidence ref 进入 tool call、thread summary 与 Agent App task events；2026-05-18 00:37 补 Host-managed secret delivery fact seam 并跑通 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-live-secret-delivery-20260518-summary.json`，显示 `adapterReadiness=host_managed_secret_delivery_adapter_ready`、`secretDeliveryStatus=ready`、`credentialMaterialExposed=false`、`tokenExposed=false`、`externalStatus=not_delivered`；2026-05-18 00:42 追加 secret-delivery assertion gate，`node --check "scripts/agent-app-connector-outbox-smoke.mjs"` 通过，replay `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-secret-delivery-assertions-replay-20260518-summary.json` 覆盖新增断言；2026-05-18 00:58 追加 Host-managed secret lease fact seam，Rust readiness 要求 `secretDelivery.binding/source/target/leaseRef` 与 material/token 不暴露同时成立，`cloud_overlay_outbox` 写入 `secret-lease://connector/...` 句柄，`agent_app_connector` 11 tests、标准 `cargo check` 与 `node --check` 通过；2026-05-18 01:00 Host Bridge 也会在已完成 `agent_app.connector_authorization` 后注入同一 `secretDelivery` lease fact，`capabilityDispatcher.test.ts` 18 tests 与 `npm run typecheck` 通过；2026-05-18 01:11 继续把同一 lease readiness 暴露到 `lime.connectors.getStatus/list`，授权 task 成功时返回 `status=authorized`、App-safe `authorizationRequest.secretDelivery` 与 App-safe `connectorRuntimeFacts.secretDelivery`，同一组 TS 回归与 typecheck 通过；2026-05-18 02:06 继续收紧 lease handle 可见性：TS Host Bridge 分出 public/internal request，Rust readiness 优先消费 `agent_app_tool_execution.internalRequest`，Cloud Overlay outbox 文件保留 internal `leaseRef`，但 ToolResult metadata/output、threadRead、Agent App task events 与 focused smoke summary 只暴露 `leaseObserved=true / leaseRefExposed=false / leaseHandleStatus=host_managed`，并新增 `secretDeliveryConcreteLeaseRefNotExposed` smoke assertion；`npm test -- "src/features/agent-app/runtime/capabilityDispatcher.test.ts"` 18 tests、`npm run typecheck`、`CARGO_TARGET_DIR=/tmp/lime-codex-target-p18e cargo test --manifest-path "src-tauri/Cargo.toml" -p lime agent_app_connector --lib` 11 tests、同 target `cargo check --manifest-path "src-tauri/Cargo.toml" -p lime --lib --message-format short`、`node --check` 与 `rustfmt --check` 均通过。 | Host first-cut、request envelope、AgentRuntime handoff、ToolRuntime owner binding、connector preview/readiness seam、connector facts envelope、Host-managed fixture mutation/evidence proof、Cloud Overlay outbox adapter first-cut、local worker delivery receipt first-cut、metadata/output/trusted connector JSON 多路径 evidence projection、focused live runtime smoke harness、Host Bridge fixture handoff，以及 structured connector evidence ref projection 已完成；真实 Connector OAuth / raw secret material delivery adapter 与外部平台真实 delivery 仍未完成。 |
| Connector secret 不能靠 App / 模型伪造授权状态。 | 2026-05-17 18:40 `agent_app_connector_preview` 5 tests passed；Rust seam 只有同时观测到 `capability=lime.connectors`、授权状态、`secretBinding=host_managed` 和 `tokenExposed=false` 才返回 `authorized_runtime_fact_observed`，generic `lime.tools.invoke` 伪造 connector facts 仍会落到 `adapter_not_configured`。2026-05-17 21:15 fixture mutation adapter 继续复用同一 seam，且测试断言无 Host-managed 授权 fact 时仍 `adapter_not_configured`。 | 本轮已加固并补 fixture mutation proof；仍不是 Cloud Overlay / 外部 OAuth mutation adapter。 |
| 内容工厂业务 App 必须在页面内完成真实 AgentRuntime 闭环，并展示运行现场和 workspace patch。 | 历史 focused completion gate 已覆盖多个按钮；2026-05-17 20:49 最新证据 `.lime/qc/gui-evidence/agent-apps/content-factory-run-scenarios-local-current-after-watch-fix-20260517-summary.json` 断言 `allActionsCompleted / allActionsUsedExpectedSkills / allActionsHaveModelUsageCost / allActionsHaveWorkspacePatch / allActionsFullRuntimeReady / processVisibleAfterEachAction / noHostFallback / consoleErrorCount=0` 均通过，direct runtime `completed / artifactCount=1 / toolCallCount=3 / hasWorkspacePatch=true / modelRunStatus=success`，页面 materialization 为 `content_factory_page`，可见 `场景概览120 个 / 场景数量120目标 120+`；只读 `agent_app_runtime_get_task` 复核同一 task/session 的 `incidents=[]`。 | 内容工厂 run-scenarios 连续 GUI materialization 已复绿；该结论只覆盖 P18.7-F 产品闭环，不代表 P18.7-E 真实 Connector OAuth/secret execution adapter 或真实 ToolRuntime mutation smoke 已完成。 |
| 最终 GUI / product smoke 可交付。 | DevBridge health 在 20:49 passing evidence 生成时可用；20:49 `content-factory-run-scenarios-local-current-after-watch-fix-20260517-summary.json` 与截图证明 run-scenarios 页面物化；21:38/21:59 `run-strategy` failure 已归类为并行运行面 / bridge delivery interruption；22:13 新增 `.lime/qc/gui-evidence/agent-apps/content-factory-run-strategy-local-current-after-cross-project-fix-20260517-summary.json` 与截图，断言 `sameIframeContext / actionCount / allActionsCompleted / allActionsUsedExpectedSkills / allActionsHaveModelUsageCost / allActionsHaveWorkspacePatch / allActionsFullRuntimeReady / processVisibleAfterEachAction / noHostFallback / consoleErrorCount=0` 全通过，direct runtime 为 `taskStatus=completed / profileStatus=completed / artifactCount=1 / toolCallCount=2 / hasWorkspacePatch=true / selectedModel=deepseek-v4-flash`，required Skills 为 `article-writer / content-reviewer`；22:19 `.lime/qc/gui-evidence/agent-apps/content-factory-run-review-local-current-20260517-summary.json` 也全断言通过，direct runtime 为 `taskStatus=completed / profileStatus=completed / artifactCount=1 / toolCallCount=1 / hasWorkspacePatch=true`，required Skill 为 `content-reviewer`。 | P18.7-F 主通过证据更新为 20:49 run-scenarios + 22:13 run-strategy + 22:19 run-review；22:33 full-flow 五个动作完成态也通过，但仍有 1 条 console error 和 aborted requests 噪声，不能当作完全干净 GUI smoke。该结论仍不能替代 P18.7-E product-level non-fixture ToolRuntime mutation smoke。 |

实时判定：**整体目标尚未完成，不应调用 goal complete**。P18.7-F 内容工厂连续 GUI materialization 已由 20:49 run-scenarios、22:13 run-strategy、22:19 run-review 与 22:33 五动作 full-flow action evidence 复绿；P18.7-E 已有 Host-managed fixture connector mutation/evidence proof、Cloud Overlay outbox adapter first-cut、local worker delivery receipt first-cut、metadata/output/trusted connector JSON 多路径 evidence projection、focused live runtime smoke harness 与 Host Bridge handoff，但下一刀仍要转向真实 Connector OAuth / raw secret material delivery adapter、外部平台真实 delivery 与产品级 GUI 送达证明，不能把 readiness seam、preview `not_available`、fixture proof 或 local worker receipt 当作真实外部平台 mutation 完成。

### 2026-05-17 21:47 并行运行面更新

- 只读进程盘点显示隔壁仍在跑 `scripts/agent-apps-content-factory-flow.mjs --actions run-strategy --prefix content-factory-run-strategy-local-current-after-delivery-route-fix-20260517` 和 `npm run verify:gui-smoke --reuse-running`；本进程不启动新的 GUI smoke、不 kill、不改运行中脚本。
- 21:38 失败证据 `content-factory-run-strategy-retry-current-20260517-failure.json` 的错误点是 DevBridge health fetch：`agent_app_inspect_local_package` 在浏览器模式下无法访问 `http://127.0.0.1:3030/health`，页面尚未进入内容工厂 runtime iframe，因此该失败只能证明当时 bridge 不稳定，不能证明 P18.7-F 业务闭环失败。
- 21:47 复核 `curl http://127.0.0.1:3030/health` 返回 DevBridge `status=ok`，`lsof` 显示 `lime` 正在监听 `127.0.0.1:3030`；后续是否形成新 passing GUI summary 仍以隔壁进程产物为准。

### 2026-05-17 21:59 run-strategy 并行失败复核

- 隔壁 `content-factory-run-strategy-local-current-after-delivery-route-fix-20260517` 已自然结束并产出 failure：`.lime/qc/gui-evidence/agent-apps/content-factory-run-strategy-local-current-after-delivery-route-fix-20260517-failure.json`。
- 该 failure 不是模型 / AgentRuntime 执行失败：direct record 显示 `taskStatus=completed`、`profileStatus=completed`、`artifactCount=1`、`toolCallCount=2`、`hasWorkspacePatch=true`、`selectedModel=deepseek-v4-flash`、`evidenceReady=true`、`invokedSkillNames=["article-writer","content-reviewer"]`。
- 该 failure 的实际断点是页面物化失败：`hostRecord=null`，iframe 仍显示旧 overview / 项目状态，且 failed requests 包含多次 `127.0.0.1:3030/invoke`、`127.0.0.1:3030/events` 和 `127.0.0.1:1420/` 的 aborted / connection refused；同时间段另有新的 `tauri:dev:headless`、`pnpm run tauri dev` 和 `smoke:at-command-registry` 活跃，说明 GUI 运行面被并行重启 / 抢占。
- 21:59 复核 `http://127.0.0.1:3030/health` 与 `http://127.0.0.1:1420/` 均恢复可达；该失败应按并行运行面不稳定 / Host record delivery interruption 处理，不推翻 20:49 P18.7-F run-scenarios passing evidence，但也不能作为新的 run-strategy 产品通过证据。

### 2026-05-17 22:05 Agent Apps smoke 复核

- 新增 evidence：`.lime/qc/gui-evidence/agent-apps/agent-apps-smoke-summary.json`、`agent-apps-smoke.png`、`agent-apps-smoke-flag-off.png`。
- 断言均为 true：formal page / installed app / cloud install review / disabled launch blocked / runtime surface / content factory iframe loaded / Host profile visible / cleanup dry-run / flag-off nav guard。
- `contentFactoryActionE2e=null`，因此该 smoke 只覆盖 Agent Apps 壳、安装/启动/清理和 iframe profile，可作为 P18.7-C/F 壳层辅证，不能替代 run-strategy completion 或 P18.7-E product-level non-fixture ToolRuntime mutation smoke。
- summary 中仍记录一次 `agent_app_fetch_cloud_package` console error；由于 cloud install review 断言通过且 failedRequests 为空，本轮只把它登记为运行面噪声，不升级为 P18.7-F 主阻塞。

### 2026-05-17 22:13 run-strategy cross-project fix 复核

- 隔壁 `content-factory-run-strategy-local-current-after-cross-project-fix-20260517` 已自然结束并产出 passing summary：`.lime/qc/gui-evidence/agent-apps/content-factory-run-strategy-local-current-after-cross-project-fix-20260517-summary.json`，截图为同名前缀 `.png`。
- 断言全部通过：`sameIframeContext / actionCount / allActionsCompleted / allActionsUsedExpectedSkills / allActionsHaveModelUsageCost / allActionsHaveWorkspacePatch / allActionsFullRuntimeReady / processVisibleAfterEachAction / noHostFallback` 均为 true，`consoleErrorCount=0`。
- direct runtime snapshot：task `agent-app-task-c0fe2a28-b0a0-4b34-937f-4dc48a2af979` / session `agent-app-runtime-c447cdca-ae8f-469a-84a7-4846775e56d4` 为 `taskStatus=completed / profileStatus=completed / artifactCount=1 / toolCallCount=2 / hasWorkspacePatch=true / evidenceReady=true`，模型为 `deepseek-v4-flash`，required Skills 为 `article-writer / content-reviewer`。
- summary 中仍有若干 `net::ERR_ABORTED` failedRequests，主要来自样例 load、DevBridge invoke/events 在流程收尾时被中止；由于 completion 和页面/过程断言均已通过、console error 为 0，本审计不把它升级为业务失败。
- 该证据修复 21:59 run-strategy 页面物化失败的缺口；但它仍只证明 P18.7-F 产品闭环，不证明 P18.7-E 真实 Connector OAuth / raw secret material delivery adapter 或 non-fixture ToolRuntime mutation 已完成。

### 2026-05-17 22:19 run-review current flow 复核

- 隔壁 `content-factory-run-review-local-current-20260517` 已自然结束并产出 passing summary：`.lime/qc/gui-evidence/agent-apps/content-factory-run-review-local-current-20260517-summary.json`，截图为同名前缀 `.png`。
- 断言全部通过：`sameIframeContext / actionCount / allActionsCompleted / allActionsUsedExpectedSkills / allActionsHaveModelUsageCost / allActionsHaveWorkspacePatch / allActionsFullRuntimeReady / processVisibleAfterEachAction / noHostFallback` 均为 true，`consoleErrorCount=0`。
- direct runtime snapshot：task `agent-app-task-f2658632-1bd2-4f3b-8add-9c8ccdcc92f3` / session `agent-app-runtime-6056a810-dd75-4bfb-bf4f-18df18829879` 为 `taskStatus=completed / profileStatus=completed / artifactCount=1 / toolCallCount=1 / hasWorkspacePatch=true / evidenceReady=true`，模型为 `deepseek-v4-flash`，required Skill 为 `content-reviewer`。
- summary 仍有少量收尾阶段 `net::ERR_ABORTED` failedRequests；由于 completion、过程、no Host fallback 和 console error gate 均通过，本审计只登记为运行面噪声。该证据进一步增强 P18.7-F，但不改变 P18.7-E 真实 connector 缺口。

### 2026-05-17 22:33 content-factory full-flow 复核

- 隔壁 `content-factory-full-flow-after-runtime-package-cross-project-fix-20260517` 已自然结束并产出 summary：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-after-runtime-package-cross-project-fix-20260517-summary.json`，截图为同名前缀 `.png`。
- 覆盖动作：`run-scenarios / run-production / run-scripts / run-strategy / run-review`；核心 action gates 均通过：`sameIframeContext / actionCount / allActionsCompleted / allActionsUsedExpectedSkills / allActionsHaveModelUsageCost / allActionsHaveWorkspacePatch / allActionsFullRuntimeReady / processVisibleAfterEachAction / noHostFallback`。
- direct runtime snapshots 均为 completed：五个 task 全部 `taskStatus=completed / profileStatus=completed / artifactCount=1 / hasWorkspacePatch=true / evidenceReady=true`；tool call 数分别为 `3 / 2 / 2 / 2 / 1`，required Skills 覆盖 `knowledge-builder`、`article-writer`、`content-reviewer`。
- 注意：summary 仍记录 `consoleErrorCount=1`，console error 为页面侧 `agent_app_list_installed` bridge health check 在启动/切换期间失败；failedRequests 还有 17 条 `net::ERR_ABORTED`，主要是 health、sample load、invoke/events 收尾中止。因此该 full-flow 可作为五动作业务 action gate 通过证据，但不能作为“完全无噪声 GUI smoke”证据。
- 该证据进一步增强 P18.7-F；整体目标仍未完成，因为 P18.7-E 真实 Connector OAuth / secret adapter 与 non-fixture ToolRuntime mutation smoke 仍缺。

## 一句话结论

P18 的 Lime-side typed Capability SDK gate 已具备当前可验证证据，并新增了可复用的 Host Bridge SDK client，完整 `verify:local` 也已绿灯；外部 `content-factory-app` 的 package-side source seam 已出现 `createLimeHostBridgeCapabilityInvoker + createLimeCoreCapabilityAdapters`，私有 bridge marker 已清零，真实外部 package verify 已通过，真实 `dist/*` 也已同步 SDK facade。P18 仍不自动等于“可提交”，因为外部 package 与 Lime 工作区仍 dirty，需 owner 明确接收写集。

## 当前绿灯 / 阻塞摘要

| 分类                                     | 最新证据                                                                                                                                                                                                                                                                                                                                                      | 判定                                                                                                             |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| Lime-side Agent App feature island       | 2026-05-16 10:17 `npm test -- src/features/agent-app`：36 files / 178 tests passed；2026-05-16 10:41 复跑 SDK seam 最小集 4 files / 11 tests passed。                                                                                                                                                                                                         | 通过当前定向测试；包含 SDK-only public surface、public SDK export regression 与内容工厂 SDK regression。         |
| P18 SDK / Host Bridge                    | 2026-05-16 07:14 P18 SDK / Host Bridge 定向测试：9 files / 43 tests passed。                                                                                                                                                                                                                                                                                  | 通过当前定向测试。                                                                                               |
| 静态与契约                               | 2026-05-16 09:07 `verify:app-version` 已重跑通过；10:20 `lint` 通过；10:15 `typecheck` 通过；10:21 `test:contracts` 通过。                                                                                                                                                                                                                                    | 通过当前本地非 GUI 验证。                                                                                        |
| 外部 `content-factory-app` 只读验证      | 2026-05-16 10:41 低优先级复跑 `npm test`：46 tests passed；`validate:app` 为 `passed`，manifest hash `sha256:6ec3fed5f163739bcf0fd2b845c51a8e10d28aa856e8c6f90259fdab9edd1e48`；`readiness:app` 为 `needs-setup`。                                                                                                                                            | 只读验证通过；不能替代会重建 `dist/*` 的 package verify。                                                        |
| P18.5.3 package-side SDK facade / verify | 2026-05-16 10:55 真实外部 package `npm run verify` 通过：build、46 tests、validate、readiness 均完成；随后 handoff gate 输出 `status=needs_handoff`、dirty `tracked=36 / untracked=5`、`hostBridgePrivate=none`、`uiTestPrivate=none`、SDK marker 命中 `2+2`、`distArtifacts=diff:0,missing:0,extra:0,total:0`。                                              | 功能和产物验收通过；仍需 owner handoff，因为 package worktree 仍 dirty 且 build/verify/e2e 脚本天然会重写 dist。 |
| P18.7-A / P18.7-B capability surface / v0.6 reference | 2026-05-16 当前会话新增 `capabilityCatalog` 事实源、catalog/profile/adapter 派生测试；`normalizeManifest` 支持 `0.6.x`，`checkReadiness` 支持 `"0.6"`，`referenceCliCrossCheck.test.ts` 兼容 capability list、layered manifest、`agentRuntime` 和 v0.6 schema divergences；定向测试、typecheck、contracts、legacy report 均通过。 | P18.7-A/B 通过；不代表 Host discovery、后端 resource 接线或内容工厂真实闭环完成。 |
| 完整 `verify:local`                      | 2026-05-16 07:33 低优先级运行 `nice -n 10 npm run verify:local` 端到端通过，覆盖 app version、lint、typecheck、Vitest smart 58 batches、`test:contracts`、`cargo test --manifest-path src-tauri/Cargo.toml` 与全套 GUI smoke；`smoke:claw-chat-ready-streaming` 同次重跑 summary 为 `verdict=pass / error=null / consoleErrorCount=0 / failureRuntime=null`。 | 全量本地验证通过；不再是 P18 当前阻塞项。                                                                        |

## 目标重述

P18 要证明 Agent App 可以只依赖 Lime 公开 SDK facade 和 Host Bridge v1 完成 App 内业务闭环：

1. App 作者调用 typed `@lime/app-sdk` facade，而不是手写私有 `postMessage` 协议或 import Lime internal modules。
2. Host Bridge 只做传输，真实能力必须经过 readiness、permission、policy、provenance 和 stable error gate。
3. `lime.agent` 能在 App 内启动、读取、取消、重试、提交 Host response 和列出 App-scoped task，不强制回跳通用 Chat。
4. 内容工厂主链必须通过 `lime.agent / lime.storage / lime.artifacts / lime.evidence` 完成 task、write-back、artifact 和 evidence。
5. P18 不执行 raw worker、不访问外部网络 / 文件系统 / secret value，不扩 marketplace、Cloud 管理台或垂直后端。
6. 外部 `content-factory-app` 最终要从手写 bridge wrapper 收敛到标准 SDK facade，并通过 package verify。

## Prompt-to-artifact checklist

| 要求                                                     | 当前证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 审计判定                                                                                                                                     |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------- | --------- | ---------- | --------------------------------- | --- | -------------- | -------------- |
| P18.1 Typed SDK facade、stable error、mock host。        | `src/features/agent-app/sdk/capabilityContract.ts`、`src/features/agent-app/sdk/capabilityErrors.ts`、`src/features/agent-app/sdk/MockCapabilityHost.ts`、`src/features/agent-app/sdk/capabilityContract.test.ts`、`src/features/agent-app/sdk/MockCapabilityHost.test.ts`。                                                                                                                                                                                                                                      | 通过当前 Lime-side 契约验证。                                                                                                                |
| P18.2 Host Bridge typed router / stable error response。 | `src/features/agent-app/runtime/hostBridge.ts`、`src/features/agent-app/runtime/capabilityDispatcher.ts`、`hostBridge.test.ts`、`capabilityDispatcher.test.ts`。                                                                                                                                                                                                                                                                                                                                                  | 通过当前定向测试；未进入 GUI smoke。                                                                                                         |
| P18.3 Core capability adapters。                         | `src/features/agent-app/sdk/capabilityAdapters.ts` 覆盖 `lime.ui / storage / artifacts / evidence / knowledge / tools / agent` facade；`capabilityAdapters.test.ts` 覆盖 adapter call envelope。                                                                                                                                                                                                                                                                                                                  | 通过当前定向测试。                                                                                                                           |
| P18.4 App-scoped Agent task SDK facade。                 | `LimeAgentCapabilityAdapter` 暴露 `startTask / streamTask / getTask / cancelTask / retryTask / submitHostResponse / listTasks`；`agentRuntimeCapabilityHost.test.ts` 覆盖 runtime host 接线。                                                                                                                                                                                                                                                                                                                     | 通过当前定向测试；后端 push subscribe 仍归 AgentRuntime owner。                                                                              |
| P18.4-H AgentRuntime handoff gate。                      | `docs/roadmap/agentapp/p18-4-h-agentruntime-handoff-gate.md` 记录 handoff 判定与 owner 缺口。                                                                                                                                                                                                                                                                                                                                                                                                                     | 文档 gate 已有；不由 P18 复制 runtime read model。                                                                                           |
| P18.5.1 Lime-side 内容工厂 SDK regression。              | `src/features/agent-app/sdk/contentFactorySdkRegression.test.ts` 断言内容工厂主链调用 `lime.agent.startTask / streamTask / submitHostResponse / storage.set / artifacts.create / evidence.record`，且 2026-05-16 10:08 已改为只从 `src/features/agent-app/sdk/index.ts` 的 SDK-only public surface 导入类型与 helper。                                                                                                                                                                                            | 通过当前定向测试；证明 Lime-side 内容工厂回归不依赖 SDK 内部深文件。                                                                         |
| P18.5.2 外部 package read-only tests。                   | `/Users/coso/Documents/dev/ai/limecloud/content-factory-app: npm test` 当前通过；2026-05-16 08:19 owner handoff gate 复核为 46 tests passed。                                                                                                                                                                                                                                                                                                                                                                     | 通过只读测试；不能替代 package-side SDK facade 或 verify。                                                                                   |
| P18.5.2 外部 package 标准只读校验。                      | 2026-05-16 07:11 与 08:42 低优先级运行 `/Users/coso/Documents/dev/ai/limecloud/content-factory-app: npm run validate:app && npm run readiness:app`；`validate:app` 返回 `ok=true / status=passed`，manifest hash 为 `sha256:6ec3fed5f163739bcf0fd2b845c51a8e10d28aa856e8c6f90259fdab9edd1e48`；`readiness:app` 返回 `ok=true / status=needs-setup`。                                                                                                                                                              | 通过只读标准校验；`needs-setup` 符合宿主需补齐 required 依赖的状态，仍不能替代会重写 `dist/*` 的 package verify。                            |
| P18.5-S Lime-side Host Bridge SDK client。               | `src/features/agent-app/sdk/hostBridgeClient.ts` 提供 `createLimeHostBridgeCapabilityInvoker`，可把标准 `createLimeCoreCapabilityAdapters` 调用转成 Host Bridge v1 `capability:invoke` envelope，并承接 ready / snapshot / theme / visibility / toast / navigate / openExternal / download / `capability:subscribe / unsubscribe / event`；`hostBridgeClient.test.ts` 覆盖 Host action、trusted origin、stable error、timeout cleanup、subscription 分发和内容工厂 task / write-back / artifact / evidence 主链。 | 通过当前定向测试；为外部 package 收敛提供目标，但不等于已改外部 package。                                                                    |
| P18.5-S SDK-only public surface。                        | `src/features/agent-app/sdk/index.ts` 只导出 capability facade、stable error、Host Bridge client、mock host 和 App package 需要的 task / storage / artifact / evidence 类型；`src/features/agent-app/sdk/publicSdkSurface.test.ts` 固定该窄导出清单，并从运行时 namespace 与源码 export 来源两侧断言不导出 UI、installer、repository、runtime host、adapter、schema 或 dispatcher。                                                                                                                               | 2026-05-16 10:08 定向测试通过，10:15 typecheck 通过；降低后续 `@lime/app-sdk` / package-local shim 误导出内部实现的风险。                    |
| P18.5-S public SDK export seam。                         | `src/features/agent-app/index.ts` 已从 feature public entry 导出 `createLimeCoreCapabilityAdapters`、`createLimeHostBridgeCapabilityInvoker`、`LIME_AGENT_APP_BRIDGE_PROTOCOL` 与 `LIME_AGENT_APP_BRIDGE_VERSION`；`src/features/agent-app/index.test.ts` 固定该 public export contract。                                                                                                                                                                                                                         | 通过 2026-05-16 07:54 定向测试；防止外部 package 后续只能依赖 Lime 内部深路径。                                                              |
| Host Bridge SDK client 文档化。                          | `docs/roadmap/agentapp/capability-sdk.md` 增加 Host Bridge SDK Client 章节，记录 package-side App 如何用 `createLimeHostBridgeCapabilityInvoker + createLimeCoreCapabilityAdapters`，以及 P18.5.3 迁移规则。                                                                                                                                                                                                                                                                                                      | 通过文档 diff check；仍不替代外部 package 改造。                                                                                             |
| P18.5.3 外部 package 迁移计划。                          | `docs/roadmap/agentapp/p18-5-3-package-sdk-migration-plan.md` 固定外部 package 当前只读事实、推荐写集、迁移步骤、验收清单和回滚点。                                                                                                                                                                                                                                                                                                                                                                               | 通过文档 diff check；等待外部 package owner 稳定后执行。                                                                                     |
| P18.5.3 owner handoff 单页。                             | `docs/roadmap/agentapp/p18-5-3-owner-handoff.md` 浓缩接管前命令、最小写集、禁止项、迁移目标、验证顺序和完成条件。                                                                                                                                                                                                                                                                                                                                                                                                 | 给外部 package owner / 后续执行者使用；不替代 handoff 确认，也不代表外部 package 已迁移。                                                    |
| P18.5.3 最新复核。                                       | 2026-05-16 10:55 真实外部 package `npm run verify` 通过；handoff gate 显示 `hostBridgePrivate=none`、`uiTestPrivate=none`、SDK marker 命中 2+2、`distArtifacts=0`、blockers 为 `none`。                                                                                                                                                                                                                                                                                                                           | P18.5.3 SDK facade / package verify / dist 同步已完成；当前只剩 owner handoff。                                                              |
| P18.5.3 package verify 风险。                            | `docs/roadmap/agentapp/p18-5-3-package-sdk-migration-plan.md` 已记录 `npm run verify` 会先执行 `npm run build` 并重建 `dist/*`；2026-05-16 10:55 用户确认后已在真实 package 执行一次 verify。                                                                                                                                                                                                                                                                                                                     | 当前 verify 已完成；后续若再次运行 build / verify / e2e，仍需 owner 接受 dist 重建。                                                         |
| P18.5.3 外部 UI tests 迁移靶点。                         | `docs/roadmap/agentapp/p18-5-3-package-sdk-migration-plan.md` 已记录 `tests/ui.test.mjs` 中需要调整的 Host Bridge 断言：保留业务闭环、manifest kind 和 capability call sequence，删除鼓励手写 `postMessage` / message type 的结构性断言。                                                                                                                                                                                                                                                                         | 后续 package-side 迁移时可只改测试 mock / facade 断言，不删除内容工厂业务行为断言。                                                          |
| P18.5.3 package-side facade 兼容导出。                   | `docs/roadmap/agentapp/p18-5-3-package-sdk-migration-plan.md` 已记录外部 `src/ui/app.js` 只直接 import `initHostBridge / notifyHost / runHostAgentTask / syncHostConfirmation / writeHostTaskResult`。                                                                                                                                                                                                                                                                                                            | 后续迁移优先保持这 5 个业务侧导出稳定，避免同时改 UI 主流程。                                                                                |
| P18.5.3 package-side 迁移 dry-run 设计。                 | `docs/roadmap/agentapp/p18-5-3-package-sdk-migration-plan.md` 已基于当前外部 `src/ui/host-bridge.js` 只读结构，记录最小 diff 顺序：SDK runtime factory、pure helper 保留、私有 transport owner 删除 / 下沉、业务 helper 只调用 `lime.agent / storage / artifacts / evidence` facade。                                                                                                                                                                                                                             | 等 handoff 后可按设计实施；当前仍未应用外部 package。                                                                                        |
| P18.5.3 package-local SDK shim 退出条件。                | 外部 `content-factory-app` 当前 `dependencies / devDependencies` 为空；`p18-5-3-package-sdk-migration-plan.md` 已把 `src/ui/lime-app-sdk.js` 或等价 shim 列为仅在正式 `@lime/app-sdk` 不可安装时的临时写集。                                                                                                                                                                                                                                                                                                      | 禁止 import Lime repo 内部路径；shim 只能暴露 SDK 等价最小接口，后续发布正式 SDK 后退出。                                                    |
| 正式 `@lime/app-sdk` package 发布状态。                  | 2026-05-16 08:37 只读复核：Lime repo 根 `package.json` 仍为 `"private": true`；`packages/` 下只有 `packages/lime-cli-npm`；未发现独立 `@lime/app-sdk` / `lime-app-sdk` package。                                                                                                                                                                                                                                                                                                                                  | 不把正式 SDK package 发布当作 P18.5.3 已完成证据；外部 App 仍不得 import Lime internal path，必要时只能使用带退出条件的 package-local shim。 |
| P18.5.3 package-side SDK facade / verify。               | 2026-05-16 10:55 真实外部 package `npm run verify` 已通过，且 handoff gate 确认 `distArtifacts=0`。                                                                                                                                                                                                                                                                                                                                                                                                               | package-side SDK facade / verify 已完成；剩余为 owner handoff / git 写集收口，不是功能 blocker。                                             |
| P18.6 Raw Worker 前 Gate。                               | `workflowRuntimeHost.test.ts`、`featureFlag.test.ts`、`runtimePackageLoader.test.ts`、`entryRuntimeGuard.test.ts` 当前通过 25 tests；2026-05-16 08:33 feature island 越界扫描无输出。                                                                                                                                                                                                                                                                                                                             | 通过当前 gate；raw worker 后移 P19。                                                                                                         |
| 不绕过 SDK / 不复活旧 SceneApp。                         | `rg -n "SceneApp                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | contentEngineering                                                                                                                           | sceneapp\_ | safeInvoke | invoke\\( | new Worker | Worker\\(" src/features/agent-app |     | true` 无输出。 | 通过当前扫描。 |
| 不抢隔壁运行任务。                                       | 本审计只读外部 package；不改 `src-tauri/*`、`src/lib/api/agentAppRuntime.ts`、`agentRuntimeCapabilityHost*` 实现逻辑、GUI smoke 脚本或外部 package。                                                                                                                                                                                                                                                                                                                                                              | 符合当前协作边界。                                                                                                                           |

## 本轮复核命令

```bash
nice -n 10 npm test -- src/features/agent-app/sdk/contentFactorySdkRegression.test.ts src/features/agent-app/sdk/capabilityAdapters.test.ts src/features/agent-app/sdk/capabilityContract.test.ts src/features/agent-app/sdk/MockCapabilityHost.test.ts src/features/agent-app/runtime/hostBridge.test.ts src/features/agent-app/runtime/capabilityDispatcher.test.ts src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts src/features/agent-app/ui/AgentAppRuntimePage.test.tsx
nice -n 10 npm test -- src/features/agent-app/sdk/hostBridgeClient.test.ts src/features/agent-app/sdk/capabilityAdapters.test.ts src/features/agent-app/sdk/contentFactorySdkRegression.test.ts
nice -n 10 npm test -- src/features/agent-app/sdk/hostBridgeClient.test.ts src/features/agent-app/sdk/contentFactorySdkRegression.test.ts src/features/agent-app/sdk/capabilityAdapters.test.ts src/features/agent-app/sdk/capabilityContract.test.ts src/features/agent-app/sdk/MockCapabilityHost.test.ts src/features/agent-app/runtime/hostBridge.test.ts src/features/agent-app/runtime/capabilityDispatcher.test.ts src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts src/features/agent-app/ui/AgentAppRuntimePage.test.tsx
nice -n 10 npm test -- src/features/agent-app/sdk/hostBridgeClient.test.ts src/features/agent-app/sdk/capabilityContract.test.ts src/features/agent-app/sdk/capabilityAdapters.test.ts src/features/agent-app/sdk/contentFactorySdkRegression.test.ts
nice -n 10 npm test -- src/features/agent-app/index.test.ts
nice -n 10 npm test -- src/features/agent-app/sdk/hostBridgeClient.test.ts src/features/agent-app/sdk/contentFactorySdkRegression.test.ts src/features/agent-app/index.test.ts
nice -n 10 npm test -- src/features/agent-app/ui/AgentAppsPage.test.tsx src/features/agent-app/ui/AgentAppManagerPanel.test.tsx src/features/agent-app/ui/AgentAppLabPage.test.tsx
nice -n 10 npm test -- src/features/agent-app
nice -n 10 npm run verify:app-version
nice -n 10 npm run lint
nice -n 10 npm run typecheck -- --pretty false
nice -n 10 npm run test:contracts
nice -n 10 npm test -- src/features/agent-app/runtime/workflowRuntimeHost.test.ts src/features/agent-app/featureFlag.test.ts src/features/agent-app/runtime/runtimePackageLoader.test.ts src/features/agent-app/runtime/entryRuntimeGuard.test.ts
git diff --check -- src/features/agent-app/sdk src/features/agent-app/runtime/hostBridge.ts src/features/agent-app/index.ts src/features/agent-app/index.test.ts docs/roadmap/agentapp
rg -n "SceneApp|contentEngineering|sceneapp_|safeInvoke|invoke\\(|new Worker|Worker\\(" src/features/agent-app || true
cd /Users/coso/Documents/dev/ai/limecloud/content-factory-app && nice -n 10 npm test
nice -n 10 npm run verify:local
nice -n 10 npm run harness:doc-freshness
```

结果：

- Lime SDK / Host Bridge 定向测试：新增 SDK client 前 8 files / 37 tests passed；新增后 9 files / 43 tests passed。
- 2026-05-16 06:15 与 07:14 低优先级重跑 P18 SDK / Host Bridge 定向测试：9 files / 43 tests passed。
- 2026-05-16 07:03 低优先级重跑 Lime-side P18 SDK 最小 contract 验证：`hostBridgeClient.test.ts`、`capabilityContract.test.ts`、`capabilityAdapters.test.ts`、`contentFactorySdkRegression.test.ts` 4 files / 13 tests passed。
- Agent Apps 入口 UI 定向测试：2026-05-16 07:18 低优先级运行 `AgentAppsPage.test.tsx`、`AgentAppManagerPanel.test.tsx`、`AgentAppLabPage.test.tsx`，3 files / 24 tests passed。
- Agent App feature island 全量定向测试：2026-05-16 07:59 低优先级运行 `npm test -- src/features/agent-app`，35 files / 169 tests passed；2026-05-16 09:09 复跑，35 files / 173 tests passed；2026-05-16 10:17 覆盖 SDK-only public surface 收口后复跑，36 files / 178 tests passed。
- Host Bridge SDK client 定向测试：1 file / 6 tests passed。
- Agent App public SDK export test：2026-05-16 07:54 低优先级运行 `npm test -- src/features/agent-app/index.test.ts`，1 file / 1 test passed。
- SDK-only public surface test：2026-05-16 10:08 低优先级运行 `npm test -- src/features/agent-app/sdk/publicSdkSurface.test.ts src/features/agent-app/sdk/hostBridgeClient.test.ts src/features/agent-app/sdk/contentFactorySdkRegression.test.ts src/features/agent-app/index.test.ts`，4 files / 11 tests passed；同次证明 `contentFactorySdkRegression.test.ts` 已从 SDK-only public surface 导入。
- 2026-05-16 08:19 当前会话低优先级复跑 SDK seam 最小集：`hostBridgeClient.test.ts`、`contentFactorySdkRegression.test.ts`、`index.test.ts`，3 files / 8 tests passed。
- App version：2026-05-16 07:15 与 08:09 低优先级运行 `npm run verify:app-version` 均通过，版本一致性为 `1.40.0`。
- Lime lint：2026-05-16 07:13、08:04 与 10:20 低优先级运行 `npm run lint` 均通过，10:20 覆盖 SDK-only public surface 与内容工厂 SDK regression 收口后的 `src` lint 面。
- Lime typecheck：通过；2026-05-16 06:16、07:06、08:03、08:24、09:04、09:52 与 10:15 低优先级重跑 `npm run typecheck -- --pretty false` 仍通过，10:15 覆盖内容工厂回归改从 SDK-only public surface 导入后的类型面。
- Lime command / harness / modality contracts：通过；2026-05-16 06:17、07:09、08:06、08:24、09:04 与 10:21 低优先级重跑 `npm run test:contracts` 仍通过，10:21 覆盖 SDK-only public surface 与 handoff gate 脚本新增后的契约面。
- P18.6 raw worker 前 gate：4 files / 25 tests passed；2026-05-16 06:19 与 07:08 低优先级重跑仍通过。
- `git diff --check`：通过；2026-05-16 06:20、06:45、07:12 与 08:08 重跑；08:08 命令已覆盖 `src/features/agent-app/index.test.ts`：`git diff --check -- src/features/agent-app/sdk src/features/agent-app/runtime/hostBridge.ts src/features/agent-app/index.ts src/features/agent-app/index.test.ts docs/roadmap/agentapp`。
- feature island 越界扫描：无输出；2026-05-16 06:19、07:04 与 08:33 重跑 `rg -n "SceneApp|contentEngineering|sceneapp_|safeInvoke|invoke\\(|new Worker|Worker\\(" src/features/agent-app || true` 均无输出。
- 外部 `content-factory-app` 只读测试：2026-05-16 07:50 与 08:19 按 owner handoff gate 低优先级重跑 `/Users/coso/Documents/dev/ai/limecloud/content-factory-app: npm test` 均为 46 tests passed；该只读结果不替代 package-side SDK facade / verify。
- 完整 `verify:local`：2026-05-16 07:33 低优先级运行 `nice -n 10 npm run verify:local` 端到端通过；本次通过 app version、lint、typecheck、Vitest smart 58 batches、`test:contracts`、`cargo test --manifest-path src-tauri/Cargo.toml`、workspace-ready、browser-runtime、site-adapters、agent-service-skill-entry、agent-runtime-tool-surface、agent-runtime-tool-surface-page、at-command-registry、agent-apps、claw-chat-ready-streaming、knowledge-gui 与 design-canvas GUI smoke，最终输出 `[verify:gui-smoke] 通过` 与 `[local-ci] 本地校验完成。`
- Doc freshness：2026-05-16 08:51 低优先级运行 `npm run harness:doc-freshness`，monitored docs 10 / existing docs 10 / issues 0 / broken markdown links 0 / broken code path refs 0，结果为 `doc freshness: clean`。

## 2026-05-16 claw smoke 只读诊断

本段只记录证据，不接管 AgentRuntime / GUI smoke owner 写集；最新完整 `verify:local` 已消费该 pass 结果。

| 证据           | 观察                                                                                                                                                                                                                                                                                                                                                                                                                         | P18 判定                                                                                    |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| Summary        | `.lime/qc/gui-evidence/claw-chat-ready-streaming/claw-chat-ready-streaming-summary.json` 在 2026-05-16 07:33 更新为 `verdict=pass / error=null / consoleErrorCount=0 / failureRuntime=null`；只读复核显示 `providerPreference=deepseek`、`modelPreference=deepseek-v4-flash`、`interruptedTurnStatus=aborted`、`followSessionId=c2742b86-f2cc-4a0b-91f2-b1baf3c6005e`、`followTurnId=091b4738-a537-43f2-a4b9-daf0ed1b3c3d`。 | 历史 blocker 已解除，并已进入完整 `verify:local` 通过证据；P18 不需要抢修 runtime / smoke。 |
| GUI smoke 链路 | 同次完整 `verify:local` 已跑过 workspace-ready、browser-runtime、site-adapters、agent-service-skill-entry、agent-runtime-tool-surface、agent-runtime-tool-surface-page、at-command-registry、agent-apps、claw-chat-ready-streaming、knowledge-gui 与 design-canvas。                                                                                                                                                         | 全局 GUI smoke 不再阻塞 P18 当前 SDK gate；后续只在外部 package 迁移后按改动范围补验证。    |
| 协作边界       | 本轮不改 `src-tauri/*`、runtime facade 或 smoke 脚本；只把已通过证据写入客户端 P18 审计。                                                                                                                                                                                                                                                                                                                                    | 避免和隔壁 AgentRuntime / GUI smoke 任务打架。                                              |

## 当前协作分工

| 工作面                                                                                                              | Owner                             | P18 处理方式                                                      |
| ------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------- |
| 外部 `content-factory-app` 源码、dist、UI、模型生成与 docs。                                                        | 隔壁 package 任务。               | 只读观察；不覆盖 dirty / untracked 文件。                         |
| AgentRuntime Rust / TS facade、push subscribe、workspace patch producer、capability catalog、真实桌面 GUI smoke。   | 隔壁 AgentRuntime 任务。          | 只消费已验证 host surface，不复制 read model 或 runtime command。 |
| Agent App P18 SDK contract、stable error、mock host、typed adapters、Lime-side regression、Host Bridge SDK client。 | Agent App P18。                   | 当前通过定向测试与 contracts。                                    |
| P18.5.3 package-side SDK facade / verify。                                                                          | 等外部 package owner 稳定后认领。 | 当前 blocked；不能用 read-only tests 伪装完成。                   |

## 2026-05-16 协作锁定

隔壁标准资料已有新增，且外部 package / AgentRuntime 仍在并行推进。P18 当前采取“只读同步标准、只写客户端计划、不抢运行写集”的分工：

| 输入 / 写集                                                                                                                   | 当前观察                                                                                                                                                                                                                                                                                                            | 本轮处理方式                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `/Users/coso/Documents/dev/ai/limecloud/agentapp`                                                                             | 工作区干净；当前 `HEAD=4bef605 fix: quote mermaid sdk labels`，zh / en 标准资料已覆盖 authoring、client implementation、reference、examples、what-is-agent-app、agent-app-vs-skills-knowledge 与 mini-program analogy，进一步固定 Agent App 是完整应用包、Expert Chat 只是 entry、能力调用必须通过 Capability SDK。 | 作为标准资料只读输入；同步到本审计和后续计划口径，不改标准仓库、不复制标准文档。                      |
| `/Users/coso/Documents/dev/ai/limecloud/content-factory-app`                                                                  | 2026-05-16 07:39 只读复核有 39 个 dirty / untracked 条目，其中 36 个 tracked modified、3 个 untracked；当时 `src/ui/host-bridge.js`、`dist/ui/host-bridge.js`、`src/ui/app.js`、`tests/ui.test.mjs`、`dist/*`、docs 和 model-generation 相关文件仍在并行改动，且私有 bridge marker 仍命中。                         | 历史 blocker；已被 10:55 真实 package verify 和 handoff gate `blockers=none / distArtifacts=0` 覆盖。 |
| `src-tauri/*`、`src/lib/api/agentAppRuntime.ts`、`src/features/agent-app/runtime/agentRuntimeCapabilityHost*`、GUI smoke 脚本 | 2026-05-16 07:05 只读复核 owner-sensitive 写集仍有 25 个 dirty / untracked 条目，属于 AgentRuntime / GUI owner 面。                                                                                                                                                                                                 | 不改；P18 只消费已验证 host surface，不抢 Rust / TS facade / GUI smoke。                              |
| `smoke:claw-chat-ready-streaming`                                                                                             | 2026-05-16 07:33 只读复核最新 summary 为 `verdict=pass / error=null`；`providerPreference=deepseek`、`modelPreference=deepseek-v4-flash`、`interruptedTurnStatus=aborted`、`followTurnId=091b4738-a537-43f2-a4b9-daf0ed1b3c3d`，`consoleErrorCount=0`、`failureRuntime=null`。                                      | 已随完整 `verify:local` 通过；不在 P18 SDK lane 内抢修 AgentRuntime / GUI smoke。                     |
| `docs/roadmap/agentapp/*`                                                                                                     | 客户端路线图需要记录标准资料和 owner 边界。                                                                                                                                                                                                                                                                         | 可做 additive 文档同步；避免把 Cloud / LimeCore、外部 package 或 AgentRuntime 实现混进客户端计划。    |
| Lime 工作区自检                                                                                                               | 2026-05-16 07:39 只读复核 `docs/roadmap/agentapp` 与 `src/features/agent-app` 范围仍有 30 个 dirty / untracked 条目，其中 16 个 tracked modified、14 个 untracked；P18 文档收口写集保持在 `docs/roadmap/agentapp/p18-completion-audit.md` 与 `docs/roadmap/agentapp/p18-5-3-package-sdk-migration-plan.md`。        | 后续继续先 `git status --short` 再动手；不把并行实现写集纳入 P18 文档收口。                           |

2026-05-16 08:58 当前只读复核：

| 检查项                                | 当前事实                                                                                                                                                                                                                                                                                                                                                                                                                           | P18 判定                                                                                                                                                                           |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 标准与服务端仓库                      | `/Users/coso/Documents/dev/ai/limecloud/agentapp` 工作区干净，`HEAD=4bef605`；`/Users/coso/Documents/dev/ai/limecloud/limecore` 工作区干净。                                                                                                                                                                                                                                                                                       | 标准 / 服务端事实源可读；本轮不改标准仓库或 LimeCore。                                                                                                                             |
| 隔壁 Capability 共享输入              | 2026-05-16 只读读取 `docs/roadmap/agentruntime/claw-capability-sharing.md`；该 draft 固定 Chat `@命令`、Agent App `lime.agent.startTask`、Automation job 只是 surface adapter，Claw 能力需收敛到 AgentRuntime capability catalog，首刀已把部分 capability hint 写入现有 Claw `*_skill_launch` metadata。                                                                                                                           | 作为 AgentRuntime owner 输入消费；P18 不复制 Claw skill launch，不新增 `content_factory_*` 垂直后端能力，也不把 capability catalog owner 迁入 Agent App SDK。                      |
| 外部 package 写集                     | `/Users/coso/Documents/dev/ai/limecloud/content-factory-app` 仍有 36 个 tracked modified、3 个 untracked；两个 `npm run dev` 进程的 cwd 都在该仓库。                                                                                                                                                                                                                                                                               | owner 仍未稳定；P18.5.3 不接管外部源码、`dist/*`、`tests/*` 或 `package.json`。                                                                                                    |
| 外部私有 bridge marker                | `src/ui/host-bridge.js` 仍包含 `pendingRequests / buildMessage / window.parent.postMessage / requestHostBridge / capability:invoke / capability:subscribe / capability:event`，未出现 `createLimeHostBridgeCapabilityInvoker` 或 `createLimeCoreCapabilityAdapters`。                                                                                                                                                              | package-side SDK facade 仍未完成。                                                                                                                                                 |
| 外部 UI 测试 marker                   | `tests/ui.test.mjs` 仍直接过滤 `message.type === 'capability:invoke'` 并模拟私有 `postMessage` transport。                                                                                                                                                                                                                                                                                                                         | 测试仍鼓励私有 transport；迁移时必须改为 SDK invoker / Host Bridge client call log。                                                                                               |
| Lime 运行面                           | Lime 当前仍有 Vite 与 `tauri:dev:headless` 进程在跑。                                                                                                                                                                                                                                                                                                                                                                              | 本轮不启停 DevBridge / Vite / Tauri，不跑 GUI smoke，不抢隔壁运行环境。                                                                                                            |
| SDK package 发布边界                  | Lime 根 `package.json` 仍是 private 桌面包；`packages/` 仅有 `@limecloud/lime-cli`，未发现正式 `@lime/app-sdk` package。                                                                                                                                                                                                                                                                                                           | 外部迁移若立即 handoff，只能用带退出条件的 package-local shim，不能 import Lime internal path。                                                                                    |
| 文档轻量验证                          | 2026-05-16 08:59 低优先级运行 `npm run harness:doc-freshness`，monitored docs 10 / existing docs 10 / issues 0 / broken markdown links 0 / broken code path refs 0。                                                                                                                                                                                                                                                               | 客户端文档引用未被本轮协作快照破坏；仍不能替代 P18.5.3 外部 package-side SDK facade / verify。                                                                                     |
| Agent App feature island 越界扫描     | 2026-05-16 09:00 只读运行 `rg -n "SceneApp\|contentEngineering\|sceneapp_\|safeInvoke\|invoke\\(\|new Worker\|Worker\\(" src/features/agent-app \|\| true`，无输出。                                                                                                                                                                                                                                                               | 当前 feature island 未复活旧 SceneApp、未直接 Tauri invoke、未执行 raw Worker；仍不能替代外部 package SDK facade / verify。                                                        |
| Agent App feature island 全量定向测试 | 2026-05-16 10:17 低优先级运行 `npm test -- src/features/agent-app`，36 files / 178 tests passed。                                                                                                                                                                                                                                                                                                                                  | 当前 Agent App feature island 回归通过，并覆盖 SDK-only public surface 收口；仍不能替代外部 package-side SDK facade / verify。                                                     |
| Lime-side SDK seam 定向测试           | 2026-05-16 09:01 低优先级运行 `npm test -- src/features/agent-app/sdk/hostBridgeClient.test.ts src/features/agent-app/sdk/contentFactorySdkRegression.test.ts src/features/agent-app/index.test.ts`，3 files / 8 tests passed。                                                                                                                                                                                                    | Host Bridge SDK client、内容工厂 SDK regression 与 public SDK export seam 当前仍绿；仍不能替代外部 package-side SDK facade / verify。                                              |
| Lime typecheck                        | 2026-05-16 10:55 低优先级运行 `npm run typecheck -- --pretty false`，通过。                                                                                                                                                                                                                                                                                                                                                        | 当前 dirty 工作区的 TypeScript 类型层仍通过，并覆盖内容工厂回归改从 SDK-only public surface 导入后的类型面。                                                                       |
| Lime lint                             | 2026-05-16 11:00 低优先级运行 `npm run lint`，通过。                                                                                                                                                                                                                                                                                                                                                                               | 当前 `src` lint 仍通过；脚本变更另由 handoff core test、diff check 与实际 gate 运行覆盖。                                                                                          |
| Lime contracts                        | 2026-05-16 10:53 低优先级运行 `npm run test:contracts`，command contracts、harness contracts、modality runtime contracts 与 cleanup report contract 均通过。                                                                                                                                                                                                                                                                       | 当前命令 / harness / modality 契约仍通过；不替代外部 package verify，但 package verify 已于 10:55 通过。                                                                           |
| Lime app version                      | 2026-05-16 09:07 低优先级运行 `npm run verify:app-version`，版本一致性检查通过：`1.40.0`。                                                                                                                                                                                                                                                                                                                                         | 当前版本事实源一致；仍不能替代外部 package-side SDK facade / verify。                                                                                                              |
| 外部 package 只读测试                 | 2026-05-16 09:05 在 `/Users/coso/Documents/dev/ai/limecloud/content-factory-app` 低优先级运行 `npm test`，46 tests passed。                                                                                                                                                                                                                                                                                                        | 只证明外部 package 当前业务测试仍绿；测试仍在私有 bridge transport 上通过，不能替代 package-side SDK facade 或会重建 `dist/*` 的 `npm run verify`。                                |
| 外部标准只读校验                      | 2026-05-16 09:06 在 `/Users/coso/Documents/dev/ai/limecloud/content-factory-app` 低优先级运行 `npm run validate:app && npm run readiness:app`；`validate:app` 为 `ok=true / status=passed`，manifest hash 为 `sha256:6ec3fed5f163739bcf0fd2b845c51a8e10d28aa856e8c6f90259fdab9edd1e48`；`readiness:app` 为 `ok=true / status=needs-setup`。                                                                                        | 标准 manifest 可读，readiness 正确停在宿主 required 依赖待绑定状态；仍不能替代 package-side SDK facade 或会重建 `dist/*` 的 `npm run verify`。                                     |
| AgentApp 文档旧口径扫描               | 2026-05-16 09:10 运行 `rg -n "08:30\|171 tests\|35 files / 171" docs/roadmap/agentapp \|\| true`，无输出。                                                                                                                                                                                                                                                                                                                         | AgentApp 文档当前验证口径已统一到 09:04 / 09:09；仍不能替代外部 package-side SDK facade / verify。                                                                                 |
| 垂直后端能力越界扫描                  | 2026-05-16 09:11 只读扫描 `src-tauri / src / scripts` 中 `content_factory_* command / skill_launch` 形态；命中项集中在 `src-tauri/src/commands/agent_app_runtime_cmd.rs` 的 `is_content_factory_runtime_task`、output contract helper 与测试，用于避免复合内容工厂任务被强制提升为单一 Claw skill launch。                                                                                                                         | 当前未发现 P18 新增垂直 `content_factory_*` 后端 command；该 AgentRuntime helper 属隔壁 runtime owner 面，P18 只消费、不接管。                                                     |
| 外部 package SDK facade marker 复核   | 2026-05-16 09:22 只读复核 `/Users/coso/Documents/dev/ai/limecloud/content-factory-app`；`src/ui/host-bridge.js` 仍包含 `pendingRequests / buildMessage / window.parent.postMessage / requestHostBridge / capability:invoke / capability:subscribe / capability:event`，`tests/ui.test.mjs` 仍直接过滤 `message.type === 'capability:invoke'`。                                                                                     | P18.5.3 唯一主阻塞仍未解除；外部 package 尚未成为 SDK facade consumer。                                                                                                            |
| Handoff 确认状态                      | 2026-05-16 09:26 已进入需显式确认状态：没有用户或外部 package owner 明确“接管外部 package 最小写集”前，不修改 `/Users/coso/Documents/dev/ai/limecloud/content-factory-app/src/ui/host-bridge.js`、`tests/ui.test.mjs`、`package.json` 或 `dist/*`。                                                                                                                                                                                | 当前只能继续只读复核 / 文档审计；不能实施 P18.5.3 package-side SDK facade。                                                                                                        |
| 运行面复核                            | 2026-05-16 09:28 只读复核进程，两个 `npm run dev` 的 cwd 都在 `/Users/coso/Documents/dev/ai/limecloud/content-factory-app`；Lime 侧仍有 Vite 与 `tauri:dev:headless` 进程。                                                                                                                                                                                                                                                        | 外部 package 与 Lime GUI / Tauri 运行面仍在并行；不能抢占或启停这些进程。                                                                                                          |
| 09:39 再复核                          | 外部 package 当时有 36 个 tracked modified、3 个 untracked；`src/ui/host-bridge.js` / `tests/ui.test.mjs` marker 命中手写 transport；外部 `npm test` 重新通过 46 tests；Lime-side SDK seam 最小集 `hostBridgeClient.test.ts / contentFactorySdkRegression.test.ts / index.test.ts` 重新通过 3 files / 8 tests；`src/features/agent-app` 越界扫描无输出。                                                                           | 历史 blocker；已被 10:55 真实 package verify 覆盖。                                                                                                                                |
| 09:48 机械 gate                       | 新增 `scripts/agent-app-package-handoff-check.mjs` / `scripts/lib/agent-app-package-handoff-core.mjs` / `scripts/lib/agent-app-package-handoff-core.test.ts`，用只读方式把 dirty 计数、私有 bridge marker、SDK facade marker、`scripts/build.mjs` 与高风险脚本转成 `ready / needs_handoff / blocked` 判定；当前外部 package 输出 `status=blocked`，并命中 `build / verify / e2e:user-flow / e2e:user-flow:fake-model` 高风险脚本。 | P18.5.3 后续不再只靠人工 grep；但当前 gate 结果仍证明外部 package 未完成 SDK facade。                                                                                              |
| 10:17 外部 gate 复核                  | 运行 `node scripts/agent-app-package-handoff-check.mjs --package-dir /Users/coso/Documents/dev/ai/limecloud/content-factory-app --check`，退出码 1；输出为 `status=blocked`、dirty `tracked=36 / untracked=3`、hostBridgeSdk `none`，私有 bridge marker 与 highRiskScripts 均未消失。                                                                                                                                              | 历史 blocker；已被 10:55 真实 package verify 覆盖。                                                                                                                                |
| 10:33 外部 gate 复核                  | 运行 `node scripts/agent-app-package-handoff-check.mjs --package-dir /Users/coso/Documents/dev/ai/limecloud/content-factory-app`，输出 `status=needs_handoff`、dirty `tracked=36 / untracked=4 / total=40`、`hostBridgePrivate=none`、`uiTestPrivate=none`、`hostBridgeSdk=createLimeHostBridgeCapabilityInvoker:2, createLimeCoreCapabilityAdapters:2`，blockers 为 `none`。                                                      | source-side facade blocker 已解除；当时主缺口切换为 owner handoff / dist verify；已在 10:55 收口。                                                                                 |
| 10:41 不重建 dist 验证                | Lime repo 低优先级运行 SDK seam 最小集：4 files / 11 tests passed；外部 package 低优先级运行 `npm test`：46 tests passed；`validate:app` passed；`readiness:app` needs-setup。                                                                                                                                                                                                                                                     | 当前 source-side facade 与标准 manifest 仍绿；仍不能替代 `npm run verify`，因为 verify 会先 build 并重建 `dist/*`。                                                                |
| 10:45 dist 重建前预审                 | 只读解析外部 `scripts/build.mjs`，确认 build 会先删除 `dist` 再复制 `src/*`；hash 对比发现 `src/ui/host-bridge.js` 与 `dist/ui/host-bridge.js` 不一致，且 `src/ui/lime-app-sdk.js` 尚无 `dist/ui/lime-app-sdk.js`。                                                                                                                                                                                                                | 安装产物缺口已明确：source-side facade 尚未同步到 dist；仍需 owner handoff 后重建 dist 或记录退出条件。                                                                            |
| 10:47 隔离 verify 演练                | 复制外部 package 当前工作区到 `/tmp/limecloud-content-factory-verify.uots6S/content-factory-app`，用 sibling symlink 保持 `../agentapp` 可用，并在临时副本运行 `npm run verify`；build、46 tests、validate、readiness 全部通过，readiness 仍为预期 `needs-setup`。                                                                                                                                                                 | 证明当前源码若允许重建 dist，大概率可通过 package verify；仍不能替代真实 package 写集验收，因为真实 `/Users/coso/Documents/dev/ai/limecloud/content-factory-app/dist/*` 未被更新。 |
| 10:55 真实 package verify             | 用户确认后，在真实 `/Users/coso/Documents/dev/ai/limecloud/content-factory-app` 运行 `nice -n 10 npm run verify`：build 通过，46 tests passed，`validate:app` passed，`readiness:app` needs-setup；随后 handoff gate 显示 `distArtifacts=diff:0,missing:0,extra:0,total:0`。                                                                                                                                                       | P18.5.3 的 package verify / dist 验收完成；gate 仍为 `needs_handoff` 是因为工作区 dirty 与高风险脚本 warning，不是 SDK facade blocker。                                            |

冲突规约：

1. 修改前先跑目标仓库 `git status --short`，发现 dirty / untracked 且不属于本任务写集时只读观察。
2. 外部 package 未稳定前，不改 `src/ui/host-bridge.js`、`dist/ui/host-bridge.js`、`tests/ui.test.mjs` 或 `package.json`。
3. AgentRuntime / Rust / GUI smoke 并行任务未收口前，不改 `src-tauri/*`、runtime facade、DevBridge / Tauri / Vite 进程和 smoke 脚本。
4. 本机磁盘空间已恢复到可跑 Rust 测试；不在未确认时删除 `src-tauri/target` 或其他构建缓存。`smoke:claw-chat-ready-streaming` 的 06:23 历史失败已在 07:33 完整 `verify:local` 中转为 pass 证据。
5. P18 后续只在通用 SDK contract 存在缺口时补 `src/features/agent-app/sdk/*`，不新增垂直 `content_factory_*` host command 或第二套 bridge。

因此当前优先级调整为：不再把“外部 source-side facade、package verify、dist 同步”作为 P18 blocker；P18.7-B 也已解除 `manifestVersion: 0.6.0` reference 阻塞。下一刀回到 P18.7-C Host capability discovery。完整 `verify:local` 仍需在并行写集稳定后再作为整体验收复跑，本轮定向验证已经覆盖 v0.6 兼容风险。

## 下一刀

1. P18.7-C：实现 `lime.capabilities` Host discovery surface，让 App 读取 catalog/profile 摘要、实现状态、stage、owner 和 unavailable reason。
2. P18.7-D：接 `lime.usage / lime.models / lime.skills / lime.memory / lime.context` 到 AgentRuntime facts，而不是 App 自己估算模型、token 或 skill 使用。
3. P18.7-E：接 `lime.search / lime.browser / lime.documents / lime.media / lime.mcp / lime.terminal / lime.connectors` 到 ToolRuntime / Desktop Host policy。
4. P18.7-F：复核内容工厂真实闭环，确保生成过程、思考、执行、Skills、模型、Token、artifact、evidence 都留在 App 内且来自 Host projection。

## 完成判定

P18.1-P18.6 和 P18.7-A/B 满足各自功能完成判定：外部 `content-factory-app` package-side SDK facade 已完成，真实 package verify 已通过，Lime formal runtime / SDK regression 有迁移后新证据，且 Agent App v0.6 reference compatibility 已复绿。P18.7 整体尚未完成，因为 Host discovery、AgentRuntime resource、Tool/Integration 和内容工厂真实闭环仍未全部接线；同时外部 package 与 Lime 工作区仍 dirty，需要 owner 决定提交 / 推送边界。

## 2026-05-18 整体目标完成审计

本次把用户目标“完成整体目标”具体化为：完成 P18 / P18.7 Agent App 主线，使业务 App 可在 Host iframe 内通过统一 `lime.* capability surface` 消费 Lime AgentRuntime / ToolRuntime / policy / evidence 能力；完成标准不能只看单测或 manifest 通过，必须逐项映射到真实 artifact / evidence。

| 显式要求 / gate | 当前 artifact / evidence | 审计结论 |
| --- | --- | --- |
| P18.1-P18.6：外部 `content-factory-app` package-side SDK facade、真实 package verify、dist 同步 | 2026-05-16 10:55 真实外部 package `npm run verify` 通过；handoff gate 显示 `distArtifacts=diff:0,missing:0,extra:0,total:0` | 已覆盖，不再作为 P18 blocker |
| P18.7-A：catalog / SDK / profile 单一事实源 | `src/features/agent-app/sdk/capabilityCatalog.ts`、`capabilityContract.test.ts`、`capabilityAdapters.test.ts`、`publicSdkSurface.test.ts`；记录在 `p18-7-parallel-validation.md` | 已完成 |
| P18.7-B：`manifestVersion: 0.6.0`、layered manifest、reference cross-check | `normalizeManifest.ts`、`checkReadiness.ts`、`parseManifest.test.ts`、`referenceCliCrossCheck.test.ts`；文档已回链 `p18-7-full-lime-capability-surface.md` | 已完成 |
| P18.7-C：`lime.capabilities` Host discovery / `getProfile` / App profile 降级 | Host Bridge、Runtime Page、SDK adapter 定向测试；外部 `content-factory-app` 63 tests passed 覆盖 `lime.capabilities.getProfile` 和缺能力时阻止本地模型兜底 | first-cut 已完成 |
| P18.7-D：`lime.models / usage / skills / memory / context / tasks` 来自 Host / AgentRuntime facts | `capabilityDispatcher.test.ts`、Agent App 定向套件、typecheck 和多轮 GUI smoke 记录；`p18-7-parallel-validation.md` 已列明 models/usage/skills/memory/context/tasks projection | first-cut 已完成；真实 mutation / runtime enable 未打开，按规划不算当前 blocker |
| P18.7-E：工具 / 集成只能通过 Host intent、AgentRuntime handoff、ToolRuntime owner binding、policy、progress、evidence | `capabilityDispatcher.test.ts`、`agent_app_runtime` / `agent_app_connector` Rust tests、`scripts/agent-app-connector-outbox-smoke.mjs` replay/live summaries、secret lease visibility gate；`p18-7-e-toolruntime-execution-gate-plan.md` 已列明 E1/E2/E3/E4 与 outbox/evidence projection | current first-cut 已完成；**深水位仍缺真实 Connector OAuth/raw secret material delivery、外部平台真实 delivery 和产品级 GUI 送达证明** |
| P18.7-F：内容工厂真实闭环，页面内完成业务 action，展示 runtime / Skills / model / usage / cost / artifact / evidence / workspace patch | `.lime/qc/gui-evidence/agent-apps/content-factory-scenarios-production-after-sidebar-nav-warning-20260518-codex-summary.json`：`run-scenarios / run-production` combined summary 的 `allActionsCompleted / allActionsUsedExpectedSkills / allActionsHaveModelUsageCost / allActionsHaveWorkspacePatch / allActionsFullRuntimeReady / processVisibleAfterEachAction / noHostFallback / noConsoleErrors` 均为 true；此前七个 focused completion gates、run-strategy / run-review / full-flow action gates 也已记录 | 当前关键产品闭环已复绿；`failedRequests` 仍有开发态收尾噪声，不扩展为“全网络零噪声” |
| Runtime idle / detached headless stderr 修复 | `src-tauri/src/profiling.rs` 使用 non-panicking stderr writer；`CARGO_TARGET_DIR=/tmp/lime-codex-target-p18f cargo test --manifest-path src-tauri/Cargo.toml -p lime profiling::tests --lib` 2 tests passed；`cargo check --manifest-path src-tauri/Cargo.toml -p lime --lib --message-format short` 通过 | 已覆盖 broken pipe 根因 |
| 最小工程验证 | 2026-05-18 scoped `node --check "scripts/agent-apps-content-factory-flow.mjs"`、`git diff --check -- "scripts/agent-apps-content-factory-flow.mjs" "src-tauri/src/profiling.rs" "docs/roadmap/agentapp/p18-7-parallel-validation.md"`、上述 Rust tests / cargo check 均通过 | 覆盖本轮脚本 / Rust / 文档写集；未替代完整 `verify:local` |
| 并行协作 / dirty 写集 | 当前 Lime 工作区仍有大量隔壁改动：Agent App v2、install-mode、packaging、runtime-profile、shell、AppSidebar、providers/server/services 等；本轮只读监控并避免双跑 GUI flow | 不能把整仓 dirty 状态当作已收口；后续 owner 仍需整理写集 |

审计结论：P18.7-A/B/C/D/F 与 P18.7-E current first-cut 已有真实 evidence；但“整体目标”若按完整 P18.7 深水位定义，仍不能标记为 complete。剩余硬缺口是 P18.7-E 的真实 Connector OAuth / raw secret material delivery / 外部平台真实 delivery、产品级 GUI 送达证明，以及当前并行大写集的最终 owner 收口和整体验证。下一刀应优先补 P18.7-E 深水位 adapter / delivery，而不是继续重复内容工厂 completion gate。

2026-05-18 13:34 追加当前工作树 live 复核：`node "scripts/agent-app-connector-outbox-smoke.mjs" --mode live --timeout-ms 600000 --output ".lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-live-current-audit-20260518-summary.json"` 通过。该 summary 使用自动选择的 `deepseek/deepseek-v4-flash`，task `agent-app-connector-outbox-runtime-1779082437236-73778` / session `63017b6f-9c6e-456e-ae7c-1d93d884b44d`，断言 `threadReadCompleted / connectorToolCallProjected / outputHadBoundedMetadata / outboxEvidenceRefObserved / toolCallEvidenceProjected / threadEvidenceProjected / taskEventEvidenceProjected / secretDeliveryConcreteLeaseRefNotExposed` 以及 secret delivery expectation 全为 true；`threadRead` 显示 `connector__notion__createPage`、`adapterReadiness=host_managed_secret_delivery_adapter_ready`、`externalStatus=not_delivered`、`nextRequired=cloud_overlay_worker_delivery`、`secretDeliveryCredentialMaterialExposed=false`、`secretDeliveryTokenExposed=false`。该证据证明当前工作树的 P18.7-E outbox/evidence/secret-delivery facts 仍可 live 复现，但 summary caveat 明确它不证明外部 OAuth handshake、raw secret material delivery 或 cloud delivery。

2026-05-18 21:35 追加 delivery receipt projection live 复核未通过：`.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-live-delivery-receipt-projection-current-20260518-summary.json` 显示 DevBridge healthy，但自动 provider 仍选择 `deepseek/deepseek-v4-flash`，runtime 在 provider 阶段失败为 `402 Payment Required: Insufficient Balance`，`threadRead.status=failed`、`toolCallCount=0`，因此未进入 connector tool call / delivery receipt projection。该失败归因为默认 provider 余额不足，不能当作 P18.7-E delivery receipt 回归失败；当前仍只能依赖 21:00 replay green 证明既有 live session 中 `outbox://...` 与 `delivery://...` 可投影。下一刀是等隔壁 `verify:gui-smoke`、content-factory flow 和 default `cargo/rustc` 释放后，用显式可用 provider `custom-da3283c4-8405-45e9-81cd-12991ffdf41c / claude-sonnet-4-6` 复跑 live smoke。整体目标仍不能标记 complete。

2026-05-18 23:20 追加 custom provider delivery receipt projection live 复核通过：`.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-live-delivery-receipt-projection-custom-provider-20260518-codex-summary.json` 使用显式 `custom-da3283c4-8405-45e9-81cd-12991ffdf41c / claude-sonnet-4-6`，task `agent-app-connector-outbox-runtime-1779117534688-37651` / session `38eb0886-1c4e-4fe4-a5b0-aba73bcefa89`，所有 connector / outbox / delivery / secret lease visibility assertions 为 true；`threadRead` 显示 `connector__notion__createPage`、`toolCallCount=1`、`outputContainsTrustedConnectorJson=true`、`adapterReadiness=host_managed_secret_delivery_adapter_ready`、`nextRequired=external_platform_delivery`、`deliveryStatus=accepted_by_local_cloud_overlay_worker`、`deliveryExternalPlatformDelivered=false`，`outbox://...` 与 `delivery://...` 均进入 tool call、thread evidence summary 和 Agent App task events。该证据把 P18.7-E current first-cut 的 focused live projection 补到当前 binary + custom provider，但仍不能把 local worker intake receipt 解释为真实 OAuth、raw secret material adapter 或外部平台送达；整体目标仍不能标记 complete。

### 2026-05-19 P18.7-E Host-managed webhook external delivery first-cut

- 本轮继续 P18.7-E，不再重复内容工厂按钮 smoke；认领写集限定在 `src-tauri/src/commands/aster_agent_cmd/tool_runtime/connector_tools/{readiness.rs,cloud_overlay_outbox.rs,tests.rs}`、`scripts/agent-app-connector-outbox-smoke.mjs` 与 P18.7-E 文档，避开隔壁 v2 / install / shell window 写集。
- 已补 Host-managed webhook external delivery seam：`readiness.rs` 只从 internal request 读取 `secretDelivery.externalDelivery`，并要求 Host-managed binding、webhook channel、安全 false flags 与受限 target；`cloud_overlay_outbox.rs` 会把脱敏 payload POST 到 webhook，并在 ToolResult / delivery receipt 中只返回 target hash、label、HTTP status、deliveredAt 和 delivery boolean。
- 本地 `http://127.0.0.1:` / `http://localhost:` webhook 现在走直接 TCP HTTP POST，不经 `reqwest` proxy 选择；这是为了解决 focused live local-webhook smoke 中旧 runtime 通过系统代理拿到 `502`、本地 webhook `receivedRequestCount=0` 的问题。远端 `https://...` 仍走 `reqwest`，失败路径只返回安全错误码，不回显 URL。
- 安全边界：ToolResult / public evidence 不暴露 webhook target URL、raw credential/token 或 concrete `secret-lease://...`；workspace-local internal receipt 仍可保留 Host-managed lease handle，供 Cloud Overlay worker 消费。
- 定向验证：`agent_app_connector_cloud_overlay_can_deliver_to_host_managed_webhook --lib` 1 test passed；`agent_app_connector --lib` 12 tests passed；`cargo check --manifest-path "src-tauri/Cargo.toml" -p lime --lib --message-format short` 通过；`rustfmt --edition 2021 --check ...connector_tools...`、`node --check "scripts/agent-app-connector-outbox-smoke.mjs"`、`git diff --check -- ...P18.7-E 写集...` 通过。验证使用 `$HOME/Library/Caches/lime-codex-target-p18e` 独立 target 和现有 `sherpa-onnx` prebuilt，避免抢隔壁 default target / GUI flow。
- live local-webhook evidence 尚未 green：`.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-live-local-webhook-20260519-codex-summary.json` 与 `...-v2-summary.json` 均显示旧 runtime binary 仍返回 `externalDelivery.httpStatus=502`，local webhook 未收到请求；两份 summary 仍证明 connector call、outbox/delivery evidence projection、target URL 不外露和 concrete lease ref 不外露。下一刀需在确认可重启 DevBridge / Tauri 后，用新 binary 复跑 `--external-delivery-local-webhook`。
- 审计结论：这把 P18.7-E 从 local worker intake receipt 推进到 external delivery adapter first-cut，但目前只证明 Rust seam 与本地 HTTP endpoint 直连能力；仍不能宣称真实 OAuth handshake、raw secret material delivery adapter、Notion/Slack 等生产外部平台送达或产品级 GUI 送达证明已完成。

### 2026-05-19 06:06 P18.7-E / F 最新证据补记

- P18.7-E live local-webhook 已 green：`.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-live-local-webhook-20260519-codex-v3-summary.json` 使用 `custom-da3283c4-8405-45e9-81cd-12991ffdf41c / claude-sonnet-4-6`，所有 outbox / delivery / secret visibility / external delivery assertions 为 true；`externalStatus=delivered`、`nextRequired=external_platform_delivery_complete`、`deliveryStatus=delivered_to_external_platform`、`externalDeliveryHttpStatus=200`、local webhook `receivedRequestCount=1`。
- P18.7-F run-review focused clean retry 已 green：`.lime/qc/gui-evidence/agent-apps/content-factory-review-yunwu-clean-retry-20260519-codex-summary.json`，`run-review` task `agent-app-task-024b3ecb-4ba5-41f6-b789-43d19ec03c24` / session `agent-app-runtime-743eaaf0-1155-4e53-a62d-6a3295fee820` 为 `taskStatus=completed / profileStatus=completed / toolCallCount=1 / artifactCount=3 / hasWorkspacePatch=true`，`allActionsFullRuntimeReady / processVisibleAfterEachAction / noHostFallback / noConsoleErrors` 均通过。
- 前序失败口径保留：`content-factory-full-flow-kimi-20260519-codex-failure.json` 仍是五动作 full-flow 失败，断点为 `run-review` 缺 `terminalReady`，但模型 runStatus 为 success 且有 review_report workspace patch；`content-factory-review-xfyun-20260519-codex-failure.json` 为 Xfyun provider runStatus error。上述失败不能覆盖 clean retry green，但也说明五动作 full-flow 仍未有最新整体 green summary。
- 完成口径不变：local webhook green 是 Host-managed HTTP endpoint first-cut，不是生产 Notion/Slack delivery；整体目标仍缺真实 Connector OAuth、raw secret material adapter、生产外部平台送达和产品级 GUI 外部送达证明，不能调用 goal complete。

## 2026-05-19 06:23 P18.7-E remote webhook secret-source hardening

- 在不抢隔壁 content-factory full-flow / GUI runtime 的前提下，focused `scripts/agent-app-connector-outbox-smoke.mjs` 增加远端 webhook URL 的安全来源：`--external-delivery-webhook-url-env` 与 `--external-delivery-webhook-url-file`。
- 该变更解决的是“真实远端 delivery proof 不能把 webhook URL 暴露在命令行 / 进程列表 / summary 中”的前置门槛；summary 只保留来源类型、target hash/label 与 HTTP delivery 结果。
- completion 口径不变：这不是 OAuth handshake、不是 raw secret material delivery adapter，也不是 Notion/Slack/Feishu 生产平台送达；整体目标仍不能标记完成。

## 2026-05-19 06:32 P18.7-F five-action full-flow evidence

- 隔壁 content-factory full-flow 已收口，summary：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-final-20260519-codex-summary.json`，screenshot：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-final-20260519-codex.png`。
- 覆盖动作：`run-scenarios / run-production / run-scripts / run-strategy / run-review`，provider/model 为 `custom-da3283c4-8405-45e9-81cd-12991ffdf41c / claude-sonnet-4-6`。
- 关键断言：`actionCount / allActionsCompleted / allActionsUsedExpectedSkills / allActionsHaveModelUsageCost / allActionsHaveWorkspacePatch / allActionsFullRuntimeReady / processVisibleAfterEachAction / sceneCountPreservedAfterFlow / noHostFallback / noConsoleErrors` 均为 true，`consoleErrorCount=0`；五个 direct runtime task 均 `completed`，对应页面 materialization 均 `ready=true`。
- 注意事项：`sameIframeContext=false`，且仍有 18 个 `net::ERR_ABORTED` failedRequests（主要是 events / materialize 这类开发态请求中断）；因此该证据可声明 P18.7-F 五动作业务闭环与 runtime/Skill/usage/cost/workspace patch/no console errors gate 通过，但不声明“所有网络请求零噪声”。
- completion 口径不变：该证据不覆盖 P18.7-E 的真实 Connector OAuth、raw secret material delivery adapter、生产外部平台 delivery 或产品级 GUI 外部送达证明；整体目标仍不能标记 complete。

## 2026-05-19 06:34 P18.7-E focused replay after secret-source hardening

- 变更 `agent-app-connector-outbox-smoke.mjs` 后，已用既有 live local-webhook session 做 replay：`.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-replay-local-webhook-source-hardening-20260519-codex-summary.json`。
- Assertions 全绿，证明 replay harness 仍可读 `outbox://` / `delivery://` refs、delivery receipt、thread evidence summary 和 Agent App task events，并确认 concrete lease / raw token / credential material 未进入 public projection。
- 这仍是 local-webhook replay，不是远端 production delivery proof；completion 缺口保持为真实 OAuth、raw secret material adapter、生产 external platform delivery 和产品级 GUI 外部送达证明。

## 2026-05-19 06:54 P18.7-F metrics full-flow evidence

- 新一轮 metrics full-flow 已收口，summary：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-metrics-20260519-codex-summary.json`，screenshot：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-metrics-20260519-codex.png`。
- 覆盖动作仍为 `run-scenarios / run-production / run-scripts / run-strategy / run-review`，provider/model 为 `custom-da3283c4-8405-45e9-81cd-12991ffdf41c / claude-sonnet-4-6`。
- 关键断言继续通过：`allActionsCompleted / allActionsUsedExpectedSkills / allActionsHaveModelUsageCost / allActionsHaveWorkspacePatch / allActionsFullRuntimeReady / processVisibleAfterEachAction / sceneCountPreservedAfterFlow / noHostFallback / noConsoleErrors` 均为 true，`consoleErrorCount=0`，`sceneCountRegressions=0`。
- 本轮比上一份 full-flow failedRequests 更少（13 vs 18）；`run-review` 页面 materialization 来源为 `direct_workspace_patch_materialized`，说明复盘结果可从 direct workspace patch 回填页面。
- Caveat 保持：`sameIframeContext=false`，仍有 13 个 `net::ERR_ABORTED` failedRequests，因此只作为更强的 P18.7-F 五动作/metrics 业务闭环证据，不代表全运行面零噪声，也不覆盖 P18.7-E production connector delivery。

## 2026-05-19 06:58 objective completion audit checklist

| 成功标准 / 明确交付物 | 当前证据 | 结论 |
| --- | --- | --- |
| Agent App 在 Host iframe 内通过统一 `lime.*` capability surface 消费 Host 能力 | P18.7 A/B/C/D/F 文档与前端/Rust/GUI 证据已覆盖；内容工厂 iframe 与 Host profile smoke 已有 green | first-cut covered |
| P18.7-F 内容工厂业务五动作闭环 | `content-factory-full-flow-yunwu-final-20260519-codex-summary.json` 与 `content-factory-full-flow-yunwu-metrics-20260519-codex-summary.json` 均显示五动作 completed、Skills/usage/cost/workspace patch/fullRuntimeReady/no Host fallback/no console errors gate 通过 | covered with caveat：`sameIframeContext=false` 与 failedRequests 仍存在 |
| P18.7-E ToolRuntime owner binding / connector evidence projection | Rust `agent_app_connector` / `runtime_evidence_projection_service` 定向测试、focused live local-webhook v3、source-hardening replay 均通过 | first-cut covered |
| Host-managed local webhook external delivery | `p18-7-e-connector-outbox-runtime-smoke-live-local-webhook-20260519-codex-v3-summary.json` 显示 delivered、HTTP 200、local webhook received，且 target/lease/token/material 不外露 | local first-cut covered |
| Remote webhook secret-safe test input | `agent-app-connector-outbox-smoke.mjs` 支持 `--external-delivery-webhook-url-env` / `--external-delivery-webhook-url-file`；`node --check`、replay、diff check 通过 | harness ready；尚未配置真实远端 secret |
| 真实 Connector OAuth handshake | 当前只有 authorization request handoff / snapshot projection / redaction evidence；没有真实 OAuth provider handshake 成功证据 | missing |
| raw secret material delivery adapter | 当前只有 Host-managed lease/ref 与不暴露证明；没有把 raw secret material 安全交给 Host/Cloud worker 的真实 adapter evidence | missing |
| 生产外部平台真实 delivery（Notion/Slack/Feishu 等） | 当前只有 local webhook 与可安全读取远端 webhook URL 的 harness；无真实平台 receiver / domain object / production delivery receipt | missing |
| 产品级 GUI evidence 证明外部平台送达 | 当前 GUI evidence 证明内容工厂业务闭环；未证明 connector mutation 到真实外部平台 | missing |
| 并行大写集 owner 收口 | 工作树仍有 v2、standalone、runtime queue、UI/i18n 等大量隔壁写集和未跟踪文件 | pending |

Audit verdict：整体目标不能标记 complete；下一刀仍优先 P18.7-E 的真实 OAuth / raw secret material adapter / production external delivery，而不是继续重复内容工厂 full-flow。

## 2026-05-19 07:03 P18.7-E inline raw secret guard

- `src-tauri/src/commands/aster_agent_cmd/tool_runtime/connector_tools/readiness.rs` 已拒绝 `externalDelivery` 中的 inline raw secret/header material；`src-tauri/src/commands/aster_agent_cmd/tool_runtime/connector_tools/tests.rs` 新增覆盖。
- 验证通过：`rustfmt --edition 2021 --check ...readiness.rs ...tests.rs`、`cargo test ... agent_app_connector_external_delivery_rejects_inline_secret_material --lib`、`cargo test ... agent_app_connector --lib`、`cargo check --manifest-path "src-tauri/Cargo.toml" -p lime --lib --message-format short`。
- Completion 口径：这是防止伪 production delivery 的 guardrail，不是 raw secret material adapter 完成；真实 adapter 仍需要 Host/Cloud 专用 secret fact/source 与生产平台 delivery evidence。

## 2026-05-19 07:44 normalized metrics full-flow failure classification

- normalized metrics full-flow 产物：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-metrics-normalized-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-metrics-normalized-20260519-codex-failure.png`。
- 失败点：`run-review` 未达到 full runtime readiness，missing `terminalReady`。
- 失败 task/session：`agent-app-task-7ac2611f-c0e4-44a8-ab30-b6110eebb4e6` / `agent-app-runtime-8366177b-708a-4c8c-ade1-f930b2daad2b`。
- direct record：`taskStatus=failed`、`profileStatus=failed`、`toolCallCount=1`、`artifactCount=0`、`hasWorkspacePatch=false`、`hasUsage=false`、`hasEstimatedUsage=true`、`hasCost=true`、`evidenceReady=true`、`invokedSkillNames=[content-reviewer]`、model runStatus=`error`。
- 分类：这是 normalized metrics 复跑中的 provider/runtime terminal readiness failure，不能覆盖前两份五动作/metrics green summary；也不能作为 P18.7-E production connector delivery 证据。

## 2026-05-19 07:51 nested raw secret guard extension

- P18.7-E guard 现在覆盖 nested raw secret material，例如 `externalDelivery.auth.bearerToken`；对应测试与 `agent_app_connector` 定向套件、`cargo check` 均通过。
- Completion 口径保持：这是防止把 raw secret 塞进 `internalRequest` 的安全 guard，不是 raw secret material delivery adapter 完成。

## 2026-05-19 08:09 Agent Apps smoke / MiniMax review failure classification

- Agent Apps smoke 失败产物：`.lime/qc/gui-evidence/agent-apps/agent-apps-smoke-failure.json`，失败断言 `installedVisible`；console error 仍是 cloud package 下载 `https://lime.local/agent-apps/content-factory-app/releases/0.3.0/package.zip` 失败。该失败不推翻此前 `agent-apps-smoke-summary.json` 的 current green，但说明当前并行 v2/standalone 写集下 Agent Apps smoke 需要 owner 复核。
- MiniMax run-review 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-minimax-normalized-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-minimax-normalized-20260519-codex-failure.png`。
- MiniMax direct runtime 本身完成：task `agent-app-task-7b4135bc-0414-4c37-8cb9-4412da7e1489` / session `agent-app-runtime-0839fc07-4e12-49dc-afed-409f61c1a86e`，`taskStatus=completed`、`profileStatus=completed`、model runStatus=`success`、`toolCallCount=1`、`artifactCount=3`、`hasWorkspacePatch=true`、`terminalReady=true`、`evidenceReady=true`、invoked `content-reviewer`。
- MiniMax 失败点不是 runtime，而是页面 90s 内未 materialize workspace patch：`run-review runtime completed but page did not materialize workspace patch`。分类为 GUI materialization gap / normalized review page sync gap；不能作为 P18.7-F green 证据，也不覆盖 P18.7-E production connector delivery。

## 2026-05-19 08:35 MiniMax fallback bridge-cooldown failure

- MiniMax fallback review 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-minimax-fallback-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-minimax-fallback-20260519-codex-failure.png`。
- 失败点发生在启动/包检查阶段：`agent_app_inspect_local_package` 通过 DevBridge 调用失败，错误为 `Failed to fetch (bridge cooldown active)`，并伴随 `127.0.0.1:3030/health` 短时 `ERR_CONNECTION_REFUSED`。
- 该失败没有进入 MiniMax runtime review 本体，不能作为 model/runtime 质量判断；分类为 DevBridge cooldown / 后端桥接短时不可用。后续 `npm run bridge:health -- --timeout-ms 120000` 在约 101s 后恢复 `status=ok`。
- Completion 口径：当前 P18.7-F 仍以 Yunwu final / metrics 两份 full-flow summary 为 green 证据，MiniMax fallback 不作为 green；P18.7-E production connector delivery 仍未覆盖。

## 2026-05-19 08:18 review fallback audit update

- Additional gap closed：models may return a valid review decision patch without `metrics` / `metricsAnalysis`. Content Factory now carries the current user-entered review metrics into workspace materialization as fallback.
- Evidence：`content-factory-app npm run build && npm test` passed `103/103` after changes.
- Provider audit：MiniMax `MiniMax-M2.7` can answer a lightweight chat probe and can sometimes produce review workspace patch, but retries are blocked by DevBridge cooldown or stuck runtime; it is not accepted as final provider evidence.
- Completion verdict unchanged：do not mark P18 / Content Factory overall complete until a latest complete full-flow green summary and Playwright MCP manual confirmation are both available.

## 2026-05-19 08:48 Agent App Runtime split / command contract audit

- `src-tauri/src/commands/agent_app_runtime_cmd.rs` 当前已经从大文件收敛成 25 LoC facade，具体职责拆到 `start_task.rs`、`cancel_task.rs`、`task_snapshot.rs`、`host_response.rs`、`metadata.rs`、`events.rs`、`tool_execution.rs` 等子模块；`src-tauri/src/app/runner.rs` 与 `src-tauri/src/dev_bridge/dispatcher/agent_apps.rs` 使用子模块 handler 路径。
- 最新 `.lime/qc/gui-evidence/agent-apps/agent-apps-smoke-summary.json` 所有 Agent Apps smoke assertions 均为 true，`failedRequests=0`、`consoleErrors=0`；delete-data 后安装态恢复和 flag-off regression 都通过。
- 验证通过：`rustfmt --edition 2021 --check ...`、`cargo test --manifest-path "src-tauri/Cargo.toml" -p lime agent_app_runtime --lib`（32 passed）、`cargo test ... agent_app_connector --lib`（13 passed）、`cargo check ... -p lime --lib --message-format short`、`npm run test:contracts`、`npm run bridge:health -- --timeout-ms 30000`。
- Completion 口径：这证明 Runtime facade 拆分、DevBridge dispatch、命令契约和 smoke 主路径当前一致；整体仍不能 complete，因为真实 Connector OAuth handshake、raw secret material adapter、production external platform delivery 和 GUI 外部送达证据仍缺失。

## 2026-05-19 09:08 remote webhook redaction guard

- `scripts/agent-app-connector-outbox-smoke.mjs` 已加固远端 webhook proof 的防泄漏断言：`externalDeliveryTargetNotExposed` 不再只检查 Tool output / output payload，也检查 `threadRead` 与 Agent App task 公共投影；`secretDeliveryConcreteLeaseRefNotExposed` 同样覆盖公共投影，避免 future proof 把 webhook URL 或 concrete `secret-lease://...` 泄漏到 task events / thread projection。
- 验证通过：`node --check "scripts/agent-app-connector-outbox-smoke.mjs"`、`git diff --check -- "scripts/agent-app-connector-outbox-smoke.mjs"`，以及只读 replay `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-replay-public-projection-redaction-20260519-codex-summary.json`；该 replay 不调用模型 provider，复核 local-webhook live session 的 outbox / delivery / thread / task event projection 仍全绿。
- 只读盘点当前 shell 环境变量名，未发现 `WEBHOOK / NOTION / SLACK / FEISHU / OAUTH / CONNECTOR / LIME_AGENT_APP` 类远端 delivery secret 来源；没有读取或打印 secret value。
- Completion 口径：这是远端 delivery 证明前的 safety gate，不是 OAuth handshake、raw secret material adapter 或 production platform delivery 完成；整体目标仍不能 complete。

## 2026-05-19 09:15 Mimo review fallback failure classification

- Mimo fallback review 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-fallback-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-fallback-20260519-codex-failure.png`。
- Runtime 本体完成：task `agent-app-task-979cc3eb-0ede-4080-91d3-c37310c00ce5` / session `agent-app-runtime-f8a4dab0-e6c4-4e56-b068-671dec457fdf`，`taskStatus=completed`、`profileStatus=completed`、`toolCallCount=1`、`artifactCount=3`、`hasWorkspacePatch=true`、`terminalReady=true`、`evidenceReady=true`、`hasCost=true`。
- 失败点：页面等待约 90s 后仍未 materialize workspace patch；`consoleErrors=0`、`failedRequests=9`。分类为 GUI materialization / review page sync gap。
- Completion 口径：Mimo 这轮不能作为 P18.7-F green 证据，也不能覆盖 P18.7-E 的真实 OAuth / raw secret material adapter / production external platform delivery。

## 2026-05-19 09:22 Mimo data-insights bridge timeout

- Mimo data-insights review 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-data-insights-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-data-insights-20260519-codex-failure.png`。
- 失败发生在启动/本地 package inspect 阶段：DevBridge 调用 `agent_app_inspect_local_package` 30s timeout，未进入 run-review runtime。
- 后续 `npm run bridge:health -- --timeout-ms 60000` 已恢复/确认 `status=ok`，耗时 473ms；分类为短时 DevBridge fetch / startup race。
- Completion 口径：该失败不推翻既有 P18.7-F green，也不能作为 Mimo runtime 质量判断，更不覆盖 P18.7-E production delivery。

## 2026-05-19 09:34 Mimo data-insights retry materialization failure

- Mimo data-insights retry 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-data-insights-retry-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-data-insights-retry-20260519-codex-failure.png`。
- Runtime 本体完成：task `agent-app-task-93e03d6c-2bfa-4d26-8c79-d70a3384705c` / session `agent-app-runtime-84b82ec3-8bb2-4a31-bdd6-ef15419dbf4b`，Mimo runStatus=`success`，`toolCallCount=1`、`artifactCount=3`、`hasWorkspacePatch=true`、`terminalReady=true`、`evidenceReady=true`、`hasCost=true`。
- 失败点仍是页面约 90s 内未 materialize workspace patch；`consoleErrors=0`、`failedRequests=8`。分类为 GUI materialization / review page sync gap。
- Completion 口径：延长 package inspect timeout 后 Mimo 可进入并完成 Runtime，但页面同步缺口仍未闭合；该失败不作为 P18.7-F green，也不覆盖 P18.7-E production delivery。

## 2026-05-19 09:51 Mimo performance retry runtime failure

- Mimo performance retry 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-performance-retry-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-performance-retry-20260519-codex-failure.png`。
- Runtime 没有达到 terminal readiness：task `agent-app-task-e1a8fb38-4511-475c-aaa7-6c9a6e029395` / session `agent-app-runtime-b1a536d5-b125-4694-8cd4-df59cc1e8983`，task/profile `failed`，missing `terminalReady`，`toolCallCount=0`、`artifactCount=0`、`hasWorkspacePatch=false`、`evidenceReady=true`、`hasCost=true`。
- 分类：provider/runtime terminal readiness failure，不同于前两次 completed-but-not-materialized。
- Completion 口径：这轮进一步证明 Mimo 不能作为当前 P18.7-F stable green provider；也不覆盖 P18.7-E production connector delivery。

## 2026-05-19 10:02 MiniMax normalized retry runtime-stall failure

- MiniMax normalized retry 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-minimax-normalized-retry-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-minimax-normalized-retry-20260519-codex-failure.png`。
- Runtime 没有达到 full readiness：task `agent-app-task-0fedd173-d8af-4cfb-932b-8763ce99a621` / session `agent-app-runtime-47f67b64-776b-4fb4-95d5-773b07a111f9`，`taskStatus=running`、`toolCallCount=1`、`artifactCount=0`、`hasWorkspacePatch=false`、missing `terminalReady / artifactReady / workspacePatchReady`。
- 运行面噪声：`consoleErrors=19`、`failedRequests=21`。
- Completion 口径：该失败不作为 P18.7-F green；MiniMax 仍不能替代既有 Yunwu full-flow green，也不覆盖 P18.7-E production delivery。

## 2026-05-19 10:16 Mimo host-timeout-fix materialization failure

- Mimo host-timeout-fix 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-host-timeout-fix-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-host-timeout-fix-20260519-codex-failure.png`。
- Runtime 本体完成：task `agent-app-task-0b1074b9-ebd7-4f26-ad99-7c37f005f235` / session `agent-app-runtime-793bd989-a603-4bbd-ace0-a4ba720105bf`，Mimo runStatus=`success`，`toolCallCount=1`、`artifactCount=3`、`hasWorkspacePatch=true`、`terminalReady=true`、`evidenceReady=true`、`hasCost=true`。
- 失败点仍是页面约 90s 内未 materialize workspace patch；但页面已显示“复盘待确认 / 确认复盘结论”，说明 Host timeout fix 让 Runtime 与部分页面状态推进，最终 materialization gate 仍未闭合。
- Completion 口径：该失败不作为 P18.7-F green，也不覆盖 P18.7-E production delivery。

## 2026-05-19 10:34 Mimo sample-size-fix materialization failure

- Mimo sample-size-fix 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-sample-size-fix-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-sample-size-fix-20260519-codex-failure.png`。
- Runtime 本体完成：task `agent-app-task-d0e8072c-cd1b-410f-9315-876ce64218a6` / session `agent-app-runtime-522d06b1-5e1e-4538-84ff-c3b1a6cbb996`，Mimo runStatus=`success`，`toolCallCount=1`、`artifactCount=3`、`hasWorkspacePatch=true`、`terminalReady=true`、`evidenceReady=true`、`hasCost=true`。
- 失败点仍是页面约 90s 内未 materialize workspace patch；`consoleErrors=0`、`failedRequests=13`。
- Completion 口径：Mimo 可稳定产出 review workspace patch，但页面 materialization gate 仍未对 Mimo patch 达成 green；不作为 P18.7-F green，也不覆盖 P18.7-E production delivery。

## 2026-05-19 10:52 Mimo review-input-cache runtime-stall failure

- Mimo review-input-cache 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-review-input-cache-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-mimo-review-input-cache-20260519-codex-failure.png`。
- Runtime 没有达到 full readiness：task `agent-app-task-28ec3454-16d7-4ca4-8f1d-b6d9b3e82ceb` / session `agent-app-runtime-92f331fb-17e9-4bc2-94fd-44c22c00d183`，`taskStatus=running`，missing `terminalReady / modelReady`，`toolCallCount=0`、`artifactCount=0`、`hasWorkspacePatch=false`、`hasCost=false`。
- 分类：provider/runtime stall；这轮不属于页面 materialization gap。
- Completion 口径：不作为 P18.7-F green，也不覆盖 P18.7-E production connector delivery。

## 2026-05-19 11:05 Mimo full-flow current failure

- Mimo full-flow 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-mimo-current-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-mimo-current-20260519-codex-failure.png`。
- 失败发生在第一动作 `run-scenarios`：task `agent-app-task-cba2cd13-0f6d-4aa4-9168-6d7bf76ca332` / session `agent-app-runtime-5b657eee-d97e-44d3-833c-4a80ac6ea3ba`，missing `terminalReady / modelReady`，`taskStatus=idle`、`toolCallCount=0`、`artifactCount=0`、`hasWorkspacePatch=false`、`hasCost=false`。
- 该 full-flow 未进入 `run-production / run-scripts / run-strategy / run-review`，不能作为 P18.7-F green；P18.7-F 仍以 Yunwu final / metrics 两份 full-flow summary 为可信 green。
- Completion 口径：Mimo full-flow failure 不影响既有 Yunwu green 的覆盖范围，但也不推进 P18.7-E production connector delivery。

## 2026-05-19 11:17 P18.7-E local webhook live redaction retry

- Focused local-webhook retry 产物：`.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-outbox-runtime-smoke-live-local-webhook-public-projection-redaction-20260519-codex-summary.json`。
- 结果：未进入 connector tool call，`threadRead.status=idle`、`toolCallCount=0`、无 outbox/delivery refs、local webhook `receivedRequestCount=0`，因此不是新的 delivery green。
- 新增 redaction guard 本身通过：`secretDeliveryConcreteLeaseRefNotExposed=true`、`externalDeliveryTargetNotExposed=true`，说明公共投影检查路径可执行。
- 失败后 `npm run bridge:health -- --timeout-ms 30000` 通过，DevBridge `status=ok`。
- Completion 口径：P18.7-E local webhook green 仍以前置 `p18-7-e-connector-outbox-runtime-smoke-live-local-webhook-20260519-codex-v3-summary.json` 为准；production OAuth / raw secret / external platform delivery 仍 missing。

## 2026-05-19 11:39 Yunwu current full-flow startup failure

- Yunwu current full-flow 失败产物：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-current-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-current-20260519-codex-failure.png`。
- 失败发生在第一动作 `run-scenarios`：task `agent-app-task-75352860-b01e-4bad-ad45-7fce427109ca` / session `agent-app-runtime-ceafd2da-34ba-4680-8f71-e32e97ec2f01`，missing `terminalReady / modelReady`，`taskStatus=idle`、`toolCallCount=0`、`artifactCount=0`、`hasWorkspacePatch=false`、`hasCost=false`。
- 该失败未进入 `run-production / run-scripts / run-strategy / run-review`，不能替代既有 Yunwu final / metrics green。
- Completion 口径：该 startup failure 不改变已有 P18.7-F green 的覆盖范围，也不推进 P18.7-E production delivery。

## 2026-05-19 11:55 Yunwu after-restart scenarios interrupted

- Yunwu after-restart scenarios 产物：`.lime/qc/gui-evidence/agent-apps/content-factory-scenarios-yunwu-after-restart-20260519-codex-failure.json`。
- 结果：进程在写入 success evidence 前收到 SIGTERM；`failedRequests=3`、`consoleErrors=0`。
- Completion 口径：这是进程中断，不是 runtime/provider 质量判断；不能作为 P18.7-F green，也不改变既有 Yunwu final / metrics green 的口径。

## 2026-05-19 12:50-13:11 MiniMax tail cleanup / next-round audit

- MiniMax tail-cleanup 五动作产物：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-minimax-tail-cleanup-20260519-codex-summary.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-minimax-tail-cleanup-20260519-codex.png`。
- 结果：`run-scenarios / run-production / run-scripts / run-strategy / run-review` 五动作 gate 均通过，required Skills、model usage/cost、workspace patch、full runtime ready、process visible、scene count preservation、no Host fallback、no console errors 均为 true；但 `sameIframeContext=false`，`failedRequests=9`。
- Agent Apps smoke 12:57 复核：`.lime/qc/gui-evidence/agent-apps/agent-apps-smoke-summary.json` 所有 assertions 为 true，但 summary 仍记录 `failedRequests=2`。
- 下一轮确认失败：`.lime/qc/gui-evidence/agent-apps/content-factory-next-round-confirm-minimax-20260519-codex-failure.json` 卡在 `button[data-campaign-step="copy"]` 约 5s 不可见；`.lime/qc/gui-evidence/agent-apps/content-factory-review-confirm-next-round-minimax-20260519-codex-failure.json` 卡在 `button[data-confirm-step="review_report"]` click，被 `agent-app-host-agent-run-dock` 拦截 pointer events。
- Completion 口径：MiniMax tail-cleanup 增强了 P18.7-F 业务闭环证据，但 next-round confirm 仍有 UI interaction gap；该组证据不覆盖 P18.7-E production connector delivery。

## 2026-05-19 P18.7-E production delivery proof boundary

- `cloud_overlay_outbox.rs` 现在输出 `productionDelivery` projection：webhook 2xx 只标记为 `proofLevel=host_managed_webhook_receipt`，local worker receipt 只标记为 `local_cloud_overlay_worker_receipt`，两者均保持 `productionPlatformDelivered=false`。
- Projection 明确保留 `oauthHandshakeRequired=true`、`rawSecretMaterialAdapterRequired=true`、`nextRequired=production_connector_delivery_adapter`，避免 local/remote webhook proof 被误升格为 Notion/Slack/Feishu 等生产平台真实送达。
- `scripts/agent-app-connector-outbox-smoke.mjs` 新增 `--expect-production-platform-delivered`，live 默认 `false`，summary 记录 `productionDeliveryStatus / productionDeliveryProofLevel / productionDeliveryNextRequired / productionPlatformDelivered`。
- 验证通过：`rustfmt --edition 2021 --check ...cloud_overlay_outbox.rs ...tests.rs`、`node --check "scripts/agent-app-connector-outbox-smoke.mjs"`、`git diff --check -- ...P18.7-E 写集...`、`cargo test --manifest-path "src-tauri/Cargo.toml" -p lime agent_app_connector_cloud_overlay_can_deliver_to_host_managed_webhook --lib`、`cargo test --manifest-path "src-tauri/Cargo.toml" -p lime agent_app_connector --lib`、`cargo check --manifest-path "src-tauri/Cargo.toml" -p lime --lib --message-format short`。
- Completion 口径：这是防止“假完成”的 audit guard，不是 OAuth handshake、raw secret material adapter 或 production platform delivery 本体；整体目标仍不能标记 complete。

## 2026-05-19 13:30 MiniMax dock-fix retry materialization audit

- Dock fix retry 产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-confirm-next-round-minimax-dock-fix-20260519-codex-failure.json`，截图：`.lime/qc/gui-evidence/agent-apps/content-factory-review-confirm-next-round-minimax-dock-fix-20260519-codex-failure.png`。
- 失败分类已经从上一轮的 `agent-app-host-agent-run-dock` pointer interception 前移：本轮 run-review Runtime 完成，task `agent-app-task-213983e7-30a4-48e8-8f5f-b3d0436824ae` / session `agent-app-runtime-3409eedf-a07e-4d56-8e9d-8a714306fcd9`，`toolCallCount=1`、`artifactCount=3`、`hasWorkspacePatch=true`、`terminalReady=true`、`evidenceReady=true`、`hasCost=true`。
- 新失败点是页面 materialization：90s 后 review dashboard 仍显示 `复盘待录入 / 样本量0条 / 看完率0%`，但页面底部已出现 `确认进入下一轮` 和 review report 摘要；说明 Runtime patch 写回到部分确认态，review metrics/state 没有完成页面物化。
- 只读归因：用该 failure 的 `workspacePatch.reviewReport` 加 E2E review input 调用 content-factory normalizer，可得到 `totalRows=240`、`avgCompletionRate=32`、`avgSearchShare=28`、`avgConversionRate=3.5`、`reuseRatio=6`，因此当前缺口更像 review input 传递 / materialize API 写回 / 页面刷新链路，而不是模型没有生成 review patch。
- Completion 口径：该 retry 证明 dock 遮挡问题已不再是当前首要失败点，但 next-round confirm 仍没有新的 green；不影响既有 P18.7-F MiniMax tail-cleanup / Yunwu full-flow green，也不覆盖 P18.7-E production connector delivery。

## 2026-05-19 14:11 MiniMax delivery-confirm next-round green

- 新 green 产物：`.lime/qc/gui-evidence/agent-apps/content-factory-review-confirm-next-round-minimax-delivery-confirm-20260519-codex-summary.json`，截图为同名前缀 `.png`。
- 覆盖动作：`run-review` 与 `run-production-next-round`；两个 action 均 `fullRuntimeReady=true`、`workspacePatchReady=true`、required Skills invoked、usage/cost ready、process visible、no Host fallback、`consoleErrorCount=0`。
- 页面物化：`run-review` 后第1轮复盘可见 `样本量240条 / 看完率32% / 搜索占比28% / 转化率3.5% / 素材复用1:6 / 确认复盘结论`；`run-production-next-round` 后第2轮内容可见 `第2轮内容20/20条`、`整理草稿20/20条`、`脚本和图片需求6条脚本/5条图片需求`。
- 支撑验证：`/Users/coso/Documents/dev/ai/limecloud/content-factory-app: npm test -- tests/core.test.mjs tests/api.test.mjs` 实际执行 package 测试入口，`109/109` passed，覆盖 `metricsSummary.sampleSize` 与 review input fallback 的 normalizer / API / UI 回归。
- 注意事项：summary 仍为 `sameIframeContext=false` 且 `failedRequests=6`，主要是 DevBridge events / sample load 收尾 abort；本证据可关闭 next-round confirm / review materialization gap，但不等于 P18.7-E production connector delivery。
- Completion 口径：P18.7-F 内容工厂业务闭环现在有 full-flow、review materialization 和 next-round production green；整体目标仍不能 complete，因为真实 Connector OAuth、raw secret material adapter、生产平台送达和 GUI 送达证明仍 missing。

## 2026-05-19 14:22 production connector credential preflight

- 只读盘点环境变量名，不打印任何值；命中的 connector / secret 类名称只有 `FACTORY_API_KEY`，未发现 `WEBHOOK / NOTION / SLACK / FEISHU / LARK / OAUTH / CONNECTOR / LIME_AGENT_APP` 类生产送达凭证。
- 只读盘点本仓、`~/.config`、`~/.lime` 的候选文件名；发现的是 GCloud / Databricks / 文档 / 脚本 / 历史 QC 文件，没有可直接用于 Notion / Slack / Feishu production connector delivery 的明确凭证文件。
- 已把该检查固化为 `scripts/agent-app-connector-production-preflight.mjs` 与 `scripts/lib/agent-app-connector-production-preflight-core.mjs`；当前 evidence 为 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-preflight-20260519-codex.json`，`status=blocked`、`checkedSecretCount=10`、`missing=10`，且只记录 secret 名称 / category，不序列化 token、endpoint 或 credential material。
- `--check` 阻塞证据已落盘到 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-preflight-check-20260519-codex.json`；当前 `status=blocked` 且命令返回 `exit=1`，可作为 release/completion gate 的机器阻断信号。
- 14:42 追加防假 ready 加固：remote webhook source 现在必须是 `https://` 且不能指向本地 host；`*_URL_FILE` 必须指向可读文件，文件内容同样只用于内部校验，不会写入 JSON 或日志。无效来源只输出 secret 名称、source type 和错误码，避免不存在的 file source、`localhost` 或普通字符串绕过 production gate。
- 14:47 追加 smoke 输入防泄漏：`scripts/agent-app-connector-outbox-smoke.mjs --external-delivery-webhook-url` 只允许本地 / 非敏感目标；远端 `https://` webhook 必须通过 `--external-delivery-webhook-url-env` 或 `--external-delivery-webhook-url-file` 注入，避免 production target URL 出现在 shell history / process args。
- 14:50 新增 production delivery gate：`scripts/agent-app-connector-production-delivery-gate.mjs` 会同时要求 preflight ready 与真实 production delivery evidence ready；`productionPlatformDelivered=true` 还必须配套 `production_connector_delivery_adapter` / production platform receipt 类 proofLevel，明确拒绝 `host_managed_webhook_receipt` 与 `local_cloud_overlay_worker_receipt`。当前输出 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-delivery-gate-20260519-codex.json` 为 `blocked / exit=1`，缺 `production_connector_preflight_not_ready` 与 `production_delivery_evidence_missing`。
- 14:55 已把两个 gate 接入 npm 入口：`npm run agent-app:connector-production-preflight -- --check ...` 与 `npm run agent-app:connector-production-delivery-gate -- --check ...`，两者当前均按预期返回 `exit=1`，让后续 release / 手工收口不用记裸脚本路径。
- 15:01 Rust ToolRuntime 已补 Host-managed production delivery receipt handoff seam：`readiness.rs` 只从 `agent_app_tool_execution.internalRequest` 读取 `secretDelivery.productionDelivery`，要求 `binding=host_managed`、`productionPlatformDelivered=true`、production adapter / platform receipt 类 `proofLevel`、`targetExposed=false`、`credentialMaterialExposed=false`、`tokenExposed=false`，且拒绝 `target/targetUrl` 与 raw secret material；`cloud_overlay_outbox.rs` 会把该 receipt 投影为 `production_platform_delivered / production_connector_delivery_complete`。这只是接收 Host/Cloud 真实 adapter receipt 的安全 handoff seam，不等于当前已有真实外部平台送达。
- 15:21 新增 remote webhook production delivery adapter CLI：`npm run agent-app:connector-production-webhook-delivery` 只从 env/file 读取 production webhook URL，不提供 CLI URL 参数，默认 dry-run 不发网络；必须显式 `--send` 才 POST。dry-run 证据 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-webhook-delivery-dry-run-20260519-codex.json` 为 `blocked / send_not_requested / productionPlatformDelivered=false`，只记录 target hash 与 payload preview，不序列化 URL、token、credential material 或 response body。
- 15:25 `--send` 前新增 preflight 硬门禁：webhook production adapter 现在默认读取 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-preflight-check-20260519-codex.json`，只有 matching connector 的 preflight `ready=true / productionPlatformDeliveryReady=true / missingSecrets=[]` 才允许 POST；当前环境执行 `--send` 会在网络请求前失败，错误为 `production preflight must be ready before --send`。
- 15:31 已生成 webhook-only current gate evidence：`.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-preflight-webhook-current-20260519-codex.json` 为 `blocked`，只缺 `LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL`；配合同一 dry-run delivery evidence 跑 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-delivery-gate-webhook-current-20260519-codex.json` 仍 `blocked / exit=1`，缺 `production_connector_preflight_not_ready` 与 `production_platform_delivered_false`。这把下一步最小可执行路径收窄为：提供 webhook URL secret source，然后显式 `--send`。
- 15:34 复查当前 shell 环境变量名，未发现 `LIME_AGENT_APP_CONNECTOR_WEBHOOK` / `WEBHOOK` / `NOTION` / `SLACK` / `FEISHU` / `LARK` / `OAUTH` / `CONNECTOR` 类 production delivery secret source；未读取或打印任何值。Webhook-only preflight 仍 `blocked / exit=1`，因此不能执行 `--send`。
- 15:36 复查 `.gitignore`：仓库已忽略 `.lime/`，因此后续可把本地 webhook URL secret file 放到 `.lime/secrets/agent-app-connector-webhook-url`，再用 `LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL_FILE=.lime/secrets/agent-app-connector-webhook-url` 跑 webhook-only preflight；`git check-ignore -v ".lime/secrets/agent-app-connector-webhook-url"` 返回 `.gitignore:132:.lime/`，确认该文件不会进入 git status。不要把 URL 写进 CLI 参数、文档或 tracked 文件。
- 15:43-15:44 复跑当前 production delivery gate：`npm run agent-app:connector-production-preflight -- --connector webhook --check --output .lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-preflight-webhook-current-rerun-20260519-codex.json` 返回 `exit=1`，仍只缺 `LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL`；`npm run agent-app:connector-production-delivery-gate -- --preflight ...current-rerun... --delivery ...webhook-delivery-dry-run... --check --output .lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-delivery-gate-webhook-current-rerun-20260519-codex.json` 返回 `exit=1`，缺 `production_connector_preflight_not_ready` 与 `production_platform_delivered_false`。
- 15:43-15:45 复跑无凭证安全验证：`node --check` 覆盖 preflight / delivery gate / webhook delivery / outbox smoke 脚本，`npm test -- "scripts/lib/agent-app-connector-production-preflight-core.test.mjs" "scripts/lib/agent-app-connector-production-delivery-gate-core.test.mjs" "scripts/lib/agent-app-connector-production-webhook-delivery-core.test.mjs"` 为 15 tests passed；`rustfmt --edition 2021 --check ...connector_tools...`、`cargo test --manifest-path "src-tauri/Cargo.toml" -p lime agent_app_connector --lib` 为 14 tests passed，`cargo check --manifest-path "src-tauri/Cargo.toml" -p lime --lib --message-format short` 通过。Rust 命令使用独立 `CARGO_TARGET_DIR=$HOME/Library/Caches/lime-codex-target-p18e`，未抢默认 Tauri dev target。
- 15:49 追加 GUI projection 硬门禁：`scripts/agent-app-connector-production-delivery-gate.mjs` 新增 `--gui-evidence <path>`，最终 ready 现在同时要求 preflight ready、production delivery evidence ready，以及 GUI evidence 证明 production delivery 已回到 Agent App UI/task events；没有 GUI evidence 时新增 missing code `production_gui_evidence_missing`。验证：`node --check "scripts/agent-app-connector-production-delivery-gate.mjs"`、`node --check "scripts/lib/agent-app-connector-production-delivery-gate-core.mjs"`、`npm test -- "scripts/lib/agent-app-connector-production-delivery-gate-core.test.mjs"` 6 tests passed、三组 production connector Node tests 合计 17 tests passed；新增 CLI ready-path 覆盖 `--gui-evidence` 可被命令入口读入并返回 `exit=0`。当前 webhook-only gate 复跑产物 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-delivery-gate-webhook-gui-gate-20260519-codex.json` 仍 `blocked / exit=1`，缺 `production_connector_preflight_not_ready`、`production_platform_delivered_false`、`production_gui_evidence_missing`。

- 15:54 追加 connector 一致性门禁：production delivery gate 现在会把 preflight connector scope、delivery evidence connector id、GUI evidence connector id 绑定到同一 connector；`preflight.connector` 必须是 `all` 或匹配 delivery connector，GUI evidence 也必须匹配 delivery connector，避免用 A connector 的凭证预检拼 B connector 的送达证据。新增测试覆盖 mismatch blocked 与 CLI ready-path；`npm test -- "scripts/lib/agent-app-connector-production-delivery-gate-core.test.mjs"` 为 7 tests passed，三组 production connector Node tests 合计 18 tests passed。当前 webhook-only gate 复跑产物 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-delivery-gate-webhook-connector-gate-20260519-codex.json` 仍 `blocked / exit=1`，但 connector 匹配链路已显示 `preflightCoversDelivery=true`、`delivery.connector=webhook`；仍缺真实 secret、真实 delivered 与 GUI evidence。

- 15:56 追加 `nextRequired` 完成态门禁：delivery evidence 即使声明 `productionPlatformDelivered=true` 且 proofLevel 合法，也必须同时给出 `nextRequired=production_connector_delivery_complete` 才能 ready；否则返回 `production_delivery_next_required_not_complete`。新增测试覆盖 delivered=true 但仍要求 `operator_confirm_send` 的假完成证据被 blocked；`npm test -- "scripts/lib/agent-app-connector-production-delivery-gate-core.test.mjs"` 为 8 tests passed，三组 production connector Node tests 合计 19 tests passed。当前 dry-run gate 复跑产物 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-delivery-gate-webhook-next-required-gate-20260519-codex.json` 仍 `blocked / exit=1`，delivery summary 明确 `nextRequired=operator_confirm_send / nextRequiredComplete=false`。

- 15:59 追加 production delivery / GUI redaction 门禁：delivery gate 现在要求 delivery evidence 与 GUI evidence 都能证明 target URL、credential material、token 未外露；delivery 可用 `targetExposed=false / credentialMaterialExposed=false / tokenExposed=false`，GUI 可用 `externalDeliveryTargetNotExposed / credentialMaterialNotExposed / tokenNotExposed` 等断言。新增测试覆盖 target / credential material 外露时 blocked；`npm test -- "scripts/lib/agent-app-connector-production-delivery-gate-core.test.mjs"` 为 9 tests passed，三组 production connector Node tests 合计 20 tests passed。当前 dry-run gate 复跑产物 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-delivery-gate-webhook-redaction-gate-20260519-codex.json` 仍 `blocked / exit=1`；delivery redaction 已为 true，但仍缺 preflight ready、productionPlatformDelivered、nextRequired complete 和 GUI evidence。

- 16:01 同步 delivery gate CLI 帮助与操作面测试：`--help` 现在明确写出 ready 需要 preflight / delivery / GUI connector 一致、`productionPlatformDelivered=true`、`nextRequired=production_connector_delivery_complete`、production proofLevel、GUI projection，以及 target / credential / token redaction；新增 CLI help 测试避免 operator 文案落后于门禁实现。`npm test -- "scripts/lib/agent-app-connector-production-delivery-gate-core.test.mjs"` 为 10 tests passed，三组 production connector Node tests 合计 21 tests passed。

- 16:03 delivery gate CLI 现在在 blocked 时打印 `missingCodes=...`，避免 operator 只能看到 missing 数量而需要打开 JSON 才知道缺口。复跑 webhook-only gate 输出 `missingCodes=production_connector_preflight_not_ready,production_platform_delivered_false,production_delivery_next_required_not_complete,production_gui_evidence_missing`，对应产物 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-delivery-gate-webhook-missing-codes-20260519-codex.json`。验证：`node --check "scripts/agent-app-connector-production-delivery-gate.mjs"`、`npm test -- "scripts/lib/agent-app-connector-production-delivery-gate-core.test.mjs"` 10 tests passed，三组 production connector Node tests 21 tests passed。

- 16:05 production preflight CLI blocked 输出现在会同时列出 alias 名称，例如 webhook-only 当前输出 `missing=webhook:LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL aliases:LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL_FILE`，方便 operator 直接选择 env 或 file secret source，仍不打印 URL 或 secret value。验证：`node --check "scripts/agent-app-connector-production-preflight.mjs"`、`npm test -- "scripts/lib/agent-app-connector-production-preflight-core.test.mjs"` 7 tests passed，三组 production connector Node tests 合计 22 tests passed；复跑产物 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-preflight-webhook-alias-output-20260519-codex.json` 仍 `blocked / exit=1`。

- 16:07 production preflight CLI `--help` 现在明确写出 webhook 可用 `LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL` 或 `LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL_FILE`，且 file/env 内容必须是非本地 `https` URL；新增 help 测试防止 operator 文案与 preflight source 规则漂移。验证：`node --check "scripts/agent-app-connector-production-preflight.mjs"`、`npm test -- "scripts/lib/agent-app-connector-production-preflight-core.test.mjs"` 8 tests passed，三组 production connector Node tests 合计 24 tests passed。

- 16:09 webhook production delivery CLI 追加 help 回归：`--help` 测试覆盖 `--webhook-url-env` / `--webhook-url-file`、`--send`、`Required ready preflight JSON before --send` 与 `No CLI URL option is provided`，确保 operator 文案继续强调 env/file target 与 send 前 preflight gate。验证：`npm test -- "scripts/lib/agent-app-connector-production-webhook-delivery-core.test.mjs"` 6 tests passed，三组 production connector Node tests 合计 24 tests passed。
- 验证通过：`node --check "scripts/agent-app-connector-production-preflight.mjs"`、`node --check "scripts/lib/agent-app-connector-production-preflight-core.mjs"`、`node --check "scripts/agent-app-connector-production-delivery-gate.mjs"`、`node --check "scripts/lib/agent-app-connector-production-delivery-gate-core.mjs"`、`node --check "scripts/agent-app-connector-production-webhook-delivery.mjs"`、`node --check "scripts/lib/agent-app-connector-production-webhook-delivery-core.mjs"`、`npm test -- "scripts/lib/agent-app-connector-production-preflight-core.test.mjs"`（8 tests passed）、`npm test -- "scripts/lib/agent-app-connector-production-delivery-gate-core.test.mjs"`（10 tests passed）、`npm test -- "scripts/lib/agent-app-connector-production-webhook-delivery-core.test.mjs"`（6 tests passed；三组 Node tests 合计 24 tests passed）、`cargo test --manifest-path "src-tauri/Cargo.toml" -p lime agent_app_connector --lib`（14 tests passed）、`cargo check --manifest-path "src-tauri/Cargo.toml" -p lime --lib --message-format short`。

- 16:12 补跑仓库契约门禁：`npm run test:contracts` 通过，覆盖 `check:agent-runtime-clients`、command contracts、harness contracts、modality runtime contracts 与 harness cleanup report check；这证明新增 production connector npm/script 入口没有破坏现有命令契约和 harness contract，但不替代真实 production delivery evidence。

- 16:13 补跑版本一致性门禁：`npm run verify:app-version` 通过，确认本轮 `package.json` 新增 npm script 没有造成 app version 事实源漂移；当前版本一致为 `1.44.0`。该校验不覆盖 production delivery，只作为 package 变更的仓库卫生证据。

- 16:14 补跑 DevBridge health：当前已有 `target/debug/lime` 运行，`npm run bridge:health -- --timeout-ms 30000` 通过，`http://127.0.0.1:3030/health` 在 79ms 返回 `status=ok`。该检查证明当前桌面桥接可连接，但不覆盖 production connector delivery；同轮复查 `.lime/secrets/agent-app-connector-webhook-url` 仍缺失。

- 16:15 复跑 production connector 脚本整体验证：`node --check` 覆盖 preflight / delivery gate / webhook delivery 三组 CLI、core 与 test 文件，随后三组 Node tests 24 passed。该复核覆盖最近几轮 missingCodes、alias 输出、help 文案、connector / GUI / redaction / nextRequired 门禁改动；仍不代表 production delivery 完成。

- 16:16 最终阻塞复核：当前 shell 仍没有 `LIME_AGENT_APP_CONNECTOR_WEBHOOK` / `WEBHOOK` / `NOTION` / `SLACK` / `FEISHU` / `LARK` / `OAUTH` / `CONNECTOR` 类 secret source 名称，`.lime/secrets/agent-app-connector-webhook-url` 仍不存在；`git check-ignore -v ".lime/secrets/agent-app-connector-webhook-url"` 确认该路径由 `.gitignore:132:.lime/` 忽略，可安全作为本地 file secret source。复跑 webhook-only preflight 产物 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-preflight-webhook-final-blocked-20260519-codex.json` 仍 `blocked / exit=1`，缺 `LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL`，可用 alias 为 `LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL_FILE`。下一步已收敛为唯一外部动作：提供真实非本地 `https` webhook URL secret source 后，才能执行显式 `--send` 与 GUI evidence gate。

- 16:17 本地 secret 目录已准备但未写入 secret：创建 `.lime/secrets/` 并设置为 `700`，`git check-ignore -v ".lime/secrets/agent-app-connector-webhook-url"` 仍确认该目标文件由 `.gitignore:132:.lime/` 忽略；真实 secret 文件本身仍不存在，避免把 placeholder 当成可用凭证。

- 16:18 secret 目录准备后复跑 webhook-only preflight：`.lime/secrets/` 存在且权限为 `700`，但 `.lime/secrets/agent-app-connector-webhook-url` 仍缺失或为空；产物 `.lime/qc/gui-evidence/agent-apps/p18-7-e-connector-production-preflight-webhook-after-secret-dir-20260519-codex.json` 仍 `blocked / exit=1`，继续缺 `LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL` 或 alias `LIME_AGENT_APP_CONNECTOR_WEBHOOK_URL_FILE`。
- Completion 口径：当前环境不具备真实 production connector delivery proof 条件；不能继续用 local webhook、Cloud Overlay local worker、remote webhook 2xx 或内容工厂 next-round green 代替 production platform delivery。
