# P1 Quality Fixture Matrix

> 状态：P1-8 done；P2 Tool / Approval / Sandbox combo evidence recorded；P2 Context packet consumer done / context evidence export done / session compaction sidecar+item done / memory-sidecar-skeleton done / memory-regression done / media-input-reference-skeleton done / media-gui-preview-smoke validated / binary-sidecar-read pending；P2 Plugin App Center projection + detail skills/tools/MCP consumer done / skill-prompt-runtime-consumer validated / mcp-guidance-runtime-consumer validated / mcp-target-status-tool-listing-skeleton done / mcp-prepare-execution-api done / mcp-prepare-trigger-skeleton done / mcp-runtime-callable-proof pending；P2 Realtime / Media runtime content owner / message delta parser done / app-server-agent-message-contentparts-skeleton validated / protocol-generated / projection-hydrate / message-list-renderer skeleton done / media-preview-target skeleton done / renderer-typecheck green / gui-media-smoke validated / binary-sidecar-read pending；Codex fifth signal config-warning-gui-done / provider-safety-gui-and-fixture-green / desktop-env-overlay-watch
> 更新时间：2026-07-07
> 目标：把 P1 Codex 原点式对齐的验证证据收敛成一张可执行矩阵，避免后续继续靠聊天或临场判断决定“该跑什么”。

## 1. 结论

P1 质量口径分成五类证据：

| 证据类型 | 证明什么 | 典型入口 |
| --- | --- | --- |
| protocol | App Server method / schema / generated client / contract 没漂移 | `npm run check:protocol-types`、`npm run test:contracts`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server-protocol ...` |
| runtime | Thread / Turn / Item、RuntimeCore、agent domain 行为真实可执行 | `cargo test --manifest-path "lime-rs/Cargo.toml" -p <crate> <filter>`、`npm run test:rust:related -- <paths...>` |
| projection | runtime event / read model / GUI projection 不靠 UI 文案猜测 | 定向 `npx vitest run ...`、Rust projection tests |
| GUI | 桌面 GUI 主路径真实可启动、可交互 | `npm run verify:gui-smoke`、必要时 Playwright / Electron smoke |
| governance | current / compat / deprecated / dead 边界没有回流 | `npm run governance:legacy-report`、`src/lib/governance/*.test.ts` |

当前全局状态：

- `npm run test:contracts` 已在 2026-07-06 完整通过；之前记录的 agent/tool orchestrator 与 WebSearch preflight contract blocker 已被当前工作树收口。
- 当前进程 2026-07-06 已复核：`lime-rs/crates/agent/**`、App Server runtime / read model / projection、RuntimeCore 仍是并行热区；第三十八刀第一至第八切片已在窄写集中完成 request/tool consumer 主链，后续不再把 token-aware truncation 当作未接线缺口。
- 当前进程 2026-07-06 已复核：Codex `db887d03e1f907467e33271572dffb73bceecd6b` 与 opencode `17166b271fb9d7bf7128f0e63732dde0c10dd963` worktree 均 clean；第三十八刀可继续引用本文件下方 source anchor，不需要重新临场找 Codex 消费点。
- 当前进程 2026-07-06 已完成第三十八刀第一至第十五补充切片：`lime-agent` typed owner、live process `bytes` truncation、native shell gate、responses/tool-call transport、provider wire-shape、Aster compat fail-closed、Aster OpenAI Responses payload/header consumer、native tool inventory / apply_patch gate、live execution fail-closed guard、token-aware final output formatter、contract gate、unified_exec alias guard、tool orchestrator output formatter、Aster `ToolResponse -> ToolEnd` compat formatter、current user-message input-modality fail-closed gate、reasoning policy request fallback consumer、context policy typed owner 与 App Server `lime_runtime` turn-context projection 已有定向证据；第三十八刀第十七切片已把 context policy 接到 App Server prompt context packet budget，代码、断言与 Rust 定向测试均已通过；第十八切片已由 Aster `prompt_input_modalities.rs` current owner 接上 history-level input modality filtering，并通过 vendor 定向测试；第十九切片已把 reasoning-output summary / verbosity 接入 provider request output-control，并通过 model-provider / lime-agent / Aster 定向测试；第二十切片纠偏已把 `lime_runtime.context_policy` 接入 `protocol_context_projection.rs`，在缺少显式 `agentui_context.memory_budget` 时生成 `AgentTurnContextSummary.memory_budget`，并对 auto compact disabled / tighter limit / remaining tokens 计算加了单元覆盖；第二十切片补充新增 `asterContextPolicyBoundary.test.ts`，禁止 context / auto-compact 新规则回流 vendor Aster；第二十一切片已让 `protocol_context_projection.rs` 从 `lime_runtime.context_usage` / `history_usage` / `token_usage` 读取真实 history usage，并按 selected model auto compact limit 标记 `auto_compact_due`；第二十二切片已新增 `project_turn_context_summary_with_active_context_tokens(...)` handoff helper；第二十三切片已让 session execution runtime 用 session usage `input_tokens` 调用 handoff helper；第二十四切片已把 actual auto compact trigger owner 落到 App Server RuntimeCore pre-turn `context_auto_compaction.rs`；MCP / Aster extension output 审计已确认 App Server `mcpTool/call*` 是 current control plane、不应套 model output truncation。
- 当前进程 2026-07-06 再次复核 scoped status：agent / app-server runtime_backend / app-server runtime / RuntimeCore / frontend model-governance 仍有并行脏热区；本轮仅补 `scripts/check-app-server-client-contract.mjs` 的 current owner 文件列表，让 contract guard 扫描到 `runtime_backend/tool_process_runtime_metadata.rs` 中的 `parse_tool_arguments(arguments)`，不改 Rust 行为。
- 当前进程 2026-07-06 并行刷新：`git status --short -- "internal/research/refactor/v1" "lime-rs/crates/agent/src/agent_tools" "lime-rs/crates/app-server/src/runtime" "src/components/agent/chat/hooks" "src/lib/governance"` 仍显示 `agent_tools/**`、App Server `runtime/**`、chat hooks 和 governance 测试为脏区，并有多个未跟踪相邻源码；本轮只更新 v1 文档，不接管这些源码。
- 当前进程 2026-07-06 已完成一次真实 upstream range diff：[upstream-diff-2026-07-06.md](./upstream-diff-2026-07-06.md)；随后又在 [upstream-diff-2026-07-06-p3-loop.md](./upstream-diff-2026-07-06-p3-loop.md) 完成第二次和第三次 range check。2026-07-07 第四次 range check 见 [upstream-diff-2026-07-07.md](./upstream-diff-2026-07-07.md)，其中 interleaved response items 已被第五次回滚覆盖；第五次 range check 见 [upstream-diff-2026-07-07-p3-fifth.md](./upstream-diff-2026-07-07-p3-fifth.md)：Codex 新增 configWarning owner 收敛、safety buffering `retry_model` adopt-now 信号，conditional dotenv 记为 desktop-adapt/watch，opencode 无新增。P3 已有连续 repeat-check 证据，但仍需证明跨周 cadence 或脚本化循环；第五次采纳信号的施工口径已沉淀到 [p2-codex-fifth-signal-handoff.md](./p2-codex-fifth-signal-handoff.md)。
- 当前进程 2026-07-06 已完成 P2 Tool lifecycle App Server owner 代码刀：`ToolLifecycleSnapshot` / approval action / sandbox decision typed gate 已进入 `runtime/tool_lifecycle.rs`，`sandbox.blocked` / `permission.denied` 会阻断后续 `tool.output.delta` / `tool.result`，只允许 `tool.failed` 收尾；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server tool_lifecycle -- --nocapture` 通过，23 个相关测试全绿；这证明 App Server runtime event lifecycle owner 通过，不代表 `lime-agent` tool execution typed owner 完成。
- 当前进程 2026-07-06 已完成 P2 Tool lifecycle `lime-agent` event construction owner、active state gate 第一刀、approval pending transition 第一刀、process lifecycle correlation 第一刀、terminal metadata correlation 第一刀、approval resolved terminal transition 第一刀、Evidence export correlation 第一刀与 action id / tool id 解耦前置刀：`agent_tools/tool_lifecycle.rs` 新增 `ToolExecutionLifecycleSnapshot`、`ToolApprovalActionSnapshot`、`ToolApprovalResolutionSnapshot`、`ToolSandboxDecisionSnapshot`、`ToolExecutionTerminalSnapshot`、`ToolProcessLifecycleSnapshot`、`ToolExecutionOutputDeltaSnapshot`、`ToolExecutionLifecycleEvents`，`tool_orchestrator.rs` 的 `ToolStart`、`ActionRequired`、`ToolEnd`、live process lifecycle / stdout stderr delta 构造已委托给 typed owner，approval-required / sandbox-blocked tool 不再发出 `ToolOutputDelta`，terminal 后重复 outcome 不再发事件；approval-required outcome 只发 `ActionRequired` 并停在 `AwaitingApproval`，不再抢先发失败 `ToolEnd`；approval resolved helper 显式把 `ToolApprovalResolutionSnapshot` 与 terminal outcome 组合后释放 terminal gate，批准后允许 terminal/output，拒绝后不允许成功 terminal；process lifecycle / output delta metadata 与 terminal `ToolEnd` metadata 稳定携带 `toolCallId`、`toolId`、`tool_id`，且不覆盖上游已有 correlation；App Server `coding_evidence_summary` 已输出 `actionRequestIds` / `actionToolCallIds`；action required / resolved metadata 已区分 `actionId` / `requestId` 与 `toolCallId` / `toolId`，并用 tool id 释放 pending lifecycle；`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent tool_lifecycle -- --nocapture` 13 tests passed，`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent tool_orchestrator -- --nocapture` 15 tests passed，`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server export_evidence_pack_includes_coding_snapshot_artifacts -- --nocapture` 1 test passed；这证明 event construction owner、active state gate、approval pending transition、process correlation、terminal correlation、approval resolved terminal transition、Evidence export correlation 第一刀与 action/tool identity split 通过，不代表真实 Aster pending tool execution future 已恢复。
- 当前进程 2026-07-06 已完成 RuntimeCore `action/respond` lifecycle guard：新增 `respond_action_infers_tool_id_and_unblocks_pending_tool_result`，通过正式 `core.respond_action(...)` approve 缺少 tool id 的 action response，断言 `action.resolved` 自动回填 pending action 的 `toolCallId`，并允许后续 `tool.result` 通过 App Server lifecycle guard；`rustfmt --edition 2021 --check "lime-rs/crates/app-server/src/runtime/tests/external_events/actions.rs"` 与 `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server respond_action_infers_tool_id_and_unblocks_pending_tool_result -- --nocapture` 已通过，最新复跑仍为 1 passed。这证明 RuntimeCore API 层 lifecycle guard 通过，不代表真实 Aster pending tool execution future 已端到端恢复。
- 当前进程 2026-07-06 第十一刀复跑已确认 RuntimeState/Aster pending resume 当前为 green：`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent confirm_tool_action_resumes_pending_aster_tool_execution -- --nocapture` 通过，结果为 1 passed。
- 当前进程 2026-07-06 第十五刀 App Server RuntimeBackend bridge 回归已通过：补齐 `coding_event_projection.rs` missing import 后，`CARGO_TARGET_DIR="/tmp/lime-codex-p2-tool-target-bridge" CARGO_BUILD_JOBS=4 cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server respond_action_tool_confirmation_resumes_pending_aster_tool_future -- --nocapture` 结果为 `1 passed`；该证据证明 `ExecutionBackend::start_turn` 能发出 `ActionRequired(tool_confirmation)`，并通过 `ExecutionBackend::respond_action(ToolConfirmation)` 释放同一 pending Aster tool future。
- 当前进程 2026-07-06 已重新执行 `npm run test:contracts`：protocol types 无漂移，App Server client / command / harness / modality / scripts / electron release / harness cleanup / docs boundary 全部通过；首次失败点是 contract 文件列表未覆盖 `tool_process_runtime_metadata.rs`，补 current owner 文件列表后 `node scripts/check-app-server-client-contract.mjs` 通过 287 checks，完整 contract gate 已绿。
- 当前进程 2026-07-06 已重新执行 `npm run smoke:agent-runtime-current-fixture`：history/cache、stream final_done、Electron fixture guard、Coding Workbench、Claw 图片 / 画图 / 停止继续 / Plan history、Skills Runtime、Multi-Agent、MCP structuredContent、Expert Skills、内容工厂 Article Editor 等 current GUI/runtime fixture 全部通过。
- 当前进程 2026-07-06 已完成 P2 Plugin / Skills / MCP 第一代码刀：新增 `plugin_packages/runtime_capabilities.rs` typed owner，从 projected plugin manifest 生成 `runtimeCapabilities` snapshot，覆盖 plugin identity/version、skill metadata、workflow-scoped prompt injection policy、tool binding 与 MCP binding；`plugin_manifest.rs` 只在 manifest projection 末尾消费该 owner，不把 App Center UI 当 runtime truth，不接管 App Server runtime/backend 热区。`CARGO_TARGET_DIR="/tmp/lime-codex-p2-plugin-capabilities" CARGO_BUILD_JOBS=4 cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime_capabilities -- --nocapture` 通过，4 tests。随后前端 App Center projection consumer 已完成：`normalizeManifest(...)` 保留 `runtimeCapabilities`，`projectApp(...)` 优先用 snapshot 生成 skill/tool/MCP capability projection，legacy `skillRefs/toolRefs` 只作为缺 snapshot fallback；schema gate 已补 snapshot identity guard；App Center detail 技能展示已迁到 `projection.skillRequirements` consumer，旧 `manifest.skillRefs` 只在 projection 缺失时 fallback；`npx vitest run "src/features/plugin/projection/projectApp.test.ts" "src/features/plugin/schema/schemaGate.test.ts" "src/features/plugin/ui/pluginDetailDeclarations.unit.test.ts" "src/features/plugin/ui/PluginsPage.test.tsx"` 通过 42 tests，`npm run typecheck` 通过。2026-07-07 继续补 detail tools/MCP consumer：`buildDetailTools(...)` 优先消费 `projection.toolRequirements`，`buildDetailMcpBindings(...)` 优先消费 `projection.runtimeCapabilities.mcpBindings`，`PluginDetailRuntimeSections` 接管 subagent / skill / tool / MCP detail sections；`npx vitest run "src/features/plugin/ui/pluginDetailDeclarations.unit.test.ts" "src/features/plugin/ui/PluginDetailRuntimeSections.unit.test.tsx" "src/features/plugin/ui/PluginsPage.test.tsx"` 通过 39 tests，`npm run typecheck` 通过。2026-07-07 已补 App Server runtime consumer：`plugin_runtime_context` 消费 plugin activation / harness runtime capabilities，Agent Skills selection 注入 workflow-scoped skill body，session prompt 渲染 `<plugin_runtime_capabilities>`，telemetry 输出 `plugin_runtime_capabilities` 事件；MCP binding 已由 `tool_inventory` 归一化为 `plugin_mcp_targets` 并消费 MCP status / tool snapshot 输出 target runtime status 与 `toolListRequest` skeleton；前端 API `mcpApi.executePrepareRequests(...)` 已能顺序执行 candidate import / start / listForContext；Workspace Harness 已接手动 prepare trigger 和执行后 inventory refresh skeleton。`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server plugin_runtime -- --nocapture` 补跑通过，8 tests；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server tool_inventory --lib` 补跑通过，6 tests；`npx vitest run "src/lib/api/mcp.test.ts" "src/lib/api/mcp.failClosed.test.ts"` 通过，18 tests；`npx vitest run "src/components/agent/chat/workspace/useWorkspaceHarnessInventoryRuntime.test.tsx" "src/components/agent/chat/components/HarnessStatusPanel.tools.test.tsx" "src/lib/api/mcp.test.ts" "src/lib/api/mcp.failClosed.test.ts"` 通过 32 tests；scoped ESLint 与 `npm run typecheck -- --pretty false` 通过。仍需 GUI smoke / caller-scoped tool callable proof 才能宣称 MCP 工具可用。
- 当前进程 2026-07-06 已完成 P2 Context / Token / Compaction 第一 consumer：App Server `runtime/context_packet.rs` 已从本地手写 `truncate_to_token_budget` 迁到 RuntimeCore `ContextFragmentEnvelope::from_input(...)`，telemetry 输出 `fragmentEnvelope`，secret / empty reject 输出 `fragmentEnvelope=null`，metadata `sidecarRef` / `sidecar_reference` 进入 `ContextSidecarReference`；`CARGO_TARGET_DIR="/tmp/lime-codex-p2-context-packet-target" CARGO_BUILD_JOBS=4 cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server context_packet -- --nocapture` 通过，4 tests。2026-07-07 已完成 Evidence export consumer：App Server `evidence/export` 从 turn runtime metadata / event payload 读取 `context_packet_telemetry`，输出 `context-evidence-summary.v1`，包含 packet / fragment / budget / source / sidecar 摘要，且不导出 preview 正文或 sha256；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server export_evidence_pack_includes_context_fragment_summary_from_turn_metadata -- --nocapture` 通过，1 test。随后已完成 session compaction sidecar source 与 compact item 第一刀：`session_compaction_prompt_context` 透传真实 `sidecarRef` 到 next-turn `ContextPacket` metadata，read model `items` materialize `context_compaction` Item；两条 `compact_agent_session_*` 定向测试通过。memory summary sidecarRef 已进入 `ContextPacket.metadata` 骨架并通过 `memory_prompt` 回归；turn attachment 中的 media URI 已进入 `ContextPacket::media_reference(...)` 与 `context_packet_telemetry`，inline `data:` media fail closed；`media-reference` GUI smoke 已证明 Workbench preview skeleton 与 read model `contentParts` 链路可用；binary sidecar read / full preview detail 仍 pending。
- 当前进程 2026-07-06 已执行 `npm run governance:legacy-report`：边界违规 0；报告摘要里仍有 1 个分类漂移候选 `rust-agent-subagent-metadata-direct-read -> deprecated / 零引用`，不阻塞 P2，但最终收口前可单独判定是否从 `deprecated` 收到 `dead-candidate`。
- 当前进程 2026-07-06 已执行 Tool / Action / Agent UI projection 定向 Vitest：`npx vitest run "src/components/agent/chat/projection/toolEventProjection.test.ts" "src/components/agent/chat/projection/actionProjection.test.ts" "src/components/agent/chat/projection/agentUiEventProjection.test.ts"` 通过，3 files / 27 tests。
- 当前进程 2026-07-07 已把 Soul 纳入 P2 Realtime / Media / Collaboration 与 Tool lifecycle 质量口径：四个 built-in Style Pack seed 必须覆盖同一套 `before_tool / tool_running / after_tool_success / after_tool_partial_failure / after_tool_failure / body_detail / closing_suggestion` surface contract 与 few-shot anchors；四个 seed 已迁到 `src/lib/soul/style-profiles/packs/*.json` manifest，`builtInProfiles.ts` 只做 registry loader 与 manifest 校验，App Server `runtime/soul/style_profile.rs` 也通过 `include_str!` 消费同一 manifest，不再维护 Rust 四 profile 静态对象；`styleProfiles` / App Server Soul 单测必须证明 anchors 不坍缩成同一模板；`claw-chat-current-fixture-soul-style-transcript-golden.mjs` 必须证明同一工具 facts 下四风格 transcript 文本不同且 required fact tokens 不漂移；`soul-style` Electron current fixture 必须通过 `--soul-style-profile` 覆盖四个 built-in profile，证明 `memory.soul` 配置、当前 pack id、强度、lifecycle surface contract 和 profile-specific transcript 经真实 GUI / App Server runtime prompt / provider / read model 到达用户可见输出；`AgentRuntimeStrip` 已消费 collaboration facts 并暴露 `data-collaboration-*` / `data-soul-*` contract；`runtime_status.rs` 旧 profile-specific title rewrite 已删除；旧共享 `com.lime.builtin.default` 按 `dead / forbidden-to-restore` 处理，不作为兼容 fallback。
- 当前进程 2026-07-07 已完成 Codex fifth signal safety buffering GUI/current fixture 收口：`inputbar-rich-restore` 单场景已恢复 text/image/path/skill；随后 `npm run smoke:agent-runtime-current-fixture` 聚合门禁自然退出，代码工作台与 Claw `image-command`、`plain-image-intent`、`cancel-then-continue`、`inputbar-rich-restore`、`plan`、`skills-runtime`、`multi-agent-team`、`mcp-structured-content`、Expert 三场景和 Content Factory article workspace summary 均为本轮 08:14-08:18 新产物，`ok=true`、common/scenario assertions 全 true、`liveProviderUsed=false`；`npm run verify:gui-smoke` 已有通过记录。

## 2. P1 证据矩阵

| P1 模块 | current owner | 最小证据 | 当前状态 | 剩余缺口 |
| --- | --- | --- | --- | --- |
| P1-1 Thread / Turn / Item invariant | `thread-turn-item-invariant.md`、`naming-alignment.md`、Agent runtime / projection 改动模板 | 文档基线检查；后续每个 Agent 改动必须说明 session/thread、turn、item 归属 | `done` | 不是单独跑一次就永久完成；后续改动需要继续引用 invariant |
| P1-2 Method registry / serialization scope | `lime-rs/crates/app-server-protocol/src/protocol/v0/**`、`packages/app-server-client`、contract scripts | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server-protocol app_server_request_serialization_scope_covers_high_risk_methods -- --nocapture`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server-protocol schema_fixtures_match_generated_output -- --nocapture`；`npm run check:protocol-types`；`npm run test:contracts` | `done / contract-green` | 后续新增 method 仍需同步 registry、schema、generated client 和 contract guard |
| P1-3 Event materialization / Turn terminal | App Server runtime projection / read model、frontend timeline projection | Runtime projection Rust tests；`npm run smoke:agent-runtime-current-fixture`；相关 UI projection Vitest | `done` | App Server runtime / read model 热区仍被并行改动占用，本进程只读避让 |
| P1-4 Core session / task runtime owner | `lime-rs/crates/agent/src/session_execution_runtime.rs` 与子模块、RuntimeCore / App Server runtime 接线 | `cargo fmt --all --manifest-path "lime-rs/Cargo.toml"`；`cargo check --manifest-path "lime-rs/Cargo.toml" -p lime-agent --lib`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent --lib session_execution_runtime -- --nocapture`；`npx vitest run "src/lib/governance/appServerRuntimeBoundary.test.ts"` | `in-progress` | agent/tool 与 App Server runtime 热区未释放；后续继续拆 runtime owner 时先看文件行数和写集 |
| P1-5 UI projection boundary | `src/components/agent/chat/projection/*`、`agentUiEventProjection.ts`、`agentUiProjectionBoundary.test.ts` | `npx vitest run "src/lib/governance/agentUiProjectionBoundary.test.ts"`；各 owner projection Vitest；renderer typecheck | `done` | 新增 UI projection 事件族必须进入 owner dispatcher，不得回流聚合器手写分发表 |
| P1-6 Persistence / Replay / Trace | `src/lib/api/agentRuntime/exportClient.ts`、`appServerEvidenceExportProjection.ts`、App Server `evidence/export` | `npx vitest run "src/lib/api/agentRuntime/appServerEvidenceExportProjection.test.ts" "src/lib/api/agentRuntime/exportClient.test.ts"`；`npx vitest run "src/lib/governance/agentRuntimeExportBoundary.test.ts"`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server export_evidence_request_telemetry_ignores_unmatched_session_and_turn_logs -- --nocapture` | `done` | 后续新增 replay / handoff 派生导出必须走 correlation helper |
| P1-7 Provider / Model capability | `src/lib/model/*Policy*.ts`、`src/lib/api/modelRegistry.ts`、App Server `ModelInfo`、submit metadata owner、`lime-agent/src/model_request_policy.rs`、`lime-agent/src/protocol_context_projection.rs`、`lime-agent/src/protocol_projection.rs`、`lime-agent/src/tool_output_truncation.rs`、`lime-agent/src/live_execution_process.rs`、`agent_tools/tool_orchestrator.rs`、`request_tool_policy/aster_reply_adapter.rs`、App Server `runtime_backend/request_context.rs`、App Server `runtime/memory_prompt.rs`、App Server `runtime/context_auto_compaction.rs`、`model-provider/src/provider_stream.rs`、Aster OpenAI Responses request policy owner、Aster `prompt_input_modalities.rs` history filtering owner、`agent_tools/native_tool_policy_gate.rs`、`message_content_adapter.rs` compat output path、`src/lib/governance/asterContextPolicyBoundary.test.ts` | 第三十七刀最小集合：`npx vitest run "src/lib/model/modelRequestPolicyMetadata.test.ts" "src/lib/model/modelCapabilitySendGate.test.ts" "src/lib/model/modelContextPolicy.test.ts" "src/lib/model/modelTruncationPolicy.test.ts" "src/components/agent/chat/hooks/agentStreamSubmitExecution.test.ts"`；第三十八刀：`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent model_request_policy -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent build_reply_message_ -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server model_selection -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server model_request_policy_context -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server fast_response_lime_runtime_keeps_context_policy -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server prompt_context_budget_policy -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server compact_agent_session_injects_next_turn_session_context_packet -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server context_auto_compaction -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent live_execution -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent tool_output_truncation -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent tool_orchestrator -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent message_content_adapter -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent projector_applies_turn_context_truncation_to_later_tool_response -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent native_tool_policy -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent native_tool -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p model-provider provider_stream -- --nocapture`、`cargo test --manifest-path "lime-rs/vendor/aster-rust/Cargo.toml" -p aster-core openai_request_policy -- --nocapture`、`cargo test --manifest-path "lime-rs/vendor/aster-rust/Cargo.toml" -p aster-core prompt_input_modalities -- --nocapture`、`cargo test --manifest-path "lime-rs/vendor/aster-rust/Cargo.toml" -p aster-core openai_responses -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent protocol_context_projection -- --nocapture`、`cargo check --manifest-path "lime-rs/Cargo.toml" -p lime-agent --lib`、`npx vitest run "src/lib/governance/asterContextPolicyBoundary.test.ts"`、Responses Lite payload filter、`npm run test:contracts`、`npm run smoke:agent-runtime-current-fixture` | `in-progress / request-tool-consumer-done / reasoning-fallback-done / reasoning-output-provider-done / context-turn-context-projection-done / context-prompt-packet-consumer-done / context-budget-projection-done / context-history-usage-projection-done / context-usage-handoff-helper-done / actual-auto-compact-trigger-done / aster-context-boundary-guard-done / mcp-output-audit-done / aster-compat-formatter-done / input-modality-rust-gate-done / input-modality-history-done / contract-green / runtime-fixture-green / rust-lib-check-green` | 前端已把 policy 带入 metadata；Rust typed owner、live bytes/token truncation、native shell gate、transport wire-shape、compat fail-closed、Aster OpenAI Responses payload/header consumer、native tool inventory / apply_patch gate、live execution fail-closed guard、tool orchestrator registry output formatter、MCP output control-plane 排除审计、Aster `ToolResponse -> ToolEnd` compat formatter、current user-message image fail-closed gate、Aster provider prompt history image filtering、reasoning policy request fallback、reasoning-output provider request output-control、App Server context policy `lime_runtime` turn-context projection、App Server prompt context packet budget consumer、`lime-agent` turn-context summary 的 context budget / history usage projection、DB read model handoff helper、RuntimeCore pre-turn actual auto compact trigger，以及禁止 context / auto-compact 回流 vendor Aster 的治理守卫已完成；P1-7 剩余主缺口不再是 context consumer，下一刀转 P2 深层能力第一代码切片 |
| P1-8 Quality fixture matrix | 本文件、`priority-tracking-plan.md`、`upstream-checkpoint.md`、`completion-audit.md`、`upstream-diff-2026-07-06.md` | 文档存在且被 README / tracking plan 引用；后续按矩阵选验证，不再临场扩散；上游跟进从 checkpoint HEAD 起算；第三十八刀 source anchor 已固定；整体完成条件已有审计表；源码接管窗口、contract 失败归属与 P2 Tool / Approval / Sandbox combo evidence 已记录 | `done` | 尚未机械化成脚本；若要脚本化，必须先遵守 `scripts/` 冻结边界 |

## 2A. P2 骨架验证矩阵

P2 已进入 `in-progress`：Tool / Approval / Sandbox 已完成 App Server lifecycle owner 代码刀，`lime-agent` typed event construction owner、active state gate 第一刀、approval pending transition 第一刀、process lifecycle correlation 第一刀、terminal metadata correlation 第一刀、approval resolved terminal transition 第一刀与 action id / tool id 解耦前置刀已落到 `agent_tools/tool_lifecycle.rs` 并由 `tool_orchestrator.rs` 消费；Evidence export correlation 第一刀已进入 App Server `coding_evidence_summary`；RuntimeCore `action/respond` lifecycle guard 已证明正式 API 会从 pending action 回填 tool id 并释放 App Server lifecycle guard；RuntimeState/Aster pending resume 底层证据已通过；RuntimeBackend bridge evidence 已通过；final governance / contract / runtime fixture / projection 组合证据已通过。Context / Token / Compaction 已在 `runtime-core` 落 `ContextFragmentEnvelope` / budget decision / sidecar reference typed owner，并精修 `reference_only` / `hidden_requires_reference` 预算状态；App Server `context_packet` consumer 已消费 envelope并输出 `fragmentEnvelope` telemetry；App Server `evidence/export` 已消费 context telemetry 并输出 redacted context decision summary；session compaction 已完成 sidecar source 与 `context_compaction` Item 第一刀。Plugin / Skills / MCP 已在 App Server plugin package projection 落 `runtimeCapabilities` snapshot owner，覆盖 manifest / installed state 可消费的 skill metadata、workflow prompt policy、tool binding 与 MCP binding；前端 App Center projection、schema identity guard 与 detail skills/tools/MCP sections 已优先消费同一 snapshot/projection，不再从 legacy refs 推断 runtime capability。后续每一刀只认领一个垂直切片，先 typed owner，再 consumer，再 projection / Evidence。

| P2 模块 | 第一 typed owner | 第一 consumer | 最小验证 | 当前状态 |
| --- | --- | --- | --- | --- |
| Tool / Approval / Sandbox | App Server `ToolLifecycleSnapshot` / approval action / sandbox decision typed gate 已完成；`lime-agent` `ToolExecutionLifecycleSnapshot` / approval snapshot / approval resolution snapshot / sandbox decision snapshot / terminal snapshot / process lifecycle snapshot / output delta snapshot / lifecycle events 已进入 `agent_tools/tool_lifecycle.rs` | App Server external event storage 已 fail-closed；`tool_orchestrator.rs` 已消费 typed owner 生成 `ToolStart` / `ActionRequired` / `ToolEnd` / live process deltas；approval / blocked 后不再发 `ToolOutputDelta`，terminal 后重复 outcome 不再发事件；approval-required 只进入 `AwaitingApproval`，不抢先发失败 `ToolEnd`；process lifecycle / stdout stderr delta 与 terminal `ToolEnd` metadata 均带 tool correlation metadata；approval resolved helper 已释放 terminal gate；App Server evidence export 已输出 action/tool correlation；action required / resolved 已能区分 action id 与 tool id；RuntimeCore `respond_action(...)` 已能回填 pending action 的 tool id 并允许后续 `tool.result`；RuntimeState/Aster pending resume 已释放同一 pending tool future；RuntimeBackend bridge evidence 已证明 `start_turn -> action.required -> respond_action -> pending tool future -> tool.result/final text/turn.completed` 闭环；governance / contract / runtime current fixture / frontend projection 组合证据已通过 | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server tool_lifecycle -- --nocapture`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent tool_lifecycle -- --nocapture`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent tool_orchestrator -- --nocapture`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server export_evidence_pack_includes_coding_snapshot_artifacts -- --nocapture`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server respond_action_infers_tool_id_and_unblocks_pending_tool_result -- --nocapture`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent confirm_tool_action_resumes_pending_aster_tool_execution -- --nocapture`；`CARGO_TARGET_DIR="/tmp/lime-codex-p2-tool-target-bridge" CARGO_BUILD_JOBS=4 cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server respond_action_tool_confirmation_resumes_pending_aster_tool_future -- --nocapture`；Tool / Action / Agent UI projection Vitest；`npm run governance:legacy-report`；`npm run test:contracts`；`npm run smoke:agent-runtime-current-fixture` | `app-server-lifecycle-owner-done / lime-agent-event-owner-done / active-state-gate-done / approval-pending-transition-done / process-correlation-done / terminal-correlation-done / approval-resolved-terminal-done / evidence-export-correlation-done / action-tool-id-split-done / action-respond-lifecycle-guard-done / runtime-state-aster-resume-evidence-done / runtimebackend-bridge-evidence-done / combo-evidence-done` |
| Context / Token / Compaction | `runtime-core` `ContextFragmentEnvelope`、`ContextFragmentBudgetDecision`、`ContextSidecarReference` typed owner 已完成并精修；preview 受 char / token 双预算约束，zero-preview + sidecar 为 `reference_only` | App Server `runtime/context_packet.rs` 已消费 envelope，输出 `fragmentEnvelope` telemetry，并把 packet metadata `sidecarRef` / `sidecar_reference` 转为 `ContextSidecarReference`；App Server `evidence/export` 已输出 context decision summary，redact preview 正文和 sidecar hash；session compaction 已透传真实 `sidecarRef` 并 materialize `context_compaction` Item；memory summary sidecarRef 已进入 `ContextPacket.metadata` 骨架，且 sidecar 只保存 memory summary 原文，不保存 `memory_soul_prompt_context`、Style Pack 或 full system prompt；media input attachment reference 已进入 `ContextPacket` telemetry skeleton，inline `data:` media fail closed；`media-reference` GUI smoke 已通过；binary sidecar read / full preview detail 仍 pending | `rustfmt --edition 2021 --check "lime-rs/crates/runtime-core/src/lib.rs" "lime-rs/crates/runtime-core/src/context_fragments.rs" "lime-rs/crates/app-server/src/runtime/context_packet.rs"`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p runtime-core context_fragments -- --nocapture` 6 tests；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server context_packet -- --nocapture` 4 tests；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server export_evidence_pack_includes_context_fragment_summary_from_turn_metadata -- --nocapture` 1 test；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server compact_agent_session_writes_session_context_artifact -- --nocapture` 1 test；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server compact_agent_session_injects_next_turn_session_context_packet -- --nocapture` 1 test；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server memory_prompt -- --nocapture` 14 tests；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server media_context -- --nocapture` 2 tests；`npm run smoke:claw-chat-current-fixture -- --scenario media-reference --timeout-ms 240000 --evidence-dir "/tmp/lime-codex-media-reference-evidence" --prefix "claw-chat-current-fixture-media-reference"` passed | `context-packet-consumer-done / context-evidence-export-done / session-compaction-sidecar-and-item-done / memory-sidecar-skeleton-done / memory-regression-done / media-input-reference-skeleton-done / media-gui-preview-smoke-validated / binary-sidecar-read-app-server-skeleton-wired / binary-sidecar-read-runtime-test-sigterm / full-binary-preview-pending` |
| Plugin / Skills / MCP | `plugin_packages/runtime_capabilities.rs` `runtimeCapabilities` snapshot typed owner 已完成；前端 `src/features/plugin/**` App Center projection consumer 已优先消费 snapshot，并保留 `promptInjectionPolicy` / `bindingKind` / `mcpBindings` / `workflowBindings`；schema gate 已校验 snapshot identity；App Center detail skills / tools / MCP sections 已消费 `projection.skillRequirements`、`projection.toolRequirements` 与 `projection.runtimeCapabilities.mcpBindings`；App Server `plugin_runtime_context` 已把 workflow-scoped skill prompt policy 接到 Agent Skills selection、session system prompt 和 telemetry；App Server `tool_inventory` 已输出 `plugin_mcp_targets` target/status/tool-listing skeleton；前端 API `mcpApi.executePrepareRequests(...)` 已能顺序执行 candidate prepare request；Workspace Harness 已接手动 trigger 与执行后 inventory refresh skeleton | MCP binding 已能对齐 runtime MCP status / tool snapshot，并已有 import/start/listForContext API execution helper；GUI smoke、自动触发与 caller-scoped 可调用闭环仍 pending | `rustfmt --edition 2021 --check "lime-rs/crates/app-server/src/plugin_packages.rs" "lime-rs/crates/app-server/src/plugin_packages/plugin_manifest.rs" "lime-rs/crates/app-server/src/plugin_packages/runtime_capabilities.rs"`；`CARGO_TARGET_DIR="/tmp/lime-codex-p2-plugin-capabilities" CARGO_BUILD_JOBS=4 cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime_capabilities -- --nocapture` 4 tests；`npx vitest run "src/features/plugin/projection/projectApp.test.ts" "src/features/plugin/schema/schemaGate.test.ts" "src/features/plugin/ui/pluginDetailDeclarations.unit.test.ts" "src/features/plugin/ui/PluginsPage.test.tsx"` 42 tests；`npx vitest run "src/features/plugin/ui/pluginDetailDeclarations.unit.test.ts" "src/features/plugin/ui/PluginDetailRuntimeSections.unit.test.tsx" "src/features/plugin/ui/PluginsPage.test.tsx"` 39 tests；`npm run typecheck`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server plugin_runtime -- --nocapture` 8 tests；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server tool_inventory --lib` 6 tests；`npx vitest run "src/lib/api/mcp.test.ts" "src/lib/api/mcp.failClosed.test.ts"` 18 tests；`npx vitest run "src/components/agent/chat/workspace/useWorkspaceHarnessInventoryRuntime.test.tsx" "src/components/agent/chat/components/HarnessStatusPanel.tools.test.tsx" "src/lib/api/mcp.test.ts" "src/lib/api/mcp.failClosed.test.ts"` 32 tests；scoped ESLint passed；`npm run typecheck -- --pretty false` passed；后续补 caller-scoped callable proof / GUI smoke | `plugin-app-center-projection-consumer-done / plugin-runtime-capability-schema-identity-guard-done / plugin-app-center-detail-consumer-done / plugin-app-center-detail-tools-mcp-consumer-done / skill-prompt-runtime-consumer-validated / mcp-guidance-runtime-consumer-validated / mcp-target-status-tool-listing-skeleton-done / mcp-prepare-execution-api-done / mcp-prepare-trigger-skeleton-done / mcp-runtime-callable-proof-pending` |
| Realtime / Media / Collaboration | RuntimeCore `RuntimeContentPart` / `RuntimeContentReference`、`RuntimeMessageDeltaContent`、LLM media output typed RuntimeEvent、team/subagent lifecycle event | `llm_protocol::runtime_event_from_llm_event(...)` 已消费 owner 输出 `message.delta.text` 与 `message.delta.contentPart/contentParts`；`RuntimeMessageDeltaContent::from_payload(...)` 已提供 Item/read model parser owner；App Server `thread_item_projection/agent_message.rs` 已消费 parser，media-only delta 可生成 `agent_message.contentParts`，同 item text/media 可合并，alias mismatch 与 inline `data:` media fail closed；protocol/generated 已导出 `AgentThreadContentReference` / `AgentThreadMessageContentPart`；projection package、GUI hydrate/streaming sync 与 MessageList renderer skeleton 已接入；`media-reference` GUI smoke 已验证 GUI media card、chat-canvas Workbench preview skeleton 与 read model `contentParts`；binary sidecar read / full preview detail 仍 pending | `cargo test --manifest-path "lime-rs/Cargo.toml" -p runtime-core runtime_content -- --nocapture`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p runtime-core llm_ -- --nocapture`；`rustfmt --edition 2021 --check "lime-rs/crates/app-server/src/runtime/thread_item_projection/agent_message.rs"` 通过；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server media_only_delta_creates_agent_message_content_parts -- --nocapture` 1 test passed；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server-protocol generated_fixture_tree_is_stable_and_canonical_json -- --nocapture` 1 test passed；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server-protocol schema_fixtures_match_generated_output -- --nocapture` 1 test passed；`npm run check:protocol-types` 通过；GUI media Vitest 5 files / 50 tests passed；`npm run smoke:claw-chat-current-fixture -- --scenario media-reference --timeout-ms 240000 --evidence-dir "/tmp/lime-codex-media-reference-evidence" --prefix "claw-chat-current-fixture-media-reference"` passed；后续 binary sidecar read / full preview detail | `runtime-content-owner-done / llm-media-content-event-done / message-delta-content-owner-done / message-delta-content-parser-done / app-server-agent-message-contentparts-skeleton-validated / protocol-generated-skeleton-validated / projection-hydrate-skeleton-done / message-list-renderer-skeleton-validated / gui-media-smoke-validated / binary-sidecar-read-app-server-skeleton-wired / binary-sidecar-read-runtime-test-sigterm / full-binary-preview-pending` |
| Interaction Soul lifecycle | `memory_soul_prompt_context`、Style Pack resolver、tool lifecycle metadata、collaboration facts、risk facts、`soul-style` Electron fixture、runtime strip metadata contract | 四个 built-in Style Pack seed 已覆盖完整工具生命周期 surface contract；fixture 已证明 current pack id、lifecycle contract 和四 profile transcript 进入真实 GUI/read model；`AgentRuntimeStrip` 已消费 collaboration facts；`runtime_status.rs` 不再 profile switch 中文标题；旧共享 pack id 已按 `dead` 清理并补负向断言 | `npm exec vitest run "src/lib/soul/style-profiles/styleProfiles.unit.test.ts" "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs" "src/components/agent/chat/components/AgentRuntimeStrip.test.tsx"`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server soul -- --nocapture`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent runtime_status -- --nocapture`；`node scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs --scenario soul-style --soul-style-profile <id> --timeout-ms 180000 --prefix <profile-prefix>` 四 profile | `prompt-surface-contract-fixture-done / electron-multi-profile-transcript-done / runtime-strip-consumer-done / runtime-status-profile-switch-removed / keep-business-surface-regression` |
| Trace / Evidence cross-cutting | session/thread/turn/item correlation owner | tool/subagent/media/context decision export | Evidence export tests；request telemetry negative tests | `skeleton-ready` |

## 3. 验证选择规则

| 改动类型 | 必跑 | 何时扩大 |
| --- | --- | --- |
| 只改 v1 文档 / 计划 | `rg -n "[ \t]+$" <docs>` 或 scoped 文档检查 | 只有文档改动改变工程规则时，补相关治理测试 |
| 改 App Server protocol / generated TS | `npm run check:protocol-types`、schema fixture Rust test、相关 client Vitest | 触碰 method / command / mock 边界时扩大到 `npm run test:contracts` |
| 改 Rust runtime / agent | `npm run test:rust:related -- <paths...>` 或定向 `cargo test --manifest-path "lime-rs/Cargo.toml" -p <crate> <filter>` | 跨 crate / protocol / release 门禁时扩大到 workspace |
| 改 frontend model policy / projection | 定向 `npx vitest run ...`、相关 ESLint、renderer typecheck | 触碰 GUI 主路径时补 `npm run verify:gui-smoke` |
| 改 GUI 输入 / workspace 主路径 | 定向 component / hook Vitest、renderer typecheck、`npm run verify:gui-smoke` | 用户可见交互复杂时补 Playwright |
| 改 legacy / governance 边界 | 对应 `src/lib/governance/*.test.ts` | 触碰命令 / bridge / mock 时补 `npm run test:contracts` |

## 3A. Codex 第五次信号验证口径

第五次 upstream 信号不能只以 P3 diff 作为完成证据。代码实现前先看 [p2-codex-fifth-signal-handoff.md](./p2-codex-fifth-signal-handoff.md)，并按下表选择最小验证。

| 信号 | current owner | 最小验证 | 完成判定 |
| --- | --- | --- | --- |
| App Server `configWarning` | App Server initialize / `thread/start` config reload warning flow；frontend / GUI 只消费 typed warning event | App Server warning tests；frontend typed consumer tests；GUI warning presentation tests；`npm run test:contracts`；触碰 GUI 主路径时补 `npm run verify:gui-smoke` | config parse warning 不再由 GUI 或 Electron 直接读 core；response/error path 均能带 typed warnings；GUI 只做五语言 presentation |
| safety buffering `retry_model` | provider stream parser -> RuntimeEvent projection -> read model / GUI presentation | provider stream parser tests；RuntimeEvent projection tests；read model diagnostic tests；GUI presentation tests；`npm run smoke:agent-runtime-current-fixture`；触碰 GUI 主路径时补 `npm run verify:gui-smoke` | payload `retry_model` 优先，explicit null 表示 unset，missing field 才 fallback 旧 header；GUI 不解析 provider raw wire；current fixture 与 GUI smoke 已 green |
| Desktop Host conditional dotenv | Desktop Host sidecar startup env owner；provider/network bootstrap | Desktop Host sidecar env tests；provider connectivity fixture；Windows/macOS startup smoke | overlay 只在 App Server sidecar spawn 前完成，内部变量受保护，不做 runtime env mutate |

## 4. 当前下一刀判定

继续 P2 深层模块源码刀前必须先确认以下热区是否释放：

- `lime-rs/crates/agent/src/agent_tools/**`
- `lime-rs/crates/agent/src/request_tool_policy/**`
- `lime-rs/crates/app-server/src/runtime/**`
- `lime-rs/crates/app-server/src/runtime_backend/**`
- `lime-rs/crates/runtime-core/**`
- `scripts/check-app-server-client-contract.mjs`

释放后优先目标：

```text
request_metadata.harness.model_request_policy
  -> lime-agent typed owner / accessor done
  -> Rust live process bytes truncation consumer done
  -> Rust live process native shell gate verified
  -> responses/tool-call transport skeleton verified
  -> provider wire-shape + Aster compat fail-closed done
  -> Aster OpenAI Responses payload/header consumer done
  -> native tool inventory surface / apply_patch gate + live execution fail-closed done
  -> token-aware final output formatter done
  -> tool orchestrator registry output truncation formatter done
  -> MCP/App Server control plane excluded from model output truncation
  -> Aster ToolResponse compat path formatter done
  -> Aster user message image fail-closed gate done
  -> App Server reasoning policy request fallback done
  -> reasoning-output provider output-control done
  -> context policy typed owner + App Server lime_runtime projection done
  -> App Server prompt packet budget consumer done
  -> Rust context budget / history usage projection owner done
  -> Aster context-policy boundary guard done
  -> context usage handoff helper done
  -> session DB read model calls handoff helper done
  -> actual auto compact trigger owner done
  -> npm run test:contracts
```

如果这些热区仍未释放，下一步只允许接已经释放的窄 consumer 切片，或继续做上游 diff / 只读验证；不夹写未移交的 runtime / contract 热区。P2 Tool / Approval / Sandbox 第一刀的可接管边界已沉淀到 [p2-tool-approval-sandbox-handoff.md](./p2-tool-approval-sandbox-handoff.md)。

本轮并行刷新结论：`lime-agent` typed event construction owner、active state gate 第一刀、approval pending transition 第一刀、process lifecycle correlation 第一刀、terminal metadata correlation 第一刀、approval resolved terminal transition 第一刀、Evidence export correlation 第一刀、action/tool identity split、RuntimeCore `action/respond` lifecycle guard、RuntimeState/Aster pending resume、RuntimeBackend bridge evidence 与 final combo evidence 已完成；Context / Token / Compaction `runtime-core` typed owner 已完成并精修，App Server `context_packet` consumer、Evidence export context summary consumer、session compaction sidecar source 与 `context_compaction` Item 第一刀、memory regression、media input reference skeleton 与 `media-reference` GUI smoke 已完成；Plugin / Skills / MCP `runtimeCapabilities` snapshot owner、App Center projection consumer、schema identity guard、App Center detail skills/tools/MCP projection consumer、App Server skill prompt runtime consumer、inventory `plugin_mcp_targets` skeleton、`mcpApi.executePrepareRequests(...)` API execution helper、Workspace prepare trigger 与执行后 inventory refresh skeleton 已完成；MCP caller-scoped callable proof / GUI smoke 仍 pending；Realtime / Media / Collaboration `RuntimeContentPart` / media reference owner、LLM media typed RuntimeEvent、`RuntimeMessageDeltaContent` payload owner/parser、App Server `agent_message.contentParts` skeleton、protocol/generated、projection/hydrate、MessageList renderer skeleton 与 GUI media smoke 已完成；binary sidecar read / full preview detail 仍 pending。Codex fifth signal handoff 已把 App Server `configWarning`、provider safety buffering `retry_model` 和 Desktop Host env overlay watch 固定到 owner / 验证口径，`configWarning` frontend typed consumer / GUI presentation 已有 typecheck、i18n、contracts 与 GUI smoke 证据，provider safety buffering read model -> reliability panel -> current fixture 聚合门禁也已 green。新增或仍未跟踪的相邻源码不由当前进程合并、删除或改名。下一个源码刀必须重新执行 scoped status，并优先选择 MCP caller-scoped callable proof / GUI smoke 或 binary sidecar read / full preview detail；不要继续围绕已绿的 Inputbar rich restore 或 GUI media smoke 做低杠杆 polish。

最新 contract 证据：

- 首次 `npm run test:contracts` 失败于 App Server client contract 锚点：`parse_tool_arguments(arguments)` 已迁到 `runtime_backend/tool_process_runtime_metadata.rs`，但 contract 文件列表未扫描该 current owner；已补 `scripts/check-app-server-client-contract.mjs` 文件列表。
- `node scripts/check-app-server-client-contract.mjs` 已在 2026-07-06 重跑并通过 287 项。
- `npm run test:contracts` 已在 2026-07-06 完整通过：`check:protocol-types` 生成文件无漂移，command / harness / modality / scripts / electron release / cleanup report / docs boundary 均通过。
- 之前记录的 `aster_registry_adapter.rs` 与 `web_search_preflight_uses_turn_context_for_permission_check` blocker 已不再复现；后续不得为了旧记录恢复 Aster registry adapter。

源码接管窗口必须同时满足：

1. `git status --short -- <第三十八刀 consumer 写集>` 不再显示他人脏改，或用户明确授权当前进程接管对应热区。
2. 计划文件先登记窄写集；typed owner 已在 `lime-rs/crates/agent/src/model_request_policy.rs` 完成，第二切片首选 responses/tool-call request shape 或 truncation/native-tool surface 中的一个，不要一次认领整个 `lime-rs/crates/agent/**`。
3. App Server 只保留 metadata 投影职责，不在 `runtime_backend` 顶层 loop 展开 `serde_json::Value` policy pointer。
4. 如果 `scripts/check-app-server-client-contract.mjs` 仍脏或失败，源码刀必须把失败归属写清：本刀修复、外部 blocker 或需移交持有该脚本的进程。

当前 upstream 记录见 [upstream-checkpoint.md](./upstream-checkpoint.md)、[upstream-diff-2026-07-06.md](./upstream-diff-2026-07-06.md)、[upstream-diff-2026-07-06-p3-loop.md](./upstream-diff-2026-07-06-p3-loop.md)、[upstream-diff-2026-07-07.md](./upstream-diff-2026-07-07.md)、[upstream-diff-2026-07-07-p3-fifth.md](./upstream-diff-2026-07-07-p3-fifth.md) 与 [p2-codex-fifth-signal-handoff.md](./p2-codex-fifth-signal-handoff.md)：初始 checkpoint 是 Codex `main@db887d03e1f907467e33271572dffb73bceecd6b` / opencode `dev@17166b271fb9d7bf7128f0e63732dde0c10dd963`；P3 第五次 range check 后，下一次建议从 Codex `8268cbfb0e5f39cb4efff928264fe8f29ddacafb` / opencode `eb6ff0c1e049e5dfb6f61eb74f925c0a8007490c` 起算。opencode 后续仍只允许进入多模型 / 多模态 allowlist。

整体完成判定见 [completion-audit.md](./completion-audit.md)：当前整体目标尚未完成；P1-7 第三十八刀 request/tool consumer 主链、tool orchestrator output formatter、MCP output 审计、Aster `ToolResponse -> ToolEnd` compat formatter、current user-message input-modality gate、history-level input modality filtering、reasoning policy request fallback、reasoning-output provider output-control、context policy typed owner / App Server `lime_runtime` projection、context prompt packet budget consumer、turn-context summary context budget projection、history usage projection、context usage handoff helper、session DB read model handoff consumer、actual auto compact trigger owner、Aster context-policy boundary guard、contract gate 和 runtime current fixture 已完成，P2 Tool / Approval / Sandbox 已完成 App Server lifecycle owner、`lime-agent` typed event construction owner、active state gate 第一刀、approval pending transition 第一刀、process lifecycle correlation 第一刀、terminal metadata correlation、approval resolved terminal transition、Evidence export correlation、action/tool identity split、RuntimeCore `action/respond` lifecycle guard、RuntimeState/Aster pending resume 与 RuntimeBackend bridge 证据；P2 Context / Token 已完成 RuntimeCore fragment owner、App Server `context_packet` consumer、Evidence export context summary consumer、session compaction sidecar source、`context_compaction` Item、memory regression、media input reference skeleton 与 media-reference GUI smoke；P2 Plugin skill prompt runtime consumer、MCP guidance consumer、inventory target/status/tool-listing skeleton 与 API execution helper 已验证；P2 Realtime / Media 已完成 RuntimeCore content/media reference owner、App Server Item skeleton、protocol/generated、projection/hydrate、MessageList renderer skeleton 与 GUI media smoke；Codex fifth signal provider safety buffering current fixture / GUI smoke 证据已关闭；硬缺口仍包括 MCP prepare trigger / refresh、binary sidecar read / full preview detail、P3 周期性循环证据和最终组合验证。

## 5. 第三十八刀 Consumer/Evidence Map

第三十八刀不再扩前端 DTO。第一切片已完成 Rust typed owner；后续必须把 `request_metadata.harness.model_request_policy` 接入 Rust runtime/request consumer，并用 Codex 源码消费点做对标，不允许只把 metadata 透传到日志。

| policy fragment | Codex consumer anchor | Lime current consumer target | 最小证据 |
| --- | --- | --- | --- |
| `responses_policy` / `use_responses_lite` | `core/tests/suite/responses_lite.rs`、`core/src/client_common.rs`、`core/src/session/turn.rs` | App Server runtime request builder / `lime-agent` turn request context；决定 Responses Lite header、instructions/tools payload location、reasoning context 与 parallel tools gate | Transport skeleton、provider wire-shape、Aster compat fail-closed 与 Aster OpenAI Responses builder payload/header consumer 已通过协作验证；后续 current provider builder 必须直接消费 `RuntimeReplyProviderRequestWireShape` |
| `truncation_policy` | `tools/src/tool_call.rs`、`core/src/session/mod.rs`、`core/src/tools/events.rs`、`core/src/unified_exec/process_manager.rs` | `lime-agent` tool call context / execution process output formatter；`tool_output_truncation.rs` 是 live native tool、current tool orchestrator output formatter 与 Aster `ToolResponse` compat formatter owner | `live_execution` Rust fixture 已覆盖 `mode=bytes` 映射到 drain `max_bytes`、`mode=tokens` 保持 drain 默认安全上限并在最终 `CallToolResult` 文本按 token budget 截断；`tool_output_truncation` 单测覆盖 token warning、small output 和 UTF-8 bytes 边界；`tool_orchestrator` 测试覆盖 registry tool output 在 `ToolExecutionOutcome` / `ToolEnd` 前按 token policy 截断；`projector_applies_turn_context_truncation_to_later_tool_response` 覆盖 Aster `ToolResponse` compat path 通过 stateful projector 复用 selected policy |
| `native_tool_policy` | `tools/src/tool_config.rs`、`protocol/src/openai_models.rs`、`core/src/session/turn_context.rs` | `lime-agent` native tool inventory / shell vs unified exec / apply patch surface gate；不得从 generic tools/runtime features 推断 | live shell hook 已接 `shell_tool_enabled=false` gate；inventory projection 已新增 `NativeToolPolicyGate`，`shell_tool_enabled=false` 隐藏 `Bash` / `PowerShell`，`apply_patch_tool_enabled=false` 或缺少 `apply_patch_tool_type=freeform` 隐藏 `apply_patch`；live execution guard 已 fail-closed 拒绝旧 shell surface；`native_tool_policy` 与 `native_tool` 定向测试已通过 |
| `tool_call_policy` | `core/src/session/turn.rs::build_prompt`、MCP `supports_parallel_tool_calls` config | Prompt / provider request shape 的 `parallel_tool_calls`；Responses Lite 时按 responses policy gate 再收窄 | Transport skeleton 已把 responses lite gate 后的 `parallel_tool_calls` 带到 `RuntimeReplyStreamRequest`；Aster OpenAI Responses builder 已写 payload 字段，后续非 Aster current provider 不得重新解析前端 metadata |
| `reasoning_policy` / `reasoning_output_policy` | `core/src/session/turn_context.rs`、`core/src/session/review.rs`、Responses client verbosity gate | `reasoning_policy.default_reasoning_level` 已作为 App Server request fallback 进入 `RuntimeModelSelection.reasoning_effort` / `AgentTurnContext.effort`；`ModelReasoningOutputPolicySnapshot` 已归一 summary / verbosity typed owner；reasoning-output 已进入 provider request output-control owner | `app-server model_selection` 覆盖 policy default 生效与显式 request effort 优先；`model_request_policy::reasoning_output_policy_*` 覆盖默认 `auto` summary、verbosity 支持 gate 与默认值保留；`model-provider provider_stream` 覆盖 `reasoning_summary / text_verbosity` wire shape；Aster `openai_request_policy` 覆盖 wire shape 和 fallback metadata 解析；Aster Responses payload 测试覆盖 `reasoning.summary` 与 `text.verbosity` 共存 output schema，且不混入 effort fallback |
| `context_policy` | `core/src/session/context_window.rs`、`core/src/session/turn.rs` | `ModelContextPolicySnapshot` 已按 selected model policy 计算 model context window / auto compact limit；App Server `turn_context_from_request(...)` 已投影到 `lime_runtime.context_policy` / `model_context_window` / `auto_compact_token_limit`；App Server `memory_prompt.rs` 已把同一 policy 接到 memory / session-compaction prompt packet budget；`protocol_context_projection.rs` 已在缺少显式 `agentui_context.memory_budget` 时把 `lime_runtime.context_policy` 投影成 `AgentTurnContextSummary.memory_budget`，并可从 `lime_runtime.context_usage` / `history_usage` / `token_usage` 读取真实 history usage 后按 effective limit 标记 `auto_compact_due`；`protocol_projection.rs` 已暴露 `project_turn_context_summary_with_active_context_tokens(...)`；session DB read model 已传入 active context tokens；App Server `runtime/context_auto_compaction.rs` 已在 turn 前按 Codex pre-sampling compact 口径触发现有 compaction event / artifact 链；`asterContextPolicyBoundary.test.ts` 已禁止 vendor Aster 重新承接 selected model context / auto-compact token-limit 规则 | `model_request_policy::context_policy_*` 覆盖 effective percent 改变 `model_context_window`、auto compact limit 使用 selected model policy、`context_window ?? max_context_window` 与缺 context 时显式 compact limit；`app-server model_request_policy_context` 与 `fast_response_lime_runtime_keeps_context_policy` 覆盖 turn-context projection 与 fast-response metadata merge；第十七切片新增 `prompt_context_budget_policy` 与 `compact_agent_session_injects_next_turn_session_context_packet` 断言，并已通过 App Server 定向测试；第二十切片新增 `protocol_context_projection` 单元覆盖 auto compact disabled、tighter limit、remaining tokens 与 agentui budget 优先级；第二十一切片新增 history usage projection / `auto_compact_due` 单元覆盖；第二十二切片新增 handoff helper单元覆盖；第二十四切片新增 `app-server context_auto_compaction` 单元 / RuntimeCore 测试，覆盖 input_tokens-only、latest compaction 去重、pre-turn trigger 和同 turn metadata 注入；`asterContextPolicyBoundary.test.ts` 覆盖 current owner 与 vendor Aster forbidden snippets；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server context_auto_compaction -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server compact_agent_session -- --nocapture`、`npx vitest run "src/lib/governance/asterContextPolicyBoundary.test.ts"` 通过 |
| `input_modality_policy` | `core/src/session/turn.rs` history `for_prompt(&input_modalities)` | `lime-agent` current user message image fail-closed gate 已完成；Aster `prompt_input_modalities.rs` 已在 provider prompt dispatch 前按 selected model input modalities 过滤历史 image part | `model_request_policy` 解析 selected input modality policy，`build_reply_message_` 覆盖 text-only model 拒绝 `input.images`；`prompt_input_modalities` 覆盖 text-only 历史 message image 与 tool result image 替换、image-capable 保留、缺 policy 兼容默认保留 |

第三十八刀完成判定：

1. Rust consumer 有 typed owner，不把 `serde_json::Value` policy 解析散落在 App Server 顶层 loop。
2. 仍走 App Server JSON-RPC current 主链；不恢复 `agent_runtime_*` 或 `lime-rs/src/**`。
3. 定向 Rust tests 覆盖上表至少 `responses_policy / truncation_policy / native_tool_policy / tool_call_policy` 四个高风险 request/tool surface；测试应先覆盖 `lime-agent` typed owner，再覆盖实际 consumer。
4. `npm run smoke:agent-runtime-current-fixture` 通过；`npm run test:contracts` 若仍失败，必须证明失败不在本刀写集。

### 5.1 第四刀：Anchor Integrity Audit

记录时间：2026-07-06。

只读复核：

```bash
git -C "/Users/coso/Documents/dev/rust/codex" rev-parse HEAD
git -C "/Users/coso/Documents/dev/rust/codex" status --short
git -C "/Users/coso/Documents/dev/js/opencode" rev-parse HEAD
git -C "/Users/coso/Documents/dev/js/opencode" status --short
```

结果：

- Codex HEAD 仍为 `db887d03e1f907467e33271572dffb73bceecd6b`，`status --short` 为空。
- opencode HEAD 仍为 `17166b271fb9d7bf7128f0e63732dde0c10dd963`，`status --short` 为空。

第三十八刀实现时优先引用这些 Codex source anchor：

| consumer | source anchor | 实现约束 |
| --- | --- | --- |
| ModelInfo policy 字段 | `codex-rs/protocol/src/openai_models.rs:238-239`、`:361`、`:380-387`、`:407-419` | Lime Rust typed owner 必须消费已经进入 `request_metadata.harness.model_request_policy` 的字段，不再从 provider/model 字符串旁路推断 |
| `ReasoningEffort::Max` | `codex-rs/protocol/src/openai_models.rs:713`、`:730` | Rust request consumer 不得把 `max` 降级成 opaque custom；如当前 Rust owner尚未处理 reasoning，可作为第三十八刀后续小刀的必测项 |
| Responses Lite | `codex-rs/core/tests/suite/responses_lite.rs:100`、`:152`、`:227`、`:271`、`:313-343`；`codex-rs/core/src/client_common.rs:54-57` | Lite 模式必须改变 header、input prefix、instructions/tools payload location、reasoning context 与 parallel tool calls gate |
| input modality filtering | `codex-rs/core/src/session/turn.rs:274`、`:1109` | Rust prompt/history builder 必须按 selected model input modalities 过滤 history；前端 send gate 不是唯一防线 |
| parallel tool calls | `codex-rs/core/src/session/turn.rs:1053` | `supports_parallel_tool_calls` 进入 provider prompt/request shape；Responses Lite 可进一步收窄 |
| native tool gate | `codex-rs/tools/src/tool_config.rs:81`、`:88` | shell / unified exec / apply patch surface 由 model info + feature/platform gate 共同决定，不从 generic runtime features 直接推断 |
| truncation policy | `codex-rs/tools/src/tool_call.rs:88`、`:102` | tool call / shell output formatter 要读取 selected model truncation policy，缺 metadata 才 fallback 到 current 默认 |

opencode 仍只作为多模型 / 多模态 allowlist 参考：

- `packages/llm/src/protocols/shared.ts:192-194` 固定 `VIDEO_MIMES`、`AUDIO_MIMES`、`MEDIA_MIMES`。
- `packages/llm/src/protocols/gemini.ts:24`、`:185`、`:278` 固定 Gemini lowering 对 `MEDIA_MIMES` 的使用。
- `packages/sdk/js/src/gen/types.gen.ts:1064-1065` 与 `packages/sdk/js/src/v2/gen/types.gen.ts:1793-1794` 只作为 model capability schema 参考；不引入 opencode Session / Tool / UI runtime 架构。

本刀不新增脚本。若后续要把 anchor audit 机械化，必须先走 `scripts/` 冻结边界评估，并同步 `scripts/README.md` / baseline / 执行计划退出条件。

### 5.2 第六刀：Rust Consumer Handoff Audit

记录时间：2026-07-06。

本刀只读 Lime / Codex 源码，不接管 Rust 热区。

Lime 当前入口：

| boundary | read-only evidence | 第三十八刀约束 |
| --- | --- | --- |
| 前端 submit metadata | `src/lib/model/modelRequestPolicyMetadata.ts` 输出 `request_metadata.harness.model_request_policy` | 不再扩前端 DTO；Rust 必须消费现有 metadata |
| App Server metadata projection | `lime-rs/crates/app-server/src/runtime_backend/request_context/turn_context.rs` 写入 `AgentTurnContext.metadata["aster_chat_request"]` 与 `["runtime_options"]` | App Server adapter 只做投影，不展开 policy 业务语义 |
| `lime-agent` turn context façade | `lime-rs/crates/agent/src/turn_context_configuration.rs` 构造 `AgentTurnContext` | typed parser / accessor 应落到 `lime-agent`，避免 consumer 各自 JSON pointer |
| `agent_protocol` current shape | `lime-rs/crates/agent-protocol/src/turn_context.rs` 目前只有通用 metadata | 第一刀可先走 `lime-agent` typed accessor；若后续要提升成 protocol 字段，必须同步 protocol/client/contract |

第三十八刀推荐 owner：

```text
lime-rs/crates/agent/src/model_request_policy.rs
  ModelRequestPolicySnapshot
  ModelResponsesPolicySnapshot
  ModelToolCallPolicySnapshot
  ModelTruncationPolicySnapshot
  ModelNativeToolPolicySnapshot
  model_request_policy_from_metadata(...)
  model_request_policy_from_turn_context(...)
```

高风险 consumer 接管点：

| policy fragment | Lime consumer owner | 最小行为断言 |
| --- | --- | --- |
| `responses_policy` | `lime-agent` request / reply adapter boundary | `responses_lite` 改变 header / payload location / reasoning context；底层未支持时明确 unsupported，不能假成功 |
| `tool_call_policy` | prompt / request shape owner | `parallel_tool_calls` 只在模型支持且 responses policy 允许时打开 |
| `truncation_policy` | `lime-agent` tool call / shell output formatter | `bytes` 与 `tokens` policy 产生不同预算；缺 metadata fallback 到 current 默认 |
| `native_tool_policy` | `lime-agent` native tool inventory / shell surface gate | `unified_exec` 还必须经过 runtime feature / 平台 gate；`apply_patch_tool_enabled=false` 时不暴露 patch tool |

建议验证顺序：

```bash
cargo fmt --all --manifest-path "lime-rs/Cargo.toml"
cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent model_request_policy -- --nocapture
cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy -- --nocapture
cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent agent_tools -- --nocapture
npm run smoke:agent-runtime-current-fixture
npm run test:contracts
```

如果 `npm run test:contracts` 仍失败，必须证明失败不在第三十八刀写集；整体完成前仍建议要求 contract 通过。

### 5.3 第七刀：接管窗口与验证门槛

记录时间：2026-07-06。

本刀只改本矩阵和跟进计划，不接管源码热区。

Codex 复核锚点：

- `openai_models.rs` 把 `input_modalities`、`shell_type`、`default_reasoning_summary`、`support_verbosity`、`apply_patch_tool_type`、`truncation_policy`、`supports_parallel_tool_calls`、`experimental_supported_tools`、`use_responses_lite` 都定义在 `ModelInfo` / wire model 上。
- `responses_lite.rs` 证明 lite 模式移除 top-level `instructions/tools`，改用 input prefix / additional tools，并在 compact request 中把 `parallel_tool_calls` 收窄。
- `client_common.rs` 证明 Responses Lite 会选择不同 request builder。
- `turn.rs` 证明 prompt history 按 `model_info.input_modalities` 过滤，且 `parallel_tool_calls` 来自 `model_info.supports_parallel_tool_calls`。
- `tool_config.rs` 证明 shell surface 由 model info 与 feature/platform gate 共同决定。
- `tool_call.rs` 证明 tool call context 持有 `truncation_policy`。

第三十八刀源码接管门槛：

1. 第一 commit-size 切片只做 typed owner：`model_request_policy_from_metadata` / `model_request_policy_from_turn_context` 和 `model_request_policy` 定向测试先通过。
2. 第二切片才接 consumer：`responses_policy` + `tool_call_policy` 进入 request / prompt shape；若底层暂不支持 Responses Lite，必须显式 fail-closed 或标记 unsupported，不允许只记录 metadata。
3. 第三切片接 tool surface：`truncation_policy` 进入 tool / shell output formatter，`native_tool_policy` 进入 inventory / shell / apply_patch gate。
4. 每个 consumer 测试必须至少有一个“缺 metadata fallback 到 current 默认”的用例，避免新 metadata 变成硬依赖。
5. `npm run test:contracts` 只有在 typed owner 和四个高风险 consumer 都有定向证据后再跑；若仍失败，必须附当前失败文件和归属。

### 5.4 第八刀：Upstream Range Diff

记录时间：2026-07-06。

本刀新增 [upstream-diff-2026-07-06.md](./upstream-diff-2026-07-06.md)，从 `upstream-checkpoint.md` 的初始 HEAD 到 fetched origin 真实跑了一次 diff。

质量口径：

| source | range | 结论 |
| --- | --- | --- |
| Codex | `db887d03e1f907467e33271572dffb73bceecd6b..be33f80bc65159c094ecd06bf155afa3061ce23d` | `15` 个非 merge commit；保留 response metadata、plugin version、tool timing、multi-agent lifecycle、model availability 等 high-value 信号 |
| opencode | `17166b271fb9d7bf7128f0e63732dde0c10dd963..be73f465df6b20e0c3091f49ab83e89c0ede3b35` | `328` 个非 merge commit；只采纳 Provider / Model / Capability / ContentPart / media / provider lowering / LLM event reducer 相关信号 |

后续约束：

1. 下一次 diff 建议从 Codex `be33f80bc65159c094ecd06bf155afa3061ce23d` 与 opencode `be73f465df6b20e0c3091f49ab83e89c0ede3b35` 起算。
2. 这只证明 P3 已跑过一次真实 diff；整体完成仍需要后续周期性循环，不能因此跳过 P1-7 第三十八刀。
3. opencode Session / Tool runtime / UI / Effect 变化继续按 `reject-for-lime` 处理。

## 6. 不变约束

- `ModelCapabilitySummary` 只服务能力摘要和发送 gate，不承接 `*_policy` projection。
- `ProviderInfo` / `ModelInfo` picker DTO 不进入 `ResolvedModelRoute` execution route。
- `opencode` 只参考多模型 / 多模态能力表达，不参与 Session / Tool / UI / runtime 架构。
- 生产路径禁止 mock fallback；mock 只能是测试夹具或 contract guard。
- 旧 `lime-rs/src/**` 与旧 `agent_runtime_*` production surface 继续按 `dead / retired guard-only` 处理。
