# Clawstream Codex-derived Guardrail 推进计划

> 状态：active
> 创建时间：2026-07-06
> 关联路线图：`internal/roadmap/test/clawstream/README.md`
> 关联账本：`internal/roadmap/test/clawstream/scenario-ledger.md`
> 关联骨架：`internal/roadmap/test/clawstream/scenario-registry.json`
> 关联主线：`internal/research/refactor/v1`
> S1 细化计划：`internal/exec-plans/clawstream-s1-p0-implementation-plan.md`

## 1. 主目标

把 Codex 的 Thread / Turn / Item、app-server integration、core session/tool runtime、TUI snapshot 测试体系，转成 Lime Claw 的可执行护栏：

```text
Codex source test
  -> Lime scenarioId
  -> normalized event item
  -> projection / read model oracle
  -> DOM / Electron fixture evidence
  -> legacy cleanup gate
```

本计划不再停留在“列出需要覆盖的能力”。推进方式固定为先快速完成全量骨架，再回头补细节：`scenario-registry.json` 负责固定全量 scenarioId、execution batch、evidence gate、detail order、优先级、状态、目标证据层、下一步细节入口和验证命令；`scenario-ledger.md` 负责记录 Codex 来源、oracle、Electron evidence 和清理目标。每一批细节推进都必须按 registry 的 `detailOrder` 让账本状态前进，并为后续删除 Claw 旧 helper、旧 fallback、旧 mock / compat 旁路提供准入证据。

## 2. Current / Compat / Deprecated / Dead

| 分类         | 对象                                                                                                                        | 处理                                              |
| ------------ | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| `current`    | `RuntimeEvent -> Thread / Turn / Item -> Projection / ReadModel / ContentPart -> GUI / Electron fixture`                    | 继续演进，所有新测试和重构都落这里                |
| `compat`     | 无 `itemId / phase / sequence` 的历史 text delta、历史 `Message.content` hydrate 兜底                                       | 只允许 fail-closed 历史兜底，不作为新场景通过证据 |
| `deprecated` | timeline 二次重建完整过程流、overlay 全局助手文本缓冲、旧 Plan/update_plan UI owner、多套 input restore fallback            | 对应 guard 成立后删除                             |
| `dead`       | 生产 mock fallback、`agent_runtime_*` 生产 surface、旧 Tauri wrapper、自然语言 lifecycle regex、无 turnId terminal fallback | 删除或 retired guard-only                         |

## 3. 执行原则

1. Codex-first 是默认执行准则；除多模型 / 多模态 provider capability、media part、模型能力矩阵和 provider lowering 参考 opencode 外，命名、架构、状态机、工具生命周期、MCP、Skills、Multi-Agent、Plan hydrate、history hydrate、projection 和测试护栏都按 Codex 对齐。
2. 先补护栏，再删旧实现；没有对应 scenario ledger 行的旧实现不动刀。
3. 每个场景至少推进两层证据；用户主路径、history hydrate、MCP、Skills、Multi-Agent、artifact 必须三层齐备。
4. 全量骨架优先于单点深挖；除 P0 正在修的首字 / reasoning / terminal / inputbar 外，不在缺 registry 的情况下继续追加散点 fixture。
5. P0 先解决首字慢、reasoning 顺序、terminal 收尾、输入恢复；这些直接影响当前 Claw 可用性。
6. 现有 Electron fixture 不能当作完成态；已有 fixture 的场景还要补 item/projection oracle。
7. 不新增 parallel legacy path；新增能力默认走 App Server JSON-RPC current 主链和前端 projection。
8. 验证以当前风险为上限，先定向 `unit/projection/component`，再跑对应 Electron fixture 和聚合 smoke。

## 4. 阶段计划

### S0：索引与推进计划

状态：completed

目标：

- 建立 Clawstream 路线图、Codex-derived index、scenario ledger 和本执行计划。
- 把 Codex 具体测试函数索引到 Lime scenarioId，而不是只列大类。
- 明确下一批 P0 写集、验证入口和删除准入。

退出条件：

- [x] `internal/roadmap/test/clawstream/README.md` 落地。
- [x] `internal/roadmap/test/clawstream/codex-derived-index.md` 落地。
- [x] `internal/roadmap/test/clawstream/scenario-ledger.md` 落地。
- [x] `internal/roadmap/test/clawstream/scenario-registry.json` 全量骨架落地，覆盖 P0/P1/P2 共 59 个场景，并按 8 个 execution batch 固定“先骨架、后细节”的执行顺序、evidence gate、detail order、验证入口、骨架字段定义和 `current / compat / deprecated / dead` 分类。
- [x] `internal/roadmap/test/clawstream/scenario-registry.test.mjs` 落地，守住 registry 与 ledger 同步，并确保每个场景必须且只能属于一个 execution batch、一个 batch detail order，每个 batch 都有非空验证命令，且 ledger 每行必须具备 Codex 来源、标准事件项、Projection / GUI oracle 和清理目标。
- [x] 本计划落地并被 Clawstream README 反链。
- [x] 文档 diff 基础检查通过。

### S1：P0 Event / Item fixture skeleton

状态：in_progress

执行细节见 `internal/exec-plans/clawstream-s1-p0-implementation-plan.md`。S1 不做泛化清理，只推进 parser boundary、inputbar restore、stale terminal 和 running status 四个 P0 工作包；全量 registry 骨架已经先闭合，后续每个工作包完成后同步回写 `scenario-ledger.md` 与 `scenario-registry.json` 状态。

优先场景：

| scenarioId                       | 目标状态                            | 最小实现落点                                                                                           |
| -------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `startup-prewarm-first-output`   | `missing -> partial`                | fixture schema + projection unit，断言 startup/prewarm 不阻塞首个 reasoning/text                       |
| `reasoning-first-visible`        | `partial -> partial+guard`          | reasoning item oracle + MessageList/StreamingRenderer DOM 回归                                         |
| `stream-parser-boundary`         | `partial+guard -> covered-electron` | default Electron fixture 覆盖 message.delta / message.completed full snapshot / turn.completed 去重    |
| `terminal-contract-after-answer` | `partial+guard -> covered-electron` | terminal event controller / completion controller 单测 + failed-after-answer Electron current fixture  |
| `inputbar-restore-matrix`        | `partial+guard -> covered-electron` | inputbar restore pure reducer / hook unit matrix + Inputbar rich restore Electron current fixture      |
| `pending-steer-queue`            | `partial+guard -> covered-electron` | product-current Electron fixture 覆盖 queued promote/cancel/resume、FIFO、pop-front reindex 与 hydrate |

拟写集：

- `packages/agent-ui-contracts/**` 或现有 Claw fixture type owner。
- `packages/agent-runtime-projection/**`。
- `src/components/agent/chat/hooks/agentStreamRuntimeHandler.unit.test.ts`。
- `src/components/agent/chat/hooks/agentStreamCompletionController.test.ts`。
- `src/components/agent/chat/components/streamingProjectionGuard.unit.test.ts`。
- `src/components/agent/chat/components/MessageList*.test.tsx`。
- 必要时新增 `scripts/agent-runtime/fixtures/clawstream/**`，不要在 `scripts/` 根目录新增脚本。

退出条件：

- `scenario-ledger.md` 中上述 5 个 P0 场景至少推进到 `partial`。
- guard 明确禁止自然语言 lifecycle regex、无 turnId terminal fallback、startup note 闪现。
- 定向 unit/projection/component 通过。

当前进度：

- [x] `startup-prewarm-first-output` 已推进到 `covered-electron`：`clawstreamP0.test.mjs` 证明 startup/prewarm 状态不会进入 visible messages，首个 reasoning/text 可见；Agent UI performance summary 与 Electron fixture 公共断言要求 text-stream 场景携带 first visible output marker，并分离 provider wait 与 client local output；`complete` Electron current fixture 已实跑通过并导出 first text paint 证据。
- [x] `reasoning-first-visible` 已推进到 `covered-electron`：projection oracle 证明 reasoning 先于 text 成为 UI message part；MessageList / AgentThreadTimeline DOM guard 证明首字前 reasoning 不展示启动说明、hydrate 后不重复，且 summary 默认可见、raw reasoning 只在展开后出现；Electron current `reasoning-first-visible` fixture 已捕获 final/done 前 `思考中 + 正在输出` 中间态，完成态与 read model 均证明 reasoning sequence 早于 final message。
- [x] `terminal-contract-after-answer` 已推进到 `covered-electron`：projection 层命名 oracle 证明 `turn.completed` 只更新 runtime status、不合成正文，无 assistant text 时 fail closed；hook/runtime guard 证明已显示 partial answer 后 `turn_failed` 只补一个失败说明，不重复 error、不吞过程卡；Electron current `terminal-failed-after-answer` fixture 已实跑，证明 partial 保留、失败说明可见且只出现一次、输入框恢复、read model 标记 `failed`；Electron current `terminal-canceled-after-answer` fixture 已实跑，证明 stop 前 partial 与 running 同屏，`turn.canceled` 后 partial 保留且只出现一次、输入框恢复、read model 标记 `canceled`。2026-07-08 已补真实 App Server normalizer 负向矩阵，并把 current smoke 迁到 `turn.completed`；`lime-agent` workflow 正向 completion 已迁到 `runtime.status`；Rust 协议层 `AgentEvent::FinalDone` / `core::agent::StreamEvent::FinalDone` 与 `direct_text_generation` 旧 usage 读取已删除。剩余 `final_done` 只允许负向 schema / test-only guard、legacy fail-closed detector 和 live runner timeout grace，不再作为 product current terminal 或 Rust 输入兼容面。
- [x] guard 已禁止核心投影文件重新引入“启动处理流程 / 已接收请求”启动说明文案。
- [x] `stream-parser-boundary` 已推进到 `covered-electron`：message seed/delta/completed full text 只保留一个 final text part，不把 completed 全量正文追加成重复 finish tail；`<proposed_plan>` 跨 message added / delta / completed 边界会 materialize 为独立 `plan` part。2026-07-09 已在 default `complete` Electron fixture 补 `message.completed` full-text snapshot，`claw-chat-current-fixture-stream-parser-boundary-rerun` summary 证明 backend ledger 有序发出 `message.delta -> message.delta -> message.completed -> turn.completed`，GUI assistant scope 与 read model guard 文本均只出现一次，`streamParserCompletedFullTextObserved / guiStreamParserNoDuplicateFinalText / readModelStreamParserNoDuplicateFinalText` 全部为 true。
- [x] `plan-parser-boundary` 已推进到 `covered-electron`：结构化 `plan.delta/final` 与 proposed_plan block 都进入 `plan` part；Electron 正向 fixture 已覆盖 Plan owner / decision drawer / history hydrate revision binding；Electron `complete` 负向 fixture 已证明无 proposed_plan 不生成 Plan owner / decision；local decision guard 已阻止无 revision / legacy `update_plan:*` 生成实施确认。
- [x] 无 turnId terminal fallback / stale terminal active-turn oracle 已推进到 `covered-electron`：pure guard 对缺失 `terminalTurnId` fail closed，handler 回归证明旧 turn terminal 不误停新 active stream；Electron current `terminal-stale-guard` 骨架证明同 session 两轮 GUI/read model 完成，第二轮期间旧 terminal marker 不污染 UI。
- [x] `inputbar-restore-matrix` 已推进到 `covered-electron`：pure policy 覆盖 output-free / visible-output / thinking-only / patch-active / queued steer，manual stop 不再本地抢先清空 queued turns；rich restore 已接到 active stream stop -> Inputbar restore request，组件 guard 覆盖 text / images / pathReferences / inputCapabilityRoute；Electron current fixture 已覆盖 rich draft text/image/path/skill 取消恢复，`claw-chat-current-fixture-inputbar-restore-matrix-covered-rerun-summary.json` 中 rich restore scenario assertions 与 common assertions 全部为 true；App Server queued snapshot 与前端 normalizer 已保留 pending steer 的 attachments / pathReferences / textElements / skill route；frontend normalizer 已按 explicit `position` 恢复多 queued turn 顺序，同 position 或 legacy 缺 position 时保持输入稳定顺序；App Server oracle 已证明多 queued read model 顺序、pop-front resume 后剩余 queued reindex 为 `position=0`，且 top-level `queued_turns` 与 `thread_read.queued_turns` hydrate 同构；devserver 与 packaged Electron Gate B fixture 已覆盖正在输出时排队富输入、stop queued rich turn 后恢复 text/image/path/skill；Electron 多队列骨架已证明 rich queued turn 与第二个 plain queued turn 按 FIFO `position=0/1` 进入 read model；`inputbar-pending-steer-pop-front-resume` 已接入真实 Electron 骨架，并完成 product-current 细化，覆盖 GUI queued panel 的“立即执行”一键触发 `agentSession/queuedTurn/promote -> agentSession/turn/cancel -> agentSession/thread/resume`、rich backend turnStart、read model second reindex 与 reload hydrate；`EmptyStateComposerPanel` 与 `AgentChatWorkspace` 两条 text/path-only fallback 已删除并加 source guard，且 production source guard 已证明 UI restore draft 只能由 `EmptyState` 与 `Inputbar/useInputbarController` 写回；stop restore 的 `getSessionReadModel` optional runtime fallback 已删除并加 current guard；stop restore queued draft、explicit remove/promote queued turn、submit failure 与 terminal completion/failure/cancel 分支都不再手动裁剪本地队列；`queue_*` current event projection 只过滤 id、不重排 position，两个 Hook 的 queued turn upsert/remove 已收敛到 `agentQueuedTurnProjection` current helper；旧 `removeQueuedTurnState` 本地状态命名已改为 `removeQueuedTurnsFromProjection` 并封 production hook 回流；新增 queue / steer / draft production owner inventory guard，限定 `inputRestoreDraft`、restore plan、`setQueuedTurns` 与 queued projection helper 只能出现在 current owner 白名单。
- [x] `pending-steer-queue` 已推进到 `covered-electron`：`inputbar-pending-steer-rich-restore`、`inputbar-pending-steer-multi-queue` 与 `inputbar-pending-steer-pop-front-resume` 三条 Electron current fixture 覆盖 active output 期间富输入排队、第二条 plain steer FIFO `position=0/1`、rich turn 未提前发到 backend、GUI queued panel 一键 promote、App Server current `agentSession/queuedTurn/promote -> agentSession/turn/cancel -> agentSession/thread/resume`、rich backend turnStart 保留 image/file reference、second queue pop-front 后 reindex 为 `position=0`、reload hydrate 后 queued panel 与输入框可用；`claw-chat-current-fixture-inputbar-pending-steer-pop-front-resume-product-current-summary.json` 中 pending steer scenario assertions 与 common assertions 全部为 true。queue / steer / draft 旧 fallback inventory 继续并入 `inputbar-restore-matrix` 细节清理，不再阻塞本场景 covered-electron。
- [x] W2 input restore policy owner 已拆出巨型 flow control：`resolveInterruptedInputRestorePlan` / `resolveQueuedTurnsForRestore` 现在由 `agentStreamInputRestorePlan.ts` 承接，read model queue 解析由 `agentStreamReadModelParsing.ts` 承接；`agentStreamFlowControl.ts` 只做 stop/remove/promote 编排并降到 639 行，source guard 防止 rich queued draft 解析、restore sort 和 policy export 回流。
- [x] W2 package-side queue projection guard 补齐：`agentStreamFlowControl.currentGuard.test.ts` 现在把 `packages/agent-runtime-projection/src/queueEvents.ts` 纳入旁路扫描，禁止标准事件 package 回流 queuedTurns 写入、input restore owner、本地 reindex 或本地 sort。
- [x] W2 input restore request 清单固化：`AgentChatWorkspace.inputRestoreGuard.test.ts` 增加 production source 白名单，`inputRestoreRequest` / `InterruptedInputRestoreRequest` 只能停留在 source / flow owner、父级 request holder、pass-through scene/runtime/props/types，以及 `EmptyState` / `Inputbar` current UI owner。
- [x] W2 聚合 current fixture gate 已恢复：Aster reply backend source adapter lifetime 修复后，`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 2` 通过，`npm run smoke:agent-runtime-current-fixture` 完整通过并覆盖 pending steer、Plan hydrate、Skills Runtime、Multi-Agent Team、MCP、media、Expert Skills 与 Content Factory Article Editor，`liveProviderUsed=false`；该证据不把 W2 改口为完成态。
- [x] `running-status-preserved` 已推进到 `covered-electron`：projection guard 覆盖首字前启动态、首字后正文仍 running、completed/stale runtimeStatus 不丢 reasoning/tool/text；DOM guard 保留 inline running indicator 且禁止 startup note 回流；Electron current `cancel` / `cancel-then-continue` fixture 证明 stop 前同一 turn 的 scoped text 同时包含 assistant 正文和“正在输出”，且 startup note 不可见；stop helper 已要求 `requireVisibleOutput`，避免首字前 loading 被误作 running-status 证据。
- [x] `no-natural-language-lifecycle-regex` 已推进到 `covered-electron`：核心 Claw 投影 owner 扫描已扩面到 App Server event parser、agent protocol parser、Claw lifecycle hook、projection owner 与 `packages/agent-runtime-projection` owner，禁止展示文案、正文正则、动态 regex 和旧 duplicate helper 回流；`dom_text / text_regex / message_text` source 只允许停留在 `threadStatusRuntimeUpdate.ts` 负向检测 owner；reasoning-first / stream-parser / running-status Electron evidence 已证明 GUI 顺序来自结构化 sequence、read model 和 current terminal，而不是正文文案匹配。
- [x] `mcp-structured-content` 追加 current item / converter / display guard：`structuredContent` 从 stream / read model tool item 贯穿到 `toolCall.result`，GUI 显示 answer + reference id，transport envelope 不外露；MCP Electron fixture 和聚合 current fixture 均通过。
- [x] `electron-resize-reflow` 已推进到 `covered-electron`：新增 Electron current fixture `electron-resize-reflow`，复用 live-tail current turn 和 external controlled backend `turn.completed`，完成后打开 files right surface，采集 `wide / compact / restored` 三组 viewport DOM + screenshot，断言 tail marker range anchor、scroll near bottom、Inputbar bottom row、files right surface owner/root、message/input/right surface no-overlap、read model completed 与 backend ledger 对齐；该 fixture 是 `current / test-only fixture`，不新增生产 mock、不恢复 `agent_runtime_*`。

### S2：现有 Electron fixture 补 projection oracle

状态：pending

优先场景：

| scenarioId                        | 现状               | 目标                                                                                                                     |
| --------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| `cancel-then-continue`            | `covered-electron` | 补 stale terminal / active stream projection oracle                                                                      |
| `plan-parser-boundary`            | `covered-electron` | 正向 Plan owner / history hydrate 与无 proposed_plan 负向 owner fixture 已覆盖；后续只在扩展 Plan item taxonomy 时补细节 |
| `mcp-structured-content`          | `covered-electron` | 补 structuredContent precedence / truncation / envelope hiding projection oracle                                         |
| `skills-runtime-search-read-gate` | `covered-electron` | 补 search/read/gate/invoke item-level oracle                                                                             |
| `multi-agent-resume-lineage`      | `covered-electron` | 补 parent/child Thread/Turn/Item lineage oracle                                                                          |
| `image-generation-item`           | `covered-electron` | 补 ImageGeneration item taxonomy oracle                                                                                  |
| `web-search-item-sequence`        | `covered-electron` | 补 WebSearch / WebFetch / final text sequence oracle                                                                     |

退出条件：

- 已有 Electron scenarios 不再只靠 GUI 文案和 backend ledger 证明。
- 每个 covered-electron 场景都有 event item 和 projection oracle。
- `npm run smoke:agent-runtime-current-fixture` 仍覆盖这些场景。

### S3：P1 Runtime capability gaps

状态：in_progress

优先场景：

| scenarioId                      | Codex 对齐点                                                                    | 目标                                                                                                                                                                                                                                                                                        |
| ------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mcp-resource-read`             | `mcp_resource.rs`                                                               | resource/template read 成为 read model item / evidence；package-side projection guard 已到 `partial+guard`，仍缺 App Server read model surface 和 Electron resource/evidence panel oracle                                                                                                   |
| `mcp-elicitation-resume`        | `mcp_server_elicitation.rs`                                                     | form/OpenAI form request/resolution 绑定 thread/turn/capability；package-side projection guard 已到 `partial+guard`，仍缺 App Server serverRequest/read model 和 Electron 表单提交后继续 tool call oracle                                                                                   |
| `mcp-inventory-status`          | `mcp_server_status.rs`                                                          | raw/sanitized/collision/auth-only 状态稳定；package-side projection guard 已到 `partial+guard`，仍缺 App Server inventory read model 与 Electron inventory/status panel oracle                                                                                                              |
| `mcp-thread-scope`              | `executor_mcp.rs`                                                               | selected executor/plugin MCP 只暴露给对应 thread/environment；package-side projection guard 已到 `partial+guard`，仍缺 App Server inventory read model 与 Electron thread 切换 oracle                                                                                                       |
| `mcp-startup-status`            | `mcp_startup.rs`                                                                | startup status 是 thread-scoped runtime status，不是 assistant item；package-side projection guard 已到 `partial+guard`，仍缺 App Server notification/read model 与 Electron header/warning oracle                                                                                          |
| `skills-catalog-scope`          | `skills_list.rs`                                                                | cwd roots、cache generation、extra roots 与 runtime request roots 同源；package-side projection guard 已到 `partial+guard`，仍缺 App Server skills list/read model 与 Electron workspace/cwd 切换 oracle                                                                                    |
| `skill-mention-input-restore`   | `composer_submission.rs`                                                        | structured skill/plugin mention binding、textElements 与 attachments 在 submit/restore 间同源；package-side projection guard 已到 `partial+guard`，仍缺 App Server/GUI blocked/queued/cancel restore 同构 oracle                                                                            |
| `selected-capability-stack`     | `selected_capability_stack.rs`                                                  | availability/resume 进入 request metadata / runtime item；package-side projection guard 已到 `partial+guard`，仍缺 App Server request/read model 与 Electron capability/status oracle                                                                                                       |
| `plugin-capability-runtime`     | `plugin_read.rs` / `plugin_list.rs` / `plugin_install.rs`                       | plugin read/list/install 只改变 capability catalog，不绕过 turn/start；package-side projection guard 已到 `partial+guard`，仍缺 App Server plugin read/list/install read model 与 Electron plugin/capability oracle                                                                         |
| `multi-agent-item-taxonomy`     | `protocol/src/items.rs` / `multi_agents_spec_tests.rs`                          | spawn/send/followup/wait/list/interrupt 都是结构化 Team item；package-side projection guard 已到 `partial+guard`，仍缺 App Server read model item 与 Electron Team DOM oracle                                                                                                               |
| `multi-agent-tool-schema`       | `multi_agents_spec_tests.rs`                                                    | Team schema 与 UI 展示同源，legacy field fail closed；package-side projection guard 已到 `partial+guard`，仍缺 Rust runtime surface 和 Electron schema error UI oracle                                                                                                                      |
| `multi-agent-visual-snapshot`   | TUI `multi_agents.rs` / `app_server.rs` collab wait/spawn snapshots             | Team transcript、roster、delegation graph 与 worker notification 必须同源于 `collabToolCall` item；package-side projection guard 已到 `partial+guard`，仍缺 App Server read model 与 Electron DOM/截图 oracle                                                                               |
| `review-start-item`             | `review.rs` / `review_mode.rs` / app-server `review/start` tests                | entered/exitedReviewMode、inline/detached review thread、approval item id 与 prompt transcript hygiene 必须同源；package-side projection guard 已到 `partial+guard`，仍缺 App Server read model 与 Electron review lane oracle                                                              |
| `local-remote-image-input`      | `turn_start.rs` / `request_validation.rs` / TUI image restore tests             | local/data/remote image refs、detail、placeholder map 与 rejection evidence 必须同源；package-side projection guard 已到 `partial+guard`，仍缺 App Server read model 与 Electron image restore/hydrate oracle                                                                               |
| `dynamic-tool-call-item`        | `dynamic_tools.rs` / `router_tests.rs` / app-server dynamic tool tests          | dynamic/extension tool namespace、deferred inventory、arguments、structured content item 与 remote image failure 必须同源；package-side projection guard 已到 `partial+guard`，仍缺 App Server read model 与 Electron tool UI oracle                                                        |
| `structured-output-message`     | `output_schema.rs` / app-server output schema tests / tool output context tests | output_schema request、assistant typed payload、copy/export、function/custom tool output type、content_items 与 structuredContent 必须同源；package-side projection guard 已到 `partial+guard`，仍缺 App Server read model 与 Electron JSON/table/card renderer oracle                      |
| `realtime-conversation-item`    | `realtime_conversation.rs` / websocket v2 realtime tests                        | Lime 当前用 not-current guard 防止 realtime 泄漏到 transcript/tool；启用时必须 thread-scoped started/closed、item/transcript/audio 投影、append-only text input 与 sideband audio non-blocking；package-side projection guard 已到 `partial+guard`，仍缺 App Server/Electron current oracle |
| `thread-resume-running-stream`  | `thread_resume.rs`                                                              | running resume active stream 继续绑定同一 thread/turn；package-side projection guard 已到 `partial+guard`，仍缺 App Server resume/read model 与 Electron running stream oracle                                                                                                              |
| `thread-inject-items`           | `thread_inject_items.rs`                                                        | raw Responses API item 注入 history / next model input；package-side projection guard 已到 `partial+guard`，仍缺 App Server read model item 与 Electron hydrate oracle                                                                                                                      |
| `thread-status-runtime-update`  | `thread_status.rs`                                                              | thread/status/changed 是 runtime notification，不写 transcript；package-side projection guard 已到 `partial+guard`，仍缺 App Server status surface 与 Electron header/sidebar oracle                                                                                                        |
| `thread-fork-lineage`           | `thread_fork.rs` / `thread_read.rs`                                             | forkedFromId / lastTurnId / parent item lineage 进入 read/sidebar/history/evidence；package-side projection guard 已到 `partial+guard`，仍缺 Electron sidebar/history/evidence oracle                                                                                                       |
| `thread-rollback-projection`    | `thread_rollback.rs` / core session rollback tests                              | ThreadRolledBack marker、removed turn pruning 与 settings/reference/token replay；package-side projection guard 已到 `partial+guard`，仍缺 App Server rollback read model 与 Electron history oracle                                                                                        |
| `thread-archive-delete-hydrate` | `thread_archive.rs` / `thread_unarchive.rs` / `thread_delete.rs`                | archive/unarchive/delete 是 thread metadata state，不改 item 内容；package-side projection guard 已到 `partial+guard`，仍缺 App Server metadata/read model 与 Electron sidebar/list/read/restore oracle                                                                                     |
| `thread-settings-live-update`   | `thread_settings_update.rs` / TUI `thread_settings.rs`                          | thread/settings/updated 是 thread-scoped metadata/runtime settings state，不写 transcript；package-side projection guard 已到 `partial+guard`，仍缺 App Server read model/settings surface 与 Electron model/cwd/header/sidebar oracle                                                      |
| `token-usage-replay`            | `token_usage_replay.rs` / `thread_resume.rs` / `thread_fork.rs`                 | restored token usage 是 thread/turn metadata，不写 transcript；package-side projection guard 已到 `partial+guard`，仍缺 App Server resume/fork read model 与 Electron status line/header/context-window oracle                                                                              |
| `context-compaction-item`       | `compaction.rs` / core reconstruction tests                                     | contextCompaction 是 started/completed thread item，replacement history verbatim；package-side projection guard 已到 `partial+guard`，仍缺 App Server compaction read model 与 Electron history hydrate/status oracle                                                                       |
| `goal-lifecycle-hydrate`        | `thread_resume.rs` / TUI `status_and_layout.rs`                                 | goal status/budget/usage 是 thread metadata/status surface；package-side projection guard 已到 `partial+guard`，仍缺 App Server objective/read model facade 与 Electron footer/header/status DOM oracle                                                                                     |
| `command-execution-item`        | `command_exec.rs` / `process_exec.rs`                                           | command/process lifecycle live + hydrate 同构                                                                                                                                                                                                                                               |
| `apply-patch-filechange`        | `apply_patch_tests.rs`                                                          | patch diff / approval / failure 共用 FileChange item                                                                                                                                                                                                                                        |

退出条件：

- MCP / Skills / Multi-Agent 不再靠裸 tool name、全局 registry、文本摘要或 mock success 证明。
- command / patch / approval 进入统一 item taxonomy。
- 对应旧实现状态从 `deprecated` 推进到 `dead` 或 retired guard-only。

### S4：History / compaction / visual snapshot gaps

状态：in_progress

优先场景：

| scenarioId                     | 目标                                                                                                                                                                                                                                                                                  |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `thread-read-page-isomorphic`  | package-side guard、App Server read model hydrate regression 与 Electron session history fixture 已覆盖 read/list/resume 同 thread、turn/item order、page cursor、history hydrate DOM 同构和 forbidden turn/start；剩余 archived read / forked_from / failed idle flag 作为细节回头补 |
| `thread-resume-running-stream` | running thread resume 继续绑定 active stream                                                                                                                                                                                                                                          |
| `thread-fork-lineage`          | parent/child lineage 进入 sidebar/history/evidence                                                                                                                                                                                                                                    |
| `thread-rollback-projection`   | rollback marker 与 read model replay 同步                                                                                                                                                                                                                                             |
| `context-compaction-item`      | compaction item 不 rewrite old items                                                                                                                                                                                                                                                  |
| `markdown-render-snapshot`     | package-side guard 已覆盖 markdown/code/table/file link/CJK snapshot；仍缺 Electron screenshot/DOM oracle                                                                                                                                                                             |
| `diff-artifact-snapshot`       | package-side guard 已覆盖 add/delete/update/rename/multi-file diff snapshot；仍缺 Electron screenshot/DOM oracle                                                                                                                                                                      |
| `electron-resize-reflow`       | package-side guard 已覆盖 MessageList/Inputbar/right surface resize anchor；仍缺 Electron screenshot/DOM oracle                                                                                                                                                                       |
| `live-tail-commit`             | package-side guard + Electron current fixture 已覆盖首字完成前可见、running 状态、overflow commit、scroll anchor、表格尾部、read model 与 backend ledger `turn.completed`；剩余 screenshot baseline 并入 resize/reflow                                                                |
| `status-surface-matrix`        | package-side guard 已覆盖 footer/header/title/rate limit/model/reasoning/goal/status preview matrix；仍缺 Electron status surface oracle                                                                                                                                              |

退出条件：

- history hydrate 与 live stream 同构有 fixture replay 证明。
- visual snapshot 不再只靠 `pageText.includes`。
- 可以开始拆 Claw 巨型文件和删除重复 projection。

### S5：删除旧路与拆大文件

状态：pending

删除准入：

1. `scenario-ledger.md` 对应场景已至少 `partial`，高风险场景已三层齐备。
2. guard 已阻止旧路回流。
3. 定向测试和必要 Electron fixture 通过。
4. 删除对象已分类为 `dead`，或 `deprecated` 且退出条件满足。

优先清理：

- 自然语言 lifecycle regex。
- startup note / timeout 合成首字。
- 无 turnId terminal fallback。
- legacy `update_plan` UI owner。
- overlay 全局 final answer 缓冲。
- timeline 二次重建完整过程流。
- 多套 input restore fallback，包括仍绕过 current read model / normalizer / `EmptyState` / `Inputbar` owner 的 queue / steer / draft 并行状态；已收掉 stop restore queued draft、explicit remove/promote、submit failure、terminal side-effect 本地队列裁剪、queue event 本地 position 重排、Hook 内联 queued turn projection helper、旧本地队列删除命名和非 current UI restore owner。
- MCP naked tool/mock fallback。
- expert skillRefs 直连。
- agent-first orphan history。

## 5. 验证矩阵

### 文档 / 计划

```bash
git diff --check -- ".gitignore" "internal/roadmap/test/README.md" "internal/roadmap/test/clawstream/README.md" "internal/roadmap/test/clawstream/codex-derived-index.md" "internal/roadmap/test/clawstream/scenario-ledger.md" "internal/exec-plans/clawstream-codex-derived-guardrail-plan.md"
rg -n "[ \t]+$" ".gitignore" "internal/roadmap/test/README.md" "internal/roadmap/test/clawstream/README.md" "internal/roadmap/test/clawstream/codex-derived-index.md" "internal/roadmap/test/clawstream/scenario-ledger.md" "internal/exec-plans/clawstream-codex-derived-guardrail-plan.md"
```

### P0 定向

```bash
npm test -- streamingProjectionGuard.unit.test.ts streamingContentPartOrder.unit.test.ts streamingContentPartSegments.unit.test.ts
npm test -- agentStreamRuntimeHandler.unit.test.ts agentStreamCompletionController.test.ts agentStreamUserInputSendPreparation.test.ts
npm test -- MessageList.reasoningFlow.test.tsx MessageList.streamingTurns.test.tsx MessageList.runtimeStatus.test.tsx
```

### Electron fixture

```bash
npm run smoke:agent-runtime-current-fixture
node scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs --scenario cancel-then-continue
node scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs --scenario plan
node scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs --scenario web-tools-rendering
node scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs --scenario mcp-structured-content
node scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs --scenario inputbar-rich-restore
```

### 高风险 GUI

```bash
npm run verify:gui-smoke
```

## 6. 当前进度日志

### 2026-07-10

- MCP structuredContent 折叠摘要优先级补齐：`InlineToolProcessStep` 对 current MCP 工具名（`mcp` / `mcp__*`）优先展示 `structuredContent` detail，避免已完成工具在折叠态只显示通用“已完成这一步”，导致 GUI fixture 看不到 answer/reference id；同时收窄 `resultText` memo 依赖，避免对象级 `toolCall.result` 触发 hooks lint 警告。
- 稳定回归补齐：`InlineToolProcessStep.test.tsx` 覆盖 `output="已完成这一步"` 且 `structuredContent.answer/ids` 存在时，折叠摘要必须展示结构化答案和 reference id。
- 验证通过：`npx eslint "src/components/agent/chat/components/InlineToolProcessStep.tsx" "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" --max-warnings 0`；`npx vitest run "src/components/agent/chat/components/InlineToolProcessStep.test.tsx"`；`npm run typecheck -- --pretty false`；`node "scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs" --scenario mcp-structured-content --prefix claw-chat-current-fixture-mcp-structured-content-regression --timeout-ms 240000`；`npm run smoke:agent-runtime-current-fixture`。最新 MCP summary 证明 `guiMcpStructuredContentVisible=true`、`guiMcpStructuredContentEnvelopeHidden=true`、`readModelMcpStructuredContentObserved=true`、`liveProviderUsed=false`。
- Claw Workspace 右侧 surface 本地状态第一刀拆分：新增 `useWorkspaceRightSurfaceLocalStateRuntime` 作为 current owner，承接 `manualRightSurface`、files/object canvas/article/browser/plugin surface 本地状态、统一 `clearActiveRightSurfaceTargets` 与 `openArticleWorkspaceRightSurface`；`AgentChatWorkspace.tsx` 不再直接声明这些 state，也不再在 artifact click / expert panel toggle 中重复手写多 setter 清理。该刀直接降低超 5000 行 Workspace 的 state 堆叠，并服务 Article Editor / files right surface 的 current GUI 主路径。
- 守卫同步：`AgentChatWorkspace.inputRestoreGuard.test.ts` 的 right surface boundary 改为检查 current hook owner；首页首发真实 session 导航守卫改为检查 `useWorkspaceTaskCenterSendRuntime.ts` 的 `handleNonMaterializedSessionReady`，并确认 Workspace 仍把 `persistTaskCenterMaterializedSessionNavigation` 接入 hook，避免旧内联函数名迁移后守卫失真。
- 右侧 surface 拆分验证通过：`npx eslint "src/components/agent/chat/AgentChatWorkspace.tsx" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceLocalStateRuntime.ts" "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" --max-warnings 0`；`npx vitest run "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts"`；`npx vitest run "src/components/agent/chat/workspace/agentChatWorkspaceHelpers.unit.test.ts" "src/components/agent/chat/workspace/workspaceRightSurfaceRuntimeProjection.unit.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfacePendingRuntime.unit.test.tsx"`；`npm run typecheck -- --pretty false`；`npm run smoke:agent-runtime-current-fixture` 完整通过并覆盖 Content Factory Article Editor / articleDraft 右侧产物闭环，`liveProviderUsed=false`。
- Claw Workspace 右侧 surface action 第二刀拆分：`handleToggleExpertInfoPanel` 已从 `AgentChatWorkspace.tsx` 下沉到 `useWorkspaceRightSurfaceActionRuntime`，与 files/object canvas/article/browser/harness/shell/plugin surface toggle 共享同一个 action owner；父级 Workspace 只接收 `handleToggleExpertInfoPanel`，不再内联清理 right surface state 或直接 consume/dismiss `expertInfo` pending request。该改动继续把超 5000 行 Workspace 收窄为 facade，`current` owner 是 right surface action runtime，未新增 `compat` 路径。
- Claw 失败态文案 i18n 补洞：恢复五语言 `agentChat.runtime.failure.prefix`，修复 runtime failure projection 在缺 final answer 时把 key 原样渲染进消息的问题；该 key 属于 current Agent runtime presentation，不恢复插件中心或旧 runtime 文案。
- 右侧 surface action 验证：`npx eslint "src/components/agent/chat/AgentChatWorkspace.tsx" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceActionRuntime.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceLocalStateRuntime.ts" "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" --max-warnings 0`；`npx vitest run "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts"`；`npx vitest run "src/components/agent/chat/workspace/agentChatWorkspaceHelpers.unit.test.ts" "src/components/agent/chat/workspace/workspaceRightSurfaceRuntimeProjection.unit.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfacePendingRuntime.unit.test.tsx"`；`npm run typecheck -- --pretty false`；`src/components/agent/chat/hooks/agentStreamRuntimeHandler.unit.test.ts` 的 4 个 failure prefix 定向用例通过；`npx vitest run "src/lib/soul/interactionCopy.unit.test.ts"` 通过；五语言 JSON 检查确认 `agentChat.runtime.failure.prefix` 存在。
- Claw Workspace 右侧 surface host 第三刀拆分：新增 `useWorkspaceRightSurfaceHostRuntime`，承接 Article Editor action dispatch、selected object 写回与 `renderWorkspaceRightSurfaceHostRuntime` 参数投影；`AgentChatWorkspace.tsx` 不再直接调用 host render、不再导入 `submitWorkspaceArticleEditorActionIntent` 或 selection writeback helper。该刀把 right surface host 渲染与 Article Editor 写回固定到 `current` host runtime，父级继续收窄为 facade，`AgentChatWorkspace.tsx` 从 `5013` 行降到 `4969` 行。
- 右侧 surface host 验证：`npx eslint "src/components/agent/chat/AgentChatWorkspace.tsx" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceHostRuntime.ts" "src/components/agent/chat/workspace/WorkspaceRightSurfaceHostRuntime.tsx" "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" --max-warnings 0`；`npx vitest run "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts"`（7 passed）；`npx vitest run "src/components/agent/chat/workspace/workspaceRightSurfaceRuntimeProjection.unit.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfacePendingRuntime.unit.test.tsx"`（35 passed）；`npm run typecheck -- --pretty false`；`git diff --check` 覆盖本轮相关文件。新增 guard 禁止父级回流 `renderWorkspaceRightSurfaceHostRuntime({`、`submitWorkspaceArticleEditorActionIntent` 与 `buildWorkspaceArticleWorkspaceSelectionUpdateRequest`。
- Claw Workspace 右侧 surface artifact 打开第四刀拆分：新增 `useWorkspaceRightSurfaceArtifactOpenRuntime`，承接 artifact click 进入 Article Editor right surface 的识别、competing surface 关闭和 `articleWorkspace/objectCanvas` pending consume；`AgentChatWorkspace.tsx` 不再直接导入 `buildArticleWorkspaceForArtifactOpen`，也不再维护 `articleEditorRightSurfaceRef` / `rightSurfacePendingActionsRef`。该刀把 artifact -> right surface action 固定为 `current` runtime owner，父级继续只做 facade 接线，未新增 `compat` 路径。
- Claw Workspace Article Editor 图片槽位第五刀拆分：新增 `useWorkspaceArticleEditorImageSlotRuntime`，承接 Article Editor 图片槽位 intent 的 `@配图` 命令组装、缺 prompt toast 与切回 chat 布局；`AgentChatWorkspace.tsx` 不再直接导入 `buildWorkspaceArticleEditorImageSlotCommand` 或 `WorkspaceArticleWorkspaceImageSlotIntent`。该刀继续把 Article right surface 子动作固定到 workspace runtime owner，父级从 `4969` 行降到 `4920` 行。
- 右侧 surface artifact / image slot 验证：`npx eslint "src/components/agent/chat/AgentChatWorkspace.tsx" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceArtifactOpenRuntime.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceArtifactOpenRuntime.unit.test.tsx" "src/components/agent/chat/workspace/useWorkspaceArticleEditorImageSlotRuntime.ts" "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" --max-warnings 0`；`npx vitest run "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceArtifactOpenRuntime.unit.test.tsx" "src/components/agent/chat/workspace/workspaceArticleEditorImageSlotDispatch.unit.test.ts" "src/components/agent/chat/workspace/workspaceArticleWorkspaceArtifactOpen.unit.test.ts"`（14 passed）；`npx vitest run "src/components/agent/chat/workspace/workspaceRightSurfaceRuntimeProjection.unit.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfacePendingRuntime.unit.test.tsx"`（35 passed）；`npm run typecheck -- --pretty false`；`git diff --check` 覆盖本轮相关文件。新增 guard 禁止父级回流 `buildArticleWorkspaceForArtifactOpen`、`articleEditorRightSurfaceRef`、`rightSurfacePendingActionsRef`、`buildWorkspaceArticleEditorImageSlotCommand` 与 `WorkspaceArticleWorkspaceImageSlotIntent`。
- Claw Workspace 右侧 surface pending bridge 第六刀拆分：新增 `useWorkspaceRightSurfacePendingBridgeRuntime`，承接 App Server pending request runtime、active surface 自动刷新判断与 pending actions bind；`AgentChatWorkspace.tsx` 不再直接调用 `useWorkspaceRightSurfacePendingRuntime`、`shouldAutoRefreshWorkspaceRightSurfacePending` 或 `bindRightSurfacePendingActions`。该刀把 pending request 生命周期固定到 `current` bridge runtime owner，父级继续只保留接线，行数降到 `4902`，未新增 `compat` 路径。
- 右侧 surface pending bridge 验证：`npx eslint "src/components/agent/chat/AgentChatWorkspace.tsx" "src/components/agent/chat/workspace/useWorkspaceRightSurfacePendingBridgeRuntime.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceArtifactOpenRuntime.ts" "src/components/agent/chat/workspace/useWorkspaceArticleEditorImageSlotRuntime.ts" "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" --max-warnings 0`；`npx vitest run "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceArtifactOpenRuntime.unit.test.tsx" "src/components/agent/chat/workspace/workspaceArticleEditorImageSlotDispatch.unit.test.ts" "src/components/agent/chat/workspace/workspaceArticleWorkspaceArtifactOpen.unit.test.ts"`（15 passed）；`npx vitest run "src/components/agent/chat/workspace/workspaceRightSurfaceRuntimeProjection.unit.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfacePendingRuntime.unit.test.tsx"`（35 passed）；`npm run typecheck -- --pretty false`；`git diff --check` 覆盖本轮相关文件。新增 source guard 禁止父级回流 pending runtime、auto-refresh helper 与 pending actions bind。
- Claw Workspace 右侧 surface coordinator 第七刀拆分：新增 `useWorkspaceRightSurfaceCoordinatorRuntime`，把 pending bridge、derived runtime、projection runtime 与 action runtime 的组合关系收敛到一个 current owner；`AgentChatWorkspace.tsx` 不再直接导入或调用 `useWorkspaceRightSurfacePendingBridgeRuntime` / `useWorkspaceRightSurfaceDerivedRuntime` / `useWorkspaceRightSurfaceProjectionRuntime` / `useWorkspaceRightSurfaceActionRuntime`，本地 right surface state 以 `rightSurfaceLocalState` 领域对象传入 coordinator。该刀减少父组件对 right surface 状态机内部顺序、availability 和 pending consume 细节的了解，父组件从 `4902` 行降到 `4815` 行，未新增 `compat` 路径。
- 右侧 surface coordinator 验证：`npx eslint "src/components/agent/chat/AgentChatWorkspace.tsx" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceCoordinatorRuntime.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfacePendingBridgeRuntime.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceArtifactOpenRuntime.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceLocalStateRuntime.ts" "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" --max-warnings 0`；`npx vitest run "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceArtifactOpenRuntime.unit.test.tsx" "src/components/agent/chat/workspace/workspaceRightSurfaceRuntimeProjection.unit.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfacePendingRuntime.unit.test.tsx" "src/components/agent/chat/workspace/workspaceArticleEditorImageSlotDispatch.unit.test.ts" "src/components/agent/chat/workspace/workspaceArticleWorkspaceArtifactOpen.unit.test.ts"`（50 passed）；`npm run typecheck -- --pretty false`；`npm run smoke:agent-runtime-current-fixture` 完整通过，覆盖首页首发、Coding Workbench、图片命令、cancel/approval/Inputbar/Plan/Skills/Multi-Agent/MCP/Expert Panel，以及 Content Factory Article Editor / articleDraft 右侧产物闭环，`liveProviderUsed=false`；`npm run verify:gui-smoke` 通过，覆盖 renderer loaded、App Server initialized、Claw workbench shell ready 与 memory settings ready（构建阶段出现重复 rpath 的非致命 `install_name_tool` 输出，命令退出码为 0）；`git diff --check` 覆盖本轮相关文件。source guard 已封住父级重新直接调用 pending/derived/projection/action 子 runtime。
- Claw Workspace 右侧 surface host 参数第八刀拆分：`useWorkspaceRightSurfaceHostRuntime` 改为直接消费 `rightSurfaceRuntime: WorkspaceRightSurfaceCoordinatorRuntime`，父级不再把 coordinator 的 browser/files/object/plugin/harness/trace state 与 host action handlers 拆成一长串参数再传回 host。`AgentChatWorkspace.tsx` 的 `rightSurfaceContent` 调用只保留 article、expert panel、harness base props、workspace id 和外部打开回调等真正 host 输入；host-only surface 字段由 host runtime owner 解包，父级从 `4815` 行降到 `4772` 行，未新增 `compat` 路径。
- 右侧 surface host 参数验证：`npx eslint "src/components/agent/chat/AgentChatWorkspace.tsx" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceHostRuntime.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceCoordinatorRuntime.ts" "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" --max-warnings 0`；`npx vitest run "src/components/agent/chat/AgentChatWorkspace.inputRestoreGuard.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfaceArtifactOpenRuntime.unit.test.tsx" "src/components/agent/chat/workspace/workspaceRightSurfaceRuntimeProjection.unit.test.ts" "src/components/agent/chat/workspace/useWorkspaceRightSurfacePendingRuntime.unit.test.tsx" "src/components/agent/chat/workspace/workspaceArticleEditorImageSlotDispatch.unit.test.ts" "src/components/agent/chat/workspace/workspaceArticleWorkspaceArtifactOpen.unit.test.ts"`（50 passed）；`npm run typecheck -- --pretty false`。新增 source guard 要求父级 host 调用传 `rightSurfaceRuntime`，并禁止回流 `activePluginSurfaceContainerId`、browser/files/object/plugin host-only 参数。
- Gate B 复核：第七刀后完整 `npm run smoke:agent-runtime-current-fixture` 已通过，包含 `home-hotpath` 与 `home-hotpath-greeting`，并覆盖 Content Factory Article Editor / articleDraft 右侧产物闭环；`npm run verify:gui-smoke` 也已通过，覆盖 renderer loaded、App Server initialized、Claw workbench shell ready 与 memory settings ready。此前 `home-hotpath-greeting` 的预算失败不再是当前 Gate B 阻塞。

### 2026-07-09

- S4 追加 `status-surface-matrix` package-side projection guard：新增 `packages/agent-runtime-projection/src/statusSurfaceMatrix.ts` 与 `tests/statusSurfaceMatrix.test.mjs`，按 Codex TUI `status_surface_previews`、`status_widget_active_snapshot` 与 `renamed_thread_footer_title_snapshot` 规范 status surface matrix；footer/header/title/rate limit/model/reasoning/goal/status preview 必须全覆盖并绑定同一 session/thread、共享 presentation owner，model/reasoning/goal/title/runtime/rate limit 都必须带结构化 facts，自然语言状态推断和组件本地重复命名 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 Electron header/footer/title/status screenshot/DOM oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（272 passed）。
- S4 追加 `live-tail-commit` package-side projection guard：新增 `packages/agent-runtime-projection/src/liveTailCommit.ts` 与 `tests/liveTailCommit.test.mjs`，按 Codex TUI `vt100_live_commit.rs::live_001_commit_on_overflow` 与 `flush_answer_stream_requests_scrollback_reflow_for_live_table_tail` 规范 live tail commit；首字/首 token 必须在 overflow commit 前可见，live tail 与 committed history item 分离但同源，overflow commit 不得改写 item sequence，scroll anchor 和 table tail reflow 必须稳定，overlay 全局助手缓冲与 pageText-only live tail oracle fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 Electron 长输出/滚动/表格 reflow oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（272 passed）。
- S4 追加 `electron-resize-reflow` package-side projection guard：新增 `packages/agent-runtime-projection/src/resizeReflowSnapshot.ts` 与 `tests/resizeReflowSnapshot.test.mjs`，按 Codex TUI `resize_reflow.rs` 规范 resize/reflow snapshot；before/after viewport 必须绑定同一 active Thread/Turn，MessageList visible anchor、Inputbar row/bottom、right surface owner/request 与 projected item sequence 必须稳定，layout state 写回 thread item 和 pageText-only layout oracle fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 Electron resize screenshot/DOM oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（272 passed）。
- S4 追加 `diff-artifact-snapshot` package-side projection guard：新增 `packages/agent-runtime-projection/src/diffArtifactSnapshot.ts` 与 `tests/diffArtifactSnapshot.test.mjs`，按 Codex TUI `diff_render` snapshots 与 apply patch flow 规范 diff artifact snapshot；artifact/workbench/review 必须共享 structured diff item，`fileChanges` 覆盖 add/delete/update/rename/multiple files，line numbers、gutter signs 与 long-line wrap 必须显式保留，raw patch string 输入 / 渲染和 fake artifact card 都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 Electron diff screenshot/DOM oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（259 passed）。
- S4 追加 `markdown-render-snapshot` package-side projection guard：新增 `packages/agent-runtime-projection/src/markdownRenderSnapshot.ts` 与 `tests/markdownRenderSnapshot.test.mjs`，按 Codex TUI `markdown_render` snapshots 与 `chatwidget_markdown_code_blocks_vt100_snapshot` 规范 markdown renderer snapshot；assistant final text、tool output、artifact preview 必须声明同一 markdown renderer owner，source/rendered coverage 覆盖 heading/table/file link/code fence/CJK/mixed-width wrap，file link 必须保留 path/line metadata，pageText-only、raw markdown table/fence 和 renderer owner 分叉都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 Electron screenshot/DOM oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（259 passed）。
- S3 追加 `realtime-conversation-item` package-side not-current/current contract guard：新增 `packages/agent-runtime-projection/src/realtimeConversationItem.ts` 与 `tests/realtimeConversationItem.test.mjs`，按 Codex app-server `realtime_conversation_streams_v2_notifications`、`websocket_v2_text_input_is_append_only_while_response_is_active`、`websocket_v2_tool_call_does_not_block_sideband_audio` 与 realtime v2 protocol parser 规范 realtime boundary；Lime 当前无 realtime current owner 时必须显式 `not_current`，任何 `thread/realtime/*` / `conversation.item.*` / `response.output_*` 泄漏到普通 transcript 或 tool output 都 fail closed；未来启用时必须保留 thread-scoped `started/closed` lifecycle、itemAdded、transcript delta/done、output audio delta、projected realtime item、active response 期间 text append-only 且不新建 response、delegated tool call 期间 sideband audio 不阻塞。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server/Electron current oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（249 passed）。
- S3 追加 `structured-output-message` package-side projection guard：新增 `packages/agent-runtime-projection/src/structuredOutputMessage.ts` 与 `tests/structuredOutputMessage.test.mjs`，按 Codex app-server `turn_start_accepts_output_schema_v2`、`turn_start_output_schema_is_per_turn_v2`、exec `exec_includes_output_schema_in_request`、core `json_result.rs` 与 `context_tests.rs::custom_tool_calls_should_roundtrip_as_custom_outputs` / `function_payloads_remain_function_outputs` 规范 structured output；`output_schema` 必须进入 `text.format={ name: "codex_output_schema", type: "json_schema", strict: true, schema }` 且只作用于当前 turn，assistant JSON result 必须保留 typed payload，JSON/table/card renderer、copy/export 必须保留结构，function/custom tool output 不能互相降级，content item array 与 `structuredContent` 不能文本化，success metadata 必须跟随 typed payload。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server read model 与 Electron JSON/table/card renderer oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（245 passed）。
- S3 追加 `dynamic-tool-call-item` package-side projection guard：新增 `packages/agent-runtime-projection/src/dynamicToolCallItem.ts` 与 `tests/dynamicToolCallItem.test.mjs`，按 Codex `dynamic_tools.rs`、`router_tests.rs::specs_filter_deferred_dynamic_tools`、`extension_tool_executors_are_model_visible_and_dispatchable` 与 app-server `dynamic_tool_call_round_trip_sends_content_items_to_model` 规范 dynamic/extension tool item；dynamic spec 必须保留 canonical `function/namespace`、tool name、description、inputSchema 与 `deferLoading`，deferred tool 不得提前 model-visible，extension executor 必须 model-visible 且 dispatchable，call item 必须保留 call id、namespace、tool、arguments、status、success/error 与 `input_text` / `input_image` content items，同 namespace 重名、无 namespace 裸工具名匹配、remote HTTP image 成功输出都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server read model 与 Electron tool UI oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（240 passed）。
- S3 追加 `local-remote-image-input` package-side projection guard：新增 `packages/agent-runtime-projection/src/localRemoteImageInput.ts` 与 `tests/localRemoteImageInput.test.mjs`，按 Codex `turn_start_defaults_local_image_detail_to_high`、`turn_start_forwards_custom_local_image_detail`、`request_handlers_reject_remote_image_urls` 与 TUI image restore/backtrack tests 规范 local/data/remote image input；local image 缺 detail 默认 `high`，custom `original` 必须保留，`low` fail closed，HTTP(S) remote 不得进入 submitted/model input 且必须有 rejection evidence，data URL 必须可提交，restore/hydrate image refs 和 placeholder map 必须稳定，纯文本 `[Image #N]` 不能作为结构化图片证据。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server read model 与 Electron image restore/hydrate oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（235 passed）。
- S3 追加 `review-start-item` package-side projection guard：新增 `packages/agent-runtime-projection/src/reviewStartItem.ts` 与 `tests/reviewStartItem.test.mjs`，按 Codex `review/start`、`review_start_runs_review_turn_and_emits_code_review_item`、`review_start_exec_approval_item_id_matches_command_execution_item`、`review_start_with_detached_delivery_returns_new_thread_id` 与 TUI `live_review_prompt_item_is_not_rendered` 规范 review lane；inline review 必须留在源 thread，detached review 必须先发新 `thread/started` 再流 review item，entered/exitedReviewMode 必须绑定同一 review turn，command approval `item_id` 必须匹配 commandExecution item，review prompt 和 final review 都不得落入普通 chat transcript。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server read model 与 Electron review lane oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（229 passed）。
- S3 追加 `multi-agent-visual-snapshot` package-side projection guard：新增 `packages/agent-runtime-projection/src/multiAgentVisualSnapshot.ts` 与 `tests/multiAgentVisualSnapshot.test.mjs`，按 Codex TUI `multi_agents.rs`、`app_server.rs::live_app_server_collab_wait_items_render_history`、`collab_spawn_end_shows_requested_model_and_effort` 规范 Team visual snapshot；`collabToolCall` item 必须保留 senderThreadId、receiver/newThreadId、requested model/effort、prompt 与 agent state，Team transcript、roster、delegation graph、worker notification 都必须从同一 item lineage 派生，legacy plain transcript、缺 parent/newThread 绑定、缺 visual surface 和缺 requested model/effort 都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server read model 与 Electron DOM/截图 oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（224 passed）。
- S3 追加 `multi-agent-item-taxonomy` package-side projection guard：新增 `packages/agent-runtime-projection/src/multiAgentItemTaxonomy.ts` 与 `tests/multiAgentItemTaxonomy.test.mjs`，按 Codex `CollabAgentToolCallItem / SubAgentActivityItem` 与 `multi_agents_spec_tests.rs` 规范 Team item taxonomy；spawn/send/followup/wait/list/interrupt 必须都是结构化 item，visual alias 必须映射回 Codex v2 tool name，running/completed/interrupted 状态、parent thread、turn id、child thread lineage、Team transcript / worker card / review lane item id binding 都纳入 guard，legacy text summary timeline 与 agent-first orphan history fail closed。registry / ledger 已把该场景从 `partial` 推进到 `partial+guard`；剩余缺口是 App Server read model item 与 Electron Team DOM oracle。
- S3 追加 `mcp-resource-read` package-side projection guard：新增 `packages/agent-runtime-projection/src/mcpResourceRead.ts` 与 `tests/mcpResourceRead.test.mjs`，以 Codex current `list_mcp_resources` / `list_mcp_resource_templates` / `read_mcp_resource` 为唯一正向 contract，旧 `ListMcpResourcesTool` / `ReadMcpResourceTool` 只作为 legacy alias fail closed；`read_mcp_resource` snapshot 保留 server、uri、mime、content preview、truncation、thread scope、artifact/evidence refs，并投影到 `timeline_evidence`，string-only ordinary tool output 不得伪装成 resource content。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server read model surface 与 Electron resource/evidence panel oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（107 passed）。
- S3 追加 `mcp-elicitation-resume` package-side projection guard：新增 `packages/agent-runtime-projection/src/mcpElicitation.ts` 与 `tests/mcpElicitation.test.mjs`，把 Codex `McpServerElicitationRequestParams` / `McpServerElicitationRequestResponse` 投影成 `action.required` / `action.resolved` HITL snapshot，保留 requestId、server、threadId、turnId、form/OpenAI form schema、client capability、response action/content/meta；unsupported OpenAI form capability 与 accepted response missing content 都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server serverRequest/read model surface 与 Electron 表单提交后继续 tool call oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（112 passed）。
- S3 补齐 `mcp-inventory-status` package-side projection guard 记录：新增 `packages/agent-runtime-projection/src/mcpInventoryStatus.ts` 与 `tests/mcpInventoryStatus.test.mjs`，按 Codex `mcp_server_status.rs` 保留 raw server/tool name、sanitized collision group、thread scope 与 `toolsAndAuthOnly` detail；naked `server__tool` 和 auth-only 加载 resources fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server inventory read model 与 Electron inventory/status panel oracle。
- S3 追加 `mcp-thread-scope` package-side projection guard：新增 `packages/agent-runtime-projection/src/mcpThreadScope.ts` 与 `tests/mcpThreadScope.test.mjs`，按 Codex `executor_mcp.rs::selected_executor_plugin_exposes_its_mcps_only_to_that_thread` 规范 selected root、plugin server owner、thread inventory、tool call 与 OAuth notification scope；Thread A 的 selected plugin MCP 不得进入 Thread B 或 global inventory，unscoped / wrong-thread tool call 和 OAuth notification 都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server inventory read model 与 Electron thread 切换 oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（121 passed）。
- S3 追加 `mcp-startup-status` package-side projection guard：新增 `packages/agent-runtime-projection/src/mcpStartupStatus.ts` 与 `tests/mcpStartupStatus.test.mjs`，按 Codex `mcp_startup_update` / `mcp_startup_complete` 和 TUI `mcp_startup.rs` 语义保留 thread scope、server/status/error/failureReason、runtime header、active turn running 共存、other-thread ignore、stale update fail-closed，并禁止 startup 状态进入 assistant item。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server notification/read model 与 Electron header/warning oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（127 passed）。
- S3 追加 `skills-catalog-scope` package-side projection guard：新增 `packages/agent-runtime-projection/src/skillsCatalogScope.ts` 与 `tests/skillsCatalogScope.test.mjs`，按 Codex `skills_list.rs::skills_list_skips_cwd_roots_when_environment_disabled`、`skills_list_preserves_requested_cwd_order`、`skills_extra_roots_set_updates_process_runtime_roots` 规范 `params.cwds`、`response.data[].cwd`、`extraRoots`、`runtimeRequestRoots`、`cacheGeneration` 与 skill path/scope/source provenance；环境关闭仍加载 cwd root、requested cwd 顺序漂移、runtime roots 漂移、缺 cache generation / provenance、workspace skill 命中 legacy global cache 都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server skills list/read model 与 Electron workspace/cwd 切换 oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（135 passed）。
- S3 追加 `skill-mention-input-restore` package-side projection guard：新增 `packages/agent-runtime-projection/src/skillMentionInputRestore.ts` 与 `tests/skillMentionInputRestore.test.mjs`，按 Codex `composer_submission.rs::submission_prefers_selected_duplicate_skill_path`、`blocked_image_restore_preserves_mention_bindings` 与 `plan_mode.rs::submit_user_message_emits_structured_plugin_mentions_from_bindings` 规范 Draft / restored Draft / submitted UserInput 三层投影；duplicate skill 必须按 selected path 提交为 `Skill` item，blocked image restore 必须保留 mention binding、textElement 与 local image，plugin binding 必须提交为 structured `Mention`，binding 降级成纯文本 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server/GUI blocked/queued/cancel restore 同构 oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（141 passed）。
- S3 追加 `selected-capability-stack` package-side projection guard：新增 `packages/agent-runtime-projection/src/selectedCapabilityStack.ts` 与 `tests/selectedCapabilityStack.test.mjs`，按 Codex `selected_capability_stack.rs::selected_capability_stack_tracks_environment_availability_and_resume`、`selected_capabilities_become_available_between_samples_in_one_turn` 规范 selected root、environment availability generation、resume/reattach 与 request/sample 注入；environment unavailable 不得注入 selected skill/MCP/plugin tool，resume 不得复用旧 available generation，reattach 必须以新 generation 恢复 capability，同一 turn 中途可用后必须重新 sample，历史 sample 改写 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server request/read model 与 Electron capability/status oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（150 passed）。
- S3 追加 `plugin-capability-runtime` package-side projection guard：新增 `packages/agent-runtime-projection/src/pluginCapabilityRuntime.ts` 与 `tests/pluginCapabilityRuntime.test.mjs`，按 Codex `plugin_read.rs::plugin_skill_read_reads_remote_skill_contents_when_remote_plugin_enabled`、`plugin_list.rs::plugin_list_returns_plugin_interface_with_absolute_asset_paths`、`plugin_install.rs::plugin_install_makes_bundled_mcp_servers_available_to_followup_requests` 和 core `request_plugin_install.rs::remote_plugin_install_refreshes_plugin_and_apps_tool_caches` 规范 plugin read/list/install 的 capability catalog 投影；remote skill read 必须保留 SKILL.md contents，interface asset 必须是 plugin root 内绝对路径，install 不得写全局 MCP config，install 后 catalog 必须带 generation/provenance/cache refresh，followup turn/start 必须看到 installed skill/MCP/app tools 并过滤 stale `request_plugin_install`。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server plugin read/list/install read model 与 Electron plugin/capability oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（156 passed）。
- S3 追加 `thread-resume-running-stream` package-side projection guard：新增 `packages/agent-runtime-projection/src/threadResumeRunningStream.ts` 与 `tests/threadResumeRunningStream.test.mjs`，按 Codex `thread_resume.rs::thread_resume_keeps_in_flight_turn_streaming`、`thread_resume_rejoins_running_thread_even_with_override_mismatch`、`thread_resume_rejects_history_when_thread_is_running` 规范 running resume active turn、initial turns page 与 live stream binding；resume 返回 NotLoaded、running turn 缺失 / completed / itemsView full、history override、override model/cwd 污染 running thread、shadow session 或 stream thread/turn mismatch 都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server resume/read model 与 Electron running stream oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（163 passed）。
- S3 追加 `thread-inject-items` package-side projection guard：新增 `packages/agent-runtime-projection/src/threadInjectItems.ts` 与 `tests/threadInjectItems.test.mjs`，按 Codex `thread_inject_items.rs::thread_inject_items_adds_raw_response_items_to_thread_history`、`thread_inject_items_adds_raw_response_items_after_a_turn` 规范 `thread/inject_items` raw Responses API item 注入；injected item 必须进入 rollout history 和下一次 model input，不得提前进入上一轮 model input，turn linkage、raw metadata 与 insertion order 必须稳定，remote image URL、metadata drop、order drift、hydrate 成 user input 或 assistant final tail 都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server read model item 与 Electron hydrate oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（170 passed）。
- S3 追加 `thread-status-runtime-update` package-side projection guard：新增 `packages/agent-runtime-projection/src/threadStatusRuntimeUpdate.ts` 与 `tests/threadStatusRuntimeUpdate.test.mjs`，按 Codex `thread_status.rs::thread_status_changed_emits_runtime_updates`、`thread_status_changed_can_be_opted_out` 规范 `thread/status/changed` runtime notification；Active -> Idle/SystemError/NotLoaded 只更新 runtime status，optOutNotificationMethods 必须过滤，waiting active flags 必须保留，状态不得落入 transcript/read-model item，status surface 必须按 thread scoped，DOM text inference fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server read model/status surface 与 Electron header/sidebar oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（178 passed）。
- S3 追加 `thread-fork-lineage` package-side projection guard：新增 `packages/agent-runtime-projection/src/threadForkLineage.ts` 与 `tests/threadForkLineage.test.mjs`，按 Codex `thread_fork.rs`、`thread_read.rs::thread_read_returns_forked_from_id_for_forked_threads` 与 TUI history replay forked line snapshot 规范 fork lineage；fork 必须生成新 thread id，`forkedFromId` 指向 source thread，`lastTurnId` 只保留 terminal prefix，parent item ids、thread/read、sidebar/history/evidence lineage 必须同源，source rollout mutation、parentThreadId 误用、plain thread render 都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server lineage read model 与 Electron sidebar/history/evidence oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（184 passed）。
- S3 追加 `thread-rollback-projection` package-side projection guard：新增 `packages/agent-runtime-projection/src/threadRollbackProjection.ts` 与 `tests/threadRollbackProjection.test.mjs`，按 Codex `thread_rollback.rs`、core `thread_rollback_drops_last_turn_from_history`、`thread_rollback_recomputes_previous_turn_settings_and_reference_context_from_replay`、`thread_rollback_persists_marker_and_replays_cumulatively` 规范 rollback replay；ThreadRolledBack marker 必须持久化且累计 replay，removed turns 不得出现在 read model / resume / history window，settings / reference context / token usage 必须按剩余历史重算，turn in progress、numTurns=0、UI-only hide 都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server rollback read model 与 Electron history oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（192 passed）。
- S3 追加 `thread-archive-delete-hydrate` package-side projection guard：新增 `packages/agent-runtime-projection/src/threadArchiveDeleteHydrate.ts` 与 `tests/threadArchiveDeleteHydrate.test.mjs`，按 Codex `thread_archive.rs`、`thread_unarchive.rs`、`thread_delete.rs`、`thread_read.rs::thread_read_can_return_archived_threads_by_id` 规范 archive/unarchive/delete hydrate；archive notification、archived read by id、active/archived list consistency、unarchive restore、delete descendant notifications、deleted read/resume fail closed、item fingerprint stable、path state consistency 与 sidebar-only archive 都纳入 guard。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server metadata/read model 与 Electron sidebar/list/read/restore oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（200 passed）。
- S3 追加 `thread-settings-live-update` package-side projection guard：新增 `packages/agent-runtime-projection/src/threadSettingsLiveUpdate.ts` 与 `tests/threadSettingsLiveUpdate.test.mjs`，按 Codex `thread_settings_update.rs`、`thread_metadata_update.rs`、TUI `thread_settings.rs` 与 `override_turn_context_sends_thread_settings_update` 规范 settings live update；settings-only update 不启动模型请求、active turn settings 不被污染、future turn 使用 updated model/serviceTier/cwd、ack 不直接改 cached session、notification 才更新 cache，transcript/read-model item leak、service tier clear、sandboxPolicy+permissions 和 turn/start override missing notification 都 fail closed。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server read model/settings surface 与 Electron model/cwd/header/sidebar oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（207 passed）。
- S3 追加 `token-usage-replay` package-side projection guard：新增 `packages/agent-runtime-projection/src/tokenUsageReplay.ts` 与 `tests/tokenUsageReplay.test.mjs`，按 Codex `token_usage_replay.rs`、`thread_resume.rs`、`thread_fork.rs` 与 core `record_initial_history_seeds_token_info_from_rollout` 规范 restored token usage；resume/fork 后 `thread/tokenUsage/updated` 必须在下一 turn 前 replay，`excludeTurns` 必须 skip，turn attribution、rebuilt turn position / stale interrupted tail、connection-scoped replay、total/last/modelContextWindow、context-window surface 更新与 transcript/read-model item leak 都纳入 guard。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server resume/fork read model 与 Electron status line/header/context-window oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（213 passed）。
- S3 追加 `context-compaction-item` package-side projection guard：新增 `packages/agent-runtime-projection/src/contextCompactionItem.ts` 与 `tests/contextCompactionItem.test.mjs`，按 Codex `compaction.rs`、`thread_compact_start_triggers_compaction_and_returns_empty_response` 与 core reconstruction tests 规范 contextCompaction item；`item/started` + `item/completed` 必须同 id，deprecated `thread/compacted` 不能替代 current item，replacement history 必须 verbatim，old item fingerprint 不得 rewrite，read model / follow-up model input 必须保留 compaction item，`thread/compact/start` 只返回 empty ack。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 App Server compaction read model 与 Electron history hydrate/status oracle。验证通过：`npm --prefix "packages/agent-runtime-projection" test`（219 passed）。
- S3 追加 `goal-lifecycle-hydrate` package-side projection guard：新增 `packages/agent-runtime-projection/src/goalLifecycleHydrate.ts` 与 `tests/goalLifecycleHydrate.test.mjs`，按 Codex `thread_goal_set_persists_resumable_stopped_statuses`、`thread_goal_set_edits_objective_without_resetting_usage`、`thread_goal_lifecycle_emits_analytics_and_clear_deletes_goal` 与 TUI goal status snapshot 规范 goal hydrate；blocked / usageLimited wire status 必须映射，objective edit 不得重置 goal id / created_at / tokens_used / time_used_seconds，clear 后 `thread/goal/get` 必须返回 null，header/footer/goal status surface 必须同源，analytics 不得序列化 objective/token_budget，goal lifecycle 不得落入 assistant transcript 或普通 read-model item。registry / ledger 已把该场景从 `partial` 推进到 `partial+guard`；剩余缺口是 App Server objective/read model facade 与 Electron footer/header/status DOM oracle。

### 2026-07-06

- 建立 Clawstream 路线图、Codex-derived index、scenario ledger。
- 从 Codex app-server v2、core session/tool handlers、TUI chatwidget、TUI suite/snapshots 抽出 P0/P1/P2 场景账本。
- 新增本推进计划，后续实现必须按 `scenario-ledger.md` 更新状态。
- 文档级检查通过：`git diff --check -- ".gitignore" "internal/roadmap/test/README.md" "internal/roadmap/test/clawstream/README.md" "internal/roadmap/test/clawstream/codex-derived-index.md" "internal/roadmap/test/clawstream/scenario-ledger.md" "internal/exec-plans/clawstream-codex-derived-guardrail-plan.md"`；尾随空白扫描无命中。
- 新增 `packages/agent-runtime-projection/tests/clawstreamP0.test.mjs`，把 `startup-prewarm-first-output`、`reasoning-first-visible`、`terminal-contract-after-answer` 推进到 projection 层护栏。
- 扩展 `streamingProjectionGuard.unit.test.ts`，禁止核心投影文件重新引入“启动处理流程 / 已接收请求”启动说明文案。
- 定向验证通过：`npm --prefix "packages/agent-ui-contracts" run build && npm --prefix "packages/agent-runtime-projection" run build && node --test "packages/agent-runtime-projection/tests/clawstreamP0.test.mjs"`；`npx vitest run "src/components/agent/chat/components/streamingProjectionGuard.unit.test.ts"`。
- 当前仍未实现 DOM / Electron fixture 层，也未删除旧 runtime/UI 实现。
- 按最新准则补充 Codex-first 执行原则：除多模型 / 多模态 provider capability、media part、模型能力矩阵和 provider lowering 参考 opencode 外，Clawstream 命名、架构、状态机、工具生命周期和测试护栏都回到 Codex。
- W1 parser/projection oracle 当时补齐到 projection 层：`stream-parser-boundary` 与 `plan-parser-boundary` 推进到 `partial+guard`，projection guard 证明 completed full text 不再作为重复 finish tail 追加，且 `<proposed_plan>` 跨 added / delta / completed 边界会 materialize 为独立 `plan` part；后续 `plan-parser-boundary` 与 `stream-parser-boundary` 已分别升到 `covered-electron`。
- W2 第一段落地：`inputbar-restore-matrix` 从 `missing` 推进到 `partial`，`resolveInterruptedInputRestorePlan` 成为输入恢复判定 pure owner；`agentStreamInputRestorePolicy.unit.test.ts` 覆盖 output-free、visible-output、thinking-only、patch-active、queued steer/manual interrupt；`stopActiveAgentStream` 不再本地清空 queued turns。
- W2 追加聚合验证通过：`npm run smoke:agent-runtime-current-fixture` 覆盖 `cancel-then-continue`、Plan hydrate、Skills Runtime、Multi-Agent Team、MCP structuredContent、Expert Skills / Plaza / Panel 与 Content Factory Article Editor，`liveProviderUsed=false`。
- W2 rich restore 接线落地：`submittedDraft` 随 active stream 保存，`stopActiveAgentStream` 只在 output-free / thinking-only 中断时请求恢复；`Inputbar` 恢复 text、images、pathReferences 与 installed skill route，且不把纯空白输入恢复成 whitespace；定向验证通过 `npx vitest run "src/components/agent/chat/hooks/agentStreamFlowControl.test.ts" "src/components/agent/chat/hooks/agentStreamInputRestorePolicy.unit.test.ts" "src/components/agent/chat/hooks/agentStreamSubmissionLifecycle.test.ts" "src/components/agent/chat/components/Inputbar/hooks/useInputbarSend.test.tsx" "src/components/agent/chat/components/Inputbar/index.test.tsx"`。
- 聚合 current fixture guard 同步加严：Expert Panel scenario 不再把空会话壳误判为目标 session，侧栏打开必须匹配 inputbar `data-session-id`；单场景 `expert-panel-skills-runtime` 和完整 `npm run smoke:agent-runtime-current-fixture` 均通过，完整聚合覆盖 Content Factory Article Editor，`liveProviderUsed=false`。
- W4 落地：`messageListItemProjection.unit.test.ts`、`MessageList.runtimeStatus.test.tsx`、`MessageList.streamingTurns.test.tsx` 覆盖输出中 running status preserved、terminal 后只清运行态、startup note 不回流。
- MCP structuredContent current fixture 缺口已修复：`agentStreamThreadItemController.test.ts`、`agentStreamToolItemMessageSync.unit.test.ts`、`components/timeline-utils/itemConverters.unit.test.ts` 与 `ToolCallDisplay.test.tsx` 覆盖 structuredContent answer/reference id 的 item -> toolCall -> GUI 链路。
- 定向验证通过：`npx vitest run "src/components/agent/chat/components/messageListItemProjection.unit.test.ts" "src/components/agent/chat/components/MessageList.runtimeStatus.test.tsx" "src/components/agent/chat/components/MessageList.streamingTurns.test.tsx" "src/components/agent/chat/hooks/agentStreamThreadItemController.test.ts" "src/components/agent/chat/hooks/agentStreamToolItemMessageSync.unit.test.ts" "src/components/agent/chat/components/timeline-utils/itemConverters.unit.test.ts" "src/components/agent/chat/components/ToolCallDisplay.test.tsx"`。
- GUI / current fixture 验证通过：`npm run build:renderer:electron`；`npm run smoke:claw-chat-current-fixture -- --scenario mcp-structured-content --timeout-ms 180000`；`npm run smoke:agent-runtime-current-fixture`，完整覆盖 `cancel-then-continue`、Plan hydrate、Skills Runtime、Multi-Agent Team、MCP structuredContent、Expert Skills / Plaza / Panel、Content Factory Article Editor，`liveProviderUsed=false`。

### 2026-07-08

- W2 Expert Panel follow-up provider/model 回流修复：`expert-panel-skills-runtime` 的失败根因是新 session 创建后没有把当前 workspace provider/model preference 同步到 session refs，follow-up turn 因此回落到全局默认 `lime-hub/gpt-5.2-pro`。`createFreshSession` 已按 current workspace preferences 初始化新 session，并只在 provider/model 均非空时标记 synced，避免空偏好污染 workspace。验证通过：单场景 `expert-panel-skills-runtime`、完整 `npm run smoke:agent-runtime-current-fixture` 与相关定向 Vitest 均通过，完整聚合覆盖 history/cache hydration、Coding Workbench、图片命令、`cancel-then-continue`、Inputbar rich restore、pending steer、Plan hydrate、Skills Runtime、Multi-Agent Team、MCP structuredContent、media reference、Expert Skills / Plaza / Panel、Content Factory Article Editor，`liveProviderUsed=false`。该证据只恢复 current fixture gate，不把 W2 改口为完成态。
- W3 legacy terminal 注入 owner 与 current smoke 收口：App Server 原始事件 normalizer 已直接 fail closed `done / final_done / cancelled / turn.done / turn.final_done / turn.cancelled`，代码产物工作台 Electron fixture 与 standalone App Server external backend smoke 都改为只使用 current `turn.completed`，live web-tool evidence terminal detector 只认 `turn.completed`；合同守卫禁止这两条 current smoke 回流 `turn.final_done`。验证通过：相关 7 个 Vitest 文件 142 tests passed、`npm run test:contracts`、`npm run smoke:app-server-external-backend`，真实 sidecar evidence events 包含 `turn.completed`。剩余 `final_done` 字符串只允许停留在负向 schema / test-only guard、legacy fail-closed detector 和 live runner 的超时 grace 观测参数中，不再作为 product current 完成态证据。
- W3 追加 GUI 侧 current terminal 证据：代码产物工作台 Electron fixture `gui-coding-input` 场景已用 external backend 两轮发出 `turn.completed`，summary 明确 `backendEmittedCurrentTerminal=true`、`backendDidNotEmitLegacyTerminal=true`，同时证明 App Server JSON-RPC、工作台打开、工具轨迹 GUI evidence、无 live provider、无 invoke errors。证据前缀 `code-artifact-workbench-gui-coding-input-turn-completed-w3-ledger`。
- W3 terminal legacy surface 第一刀收口：Claw GUI 完成计划命名从旧 `FinalDone` 改成 `TerminalCompletion`，current `turn_completed` 是唯一成功收尾入口；fixture event summary 只把 `turn.completed / turn.failed / turn.canceled` 计为 current terminal，不再把 `turn.final_done` 或 `turn.done / turn.cancelled` 算作 terminal 证据。legacy `turn.final_done` 仍保留在 App Server event projection 的 fail-closed detector 和负向测试里，分类为 `compat / test-only`，用于证明它不关闭 current 路由、不投递 GUI。
- W3 聚合 current fixture 复跑通过：`npm run smoke:agent-runtime-current-fixture` 在 current terminal 迁移后完整通过，覆盖 history/cache hydration、`turn_completed` 工具收尾、Electron/App Server fixture guard、Coding Workbench、图片命令、普通画图意图、`cancel-then-continue`、Inputbar rich restore、pending steer rich / multi queue / pop-front resume、Plan hydrate、Skills Runtime、Multi-Agent Team、MCP structuredContent、media reference、Expert Skills / Plaza / Panel、Content Factory Article Editor，`liveProviderUsed=false`。此前 packaged fixture build 的 Electron Host typecheck 被测试 helper `setTurnStartRequestMode` union 缺少 `"hang-request"` 阻塞，已在 `electron/appServerHost.test.ts` 补齐；验证通过 `npm run typecheck:electron` 与 `npx vitest run "electron/appServerHost.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`。该修复只恢复 current fixture gate，不新增生产协议面。
- W3 Rust workflow 正向旧终态收口：`lime-rs/crates/agent/src/skill_execution.rs` 的 workflow 成功 / 失败 completion 不再发 `RuntimeAgentEvent::FinalDone`，改发 `RuntimeAgentEvent::RuntimeStatus`，由 App Server raw event 映射为非终态 `runtime.status`；真正 turn terminal 继续由 App Server current 回合边界发 `turn.completed / turn.failed / turn.canceled`。验证通过：`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent skill_execution --lib -j 2`（9 passed）与 `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime_status_raw_runtime_event_maps_to_non_terminal_status_event --lib -j 2`（1 passed）。
- W3 Rust `FinalDone` 协议枚举删除：`lime-rs/crates/agent/src/protocol.rs` 已删除 `AgentEvent::FinalDone`，`lime-rs/crates/core/src/agent/types.rs` 已删除 `StreamEvent::FinalDone`，`lime-rs/crates/agent/src/direct_text_generation.rs` 不再从旧 `FinalDone` 读取 usage；App Server 测试 backend 命名从 `FinalDone*` 改为 `TurnCompleted*`。这些 surface 现分类为 `dead / deleted`，负向 schema / test-only guard 继续保留以证明旧终态不关闭 current 路由。
- W3 current 文档事实源同步收口：`internal/roadmap/appserver/frontend-integration-matrix.md`、`internal/roadmap/agentworkbench/v2.md` 与旧 AgentUI roadmap 中把 `turn.final_done / final_done` 写成 current / 合法兼容流的段落已改为 `turn.completed / turn.failed / turn.canceled` current 终态，legacy `final_done` 只允许负向 guard / fail-closed detector，避免后续按旧文档恢复旧终态。
- W3 package-side legacy terminal fail-closed 收口：`packages/agent-ui-contracts/src/runtimeTerminal.ts` 新增单一 `LEGACY_RUNTIME_TURN_TERMINAL_EVENT_CLASSES` / `isLegacyRuntimeTurnTerminalEventClass(...)` owner，schema 与 validation 复用该 owner，不再各自维护旧终态列表；`packages/agent-runtime-projection/src/appServerFacts.ts` 在 legacy event type 上忽略 payload `status`，避免 `done / turn.final_done / turn.cancelled` 携带 `done/completed/cancelled` payload 时污染成 completed。验证通过：`npm --prefix "packages/agent-ui-contracts" test`（30 passed）与 `npm --prefix "packages/agent-runtime-projection" test`（96 passed）。
- W3 frontend detector 接入同一 owner：`src/lib/api/agentRuntime/appServerEventPayloadUtils.ts` 不再维护第三套 legacy terminal 字符串列表，改为复用 `isLegacyRuntimeTurnTerminalEventClass(...)`；`appServerEventStream.test.ts` 的负向矩阵复用 `LEGACY_RUNTIME_TURN_TERMINAL_EVENT_CLASSES`。验证通过：`npx vitest run "src/lib/api/agentRuntime/appServerEventStream.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`（13 passed）与 `npm run test:contracts`。
- W3 current fixture 文档 residual 收口：`internal/roadmap/turn/test-cases.md`、Claw streaming / Plan runtime / MCP / Agent App / Soul / App Server 执行计划中的聚合 current fixture evidence 已改为 `turn.completed` 工具收尾，legacy `final_done` 只作为负向 guard / fail-closed 说明保留。复核 `scripts/agent-runtime/expert-skills-live-runner.mjs` 后确认旧 grace 只围绕 `threadSettled(threadRead)` 做通用 live runner 轮询，不是 `final_done` owner；随后已把脚本参数和内部字段从 `completionGraceMs / --completion-grace-ms` 改成 neutral `settledGraceMs / --settled-grace-ms`。验证通过：live runner 旧名扫描无命中、`node --check "scripts/agent-runtime/expert-skills-live-runner.mjs"`、`npx vitest run "scripts/agent-runtime/expert-skills-live-runner.test.mjs" --silent=passed-only --disableConsoleIntercept`（7 passed）、相关文档 `npx prettier --check` 与 `git diff --check`；精准扫描只剩“从旧词改为新词”、`dead` 分类、负向 guard 或 history-only 记录。
- W3 追加 `command-execution-item` projection guard：`command_execution` thread item 转 `exec_command` tool part 时会保留 command/cwd/exit_code metadata，但只把真实 stdout 作为展示 output，精确剥离 `Exit code: ... / Wall time / Output:` shell wrapper，避免 GUI 或 hydrate 把 wrapper 当用户可读命令输出。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 working -> completed/failed hydrate、processId/duration/stdout/stderr 细化和 Electron command lifecycle oracle。
- W3 追加 `apply-patch-filechange` projection guard：`patch` timeline item 现在投影为带 `thread_item_patch` provenance 的 `file_changes_batch`，structured `metadata.file_change` 的 diff/path/count/status 作为唯一 diff summary 输入，并保留 `environmentId / approvalId / approvalState` 供后续 artifact/approval oracle 共享；同路径 `file_artifact` 被 projection 去重，不再重复渲染 fake artifact。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是独立 FileChange event item、Electron artifact workbench 和 approval modal 端到端 oracle。
- W3 追加 `approval-request-and-resume` owner linkage guard：`approval_request` 与 `request_user_input` thread item 转 `ActionRequired` 时保留 `threadId / turnId` scope，避免 hydrate / resolved action 无法回到原 turn 或误匹配全局 pending queue。registry / ledger 已把该场景从 `partial` 推进到 `partial+guard`；剩余缺口是 pending approval hydrate 后 GUI 可操作，以及 resolve 后经 App Server current resume 继续原 turn 的 Electron oracle。
- W3 追加 `approval-request-and-resume` Electron current fixture：新增 `approval-request-resume` 场景，external controlled fixture 第一次 `turnStart` 停在 `tool.started + action.required`，GUI pending approval 和 read model pending request 都先 hydrate；点击“允许并继续”后经 App Server current `agentSession/action/respond` 进入同一 turn，backend 记录 `approvalRequestResumeActionRespond`，并继续发出 `action.resolved -> tool.result -> message.delta -> turn.completed`。registry / ledger 已把该场景从 `partial+guard` 推进到 `covered-electron`。分类：`agentSession/action/respond` 与新增 Electron fixture oracle 属于 `current`；`ExternalBackend` 仅作为 `compat / controlled-fixture`；旧 `agent_runtime_respond_action` 继续是 `dead / retired guard-only`，只允许负向断言证明未回流。
- W3 追加 `approval-request-and-resume` 实跑 evidence 固化：`LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario approval-request-resume --timeout-ms 180000` 通过，session=`claw-chat-current-1783513833702-52703`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`。本轮修复 action request id owner：`action.required / action.resolved` 必须优先保留 `actionId / requestId / id`，外层 turn trace `request_id=claw_request_*` 不得覆盖 approval request id；该回归已覆盖 `src/lib/api/agentRuntime/appServerEventPayloadProjection.ts`、`eventSequenceGate.ts`、`agentProtocolToolParsers.ts` 与 `agentProtocolParserUtils.ts`。
- W3 追加 `request-permissions-turn-scoped` projection guard：新增 `normalizeActionArguments(...)` current helper，把 runtime `policy` / `metadata.permission_facts` 统一投影为 UI 可读 `permission_facts`，同时保留原始 action arguments；raw `action_required` parser、App Server `action.required` projection 与 history `approval_request -> ActionRequired` hydrate 三条入口都覆盖 thread/turn scope + permission facts，避免权限弹层退回前端启发式判断。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是真实 request permission grant、environment keyed isolation 与 Electron approval oracle。
- W3 追加 `network-approval-item` projection guard：`normalizeActionArguments(...)` 继续承接 Codex `networkApprovalContext` / `network_approval_context`，统一投影 `network_approval` facts，保留 host / protocol / port / environment / owner call / proposed amendments；raw `action_required` parser、App Server `action.required` projection 与 history `approval_request -> ActionRequired` hydrate 三条入口都覆盖该结构，禁止 GUI 继续靠命令自然语言说明网络批准。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是真实 Electron allow once / allow for session / deny lifecycle oracle。
- W3 追加 `guardian-review-status` projection guard：`normalizeActionArguments(...)` 继续承接 Codex guardian approval review lifecycle，统一投影 `guardian_review` facts，保留 reviewId / targetItemId / status / risk / decisionSource / rationale / action；raw `action_required` parser、App Server `action.required` projection 与 history `approval_request -> ActionRequired` hydrate 三条入口都覆盖该结构，禁止 GUI 继续把 guardian warning 与 approval request 文案混写。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是独立 guardian lifecycle item、parallel aggregate status 与 Electron denied / timedOut oracle。
- W3 追加 `hook-lifecycle-events` projection guard：Codex `hook.started / hook.completed` 与 Lime current `workflow.hook.started / workflow.hook.completed` 现在统一投影为独立 `hook` thread item，保留 runId、eventName、handlerType、executionMode、scope、sourcePath、status、entries、output、durationMs 与 targetItemId；timeline converter 将其作为过程 toolCall 展示，禁止 hook 输出继续混入 assistant text 或只退化成 transient runtime status。registry / ledger 已把该场景从 `missing` 推进到 `partial+guard`；剩余缺口是 parallel quiet hooks collapse、completed hook output before following assistant message DOM 与 Electron lifecycle oracle。
- W3 owner linkage guard 验证通过：`npx vitest run "src/components/agent/chat/components/timeline-utils/itemConverters.unit.test.ts" "src/components/agent/chat/components/messageListItemProjection.timelineFlow.unit.test.ts" "src/components/agent/chat/hooks/agentStreamThreadItemController.test.ts"`（21 passed）、`npx vitest run "internal/roadmap/test/clawstream/scenario-registry.test.mjs"`（6 passed）、ESLint / Prettier / `git diff --check` 均通过。`npm run smoke:agent-runtime-current-fixture` 首轮在 `media-reference` 启动时遇到 packaged renderer `file://dist/index.html` 偶发 `ERR_FILE_NOT_FOUND`，同一构建产物单场景复跑通过；随后 `LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:agent-runtime-current-fixture` 完整通过，覆盖 Coding Workbench、图片命令、cancel-then-continue、Inputbar rich restore、pending steer、Plan hydrate、Skills Runtime、Multi-Agent、MCP structuredContent、media reference、Expert Skills / Plaza / Panel、Content Factory Article Editor，`liveProviderUsed=false`。
- W3 仍不标 complete：本轮已补 App Server normalizer 注入 owner 负向矩阵、迁掉两条 current smoke 的 `turn.final_done` 依赖、补了代码产物 Electron GUI 侧 `turn.completed` 证据，收掉 `lime-agent` workflow 正向 `FinalDone` emit，删除 Rust 协议层 `FinalDone` 输入面，把 packages/frontend legacy detector 收敛到 shared fail-closed owner，清掉 current fixture evidence 的旧终态 wording，并把 live runner grace 命名从 completion 语义改成 settled 语义。下一刀回到 fixture 结构债和真实 runtime UX：继续拆 backend fixture script rendering，再推进 reasoning/tool/text/artifact 端到端 fixture 的缺口。

### 2026-07-07

- 按“先快速完成骨架，再回头完成细节”收口 `scenario-registry.json`：8 个 execution batch 均补齐 `evidenceGate`、全量 `detailOrder` 和 batch 级 `verificationCommands`，后续 P0/P1/P2 细节必须按 registry 顺序推进。
- 加严 `scenario-registry.test.mjs`：除 registry / ledger 同步与 batch 覆盖外，新增 detail order 全量覆盖、无重复、验证命令非空、禁止 `agent_runtime_` 旧命名回流的结构守卫。
- 继续把“骨架完成”机械化：`scenario-registry.json` 新增 `skeletonDefinition` 与 `governanceClassification`，`scenario-registry.test.mjs` 解析 `scenario-ledger.md` 六列表格，强制每个 scenario 都有 Codex 来源、标准事件项、Projection / GUI oracle、清理目标和同步状态，避免后续只登记场景名却没有验收口径。
- `no-natural-language-lifecycle-regex` 从 `guard-needed` 推进到 `partial+guard`：`streamingProjectionGuard.unit.test.ts` 已作为核心投影 owner 的回流守卫；`running-status-preserved` 追加 Electron current `cancel` fixture 证据并推进到 `covered-electron`；registry 状态统计同步更新为 `covered-electron=10 / partial+guard=6 / partial=7 / missing=36 / guard-needed=0`。2026-07-09 追加扩面后，`no-natural-language-lifecycle-regex` 已进一步推进到 `covered-electron`，P0 剩余清零。
- `reasoning-first-visible` 追加 Electron current `reasoning-first-visible` fixture 证据并推进到 `covered-electron`：`npm run smoke:claw-chat-current-fixture -- --scenario reasoning-first-visible --timeout-ms 240000` 通过，session=`claw-chat-current-1783403517680-48086`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`；中间态 `hasFinalText=false / hasDoneText=false / startupNoteVisible=false`，完成态 `reasoningIndex=38 < finalAnswerIndex=63`，read model `reasoningSequence=1 < finalSequence=9`。registry 状态统计更新为 `covered-electron=11 / partial+guard=5 / partial=7 / missing=36 / guard-needed=0`。
- `terminal-contract-after-answer` 从 `partial` 推进到 `partial+guard`：新增 hook/runtime guard，证明 `turn_failed` 在已有 partial answer 时保留过程卡、partial answer 只出现一次、失败说明只出现一次；同时修正 `turn_failed` toast 使用真实 runtime error presentation，不再误报“模型未输出最终答复”。`stale-terminal-does-not-stop-new-turn` 同步按已有 pure guard / handler oracle 校正到 `partial+guard`。registry 状态统计更新为 `covered-electron=11 / partial+guard=7 / partial=5 / missing=36 / guard-needed=0`。
- `stale-terminal-does-not-stop-new-turn` 追加 Electron current `terminal-stale-guard` 骨架：第一轮和第二轮都经 GUI 输入、App Server current read model 完成，第二轮 backend ledger 记录旧 terminal marker，GUI 断言 stale done marker 不可见且第二轮完成态恢复输入框。验证通过：`npm run smoke:claw-chat-current-fixture -- --scenario terminal-stale-guard --timeout-ms 240000`，session=`claw-chat-current-1783406820348-36412`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`。registry 状态统计更新为 `covered-electron=12 / partial+guard=6 / partial=5 / missing=36 / guard-needed=0`；真实旧 terminal event 注入 owner 投影仍留作细节补强。
- `terminal-contract-after-answer` 追加 Electron current `terminal-failed-after-answer` 骨架：后端先发 `message.delta` partial，再发 `turn.failed`，GUI 证明 partial 与 failure marker 各只出现一次、输入框恢复且 stop 隐藏，read model 证明 `latestTurnStatus=failed` 并保留 prompt / partial / failure，backend ledger 记录 `turn.failed`。验证通过：`npm run smoke:claw-chat-current-fixture -- --scenario terminal-failed-after-answer --timeout-ms 240000`，session=`claw-chat-current-1783408449429-62469`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`。registry 状态统计更新为 `covered-electron=13 / partial+guard=5 / partial=5 / missing=36 / guard-needed=0`。
- `terminal-contract-after-answer` 追加 Electron current `terminal-canceled-after-answer` 骨架：后端先发 `message.delta` partial 并等待 GUI stop，`turnCancel` 走 current `turn.canceled`；GUI 证明 stop 前 partial 与“正在输出”同屏且启动说明不出现，取消后 partial 保留且只出现一次、输入框恢复、stop 隐藏，read model 证明 `latestTurnStatus=canceled` 并保留 prompt / partial，backend ledger 记录 `turn.canceled`。验证通过：`LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario terminal-canceled-after-answer --timeout-ms 240000`，session=`claw-chat-current-1783417713111-42454`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`；未设 `LIME_ELECTRON_FIXTURE_BUILD_READY` 的首次运行在 packaged renderer/assets build 阶段以 `143/SIGTERM` 退出，未进入场景执行。registry 状态统计保持 `covered-electron=13 / partial+guard=5 / partial=5 / missing=36 / guard-needed=0`；`final_done` grace timer 删除留作真实旧 terminal owner 投影细节之后。
- fixture 结构债继续收口：新增 `scripts/agent-runtime/claw-chat-current-fixture-terminal-after-answer.mjs`，把 `terminal-failed-after-answer` 与 `terminal-canceled-after-answer` 的 GUI / read model / backend ledger flow 从中心 `claw-chat-current-fixture-scenario-flow.mjs` 拆出；中心 flow 从 `1671` 行降到 `1530` 行，新增模块 `208` 行。随后新增 `scripts/agent-runtime/claw-chat-current-fixture-terminal-stale-guard.mjs`，把 `terminal-stale-guard` 双回合 GUI / read model / backend ledger flow 拆出；再新增 `scripts/agent-runtime/claw-chat-current-fixture-web-tools-rendering.mjs`，把 reasoning/tool/text 时序 fixture 的 GUI 中间态、失败 probe 和 read model 汇总拆出；最后新增 `scripts/agent-runtime/claw-chat-current-fixture-skills-runtime-flow.mjs`，把 Skills Runtime / Expert Skills Runtime / Expert Plaza / Expert Panel 的长流程拆出。中心 flow 已降到 `810` 行，新模块分别为 `208` / `136` / `172` / `586` 行；结构守卫已把四个模块纳入 source 聚合。下一步继续拆 backend-file / scenario-assertions / smoke test 的长段落，然后再回到真实旧 terminal owner 投影和 `final_done` grace timer 删除。
- 模块拆分后验证通过：`node --check` 覆盖 `claw-chat-current-fixture-scenario-flow.mjs` 与四个新 scenario module；`npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs"`（33 tests passed）；`npx vitest run "internal/roadmap/test/clawstream/scenario-registry.test.mjs"`（6 tests passed）；真实 Electron devserver smoke 通过 `skills-runtime`、`web-tools-rendering`、`terminal-stale-guard`，sessions 分别为 `claw-chat-current-1783420240239-58404`、`claw-chat-current-1783420289456-67638`、`claw-chat-current-1783420326278-71150`。
- assertion 结构债继续收口：新增 `scripts/agent-runtime/claw-chat-current-fixture-terminal-assertions.mjs` 与 `scripts/agent-runtime/claw-chat-current-fixture-web-tools-assertions.mjs`，把 terminal failed/canceled/stale 与 reasoning/tool/text 时序断言从中心 `claw-chat-current-fixture-scenario-assertions.mjs` 拆出；中心断言文件从 `1849` 行降到 `1566` 行。验证通过：`node --check` 覆盖两个新 assertion module 与中心断言文件；`npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs"`（33 tests passed）；`npx vitest run "internal/roadmap/test/clawstream/scenario-registry.test.mjs"`（6 tests passed）。
- assertion 结构债第二轮收口：新增 `scripts/agent-runtime/claw-chat-current-fixture-skills-runtime-assertions.mjs`，把 Skills Runtime / Expert Skills / Expert Plaza / Expert Panel 断言从中心文件拆出；新增 `scripts/agent-runtime/claw-chat-current-fixture-runtime-surface-assertions.mjs`，把 reasoning-first、MCP structured content、media reference 与 Multi-Agent Team runtime surface 断言拆出；`claw-chat-current-fixture-smoke.test.mjs` 的 source guard 已纳入两个新模块。
- 代码体量登记：`claw-chat-current-fixture-scenario-flow.mjs` 已从 `1671` 行降到 `810` 行，退出 `1000` 行硬风险；`claw-chat-current-fixture-scenario-assertions.mjs` 已从 `1849` 行降到 `944` 行，退出 `1000` 行硬风险；`claw-chat-current-fixture-smoke.test.mjs` 已从 `2023` 行降到 `855` 行，退出 `1000` 行硬风险；`scripts/agent-runtime/claw-chat-current-fixture-backend-file.mjs`（`1786` 行）仍超过 `1000` 行。退出条件是在 fixture 细节回补前，继续把 backend fixture script 渲染从 runtime env / provider server owner 中拆出，避免新场景继续向巨型文件追加。
- assertion 第二轮验证通过：`node --check "scripts/agent-runtime/claw-chat-current-fixture-scenario-assertions.mjs"`；`node --check "scripts/agent-runtime/claw-chat-current-fixture-skills-runtime-assertions.mjs"`；`node --check "scripts/agent-runtime/claw-chat-current-fixture-runtime-surface-assertions.mjs"`；`node --check "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs"`；`npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs"`（33 tests passed）；`npx vitest run "internal/roadmap/test/clawstream/scenario-registry.test.mjs"`（6 tests passed）。
- assertion 第二轮真实 Electron current fixture 验证通过：`npm run smoke:claw-chat-current-fixture -- --scenario skills-runtime --timeout-ms 240000`，session=`claw-chat-current-1783421964581-73521`；`npm run smoke:claw-chat-current-fixture -- --scenario mcp-structured-content --timeout-ms 240000`，session=`claw-chat-current-1783421964570-73526`。两条均使用 external fixture backend，不调用正式模型。
- smoke guard 结构债收口：新增 `scripts/agent-runtime/claw-chat-current-fixture-smoke-domain-guards.mjs`，承接 Image Command / Content Factory Article Workspace / Multi-Agent Team 结构守卫；新增 `scripts/agent-runtime/claw-chat-current-fixture-smoke-skills-runtime-guards.mjs`，承接 Skills Runtime / Expert Skills / Expert Plaza / Expert Panel 结构守卫与 evidence summarizer 单测。两个 helper 只由 `claw-chat-current-fixture-smoke.test.mjs` 注册执行，不加入 `readSmokeScript()` source 聚合，避免测试 helper 字符串污染 current fixture 负向断言。
- smoke guard 拆分验证通过：`node --check "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs"`；`node --check "scripts/agent-runtime/claw-chat-current-fixture-smoke-domain-guards.mjs"`；`node --check "scripts/agent-runtime/claw-chat-current-fixture-smoke-skills-runtime-guards.mjs"`；`npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs"`（33 tests passed）。
- backend fixture 结构债收口：新增 `scripts/agent-runtime/claw-chat-current-fixture-backend-script.mjs` 承接 `writeFixtureBackend` 与主 backend script 模板；新增 `scripts/agent-runtime/claw-chat-current-fixture-backend-tool-skill-events.mjs` 承接 Web tools / MCP structuredContent / Skills Runtime / Expert / Multi-Agent tool event fragment；`claw-chat-current-fixture-backend-file.mjs` 从 `1786` 行降到 `545` 行，新增模块为 `761` / `518` 行，三者均低于 `1000` 行硬风险。
- 拆分后修复 MCP structuredContent live GUI 回归：`tool.result.payload.result` 必须是完整 ToolExecutionResult，不能只放 `structuredContent`；否则前端 `appServerEventStream` 会以 nested `result` 为 source，丢失 `success/output/metadata`，GUI 只显示工具已完成而不显示 structured answer / reference id。本轮补齐 nested `result.success/output/metadata/structuredContent`，并在 smoke guard 中要求 MCP nested result 保持完整形状。
- backend 拆分与 MCP 修复验证通过：`node --check` 覆盖 `claw-chat-current-fixture-backend-file.mjs`、`claw-chat-current-fixture-backend-script.mjs`、`claw-chat-current-fixture-backend-tool-skill-events.mjs`、`claw-chat-current-fixture-smoke.test.mjs`；临时生成 backend script 后 `node --check` 通过，手动 turnStart stdout 已确认 MCP `tool.result` 带完整 nested result；`npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs"`（33 tests passed）；`npx vitest run "src/lib/api/agentRuntime/appServerEventStream.test.ts"`（12 tests passed）；`npx vitest run "src/components/agent/chat/hooks/agentStreamThreadItemController.test.ts" "src/components/agent/chat/hooks/agentStreamToolItemMessageSync.unit.test.ts" "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" "src/components/agent/chat/components/ToolCallDisplay.test.tsx"`（55 tests passed）。
- backend 拆分后的真实 Electron current fixture 验证通过：`npm run smoke:claw-chat-current-fixture -- --scenario mcp-structured-content --timeout-ms 240000`，session=`claw-chat-current-1783424714794-33605`；`npm run smoke:claw-chat-current-fixture -- --scenario skills-runtime --timeout-ms 240000`，session=`claw-chat-current-1783425064534-56136`。两条均使用 external fixture backend，不调用正式模型。观察到 packaged renderer 每次强制重建分别约 `4m48s` / `4m44s`，这是当前 fixture 执行慢的主要来源；下一刀应把 build reuse / `LIME_ELECTRON_FIXTURE_BUILD_READY` 口径纳入 current fixture 骨架加速，而不是把耗时误判成模型首字慢。
- Electron fixture build reuse 骨架落地：`scripts/lib/electron-fixture-build.mjs` 不再在单场景 smoke 中无条件重建；现在先检查 `dist/index.html`、Electron main/preload、App Server release manifest 与 packaged app-server binary，若这些产物新于 `src` / `electron` / `public` / `packages/app-server-client/src` / `lime-rs/crates` 及相关构建配置则复用，否则按 `stale-source` fail-closed 重建。聚合入口仍先准备一次构建并向子场景透传 `LIME_ELECTRON_FIXTURE_BUILD_READY=1`。当前脏工作树若有源码新于现有 dist，会继续正确判定 `stale-source` 并默认重建一次；受控复用验证 `LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario mcp-structured-content --timeout-ms 240000` 通过，session=`claw-chat-current-1783426222579-25809`，约 `16s` 完成且未进入 packaged build 阶段。
- W2 rich restore Electron current fixture 收口：Inputbar 取消恢复已覆盖 text、image attachment、path reference 与 installed skill route；fixture 断言绑定本场景 `turnStart.sessionId / turnId`，不再裸比动态 `SESSION_ID` 常量。
- Gate B 验证通过：`APP_SERVER_BIN="/Users/coso/Documents/dev/ai/aiclientproxy/lime/dist-electron/app-server/darwin-arm64/app-server" npm run smoke:claw-chat-current-fixture -- --scenario inputbar-rich-restore --timeout-ms 180000`，session=`claw-chat-current-1783381197658-93694`。
- 结构守卫与卫生检查通过：`npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs"`（27 tests passed）；`git diff --check`。
- W2 pending steer rich snapshot guard 落地：App Server queued read model 从 current `turn_inputs + turn_runtime_options.metadata` 投影 `attachments / pathReferences / textElements / inputCapabilityRoute`；前端 `queuedTurn` normalizer 与 restore policy 保留这些字段，发送准备会把 input restore draft 的 path references、text elements 和 skill route 写入 current turn metadata。
- 定向验证通过：`npx vitest run "src/lib/api/queuedTurn.test.ts" "src/components/agent/chat/hooks/agentStreamInputRestorePolicy.unit.test.ts" "src/components/agent/chat/hooks/agentStreamUserInputSendPreparation.test.ts" "src/components/agent/chat/components/Inputbar/hooks/useInputbarSend.test.tsx"`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server read_session_projects_queued_turn_input_snapshot -- --nocapture`。
- 聚合 current fixture 通过：`npm run smoke:agent-runtime-current-fixture`，覆盖 Inputbar rich restore、Plan hydrate、Skills Runtime、Multi-Agent Team、MCP structuredContent、Expert Skills / Plaza / Panel、Content Factory Article Editor，`liveProviderUsed=false`。
- W2 pending steer rich restore devserver Electron Gate B 收口：`inputbarPendingSteerQueuedRichTextPreserved` 按结构化 `textElementTexts` 验证用户可编辑正文，允许 queued raw `text` 保留 slash command 前缀；验证通过 `node --check "scripts/agent-runtime/claw-chat-current-fixture-scenario-assertions.mjs"`、`npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs"`（28 tests passed）、`npm run smoke:claw-chat-current-fixture -- --app-url "http://127.0.0.1:1420/" --scenario inputbar-pending-steer-rich-restore --prefix claw-chat-current-fixture-inputbar-pending-steer-rich-restore-devserver-text-element-assertion --timeout-ms 180000`，session=`claw-chat-current-1783389967237-36808`。
- W2 pending steer fixture 骨架补齐 packaged 证据：`inputbar-pending-steer-rich-restore` 语义收敛为 queued rich turn restore，而不是 active turn cancel；验证通过 `APP_SERVER_BIN="/Users/coso/Documents/dev/ai/aiclientproxy/lime/dist-electron/app-server/darwin-arm64/app-server" LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario inputbar-pending-steer-rich-restore --prefix claw-chat-current-fixture-inputbar-pending-steer-rich-restore-packaged-current --timeout-ms 300000`，session=`claw-chat-current-1783428460310-22335`。
- W2 pending steer 多队列 Electron 骨架落地：新增 `inputbar-pending-steer-multi-queue`，在 active streaming 期间先 defer rich draft，再 defer 第二条 plain steer，read model 证明 queued rich / second plain turn 的 FIFO `position=0/1`、`promptOrder=["rich","second"]`，且 rich turn 未提前发到 backend。修复 external backend fixture 对该场景的 active turn 长流式保持，并将该场景从通用 completed 新闻正文等待中排除。验证通过 `APP_SERVER_BIN="/Users/coso/Documents/dev/ai/aiclientproxy/lime/dist-electron/app-server/darwin-arm64/app-server" LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario inputbar-pending-steer-multi-queue --prefix claw-chat-current-fixture-inputbar-pending-steer-multi-queue-packaged-current --timeout-ms 240000`，session=`claw-chat-current-1783429458391-18636`。
- W2 pending steer pop-front / hydrate 骨架接入并补齐 packaged 证据：新增 `inputbar-pending-steer-pop-front-resume`，沿 `active streaming -> rich/second defer -> GUI queued panel promote -> App Server current cancel active -> agentSession/thread/resume -> rich backend turnStart -> second queue position=0 -> renderer reload hydrate` 验证真实 pop-front 顺序。公共 `guiNotStuckStreaming` 对该场景改为验证 hydrate 后 active/rich 输出仍可见、second queue 为 `position=0` 且输入框可用；`stopButtonVisible=true` 在这里是预期“正在输出”状态，不再按普通完成态误判 stuck。验证通过 `APP_SERVER_BIN="/Users/coso/Documents/dev/ai/aiclientproxy/lime/dist-electron/app-server/darwin-arm64/app-server" LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario inputbar-pending-steer-pop-front-resume --prefix claw-chat-current-fixture-inputbar-pending-steer-pop-front-resume-packaged-current --timeout-ms 300000`，session=`claw-chat-current-1783433643324-49596`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-inputbar-pending-steer-pop-front-resume-packaged-current-summary.json`。
- W2 pending steer GUI 产品闭环完成：`QueuedTurnsPanel` 的“立即执行”不再靠 fixture 代发 cancel/resume，而是从产品路径串联 `promoteQueuedTurn -> interruptTurn -> resumeThread -> refreshSessionReadModel`；read model active turn 解析不再把 queued turn 当成 interrupt target，queued panel 缺 current handler 时 fail closed。验证通过 `APP_SERVER_BIN="/Users/coso/Documents/dev/ai/aiclientproxy/lime/dist-electron/app-server/darwin-arm64/app-server" npm run smoke:claw-chat-current-fixture -- --scenario inputbar-pending-steer-pop-front-resume --prefix claw-chat-current-fixture-inputbar-pending-steer-pop-front-resume-product-current --timeout-ms 300000`，session=`claw-chat-current-1783437285396-26073`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-inputbar-pending-steer-pop-front-resume-product-current-summary.json`；summary 中 `appServerRequestMethods` 包含 `agentSession/queuedTurn/promote`、`agentSession/turn/cancel`、`agentSession/thread/resume`，rich backend turnStart 保留 image/file reference，second queue reload hydrate 为 `position=0` 且“正在输出”状态保留。
- W2 pending steer fixture 拆分完成：`claw-chat-current-fixture-inputbar-pending-steer.mjs` 从 1050 行降为 348 行 scenario facade，GUI DOM / queued panel 操作移到 `claw-chat-current-fixture-pending-steer-gui-actions.mjs`，queued read-model 投影与 wait 移到 `claw-chat-current-fixture-pending-steer-read-model.mjs`；source guard 已纳入新模块。验证通过 `node --check` 三个 pending-steer 模块、`npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs" "scripts/agent-runtime/current-fixture-regression-smoke.test.mjs" "internal/roadmap/test/clawstream/scenario-registry.test.mjs"`（51 tests passed）、`npm run governance:scripts`、`jq empty "internal/roadmap/test/clawstream/scenario-registry.json"`、`git diff --check`。
- W2 旧 input restore fallback 第一刀删除：`EmptyStateComposerPanel` 不再接收 `inputRestoreRequest`，删掉只恢复 `draft.text` 的子面板 fallback；首页 current owner 固定为父 `EmptyState`，inline 输入框 current owner 固定为 `Inputbar/useInputbarController`。新增 `EmptyState.test.tsx` 回归证明首页中断恢复会完整恢复 text、image、path reference 与 installed skill route，并在发送时带 `inputRestoreDraft`；`EmptyStateComposerPanel.inputFlow.test.tsx` 增加 source guard，禁止子面板重新声明 `inputRestoreRequest` 或 `InterruptedInputRestoreRequest`。验证通过 `npx vitest run "src/components/agent/chat/components/EmptyState.test.tsx"`；`npx vitest run "src/components/agent/chat/components/EmptyStateComposerPanel.inputFlow.test.tsx" "src/components/agent/chat/components/EmptyState.test.tsx" "src/components/agent/chat/components/Inputbar/index.test.tsx" "src/components/agent/chat/components/Inputbar/hooks/useInputbarSend.test.tsx" "src/components/agent/chat/hooks/agentStreamFlowControl.test.ts" "src/components/agent/chat/hooks/agentStreamInputRestorePolicy.unit.test.ts" "src/components/agent/chat/hooks/agentStreamUserInputSendPreparation.test.ts"`（212 tests passed）。
- W2 旧 input restore fallback 第二刀删除：`AgentChatWorkspace` 不再在收到 `inputRestoreRequest` 时执行 text/path-only 预恢复；父级只转发 request，完整恢复继续由 `EmptyState` / `Inputbar/useInputbarController` current owner 执行。新增 `AgentChatWorkspace.inputRestoreGuard.test.ts` source guard，禁止父级重新写入 `setInput(request.draft.text)`、`handleClearPathReferences()`、`handleAddPathReferences`、`replacePendingImages` 或 `setActiveCapability`。
- W4 fixture 时序护栏补强：`cancel` / `cancel-then-continue` stop helper 必须 `requireVisibleOutput`，停止前先等同一 turn 的 assistant 正文与 running status 同屏。验证通过 `npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs"`、`LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario cancel-then-continue --prefix claw-chat-current-fixture-cancel-then-continue-require-visible-output --timeout-ms 300000`、`LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:agent-runtime-current-fixture`，`liveProviderUsed=false`。
- W4 cancel-then-continue current terminal route 修复：`AppServerAgentSessionEventDrainRouter` 注册时保留本地请求 `turnId` 作为候选，首个同 session current 事件到达且未命中 closed route 时采用 App Server 真实 `turnId`，后续仍按真实 `turnId` 严格过滤；terminal 关闭时同时登记真实 `turnId` 与请求 `turnId` closed key，并用原始 registration key 删除 route，避免 `message.delta` 可见但 `turn.completed` 漏投导致 Stop / “正在输出”残留。新增 `threadClient.test.ts` 覆盖本地 pending turnId 与后端真实 turnId 不一致时仍投递 `message.delta -> turn.completed`。验证通过：`npx vitest run "src/lib/api/agentRuntime/threadClient.test.ts" "src/lib/api/agentRuntime/appServerEventStream.test.ts" --silent=passed-only --disableConsoleIntercept`（59 passed）；`npx vitest run "src/components/agent/chat/hooks/agentStreamTurnEventBinding.test.ts" "src/components/agent/chat/hooks/useAgentRuntimeSyncEffects.terminalReadModel.test.tsx" "src/components/agent/chat/hooks/agentStreamTurnEventBinding.tailRecovery.test.ts" --silent=passed-only --disableConsoleIntercept`（23 passed）。Gate B 证据：`LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario cancel-then-continue --prefix claw-chat-current-fixture-cancel-continue-isolated --timeout-ms 240000` 通过，session=`claw-chat-current-1783524983624-43497`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-cancel-continue-isolated-summary.json`，`guiContinueCompleted.stopButtonVisible=false`、`readModelContinueCompleted.latestTurnStatus=completed`、`backendRecordedCancelThenContinue=true`。随后 `LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:agent-runtime-current-fixture` 完整通过，覆盖 history/cache hydration、Coding Workbench、图片命令、普通画图意图、`cancel-then-continue`、Inputbar rich restore、pending steer、Plan hydrate、Skills Runtime、Multi-Agent Team、MCP structuredContent、media reference、Expert Skills / Plaza / Panel、Content Factory Article Editor，`liveProviderUsed=false`。注意：同一时段存在并行 `soul-style` fixture 写入默认 summary，最终以独立 `--prefix` evidence 和聚合 smoke 为准。
- W4 live-tail-commit Electron current fixture 骨架落地：新增 `live-tail-commit` 场景，external controlled backend 先发首字 marker 并保持 running，再延迟发出长输出、overflow marker、24 行 markdown table tail 与 current `turn.completed`；GUI oracle 捕获 completion 前首字 + “正在输出/停止”状态且不展示 startup note，完成态验证 overflow commit、scroll anchor、table tail reflow、输入框恢复、read model completed 和 backend ledger `liveTailCommitCompleted`。registry / ledger 已从 `partial+guard` 推进到 `covered-electron`；剩余 screenshot baseline 与 viewport resize oracle 并入 `electron-resize-reflow` 下一刀。
- W4 electron-resize-reflow Electron current fixture 骨架落地：新增 `electron-resize-reflow` 场景，沿 live-tail 长输出完成态继续打开 files right surface，并在真实 Electron viewport 下采集 `wide / compact / restored` 三组 DOM + screenshot oracle；断言同一 current turn 的 tail marker range、scroll anchor、Inputbar bottom row、right surface owner/root 与 message/input/right surface no-overlap 稳定。registry / ledger 已从 `partial+guard` 推进到 `covered-electron`；剩余视觉细节并入 markdown/diff/status screenshot baseline 统一口径。
- W4 electron-resize-reflow 实跑 evidence 收口：修复两类 Gate B 阻塞后，`npm run smoke:claw-chat-current-fixture -- --scenario electron-resize-reflow --prefix claw-chat-current-fixture-electron-resize-reflow --timeout-ms 300000` 通过，session=`claw-chat-current-1783547918219-81473`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-electron-resize-reflow-summary.json`。修复点：`useMessageListScrollController` 在仍处于底部跟随时用 `ResizeObserver` 对 reflow 后的滚动容器重新贴底；fixture 先经 App Server current `workspaceRightSurface/request` 创建 files right surface request 再点击 GUI files toggle；`electron-resize-reflow` 专用 flow 完成后不再落回默认新闻 completion wait；Electron fixture packaged build 增加 `.lime/electron-fixture-build.lock` 串行化重建，smoke renderer build 设置 `LIME_VITE_EMPTY_OUT_DIR=0`，避免并行 smoke 清空共享 `dist/index.html` 导致 `ERR_FILE_NOT_FOUND`。
- W4 Electron fixture build freshness 拆分完成：`scripts/lib/electron-fixture-build.mjs` 已把 packaged fixture freshness 拆成 `renderer / host / appServer` 三段，Rust-only app-server source 更新只会触发 `electron:build:app-server-assets`，不再重跑 `build:renderer:electron:smoke` 或 `typecheck:electron`。验证通过：`npx vitest run "scripts/lib/electron-fixture-build.test.mjs"` 覆盖 Rust-only stale 只 rebuild `appServer`；真实 Gate B `npm run smoke:claw-chat-current-fixture -- --scenario electron-resize-reflow --prefix claw-chat-current-fixture-electron-resize-reflow-split-freshness --timeout-ms 300000` 在 `segments=appServer` 下约 19s 通过，session=`claw-chat-current-1783548569280-22625`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-electron-resize-reflow-split-freshness-summary.json`，日志未进入 renderer build。该改动只改变 test-only fixture build gate，不改变 production Electron/App Server 主链。
- W4 Plan hydrate owner 级 Electron oracle 收紧：`plan` fixture 不再用整页 `pageText` 或 `mainText` 证明计划可见，`waitForGuiPlanCompleted` 与 `verifyPlanHistoryHydrate` 只认明确 DOM owner（`agent-plan-block` / `task-center-run-control-plan` / `task-center-task-rail-plan`）中的步骤，且要求 decision drawer 绑定 `plan-composer-revision-status` revision。断言层 `proposedPlanVisible` 也改为消费结构化 `planOwnerHasAllSteps`，防止退回 legacy `update_plan` 或普通正文包含判断。验证通过：`node --check "scripts/agent-runtime/claw-chat-current-fixture-gui-completion-waits.mjs"`、`node --check "scripts/agent-runtime/claw-chat-current-fixture-plan-history.mjs"`、`node --check "scripts/agent-runtime/claw-chat-current-fixture-scenario-assertions.mjs"`、`npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`（37 passed）、`LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario plan --prefix claw-chat-current-fixture-plan-owner-oracle --timeout-ms 240000`，session=`claw-chat-current-1783549495263-21294`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-plan-owner-oracle-summary.json`；summary 中 `planOwnerKindsWithAllSteps=["message-plan-block"]`、`planDecisionRevisionId="proposed_plan:fixture-1"`、read model `source="proposed_plan"` 且 `legacyUpdatePlanToolItemCount=0`。
- W4 Plan implementation decision 负向 guard 收口：`planImplementationDecision` 不再把无 revision 的历史 plan item 或 legacy `update_plan:*` revision/source 当成本地实施确认候选；current thread plan item 必须带 revision，legacy `update_plan` 只能停在隐藏 guard，不再弹出 Plan composer decision。验证通过：`npx vitest run "src/components/agent/chat/workspace/planImplementationDecision.unit.test.ts" --silent=passed-only --disableConsoleIntercept`（11 passed）；`npx vitest run "src/components/agent/chat/components/generalWorkbenchTaskRailViewModel.unit.test.ts" "src/components/agent/chat/utils/planState.unit.test.ts" "src/components/agent/chat/components/messageListTimelineContentParts.unit.test.ts" --silent=passed-only --disableConsoleIntercept`（39 passed）。
- W4 Plan 无 proposed_plan Electron 负向 owner fixture 收口：普通 `complete` 场景的 GUI completion snapshot 现在采集明确 Plan owner / decision absence，并断言 `planOwnerCount=0`、`planDecisionVisible=false`、`legacyUpdatePlanVisibleHits=[]`。验证通过：`node --check "scripts/agent-runtime/claw-chat-current-fixture-gui-completion-waits.mjs"`、`node --check "scripts/agent-runtime/claw-chat-current-fixture-scenario-assertions.mjs"`、`npx vitest run "scripts/agent-runtime/claw-chat-current-fixture-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`（37 passed）、`LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario complete --prefix claw-chat-current-fixture-plan-negative-owner --timeout-ms 240000`，session=`claw-chat-current-1783550815248-4677`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-plan-negative-owner-summary.json`，`guiNoPlanUiWithoutProposedPlan=true`。`plan-parser-boundary` 从 `partial+guard` 升级为 `covered-electron`。
- S4 `thread-read-page-isomorphic` package-side guard 落地：新增 `threadReadPageIsomorphic` projection owner，覆盖 read/list/resume 同 thread、turn/item order、live content part hydrate、pagination duplicate/reorder 与 legacy paginated history fail closed；registry / ledger 从 `partial` 推进到 `partial+guard`。验证通过：`npm --prefix "packages/agent-runtime-projection" run build && node --test "packages/agent-runtime-projection/tests/threadReadPageIsomorphic.test.mjs"`。
- S4 `history-replay-visual` package-side guard 落地：新增 `historyReplayVisual` projection owner，按 Codex TUI `history_replay.rs` 规范历史恢复视觉同构；replayed item 的 itemId/turnId/type/status 必须与 hydrate/read model/visual row 同源，user textElements、本地/远程图片 refs、reasoning summary dedupe、MCP in-progress active 状态、live/hydrate renderer owner 与 pageText-only oracle 都纳入 fail-closed guard。registry / ledger 已把最后一个裸 `partial` 推进到 `partial+guard`；剩余缺口是 App Server read model 与 Electron history hydrate DOM/screenshot oracle。
- S4 `history-replay-visual` Electron current oracle 收口：`thread_read.tool_calls` fallback 继承同源 `thread_items` provenance，inline tool process 暴露稳定 `tool-call-row`，带 thread item provenance 的 thinking part 不再重复走外置 thinking 通道；Electron session history fixture 已覆盖 App Server current `agentSession/read/list`、history hydrate DOM/screenshot、user textElements、本地/远程图片附件、reasoning 单次渲染、MCP running owner/tool row、active turn 和 forbidden `agentSession/turn/start`。registry / ledger 从 `partial+guard` 推进到 `covered-electron`。验证通过：`npx vitest run "src/components/agent/chat/hooks/agentChatHistoryThreadItems.test.ts" "src/components/agent/chat/hooks/agentChatHistoryReadModel.test.ts" "src/components/agent/chat/hooks/agentChatHistory.timeline.test.ts" "src/components/agent/chat/components/StreamingRenderer.processGroups.test.tsx" "src/components/agent/chat/components/messageListTimelineContentParts.imported.unit.test.ts" "src/components/agent/chat/components/MessageList.importedHistory.codexImport.test.tsx" "src/components/agent/chat/components/MessageList.reasoningFlow.test.tsx" "src/components/agent/chat/components/messageListItemProjection.timeline.unit.test.ts" --silent=passed-only --disableConsoleIntercept`（50 passed）；`npm run smoke:agent-session-history-electron-fixture -- --prefix agent-session-history-history-replay-visual --timeout-ms 240000`，summary=`.lime/qc/gui-evidence/agent-session-history-electron-fixture/agent-session-history-history-replay-visual-summary.json`。
- S4 `thread-read-page-isomorphic` App Server + Electron current oracle 收口：`load_session_current` 在 runtime memory session 存在但 history read detail 为空时回退 projection session，`ensure_current_session_hydrated` 直接插入 stored session，避免 resume hydrate 后重新反建空 session；`ProviderTraceEvent` DTO 修正只服务 current `agent-protocol`，没有让 `agent-compat` 反向依赖 current owner。验证通过：`CARGO_TARGET_DIR="$(pwd)/.lime/cargo-target/clawstream-app-server-test" RUSTFLAGS="-L native=.../sherpa-onnx-prebuilt/.../lib" cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server resume_hydrated_projection_history_read_preserves_messages_and_items --lib -- --nocapture`（1 passed）、`npm run smoke:agent-session-history-electron-fixture -- --prefix agent-session-history-thread-read-page-isomorphic --timeout-ms 240000`，summary=`.lime/qc/gui-evidence/agent-session-history-electron-fixture/agent-session-history-thread-read-page-isomorphic-summary.json`，覆盖 `agentSession/read/list/thread/resume`、三页 cursor、3 个 turn group、DOM text order stable、startup note hidden 与 forbidden `agentSession/turn/start`。registry / ledger 从 `partial+guard` 推进到 `covered-electron`。

## 7. 下一刀

P0 已清零，快速骨架已清零，`history-replay-visual` 与 `thread-read-page-isomorphic` 已收口到 `covered-electron`。下一刀回到 P1/S4 其它真实 App Server / Electron 细节，不穿插大规模清理：

1. P1/S2 接续已有 Electron 场景的 item/projection oracle：优先 MCP truncation / resource / elicitation、Skills search-read-gate、Multi-Agent lineage。
2. `thread-read-page-isomorphic` 只回头补 archived read / forked_from / failed idle flag 细节，不再作为 App Server / Electron 主链阻塞项。
3. Visual baseline 细节回到 markdown/diff/status screenshot 统一口径，不再把 `history-replay-visual` 或 `thread-read-page-isomorphic` 当阻塞项。
