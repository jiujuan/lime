# Aster Phase 6 Provider Reply Backend Plan

> 状态：active  
> 更新时间：2026-07-10  
> 主路线图：`internal/roadmap/astermigration/phase6-continuation-tracker.md`  
> 当前阶段：R2/R3 provider / reply loop + R4 native dispatch 去 Aster
> 写集边界：`lime-rs/crates/model-provider/src/provider_stream.rs`、`lime-rs/crates/model-provider/src/provider_stream/**/*.rs`、`lime-rs/crates/tool-runtime/src/turn_tool_surface.rs`、`lime-rs/crates/tool-runtime/src/tool_call_surface.rs`、`lime-rs/crates/tool-runtime/src/tool_result_projection.rs`、`lime-rs/crates/tool-runtime/src/native_dispatch_execution.rs`、`lime-rs/crates/tool-runtime/src/shell_execution.rs`、`lime-rs/crates/tool-runtime/src/shell_execution/tests.rs`、`lime-rs/crates/tool-runtime/src/file_read_execution.rs`、`lime-rs/crates/tool-runtime/src/file_search_execution.rs`、`lime-rs/crates/tool-runtime/src/request_user_input.rs`、`lime-rs/crates/tool-runtime/src/request_user_input/tests.rs`、`lime-rs/crates/tool-runtime/src/lib.rs`、`lime-rs/crates/tool-runtime/Cargo.toml`、`lime-rs/crates/agent-runtime/src/reply_stream.rs`、`lime-rs/crates/agent-runtime/src/ask.rs`、`lime-rs/crates/agent-compat/src/agents/reply_parts.rs`、`lime-rs/crates/agent-compat/src/agents/agent.rs`、`lime-rs/crates/agent-compat/src/tools/ask.rs`、`lime-rs/crates/agent-compat/src/tools/mod.rs`、`lime-rs/crates/agent-compat/src/tools/file/mod.rs`、`lime-rs/crates/agent-compat/src/tools/search/mod.rs`、`lime-rs/crates/agent/src/request_tool_policy/**`、`src/lib/governance/asterMigrationBoundary.test.ts`、`internal/roadmap/astermigration/phase6-continuation-tracker.md`、`internal/roadmap/astermigration/refactor-v1-impact-audit.md`、`internal/roadmap/astermigration/README.md`

## 目标

把 R2/R3 最后一跳固定为明确的 `provider_reply_exit_source` 私有退场模块，并继续为后续把 provider trait object execution 迁到 `model-provider` current backend 做准备。同时补上 Codex-style response event materializer，让非文本 delta、item lifecycle、completed 与 rate limits 先进入 Lime current projection 链。`agent-compat` 是要迁移走的 staging / compat blocker，不是 current owner；本计划不得再把 compat 命名或 `agent-compat` 方向当成完成态。

## Current / Compat / Dead

R4 native dispatch 补充分类：

- `current`：`tool-runtime::native_dispatch` 是标准 native tools 的执行事实源；`tool-runtime::native_dispatch_execution::execute_runtime_native_dispatch_tool(...)` 承接 dispatch-backed 工具接管、current permission preflight、cancel 检查、执行和结果投影；`tool-runtime::native_overlay::runtime_native_tool_overlay_for_dispatch_name(...)` 用 current dispatch canonical lookup 反查 dispatch-backed overlay；`tool-runtime::shell_execution` 承接 Bash / PowerShell 前台执行；`tool-runtime::file_read_execution` 承接 Read 文件读取；`tool-runtime::file_search_execution` 承接 Glob/Grep 文件搜索；`tool-runtime::request_user_input` 是 `request_user_input` tool surface DTO/schema/parse/validation/normalization owner；`agent-runtime::ask` 是 Turn-side request_user_input runner；reply loop 只在迁移期把当前 session/turn context 适配给这些 current executor 与 request_user_input action callback。
- `compat blocker`：Aster `ToolRegistry` / `Tool` / `ToolContext` 仍是 reply loop native fallback，只允许服务 background / workspace sandbox shell、Ask / Skill、gateway-backed tools、MCP / extension bridge 与尚未迁出的 tool trait 壳；标准 dispatch-backed tools、foreground shell、Read、Glob、Grep 不得再优先走 `registry.execute(...)`。
- `dead / guarded`：标准 native tools、foreground shell、Read、Glob、Grep 重新优先走 Aster `registry.execute(...)`、在 `agent-compat` 恢复本地 native tool-name overlay 表 / file read owner / search owner、公开 re-export `BashTool` / `PowerShellTool` / `ReadTool` / `GlobTool` / `GrepTool` / `AskTool` 具体实现类型，或把 current permission / approval preflight 写成 Aster session cache / hook / pending map。

- `current`：`agent-runtime` provider source request / run call / execution payload contract，`model-provider::provider_stream::source_execution` provider source backend trait / execution wrapper，`model-provider` provider response event/item DTO、provider response content contract、provider response context header extraction、provider sampling request/mode、tool-input delta event policy、lead/worker model-change policy、provider stream notification envelope / text classification、progress / milestone state、provider failure logging / telemetry category classification、provider trace failure projection / trace DTO alias、plaintext tool-use parser / stream progress、image input policy 与 empty-first-content retry policy，`tool-runtime::turn_tool_surface` 的 turn tool surface / scope policy、tool exposure / resource gate / registration gate，`tool-runtime::tool_call_surface` 的工具调用名称和参数 normalization，`tool-runtime::tool_result_projection` 的 native tool result -> MCP `CallToolResult` 投影和 `tool_surface_updated` 判定，`agent-runtime::reply_stream::RuntimeReplyResponseMaterializer`。
- `transitional current adapter`：`request_tool_policy/agent_reply_stream.rs::runtime_agent_events_from_response_event(...)`，暂时把 response projection 转进现有 `RuntimeAgentEvent` / timeline item 主链；`request_tool_policy/response_event_adapter.rs` 持有 `RuntimeAgentEvent` -> `RuntimeReplyResponseEvent` 过渡映射；`request_tool_policy/aster_reply_stream_adapter.rs::AsterReplyStreamProjector` 只作为 Aster source adapter，把 Aster message/direct tool-input delta 前移成 current `RuntimeReplyResponseEvent`。
- `compat blocker`：`request_tool_policy/provider_reply_exit_source.rs::ProviderReplyExitSource`，只允许作为删除前唯一私有 Aster provider reply exit source implementation；其退出条件是 provider trait object / provider stream execution 迁到 `model-provider` current backend 后删除。
- `dead / guarded`：`compat_provider_reply_backend.rs`、`CompatProviderReplyBackend`、`CompatReplySource*`、`run_compat_provider_reply_backend(...)`、`Aster*ReplySource`，恢复 `agent-compat::Agent::reply(...)` wrapper，把 provider source execution wrapper 放回 `agent-runtime` / `agent-compat` / `provider_stream.rs` 主文件，把 `agent-compat` 当作 current owner 或让 `agent-compat` 新增 current owner 依赖的任何改动，response event 只消费 text delta、吞掉 tool input / reasoning / item / completed / rate limits 的旧形状，在 `agent-compat/src/agents/agent.rs` 恢复 provider response text / notification / tool-input delta 直接规则，恢复 `agent-compat` 本地 `ProviderTraceStage` / `ProviderTraceEvent` DTO，恢复 `event_converter` provider trace stage 映射 / 字段复制，或恢复 native tool result projection helper。

## 本轮任务

R4 native dispatch 补充任务：

- 标准 native tools 必须在 live-execution hook 之后先走 `tool-runtime` dispatch-backed overlay lookup、current permission preflight 和 current dispatcher；Aster registry 只作为未迁工具 fallback。
- Bash / PowerShell 前台执行必须在 live-execution hook 后先走 `tool-runtime::shell_execution`，由 current owner 处理参数、permission/preflight、embedded execution、output decode 和 result projection；background、workspace sandbox 和非 full-access warning 暂时 fallback，不在 `agent-compat` 内新增 approval cache。
- Read / Glob / Grep 前台执行必须在 live-execution hook 和 shell/collab current executor 之后、native dispatch 与 Aster registry fallback 之前先走 `tool-runtime::{file_read_execution,file_search_execution}`；`agent-compat` 只能传递 Aster `ToolContext` 的 cwd / cancel token，不得恢复文件读取 / 搜索执行 owner。
- `request_user_input` 的 tool surface contract 必须归属 `tool-runtime::request_user_input`；`agent-runtime::ask` 只能做 Turn-side runner，Aster `AskTool` 只能做 callback / timeout / Tool trait adapter。
- `agent-compat/src/agents/agent.rs` 中新增/保留的 helper 只能做当前 session/turn context 适配、错误投影和 dispatch 调用编排；不得在 `agent-compat` 内复制工具名表、permission policy 或 approval cache。
- 治理测试必须断言 `runtime_native_tool_overlay_for_dispatch_name(...)` 存在、reply loop 消费 `runtime_native_dispatch_handle`，且 `execute_runtime_native_dispatch_tool(...)` 的调用顺序早于 fallback `registry.execute(...)`。
- 治理测试必须断言 Aster AskTool 不恢复本地 DTO/schema/parse/validation/normalization owner。

1. 源码只保留 `provider_reply_exit_source.rs`，模块入口为 `mod provider_reply_exit_source;`。
2. 退场模块内部命名固定为 `ReplyExitSource`、`ReplyExitSourceExecutor`、`ProviderReplyExitSource`、私有 `run_provider_reply_exit_source(...)`。
3. `aster_reply_backend_adapter.rs` 只能引用 `super::provider_reply_exit_source::ReplyExitSource`，不得恢复 provider source backend impl 或 Aster provider trait import。
4. 治理测试必须同时断言 `provider_reply_exit_source.rs` 存在、`compat_provider_reply_backend.rs` 不存在、旧 `Compat*` / `Aster*ReplySource` 类型和函数名不回流。
5. `lime-rs/crates/agent-compat/**` 只允许发生迁出、删除或减少依赖的改动；不得新增 `agent-compat` 到 current owner 的依赖，也不得把 `agent-compat` 当作 owner 继续补 reply loop / provider / tool / session 逻辑。`agent-compat` 现存 `document-preview` / `model-provider` / `tool-runtime` 本地依赖只是 burn-down allowlist，退出条件是迁出对应调用并删除。
6. `RuntimeReplyResponseMaterializer` 必须覆盖 `OutputItemAdded`、`OutputItemDone`、`ToolCallInputDelta`、`ReasoningDelta`、`Completed`、`RateLimits` 的 current projection，并由 `agent_reply_stream.rs` 消费到现有 runtime event / timeline item 主链。
7. `AsterReplyStreamProjector` 必须把 Aster Message text/thinking/tool-input delta、direct `AgentEvent::ToolInputDelta` 和可表达的 item lifecycle 转为 `RuntimeReplyResponseEvent`，不得继续把这些非文本/工具参数流或 provider response item 直接输出为 legacy-shaped runtime delta。
8. 本轮不宣称删除 `Agent::reply_with_provider(...)`；该最后一跳仍是 R2/R3 compat blocker。

## 验证门槛

```bash
cargo fmt --manifest-path "lime-rs/Cargo.toml" --package tool-runtime --package aster-core -- --check
CARGO_TARGET_DIR=".lime/cargo-target/native-dispatch-reply-loop-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime native_overlay --lib -j 1 -- --nocapture
CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime shell_execution --lib -j 2 -- --nocapture
CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 CARGO_TARGET_DIR=".lime/cargo-target/file-search-execution" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime file_read_execution --lib -j 2 -- --nocapture
CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 CARGO_TARGET_DIR=".lime/cargo-target/file-search-execution" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime file_search_execution --lib -j 2 -- --nocapture
CARGO_TARGET_DIR=".lime/cargo-target/request-user-input-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime request_user_input --lib -j 1 -- --nocapture
CARGO_TARGET_DIR=".lime/cargo-target/request-user-input-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p agent-runtime ask --lib -j 1 -- --nocapture
CARGO_TARGET_DIR=".lime/cargo-target/request-user-input-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent ask_bridge --lib -j 1 -- --nocapture
cargo fmt --manifest-path "lime-rs/Cargo.toml" --package agent-runtime --package lime-agent -- --check
npx prettier --check "src/lib/governance/asterMigrationBoundary.test.ts" "internal/roadmap/astermigration/phase6-continuation-tracker.md" "internal/roadmap/astermigration/refactor-v1-impact-audit.md" "internal/exec-plans/aster-phase6-provider-reply-backend-plan.md"
npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000
CARGO_TARGET_DIR=".lime/cargo-target/response-event-nontext-agent-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p agent-runtime reply_stream --lib -j 1 -- --nocapture
CARGO_TARGET_DIR=".lime/cargo-target/response-event-nontext-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture
LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:agent-runtime-current-fixture
LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario approval-request-full-access --timeout-ms 240000 --cdp-port 9232 --prefix claw-chat-current-fixture-approval-full-access-shell-r4-after-input-helper --evidence-dir ".lime/qc/gui-evidence/claw-chat-current-fixture"
git diff --check -- "lime-rs/crates/agent-runtime/src/reply_stream.rs" "lime-rs/crates/agent/src/request_tool_policy/agent_reply_stream.rs" "src/lib/governance/asterMigrationBoundary.test.ts" "internal/roadmap/astermigration/phase6-continuation-tracker.md" "internal/roadmap/astermigration/refactor-v1-impact-audit.md" "internal/exec-plans/aster-phase6-provider-reply-backend-plan.md"
```

## 进度日志

- 2026-07-10：继续 R4 collab Aster `Tool` trait 壳删除。`tool-runtime::collab_agent` 新增 `collab_agent_canonical_tool_name(...)`、`collab_agent_tool_definition(...)` 与 `collab_agent_tool_definitions(...)`，承接 Agent / SendMessage / TeamCreate / TeamDelete / ListPeers 的 definitions 与 legacy alias canonical lookup；`agent-compat/src/agents/agent.rs` 在 current collab canonical/list 注入时保留 `allowed_tool_names` 约束；`agent-compat/src/tools/agent_control.rs` / `team_tools.rs` 删除 `SpawnAgentTool` / `SendInputTool` / `TeamCreateTool` / `TeamDeleteTool` / `ListPeersTool` Aster `Tool` trait 壳和 `register_agent_control_tools(...)`，只保留 callback、session/team resolver 与 storage adapter。分类：`current` = `tool-runtime::collab_agent` definitions/canonical/execution + reply-loop current collab bridge；`compat blocker` = callback adapter、R5/R6 session/team storage adapter；`dead / guarded` = 协作工具 Aster `Tool` trait 壳和 registry registration。验证：`rustfmt --edition 2021 --check` 本轮 Rust 写集通过；`cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime collab_agent --lib -j 1 -- --nocapture`，`20 passed`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent agent_tools --lib -j 1 -- --nocapture`，`102 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，`172 passed`。R4 仍未完成，root `aster` dependency 仍不能删除。

- 2026-07-10：继续 R4 Read / Glob / Grep 前台执行壳迁出。新增 `tool-runtime::file_read_execution::{RuntimeFileReadRequest, execute_runtime_file_read_tool(...)}` 与 `tool-runtime::file_search_execution::{RuntimeFileSearchRequest, execute_runtime_file_search_tool(...)}`，分别承接 Read 路径解析、文本/文档/SVG/Notebook 读取、图片/PDF retired fail-closed，以及 Glob mtime 排序 / exclude / truncation、Grep content/files/count 模式、regex parse、hidden/binary 策略和 result metadata projection；`agent-compat/src/agents/agent.rs` 只把 Aster `ToolContext` 的 working directory / cancellation token 适配给 current owner，reply loop 顺序固定为 collab -> shell -> file read -> file search -> native dispatch -> Aster registry fallback。分类：`current` = `tool-runtime::{file_read_execution,file_search_execution}`；`compat blocker` = Aster `Tool` trait 壳 / ToolRegistry fallback、Ask/Skill/gateway-backed/MCP、background / sandbox shell；`dead / guarded` = 在 `agent-compat` 恢复本地 Read / Glob / Grep execution owner 或让这些工具重新优先走 registry。验证通过：`rustfmt --edition 2021 --config skip_children=true --check` 本轮 Rust 写集；`CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 CARGO_TARGET_DIR=".lime/cargo-target/file-search-execution" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime file_read_execution --lib -j 2 -- --nocapture`，`3 passed`；同 target `cargo test ... -p tool-runtime file_search_execution --lib`，`3 passed`；同 target `cargo test ... -p lime-agent request_tool_policy --lib`，`81 passed`。R4 仍未完成，R2/R3 provider reply exit source、R5/R6 session/runtime source 与 root `aster` dependency 仍不能删除。

- 2026-07-10：继续 R4 collab reply-loop current executor 接管。`agent-compat/src/agents/agent.rs` 在 live hook 后、shell/native dispatch 前先调用 `execute_runtime_collab_tool(...)`；`SendMessage` 经 `execute_agent_control_runtime_tool(...)`，`TeamCreate` / `TeamDelete` / `ListPeers` 经 `execute_team_runtime_tool(...)`，Aster `registry.execute(...)` 只保留后续 fallback。分类：`current` = `tool-runtime::collab_agent::execution` + reply-loop 调用顺序；`compat blocker` = Aster `Tool` trait 壳 / ToolRegistry fallback、session/team storage adapter；`dead / guarded` = 协作工具优先回落 Aster registry 或在 `agent-compat` 恢复执行 owner。验证：`rustfmt --edition 2021 --check` 本轮 Rust 写集通过；`cargo check --manifest-path "lime-rs/Cargo.toml" -p aster-core --lib -j 1` 通过；`cargo check --manifest-path "lime-rs/Cargo.toml" -p tool-runtime --lib -j 1` 通过；`cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime collab_agent --lib -j 1 -- --nocapture`，`20 passed`；`npx prettier --check "src/lib/governance/asterMigrationBoundary.test.ts"` 通过；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，`172 passed`；scoped `git diff --check` 通过。R4 仍未完成，root `aster` dependency 仍不能删除。

- 2026-07-10：继续 R4 collab agent execution owner 体量收口。`tool-runtime/src/collab_agent/execution.rs` 的 inline execution tests 拆到 `collab_agent/execution_tests.rs`，生产 execution owner 保持约 `624` 行，避免 current owner 在继续承接协作工具执行时重新进入 `800` 行预警区；模块接线固定为 `#[cfg(test)] #[path = "execution_tests.rs"] mod tests;`。分类：`current` = `tool-runtime::collab_agent::execution` 生产 owner + 独立测试文件；`compat blocker` = Aster `Tool` trait 壳 / ToolRegistry fallback、session/team adapter；`dead / guarded` = 为了测试便利把协作执行规则放回 `agent-compat` 或重新膨胀中心文件。验证：`rustfmt --edition 2021 --check "lime-rs/crates/tool-runtime/src/collab_agent/execution.rs" "lime-rs/crates/tool-runtime/src/collab_agent/execution_tests.rs"` 通过；`cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime collab_agent --lib -j 1 -- --nocapture`，`19 passed`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx prettier --check ...` 通过；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，`172 passed`；scoped `git diff --check` 通过。

- 2026-07-10：继续 R4 team mutation executor 前移。`tool-runtime::collab_agent::execution` 新增 `CollabAgentTeamExecutionBackend`、runtime-neutral team DTO 与 `execute_collab_team_create(...)` / `execute_collab_team_delete(...)` / `execute_collab_list_peers(...)`，承接 TeamCreate / TeamDelete / ListPeers 的参数解析、team name 冲突处理、lead 校验、active member 拒绝删除、cleanup 调度、peer output 与 metadata projection；`agent-compat/src/tools/team_tools.rs` 只保留 Aster `Tool` trait adapter、session/team storage adapter 和 reachable member / local peer 查询 adapter。分类：`current` = `tool-runtime::collab_agent::execution` team executor；`dead / guarded` = Aster team tool 本地 mutation / output owner；`compat blocker` = Aster `Tool` trait 壳、ToolRegistry fallback 与 session/team storage DTO adapter。验证：`rustfmt --edition 2021 --check` 本轮 Rust 写集通过；`CARGO_TARGET_DIR=".lime/cargo-target/collab-agent-team-execution" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime collab_agent --lib -j 1 -- --nocapture`，`19 passed`；同 target `cargo check --manifest-path "lime-rs/Cargo.toml" -p aster-core --lib -j 1` 通过。

- 2026-07-10：继续 R4 collab agent execution owner 前移。新增 `tool-runtime::collab_agent::execution`，承接 Agent spawn 与 SendMessage 的参数 parse、target normalization、team-lead validation、message 构造、callback dispatch、routing/result projection 和 metadata 包装；`agent-compat/src/tools/agent_control.rs` 只实现 `CollabAgentExecutionBackend` 的 session/team resolver 与 Aster callback adapter，再把 current `RuntimeCollabToolOutput` 包装为 Aster `ToolResult`。分类：`current` = `tool-runtime::collab_agent::{execution,projection,validation}`；`dead / guarded` = Aster agent-control 本地 SendMessage 投递循环 / projection owner；`compat blocker` = Aster `Tool` trait 壳、ToolRegistry fallback、team/session resolver 和 callback bridge。验证：`rustfmt --edition 2021 --check` 本轮 Rust 写集通过；`CARGO_TARGET_DIR=".lime/cargo-target/collab-agent-execution" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime collab_agent --lib -j 1 -- --nocapture`，`17 passed`；同 target `cargo check --manifest-path "lime-rs/Cargo.toml" -p aster-core --lib -j 1` 通过。

- 2026-07-10：继续 R4 collab agent DTO 代理出口收缩。`agent-compat/src/tools/agent_control.rs` 不再 `pub use tool_runtime::collab_agent::{SpawnAgentRequest, SpawnAgentResponse, SendInputRequest, SendInputResponse}`；`agent-compat/src/tools/mod.rs` 不再把 `SpawnAgentRequest` / `SpawnAgentResponse` 作为内部 tools facade re-export，`agent.rs` 直接从 `tool-runtime::collab_agent` 消费 current DTO。分类：`current` = `tool-runtime::collab_agent` DTO；`dead / guarded` = Aster tools facade / agent_control 对 current DTO 的二次出口；`compat blocker` = `AgentControlToolConfig`、Aster `Tool` trait adapter 与 callback bridge 仍待迁出。验证：`rustfmt --edition 2021 --check` 本刀 Rust 写集通过；`npx prettier --check ...` 通过；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`172 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/collab-agent-validation" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime collab_agent --lib -j 1 -- --nocapture`，`15 passed`；`CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=1 cargo check --manifest-path "lime-rs/Cargo.toml" -p aster-core --lib -j 1` 通过；scoped `git diff --check` 通过。

- 2026-07-10：继续 R4 SendMessage 纯校验规则前移。新增 `tool-runtime::collab_agent/validation.rs`，承接 peer address target normalization、string message summary requirement、structured cross-session/broadcast rejection、shutdown response target/reason 校验和 plan approval team-lead 校验；`agent-compat/src/tools/agent_control.rs` 只负责解析参数、读取 team/session 状态后调用 current helper。分类：`current` = `tool-runtime::collab_agent` validation；`dead / guarded` = `agent-compat` 本地 SendMessage validation 分支；`compat blocker` = SendMessage Aster `Tool` trait adapter / callback bridge 和 session/team routing。

- 2026-07-10：继续 R4 collab agent peer address surface 收口。删除只做 re-export 的 `agent-compat/src/tools/peer_address_surface.rs`，`agent_control.rs` 直接消费 `tool-runtime::collab_agent::{parse_peer_address, ParsedPeerAddress, PeerAddressScheme, is_cross_session_local_peer_address}`；`tools/mod.rs` 不再声明 `mod peer_address_surface;`。分类：`current` = `tool-runtime::collab_agent` peer address contract；`dead / guarded` = `agent-compat` peer address re-export helper；`compat blocker` = SendMessage Aster `Tool` trait adapter / callback bridge 仍待迁出。验证：`rustfmt --edition 2021 --check`、`rg peer_address_surface` 无源码命中、`git diff --check`、`asterMigrationBoundary.test.ts` `172 passed`、`cargo check --manifest-path "lime-rs/Cargo.toml" -p aster-core --lib -j 1` 通过。

- 2026-07-10：删除零外部引用的 Aster session plan-mode extension 文件 `agent-compat/src/session/plan.rs`，并从 `session/mod.rs` 移除 `mod plan;`。`SessionPlanModeState` 不再作为 public/root surface 或内部 dead code 存活；Codex-style plan/update_plan current owner 保持在 `tool-runtime::update_plan`。同轮清理 `agent_control.rs` / `peer_address_surface.rs` 中无消费者 collab-agent re-export，配合 `tool-runtime::collab_agent` surface 前移，避免 R4 adapter 编译噪声。分类：`dead / guarded` = Aster session plan-mode extension；`compat blocker` = R4 agent-control `Tool` trait adapter 与其余 Aster session/runtime store DTO。

- 2026-07-10：继续 R4 collab agent tool surface 收口。`SpawnAgentTool` / `SendInputTool` 与 `register_agent_control_tools(...)` 降为 crate-private，`tools/mod.rs` 不再公开 re-export agent-control 注册函数或 callback DTO；治理守卫要求 Agent / SendMessage / Team / ListPeers DTO/schema/工具名/描述只归 `tool-runtime::collab_agent`，`agent-compat` 只能保留 Aster `Tool` trait adapter。分类：`current` = `tool-runtime::collab_agent`；`compat blocker` = Aster registry fallback；`dead / guarded` = 本地 DTO/schema 和公开具体工具实现 surface。

- 2026-07-10：继续 R4 collab agent 结果投影前移。新增 `tool-runtime::collab_agent/projection.rs`，承接 SendMessage 输出/metadata、unsupported bridge peer 返回、TeamCreate / TeamDelete / ListPeers metadata 规则；`agent-compat/src/tools/agent_control.rs` 与 `team_tools.rs` 只保留 session/team 状态接线、回调调用和 Aster `ToolResult` 包装，不再本地构造 `BroadcastOutput` / `MessageOutput` / `RequestOutput` / `ResponseOutput`。分类：`current` = `tool-runtime::collab_agent` projection；`compat blocker` = Aster `Tool` trait adapter / registry fallback；`dead / guarded` = `agent-compat` 本地协作工具结果 projection。验证通过：`rustfmt --edition 2021 --check`、`npx prettier --check`、`rg` 旧 projection 片段无命中、`git diff --check`、`asterMigrationBoundary.test.ts` `172 passed`、`tool-runtime collab_agent` `7 passed`、`lime-agent request_tool_policy` `81 passed`；此前磁盘不足阻塞已通过删除本轮临时 target 并复用既有 Cargo target 解除。

- 2026-07-10：继续 R4 Agent spawn request / result projection 前移。`tool-runtime::collab_agent::spawn_agent_request_from_input(...)` 承接 `AgentInput` -> `SpawnAgentRequest` 的参数归一化、allowed/disallowed tools 去重和默认字段填充，`project_spawn_agent_result(...)` 承接 `Agent launched` 输出与 `agent` metadata；`agent-compat/src/tools/agent_control.rs` 的 `SpawnAgentTool` 只保留 Aster `Tool` trait adapter、callback 调用和 `ToolResult` 包装。为守住文件体量，`collab_agent.rs` inline tests 拆到 `collab_agent/tests.rs`，主 owner 文件约 `731` 行。分类：`current` = `tool-runtime::collab_agent` Agent spawn request/projection；`compat blocker` = Aster `SpawnAgentTool` callback bridge / registry fallback；`dead / guarded` = `agent-compat` 本地 `SpawnAgentRequest { ... }` 构造和 agent metadata 拼装。验证通过：`rustfmt --edition 2021 --check`、`tool-runtime collab_agent` `15 passed`、`lime-agent request_tool_policy` `81 passed`、`asterMigrationBoundary.test.ts` `172 passed`、`git diff --check`。

- 2026-07-10：继续 R4 team tool 具体实现公开面收缩。`agent-compat/src/tools/mod.rs` 不再公开 re-export `TeamCreateTool` / `TeamDeleteTool` / `ListPeersTool`，`agent-compat/src/tools/team_tools.rs` 的三个具体工具类型和构造器降为 `pub(crate)`；它们只作为未迁出的 Aster reply-loop team registry fallback。`SpawnAgentRequest` / `SpawnAgentResponse` 与 `AgentControlToolConfig` 仍因 `agent.rs` callback bridge 暂留，不在本刀误删。分类：`dead / guarded` = Aster team 具体工具实现 public surface 与外部 `aster::Team*Tool` / `aster::ListPeersTool` 引用；`compat blocker` = crate-private Aster team `Tool` trait fallback；`current` = `tool-runtime` tool exposure / registration gate、后续 current multi-agent / team executor。R4 仍未完成，下一步仍要迁出 Team/Agent/SendMessage 与 Bash/PowerShell/Read/Grep/Glob/Skill/gateway-backed/MCP 的执行壳。

- 2026-07-10：继续 R4 tool exposure 纯规则迁出。`tool-runtime::turn_tool_surface` 新增 `RuntimeToolSurfaceGates`、`runtime_tool_surface_gates_from_env_map(...)`、`runtime_tool_surface_should_register_name(...)`、`runtime_registered_tool_exposure_allows_tool_name(...)` 与 `runtime_turn_tool_exposure_allows_tool_name(...)`，承接 MCP resource gate、PowerShell registration gate、subagent native/coordination/team tool allowlist 和 extension-prefixed tool exposure policy；`agent-compat/src/agents/agent.rs` 只把 Aster `SessionType` / resource support / team state / tool 名称常量适配给 current helper，`agent-compat/src/tools/mod.rs` 只注册 Aster `Tool` trait 壳。验证带出的零引用 `SessionPlanModeState` session root re-export 已删除，避免已退场 Aster plan-mode surface 继续作为 public session extension 暴露。分类：`current` = `tool-runtime` tool exposure / registration gate policy；`compat blocker` = Aster `ToolRegistry` / `Tool` / `SessionType` 仍服务 reply loop fallback；`dead / guarded` = `agent-compat` 本地 resource gate、subagent allowlist、PowerShell gate owner 和 `SessionPlanModeState` public re-export。验证通过：`rustfmt --edition 2021 --check` 本轮 Rust 写集；`CARGO_TARGET_DIR=".lime/cargo-target/turn-tool-exposure-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime turn_tool_surface --lib -j 1 -- --nocapture`，`8 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，`172 passed`；`npx prettier --check ...` 与 `git diff --check` 通过。R2/R3 `ProviderReplyExitSource` / `reply_with_provider(...)` 最后一跳仍未删除，不能宣称 Phase 6 完成。

- 2026-07-10：继续削薄 provider input modality policy 交界。`model-provider::provider_stream::image_input` 新增 `provider_stream_input_modality_policy_from_metadata(...)`、`provider_stream_input_modality_policy_allows_image_input(...)` 与 `provider_stream_metadata_allows_image_input(...)`，承接 provider/media lowering 侧的 input modality metadata 解析和 image input allowed 判定；`agent-compat/src/agents/prompt_input_modalities.rs` 删除本地 `input_modality_policy_*` parser，只保留 Aster `MessageContent::Image` 与 RMCP image content 到文本占位的 adapter。分类：`current` = `model-provider` provider input modality policy；`compat blocker` = Aster `Message` / RMCP `Content` 仍是未迁 provider prompt source adapter；`dead / guarded` = `agent-compat` 本地 input modality parser。验证通过：`rustfmt --edition 2021 --check` 本轮 Rust 写集；`CARGO_TARGET_DIR=".lime/cargo-target/input-modality-model-provider" cargo test --manifest-path "lime-rs/Cargo.toml" -p model-provider provider_stream --lib -j 1 -- --nocapture`，`49 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，`172 passed`。R2/R3 `ProviderReplyExitSource` / `reply_with_provider(...)` 最后一跳仍未删除，不能宣称 Phase 6 完成。

- 2026-07-10：继续 R4 request_user_input / Ask tool surface 迁出。新增 `tool-runtime::request_user_input` 承接 `request_user_input` 工具名、DTO、输入 schema、parse、current surface validation、requested schema / elicitation schema、response extraction 与 result normalization；`agent-runtime::ask` 删除本地 DTO/schema/helper，只保留 Turn-side runner；`agent-compat/src/tools/ask.rs` 削薄为 Aster `Tool` trait / callback / timeout adapter。为守住文件体量边界，`request_user_input` 测试拆到 `request_user_input/tests.rs`，生产 owner 文件低于 1000 行。分类：`current` = `tool-runtime::request_user_input` + `agent-runtime::ask` runner；`compat blocker` = Aster `AskTool` / `AskCallback` / `ActionRequiredManager` callback bridge；`dead / guarded` = `agent-compat/src/tools/ask.rs` 本地 DTO/schema/parse/validation/normalization owner。验证通过：`CARGO_TARGET_DIR=".lime/cargo-target/request-user-input-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime request_user_input --lib -j 1 -- --nocapture`，`4 passed`；同 target `cargo test ... -p agent-runtime ask --lib`，`4 passed`；同 target `cargo test ... -p lime-agent ask_bridge --lib`，`3 passed`。R4 仍未完成，Bash/Read/Grep/Glob/Skill/gateway-backed/MCP 和 Aster `ToolRegistry` fallback 仍待迁出。

- 2026-07-10：继续 R4 native dispatch 执行迁出。`tool-runtime::native_overlay::runtime_native_tool_overlay_for_dispatch_name(...)` 通过 current `native_dispatch` canonical lookup 反查 dispatch-backed overlay；reply loop native branch 在 live-execution hook 之后先执行 current permission preflight，再调用 `runtime_native_dispatch_handle().execute(...)`，只有 Bash/PowerShell/Read/Glob/Grep/Ask/Skill、gateway-backed tools 与 MCP / extension bridge 继续作为 Aster registry fallback。分类：`current` = `tool-runtime::native_dispatch` / native overlay lookup / current permission preflight；`compat blocker` = Aster `ToolRegistry` fallback；`dead / guarded` = 标准 native tools 重新优先走 `registry.execute(...)` 或在 `agent-compat` 恢复本地工具名表。验证通过：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package tool-runtime --package aster-core -- --check`；`CARGO_TARGET_DIR=".lime/cargo-target/native-dispatch-reply-loop-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime native_overlay --lib -j 1 -- --nocapture`，`13 passed`；`CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=".lime/cargo-target/provider-image-input-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`。备注：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package tool-runtime --package aster-core --package lime-agent -- --check` 仍会因既有脏文件 `ask_bridge.rs`、`native_tools/gateway_bridge.rs`、`aster_reply_adapter.rs` import 排序失败，本轮未扩大写集。

- 2026-07-10：继续 R4 工具实现公开面收缩。`agent-compat/src/tools/mod.rs` 不再公开 re-export `BashTool` / `PowerShellTool` / `ReadTool` / `GlobTool` / `GrepTool` / `AskTool` / `DEFAULT_ASK_TIMEOUT_SECS`，`tools/file/mod.rs` 与 `tools/search/mod.rs` 的实现 re-export 也改为 `pub(crate)`；`register_all_tools(...)` / `register_default_tools(...)` 收成 crate-private，只服务未迁出的 Aster reply-loop registry fallback。分类：`dead / guarded` = Aster 具体工具实现 public surface；`compat blocker` = crate-private Aster `Tool` trait fallback；`current` = `tool-runtime::native_dispatch`、`tool-runtime::request_user_input` 与后续 current gateway executor。验证通过：`rustfmt --edition 2021 --check` 本轮 Rust 写集；`npx prettier --check` 覆盖治理测试与 Aster 迁移文档；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，`172 passed`；`CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=".lime/cargo-target/aster-tools-impl-surface-private" CARGO_BUILD_JOBS=2 cargo check --manifest-path "lime-rs/Cargo.toml" -p aster-core --lib -j 2` 通过，仅有既有 `SessionPlanModeState` unused import warning；同 target `cargo check ... -p lime-agent --lib -j 2` 通过，仅有既有 `NativeRegistration::name` unused warning。R4 仍未完成，下一步仍要迁出 Bash/PowerShell/Read/Grep/Glob/Skill/gateway-backed/MCP 的执行壳。

- 2026-07-09：创建计划。并行写集曾把旧 `provider_reply_exit_source.rs` / `ReplyExitSource` 命名回流；本计划固定本轮只收口 R2/R3 provider reply backend 命名和守卫，不在 `agent-compat` staging crate 内补 provider / reply loop 业务逻辑，但后续仍必须继续迁出、删除和减少依赖。
- 2026-07-09：按 `agent-compat` 要迁移走的口径纠偏。撤回 `compat_provider_reply_backend.rs` / `Compat*` 命名方向，固定 `provider_reply_exit_source.rs` 是唯一私有退场点；下一刀继续把 `run_provider_reply_exit_source(...)` 内的 Aster provider trait object execution 迁到 current backend。
- 2026-07-10：Codex 对照确认 response event 由 Turn loop 消费后再 materialize item/tool/reasoning/completed；本轮把 Lime response event materializer 接到现有 runtime event / timeline projection，覆盖非文本 delta、item lifecycle、completed 和 rate limits。`ProviderReplyExitSource` / `run_provider_reply_exit_source(...)` 仍是 compat blocker，`agent-compat` 不得作为 current owner。
- 2026-07-10：验证通过：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package agent-runtime --package lime-agent -- --check`；`npx prettier --check ...`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`144 passed`；`agent-runtime reply_stream`，`20 passed`；`lime-agent request_tool_policy`，`77 passed`。`lime-agent` 首次冷编译被 SIGTERM 结束，退出码 `143`，无源码错误；复用同一 target dir 重跑通过。
- 2026-07-10：巨型文件风险登记：`src/lib/governance/asterMigrationBoundary.test.ts` 已是既有超大治理守卫文件。本轮只在 response-event 守卫附近追加断言，未做拆分，原因是当前主线目标是 R2/R3 provider/reply loop 收口，拆分整个治理套件会扩大写集并干扰并行迁移。退出条件：下一次治理测试结构化整理时，把 response event / provider reply exit source 守卫抽成独立 helper 或分域测试文件，保留当前 `144 passed` 守卫覆盖。
- 2026-07-10：修正 `agent-compat` 执行口径：它不是冻结目录，允许迁出、删除或减少依赖；禁止的是新增本地 path dependency 或 current owner 依赖给 staging crate 续命。`asterMigrationBoundary.test.ts` 已增加 `agent-compat/Cargo.toml` 本地 path dependency burn-down allowlist 守卫。
- 2026-07-10：再次纠偏：`agent-compat` 是迁移对象，不是保护区。本计划所有“暂缓改 staging”类表述都必须解释为“不得在 staging 内补新业务逻辑”，不能解释为停止迁出或停止删除。
- 2026-07-10：继续 R2/R3 response event 前移。`AsterReplyStreamProjector` 的转换入口从 Message-only 扩展到所有 Aster projector runtime events，direct `AgentEvent::ToolInputDelta` 也会进入 `RuntimeReplyResponseEvent::ToolCallInputDelta`，避免工具参数流绕过 current materializer。`asterMigrationBoundary.test.ts` 增加守卫并允许已删除的 `agent-compat/src/hooks/{loader,types}.rs` 作为更收口的 dead 状态。验证：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package lime-agent -- --check` 通过；`CARGO_TARGET_DIR=".lime/cargo-target/aster-projector-response-event-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p agent-runtime reply_stream --lib -j 1 -- --nocapture` 通过，`22 passed`；`npx prettier --check "src/lib/governance/asterMigrationBoundary.test.ts"` 通过；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...` 通过，`146 passed`；`git diff --check -- ...` 通过。阻塞：`lime-agent request_tool_policy` Rust 定向测试在编译 `aster-core` 时因当前 `agent-compat` 脏树缺失 `subagent_execution_tool`、`subagent_scheduler`、`communication`、`monitor`、`specialized`、`error_handling`、`recipe/build_recipe` 与 `scheduler/types.rs` 失败，未触达本次 adapter 用例；按本计划不在 `agent-compat` 补逻辑。
- 2026-07-10：继续前移 provider response item lifecycle。`AsterReplyStreamProjector` 现在把 `RuntimeAgentEvent::ItemStarted` / `ItemCompleted` 中可表达为 provider response item 的 `AgentMessage` / `Reasoning` / `ToolCall` payload 映射为 `OutputItemAdded` / `OutputItemDone`；`Warning` 等不属于 provider response item 的 payload 保持原 runtime event，避免信息丢失。`agent-compat` 未修改。
- 2026-07-10：继续补齐 Turn-side response item materialization。`RuntimeReplyResponseMaterializer` 现在会在 `ToolCallInputDelta` 带有工具名，或此前 `OutputItemAdded` 已记录同一 `call_id` 工具名时，同步投影 `ItemUpdated` 工具项，并把累积参数解析为 JSON，无法解析时保留原始字符串；未知工具名保持只发 `ToolInputDelta`，避免伪造 item。该改动让工具参数流进入 current Item/read model 过渡投影，但 provider source 仍未直接产出 Lime-owned response event，`ProviderReplyExitSource` / Aster `Agent::reply_with_provider(...)` 仍是 compat blocker。
- 2026-07-10：解除 `lime-agent request_tool_policy` 定向验证阻塞。只在 `agent-compat` staging crate 内做最小编译清障：删除/注释已不存在模块声明后的残留引用，局部化 `OverflowHandler`，把 recipe / prompt / sandbox / image / user-message / repetition inspector 等已退场 helper 对齐为 no-op 或解析型 shim；没有恢复 `subagent_execution_tool`、`subagent_scheduler`、`communication`、`monitor`、`specialized`、`error_handling` 等旧目录，也没有新增 `agent-compat` -> current owner 依赖。分类：`compat blocker` 编译清障，不是 current owner 或完成证据。验证通过：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package agent-runtime --package lime-agent --package aster-core -- --check`；`CARGO_TARGET_DIR=".lime/cargo-target/response-event-mapper-agent-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p agent-runtime reply_stream --lib -j 1 -- --nocapture`，`26 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/response-event-nontext-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`147 passed`。
- 2026-07-10：继续 R2/R3 provider execution owner 纠偏。`model-provider::provider_stream` 现在持有 `RuntimeReplyProviderExecutionRunner<R>`、`RuntimeReplyProviderExecutionSource<R>` 与 `run_provider_source_execution(...)`，provider source backend wrapper 不再归 `agent-runtime`；`agent-runtime::reply_backend::RuntimeReplyProviderSourceExecution` 只保留 Turn execution payload 和 `from_source_request(...)` / `from_run_call(...)` materialization。`ProviderReplyExitSource` 仍是 Aster 最后一跳 compat blocker，但只能消费 model-provider wrapper，不能把执行 wrapper 塞回 `agent-runtime` 或 `agent-compat`。
- 2026-07-10：收紧 provider execution handoff。`agent-runtime::reply_backend::{run_default_provider_source_backend, run_provider_source_backend}` 现在先把 raw source request materialize 为 `RuntimeReplyProviderSourceExecution` 再交给 provider backend；`ProviderReplyExitRunner` 实现 `RuntimeReplyProviderExecutionRunner<RuntimeReplyProviderSourceExecution>`，不再接收 `RuntimeReplyProviderSourceBackendRequest` 或本地实现 `RuntimeReplyProviderSourceBackend`。验证通过：`CARGO_TARGET_DIR=".lime/cargo-target/provider-wrapper-model-provider" cargo test --manifest-path "lime-rs/Cargo.toml" -p model-provider provider_stream --lib -j 1 -- --nocapture`，`24 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-wrapper-agent-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p agent-runtime reply_backend --lib -j 1 -- --nocapture`，`20 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-wrapper-lime-agent-fresh" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`149 passed`；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider --package agent-runtime --package lime-agent -- --check`、`npx prettier --check ...` 和 `git diff --check -- ...` 均通过。
- 2026-07-10：按 `agent-compat` 继续迁出口径清理回流残留。`agent-compat/src/prompts` 只是已删 Aster prompt template surface 的空目录残留，已删除；分类为 `dead / deleted / forbidden-to-restore`，不是 staging crate 可保留面。验证：`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...` 通过，`150 passed`。
- 2026-07-10：继续对齐 Codex provider stream owner。`RuntimeReplyResponseEvent`、`RuntimeReplyResponseItem` 与 `RuntimeReplyResponseItemPayload` 从 `agent-runtime::reply_stream` 前移到 `model-provider::provider_stream`；`agent-runtime::reply_stream` 只 re-export DTO 并保留 `RuntimeReplyResponseMaterializer` / projection。分类：`current` = model-provider provider response event/item contract；`compat blocker` = Aster `AgentEvent` projector 仍作为迁移期输入；`dead / guarded` = 在 `agent-runtime` 重新定义 provider response DTO。验证通过：`model-provider provider_stream`，`25 passed`；`agent-runtime reply_stream`，`26 passed`；`lime-agent request_tool_policy`，`81 passed`；`asterMigrationBoundary.test.ts`，`151 passed`。
- 2026-07-10：继续把 provider sampling policy 从 Aster reply loop 外迁。`model-provider::provider_stream` 新增 `RuntimeReplyProviderSamplingRequest`、`RuntimeReplyProviderSamplingMode`、`PROVIDER_EMPTY_STREAM_RETRY_MARKER` 与 `provider_stream_should_retry_empty_first_content(...)`；`agent-compat/src/agents/reply_parts.rs` 只消费 current helper 以缩短 R2/R3 compat blocker，不再本地硬编码 Anthropic empty-stream retry marker，也不成为 owner。`asterMigrationBoundary.test.ts` 增加守卫，要求 sampling contract 留在 `model-provider`，禁止 `agent-compat` 恢复硬编码 marker。验证通过：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider --package aster-core --package lime-agent -- --check`；`model-provider provider_stream`，`28 passed`；`agent-runtime reply_stream`，`26 passed`；`lime-agent request_tool_policy`，`81 passed`；`asterMigrationBoundary.test.ts`，`152 passed`。
- 2026-07-10：体量风险登记：`lime-rs/crates/model-provider/src/provider_stream.rs` 当前约 `931` 行，已超过 `800` 行拆分预警但未超过强制拆分线。本轮不扩成模块拆分，原因是主线目标是 R2/R3 provider/reply loop owner 前移；退出条件是下一次继续向 `provider_stream.rs` 加 provider policy / response DTO 前，优先拆出 `provider_stream/response.rs` 或 `provider_stream/sampling.rs` 这类子模块，保持 provider owner 不膨胀成新的中心文件。
- 2026-07-10：继续删除 Aster loop 本地 provider stream poll policy。`agent-compat/src/agents/agent.rs` 不再定义 `PROVIDER_STREAM_CANCEL_*` 常量和 cancel reason 字符串，改为消费 `model-provider::provider_stream::{provider_stream_cancel_poll_interval, provider_stream_timeout_poll, provider_stream_event_poll, ProviderStreamPoll}`；`provider_stream_timeout_poll` 改为泛型返回 `ProviderStreamPoll<T>`，方便真实 stream item 分支统一。分类：`current` = model-provider provider stream poll/cancel policy；`compat blocker` = Aster `reply_internal(...)` 仍执行 provider trait object stream；`dead / guarded` = staging crate 本地 cancel policy 回流。验证通过：`model-provider provider_stream`，`28 passed`；`lime-agent request_tool_policy`，`81 passed`。
- 2026-07-10：继续删除 Aster loop 本地 first-text delta 规则。`model-provider::provider_stream::text_delta` 新增 `provider_stream_first_text_delta_chars(...)`，承接 provider first non-empty text delta 的 trim/count policy；`agent-compat/src/agents/agent.rs` 与 `reply_parts.rs` 只从 Aster `MessageContent::Text` 投影 `&str` 后调用 current helper，不再复制 trim/count 规则。分类：`current` = model-provider provider text delta policy；`compat blocker` = Aster `Message` 仍是 provider stream item source；`dead / guarded` = staging crate 重新维护 first-text delta trim/count policy。验证通过：`model-provider provider_stream`，`29 passed`；`lime-agent request_tool_policy`，`81 passed`。
- 2026-07-10：继续去 Aster 命名。新增 `request_tool_policy/response_event_adapter.rs`，把 `RuntimeAgentEvent` -> `RuntimeReplyResponseEvent` 过渡映射、provider response item payload projection 和 unsupported item fail-closed 测试从 `aster_reply_stream_adapter.rs` 移出；`aster_reply_stream_adapter.rs` 只保留 Aster `AgentEvent` / `Message` source 读取、provider side-channel、inline provider error 和 response hints。分类：`transitional current adapter` = `response_event_adapter.rs`；`compat blocker` = `AsterReplyStreamProjector` 仍是 Aster source adapter；`dead / guarded` = 在 Aster 命名文件里重新持有 response mapper / response item projection。验证：`CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=".lime/cargo-target/response-event-adapter-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`153 passed`。
- 2026-07-10：继续把 provider tool-input delta 纯规则迁出 `agent-compat`。新增 `model-provider::provider_stream::tool_input_delta::{RuntimeReplyProviderToolInputDelta, provider_stream_tool_input_delta_events(...)}`，承接“整条 provider message 必须全是 tool-input delta”、空 `call_id` / 空 `delta` 跳过、以及 current `RuntimeReplyResponseEvent::ToolCallInputDelta` 构造规则；`agent-compat/src/agents/agent.rs` 只把 Aster `MessageContent::ToolInputDelta` 投影成中立输入，并把 current event 映射回临时 `AgentEvent`。分类：`current` = model-provider provider tool-input delta event policy；`compat blocker` = Aster `MessageContent` 仍是 source；`dead / guarded` = staging loop 本地维护 tool-input delta 过滤 / projection 规则。验证通过：`model-provider provider_stream`，`32 passed`；`lime-agent request_tool_policy`，`81 passed`。
- 2026-07-10：继续把 lead/worker provider model-change 分类迁出 `agent-compat`。新增 `model-provider::provider_stream::model_change::{RuntimeReplyProviderModelChange, RuntimeReplyProviderModelChangeMode, provider_stream_model_change(...)}`，承接 active model 与 lead/worker model 的匹配以及 `lead` / `worker` / `unknown` wire mode；`agent-compat/src/agents/agent.rs` 只从 Aster lead-worker provider 取 `usage.model` / `get_model_info()` 并调用 current helper。分类：`current` = model-provider provider model-change metadata policy；`compat blocker` = Aster lead-worker provider trait 仍是 source；`dead / guarded` = staging loop 本地维护 lead/worker/unknown 字符串分类。验证通过：`model-provider provider_stream`，`33 passed`；`lime-agent request_tool_policy`，`81 passed`。
- 2026-07-10：继续去 Aster 命名并迁出 provider stream notification envelope。新增 `model-provider::provider_stream::notification::{provider_stream_notification_text, provider_stream_notification_payload_from_text, PROVIDER_STREAM_EVENT_NOTIFICATION_PREFIX}`，把 provider side-channel notification 的 prefix 与 JSON envelope 组包/解析固定到 current provider owner；旧内部 prefix 从 `__aster_provider_stream_event__:` 改为 `__provider_stream_event__:`，不做兼容。`agent-compat/src/providers/formats/openai_responses.rs` 只负责把 current notification text 包成 Aster `SystemNotification`，`aster_reply_stream_adapter.rs` 仍只是 Aster Message source adapter。分类：`current` = model-provider provider stream notification envelope；`compat blocker` = Aster Message/SystemNotification 仍是临时 source container；`dead / guarded` = Aster 格式文件恢复 notification prefix / event kind / JSON envelope 规则。验证通过：`model-provider provider_stream`，`32 passed`；`aster-core responses_streaming_safety_buffering`，`1 passed`；`lime-agent request_tool_policy`，`81 passed`。
- 2026-07-10：继续削薄 Aster loop provider stream milestone 状态。新增 `model-provider::provider_stream::progress::RuntimeReplyProviderStreamProgress`，承接 first event、first content、first text delta 与 empty-first-content retry state；`agent-compat/src/agents/agent.rs` 和 `reply_parts.rs` 只消费 current progress helper，不再维护 `provider_first_event_seen` / `provider_first_text_delta_seen` / `first_provider_content_seen` 本地布尔状态。分类：`current` = model-provider provider stream progress / milestone policy；`compat blocker` = Aster provider trait object stream 仍是 source；`dead / guarded` = staging loop 本地恢复 milestone booleans 或把 helper 消费写成完成态。验证通过：`model-provider provider_stream`，`34 passed`；`lime-agent request_tool_policy`，`81 passed`，冷编耗时 `14m57s`；`asterMigrationBoundary.test.ts`，`153 passed`；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider --package aster-core -- --check`；`npx prettier --check "src/lib/governance/asterMigrationBoundary.test.ts"`。
- 2026-07-10：继续把 provider failure logging classification 从 Aster loop 迁出。新增 `model-provider::provider_stream::failure::{RuntimeReplyProviderFailure, RuntimeReplyProviderFailureKind, provider_stream_failure_should_log_as_error, provider_stream_failure_message_should_log_as_warning}`，承接 provider 失败是否按 error/warn 记录和非重试 provider rejection 静默规则；`agent-compat/src/agents/agent.rs` 只把 Aster `ProviderError` 投影成 kind/retry/rejection 三个中立事实，不再本地 `matches!(ProviderError::ServerError | ExecutionError | UsageError)` 或调用 `ProviderError::message_is_non_retryable_provider_rejection(...)` 决策日志等级。分类：`current` = model-provider provider failure logging classification；`compat blocker` = Aster `ProviderError` 仍是 reply loop source error；`dead / guarded` = staging loop 本地恢复 provider failure logging policy。验证通过：`model-provider provider_stream`，`36 passed`；`lime-agent request_tool_policy`，`81 passed`；`asterMigrationBoundary.test.ts`，`153 passed`；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider --package aster-core --package lime-agent -- --check`。磁盘阻塞处理：fresh target 编译曾因 `No space left on device` 失败，已删除本轮临时 `.lime/cargo-target/provider-failure-policy-lime-agent` 后复用既有 target 通过。
- 2026-07-10：继续把 provider plaintext tool-use parser 从 Aster reply loop 迁出。新增 `model-provider::provider_stream::plaintext_tool_use::{RuntimeReplyProviderPlaintextToolCall, RuntimeReplyProviderPlaintextToolUse, RuntimeReplyProviderPlaintextToolUseProgress, provider_stream_plaintext_tool_uses, provider_stream_plaintext_tool_use_progress}`，承接 `<tool_use>` XML block、JSON fence、`WebSearch` / `Search` inline alias、split-stream tool input delta progress 与 provider source marker；`agent-compat/src/agents/reply_parts.rs` 只把 Aster `MessageContent::Text` 投影成字符串并把 current DTO 装回临时 `CallToolRequestParam` / `MessageContent::ToolInputDelta`，不再本地持有 marker、XML attribute、JSON fence 或 alias parser。分类：`current` = model-provider provider plaintext tool-use normalization；`compat blocker` = Aster `Message` / `CallToolRequestParam` 仍是 reply loop source adapter；`dead / guarded` = `agent-compat` 本地恢复 plaintext tool-use parser。验证通过：`model-provider provider_stream`，`39 passed`；`lime-agent request_tool_policy`，`81 passed`，冷编耗时 `24m01s`；`asterMigrationBoundary.test.ts`，`154 passed`；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider --package aster-core --package lime-agent -- --check`；`npx prettier --check "src/lib/governance/asterMigrationBoundary.test.ts"`；`git diff --check -- ...`。
- 2026-07-10：继续把 provider image input policy 从 Aster reply parts 迁出。新增 `model-provider::provider_stream::image_input::{RuntimeReplyProviderImageInputPolicy, provider_stream_model_supports_image_input, provider_stream_image_input_policy_disables_provider_images, provider_stream_should_omit_image_input}`，承接 canonical model image modality lookup、turn runtime image policy 解析和 provider image omission 决策；`agent-compat/src/agents/reply_parts.rs` 不再直接 import `model_provider::canonical::maybe_get_canonical_model` 或本地解析 `image_input_policy`，只保留当前 turn metadata 读取和 Aster `Message` / `ToolResponse` 图片内容 stripping adapter。分类：`current` = model-provider provider image input policy；`compat blocker` = Aster `MessageContent::Image` / rmcp image content 仍是 provider/reply loop source adapter；`dead / guarded` = `agent-compat` 本地恢复 canonical image capability lookup 或 runtime image policy 判定。验证通过：`model-provider provider_stream`，`42 passed`；`lime-agent request_tool_policy`，`81 passed`，冷编耗时 `19m43s`；`asterMigrationBoundary.test.ts`，`157 passed`；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider --package aster-core --package lime-agent -- --check`、`npx prettier --check ...` 和 `git diff --check -- ...` 均通过。
- 2026-07-10：体量风险登记更新：`model-provider/src/provider_stream.rs` 当前约 `975` 行，已接近 `1000` 行强制拆分线；本轮只新增 `mod image_input` / re-export，实际 image input policy 落在 `provider_stream/image_input.rs`（约 `74` 行），未继续把逻辑堆进中心文件。`agent-compat/src/agents/reply_parts.rs` 约 `3482` 行，本轮删除本地 canonical image capability lookup 和 policy parser，继续减少 staging 业务逻辑；退出条件是 R2/R3/R4/R5/R6 迁完后删除或拆散该 staging 文件。`src/lib/governance/asterMigrationBoundary.test.ts` 仍是既有超大治理守卫文件，本轮只在 provider stream owner 守卫附近追加断言；退出条件仍是下一次治理测试结构化整理时，把 provider stream owner / Aster boundary guard 拆成分域测试文件。
- 2026-07-10：Fast A2 并行清理补充。`agent-compat/src/agents/prompt_manager.rs` 的旧 prompt snapshot 测试、test-only `PromptManager::with_timestamp(...)` 和 `agent-compat` 的 `insta` direct dependency 已删除；这是 test-only / evidence surface 清理，不改变 R2/R3 provider reply blocker。守卫扩展到 `insta =`、`assert_snapshot!` 和 `PromptManager::with_timestamp`，后续 prompt / unicode 回归必须迁到 current owner tests。
- 2026-07-10：收口 provider stream notification text classification 验证，并继续削薄 `agent-compat` reply surface。`model-provider::provider_stream::notification` 已持有 current prefix、payload lookup 和 text classification helper，`agent-compat/src/agents/agent.rs` / `providers/formats/openai_responses.rs` 只消费 current helper；旧 `__aster_provider_stream_event__:` prefix 不兼容。`Agent::reply(...)` 没有 Lime 外部生产调用，subagent staging adapter 已直接调用 pinned-provider `reply_with_provider(...)`，`Agent::reply(...)` wrapper 已删除并由治理守卫禁止恢复；`reply_with_provider(...)` 仍因 `provider_reply_exit_source.rs` 最后一跳暂时 public，继续是 R2/R3 blocker。验证通过：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider --package aster-core -- --check`；`npx prettier --check ...`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`161 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-notification-classification-model-provider" cargo test --manifest-path "lime-rs/Cargo.toml" -p model-provider provider_stream --lib -j 1 -- --nocapture`，`42 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-notification-classification-aster-core" cargo test --manifest-path "lime-rs/Cargo.toml" -p aster-core responses_streaming_safety_buffering --lib -j 1 -- --nocapture`，`1 passed`，冷编耗时 `21m44s`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`。分类：`current` = model-provider notification envelope / text classification；`dead / guarded` = `Agent::reply(...)` wrapper 与旧 Aster notification prefix；`compat blocker` = `ProviderReplyExitSource` / `run_provider_reply_exit_source(...)`、`reply_with_provider(...)` 最后一跳和 Aster internal reply loop。
- 2026-07-10：继续削薄 `agent-compat/src/agents/reply_parts.rs` 的 turn tool surface / scope 纯规则。新增 `tool-runtime::turn_tool_surface::{RuntimeTurnToolSurfaceMode, RuntimeTurnToolScope, runtime_turn_tool_surface_mode_from_metadata, runtime_turn_tool_scope_from_metadata, runtime_turn_tool_surface_allows_tool_name, runtime_turn_tool_scope_allows_tool_name}`，承接 `direct_answer` / `local_workspace` / `compact_tools` 工具面、allowed / disallowed tool scope、prompt guidance、extension prompt context 与 workspace hints 判定。`reply_parts.rs` 只读取 current turn metadata、用 Aster `ToolRegistry::canonical_name(...)` 作为临时 alias resolver，并适配 `Vec<rmcp::model::Tool>`；不再本地维护 tool surface/scope parser。分类：`current` = `tool-runtime` turn tool surface / scope policy；`compat blocker` = Aster `ToolRegistry` / rmcp `Tool` 仍是未迁 reply loop 的 tool source adapter；`dead / guarded` = `reply_parts.rs` 本地 `normalize_turn_metadata_tool_list` / `matches_turn_tool_scope` / prompt policy 回流。验证通过：`CARGO_TARGET_DIR=".lime/cargo-target/turn-tool-surface-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime turn_tool_surface --lib -j 1 -- --nocapture`，`5 passed`；`CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=".lime/cargo-target/provider-image-input-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`162 passed`；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package tool-runtime --package aster-core --package lime-agent -- --check`。
- 2026-07-10：继续迁出 `reply_parts.rs` 的工具调用 surface normalization。新增 `tool-runtime::tool_call_surface::{runtime_tool_call_surface_name, runtime_tool_call_normalize_arguments}`，承接 available tool exact / case-insensitive match、Aster registry canonical alias 结果落回 current surface name，以及 `Read` 的 `file_path` / `filePath` / `head`、`Glob` / `Grep` 的 `query -> pattern` 参数 normalization；`reply_parts.rs` 只把 `rmcp::CallToolRequestParam` 和 Aster `ToolRegistry::canonical_name(...)` 适配给 current helper。分类：`current` = `tool-runtime` 工具调用名称和参数 normalization；`compat blocker` = Aster `ToolRegistry` / rmcp `Tool` / Aster reply loop native tool execution 仍是 source adapter；`dead / guarded` = `reply_parts.rs` 本地 `current_surface_tool_name` / `normalize_current_surface_tool_call` / `normalize_current_surface_tool_arguments` / `integer_argument` / `copy_string_argument_if_missing` 回流。验证通过：`CARGO_TARGET_DIR=".lime/cargo-target/tool-call-surface-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime tool_call_surface --lib -j 1 -- --nocapture`，`5 passed`；`CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=".lime/cargo-target/provider-image-input-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`164 passed`；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package tool-runtime --package aster-core --package lime-agent -- --check`。
- 2026-07-10：体量风险更新：`agent-compat/src/agents/reply_parts.rs` 经本轮继续削薄后约 `1052` 行，仍超过 `1000` 行强制拆分线；本轮只删除本地工具调用 normalization 并迁到 `tool-runtime`，没有在 staging 文件追加新业务。退出条件：下一刀优先迁出 R4 native `ToolRegistry` / `Tool` trait 执行壳或 R2/R3 provider reply 最后一跳，继续减少该文件，禁止继续在其中新增 provider / tool / session 业务逻辑。`src/lib/governance/asterMigrationBoundary.test.ts` 仍是既有超大守卫文件，本轮只追加回流守卫；退出条件仍是后续把 provider/tool owner 守卫拆成分域测试文件。
- 2026-07-10：继续收紧 provider source execution owner。`RuntimeReplyProviderSourceBackendCall`、`RuntimeReplyProviderSourceFuture`、`RuntimeReplyProviderExecutionRunner<R>`、`RuntimeReplyProviderExecutionSource<R>`、`run_provider_source_execution(...)` 与对应 wrapper tests 已从 `model-provider/src/provider_stream.rs` 拆入 `model-provider/src/provider_stream/source_execution.rs`，`provider_stream.rs` 降到约 `876` 行，退出 1000 行强制拆分风险区。分类：`current` = `model-provider::provider_stream::source_execution`；`compat blocker` = `ProviderReplyExitRunner` / 私有 `run_provider_reply_exit_source(...)` 仍调用 Aster `Agent::reply_with_provider(...)`；`dead / guarded` = 把 wrapper 实现放回 `agent-runtime`、`agent-compat` 或 `provider_stream.rs` 主文件。验证通过：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider -- --check`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-source-execution-split-model-provider" cargo test --manifest-path "lime-rs/Cargo.toml" -p model-provider provider_stream --lib -j 1 -- --nocapture`，`42 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-source-execution-split-agent-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p agent-runtime reply_backend --lib -j 1 -- --nocapture`，`20 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`165 passed`。
- 2026-07-10：继续 R4 native tool loop 削薄。新增 `tool-runtime::tool_result_projection::{RuntimeToolResultParts, runtime_tool_result_to_call_tool_result, runtime_tool_result_surface_updated}`，承接 Aster native `ToolResult` 到 MCP `CallToolResult` 的文本 / error / metadata / model-visible image / `tool_surface_updated` 投影；`agent-compat/src/agents/agent.rs` 只把旧 result 字段搬运到 current parts，不再持有本地 projection helper。为避免 `tool_result.rs` 膨胀，projection 拆到约 `204` 行新模块，`tool_result.rs` 保持约 `744` 行。分类：`current` = `tool-runtime::tool_result_projection`；`compat blocker` = Aster `ToolRegistry::execute(...)` / `Tool` / `ToolContext` 仍服务 reply loop native tool execution；`dead / guarded` = `agent-compat` 本地 native tool result projection。验证通过：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package tool-runtime --package aster-core --package lime-agent -- --check`；`CARGO_TARGET_DIR=".lime/cargo-target/tool-result-projection-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime tool_result_projection --lib -j 1 -- --nocapture`，`3 passed`；`CARGO_INCREMENTAL=0 CARGO_TARGET_DIR=".lime/cargo-target/provider-image-input-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`168 passed`。
- 2026-07-10：继续拆薄 provider stream 主 facade。`ProviderStreamPoll` / cancel reason / poll helper 拆入 `provider_stream/poll.rs`，`RuntimeReplyProviderSamplingRequest` / sampling mode / empty-first-content retry 拆入 `provider_stream/sampling.rs`，`RuntimeReplyResponseEvent` / `RuntimeReplyResponseItem` / payload 拆入 `provider_stream/response_event.rs`；`provider_stream.rs` 只保留 `mod` / `pub use` facade 和 provider handle / request / stream event 主 contract，当前约 `701` 行。分类：`current` = `model-provider::provider_stream::{poll,sampling,response_event,source_execution}`；`compat blocker` = Aster `AgentEvent` projector 和 `reply_with_provider(...)` 最后一跳仍未删除；`dead / guarded` = 把 poll / sampling / response event DTO 重新塞回 `provider_stream.rs` 主文件、`agent-runtime` 或 `agent-compat`。验证通过：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider -- --check`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-stream-contract-split-model-provider" cargo test --manifest-path "lime-rs/Cargo.toml" -p model-provider provider_stream --lib -j 1 -- --nocapture`，`42 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`，耗时 `3m08s`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`167 passed`。
- 2026-07-10：继续把 provider response content 规则从 Aster loop 中抽离。新增 `model-provider::provider_stream::response_content::{RuntimeReplyProviderResponseContent, provider_stream_response_text_chars, provider_stream_response_has_notification_text, provider_stream_response_tool_input_delta_events}`，承接 provider response 中 text 首字计数、notification 文本分类和 tool-input delta event 投影的组合入口；`agent-compat/src/agents/agent.rs` 只把 Aster `MessageContent` 映射为 current response content enum。分类：`current` = `model-provider::provider_stream::response_content`；`compat blocker` = Aster `MessageContent` 仍是 provider stream source adapter，`reply_with_provider(...)` 最后一跳仍未删除；`dead / guarded` = `agent-compat/agent.rs` 恢复直接调用 `provider_stream_first_text_delta_chars` / `provider_stream_tool_input_delta_events` / `provider_stream_has_notification_text` 或本地过滤规则。验证通过：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider --package aster-core --package lime-agent -- --check`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-response-content-model-provider" cargo test --manifest-path "lime-rs/Cargo.toml" -p model-provider provider_stream --lib -j 1 -- --nocapture`，`44 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`168 passed`。
- 2026-07-10：继续削薄 provider failure / trace 交界。`model-provider::provider_stream::failure` 新增 `RuntimeReplyProviderFailureKind::from_category(...)` / `as_category(...)` 与 `RuntimeReplyProviderFailure::from_category(...)`，承接 provider telemetry category 到 current failure DTO 的映射；`agent-compat/src/agents/agent.rs` 不再维护 `provider_failure_kind(...)` 或 `ProviderError::* -> RuntimeReplyProviderFailureKind` 本地分支，只把 Aster `ProviderError` 的 category/retry/rejection 三个事实投影给 current DTO；`agent-compat/src/agents/provider_trace.rs` 不再直接依赖 Aster `ProviderError`，trace failed event 只消费 current failure DTO。分类：`current` = `model-provider` provider failure category projection；`compat blocker` = Aster `ProviderError` 仍是 reply loop source error，`ProviderReplyExitSource` / `reply_with_provider(...)` 最后一跳仍未删除；`dead / guarded` = staging loop 恢复 `provider_failure_kind(...)` 或本地 `ProviderError::*` 分类表。验证通过：`rustfmt --edition 2021 --check ...` 本轮 Rust 写集；`CARGO_TARGET_DIR=".lime/cargo-target/provider-failure-category-model-provider" cargo test --manifest-path "lime-rs/Cargo.toml" -p model-provider provider_stream --lib -j 1 -- --nocapture`，`45 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`169 passed`。备注：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --package model-provider --package aster-core --package lime-agent -- --check` 仍会因既有脏文件 `ask_bridge.rs`、`native_tools/gateway_bridge.rs`、`aster_reply_adapter.rs` import 排序失败，本轮未改这些无关写集。
- 2026-07-10：继续迁出 provider response context header 规则。新增 `model-provider::provider_stream::response_context::{RuntimeReplyProviderResponseContext, provider_stream_response_context_from_header_pairs(...)}`，复用 `agent-protocol::provider_trace::ProviderTraceResponseContext` 作为 DTO，承接 provider response request-id header allowlist、长度限制和可见 ASCII 清洗；`agent-compat/src/session_context.rs` 只保留 task-local 存储和 `reqwest::HeaderMap` 读取适配，不再维护 `PROVIDER_REQUEST_ID_HEADERS`、`MAX_PROVIDER_REQUEST_ID_LEN` 或 `normalize_provider_request_id(...)`。分类：`current` = `model-provider` provider response context extraction + `agent-protocol` provider trace response context DTO；`compat blocker` = Aster `session_context` 仍是 reply loop task-local carrier；`dead / guarded` = staging session context 本地 request-id header policy。验证通过：`rustfmt --edition 2021 --check ...` response context 写集；`CARGO_TARGET_DIR=".lime/cargo-target/provider-failure-category-model-provider" cargo test --manifest-path "lime-rs/Cargo.toml" -p model-provider provider_stream --lib -j 1 -- --nocapture`，`47 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`171 passed`。
- 2026-07-10：继续 R4 native tool loop 削薄。`tool-runtime::tool_batch` 新增 `ToolExecutionScheduleBatch<T>`、`runtime_tool_call_concurrency_safe(...)` 与 `partition_tool_execution_requests(...)`，承接 reply loop 工具并发安全分类和相邻安全请求合批策略；`agent-compat/src/agents/agent.rs` 只把 Aster `ToolRequest` 投影为 tool name / optional command 后调用 current helper，不再直接依赖 `tool_runtime::shell_analysis::{is_bash_command_concurrency_safe,is_powershell_command_concurrency_safe}` 或维护本地 `ToolExecutionBatch` / partition 规则。顺手收缩 `agent-compat` private module 的无消费者 `RetryConfig` / provider helper re-export，解除 `lime-agent request_tool_policy` 编译阻塞且不恢复旧 public surface。分类：`current` = `tool-runtime::tool_batch` execution scheduling policy；`compat blocker` = Aster `ToolRequest` / `ToolRegistry` / `Tool` trait execution 仍服务 reply loop fallback；`dead / guarded` = staging loop 本地工具并发安全 / 执行合批规则。验证通过：`rustfmt --edition 2021 --check` 本轮 Rust 写集；`CARGO_TARGET_DIR=".lime/cargo-target/tool-batch-schedule-tool-runtime" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime tool_batch --lib -j 1 -- --nocapture`，`4 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx prettier --check "src/lib/governance/asterMigrationBoundary.test.ts"`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`171 passed`。R2/R3 `ProviderReplyExitSource` / `reply_with_provider(...)` 最后一跳仍未删除，不能宣称 Phase 6 完成。
- 2026-07-10：继续削薄 provider trace DTO 交界。`model-provider::provider_stream` re-export `agent-protocol::provider_trace::{ProviderTraceEvent, ProviderTraceStage, ProviderTraceFailure}` 为 `RuntimeReplyProviderTrace*` alias，并通过 `provider_stream_trace_failure(...)` 把 current failure DTO 转为 trace failure；`agent-compat/src/agents/provider_trace.rs` 不再定义本地 `ProviderTraceStage` / `ProviderTraceEvent`，只保留 thin wrapper 生成 current DTO；`agent/src/event_converter.rs` 对 `AgentEvent::ProviderTrace` 直接透传 current DTO，不再做 stage 映射或字段复制，避免丢失 runtime provider metadata。分类：`current` = `agent-protocol` provider trace DTO + `model-provider` provider trace alias / failure projection；`compat blocker` = Aster `AgentEvent::ProviderTrace` 仍从未迁 reply loop 传出；`dead / guarded` = `agent-compat` 本地 provider trace DTO、`event_converter` stage 转换 / 字段复制。验证通过：`rustfmt --edition 2021 --check` 本轮 Rust 写集；`CARGO_TARGET_DIR=".lime/cargo-target/provider-trace-dto-model-provider" cargo test --manifest-path "lime-rs/Cargo.toml" -p model-provider provider_stream --lib -j 1 -- --nocapture`，`47 passed`；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx prettier --check "src/lib/governance/asterMigrationBoundary.test.ts"`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，`172 passed`；`git diff --check` 本轮写集通过。R2/R3 `ProviderReplyExitSource` / `reply_with_provider(...)` 最后一跳仍未删除，不能宣称 Phase 6 完成。
- 2026-07-10：继续收缩 provider trace public surface。`agent-compat` root 和 `agents/mod.rs` 不再 re-export `ProviderTraceStage`；该 stage 类型只留在 current `agent-protocol` / `lime-agent` protocol alias，`agent-compat` 只因 `AgentEvent::ProviderTrace` 字段暂时暴露 `ProviderTraceEvent`。分类：`dead / guarded` = `aster::ProviderTraceStage` root re-export；`compat blocker` = `ProviderTraceEvent` root re-export 仍随 Aster `AgentEvent` source adapter 退场；`current` = `agent-protocol::provider_trace::ProviderTraceStage`。验证通过：`rustfmt --edition 2021 --check` provider trace re-export 写集；`CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 1 -- --nocapture`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`172 passed`。

- 2026-07-10：继续 R4 标准 native dispatch 执行 owner 收敛。新增 `tool-runtime::native_dispatch_execution::{RuntimeNativeDispatchToolRequest, execute_runtime_native_dispatch_tool(...)}`，承接 dispatch-backed 标准工具的 overlay lookup、permission preflight、cancel fail-fast、`runtime_native_dispatch_handle().execute(...)` 和 `RuntimeToolResultParts` 投影；`agent-compat/src/agents/agent.rs` 只把 Aster `ToolContext` / current turn context 适配成 request，不再直接 import `runtime_native_dispatch_handle`、`check_runtime_native_tool_permissions` 或 `RuntimeNativePermissionDecision`。分类：`current` = `tool-runtime::native_dispatch_execution`；`compat blocker` = Aster `ToolRegistry::execute(...)` fallback 仍服务 Bash/PowerShell/Read/Glob/Grep/Ask/Skill、gateway-backed tools 与 MCP / extension bridge；`dead / guarded` = 在 `agent-compat` 恢复 dispatch/permission 编排。验证通过：`rustfmt --edition 2021 --config skip_children=true --check` 覆盖本轮 Rust 写集；`CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime native_dispatch_execution --lib -j 2 -- --nocapture`，`2 passed`；同 target `cargo test ... -p lime-agent request_tool_policy --lib`，`81 passed`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" ...`，`172 passed`；`npx prettier --check ...` 与 scoped `git diff --check` 通过。R4 仍未完成，R2/R3 `ProviderReplyExitSource` / `reply_with_provider(...)` 最后一跳也未删除。

- 2026-07-10：继续 R4 Bash / PowerShell 前台执行壳迁出。新增 `tool-runtime::shell_execution::{RuntimeShellToolRequest, execute_runtime_shell_tool(...)}`，承接前台 Bash / PowerShell 的参数解析、current shell permission 判定、read-target / blocked sleep / Windows WSL path preflight、embedded process execution、output decode、metadata / result projection，并在执行环境写入 `AGENT_TERMINAL=1`。`agent-compat/src/agents/agent.rs` 只把 Aster `ToolContext`、environment、cancel token、workspace sandbox 和 current turn context 适配给 shell owner；reply loop native branch 的顺序固定为 live-execution hook -> current shell executor -> current native dispatch executor -> Aster `registry.execute(...)` fallback。完全授权语义同步到 shell owner：`approval_policy=never`、`sandbox_policy=danger-full-access` 或 turn metadata `accessMode=full-access` 时，warning command 不再回落到 Aster approval / registry；非 full-access warning、background request 和 workspace sandbox 仍 fallback，避免本刀扩到旧 `TaskManager` / HITL。分类：`current` = `tool-runtime::shell_execution`；`compat blocker` = background shell、workspace sandbox、Read / Glob / Grep / Ask / Skill、gateway-backed tools、MCP / extension bridge 与 Aster `Tool` trait 壳；`dead / guarded` = 在 Aster shell adapter 恢复 shell permission / process execution owner，或恢复 `ASTER_TERMINAL` 环境标记。验证通过：`rustfmt --edition 2021 --config skip_children=true --check` 覆盖 shell 写集；`CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 CARGO_TARGET_DIR=".lime/cargo-target/provider-progress-lime-agent" cargo test --manifest-path "lime-rs/Cargo.toml" -p tool-runtime shell_execution --lib -j 2 -- --nocapture`，`7 passed`；同 target `cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy --lib -j 2 -- --nocapture`，`81 passed`；`npx prettier --check "src/lib/governance/asterMigrationBoundary.test.ts"`；`npx vitest run "src/lib/governance/asterMigrationBoundary.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，`172 passed`。R4 仍未完成，root `aster` dependency 和 `lime-agent` 的 `aster.workspace = true` 仍不能删除。

- 2026-07-10：R4 shell/full-access GUI Gate A/B 收口验证。`scripts/agent-runtime/claw-chat-current-fixture-gui-actions.mjs` 的 GUI 输入改为受控 value setter + `InputEvent("input")` + `change`，避免 Playwright `textarea.fill(...)` 在中文 mention prompt 中追加异常 ASCII 字符；`claw-chat-current-fixture-smoke.test.mjs` 已守卫 helper 存在，`homeInputToSubmitAcceptedMs` 预算从 `1500ms` 调整为 `1800ms`，仍保留 preview / dispatch / paint 子预算。验证通过：Gate B `approval-request-full-access` Electron CDP fixture 通过，summary 为 `.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-approval-full-access-shell-r4-after-input-helper-summary.json`，证明完全授权下 shell warning 不出现确认框；Gate A `LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:agent-runtime-current-fixture` 完整通过，覆盖 approval resume/decline/cancel/full-access、inputbar restore/pending steer、Plan hydrate、Skills Runtime、Multi-Agent Team、MCP structuredContent、media reference 与 Content Factory Article Editor，`liveProviderUsed=false`。该验证只证明 current fixture 主链可用；R4 remaining registry fallback、R2/R3 provider reply exit source 和 R5/R6 session/runtime store 仍是 blocker。
