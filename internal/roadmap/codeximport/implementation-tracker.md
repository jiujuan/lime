---
title: Codex 对话导入实施问题跟踪
status: active
owner: app-server-runtime
updated: 2026-06-19
---

# Codex 对话导入实施问题跟踪

本文件跟踪 Codex 对话导入从 PRD 到落地过程中遇到的架构问题、兼容差距、处理状态和验证入口。目标是让实现过程中的判断沉淀到仓库，而不是只停留在聊天上下文。

## 状态口径

| 状态          | 含义                                       |
| ------------- | ------------------------------------------ |
| `open`        | 已确认存在，尚未处理。                     |
| `in_progress` | 正在本轮处理。                             |
| `resolved`    | 已实现并有定向验证入口。                   |
| `deferred`    | 不阻塞当前全量产品化阶段，已明确后续入口。 |
| `blocked`     | 需要外部信息或产品决策才能继续。           |

## 当前主线

Codex-first 的导入主线为：

```text
Codex state_*.sqlite / rollout JSONL
  -> App Server conversationImport/source/scan
  -> App Server conversationImport/thread/preview
  -> 用户确认弹窗
  -> App Server conversationImport/thread/commit
  -> RuntimeCore StoredSession + AgentEvent
  -> agentSession/read / evidence/export / replay current 主链
```

首期明确不写回 Codex 原始目录，不新增 renderer 本地扫描，不创建第二套 transcript store。

## 2026-06-19 进度记录：手动选择本地历史目录对齐 FR-02

- `resolved`：导入弹窗新增“选择目录”入口，复用现有 Electron Desktop Host `agent_app_select_directory` current 壳能力，经 `src/lib/api/agentApps.ts -> selectAgentAppDirectory(...)` 进入系统目录选择器；renderer 仍不直接扫描本地文件，也不新增第二套导入命令。
- `resolved`：用户选择目录后，弹窗会把选中路径写回本地历史数据目录输入框，并立即按该 `sourceRoot` 重新调用 `conversationImport/source/scan`；扫描仍携带 `includeArchived=true`，保持 active + archived 本地历史同一导入主链。
- `guarded`：`AppSidebar.conversations.test.tsx` 新增回归，覆盖系统目录选择器返回路径后使用该路径重新扫描，并断言目录选择 title 走五语言资源；`AppSidebar.testFixtures.tsx` 补齐 `selectAgentAppDirectory` mock，避免组件测试绕过 API 网关。
- `i18n`：新增 `navigation.sidebar.importDialog.sourceRoot.dialogTitle`、`action.chooseDirectory`、`error.chooseDirectory`，覆盖 `zh-CN / zh-TW / en-US / ja-JP / ko-KR`。
- 已通过验证：`npx vitest run "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/AppSidebar.conversations.test.tsx" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，56 tests passed；`npm run i18n:check:json`，五语言 coverage 100%、missing=0、extra=0；`npx eslint "src/components/app-sidebar/AppSidebarConversationImportDialog.tsx" "src/components/app-sidebar/AppSidebarConversationImportThreadList.tsx" "src/components/AppSidebar.conversations.test.tsx" "src/components/AppSidebar.testFixtures.tsx" --max-warnings 0` 通过；`npm run test:contracts` 通过；相关写集 `git diff --check` 通过。
- `resolved`：导入后的 HTML preview 空白阻塞已关闭。Canvas Workbench HTML 模式在 read model 已有 HTML 内容时优先使用 `srcDoc`，只有内容为空时才回退本地文件 URL；同一 `previewOpenRequest.requestKey` 处理后不再反复把用户手动切换的 Markdown / HTML / Code 预览模式拉回默认模式。2026-06-19 复跑真实 Electron 点击闭环通过，summary `.lime/qc/gui-evidence/codex-import-click-through-fixture/codex-import-click-through-fixture-summary.json` 显示 `ok=true`、`consoleErrors=[]`、`importedFilePreviewArtifactsSummary.openedAllImportedPreviewArtifacts=true`、HTML 项 `htmlPreviewVisible=true` 且 `fallbackSurfaceVisible=false`；通用代码产物工作台 fixture 也通过，证明非导入 artifact workbench 打开路径未被误伤。
- 已通过验证：`npx vitest run "src/components/agent/chat/components/canvas-workbench/CanvasWorkbenchPreviewModeViewModel.unit.test.ts" "src/components/agent/chat/components/CanvasWorkbenchLayout.test.tsx" "src/components/agent/chat/components/CanvasWorkbenchLayout.coding.test.tsx" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，27 tests passed；`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000` 通过；`npm run smoke:code-artifact-workbench-electron-fixture` 通过；定向 ESLint 与相关写集 `git diff --check` 通过。

## 2026-06-19 进度记录：导入文件预览点击链路收口

- `resolved`：导入 `read_file` 工具按钮现在会把历史工具输出作为文件内容传给工作台；当历史工具只有 `path` 而没有 `content/text` 时，不再把内容固定为空，避免导入会话只能依赖临时路径读取。
- `resolved`：非通用工作台的任务文件类型识别补齐 PDF、DOCX、XLSX、PPTX、HTML、JSON、YAML、CSV/TSV 等文档扩展名；带文本的导入 PDF 会作为 `document` 进入 `task:<id>` 预览选择，不再被 `other` 类型拦截后停留在上一份文件。
- `resolved`：点击闭环 fixture 的 PDF 工具输出与 fixture 文件内容对齐为 `IMPORTED_PREVIEW_PDF_TEXT`，并在 GUI helper 失败证据中记录所有 `inline-tool-open-file` 按钮、匹配按钮和 `fileSystem/readFilePreview` trace 信号，后续若再出现停留在上一份文件，可以直接区分“没点到 / 内容为空 / preview request 被覆盖”。
- `verified`：真实 Electron 点击闭环在刷新 `dist` 后通过，summary `.lime/qc/gui-evidence/codex-import-click-through-fixture/codex-import-click-through-fixture-summary.json` 为 `ok=true`、`consoleErrors=[]`、`importedFilePreviewArtifactsSummary.openedAllImportedPreviewArtifacts=true`；Markdown、HTML、DOCX、XLSX、PPTX、PDF 六类导入文件均进入 Canvas Workbench，图片附件预览、侧栏会话可发现性、环境弹层边界、同 session 续聊和三视口 visual audit 同时通过。
- 已通过验证：`npx vitest run "src/components/agent/chat/workspace/useWorkspaceArtifactPreviewActions.test.tsx" "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" "src/components/agent/chat/components/StreamingRenderer.test.tsx" --silent=passed-only --disableConsoleIntercept`，109 tests passed；`npx vitest run "scripts/electron/codex-import-click-through-fixture-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`，4 tests passed；定向 ESLint 通过；`npm run build:renderer:electron:smoke` 通过；`npm run smoke:codex-import-click-through-electron-fixture -- --timeout-ms 180000` 通过。

## 2026-06-19 进度记录：导入入口与会话展示五语言 guard 收口

- `resolved`：导入弹窗与侧栏入口的 i18n 守卫从少量 warning key 扩展为 `navigation.sidebar.importDialog.*` 与 `navigation.sidebar.conversations.import*` 整个前缀，后续新增按钮、分组、toast、空态、confirm 或 provenance 展示文案时，必须同时覆盖 `zh-CN / zh-TW / en-US / ja-JP / ko-KR`。
- `resolved`：导入后的会话展示继续收口到 `agent` namespace。`generalWorkbench.taskRail.importedProcess.*` 与 `generalWorkbench.importedHistory.*` 已有五语言资源守卫；`AgentThreadTimeline` 真实渲染路径通过 `useTranslation("agent")` 把 `t` 传入 timeline view model，导入命令 / 思考 / 搜索 / 补丁摘要不依赖硬编码中文 fallback。
- `resolved`：导入来源摘要默认 fallback 改为 source-neutral 英文，真实 UI 仍由 locale resource 决定展示文本；协议里的 `sourceClient`、thread id、provenance 和工具枚举继续作为 stable facts，不做本地化判断。
- `resolved`：导入来源摘要和导入过程摘要的缺资源 fallback 已全部收口为 source-neutral 英文，并补充无 `t` / 缺 key 场景回归，避免 i18n 未初始化或测试桩缺 key 时把中文 fallback 暴露到非中文 locale。
- `resolved`：TypeScript 5.9 下 `react-i18next` 重载函数不能再直接透传给动态 key helper。`ToolSearchSummaryPanel`、`PlanComposerDecisionPanel`、`AgentThreadTimeline`、`ImportedRuntimeEventDetailPanel`、artifact fallback / source summary 均改为窄 `String(t(key as never, options as never))` 包装，保留资源事实源，同时避免动态 key 被推导成 `unknown` 或触发 compiler 内部崩溃。
- `resolved`：工具展示 i18n 漏点已补齐。`ToolSearchSummaryPanel` 清洗底层 `deferred` note 时不再写死中文“更多”，改为 `agentChat.toolCall.toolSearch.note.deferredLabel` 五语言资源；记忆工具证据缺少 path 时不再写死英文 `memory store`，改为 `agentChat.toolCall.memoryEvidence.defaultPath` 五语言资源。协议事实、工具名、source id 仍保持 stable，不进入本地化判断。
- 已通过验证：`npx vitest run "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/agent/chat/utils/agentThreadGrouping.test.ts" "src/components/agent/chat/workspace/importedConversationSourceSummary.unit.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，37 tests passed；`npm run i18n:check:json`，五语言 coverage 100%、missing=0、extra=0；`npx eslint "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/agent/chat/utils/agentThreadGrouping.ts" "src/components/agent/chat/utils/agentThreadGrouping.test.ts" "src/components/agent/chat/workspace/importedConversationSourceSummary.ts" "src/components/agent/chat/workspace/importedConversationSourceSummary.unit.test.ts" --max-warnings 0` 通过。
- 追加验证：`npx vitest run "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/agent/chat/utils/agentThreadGrouping.test.ts" "src/components/agent/chat/workspace/importedConversationSourceSummary.unit.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，39 tests passed；`npm run i18n:check:json`，五语言 coverage 100%、missing=0、extra=0；同范围 ESLint 与 `git diff --check` 通过。
- 追加验证：文件级 TS crash locator 已完成 `2828` 个源码文件语义诊断，无 TypeScript `Debug Failure`；`npm run i18n:check:json` 仍为五语言 coverage 100%、missing=0、extra=0；`npx vitest run "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/agent/chat/components/ToolSearchSummaryPanel.test.tsx" "src/components/agent/chat/workspace/PlanComposerDecisionPanel.test.tsx" --silent=passed-only --disableConsoleIntercept --testTimeout=30000` 通过，22 tests passed；`npx vitest run "src/i18n/__tests__/types.test.ts" "src/components/agent/chat/components/ImportedRuntimeEventDetailPanel.test.tsx" "src/components/agent/chat/components/AgentThreadTimeline.process.test.tsx" --silent=passed-only --disableConsoleIntercept --testTimeout=30000` 通过，17 tests passed；相关 ESLint 与 `git diff --check` 通过。后续非 i18n 类型缺口已在 2026-06-19 的 current quality pass 中复核关闭。
- 追加验证：`npx vitest run "src/components/agent/chat/components/ToolSearchSummaryPanel.test.tsx" "src/components/agent/chat/utils/memoryToolEvidence.unit.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000` 通过，7 tests passed；`npx vitest run "src/i18n/__tests__/types.test.ts" "src/i18n/__tests__/loadNamespace.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000` 通过，8 tests passed；`npm run i18n:check:json` 五语言 coverage 100%、missing=0、extra=0；相关 ESLint 与 `git diff --check` 通过。
- `resolved`：多模态任务预览继续从巨型 `taskPreviewFromToolResult.ts` 收口。图片、视频、联网搜图、配音、转写分支已拆到独立模块，中心文件从 887 行降到 290 行；共享工具结果解析收敛到 `taskPreviewToolResultShared.ts`，避免后续继续向中心分发文件堆叠新多模态逻辑。
- `resolved`：图片 / 视频任务默认状态、fallback prompt 已切到 `agentChat.taskPreview.image.*` 与 `agentChat.taskPreview.video.*` 五语言资源；联网搜图、配音、转写任务预览与 artifact document 继续走同一 `agent` namespace。协议事实、tool name、task type、provider、model、path、sourceClient 和 provenance 保持 stable facts，不进入本地化判断。
- `guarded`：`taskPreviewFromToolResult.test.ts` 新增英文 locale 回归，覆盖图片 / 视频默认状态、联网搜图 artifact、配音 / 转写 artifact，防止非中文界面重新混入中文 fallback；同时新增图片 / 视频任务预览资源覆盖断言，要求 `zh-CN / zh-TW / en-US / ja-JP / ko-KR` 全量存在。
- 已通过验证：`npx eslint "src/components/agent/chat/utils/taskPreviewFromToolResult.ts" "src/components/agent/chat/utils/taskPreviewFromToolResult.test.ts" "src/components/agent/chat/utils/taskPreviewAudioTranscription.ts" "src/components/agent/chat/utils/taskPreviewAudioTranscriptionArtifact.ts" "src/components/agent/chat/utils/taskPreviewCopy.ts" "src/components/agent/chat/utils/taskPreviewWebImageSearch.ts" "src/components/agent/chat/utils/taskPreviewImage.ts" "src/components/agent/chat/utils/taskPreviewVideo.ts" "src/components/agent/chat/utils/taskPreviewToolResultShared.ts" --max-warnings 0` 通过；`npx vitest run "src/components/agent/chat/utils/taskPreviewFromToolResult.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，20 tests passed；`npm run i18n:check:json` 五语言 coverage 100%、missing=0、extra=0；`npm run i18n:unused -- --check` unused=0；相关文件 `git diff --check` 通过。
- 追加真实 Electron 验证：`npm run smoke:agent-runtime-current-fixture` 通过，覆盖 history/cache hydration、final_done 工具收尾、Claw 终态 UI、代码工作台、停止后同会话继续输出，`liveProviderUsed=false`；`npm run smoke:codex-import-click-through-electron-fixture -- --timeout-ms 180000` 通过，summary `ok=true`、`consoleErrors=[]`，导入 13 items / 4 messages，覆盖导入弹窗、确认导入、导入细节、附件/文件预览、侧栏可发现性、环境弹层边界、同 session 续聊和三视口 visual audit；`npm run smoke:local-history-import-real-sample-visual-audit -- --timeout-ms 300000 --commit-timeout-ms 240000` 通过，真实 `/Users/coso/Documents/dev/ai/limecloud/content-studio` 样本导入 read model 为 92 messages / 1901 items，完整记录下钻 probe 为 `totalEvents=19419`、`materializedRuntimeEvents=2343`、`sidecarRuntimeEvents=17076`，`consoleErrors=[]`。
- 追加质量复核：`npm run test:contracts` 通过，覆盖协议类型生成检查、App Server client contract、command contracts、harness、modality、scripts governance、Electron release workflow、cleanup report 和 docs boundary；`npm run verify:gui-smoke` 首次暴露拆分后的 artifact document builder 重新导出错误，已把 `taskPreviewFromToolResult.ts` 改为从 `taskPreviewAudioTranscriptionArtifact.ts` 重新导出，随后复跑完整通过，覆盖 renderer production build、Electron main / preload build、`typecheck:electron`、App Server sidecar assets、renderer load、App Server ready、Claw workbench shell 和 memory settings。定向 `npx eslint "src/components/agent/chat/utils/taskPreviewFromToolResult.ts" "src/components/agent/chat/utils/taskPreviewAudioTranscription.ts" "src/components/agent/chat/utils/taskPreviewAudioTranscriptionArtifact.ts" --max-warnings 0` 与 `npx vitest run "src/components/agent/chat/utils/taskPreviewFromToolResult.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000` 同步通过。
- 追加 Rust 定向复核：此前因 artifact lock 未拿到全组通过证据的 `runtime::conversation_import::tests::runtime_events` 已复跑通过，10 tests passed，覆盖重复用户消息合并、未闭合 lifecycle、assistant 顺序、tool/command/patch、late runtime events、update_plan、specialized runtime items 和高容量事件预算；`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent request_tool_policy::tests::allowed_web_search_should_not_run_preflight_from_message_keywords --lib` 通过，确认 WebSearch preflight 默认关闭回归可编译，且本轮未复现 `should_run_web_search_preflight` dead_code warning。
- 追加 TypeScript 复核：`npm run typecheck -- --pretty false` 首次暴露 `useWorkspaceArtifactPreviewActions.test.tsx` 夹具类型漂移：mocked `crypto.randomUUID()` 不是 UUID 形状，且非通用工作台测试把 `mappedTheme` 写成不再被 hook 接受的 `"article"`。已按真实类型修正测试夹具，保持 `activeTheme="article"` 表达主题工作台场景、`mappedTheme="general"` 走当前 hook 类型，并更新 task selection 断言；`npx vitest run "src/components/agent/chat/workspace/useWorkspaceArtifactPreviewActions.test.tsx" --silent=passed-only --disableConsoleIntercept --testTimeout=30000` 通过，19 tests passed；同文件 ESLint 通过；复跑 `npm run typecheck -- --pretty false` 通过。

## 2026-06-19 进度记录：只读 source health 对齐 FR-04

- `resolved`：PRD FR-04 要求 health check 返回 Codex home 是否存在、state DB 是否可读、rollout 文件数量。此前 `conversationImport/source/scan` 只有 `readable/threadCount/statePath/message`，无法机械证明 SQLite 不可读但 `session_index.jsonl` fallback 可用、或 source home 缺失时的具体 health 事实。
- `resolved`：`ConversationImportSourceSummary` 新增 `sourceHomeExists / stateDbReadable / rolloutFileCount`。Codex importer 在 scan / preview 中只读统计 source home、`state_*.sqlite` 可读性和 `sessions` / `archived_sessions` 下 rollout 文件数量；renderer 仍只经 App Server JSON-RPC 获取结果，不直接扫描 `.codex`。
- `guarded`：Rust source scan 测试覆盖 state DB 可读、session_index fallback、missing source 三种 health；前端 `conversationImport` 网关 shape guard 把三个 health 字段作为 current scan response 必备事实，缺字段 fail closed；矩阵守卫新增 `read-only source health` 行。
- 验证入口：`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import::tests::scans_ --lib`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import::tests::health --lib`；`npx vitest run "src/lib/api/conversationImport.test.ts" "scripts/electron/codex-import-fidelity-acceptance-matrix.test.mjs" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`；`npm run check:protocol-types`。

## 2026-06-19 进度记录：项目路径筛选对齐 FR-08

- `resolved`：PRD FR-08 要求项目路径筛选支持 cwd exact / prefix / contains；后端 `conversationImport/source/scan(projectPath)` 此前只做 exact，真实 Codex 会话如果 cwd 落在项目子目录或用户输入相对路径片段，会被漏掉。
- `resolved`：新增 Codex importer project filter，保持现有协议不变，只扩展服务端筛选语义：exact 命中当前项目、child-prefix 命中项目子目录、path-segment contains 命中路径片段；同时避免 `/workspace/lime` 误匹配 `/workspace/lime-old` 这类同前缀兄弟目录。
- `guarded`：`fidelity-acceptance-matrix.md` 新增 `project path filtering` current 行，矩阵守卫绑定 `scans_codex_state_db_project_path_exact_prefix_and_contains`，防止后续退回 exact-only。
- 验证入口：`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import::tests::path_resolution::scans_codex_state_db_project_path_exact_prefix_and_contains --lib`；`npx vitest run "scripts/electron/codex-import-fidelity-acceptance-matrix.test.mjs" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`。

## 2026-06-19 进度记录：归档会话导入筛选对齐 FR-07

- `resolved`：前端导入弹窗和项目侧栏异步探测不再固定 `includeArchived=false`；`conversationImport/source/scan` 现在默认以 `includeArchived=true` 读取 active + archived 本地历史，避免只存在归档会话的项目隐藏导入入口。
- `resolved`：导入弹窗新增全部 / 未归档 / 已归档筛选，默认全部，不再默认隐藏 archived sessions；归档线程以中性状态标签展示，仍走同一 preview / commit 主链，不新增第二套导入模型。
- `guarded`：`conversationImportDialogViewModel` 增加归档筛选纯函数回归；`AppSidebar.conversations.test.tsx` 覆盖默认 scan 参数、归档筛选 UI 和项目探测参数；新增文案已覆盖 `zh-CN / zh-TW / en-US / ja-JP / ko-KR`。
- `refactor`：导入弹窗左侧 source root、分组、归档筛选和线程列表已拆到 `AppSidebarConversationImportThreadList.tsx`；主弹窗降到 706 行，低于 800 行预警线，后续批量筛选不再继续膨胀弹窗主体。
- 已通过验证：`npx vitest run "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/AppSidebar.conversations.test.tsx" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，55 tests passed；`npm run i18n:check:json`，五语言 coverage 100%、missing=0、extra=0；定向 ESLint 与相关文件 `git diff --check` 通过。

## 2026-06-19 进度记录：真实大样本完整记录下钻 E2E 收口

- `resolved`：增强 `scripts/electron/local-history-import-real-sample-visual-audit-smoke.mjs` 的完整记录下钻诊断。失败时不再只留下 `runtimeDetailDrilldown=null`，会把 `conversationImport/thread/runtimeEvents/read` JSON-RPC probe、环境面板文本、toggle 状态、loading / error / empty、事件容器、事件 kind、payload 预览和最近按钮文本写入 summary/raw evidence，并把同一快照带入断言错误消息。
- `verified`：真实大样本导入 smoke 已通过。样本来自 `/Users/coso/Documents/dev/ai/limecloud/content-studio`，导入后 `agentSession/read` 为 `messagesLength=92`、`itemsLength=1901`，timeline 包含 `agent_message=1695`、`command_execution=80`、`plan=40`、`patch=27`、`context_compaction=24`、`tool_call=35`；`executionRuntime.sourceClient=codex`，且保留 imported thread settings / continuation。
- `verified`：完整记录下钻当前通过真实 Electron GUI 渲染验证：App Server probe 返回 `totalEvents=19419`、`sourceRuntimeEvents=19419`、`materializedRuntimeEvents=2343`、`sidecarRuntimeEvents=17076`，首批事件类型包含 `message.delta / tool.started / command.started`；GUI 下钻面板首屏分页渲染 `10` 张事件卡片，具备事实摘要、payload 预览、语义标题和来源规模摘要，无 source path / sourceThreadId / raw 字段泄漏，`consoleErrors=[]`。
- 已通过验证：`npx vitest run "scripts/electron/local-history-import-real-sample-visual-audit-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，4 tests passed；`npm run smoke:local-history-import-real-sample-visual-audit -- --timeout-ms 300000 --commit-timeout-ms 240000` 通过，summary `.lime/qc/gui-evidence/local-history-import-real-sample-visual-audit/local-history-import-real-sample-visual-audit-summary.json` 为 `ok=true`；`npx eslint "scripts/electron/local-history-import-real-sample-visual-audit-smoke.mjs" "scripts/electron/local-history-import-real-sample-visual-audit-smoke.test.mjs" --max-warnings 0` 通过；相关文件 `git diff --check` 通过。Rust `runtime::conversation_import::tests::runtime_events::` 全组本轮曾因外部 `cargo test --manifest-path lime-rs/Cargo.toml` 持有 artifact lock 未完成，不作为通过证据；关键 runtime event 投影定向用例已单独通过。

## 2026-06-19 进度记录：已导入会话重导入清理收口

- `resolved`：`replaceExisting=true` 的重导入清理从内存 session、projection store、event log 扩展到 session-scoped sidecar。导入完整记录写在 `runtime/sidecar/sessions/<session>/conversation-import/runtime-events.jsonl`，旧 session 被清理后该目录也会删除，避免“清理并重新导入”留下不可见 orphan 完整记录。
- `verified`：`SidecarStore::clear_session` 支持幂等清理，不存在时返回成功；重导入 idempotency 回归现在断言旧 projection、旧 event log、旧 sidecar 均消失，新 session projection 与 sidecar 均可读。
- 已通过验证：`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::sidecar_store::tests::clear_session_removes_session_scoped_sidecars -- --nocapture`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import::tests::idempotency::committing_same_codex_thread_with_replace_existing_reimports_source -- --nocapture`；相关文件 `git diff --check` 通过。

## 2026-06-19 进度记录：用户级删除 / 保留策略 current 收口

- `resolved`：侧栏删除会话已从 `agentSession/update archived=true` 的归档 / 隐藏语义切到 App Server JSON-RPC `agentSession/delete` current 主链。`RuntimeCore::delete_agent_session` 会 trim `sessionId`，清理内存 session、`ProjectionStore`、`EventLogWriter` 和 session-scoped `SidecarStore`；不存在时返回 `SESSION_NOT_FOUND`。该删除只作用于 Lime 持久化出的 session 数据，不删除外部来源目录。
- `guarded`：归档 / 恢复仍走 `agentSession/update`，批量归档仍走 `agentSession/archiveMany`；删除只能走 typed `deleteSession` / `agentSession/delete`，不得回退通用 request、legacy bridge 或旧归档投影。协议 schema fixture、generated TS、npm client、前端 thin gateway 和 contract guard 已同步。
- 已通过验证：`cargo run --manifest-path "lime-rs/Cargo.toml" -p app-server-protocol --bin write_schema_fixtures`；`npm run generate:protocol-types`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server-protocol`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server persisted_session_delete_clears_projection_and_event_log --test session_archive_jsonrpc`；`npx vitest run "src/lib/api/agentRuntime/sessionClient.current-boundary.test.ts" "src/lib/api/agentRuntime/appServerSessionClient.test.ts" "src/lib/api/agentRuntime/clientFactory.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`；`npm --prefix "packages/app-server-client" test`；`npm run check:protocol-types`；`npm run test:contracts`；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --all -- --check`；相关文件 `git diff --check` 通过。
- 后续入口：导出后删除、保留期限、删除前导出确认、跨来源保留策略仍是独立产品规则；上线前必须补确认范围、i18n 文案、UI 回归和对应存储清理守卫。

## 2026-06-19 进度记录：review 退出事件验收口径收口

- `resolved`：对照 `/Users/coso/Documents/dev/rust/codex` 的 `TurnItem` / `ThreadItem` review 语义，`exited_review_mode` 不应只停留在完整记录侧链；它承载 review 退出后的用户可见评审文本，需要明确验收为导入 `agent_message`。
- `guarded`：`fidelity-acceptance-matrix.md` 的 context / review / subagent / collab 行已补 `exited_review_mode -> agent_message`，矩阵守卫同步检查 Rust fixture token，防止后续只覆盖 `entered_review_mode` reasoning 而漏掉 review 退出文本。
- 已通过验证：`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import::tests::runtime_events::commit_projects_codex_runtime_specialized_items_into_existing_timeline_types --lib`；`npx vitest run "scripts/electron/codex-import-fidelity-acceptance-matrix.test.mjs" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`；相关文件 `git diff --check` 通过。

## 2026-06-19 进度记录：导入弹窗 warning 与完整记录 payload i18n 收口

- `resolved`：导入预览不再直接渲染 App Server 返回的英文 warning 字符串。`AppSidebarConversationImportDialog` 改为通过 `resolveImportWarningText` 把后端诊断事实映射到 `navigation.sidebar.importDialog.warnings.*` 展示文案，覆盖 provenance-only preview / commit、高容量运行记录和 generic fallback。后端协议字段和诊断字符串继续作为 stable facts，不进入 locale 判断。
- `resolved`：完整记录下钻里的 scalar payload 类型不再直接展示 JS 类型名 `string/number/boolean`；`ImportedRuntimeEventDetailPanel` 新增 `generalWorkbench.taskRail.importedRuntime.payload.type.*` 展示映射，五语言覆盖文本、数字、布尔值、大整数、符号、函数和值 fallback。
- `resolved`：`ImportedRuntimeEventDetailPanel` 不再只依赖上层传入 `t`；组件自身通过 `useTranslation("agent")` 读取当前 locale，上层传入 `t` 时仍可覆盖。这样后续新增入口直接渲染完整记录面板，也不会默认掉回中文 fallback。
- `verified`：新增五语言资源覆盖断言，防止 warning / payload type 新 key 只补中文或英文；新增组件回归，断言英文环境下字符串 payload 摘要显示 `Text · 18` 而不是 `string · 18`，且未传上层 `t` 时仍使用组件内 `agent` namespace 翻译，不回退到中文 `defaultValue`。
- 已通过验证：`npx vitest run "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/agent/chat/components/importedRuntimeEventDetailViewModel.test.ts" "src/components/agent/chat/components/ImportedRuntimeEventDetailPanel.test.tsx" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，21 tests passed；`npm run detect-translations -- --verbose` 五语言 coverage 100%、missing=0、extra=0；`npm run i18n:scan -- "src/components/app-sidebar/AppSidebarConversationImportDialog.tsx" "src/components/app-sidebar/conversationImportDialogViewModel.ts" "src/components/agent/chat/components/ImportedRuntimeEventDetailPanel.tsx" "src/components/agent/chat/components/generalWorkbenchTaskRailText.ts"` 通过，4 files / 0 findings；`npx eslint "src/components/agent/chat/components/ImportedRuntimeEventDetailPanel.tsx" "src/components/agent/chat/components/ImportedRuntimeEventDetailPanel.test.tsx" "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/agent/chat/components/importedRuntimeEventDetailViewModel.test.ts" --max-warnings 0` 通过；相关文件 `git diff --check` 通过。

## 2026-06-19 进度记录：导入 timeline 摘要 i18n 收口

- `resolved`：`AgentThreadTimeline` 现在把 `agent` namespace 的翻译函数传入 `buildAgentThreadDisplayModel`，导入过程块的标题、计数、思考 / 命令 / 搜索 / 文件变更摘要和展开标签不再只能使用中文 fallback。
- `resolved`：新增 `generalWorkbench.taskRail.importedProcess.*` 五语言资源，覆盖 `zh-CN / zh-TW / en-US / ja-JP / ko-KR`。协议字段、source client、thread id 和 provenance 继续保持 stable facts，不进入 presentation 翻译层。
- `verified`：新增纯 view model 回归，断言导入过程摘要可接收调用方本地化文案；新增 `AgentThreadTimeline` 英文 locale 渲染回归，断言真实组件在 `en-US` 下显示英文导入过程摘要，且不混入中文“导入的命令记录 / 搜索记录”。
- 已通过验证：`npm run detect-translations -- --verbose`，五语言覆盖 100%、missing=0、extra=0；`npx vitest run "src/components/agent/chat/utils/agentThreadGrouping.test.ts" "src/components/agent/chat/components/AgentThreadTimeline.process.test.tsx" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，32 tests passed；`npx eslint "src/components/agent/chat/utils/agentThreadGrouping.ts" "src/components/agent/chat/utils/agentThreadGrouping.test.ts" "src/components/agent/chat/components/AgentThreadTimeline.tsx" "src/components/agent/chat/components/AgentThreadTimeline.process.test.tsx"` 通过；相关文件 `git diff --check` 通过。

## 2026-06-18 进度记录：导入来源硬编码与过程组文案收口

- `resolved`：复核用户截图对应的导入 / timeline 展示链路后，确认 `sourceClient="codex"` 作为导入协议枚举和 fixture fact 可以保留，但普通 GUI 展示不应把来源品牌或中文摘要硬编码在组件逻辑里。Context7 / WebSearch 对 `react-i18next` 的结论与 AG-UI 参考一致：事件类型、角色、工具调用和 source id 是 stable protocol facts；human-readable copy 应由 UI 配置 / i18n 资源承接。
- `resolved`：新增 `src/components/agent/chat/utils/importedSourceMetadata.ts`，集中判断 `imported / imported_synthetic / importedSynthetic / source_client / sourceClient` 导入来源信号；`StreamingProcessGroup`、`ToolCallDisplayViewModel` 和 `toolDisplayInfo` 不再各自写死 `source_client === "codex"`。这为后续 Claude Code 导入复用同一只读命令记录 / 工具批次展示打基础，同时不改变当前 Codex 导入协议事实。
- `resolved`：`StreamingProcessGroup` 的过程组摘要不再直接拼中文“已完成思考 / 导入过程 N 个步骤 / N 个工具调用”等展示文案，改为 `agentChat.processGroup.*` 五语言 i18n key；组件只处理状态、数量和分组，不负责 presentation copy。
- `verified`：新增回归覆盖 `source_client="claude_code"` 的导入命令记录，断言只读命令记录可见、原始命令 / 来源枚举不泄漏、命令摘要和 stdout/stderr 不渲染；同时保持 Codex 导入路径现有断言。
- 已通过验证：`npx vitest run "src/components/agent/chat/components/StreamingRenderer.test.tsx" "src/components/agent/chat/components/ToolCallDisplayViewModel.unit.test.ts" "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/i18n/__tests__/loadNamespace.test.ts" "src/i18n/__tests__/types.test.ts" --silent=passed-only --disableConsoleIntercept`，88 tests passed。
- 已通过真实 Electron GUI 审计：`npm run smoke:local-history-import-visual-audit -- --timeout-ms 180000`，summary `.lime/qc/gui-evidence/local-history-import-visual-audit/local-history-import-visual-audit-summary.json` 为 `ok=true`、`visibleTextLeaks=[]`；desktop / compact / narrow 三视口均确认输入框可用、消息列表可见、命令记录 / patch / 搜索 / 审批记录可见，且 `hidesRawImportedCommand=true`、`hidesSourceBrandText=true`。

## 2026-06-18 进度记录：工具过程展示 i18n 收口

- `resolved`：`InlineToolProcessStep` 的用户可见过程文案已从组件直写中文迁到 `agentChat.toolCall.inline.*` / `agentChat.toolCall.siteResult.*` 五语言资源，覆盖工具搜索预览、搜索结果数量、搜索诊断、实时输出、进度、系统预执行 badge、技能 badge、展开 / 收起详情、站点保存提示和保存内容入口。
- `resolved`：`ToolSearchSummaryPanel` 展开态的“找到工具 / 查询 / MCP 服务仍在连接 / 无更多匹配 / 可直接执行”等文案已迁到 `agentChat.toolCall.toolSearch.*` 五语言资源；组件只负责过滤协议噪音和渲染结构，不再承载展示文案。
- `resolved`：`toolProcessSummary` 中站点保存折叠摘要改为复用同一组 i18n key，避免英文 / 日文等界面里混入“已保存到当前项目”等中文折叠摘要。站点 target fallback 新增 `agentChat.toolCall.siteResult.target.generic`，五语言覆盖。
- `resolved`：`toolProcessSummary` 高频折叠摘要继续收口到 `agentChat.toolCall.processSummary.*` 五语言资源，覆盖 ToolSearch 预检 / 完成摘要、WebSearch 来源计数、Fetch/Search 弱失败提示、命令预判摘要和浏览器操作 pre/post 摘要。判断条件、工具 family 和状态机保持不变，只把 presentation copy 从中心文件移到资源层。
- `resolved`：`toolProcessSummary` 的任务 / 技能 / MCP / 计划 / 工作树 / workflow / 等待 / 子任务 / 团队 / 定时触发 / 远程触发 / 服务技能兼容路径的 pre/post 摘要已迁到 `agentChat.toolCall.processSummary.*` 五语言资源；`InlineToolProcessStep` 新增英文回归覆盖 Skill、TaskCreate、ListMcpResources、McpAuth 与 `update_plan`，避免英文界面回退到中文或 key 文本。
- `resolved`：`toolProcessSummary` 的 generic fallback、vision、站点能力和 runtime protocol error 展示文案继续迁入五语言资源，覆盖 read/list/write/edit/fetch/search/plan/default、图片查看 / 分析、站点能力 list/recommend/search/info/run，以及 “执行失败 / 原始错误 / WebSearch 未接通 / runtime 无详情” 等错误包装。源码中剩余中文命中只保留为历史错误识别规则和 content workbench 已有 i18n 资源兜底 defaultValue，不再是普通 GUI 展示事实源。
- `verified`：新增 `InlineToolProcessStep.test.tsx` 英文切换回归，断言实时输出、系统预执行、技能 badge、站点保存摘要和站点按钮随 `en-US` 切换，且不再出现对应中文硬编码。
- `verified`：补充 `InlineToolProcessStep.test.tsx` 英文切换回归，覆盖 ToolSearch / WebFetch / Bash / browser navigate 高频摘要，防止后续重新在工具过程卡折叠态写死中文。
- `verified`：补充 `toolProcessSummary.test.ts` / `InlineToolProcessStep.test.tsx` 定向验证，`npx vitest run "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" "src/components/agent/chat/utils/toolProcessSummary.test.ts" --silent=passed-only --disableConsoleIntercept` 通过，47 tests passed；`npx eslint "src/components/agent/chat/utils/toolProcessSummary.ts" "src/components/agent/chat/utils/toolProcessSummary.test.ts" "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" --max-warnings 0` 通过；`npm run detect-translations -- --verbose` 五语言覆盖 100%、missing=0、extra=0。
- `verified`：本轮复跑 `npx vitest run "src/components/agent/chat/utils/toolProcessSummary.test.ts" "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" --silent=passed-only --disableConsoleIntercept`，48 tests passed；`npm run detect-translations -- --verbose` 五语言覆盖 100%、missing=0、extra=0；`npx eslint "src/components/agent/chat/utils/toolProcessSummary.ts" "src/components/agent/chat/utils/toolProcessSummary.test.ts" "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" --max-warnings 0` 通过；相关文件 `git diff --check` 通过。
- `resolved`：`toolProcessSummary.ts` 的普通工具过程展示中文兜底已收口。`resolveContentWorkbenchToolCopy` 改为 required-resource 语义，内容任务标签、工具组标题、任务失败提示和过程摘要统一从 `agentChat.contentWorkbenchTools.*` 五语言资源读取；`summary.direct/task.*` 调用不再携带中文 defaultValue。源码剩余中文只用于识别历史 WebSearch / JSON-RPC 错误文本与剥离错误前缀，不作为 GUI 展示文案。
- `verified`：已补英文 locale 回归，覆盖配音任务、素材检索任务、封面图直接生成，断言过程摘要切到英文且不回退“先发起 / 已发起 / 先生成 / 已生成 / 配音生成 / 素材检索 / 封面图”等中文兜底。验证已通过 `npx vitest run "src/components/agent/chat/utils/toolProcessSummary.test.ts" "src/components/agent/chat/utils/toolDisplayInfo.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，41 tests passed；`npx eslint "src/components/agent/chat/utils/contentWorkbenchToolCopy.ts" "src/components/agent/chat/utils/toolProcessSummary.ts" "src/components/agent/chat/utils/toolProcessSummary.test.ts" --max-warnings 0`；`npm run i18n:unused -- --check`；`npm run i18n:check:json`；`npm run typecheck -- --pretty false`。
- `guarded`：导入 fidelity 矩阵守卫复跑通过：`npx vitest run "scripts/electron/codex-import-fidelity-acceptance-matrix.test.mjs" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`，6 tests passed，确认本轮 i18n / 工具展示收口没有把 Codex-first 导入验收矩阵漂成散文记录。
- 已通过验证：`npx vitest run "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" --silent=passed-only --disableConsoleIntercept`，25 tests passed；`npx eslint "src/components/agent/chat/components/InlineToolProcessStep.tsx" "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" "src/components/agent/chat/components/ToolSearchSummaryPanel.tsx" "src/components/agent/chat/utils/toolProcessSummary.ts" --max-warnings 0` 通过；`npx eslint "src/components/agent/chat/utils/toolProcessSummary.ts" "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" --max-warnings 0` 通过；`npm run detect-translations -- --verbose` 五语言覆盖 100%、missing=0、extra=0；相关文件 `git diff --check` 通过。

## 2026-06-18 进度记录：工具批次标题 hard code 继续收口

- `resolved`：`buildToolGroupHeadline` 的批次标题已从组件内中文拼接切到 `agentChat.toolCall.group.*` i18n key，`agentChatCopy` 负责在 i18n 已初始化和未初始化两种情况下都读取 `agent` namespace 的五语言资源。AG-UI 官方文档和本地 `ag-ui` 参考都把 tool call / reasoning / state 视为协议事件事实，前端只应负责投影和展示，不应把协议名或批次文案当成 UI 逻辑常量。
- `resolved`：`agentChat.toolCall.importedCommandRecord.groupTitle` 也已进入 i18n 资源，导入来源命令记录不再依赖组件里的中文字面量。当前截图处如果看到中文，来源应是资源层而不是流程逻辑层。
- `resolved`：`TOOL_STATUS_ACTIONS` 与 `USER_FACING_TOOL_LABELS` 已从 `toolDisplayInfo.ts` 拆到 `toolDisplayCopy.ts`，并继续迁到 `agentChat.toolCall.action.*` / `agentChat.toolCall.userFacing.*` 五语言 i18n key。`toolDisplayCopy.ts` 只保留协议/展示标签到 key 的归一化映射，展示文本由 `agentRuntime.json` 承接，避免把中文从一个大文件搬到另一个大文件。
- `resolved`：`EXACT_TOOL_CONFIGS` / MCP operation configs / browser matcher configs 已从 `toolDisplayInfo.ts` 拆到 `toolDisplayConfig.ts`，`toolDisplayInfo.ts` 降到约 501 行，只负责 display descriptor 和 group headline 投影；工具名 normalization、参数解析、主体提取和 search label 提取拆到 `toolDisplaySubject.ts`，并由 `toolDisplayInfo.ts` re-export 旧入口，避免破坏现有调用方。
- `resolved`：工具展示输出层新增 `agentChat.toolCall.label.*` / `groupTitle.*` / `verb.*` / `actionOverride.*` / `subject.*` 五语言资源。`toToolDisplayDescriptor` 现在对 label、groupTitle、verb、custom action 和 subject fallback 都走 i18n resolver；协议字段、工具名、source metadata 和 stable enum 不本地化，继续只作为投影输入。
- `resolved`：`toolDisplayConfig.ts` 已拆为聚合入口与 domain 子模块：`toolDisplayConfig/core.ts` 承接基础文件 / MCP auth / search / command / plan / skill / worktree / vision 等配置，`content.ts` 承接内容生成与子任务 / 触发器 / 服务技能兼容入口，`site.ts` 承接站点能力，`mcp.ts` 承接 MCP operation config，`browser.ts` 承接浏览器 matcher，`fallback.ts` 承接启发式 fallback config。拆分后最大文件为 `core.ts` 756 行，低于 800 行预警线；`toolDisplayConfig.ts` 保持旧聚合导入路径，避免破坏调用方。
- `verified`：`src/components/agent/chat/utils/toolDisplayInfo.test.ts` 已补英日韩的资源覆盖与未初始化 i18n fallback 回归；后续若继续收口 hard code，应优先迁移这三张大表的静态字面量，而不是再往 `toolDisplayInfo.ts` 里加新文案。
- `verified`：本轮复跑 `npx vitest run "src/components/agent/chat/utils/agentChatCopy.test.ts" "src/components/agent/chat/utils/toolDisplayCopy.test.ts" "src/components/agent/chat/utils/toolDisplayInfo.test.ts" --silent=passed-only --disableConsoleIntercept`，26 tests passed；`npx eslint "src/components/agent/chat/utils/toolDisplayCopy.ts" "src/components/agent/chat/utils/toolDisplayCopy.test.ts" "src/components/agent/chat/utils/toolDisplayConfig.ts" "src/components/agent/chat/utils/toolDisplayConfig/core.ts" "src/components/agent/chat/utils/toolDisplayConfig/content.ts" "src/components/agent/chat/utils/toolDisplayConfig/site.ts" "src/components/agent/chat/utils/toolDisplayConfig/mcp.ts" "src/components/agent/chat/utils/toolDisplayConfig/browser.ts" "src/components/agent/chat/utils/toolDisplayConfig/fallback.ts" "src/components/agent/chat/utils/toolDisplayInfo.ts" "src/components/agent/chat/utils/toolDisplayInfo.test.ts" "src/components/agent/chat/utils/toolDisplaySubject.ts" "src/components/agent/chat/utils/toolDisplayTypes.ts" --max-warnings 0` 通过；`npm run detect-translations -- --verbose` 五语言 100% 覆盖；相关文件 `git diff --check` 通过。

## 2026-06-18 进度记录：图片附件重复展示收口

- `resolved`：修复用户图片消息里正文、broken image alt 与附件说明重复展示的问题。对齐 `react-markdown` 与 AG-UI 多模态消息思路：图片失败态由结构化附件 / markdown `img` 组件接管，正文清洗只消除 markdown 图片 alt 的旁路回显，以及结构化图片消息里的纯 `[Image #n]` 兼容占位；不再按本地化错误词或平台品牌名解析正文。
- `resolved`：图片附件 `<img>` 加载失败后不再让浏览器直接显示 alt 文本；改为一处受控短占位“图片暂时无法显示”，点击预览入口仍保留，结构化 `previewUrl/sourceUri/sourcePath/data/metadata` 不丢失。
- `resolved`：Markdown 正文图片加载失败后同样切到受控占位，不再在正文里显示 `image` / alt 文本；base64 图片仍保留精简 caption 和点击预览能力。
- `resolved`：复核并清理图片展示硬编码：base64 markdown 图片默认 alt 从固定英文 `Generated Image` 改为五语言 i18n key；去重回归测试不再使用平台品牌文本作为样例，生产源码中不保留来源品牌或特定错误词匹配。后续只能按 markdown image 结构、结构化 attachments 与 `[Image #n]` 兼容占位判断，不得重新按本地化错误句子做清洗。
- `verified`：2026-06-18 复查 `src/components/agent/chat` / `src/lib` / `src/i18n/resources` / 本路线图，生产代码不再命中 `Generated Image`、来源品牌平台名或旧图片不可达诊断句；命中项只剩 i18n presentation、测试断言和本 tracker 记录。
- `verified`：2026-06-18 复跑 `npm run smoke:local-history-import-visual-audit -- --timeout-ms 180000` 通过，summary `.lime/qc/gui-evidence/local-history-import-visual-audit/local-history-import-visual-audit-summary.json`：`ok=true`、三视口 `inputbarVisible=true` / `messageListVisible=true` / `hasCommandRecordVisible=true` / `hasPatchText=true` / `hasSearchEvidence=true` / `hasApprovalText=true`、`visibleTextLeaks=[]`、`hidesSourceBrandText=true`。
- 当前已通过：`npx vitest run "src/components/agent/chat/components/MessageImageAttachments.test.tsx" "src/components/agent/chat/components/MarkdownRenderer.test.tsx" "src/components/agent/chat/utils/messageDisplaySanitizer.test.ts" "src/components/agent/chat/components/messageListItemProjection.unit.test.ts" "src/components/agent/chat/components/MessageList.test.tsx" --silent=passed-only --disableConsoleIntercept`；`npx vitest run "src/i18n/__tests__/loadNamespace.test.ts" "src/i18n/__tests__/types.test.ts" --silent=passed-only --disableConsoleIntercept`；`npm run smoke:agent-runtime-current-fixture`（覆盖 history/cache hydration、final_done 工具收尾、failed read model、Claw 终态 UI、真实 Electron Coding Workbench fixture、Claw GUI current fixture guard、停止后同会话继续输出，`liveProviderUsed=false`）；`npm run smoke:local-history-import-visual-audit -- --timeout-ms 180000`；`git diff --check`。`npm run typecheck` 此前复现 TypeScript 5.9.3 内部崩溃 `Debug Failure. No error for last overload signature`，未输出源码位置；本轮仍不把全仓 typecheck 当作通过证据。

## 2026-06-18 进度记录：导入协议枚举事实源单一化

- `resolved`：`ConversationImportSourceClient` / `ConversationImportSourceStatus` / `ConversationImportThreadStatus` 的运行时白名单现在由 `packages/app-server-client/src/protocol.ts` 统一导出，`src/lib/api/conversationImport.ts` 不再手写 `Set(["codex", "claude_code"])` 之类重复常量。协议事实继续保持为枚举，UI 展示继续由 `conversationImportDialogViewModel.ts` 承接本地化映射，不把协议值直接翻译成文案。
- `resolved`：导入 API 的 shape guard 改为显式类型守卫 `isConversationImportSourceClient / isConversationImportSourceStatus / isConversationImportThreadStatus`，与 app-server-protocol 的生成类型同向收敛；这样既保留 fail-closed，又减少前后端白名单漂移。
- `verified`：`npx vitest run "src/lib/api/conversationImport.test.ts" "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" --silent=passed-only --disableConsoleIntercept` 通过，覆盖 source client 协议枚举、`claude_code` 扫描响应和导入侧栏 label 映射。

## 2026-06-18 进度记录：真实 content-studio 大样本收口

- `resolved`：真实 `/Users/coso/Documents/dev/ai/limecloud/content-studio` 大样本导入已从 commit 超时收口到可重复通过。App Server 写入链路改为批量 event log append + projection transaction：`ProjectionStore::apply_events`、`EventLogWriter::append_events` 与 `append_runtime_events_to_stored_session` 不再逐事件重复打开高成本投影事务，完整 normalized runtime events 仍进入 sidecar / provenance，默认 read model 只物化可承载窗口。
- `resolved`：前端“查看完整记录”入口不再只依赖当前窗口已物化 item；`hasImportedRuntimeDetailSignal` 同时读取 `threadItems`、`executionRuntime` 与 `threadRead.diagnostics/runtime_summary`，支持 `source_client/sourceClient`、imported continuation/settings/projection 信号，避免大样本默认窗口外事件导致入口消失。
- `resolved`：真实 E2E 脚本已改成等待完整记录入口异步 hydration，完成下钻断言后关闭弹窗，再执行主会话多视口审计；滚动截图阶段不再把 textarea 当前可见当成目标会话判断，改由目标标题、导入命令记录和 `openSnapshot` 共同证明当前会话与输入框绑定。
- 最终真实 E2E 已通过：`APP_SERVER_BIN="/tmp/lime-app-server-projection-target/debug/app-server" npm run smoke:local-history-import-real-sample-visual-audit -- --timeout-ms 300000 --commit-timeout-ms 240000`。证据文件 `.lime/qc/gui-evidence/local-history-import-real-sample-visual-audit/local-history-import-real-sample-visual-audit-summary.json` 中 `ok=true`、`consoleErrors=[]`；source thread `019ebca5-b873-7ba0-8a3d-05444bbbfd7b`，read model 为 92 messages、1901 items，其中 `command_execution=80`、`tool_call=35`、`patch=27`、`plan=40`、`context_compaction=24`；完整记录下钻首屏展示 10 个事件，probe 仍保留 `totalEvents/sourceRuntimeEvents/materializedRuntimeEvents/sidecarRuntimeEvents` 总量，包含 `message/tool/command` kind、facts、payload preview 与 source summary，未发现 source/raw 泄漏。
- 已通过的定向验证：`npx vitest run "src/components/agent/chat/components/TaskCenterUtilityToolbar.integration.test.tsx" "src/components/agent/chat/utils/sessionExecutionRuntime.test.ts" "src/components/agent/chat/workspace/importedConversationSourceSummary.unit.test.ts" --silent=passed-only --disableConsoleIntercept`；`npx vitest run "scripts/electron/local-history-import-real-sample-visual-audit-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`；`npm run build:renderer:electron:smoke`；相关 Rust 定向测试与 App Server build 已通过。

## 2026-06-18 进度记录：原生会话 timeline parity

- `resolved`：本轮修复的主对象是 Lime 原生会话恢复 / 刷新后的 conversation timeline，不是导入弹窗。对齐参考是 Codex `thread.turns -> turn.items -> transcript cells` 的消费原则：顺序事实源先归一，UI 分组层只消费稳定 timeline。
- 已新增 `src/components/agent/chat/utils/conversationTimelineOrdering.ts`，在 `hydrateSessionDetailMessages` 与 `mergeHydratedMessagesWithLocalState` 出口统一按 `runtimeTurnId` 投影为 `user -> assistant/process`。该投影只移动 `Message` 对象位置，不重建负载，保留 Lime 多模型 `usage/provider/runtime metadata` 与多模态 `images/attachments/artifacts/taskPreview/imageWorkbenchPreview/toolCalls/thinkingContent`。
- 已把 `messageTurnGrouping` 从“边遍历边回挂助手消息”降级为稳定 timeline 消费者，减少 UI 层继续猜测 turn 归属造成的错序。
- 已给 thread item / turn fallback 用户消息补 `runtimeTurnId`，让旧数据、导入数据和原生 read model 在同一个 turn 锚点上聚合。
- 验证已通过：`npx vitest run "src/components/agent/chat/utils/conversationTimelineOrdering.test.ts" "src/components/agent/chat/utils/messageTurnGrouping.test.ts" "src/components/agent/chat/hooks/agentChatHistory.test.ts" --silent=passed-only --disableConsoleIntercept`；`npm run smoke:agent-runtime-current-fixture`。后者覆盖 history/cache hydration、Claw 终态 UI、Electron coding workbench fixture 和停止后同会话继续输出。
- 剩余缺口：仍需继续对真实 live provider 失败链路单独排查；本轮只治理会话显示顺序和投影归属，不宣称 provider/runtime 错误已全部解决。

## 2026-06-18 进度记录：真实 Electron E2E 收口

- `resolved`：已复跑真实 Electron 会话历史 fixture，覆盖 `agentSession/start/read/update/list`、持久化归档 / 反归档、侧栏 GUI 归档、设置页 GUI 恢复和重启读回。当前证据文件为 `.lime/qc/gui-evidence/agent-session-history-electron-fixture/agent-session-history-electron-fixture-summary.json`，`ok=true`，`consoleErrors=0`。
- `resolved`：已复跑真实 Electron Claw current fixture，覆盖 GUI 输入框发送、`Frontend -> Electron preload bridge -> app_server_handle_json_lines -> App Server JSON-RPC -> RuntimeCore/backend`、完成态 UI 和 read model 完成。当前证据文件为 `.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`，`ok=true`，`consoleErrors=0`。
- 已修复历史 fixture 的设置入口定位：不再固定依赖 `button[aria-label="设置"]`，改为在账号菜单内按文本、aria 和 title 匹配设置入口，避免不同语言 / 文案下 E2E 假失败。
- `resolved`：`npm run verify:gui-smoke` 已在本轮完整通过，覆盖 renderer production build、Electron host build、`typecheck:electron`、App Server sidecar assets、Electron smoke 加载、App Server ready 和 Claw workbench shell ready。此前“renderer build / cargo lock 导致未作为通过证据”的记录已被本次复跑替换。

## 2026-06-18 进度记录：全量 GUI smoke / contract guard 收口

- `resolved`：全量 GUI smoke 已通过：`npm run verify:gui-smoke`。关键输出包括 renderer build `✓ built in 6m 52s`、Electron main / preload SSR bundle build 通过、App Server sidecar `protocol=appserver.v0 version=1.72.0` ready、`[electron-smoke] renderer loaded` 与 `[electron-smoke] claw workbench shell ready`。
- `resolved`：协议和治理合同已通过：`npm run test:contracts`。覆盖 `check:protocol-types`、`check-app-server-client-contract`、`check-command-contracts`、`check-harness-contracts`、`governance:modality-contracts`、`governance:scripts`、`governance:electron-release-workflow`、`harness:cleanup-report:check` 与 `docs:boundary`；其中 command contracts 显示 frontend commands 30、Electron host commands 90、mock priority commands 0、DevBridge truth commands 18。
- `resolved`：`npm run typecheck -- --pretty false` 已在 2026-06-19 当前工作树内通过，不再作为本地历史导入主线的 deferred 类型门禁缺口；此前长跑无输出记录保留为历史环境问题。复核同时覆盖 `npm run test:contracts`、`npm run i18n:unused -- --check`、`npm run i18n:check:json`、i18n loader / types / unused-key 检测器定向 Vitest 和全仓 `git diff --check`。

## 2026-06-18 进度记录：导入文件预览 click-through 扩展

- `resolved`：click-through fixture 的临时本地历史源已从 Markdown / HTML / DOCX / PDF 扩展到 Markdown / HTML / DOCX / XLSX / PPTX / PDF。XLSX / PPTX 使用最小 OpenXML ZIP 结构，可解析 PDF 使用最小 PDF 文本流；全部沿真实 `read_file` 工具轨进入 `inline-tool-open-file`，由 Workspace 打开统一 Preview Artifact Contract。
- `resolved`：真实 Electron 点击闭环已覆盖 `imported-preview.md/html/docx/xlsx/pptx/pdf`。Markdown、DOCX、XLSX、PPTX、可解析 PDF 均进入 Artifact Workbench 文档预览；HTML 进入 iframe / 独立预览；Office 预览断言不暴露 `xl/worksheets/sheet1.xml`、`ppt/slides/slide1.xml`、`word/document.xml` 等 ZIP/OpenXML 噪音。抽取为空或不支持的二进制 fallback 仍由 `previewArtifact.test.ts` 与 `ArtifactRenderer.ui.test.tsx` 覆盖，不把 GUI click-through 绑定到系统打开权限。
- 当前已通过：`npm run smoke:codex-import-click-through-electron-fixture -- --timeout-ms 180000`，summary `.lime/qc/gui-evidence/codex-import-click-through-fixture/codex-import-click-through-fixture-summary.json` 中 `ok=true`、`consoleErrors=[]`；`npx vitest run "scripts/electron/codex-import-click-through-fixture-smoke.test.mjs" "src/lib/artifact/previewArtifact.test.ts" "src/components/artifact/ArtifactRenderer.ui.test.tsx" "src/components/agent/chat/workspace/useWorkspaceArtifactPreviewActions.test.tsx" --silent=passed-only --disableConsoleIntercept`；相关 `git diff --check`。

## 问题清单

| ID     | 问题                                                                                                                                                                                                                                                                                                                          | 决策 / 处理方式                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 状态     | 验证入口                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CI-001 | 只读扫描和预览已经可用，但缺少确认后的真正导入写入。                                                                                                                                                                                                                                                                          | 已新增 `conversationImport/thread/commit`，写入前强制 `confirmed=true`；UI 弹窗后续只负责拿到用户确认，后端仍 fail closed。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | resolved | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import --lib`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| CI-002 | `conversation_import/mod.rs` 已接近 1000 行，继续追加写入逻辑会违反模块体量边界。                                                                                                                                                                                                                                             | 已新增 `conversation_import/commit.rs` 承接写入、`conversation_import/tests.rs` 承接测试，并把 Codex state DB / rollout parser 拆到 `conversation_import/codex.rs`；Codex 多模态附件映射继续拆到 `conversation_import/codex/media.rs`，`mod.rs` 只保留 source 分发和 Claude Code unsupported 处理。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | resolved | Rust fmt + 定向测试                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| CI-003 | Codex rollout 中工具调用、approval、patch、plan、subagent 等事件无法一次性完整映射到 Lime timeline。                                                                                                                                                                                                                          | 已对齐 Codex rollout raw event：`function_call/custom_tool_call/tool_search/web_search/mcp_tool_call_end` 映射为 Lime `tool.*`，`exec_command` 同步投影为 `command.*`，`patch_apply_end` 映射为 `patch.applied/failed`，approval request 作为只读 `action.required/action.resolved` 导入；高容量工具事件不再被写入层永久裁剪，commit 会保留 source fidelity，并把完整规范化 runtime event 写入 import sidecar / source provenance，默认 `agentSession/read.detail.items` 只物化可承载窗口，避免大 rollout 导入超时。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | resolved | `commit_preserves_codex_tool_command_and_patch_timeline`、`commit_preserves_high_volume_codex_tool_events_with_bounded_default_projection`、真实 content-studio smoke                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| CI-004 | Lime 必须导入后可继续对话，不能只是静态历史浏览。                                                                                                                                                                                                                                                                             | commit 写入 RuntimeCore `StoredSession` 和标准 `message.created` / `message.delta` / `turn.completed`，导入后继续走 `agentSession/turn/start`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | resolved | `agentSession/read` messages 断言                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| CI-005 | Codex `state_*.sqlite` 版本可能变化，当前只按 `state_5.sqlite`/最新 `state_*.sqlite` 与 `threads` 表字段读取。                                                                                                                                                                                                                | 对齐 Codex 当前 `state_5.sqlite` 与 thread inventory 字段；缺表或不可读时 fallback `session_index.jsonl`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | resolved | 扫描/预览 Rust 测试                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| CI-006 | Claude Code 需要后续导入，但不是当前架构主参考。                                                                                                                                                                                                                                                                              | importer contract 保留 `claude_code` source client；当前返回 unsupported，不污染 Codex-first 主线。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | deferred | scan unsupported 分支                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| CI-007 | 用户要求导入前确认弹窗。                                                                                                                                                                                                                                                                                                      | 已新增侧边栏 Codex 导入入口与确认弹窗：先 scan/preview，再由用户点击确认后调用 `conversationImport/thread/commit` 且强制传 `confirmed=true`；取消/关闭不提交 commit。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | resolved | `npx vitest run "src/components/AppSidebar.conversations.test.tsx" --silent=passed-only --disableConsoleIntercept`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| CI-008 | 多模态内容需要按 Codex 历史格式导入，不能只保留统计。                                                                                                                                                                                                                                                                         | 已对齐 Codex `event_msg.user_message.images/local_images` 与 `response_item.message.content[].type=input_image`：统一映射为 Lime `AgentAttachment(kind=image)`，保留 `sourceType/codexField/detail/localPath/mediaType` provenance；commit 写入 `AgentInput.attachments`，`agentSession/read` 把用户消息附件投影到 `content` 与 `attachments`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | resolved | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server conversation_import --lib`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| CI-009 | Lime 初版 parser 只把 `response_item` 当消息来源，不符合 Codex metadata 提取规则。                                                                                                                                                                                                                                            | 已对齐 Codex `state/src/extract.rs`：`event_msg.user_message` 是标题/首条用户消息主来源，并按 `USER_MESSAGE_BEGIN` 去前缀；`response_item` 只作为补充，role/text 全局去重。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | resolved | `previews_codex_event_messages_as_primary_codex_history`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| CI-010 | `AppSidebar.tsx` 已超过体量边界，`AppSidebarConversationShelf.tsx` 接近边界，继续塞导入状态机会恶化架构。                                                                                                                                                                                                                     | 本轮只在侧边栏做薄接线，导入流程封装到 `AppSidebarConversationImportDialog.tsx`，并抽出 `AppSidebarConversationEmptyState.tsx` 让 shelf 回到 1000 行内。后续应继续拆 `AppSidebar` 的账号、搜索、邀请、会话导入等 feature controller。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | deferred | 本轮 UI 回归；后续 sidebar split 执行计划                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| CI-011 | 需要用真实 Codex 项目会话验证导入主链，而不只依赖 fixture。                                                                                                                                                                                                                                                                   | 已将 `smoke:codex-import-content-studio` 升级为全量可导入线程 dogfood：通过 App Server sidecar JSON-RPC 只读扫描 `/Users/coso/.codex/state_5.sqlite`，按 `projectPath=/Users/coso/Documents/dev/ai/limecloud/content-studio` 找到 13 条真实可读 Codex 线程，先验证未确认 commit 被拒，再逐条 preview / confirmed commit / `agentSession/read` 校验消息、附件和 provenance；本机 Codex DB 另有 2 条 active stale row，因 rollout 文件不存在不展示为可导入项。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | resolved | `npm run smoke:codex-import-content-studio`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| CI-012 | Codex 当前 `state_5.sqlite` 已新增 `created_at_ms/updated_at_ms/model/reasoning_effort/git_* / cli_version / first_user_message / preview / thread_source`，旧读取会丢失多模型 provenance 且毫秒时间不精确。                                                                                                                  | 已改为列感知读取：优先使用 `created_at_ms/updated_at_ms`，`source` 保持 Codex session source，`threadSource/model/reasoningEffort/git/cli/preview` 进入 `ImportedThreadSummary.metadata`；commit 把这些信息写入 session business object 和 turn runtime metadata，旧 schema 仍兼容。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | resolved | `scans_current_codex_state_db_metadata_and_millisecond_timestamps`、真实 content-studio smoke                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| CI-013 | Codex state DB 可能存在 stale `rollout_path`、archive mismatch 或压缩 rollout，Lime 如果只信 DB path / 只读明文 JSONL 会在确认导入后失败。                                                                                                                                                                                    | 已按 Codex `rollout/src/list.rs` / `compression.rs` 的口径补齐路径解析和读取：优先使用可读 DB path；DB path 不存在时按 `sourceThreadId` 在对应 `sessions` / `archived_sessions` 目录中查找 rollout 文件；仍不可读的 state row 不进入 scan 可导入列表，preview / commit 保持 fail closed；`.jsonl.zst` 通过 zstd streaming decoder 透明读取，不需要 materialize 回明文文件。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | resolved | `scans_codex_state_db_repairs_stale_rollout_path_from_sessions`、`previews_archived_codex_thread_from_archived_sessions_when_db_path_is_stale`、`previews_and_commits_compressed_codex_rollout`、真实 content-studio smoke                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| CI-014 | PRD 要求同一 source thread 重复导入不重复创建内容；此前每次 confirmed commit 都会创建新 Lime session。                                                                                                                                                                                                                        | 已新增 `conversation_import/import_status.rs`：以内存 `RuntimeCore` 当前 session store 为优先幂等事实源，miss 后按 `ProjectionStore.projected_sessions.metadata_json` 查询 `conversation.import + sourceClient + sourceThreadId`；commit 命中既有导入 session 时返回同一 session，不追加 turns / events；scan / preview 在同进程和 App Server 重启后都会把已导入 thread 标为 `imported`。projection hydrate 会恢复导入会话的 `BusinessObjectRef(kind=conversation.import)`，确保重启后继续对话仍继承 cwd、模型、reasoning、approval、sandbox 和 memory provenance。                                                                                                                                                                                                                                                                                                                                                                                                                           | resolved | `committing_same_codex_thread_reuses_existing_imported_session`、`scan_and_preview_mark_previously_imported_codex_thread`、`committing_same_codex_thread_after_restart_reuses_projected_session`、真实 content-studio smoke 的 duplicate commit / rescan 断言                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| CI-015 | PRD 要求导入会话进入 `evidence/export` 主链并携带 source provenance；此前只验证了 `agentSession/read`。                                                                                                                                                                                                                       | 已新增 `conversation_import/tests/evidence.rs`，commit Codex rollout 后直接调用 RuntimeCore `export_evidence`，断言 `BusinessObjectRef`、session metadata、`message.created` session metadata 和 `message.delta.sourceClient` 均带 Codex provenance；这证明导入不是第二套 transcript store，而是进入 Lime current evidence/export 上下文。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | resolved | `imported_codex_thread_exports_evidence_with_source_provenance`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| CI-016 | `conversation_import/codex.rs` 与 `conversation_import/tests.rs` 再次越过 800 行预警线，继续追加逻辑会违反体量边界趋势。                                                                                                                                                                                                      | 已把 preview message merge 拆到 `codex/messages.rs`，把 dry-run impact projection 拆到 `codex/dry_run.rs`，新增 dry-run 测试落到 `tests/dry_run.rs`；`codex.rs` 已降到 800 行以下。`tests.rs` 仍是历史聚合入口，后续触碰基础 preview 测试时优先迁到 `tests/preview.rs`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | resolved | `wc -l lime-rs/crates/app-server/src/runtime/conversation_import/codex.rs lime-rs/crates/app-server/src/runtime/conversation_import/codex/dry_run.rs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| CI-017 | PRD FR-15 要求 dry-run impact summary，但原 preview 只有只读预览和 raw item count，无法确认 commit 会创建/复用 session、写入多少 Lime 投影消息、turn、附件和 timeline item。                                                                                                                                                  | 已扩展 `ConversationImportPreviewSummary.dryRun`，不新增 JSON-RPC method；preview/commit 统一返回 `willCreateSession / willAppendToExistingSession / willImportMessages / willImportTurns / willImportTimelineItems / willImportAttachments / unsupportedItems`。`willImportMessages` 采用 Lime commit 投影后的写入消息数，`messageCount` 保留 Codex raw message item 数；导入确认弹窗展示 dry-run 影响摘要，API shape guard 缺字段 fail closed。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | resolved | `preview_dry_run_summary_counts_full_timeline_beyond_preview_limit`、`imported_preview_dry_run_marks_append_to_existing_session`、`src/lib/api/conversationImport.test.ts`、真实 content-studio smoke                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| CI-018 | Codex App 级还原需要 item 级 source provenance，不能只在 session metadata 上写 `sourceThreadId`。                                                                                                                                                                                                                             | 已新增 `ConversationImportSourceProvenance` 并贯通 preview message/event、timeline runtime event payload、turn metadata 和 assistant `message.delta.sourceProvenance`。字段包含 source client、thread id、source path、rollout line seq、outer event type、payload type、call id、role/channel；parser 先按 JSONL line index 生成 item provenance，解析结束后统一补 thread/path；重复消息合并时保留 provenance。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | resolved | `preview_dry_run_summary_counts_full_timeline_beyond_preview_limit`、`imported_codex_thread_exports_evidence_with_source_provenance`、`src/lib/api/conversationImport.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| CI-019 | 导入细节还原需要可解释的 fidelity summary，否则 UI 只能展示“已导入若干消息”，无法说明工具、命令、补丁、审批、MCP、搜索和默认投影覆盖度。                                                                                                                                                                                      | 已新增 `ConversationImportFidelitySummary`，preview/commit 统一返回 messages、attachments、reasoning、tools、commands、patches、approvals、mcp、webSearch、unsupported、provenanceOnly、budgetDropped。commit 会把 source fidelity 写入 session `BusinessObjectRef.metadata.codexImportFidelity`、turn `RuntimeOptions.metadata.codexImportFidelity`，并额外写入 `importedRuntimeProjection`，记录 source runtime event 总数、默认物化窗口、sidecar event 数和窗口上限。`budgetDropped` 当前保持 0；默认窗口不是事实源裁剪，完整事实源来自 source rollout / import sidecar。统计口径是 source item / mapped runtime event 级别，不是 unique tool call 去重计数。                                                                                                                                                                                                                                                                                                                                                           | resolved | `commit_preserves_high_volume_codex_tool_events_with_bounded_default_projection`、`src/components/AppSidebar.conversations.test.tsx`、`npm run smoke:codex-import-content-studio`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| CI-020 | 导入 commit 后 `agentSession/read.detail.items` 没有把 Codex runtime events 投影到 GUI 会话页，导致现有 MessageList 只能看到 user/assistant 正文，reasoning / command / patch / approval / web search 无法按 turn 还原。                                                                                                      | 已新增 `runtime/thread_item_projection.rs`，从 RuntimeCore `StoredSession.events` 聚合出 `detail.items`：`reasoning.* -> reasoning`，`tool.* -> tool_call/web_search`，`command.* -> command_execution`，`patch.* -> patch`，`action.* -> approval_request`，并按 call id / request id 合并 lifecycle，保留 source event metadata；前端 `agentChatHistory` 复用 `toToolCallState/toActionRequired`，把这些 item 合入同一 turn 的 assistant 气泡 `contentParts/toolCalls/actionRequests`。这补的是导入后会话页展示主链，不新增平行导入浏览器。                                                                                                                                                                                                                                                                                                                                                                                                                                                 | resolved | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server conversation_import::tests::runtime_events --lib`、`npx vitest run "src/components/agent/chat/hooks/agentChatHistory.test.ts" --silent=passed-only --disableConsoleIntercept`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| CI-021 | 用户要求导入后的会话能继续在 Lime 里对话，不能只静态浏览；必须证明后续 turn 仍走同一个 current `AgentSession`。                                                                                                                                                                                                               | 后端 commit 仍写入 current `AgentSession`，`can_continue=true`，session metadata / runtime options 带 `importedContinuation`、`importedThreadSettings`、cwd、model、reasoning、approval、sandbox 和 memory provenance，后续 turn 经 `agentSession/turn/start` 进入 current RuntimeCore。Electron current fixture 已用真实 preload bridge 调用 `app_server_handle_json_lines`，按 `conversationImport/thread/commit -> agentSession/read -> agentSession/turn/start -> agentSession/read` 验证同一 session 续聊；external backend ledger 断言 runtime options 带 `imported=true` 与导入 cwd `/workspace/imported-codex`。                                                                                                                                                                                                                                                                                                                                                                      | resolved | `imported_conversation_continues_with_imported_runtime_context`；`node scripts/electron/codex-import-continuation-fixture-smoke.mjs --app-url "http://127.0.0.1:1420/" --timeout-ms 180000`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| CI-022 | Electron fixture 已证明 current 链路，但完整用户点击路径仍缺：从侧边栏导入弹窗确认、跳到会话页、看到导入细节、在输入框发送 follow-up。                                                                                                                                                                                        | 已新增 `codex-import-click-through-fixture-smoke`：使用临时 `CODEX_HOME/session_index.jsonl + rollout JSONL`，从真实侧边栏按钮打开导入弹窗，确认预览“导入细节还原”后点击导入，进入 Lime 会话页验证导入消息、reasoning、command、patch、web search、approval 可见，再通过真实输入框发送 follow-up；backend ledger 断言续聊仍在同一导入 session，且继承 `imported=true` 与 cwd。2026-06-17 又补齐全量产品化第一批 P0：commit 后 `initialSessionId` 未切到目标 session 前禁止 task-center fallback 抢回旧 tab；导入历史 process block 在完成态 timeline 中默认/强制展开，`command_execution` / `web_search` 等转换链保留 source metadata；导入预览不得暴露 `event_msg/user_message/agent_message/response_item` raw 字段；续聊不得暴露 fixture 哨兵；导入支持能力不得进入环境 / 运行控制面板独立主线卡；导入后侧栏会话 shelf 必须立即显示可继续进入的会话入口，不能只停留在“暂无聊天”。该入口是稳定 Electron GUI fixture，不等同于 Playwright MCP 手工视觉审计，也不表示全量产品化目标已经完成。 | resolved | `npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| CI-023 | 导入 timeline 已进入 GUI 主链后，代码产物工作台真实 Electron fixture 暴露两个证据误判：工具过程默认折叠时 `toolTimelineEvidencePresent` 看不到工具输出预览；空会话真实输入框已可用但 `data-session-id` 延迟水合时 fixture 误判未就绪。                                                                                        | 已让 fixture 先从侧栏真实点击进入会话，再展开 `streaming-process-group` 采集工具轨迹 GUI 证据；空会话输入判定改为“输入框可见、未禁用、页面已处于目标会话 shell”即可发送，后续仍由 `agentSession/read` 与 backend ledger 校验真实 session。该修复不新增 mock、不回退旧事件，只把测试口径对齐真实用户路径。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | resolved | `npm run smoke:code-artifact-workbench-electron-fixture`、`node scripts/electron/code-artifact-workbench-fixture-smoke.mjs --scenario gui-coding-input --timeout-ms 180000`、`npm run smoke:agent-runtime-current-fixture`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| CI-024 | 用户明确要求除导入场景外，产品通用面不要出现来源品牌字眼。此前通用输入命令命名、provider 展示、MCP 页面、模型兼容提示、通用工具展示 helper 和部分测试描述仍把来源品牌当通用产品名。                                                                                                                                           | 已把通用输入命令从 source-specific `*Slash*` 命名收敛为中性 `SlashCommand*`，命令帮助文案改为“输入命令”；provider / settings / MCP 页面展示改为“本地 CLI”；通用工具展示 helper 从 `ImportedCodex*` 泛化为 `ImportedSource*`；模型兼容 issue code 改为 `local_cli_account_model_unsupported`；后端真实 Provider 查找错误文案改为“本地 CLI Provider”；通用测试描述改为“顶部标签 / 输入设置浮层 / 结构化计划 / hover footer”。底层 `codex` 协议枚举、模型 ID、导入来源、provenance、fixture 和路线图导入说明保留来源名。后续若通用 UI 文案或通用 helper 命名回流，应优先按本条治理。                                                                                                                                                                                                                                                                                                                                                                                                             | resolved | `npx vitest run "src/components/agent/chat/hooks/agentChatSendMessage.test.ts" "src/components/agent/chat/skill-selection/inputCapabilitySections.test.ts" --silent=passed-only --disableConsoleIntercept`、`npx vitest run "src/components/agent/chat/utils/providerModelCompatibility.test.ts" "src/i18n/__tests__/loadNamespace.test.ts" --silent=passed-only --disableConsoleIntercept`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| CI-025 | Electron 点击 fixture 与截图复核确认：导入只是会话恢复支撑能力，不应在 Agent Workspace 消息主线、环境信息或运行控制面板里作为独立状态卡出现。此前 `imported-source` projection 曾进入 task rail / run control，已处理审批摘要还可能把原始 `Approve ... npm test` 或来源 rollout 文件名带进环境弹层，造成产品层级误导。        | 已撤回 `imported-source` 在 `generalWorkbenchTaskRailContextViewModel`、`generalWorkbenchRunControlSurfaceViewModel`、`TaskCenterRunControlSurface` 与 `WorkspaceConversationScene` 的主线投影；导入会话不在消息列表前置 `imported-source-banner`，也不在环境信息 / 运行控制面板展示“本地历史导入 / 已还原”状态条。详情继续保留在导入弹窗预览、消息 timeline、友好 command / approval / patch / web search 只读记录、source provenance 和 evidence/export 中。已处理的导入审批在 task rail 摘要中只显示“导入的权限记录”，不外露原始审批命令；task rail 来源摘要会跳过导入 provenance 的 source path / thread id，只保留真实检索 query 等用户可读来源线索。Electron click-through fixture 断言：消息列表主线隐藏导入摘要；打开 `task-center-environment-popover` 后不得出现 `task-center-run-control-imported`，task rail 不得展示“本地历史导入 / 已还原 / Approve ... / npm test”主线状态、内部命令或来源 thread id。                                                                         | resolved | `npx vitest run "src/components/agent/chat/workspace/WorkspaceConversationScene.test.tsx" "src/components/agent/chat/components/generalWorkbenchTaskRailViewModel.unit.test.ts" "scripts/electron/codex-import-click-through-fixture-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`、`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000`                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| CI-026 | 用户进一步要求“除了导入的，不要出现来源品牌字眼”；此前 click-through fixture 的可见测试数据、普通 hooks / provider 注释、warning 文案和部分本地化资源缺少防回流约束。                                                                                                                                                         | 已把普通工作台 / provider 注释、fixture 可见标题、查询、warning 和脚本断言文案中性化；`WorkspaceConversationScene` 不再把导入摘要接入消息列表主线，导入状态也不进入 run control projection；`i18n` loader 新增五语言 value guard，禁止普通展示文案直接写来源品牌，保留协议 key、`sourceClient`、模型 ID、导入来源和路线图说明。Electron click-through fixture 现在只接受“导入细节还原”，并验证导入摘要不出现在消息主线。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | resolved | `npx vitest run "src/i18n/__tests__/loadNamespace.test.ts" "src/components/agent/chat/workspace/WorkspaceConversationScene.test.tsx" "src/components/agent/chat/components/StreamingRenderer.test.tsx" "src/components/agent/chat/components/AgentThreadTimeline.process.test.tsx" "src/components/agent/chat/components/AgentThreadTimelineViewModel.unit.test.ts" --silent=passed-only --disableConsoleIntercept`、`npx vitest run "src/lib/api/conversationImport.test.ts" "src/components/AppSidebar.conversations.test.tsx" "scripts/electron/codex-import-click-through-fixture-smoke.test.mjs" "scripts/electron/codex-import-continuation-fixture-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`                                                                                                                                                                      |
| CI-027 | 即使导入弹窗使用中性“本地历史”文案，来源 thread id、默认数据目录、source path 或 fallback call id 仍可能从列表副标题、预览 meta、错误提示、测试 fixture 标题等可见位置泄漏来源品牌。                                                                                                                                          | 已把导入弹窗的标题 fallback 收敛为“本地历史对话”，列表副标题 fallback 改为时间 / cwd / “本地历史”，预览 meta 不再插值原始 `sourceThreadId`；五语言 `navigation.sidebar.importDialog.preview.meta` 同步删除 `threadId` 插值，并新增 `threadList.localHistory` key；`provenance.call` 改为中性“调用记录”，不回显原始 `sourceCallId`。侧栏回归断言导入预览和导入后项目会话区不得出现 `codex`、`.codex` 或 `codex-thread-*`，view model 单测覆盖 provenance 标签不泄漏 source client / path / call id；API 参数和 provenance 仍保留 source client / thread id / call id。                                                                                                                                                                                                                                                                                                                                                                                                                         | resolved | `npx vitest run "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/AppSidebar.conversations.test.tsx" "src/i18n/__tests__/loadNamespace.test.ts" --silent=passed-only --disableConsoleIntercept`、`npx eslint "src/components/app-sidebar/AppSidebarConversationImportDialog.tsx" "src/components/app-sidebar/conversationImportDialogViewModel.ts" "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/AppSidebar.conversations.test.tsx" "src/i18n/__tests__/loadNamespace.test.ts"`                                                                                                                                                                                                                                                                                                                        |
| CI-028 | “除了导入的不要出现来源品牌字眼”还需要划清模型 ID 和协议枚举边界：真实模型 ID 可能天然包含来源词，底层 `sourceClient` / provider type / metadata key 也需要保持稳定；如果一刀切清理会破坏模型选择、导入幂等、provenance 和 App Server contract。                                                                              | 已复核 `src` 普通 GUI、MCP 页面、Provider 展示、导入弹窗、i18n 资源和历史治理目录引用：可见产品文案继续使用“本地历史 / 本地 CLI / 导入细节还原”等中性表达；`gpt-*-codex` 模型 ID、`sourceClient: "codex"`、`enabled_codex`、provider type、fixture、路线图和治理 catalog 仍按事实源保留。五语言 i18n value guard 只允许真实模型 ID 作为展示值例外，普通展示文案不得回流来源品牌。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | resolved | `rg -n --hidden -S "\\b[Cc]odex\\b                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | codex\_\\w+                                                         | codex-\\w+ | \\w+Codex\\w*" "src/components" "src/features" "src/pages" "src/i18n/resources" -g '!\*\*/*.test._' -g '!\*\*/_.test/**' -g '!**/_Fixtures_'`、`npx vitest run "src/i18n/**tests**/loadNamespace.test.ts" "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/AppSidebar.conversations.test.tsx" --silent=passed-only --disableConsoleIntercept`、`npx eslint "src/i18n/**tests**/loadNamespace.test.ts" "src/components/app-sidebar/conversationImportDialogViewModel.ts" "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/app-sidebar/AppSidebarConversationImportDialog.tsx" "src/components/AppSidebar.conversations.test.tsx"` |
| CI-029 | 真实 content-studio dogfood smoke 使用持久 App Server data-dir，第一次导入后再次运行会把 scan/preview 标记为已导入，导致脚本在“首次导入会创建 session”断言处失败；这会让真实大样本验证不可重复，削弱后续产品化验收。                                                                                                          | 已让 `smoke:codex-import-content-studio` 默认创建临时 App Server `--data-dir`，每次运行从干净 RuntimeCore / ProjectionStore 开始，同时保留 `CODEX_IMPORT_SMOKE_DATA_DIR` 用于显式复用调试、`CODEX_IMPORT_SMOKE_KEEP_DATA_DIR=1` 用于保留现场。脚本仍在同一次隔离运行内覆盖首次 preview/commit、`confirmed=false` 拒绝、duplicate commit 复用同一 session、rescan 全部 imported。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | resolved | `npm run smoke:codex-import-content-studio` 连续复跑通过：13/13 真实线程，458 条投影消息，281 个 turn，4 条多模态线程，13 条模型 metadata 线程；`npx vitest run "scripts/app-server/codex-import-content-studio-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`、`npx eslint "scripts/app-server/codex-import-content-studio-smoke.mjs" "scripts/app-server/codex-import-content-studio-smoke.test.mjs"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| CI-030 | 导入点击闭环已有多视口截图，但缺少可重复的产品视觉边界入口；此前 fixture 中补丁路径带来源品牌测试目录，GUI 快照可见文本会出现非导入语境来源词。                                                                                                                                                                               | 已新增 `smoke:local-history-import-visual-audit`：复用真实 Electron 点击闭环 fixture，再机械检查 `desktop / compact / narrow` 三视口的消息列表、输入框、导入命令 / 补丁 / 搜索 / 审批记录、无导入主线 banner / run control 卡，并扫描 GUI 可见文本，确认普通展示不泄漏来源品牌。点击 fixture 的可见工作区路径改为 `/workspace/imported-local-history`，底层 `sourceClient`、provenance、模型 ID 和路线图说明不变。该入口不读取真实用户历史目录，不调用正式模型，不使用 renderer mock fallback。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | resolved | `npm run smoke:local-history-import-visual-audit -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000`，summary `.lime/qc/gui-evidence/local-history-import-visual-audit/local-history-import-visual-audit-summary.json`：`ok=true`、`visibleTextLeaks=[]`、三视口 `inputbarVisible=true` / `messageListVisible=true` / `hidesSourceBrandText=true`；`npx vitest run "scripts/electron/local-history-import-visual-audit-smoke.test.mjs" "scripts/electron/codex-import-click-through-fixture-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`、`npx eslint "scripts/electron/local-history-import-visual-audit-smoke.mjs" "scripts/electron/local-history-import-visual-audit-smoke.test.mjs" "scripts/electron/codex-import-click-through-fixture-smoke.mjs"`                                                                                                            |
| CI-031 | `scripts/electron/codex-import-click-through-fixture-smoke.mjs` 曾达 1952 行，继续追加视觉 / 品牌边界逻辑会违反脚本体量边界并增加维护风险。                                                                                                                                                                                   | 已把点击闭环 fixture 拆成场景编排脚本 + `scripts/electron/lib/local-history-import-click-through-fixture.mjs` + `scripts/electron/lib/local-history-import-click-through-gui.mjs` + 共享 `local-history-import-smoke-utils.mjs`。主脚本回落到 850 行，fixture / GUI helper / shared helper 分别为 257 / 777 / 525 行；测试守卫同步改为读取拆分后的完整 smoke surface。2026-06-17 真实 Electron 点击闭环复跑通过，summary `ok=true`、`electronPreloadBridge=true`、`consoleErrors=[]`、read model 保留 reasoning / command / patch / web search / approval，backend ledger 证明同 session 续聊继承 imported metadata 与 cwd，三视口视觉审计通过。首次复跑曾因 Vite 旧热更新产物触发 `planComposerPendingActions` 初始化顺序崩溃，源码当前变量已在首次 JSX 使用前定义，重新运行后通过。                                                                                                                                                                                                         | resolved | `wc -l scripts/electron/codex-import-click-through-fixture-smoke.mjs scripts/electron/lib/local-history-import-click-through-gui.mjs scripts/electron/lib/local-history-import-click-through-fixture.mjs scripts/electron/lib/local-history-import-smoke-utils.mjs`、`npx vitest run "scripts/electron/codex-import-click-through-fixture-smoke.test.mjs" "scripts/electron/local-history-import-real-sample-visual-audit-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`、`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000`                                                                                                                                                                                                                                                                               |
| CI-032 | 小 fixture 视觉审计不能代表真实 content-studio 长历史；真实导入中还会出现用户/助手原文里的来源词，若把裸 `.codex` 一刀切当 GUI 泄漏，会把导入正文保真误判成产品泄漏。                                                                                                                                                         | 已新增 `smoke:local-history-import-real-sample-visual-audit`：启动真实 Electron + 隔离 App Server data，从真实 content-studio 本地历史源只读 scan/preview，选择最长/最复杂线程 commit，再从侧边栏打开导入会话，采集 `desktop / compact / narrow` 与 `top / middle / bottom` 九张截图。审计边界改为禁止具体 sourceRoot/sourcePath/sourceThreadId/raw event 字段和来源品牌状态文案泄漏，但允许导入正文原文保真；证据 JSON 只保留摘要、计数和截图索引，不写真实对话正文。为避免新脚本继续膨胀，已把启动、JSON-RPC、侧栏打开和视觉审计 helper 抽到 `scripts/electron/lib/local-history-import-smoke-utils.mjs`。                                                                                                                                                                                                                                                                                                                                                                                  | resolved | `npm run smoke:local-history-import-real-sample-visual-audit -- --app-url "http://127.0.0.1:1420/" --timeout-ms 240000`，summary `.lime/qc/gui-evidence/local-history-import-real-sample-visual-audit/local-history-import-real-sample-visual-audit-summary.json`：`ok=true`、真实线程 `14290` 行 / `1756` 条 source message / `12739` 个 willImportTimelineItems，read model `105` 条消息 / `413` 个 timeline item，九张截图 `leakedTokens=[]`、输入框与消息列表均可见、无导入主线 banner / run control；`npx vitest run "scripts/electron/local-history-import-real-sample-visual-audit-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`、`npx eslint "scripts/electron/local-history-import-real-sample-visual-audit-smoke.mjs" "scripts/electron/local-history-import-real-sample-visual-audit-smoke.test.mjs" "scripts/electron/lib/local-history-import-smoke-utils.mjs"` |
| CI-033 | 真实 Electron 审计期间，renderer 调试日志上报到 `report_frontend_debug_log` 时，如果父进程 stdout/stderr 已关闭，Electron main 直接 `console.log` 会触发未捕获 `write EPIPE` 并中断桌面壳。                                                                                                                                   | 已在 Electron Desktop Host current command `report_frontend_debug_log` 加 `safeWriteElectronHostLog`，只吞掉 `EPIPE`、`ERR_STREAM_DESTROYED`、`ERR_STREAM_WRITE_AFTER_END` 这类已关闭输出管道错误；其它日志错误继续抛出，避免掩盖真实问题。该修复不新增命令、不走 mock，只收紧 host 日志写入边界；已执行 `electron:build:host:dev` 刷新本地运行产物。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | resolved | `npx vitest run "electron/hostCommands.test.ts" --silent=passed-only --disableConsoleIntercept`、`npm run typecheck:electron`、`npx eslint "electron/hostCommands.ts" "electron/hostCommands.test.ts"`、`npm run electron:build:host:dev`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| CI-034 | 项目级入口最初会从侧栏项目行直接露出，且入口文案过长；用户要求迁入项目更多菜单，并且只有异步确认存在可导入本地历史时才显示。                                                                                                                                                                                                  | 已把项目行级导入按钮迁入项目更多菜单，五语言入口文案收敛为“导入对话”；`useAppSidebarConversationImport` 异步按项目 `rootPath` probe `conversationImport/source/scan`，只有 source ready 且存在可导入线程时才把项目 id 放入 `importableProjectIds`。effect 使用稳定 key 派生 probe targets，避免 projects 引用抖动导致无限渲染。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | resolved | `npx vitest run "src/components/AppSidebar.conversations.test.tsx" --silent=passed-only --disableConsoleIntercept`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| CI-035 | 项目导入弹窗确认时曾只提交当前预览的一条线程，真实项目会出现“只导入了一个对话”的缺口；后续又曾把确认按钮改成默认全量导入，和“弹窗确认 + 可勾选批量导入”产品语义冲突。                                                                                                                                                                                                                                        | 已把导入弹窗收敛为显式勾选模型：scan 使用 cursor 循环翻页，默认只勾选第一条可导入对话，用户可通过“全选”或“选择本组”按天 / 按月批量勾选；确认时只提交 `checkedThreadIds` 中的 `not_imported/imported` 线程。已导入线程保持 `replaceExisting=true`，实现“清理已导入并重新导入”。成功 toast 汇总实际提交响应的消息数，侧栏按实际结果 optimistic 插入。该口径避免无意全量写入，同时解决“只导入一个对话”的批量入口缺口。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | resolved | `npx vitest run "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/AppSidebar.conversations.test.tsx" "src/lib/api/conversationImport.test.ts" --silent=passed-only --disableConsoleIntercept`；`AppSidebar.conversations.test.tsx` 覆盖默认单选只提交 1 条、按组选择后提交同组 2 条、已导入重导入透传 `replaceExisting=true`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| CI-036 | 已导入线程需要支持“清理已导入结果并重新导入”，不能只返回幂等旧 session；同时重新导入要清掉旧 projection / event log / 完整记录 sidecar，避免侧栏、会话详情和完整记录下钻读到脏数据。                                                                                                                                                                           | `ConversationImportThreadCommitParams` 已新增 `replaceExisting`；默认重复导入仍幂等复用旧 session，`replaceExisting=true` 时先从 `RuntimeCore.state.sessions`、`ProjectionStore`、`EventLogWriter` 和 `SidecarStore` 清理旧导入 session，再重新读取源文件创建新 session。前端对 `importStatus=imported` 的线程展示“清理并重新导入”，提交时带 `replaceExisting: true`；后端 warning presentation 同步收敛为“local history / 本地历史”语义，避免普通 UI 外露来源品牌。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | resolved | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import::tests::idempotency:: -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::sidecar_store::tests::clear_session_removes_session_scoped_sidecars -- --nocapture`、`src/components/AppSidebar.conversations.test.tsx` 已导入确认断言 `replaceExisting: true`、`npm run test:contracts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| CI-032 | Codex 导入闭环已经通过多条 smoke，但还没有作为 Agent Workspace P0 evidence 沉淀，容易让后续评估把“导入支持能力已验证”和“Workspace 主线全部完成”混在一起。                                                                                                                                                                     | 已新增 `internal/roadmap/agent-workspace/evidence/agent-workspace-session-restore.20260617-1559.json`，把真实 Electron 点击导入、会话页恢复、工具 / 补丁 / 搜索 / 审批细节可见、同 session 续聊、三视口视觉边界和“导入不进主线状态卡”的断言纳入 `agent-workspace-session-restore` 正式 evidence。该 evidence 明确导入只是 session restore / provenance 支持能力，不代表 Agent Workspace 11 个 P0 场景全量完成。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | resolved | `internal/roadmap/agent-workspace/evidence/agent-workspace-session-restore.20260617-1559.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| CI-037 | 真实会话截图暴露 timeline 时序仍可能混乱：工具 / 命令过程块先显示，而助手 `phase=commentary` 首句被丢弃或积压到后面；同时用户截图出现“当前处理失败”，需要区分显示投影缺陷和 live provider 失败。                                                                                                                              | 已把 `phase=commentary` 的 `agent_message` 纳入 timeline inline content parts，按 `agent_message(commentary) -> command/tool/patch/approval -> agent_message(final_answer)` 事件序渲染；`actionContent`、复制和引用仍只取 `final_answer / final / null`，不把 commentary 当最终正文。稳定 current fixture 中未复现“当前处理失败”：真实 content-studio 长样本审计和点击续聊 fixture 均 `ok=true`、`consoleErrors=[]`；若 live provider 仍失败，下一刀应沿 `agentSession/turn/start -> turn_failed` 的 provider/runtime error 排查，而不是继续改导入投影。                                                                                                                                                                                                                                                                                                                                                                                                                                      | resolved | `npm exec vitest -- run "src/components/agent/chat/components/messageListTimelineContentParts.unit.test.ts"`、`npm exec vitest -- run "src/components/agent/chat/components/messageListItemProjection.timeline.unit.test.ts"`、`npm run smoke:local-history-import-real-sample-visual-audit`、`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000`                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| CI-038 | 导入会话的来源模型曾被前端误当成 Lime 当前可执行 provider/model，导致续聊时裁掉用户当前模型选择，后端可能因缺少有效 provider/model selection 进入“当前处理失败”。                                                                                                                                                             | 已把导入来源 runtime 明确降级为 provenance/context：`source_client`、`imported_continuation`、`imported_thread_settings` 只用于识别导入来源；当 runtime 没有当前 `provider_selector` 时，前端不会再用来源 `provider_name/model_name` 生成 session model preference，也不会在 submit op compaction 中把当前 provider/model 裁掉。用户当前选择或后续真正写入的 `provider_selector` 仍按 Lime 多模型主链提交。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | resolved | `npm exec vitest -- run src/components/agent/chat/utils/sessionExecutionRuntime.test.ts src/components/agent/chat/utils/submitOpRuntimeCompaction.test.ts --silent=passed-only --disableConsoleIntercept`、`npm exec vitest -- run src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -t "provider sync                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 导入会话的来源模型" --silent=passed-only --disableConsoleIntercept` |
| CI-039 | PRD 安全验收要求敏感文件 denylist 结构测试；此前 unknown event、幂等导入和 missing rollout 已覆盖，但 state DB 或显式 `sourcePath` 仍缺 fail-closed 守卫。                                                                                                                                                                    | 已把 Codex source path 解析统一收敛到 `codex/paths.rs`：只允许 source root 内的 `rollout-*.jsonl` / `.jsonl.zst`，拒绝 `auth.json`、token、secret、config、credentials 等敏感文件名；scan 修复 DB path、preview 显式 `sourcePath` 和 commit 显式 `sourcePath` 复用同一 guard。新增 `conversation_import/tests/security.rs`，避免继续膨胀 `tests.rs`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | resolved | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server conversation_import::tests::security --lib`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| CI-040 | 导入来源识别如果只认 App Server snake_case 字段，未来前端 normalizer / fixture 以 camelCase 投影 `sourceClient/importedContinuation/importedThreadSettings` 时，仍可能把导入历史 failed turn 冒充为当前“处理失败”，或把来源 provider/model 当成当前 Lime 模型偏好。                                                           | 已把前端导入来源识别统一兼容 snake_case 与 camelCase：`agentChatHistory` 的失败态 hydrate 会跳过两种命名的导入历史；`sessionExecutionRuntime` 与 submit op compaction 也不会用 camelCase 来源模型裁掉当前用户选择。该修复不改协议、不新增 mock，只补前端防回流边界。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | resolved | `npm exec vitest -- run "src/components/agent/chat/utils/sessionExecutionRuntime.test.ts" "src/components/agent/chat/utils/submitOpRuntimeCompaction.test.ts" "src/components/agent/chat/hooks/agentChatHistory.test.ts" --silent=passed-only --disableConsoleIntercept`、`npx eslint "src/components/agent/chat/utils/sessionExecutionRuntime.ts" "src/components/agent/chat/utils/sessionExecutionRuntime.test.ts" "src/components/agent/chat/utils/submitOpRuntimeCompaction.test.ts" "src/components/agent/chat/hooks/agentChatHistory.ts" "src/components/agent/chat/hooks/agentChatHistory.test.ts"`                                                                                                                                                                                                                                                                                     |
| CI-041 | source metadata、warnings、turn runtime options 和 item provenance helper 分散在 `commit.rs`、`codex.rs`、`codex/events.rs`，后续接入其他本地历史来源时容易再次复制一套来源证明逻辑，也会让 `commit.rs` / `codex.rs` 重新逼近 800 行预警线。                                                                                  | 已新增 `conversation_import/provenance.rs` 作为 source metadata / warnings / continuation runtime options / item provenance / compact JSON 的 current owner；`commit.rs` 只保留写入流程，`codex/events.rs` 保留 raw event -> runtime event 映射并委托 provenance helper。该拆分不新增 JSON-RPC method，不改协议，不改 GUI 文案；`commit.rs` 490 行、`codex.rs` 755 行、`events.rs` 552 行、`provenance.rs` 360 行，均低于体量预警线。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | resolved | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import::tests::runtime_events -- --nocapture`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server conversation_import::tests::idempotency --lib`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server conversation_import::tests::evidence --lib`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| CI-042 | 真实 content-studio 长样本导入后，read model 已有 `command_execution` / `patch` 等 timeline item，但 GUI 仍可能不显示“导入的命令记录”，用户只看到“处理失败”或过程流缺失，说明长历史压缩与 inline timeline 投影仍会吞掉只有工具/命令的导入回合。                                                                               | 已把 `messageListTimelineContentParts` 的导入来源 process item 从“必须同 turn 有可渲染 agent message”限制中豁免：只要 timeline item 带导入 provenance，命令、工具、patch、approval 也能单独生成 inline process parts；普通非导入历史仍沿用原折叠策略。`messageListItemProjection.imported.unit.test.ts` 新增“无额外助手过程文本也应显示导入命令记录”回归；真实 Electron 长样本审计复跑后九张截图通过，summary 中 `hasCommandRecordVisible=true`、`hasPatchText=true`、`hasApprovalText=true` 至少在可见滚动位出现，`leakedTokens=[]`、`consoleErrors=[]`。                                                                                                                                                                                                                                                                                                                                                                                                                                    | resolved | `npx vitest run "src/components/agent/chat/components/messageListItemProjection.imported.unit.test.ts" "src/components/agent/chat/components/messageListTimelineContentParts.unit.test.ts" "src/components/agent/chat/components/messageListItemProjection.timeline.unit.test.ts" --silent=passed-only --disableConsoleIntercept`、`npm run smoke:local-history-import-real-sample-visual-audit`、`npm run smoke:codex-import-content-studio`                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| CI-043 | 真实截图中的“处理失败”和首句错位还有后端根因：导入 commit 只支持 `event_msg -> response_item` 的用户消息去重，若 source rollout 中 `response_item` 先于 `event_msg`，同一句用户输入会被拆成两个 Lime turn；同时未闭合的导入工具 / 命令 / patch lifecycle 被 synthetic `failed` 终态补齐，导致只读历史被误投影成当前执行失败。 | 已按 source rollout 语义修复后端导入：用户消息去重支持 `response_item <-> event_msg` 双向交叉合并，但不合并同来源重复消息，避免吞掉真实下一轮；导入 turn 的 `started_at` 使用来源用户消息 timestamp，避免用户首句按导入时间积压；未闭合 lifecycle 以 `completed + importedIncomplete=true + failureCategory=incomplete_import` 收口，保留诊断信息但不在 GUI 显示为执行失败；补齐 `patch_apply_begin -> patch.started` 映射。该修复不新增协议枚举，不放松 RuntimeCore 事件校验，不改变 Lime 多模型 / 多模态续聊主链。                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | resolved | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import::tests::runtime_events -- --nocapture`、`npm run smoke:codex-import-content-studio`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| CI-044 | 真实长样本审计中，后端 `agentSession/read.detail.items` 已有 `command_execution=80`，但 GUI 仍可能展示原始命令块而不是“导入的命令记录”；根因是前端 `agentChatHistory` 虽然计算了 normalized tool result metadata，却继续把原始 `toolCall` 写入 `draft.toolCalls/contentParts`，导致导入 provenance 没有稳定进入渲染层。       | 已让历史 hydrate 使用带 normalized result metadata 的 `normalizedToolCall` 写入 `toolCalls` 与 `contentParts`；导入命令 metadata 中的 `imported/source_client/exit_code/cwd` 可被 `ToolCallDisplay` 识别，继续走友好只读摘要，不渲染原始命令摘要和输出流。新增 `agentChatHistory.test.ts` 回归，确认导入命令 metadata 贯穿 hydrate 后的 tool call 和 content part；真实 content-studio 长样本 Electron 审计已复跑通过，summary `ok=true`、`desktop:bottom.hasCommandRecordVisible=true`、`consoleErrors=[]`、`leakedTokens=[]`。                                                                                                                                                                                                                                                                                                                                                                                                                                                              | resolved | `npx vitest run "src/components/agent/chat/hooks/agentChatHistory.test.ts" "src/components/agent/chat/components/ToolCallDisplayViewModel.unit.test.ts" "src/components/agent/chat/components/StreamingRenderer.test.tsx" --silent=passed-only --disableConsoleIntercept`、`npx eslint "src/components/agent/chat/hooks/agentChatHistory.ts" "src/components/agent/chat/hooks/agentChatHistory.test.ts"`、`npm run smoke:local-history-import-real-sample-visual-audit -- --app-url "http://127.0.0.1:1420/" --timeout-ms 240000`                                                                                                                                                                                                                                                                                                                                                              |
| CI-045 | 导入来源的单条 `reasoning` 会被普通短答安全阈值吞掉，导致用户截图里“最先的一句思考”没有按事件序出现在命令 / 工具之前；仅靠 tool metadata 展开还不能覆盖纯思考或思考领先工具的导入回合。                                                                                                                                       | 已让导入来源的单条 `reasoning` 进入 inline `thinking` content part，并把 item 级 import metadata 贯通到 `ContentPart.metadata`；`StreamingRenderer` 仅对带导入 provenance 的 thinking 过程默认展开并展示原文，普通非导入单条 reasoning 仍按安全阈值折叠，不把内部推理误放进正文。新增回归断言导入思考默认展开并排在命令记录之前。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | resolved | `npx vitest run "src/components/agent/chat/components/messageListTimelineContentParts.unit.test.ts" "src/components/agent/chat/components/StreamingRenderer.test.tsx" "src/components/agent/chat/components/ToolCallDisplayViewModel.unit.test.ts" --silent=passed-only --disableConsoleIntercept`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| CI-046 | `update_plan` 在来源客户端中是独立的计划快照事件，但 Lime 导入此前只把它当普通工具调用；同时 Codex rollout 只持久化 `ItemCompleted(TurnItem::Plan)`，不持久化 `EventMsg::PlanUpdate`，导致计划可能无法进入 `AgentThreadPlanItem`、assistant inline 过程和右侧任务轨。 | 已按来源客户端 `update_plan` 工具形状 `{ explanation, plan: [{ step, status }] }` 导入：后端仍保留原始 `tool.started` 事件用于 provenance，但额外投影 `plan.final`，`thread_item_projection` 生成 `type=plan` 的 timeline item，并把结构化 `plan/explanation/tool_call_id/source_provenance` 写入 metadata；同时对齐 Codex rollout 持久化策略，支持 `event_msg.item_completed` 且 `item.type=Plan/plan` 的 completed plan item 直接投影为 `plan.final`，保留 `source_item_id/source_provenance`。GUI inline content parts 将 plan item 包装为 `<proposed_plan>` 复用现有 `AgentPlanBlock`，顺序保持在 reasoning 与命令之间；右侧任务轨优先从 `plan` item metadata 恢复多步骤计划，避免整段 Markdown 退化成一个步骤。该刀不新增 JSON-RPC、不改 Electron bridge，不触碰 `read_file` 参数链路。 | resolved | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import::tests::runtime_events -- --nocapture`、`npx vitest run "src/components/agent/chat/components/messageListTimelineContentParts.unit.test.ts" "src/components/agent/chat/components/generalWorkbenchTaskRailViewModel.unit.test.ts" --silent=passed-only --disableConsoleIntercept` |
| CI-047 | 真实导入主线已经跨 Rust、App Server、Electron smoke 和 Preview Artifact Contract 收口，但如果没有单一机器可读验收矩阵，后续容易把“全量还原”退化成散文记录或局部 happy path。 | 已新增 `fidelity-acceptance-matrix.md`，按 source discovery / message ordering / attachments / reasoning / command / read_file / preview artifact / patch / approval / web search / MCP / plan / subagent / incomplete lifecycle / high-volume rollout / runtime detail drilldown / continue session / evidence / privacy / non-Codex importer 分类，把每一项绑定到现有 current 事实源与定向测试 / GUI smoke surface；同时补 `scripts/electron/codex-import-fidelity-acceptance-matrix.test.mjs`，只读路线图、Rust 定向测试和 smoke surface，防止路线图漂移。 | resolved | `npx vitest run "scripts/electron/codex-import-fidelity-acceptance-matrix.test.mjs" --silent=passed-only --disableConsoleIntercept` |
| CI-048 | Preview Artifact Contract 已覆盖 DOCX 文本抽取和图片，但 PDF、Excel、PPT 等二进制文档如果被误投影为 `document_text`，右侧工作台会显示“成功打开但正文为空”；URL / 数据记录 / App shell 如果继续伪装成普通文件，也会形成文件预览与 artifact 打开双轨。 | 已把文档类 preview artifact 分界固定为：只有后端抽取到可读文本时才进入 `renderMode=document_text`；DOCX / XLSX / PPTX 与可解析 PDF 文本流由 `document-preview` 抽取，抽取为空、扫描件、复杂 PDF 字体映射或旧版 Office 二进制格式仍生成 source-backed preview artifact，但必须进入 `renderMode=system_open`，由同一 ArtifactToolbar 调用 Desktop Host `open_with_default_app`。Workspace 文件打开会保留 App Server `readFilePreview.path`，确保二进制文档系统打开拿到真实路径；音频 / 视频进入媒体 renderer；URL、database_record 与 app shell 进入 `PreviewSourceSummaryRenderer` 来源摘要面；WebSearch 搜索结果点击优先生成 `source=url` preview artifact 并打开右侧工作台，URL 外部打开只保留在 ArtifactToolbar `open_external_url` current 通道；record / app 后续动作由对应业务工作台承接。 | resolved | `cargo test --manifest-path "lime-rs/Cargo.toml" -p document-preview --lib`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-services file_browser_service --lib`、`npx vitest run "src/lib/artifact/previewArtifact.test.ts" "src/components/artifact/ArtifactRenderer.ui.test.tsx" "src/components/artifact/ArtifactToolbar.ui.test.tsx" "src/components/agent/chat/workspace/useWorkspaceArtifactPreviewActions.test.tsx" --silent=passed-only --disableConsoleIntercept`、`npx vitest run "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" "src/components/agent/chat/components/MessageList.test.tsx" "src/components/agent/chat/workspace/useWorkspaceConversationSceneRuntime.test.ts" --silent=passed-only --disableConsoleIntercept` |
| CI-049 | WebSearch 结果已经能生成 `source=url` preview artifact，但右侧工作台此前只能看到搜索摘要；Codex / Lime 工具轨里常见 WebSearch 后紧跟同 URL WebFetch，若不复用已有页面正文，导入会话的网页证据还原度不足，同时新增 renderer 抓取器又会形成第二套 URL viewer。 | 已新增 `urlPreviewSnapshot` current helper：只在同一消息 / 同一 Streaming process run 内寻找 URL 匹配、状态成功、非 RSS/XML / timeout / HTTP 诊断噪音的 WebFetch，并把结构化 `markdown/content/text/body/summary` 正文附加到 `SearchResultPreviewItem.snapshotContent`。`InlineToolProcessStep` 和 `StreamingRenderer` 都复用该 helper；混合 WebSearch / WebFetch 批次展开时渲染同一 `SearchResultPreviewList`，点击后继续走 `onOpenUrlPreview -> AgentChatWorkspace -> createPreviewArtifact(source=url)`，Workspace 优先把快照正文写入 preview artifact，搜索摘要只作为 fallback。该刀不新增网络请求、不改 Electron bridge、不新增 URL 独立 viewer。 | resolved | `npx vitest run "src/components/agent/chat/utils/urlPreviewSnapshot.unit.test.ts" "src/components/agent/chat/components/InlineToolProcessStep.test.tsx" "src/components/agent/chat/components/StreamingRenderer.test.tsx" --silent=passed-only --disableConsoleIntercept` |
| CI-050 | 已导入线程的 `replaceExisting=true` 已能清理旧 projection / event log / session-scoped sidecar 并重新导入，但用户在侧栏“删除会话”不能继续伪装成归档 / 隐藏；否则会和归档恢复、重导入替换、导出后保留策略混成同一语义。 | 已明确三层语义：归档 / 恢复继续走 `agentSession/update`；重导入替换继续由导入 commit 的 `replaceExisting=true` 清理旧导入 session；用户删除 Lime session 数据走 App Server `agentSession/delete` current 主链，清理 memory / projection / event log / sidecar，不删除外部来源目录。导出后删除、保留期限和删除前导出确认仍是后续产品规则。 | resolved | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server persisted_session_delete_clears_projection_and_event_log --test session_archive_jsonrpc`、`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server-protocol`、`npx vitest run "src/lib/api/agentRuntime/sessionClient.current-boundary.test.ts" "src/lib/api/agentRuntime/appServerSessionClient.test.ts" "src/lib/api/agentRuntime/clientFactory.test.ts" --silent=passed-only --disableConsoleIntercept --testTimeout=30000`、`npm --prefix "packages/app-server-client" test`、`npm run test:contracts` |

## 进行中记录

### 2026-06-18

- 已按 `/Users/coso/Documents/dev/rust/codex` 的 `parse_turn_item` / contextual user fragment 规则修复导入消息分类：`# AGENTS.md instructions`、`<environment_context>`、`<skill>`、`<user_shell_command>`、`<skills_instructions>`、`<permissions instructions>` 等来源上下文片段不再进入 Lime 主消息流；它们仍作为 source rollout / provenance / imported continuation 的运行上下文来源保留，不影响后续继续对话。
- 已补预览与 commit 双层回归：preview 不显示 AGENTS / skills / environment 注入消息，commit 后 `agentSession/read.detail.messages` 也只包含真实用户请求和助手回复；这修复真实 content-studio 导入截图中 AGENTS.md 被当作首条普通消息上屏的问题。
- 已复跑 `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import -- --nocapture`，36 个 conversation import 测试通过，覆盖消息过滤、图片附件、重复消息合并、工具 / 命令 / patch / approval / reasoning / plan / 高容量 runtime event 投影。
- 已补 WebSearch / WebFetch URL 快照复用：同一消息 / 过程组里成功 WebFetch 的结构化正文可进入 URL preview artifact，右侧工作台不再只能展示搜索摘要；仍不新增主动 URL 抓取器。前端定向回归 `urlPreviewSnapshot.unit.test.ts`、`InlineToolProcessStep.test.tsx`、`StreamingRenderer.test.tsx` 已通过。
- 已把文档预览抽取范围从 DOCX 扩到 XLSX、PPTX 与 best-effort PDF 文本流：Codex 导入工具轨和普通文件点击仍走同一 `readFilePreview -> Preview Artifact Contract -> Artifact Workbench` 链路，抽取成功时进入 `document_text`，抽取为空时继续 `system_open` 兜底；不新增 renderer 文档解析或第二套打开逻辑。
- 本轮同时收掉 `InlineToolProcessStep.tsx` 硬阈值风险：把工具结果正文解析抽到 `toolResultDetailText.ts`，把任务板结果摘要抽到 `taskBoardToolResultDetail.ts`，组件从约 1034 行降到 927 行。`StreamingRenderer.tsx` 仍约 1990 行，是历史巨型组件；本轮只做薄接线和 process run 上下文透传。下一次触碰 `StreamingRenderer` 时应优先拆 `StreamingProcessGroup` / web search process summary / process entry renderer 到独立小组件或 hook，退出条件是主文件低于 1000 行或至少把 process group 职责移出。
- 已完成导入弹窗硬编码审计收口：对照 react-i18next / i18next 的组件文本本地化模式、WAI 图片 alt 语义要求，以及 AG-UI demo 中通过 `labels` / 数据字段承载展示文本的做法，Lime 保留 `sourceClient` 等协议枚举为稳定契约值，但用户可见标题、来源 label、confirm、toast、图片 alt / unavailable 文案必须走 i18n 或 view model。`AppSidebarConversationImportDialog` 已改用 `DEFAULT_CONVERSATION_IMPORT_SOURCE_CLIENT`，标题 fallback 和 source label 进入 `conversationImportDialogViewModel` + 五语言 resources；组件测试也从旧长文案 / 默认全量导入断言改为当前精简弹窗、默认单选、按组批量勾选的产品事实。
- 已继续收敛导入来源硬编码：侧栏项目导入异步探测不再直接写 `sourceClient` 字面量，统一复用 `DEFAULT_CONVERSATION_IMPORT_SOURCE_CLIENT`；导入来源展示 label 改为 `IMPORT_SOURCE_CLIENT_LABELS` 表驱动，协议枚举保持稳定、presentation 只走 i18n key。静态复核 `src/components/app-sidebar` 非测试源码后，仅剩 view model 内协议常量和 label 映射 key，普通组件 / hook 不再散落来源品牌字面量。
- 已复跑硬编码收口回归：`npx vitest run "src/components/app-sidebar/conversationImportDialogViewModel.unit.test.ts" "src/components/AppSidebar.conversations.test.tsx" "src/lib/api/conversationImport.test.ts" --silent=passed-only --disableConsoleIntercept` 通过，61 个测试覆盖默认单选、按组批量、重导入 replace、来源品牌不外泄；`npx vitest run "src/i18n/__tests__/loadNamespace.test.ts" "src/i18n/__tests__/types.test.ts" --silent=passed-only --disableConsoleIntercept` 通过，确认五语言 navigation 新 key 可加载且普通展示文案不暴露底层来源品牌。
- 已收口侧栏导入入口文案一致性：普通对话区按钮、项目菜单可见项和导入弹窗主按钮统一显示短文案“导入对话”；项目菜单仍通过 `aria-label/title` 保留“导入到 {{title}}”上下文，避免短文案牺牲辅助语义。五语言 `navigation.sidebar.conversations.importProjectConversation` 已同步改为短文案，`importProjectConversationFor` 保留项目上下文。组件回归补断言项目菜单可见文案短、`aria-label/title` 包含项目名；本轮复跑导入弹窗 / 侧栏 / i18n 定向测试和 `git diff --check` 通过。
- 已修复导入点击闭环 visual audit 的 read model 断言：`web_search` 轨迹不再只认旧 `action === "search_query"` 结构，而是接受 current read model 中的 `type === "web_search"` + query / tool 名 / output 片段任一稳定信号，避免把真实导入的搜索证据误判为缺失。2026-06-18 重新复跑 `npm run smoke:codex-import-click-through-electron-fixture -- --timeout-ms 180000` 和 `npm run smoke:local-history-import-visual-audit -- --timeout-ms 180000` 均通过，summary 都显示 `ok=true`、`consoleErrors=[]`、`hasWebSearchItem=true`，三视口 `desktop / compact / narrow` 的 `hasSearchEvidence=true`、`hidesSourceBrandText=true`、`inputbarVisible=true`。

### 2026-06-17

- 已确认当前实现位于 App Server JSON-RPC current 主链，正确落点是 `lime-rs/crates/app-server/src/runtime/conversation_import/**`、`app-server-protocol`、`packages/app-server-client` 与 `src/lib/api/conversationImport.ts`。
- 已确认 scan/preview 是只读能力，不需要用户确认；commit 必须通过弹窗取得用户确认，并由后端 `confirmed=true` 再次强制。
- 已确认 `RuntimeCore` read model 会从 `turn_inputs` 投影用户消息，从 `message.delta` 聚合 assistant 消息；commit 不需要新增 transcript store。
- 已补齐 `conversationImport/thread/commit` 后端、协议、client、前端网关和定向测试。
- 已按 `/Users/coso/Documents/dev/rust/codex` 校正两个关键规则：优先读取 `state_5.sqlite`，并把 `event_msg.user_message / event_msg.agent_message` 作为 Codex 用户可见历史主来源。
- 已验证 commit 后导入结果能通过 `agentSession/read` 看到 user/assistant messages；新 turn 仍应走 Lime `agentSession/turn/start` current 主链。
- 已完成第一刀结构治理：`conversation_import/mod.rs` 不再超过 1000 行，commit 和 tests 已拆出；Codex parser 继续作为后续 P1 拆分项。
- 已新增真实 GUI 确认弹窗：侧边栏提供 Codex 导入入口，弹窗执行 scan/preview，展示 source、target、message count、warnings 和消息预览；用户确认前不会调用 commit。
- 已补五语言 `navigation` 文案与侧边栏回归：取消导入不提交 commit，项目范围导入会把 `workspaceId/projectPath` 带入 current App Server 主链，确认后导航到导入会话。
- 已完成第二刀结构治理：`conversation_import/codex.rs` 承接 Codex state / rollout parser，`conversation_import/codex/media.rs` 承接 Codex 图片 / 附件映射，`mod.rs` 收缩为 importer facade 和 source dispatch，避免继续把 Codex 细节塞回中心文件；`codex.rs` 已从拆分预警区降到 800 行以下。
- 已按 Codex 当前记录格式补齐多模态导入：`response_item.input_image`、`event_msg.images`、`event_msg.local_images` 均进入 Lime `AgentAttachment`；图片-only 用户消息保留 `[Image]` 占位，重复的 `event_msg` / `response_item` 用户消息会合并附件并保留 `event_msg` 为主要历史来源。
- 已把导入附件贯通到 `RuntimeCore` read model：commit 阶段写入用户 `AgentInput.attachments`，`agentSession/read` 返回的用户消息包含 `content` 媒体项和 `attachments` 原始投影，便于后续多模态继续对话和证据导出复用。
- 已补前端弹窗附件计数展示，测试夹具使用 Codex 图片附件覆盖“导入前预览确认”路径。
- 已用真实项目 `/Users/coso/Documents/dev/ai/limecloud/content-studio` 的 Codex 会话完成全量 dogfood：`smoke:codex-import-content-studio` 从 15 条 active DB row 中过滤 2 条 rollout 文件缺失的 stale row，导入 13 条真实可读线程，最新复跑累计 454 条 Lime 投影消息 / 279 个 turn，覆盖 4 条多模态线程和 13 条模型 metadata 线程；验证 `confirmed=false` 会被后端拒绝，`confirmed=true` 后每条 session 均可通过 `agentSession/read` 读取，并保留 Codex 多模型 / 多模态 provenance。
- 已对齐 Codex rollout path lookup 口径：`state_5.sqlite` 中的 `rollout_path` 仅作为优先候选，路径不可读时按 `sourceThreadId` 在对应 `sessions` / `archived_sessions` 目录修复；修复后仍不存在的 stale row 不作为可导入项展示，避免用户确认后才失败。
- 已对齐 Codex 压缩 rollout 读取口径：导入 parser 支持 `.jsonl` 与 `.jsonl.zst`，压缩文件通过 `zstd` streaming decoder 只读解析；这补齐了 Codex 冷归档压缩后的 preview / commit 能力，不写回或 materialize Codex 原始目录。
- 已对齐 Codex 当前 `state_5.sqlite` schema：优先读取毫秒时间戳，保留 `model/reasoningEffort/threadSource/git/cli/preview/firstUserMessage` metadata；这让 Lime 导入不仅兼容 Codex 会话，还能承接 Lime 的多模型、多模态后续能力。
- 已补 Codex 复杂 timeline 映射：`function_call/function_call_output/custom_tool_call/custom_tool_call_output/tool_search/web_search/mcp_tool_call_end` 进入 Lime `tool.started/tool.result/tool.failed`；`exec_command` 额外进入 `command.started/command.output/command.exited`，可被 `thread_read.commands` 消费；`patch_apply_end` 进入 `patch.applied/patch.failed` 并投影 changed files；approval request 作为 imported read-only action 立即 resolved，不触发真实审批。
- 已修正 Codex 双写历史：同一用户输入同时出现在 `event_msg.user_message` 和 `response_item.message(role=user)` 时按同一 turn 合并，并把附件补齐；相同文本出现在后续真实 turn 时仍保留为独立 turn，避免导入历史丢轮次。
- 已处理真实大 rollout 性能缺口：content-studio 最大线程约 8245 行、近 2000 次 function call；全量逐事件同步写入 RuntimeCore / projection store 会触发序列校验高成本扫描。当前 commit 层改为更接近 Codex rollout/reconstruction 的分层：完整 source fidelity 与规范化 runtime event 写入 source provenance / import sidecar，默认 `agentSession/read.detail.items` 只物化前 80 个 command tool call 与前 40 个其他 tool call 的可承载窗口，避免导入超时，同时不把未物化事件标记为“已丢弃”。`conversationImport/thread/runtimeEvents/read` 已提供 current App Server 分页读取面，GUI 可以按 `sessionId + offset/limit + turnIndex/eventType` 从 sidecar 懒加载完整导入事件。
- 已补导入幂等：重复 confirmed commit 同一个 Codex `sourceThreadId` 会返回既有 Lime session，不再重复创建内容；scan / preview 会把当前 RuntimeCore 已导入 thread 标为 `imported`。
- 已补跨重启导入幂等：`import_status` 内存 miss 后会查询 `ProjectionStore` 中的 projected session metadata，按 `conversation.import + sourceClient + sourceThreadId` 复用既有 session；`read_session_current` 从 projection hydrate 时会恢复导入业务引用，确保 App Server 重启后继续对话仍继承导入上下文。
- 已补 evidence/export 闭环：导入后的 `conversation.import` session 可直接由 RuntimeCore `export_evidence` 消费，并在 session business object、event payload session metadata 和 evidence pack 上保留 Codex source provenance。
- 已补 FR-15 dry-run impact summary：preview / commit 返回结构化 `summary.dryRun`，确认弹窗展示将写入的 Lime 投影消息、turn、附件、timeline item 与 unsupported item；导入过的 thread preview 会标记为复用既有 session，重复 commit 返回同一 session 且不追加内容。
- 已补 item 级 source provenance：每条 preview message/event、导入 timeline runtime event、turn metadata 和 assistant delta 都可追溯到 Codex rollout 的 line seq、event type、payload type、call id、source path 与 source thread；这为 Agent Workspace 的 run rail、tool 下钻、evidence/replay 定位提供前置事实。
- 已补 Codex fidelity summary：preview/commit 结构化输出 messages / attachments / reasoning / tools / commands / patches / approvals / MCP / webSearch / unsupported / provenance-only，并在 commit 后补 `importedRuntimeProjection` 记录默认物化窗口与完整 sidecar/source 追溯信息；`budgetDropped` 当前保持 0，避免把未默认物化的完整来源事件误解释为永久丢失。完整事件读取已进入 App Server protocol、`packages/app-server-client`、前端 `conversationImport` API 网关与 checked-in schema manifest。
- 已再次用真实项目 `/Users/coso/Documents/dev/ai/limecloud/content-studio` 验证导入：`smoke:codex-import-content-studio` 复跑通过，13/13 线程、454 条 Lime 投影消息、279 个 turn、4 条多模态线程、13 条模型 metadata 线程；脚本同时覆盖未确认 commit 拒绝、重复导入返回同一 session、dry-run impact summary、导入后 rescan 全部为 `imported`。
- 已补导入后会话页展示投影：`agentSession/read.detail.items` 现在从 stored runtime events 生成聚合 timeline item，前端历史恢复会把 Codex reasoning、命令、补丁、web search、approval 合入同一 assistant 气泡的 `contentParts/toolCalls/actionRequests`。这直接修复“导入成功但会话页细节无法还原”的主缺口。
- 已补导入会话续聊上下文：commit 将 cwd、provider/model、reasoning、approval、sandbox、memory、AGENTS 路径等信息落到 `importedContinuation` / `importedThreadSettings`，`agentSession/turn/start` 会在没有显式 turn options 时合成中性的 imported session runtime options；除导入来源与 provenance 外，续聊运行时不再使用来源专有命名。
- 已补真实 Electron current fixture 证据：`codex-import-continuation-fixture-smoke` 使用真实 Electron preload bridge 调用 App Server JSON-RPC，验证导入后 `detail.items` 包含 reasoning / command / patch / web_search / approval，并在同一导入 session 发起 follow-up；external backend ledger 证明 `agentSession/turn/start` 收到 `imported=true` runtime metadata 和导入 cwd。2026-06-17 本轮复测通过时使用已有 Vite renderer：`node scripts/electron/codex-import-continuation-fixture-smoke.mjs --app-url "http://127.0.0.1:1420/" --timeout-ms 180000`，证据写入 `.lime/qc/gui-evidence/codex-import-continuation-fixture/codex-import-continuation-fixture-summary.json`。
- 全量产品化点击闭环第一批 P0 已由真实 Electron fixture 覆盖：从侧边栏导入弹窗开始，确认后进入会话页，观察 task rail / message list 的导入细节，再从输入框发送 follow-up；这只关闭“可点击、可还原、可续聊”的机械门禁，不把完整产品级视觉目标标记完成。剩余是 Playwright MCP / 人工截图审计 task rail 版式、长 rollout 展示密度、不同窗口尺寸下的视觉还原和真实大样本导入后的交互舒适度。
- 已补真实 Electron 点击闭环 fixture：`codex-import-click-through-fixture-smoke` 从侧边栏点击导入入口，使用临时 Codex home 触发 scan/preview，确认导入后进入会话页，断言导入消息、reasoning、友好命令记录、patch、web search、approval 与续聊输出可见；该脚本通过真实输入框发送 follow-up，并用 external backend ledger 证明 `agentSession/turn/start` 仍携带导入 cwd 与 imported metadata。
- 已把导入展示产品化断言写进点击闭环 fixture：导入预览必须展示“来源行 / 用户消息 / 助手回复”等用户可读 provenance 标签，且不得暴露 `event_msg/user_message/agent_message/response_item` raw 字段；导入后会话页必须显示友好的命令、审批、补丁、搜索和 reasoning 还原记录；消息列表主线不得出现 `imported-source-banner` 或“本地历史导入 / 已还原”独立状态条；续聊输出不得出现历史 fixture 哨兵 `CODEX_IMPORT_CLICK_THROUGH_DONE`。导入是支持能力，不进入消息主线、环境信息或运行控制面板主线状态卡。
- CI-022 当前可按 Electron GUI fixture 口径关闭；点击闭环 fixture 已补 `desktop / compact / narrow` 三种视口结构化视觉审计和截图证据，断言输入框可见可用、消息列表可见、导入细节仍可见、主线无导入 banner / run control 卡。剩余视觉层缺口是更大真实样本的 Playwright MCP / 人工截图审计，继续看 task rail 版式、长 rollout 展示密度和真实大样本导入后的交互舒适度，不再把第一批点击式可用闭环误认为全量产品化完成。
- 已修复导入 commit 后路由竞态：当 URL / initial session 已指向刚导入的 session，但当前 React session 尚未切过去时，task-center fallback restore 会以 `initial-session-pending` 跳过，避免旧首页或旧任务 tab 覆盖导入会话。
- 已修复 Codex 导入历史完成态细节默认可见：含 imported Codex metadata 的 process block 会 `defaultExpanded/forceExpanded`，timeline 展开逻辑尊重 `forceExpanded`，`command_execution` / `web_search` 转 `ToolCallState` 时保留 metadata，保证 command、patch、web search、approval 等细节在导入页可被直接看到。
- 已复跑真实 Electron 点击闭环：`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000` 通过，summary 写入 `.lime/qc/gui-evidence/codex-import-click-through-fixture/codex-import-click-through-fixture-summary.json`；本次证据 `ok=true`、`electronPreloadBridge=true`、`missingRequiredMethods=[]`、`backendSessionMatches=true`、`backendCwd=/workspace/imported-codex`，并断言 preview raw source event 隐藏、来源标签可读、导入命令友好记录可见、原始命令参数隐藏、续聊 fixture 哨兵隐藏。根据产品定位，导入支持能力不再出现在环境 / 运行控制面板独立主线卡。
- 已按产品边界撤回 `imported-source` projection：导入支持能力不进入消息主线、环境信息或运行控制面板独立状态卡；命令记录和审批记录五语言文案继续使用“本地历史导入 / local history”作为工具 / 审批只读来源说明，已处理审批在 task rail 摘要中只显示“导入的权限记录”，不在导入详情外露来源品牌或原始审批命令。
- 已完成本轮视觉审计：Playwright MCP 因当前 profile 被占用无法复用，未关闭未知浏览器进程；本轮用真实 Electron fixture 截图 `.lime/qc/gui-evidence/codex-import-click-through-fixture/codex-import-click-through-fixture.png` 审计导入会话页，未见来源品牌泄漏、明显重叠或输入框遮挡。左侧对话 shelf 刷新缺口已关闭：独立导入会话即使带 `working_dir` 且未匹配已打开项目，也会落回普通“对话”区；导入 commit 后前端先做窄 optimistic overlay，待 App Server session list 返回同 id 后自动校正，不新增第二套 transcript store。
- 已复跑真实 Electron 点击闭环并补侧栏可发现性证据：`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000` 通过，summary `.lime/qc/gui-evidence/codex-import-click-through-fixture/codex-import-click-through-fixture-summary.json` 中 `sidebarImportDiscoverabilitySummary.importedEntryVisible=true`、`emptyStateOnly=false`、`readModelSummary.hasCommandItem/hasPatchItem/hasWebSearchItem/hasApprovalItem=true`、`backendCwd=/workspace/imported-codex`。
- 已完成侧栏导入 controller 第一刀拆分：新增 `useAppSidebarConversationImport`，把导入弹窗 open/project 状态、成功 toast、侧栏 optimistic session、session list 刷新从 `AppSidebar.tsx` 移出；`AppSidebar` 只保留导入成功后的页面导航边界。该拆分不改变协议、不新增 mock、不改变导入确认弹窗行为，为后续批量导入和其他来源导入避免继续膨胀主侧栏文件。复跑真实 Electron 点击闭环通过，summary `.lime/qc/gui-evidence/codex-import-click-through-fixture/codex-import-click-through-fixture-summary.json` 中 `missingRequiredMethods=[]`、`sidebarImportDiscoverabilitySummary.importedEntryVisible=true`、`backendMetadataImported=true`、`backendCwd=/workspace/imported-codex`。
- 已修复代码产物工作台 fixture 对 current GUI 的两个误判：真实会话打开不再依赖输入框隐藏 `data-session-id` 立即水合，而是以侧栏 active conversation + conversation/canvas shell 为主证据；工具 timeline GUI 证据会先展开过程组再检查输出预览，避免把“后端已持久化但用户看不到”误判为通过或失败。
- 已完成本轮回归：`npm run smoke:code-artifact-workbench-electron-fixture` 通过，summary 断言 `toolTimelineEvidencePresent=true`、`codingChanges/Outputs/LogsEvidencePresent=true`、`codingRecoveryReachedBackend=true`；`node scripts/electron/code-artifact-workbench-fixture-smoke.mjs --scenario gui-coding-input --timeout-ms 180000` 通过；`npm run smoke:agent-runtime-current-fixture` 通过，覆盖历史恢复、流式终态、消息列表终态、代码工作台 Electron fixture 和停止后同会话继续输出。
- 已收敛非导入通用命名：输入框命令代码与帮助文案改为中性 `SlashCommand` / “输入命令”；provider、settings 和 MCP 页面把来源品牌展示改为“本地 CLI”；工具展示通用 helper 改为 `ImportedSource*`；Provider 兼容 issue code 和真实 Provider 错误文案改为“local_cli / 本地 CLI”；通用测试描述不再用来源品牌描述顶部标签、输入浮层、hover footer 或 proposed_plan。保留范围仅限导入来源、provenance、fixture、路线图说明、底层协议枚举和模型 ID。
- 已补本地化防回流与可见 fixture 中性化：五语言 i18n value guard 禁止普通展示文案直接写来源品牌；导入点击 fixture 的标题、搜索 query、帮助文案、warning 和预览标题均改为“本地历史 / 导入细节还原”；Workspace 消息主线不再展示导入摘要 banner，“本地历史导入 / 已还原 / 消息 / 命令 / 补丁”等中性字段只保留在导入预览、timeline、只读工具 / 审批记录和 evidence/provenance。保留范围仍限定为导入来源、provenance、脚本入口、路线图说明、底层协议枚举和模型 ID。
- 已补导入弹窗来源标识防泄漏：可见标题 fallback 不再使用原始来源 thread id，预览 meta 只显示“历史对话 · 时间”，列表副标题 fallback 只显示时间、cwd 或“本地历史”；侧栏导入回归新增 DOM 断言，确认预览和项目会话区不出现来源品牌、默认数据目录或来源 thread id。底层 API 参数、source client、thread id 和 provenance 保持不变，确保导入主链仍可幂等和追溯。
- 已完成导入弹窗 view model 第一刀拆分：新增 `conversationImportDialogViewModel.ts` 和单元测试，把标题 / 副标题 / source label / preview meta / provenance 标签 / 截断 / 默认选择策略从 `AppSidebarConversationImportDialog.tsx` 移出；弹窗从 956 行降到 793 行，回到 800 行预警线以下。`provenance.call` 五语言文案同步改为“调用记录 / Call record”，不再在可见层回显 fallback call id，底层 provenance 不变。
- 已修复导入续聊后的历史过程投影：`phase: "final"` 与 `final_answer` 同义，历史 hydrate 可从 `contentParts` 最后一条 text 生成首帧预览；导入来源 process block 即使混合 reasoning / assistant text，也会保留“导入的命令记录”入口；patch timeline 优先投影为 `file_changes_batch`，不再被普通工具过程吞掉。
- 已修复环境信息来源泄漏：`generalWorkbenchTaskRailContextViewModel` 会跳过导入 provenance 的 source path / source thread id，不再把 rollout 文件名或来源 thread id 放进普通 task rail 来源摘要；保留 web search query 等真实资料来源。Electron fixture 同步断言环境弹层不得出现来源 thread id。
- 已复跑真实 Electron 点击闭环：`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000` 通过，summary `.lime/qc/gui-evidence/codex-import-click-through-fixture/codex-import-click-through-fixture-summary.json` 中 `ok=true`、`electronPreloadBridge=true`、`readModelSummary.hasCommandItem/hasPatchItem/hasWebSearchItem/hasApprovalItem=true`、`environmentPopoverSummary.hidesImportedRunControlCard=true`、`visualAuditSummary` 的 `desktop / compact / narrow` 均通过，且 `taskRailText` 只显示 `search_query` 作为来源摘要，不再显示来源 thread id。
- 已把历史 timeline 到正文 `contentParts` 的构造从 `messageListItemProjection.ts` 拆到 `messageListTimelineContentParts.ts`：主投影文件从 1000 行以上降到 903 行，投影单测从 1000 行以上降到 929 行；导入命令 metadata 回归迁到 `messageListItemProjection.imported.unit.test.ts`，动态 MCP / 导入 patch 交错顺序回归迁到 `messageListTimelineContentParts.unit.test.ts`。
- 已修正导入 patch 的文件变更还原口径：Codex 导入只有 `paths` 而没有真实 diff metadata 时，正文渲染为路径级 `file_changes_batch`，不再把 `Patch changed ...` 说明文本伪造成 `+1` diff；真实 diff 仍由 `metadata.file_change` 或文件工具参数路径进入聚合器。
- 已再次复跑真实 Electron 点击闭环：`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000` 通过，summary `ok=true`；`readModelSummary` 同时包含 imported/continue 的 user/assistant message，以及 reasoning、command、patch、webSearch、approval item；`desktop / compact / narrow` 三视口均确认命令记录、补丁、搜索证据、审批摘要可见，`importedBannerVisible=false`、`importedRunControlVisible=false`、输入框可见且未遮挡主内容。
- 已复核“除导入外不出现来源品牌字眼”的产品边界：普通用户 GUI 和五语言展示文案继续保持中性；MCP / Provider 页面展示为“本地 CLI”，导入弹窗展示为“本地历史”；协议枚举、provider type、`sourceClient`、fixture、路线图、治理事实源和真实模型 ID 保留来源名，避免破坏导入幂等、provenance、模型选择和 App Server contract。
- 已修复真实大样本 dogfood 可重复性：`smoke:codex-import-content-studio` 默认使用临时 App Server data-dir，不再被本机既有导入状态污染；同一隔离运行内仍覆盖首次导入、未确认拒绝、重复导入幂等和导入后 rescan。2026-06-17 连续复跑通过，真实 content-studio 样本为 13/13 线程、458 条 Lime 投影消息、281 个 turn、4 条多模态线程、13 条模型 metadata 线程。
- 已清掉本轮阻塞的前端类型门禁：`DecisionPanel` 导入只读审批文案不再把 i18next 重载类型带入 helper，`StreamingRenderer` 导入审批测试改用 `submittedResponse` current 字段，侧栏 session 时间排序显式收窄 number；`npm run typecheck` 已通过。
- 已完成本轮 current 主线回归：`npx vitest run "src/components/agent/chat/components/MessageList.test.tsx" "src/components/agent/chat/components/messageListItemProjection.unit.test.ts" "src/components/agent/chat/components/messageListItemProjection.imported.unit.test.ts" "src/components/agent/chat/components/messageListTimelineContentParts.unit.test.ts" "src/components/agent/chat/components/StreamingRenderer.test.tsx" "src/components/agent/chat/components/DecisionPanel.test.tsx" "src/components/app-sidebar/sidebarSessions.test.ts" --silent=passed-only --disableConsoleIntercept` 通过，236 个测试覆盖导入正文投影、命令/搜索/patch/approval 展示和侧栏会话排序。
- 已复跑真实大样本与 current runtime 聚合门禁：`npm run smoke:codex-import-content-studio` 通过，13/13 真实 content-studio Codex 线程、458 条投影消息、281 个 turn；`npm run smoke:agent-runtime-current-fixture` 通过，覆盖历史/缓存恢复、流式终态、Electron 代码工作台、Claw 停止后继续输出，确认导入主线没有破坏当前 Agent Runtime。
- 已补产品视觉边界机械审计：新增 `smoke:local-history-import-visual-audit`，复用真实 Electron 点击闭环后扫描 GUI 展示快照和三视口 summary；本轮修掉 fixture 可见补丁路径里的来源品牌测试目录，最新 summary `ok=true`、`visibleTextLeaks=[]`、`desktop / compact / narrow` 均确认输入框可用、消息列表可见、命令 / 补丁 / 搜索 / 审批记录可见、无导入主线 banner / run control 卡。
- 已关闭点击闭环脚本体量风险：`codex-import-click-through-fixture-smoke.mjs` 已从 1952 行拆到 850 行，新增 `local-history-import-click-through-fixture.mjs` 承接临时本地历史源 / external backend fixture，新增 `local-history-import-click-through-gui.mjs` 承接弹窗确认、续聊和三视口 GUI 审计，`local-history-import-smoke-utils.mjs` 承接 App Server JSON-RPC / renderer ready / shared audit helper。真实 Electron 点击闭环复跑通过，summary `.lime/qc/gui-evidence/codex-import-click-through-fixture/codex-import-click-through-fixture-summary.json` 中 `ok=true`、`consoleErrors=[]`、`visualAudit` 覆盖 3 个视口。
- 已补真实 content-studio 长历史 GUI 审计：`smoke:local-history-import-real-sample-visual-audit` 从真实本地历史源只读扫描并导入最长/最复杂样本，Electron GUI 九张截图全部通过；审计发现裸 `.codex` 可能来自导入正文原文，已把泄漏边界收敛为具体 source path/thread/raw event 字段和来源状态文案，避免破坏导入保真。summary `ok=true`、`leakedTokens=[]`、`consoleErrors=[]`、输入框 / 消息列表可见、无导入主线 banner / run control 卡。
- 已修复真实 Electron 审计暴露的 Desktop Host 日志崩溃：`report_frontend_debug_log` 不再因 stdout/stderr 已关闭的 `EPIPE` 把 main process 打崩；本地 `electron:build:host:dev` 已刷新运行产物。
- 已修复连续发送 / 历史恢复时的消息归组串线：实时提交阶段会给本地用户草稿和助手草稿写入同一个 `pendingTurnKey`，App Server read model 同步给 user / assistant messages 返回 `runtimeTurnId` 与 `runtime_turn_id`，前端 hydrate 保留该字段，`MessageList` 分组优先按 turn id 回挂 assistant 消息；无 turn id 的旧消息仍走原顺序兜底。这关闭了“第一条消息回复被第二条用户消息刷没 / 挂错组”的主路径缺口，不改变导入支持能力的产品层级。
- 已修复 timeline 首句时序缺口：`phase=commentary` 的 assistant 文本现在按 thread item 顺序进入 inline content parts，工具、命令、patch、approval、web search 仍保留在同一条过程流中；最终正文 / 复制 / 引用仍只使用 final answer，避免 commentary 污染正式答复。2026-06-17 复跑真实 content-studio 长样本 Electron 审计和导入点击续聊 fixture 均通过，summary `ok=true`、`consoleErrors=[]`；当前稳定 fixture 未复现用户截图里的“当前处理失败”，后续若 live provider 继续失败，应追 `agentSession/turn/start -> turn_failed` 的 provider/runtime error，而不是再把失败文案当导入显示问题处理。
- 已修复导入续聊模型选择缺口：导入来源 runtime 的 `provider_name/model_name` 只表示来源上下文，不再伪装成当前 Lime 会话模型偏好；没有当前 `provider_selector` 时，续聊 submit op 必须继续提交用户当前选择的 provider/model，避免后端因缺少有效模型选择进入“当前处理失败”。这保持了对来源历史的保真，同时不破坏 Lime 多模型、多模态续聊主线。
- 已完成后端导入 provenance owner 收口：`conversation_import/provenance.rs` 统一承接 source metadata、warnings、turn runtime options、continuation context 和 item source provenance helper；`commit.rs` 只保留写入流程，`codex/events.rs` 保留 raw event 映射并委托公共 provenance helper。Rust 定向测试已覆盖 runtime event 时序、重新导入幂等和 evidence source provenance。
- 已修复真实长样本导入 GUI 过程记录缺失：导入来源的 command/tool/patch/approval 即使没有额外 agent_message 也会生成 inline process parts，不再被普通长历史压缩吞掉；真实 content-studio 长样本 GUI audit 和 14/14 批量导入 smoke 均通过。
- 已修复后端导入时序和失败误报根因：`response_item` 先于 `event_msg` 的同一句用户输入现在仍合并到同一 turn，导入 turn 使用来源用户消息 timestamp；未闭合工具 / 命令 / patch lifecycle 以只读不完整导入记录收口，不再 synthetic 成 `failed` timeline item。Rust runtime event 定向测试和真实 content-studio 14/14 批量导入 smoke 已通过。
- 已完成侧栏会话 action controller 第一刀拆分：新增 `useAppSidebarConversationActions`，把会话导航、新建、搜索结果点击、重命名、归档和删除从 `AppSidebar.tsx` 移出；`AppSidebar.conversations.test.tsx` 与相关 ESLint 已通过。`AppSidebarConversationShelf.tsx` 仍约 1000 行，后续继续拆菜单 / row view model。
- 已完成侧栏项目会话组拆分：新增 `AppSidebarProjectConversationGroups.tsx` 与 `sidebarProjectDisplayName.ts`，把项目分组折叠、项目内新建对话、项目菜单入口和对应样式从 `AppSidebarConversationShelf.tsx` 移出；`AppSidebarConversationShelf.tsx` 从 1005 行降到 858 行，低于 1000 行硬边界。该刀不改变导入弹窗协议和文件工具轨，只为后续批量导入 / 多来源导入避免继续膨胀侧栏主组件。
- 已补导入会话文件对象真实打开证据：`codex-import-click-through-fixture-smoke` 的本地历史 fixture 现在生成真实 Markdown / HTML / DOCX 文件，并在导入 timeline 中保留 `read_file` 工具轨；Electron GUI helper 点击 `inline-tool-open-file` 后统一走 Workspace Preview Artifact / Workbench 链路，断言 Markdown 与 DOCX 正文可见、HTML iframe 预览可用、DOCX 不显示 ZIP/OpenXML 噪音。这关闭“导入消息能看，但工具轨文件/DOCX/HTML 不能正常在 Lime 打开”的证据缺口；仍不表示所有未来 source 类型已全量覆盖。
- 已修复导入过程组 reasoning 被文件工具轨覆盖的展示缺口：同一导入回合内 reasoning、命令、多个 `read_file`、搜索和 patch 混合时，MessageList 仍按事件序把 reasoning 投影到 assistant `contentParts`；StreamingRenderer 在导入工具组摘要中保留“已完成思考”，并对同组 thinking 保留来源原文，避免普通英文内部思考过滤把导入原始 reasoning 吃掉。前端定向回归已通过，下一步仍以真实 Electron 点击闭环 smoke 作为 GUI 交付门槛。
- 已修复导入文件打开入口的后端参数丢失：App Server read model 会把 `tool.started.arguments` 写入 `thread_read.tool_calls`，并在 `tool.result` 合并完成态后保留 `arguments.path`。历史 hydrate 因此可以从 completed `toolCall` 恢复 `read_file` 文件路径，不再只看到输出摘要却没有可点击文件入口。
- 已修复 preview artifact 工作台选中态漂移：普通文件 / HTML / DOCX preview artifact 打开时会同步发送 `previewOpenRequest.selectionKey=artifact:<id>`，让 Canvas Workbench 从上一张图片、审查或日志面板切到当前文件；媒体 preview artifact 继续走 `renderMode=media` 的显式 selection request，避免图片附件被普通文件打开逻辑抢焦点。最新真实 Electron 点击闭环通过：`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000`，summary `ok=true`、`consoleErrors=[]`、`openedAllImportedPreviewArtifacts=true`，`readModelSummary` 同时包含 reasoning / command / patch / webSearch / approval / imported attachment，backend ledger 证明同一导入 session 续聊继承 imported metadata 与 cwd。
- 已修复导入计划快照投影缺口：来源 `update_plan` 不再只作为普通工具轨出现，App Server 会额外生成 `plan.final` 并投影到 `detail.items[type=plan]`；assistant 气泡复用 `<proposed_plan>` 渲染计划块，右侧任务轨从 `plan` item metadata 恢复结构化步骤。该刀避让 `read_file` 并行写集，只收敛 plan/update_plan 事件、timeline 和任务轨恢复链路。
- 已修复真实 content-studio 长样本导入 commit 性能缺口：`conversationImport/thread/commit` 从逐事件写入 / 投影改为批量 event log append + projection transaction，同时按整条导入会话共享默认物化 selector，避免每个 turn 重建窗口导致长线程过量物化。完整 normalized event 仍写入 sidecar，下钻接口继续可读默认窗口外细节；真实大样本 visual audit 已用 `/Users/coso/Documents/dev/ai/limecloud/content-studio` 复跑通过，summary `ok=true`、`consoleErrors=[]`、完整记录下钻无 source/raw 泄漏。
- 已补 Codex 导入 Fidelity 验收矩阵：把全量导入主线拆成可机械验证的 current surface，后续新增事件类型或 preview artifact 扩展必须先回填矩阵，再允许进入 PRD / tracker 叙述层。
- 已补本轮 i18n / protocol / typecheck 收口：工具搜索 deferred 文案、记忆证据默认路径、artifact preview toast 和设置页记忆审阅文案均进入 current 五语言资源；`messageListTimelineContentParts` 修复导入过程 + final text 保留，避免为隐藏正文而制造错序；`packages/app-server-client` 重新生成 App Server protocol types，补齐 `memoryStore/review/*` current 类型。验证已通过 `npm run typecheck -- --pretty false`、`npm run i18n:check:json`、`npm run test:contracts`、相关 Vitest 定向回归、ESLint 和 `git diff --check`。
- 已恢复 i18n unused 门禁可信度：`scripts/i18n/i18n-unused-key-check.ts` 现在能识别 `resolveAgentChatCopy` / `resolveRequiredAgentChatCopy` 的 `agentChat.` 前缀、工具展示 partial key，以及 `resolveProcessSummaryCopy` / `resolvePhasedProcessSummaryCopy` 派生的 `WithSubject` / `pre` / `post` 文案；五语言资源同步删除导入弹窗精简后不再使用的旧说明、旧 fidelity 明细、旧 memory 侧栏项和旧 skills suggestion 文案。验证已通过 `npm run i18n:unused -- --check`、`npm run i18n:check:json`、`npx vitest run "src/i18n/__tests__/loadNamespace.test.ts" "src/i18n/__tests__/types.test.ts" "scripts/i18n/i18n-unused-key-check.test.ts" --silent=passed-only --disableConsoleIntercept`。
- 已补 2026-06-19 当前质量复核：`npm run typecheck -- --pretty false` 在当前大工作树通过；`npm run test:contracts` 通过，App Server client contract 278 项、command contracts、harness、modality、scripts governance、Electron release workflow、cleanup report 和 docs boundary 均通过；`npm run i18n:unused -- --check` 保持 unused=0；`npm run i18n:check:json` 保持五语言 coverage 100%、missing=0、extra=0；`npx vitest run "scripts/i18n/i18n-unused-key-check.test.ts" "src/i18n/__tests__/loadNamespace.test.ts" "src/i18n/__tests__/types.test.ts" --silent=passed-only --disableConsoleIntercept` 通过，22 tests passed；全仓 `git diff --check` 通过。

## 拆分入口与状态

| 状态 | 拆分项                                                        | 目的                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---- | ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| done | `conversation_import/commit.rs`                               | 隔离写入会话主链，避免 facade 膨胀。                                                                                                                                                                                                                                                                                                                                                                  |
| done | `conversation_import/codex.rs`                                | 把 Codex state DB / rollout parser 从 facade 拆出，便于继续对齐 Codex。                                                                                                                                                                                                                                                                                                                               |
| done | `conversation_import/codex/media.rs`                          | 隔离 Codex `input_image/images/local_images` 到 Lime attachment 的映射，避免 Codex parser 再次膨胀。                                                                                                                                                                                                                                                                                                  |
| done | `conversation_import/codex/paths.rs`                          | 隔离 Codex rollout path repair / active-archived lookup，避免 parser 文件继续膨胀并对齐 Codex filesystem fallback。                                                                                                                                                                                                                                                                                   |
| done | compressed rollout parser                                     | 支持 `.jsonl.zst` 透明解压读取，保持 Codex 原始目录只读，不 materialize 压缩文件。                                                                                                                                                                                                                                                                                                                    |
| done | `conversation_import/codex/events.rs`                         | 隔离 Codex raw rollout item 到 Lime runtime events 的映射，覆盖 tool / command / patch / action。                                                                                                                                                                                                                                                                                                     |
| done | `conversation_import/codex/dry_run.rs`                        | 隔离 dry-run impact projection，区分 Codex raw message item 与 Lime commit 后实际写入消息 / turn / attachment / timeline 计数。                                                                                                                                                                                                                                                                       |
| done | `conversation_import/commit_events.rs`                        | 隔离导入 runtime event 预算、lifecycle 补齐和 synthetic imported event，避免 `commit.rs` 膨胀并保持 RuntimeCore 全局校验不放松。                                                                                                                                                                                                                                                                      |
| done | `conversation_import/import_status.rs`                        | 隔离导入状态叠加与同进程幂等查重，避免 scan / commit 继续互相耦合。                                                                                                                                                                                                                                                                                                                                   |
| done | `conversation_import/provenance.rs`                           | 集中 source metadata、warnings、continuation runtime options、item source provenance 和 compact JSON helper；`commit.rs` / `codex/events.rs` 不再各自维护来源证明逻辑。                                                                                                                                                                                                                               |
| done | cross-restart import idempotency                              | `ProjectionStore` 已提供窄查询，按 projected session metadata 查 `conversation.import + sourceClient + sourceThreadId`；projection hydrate 也会恢复导入业务引用，避免重启后重复导入或续聊丢上下文。                                                                                                                                                                                                   |
| done | dry-run summary                                               | `ConversationImportPreviewSummary.dryRun` 已显式返回 commit impact summary；artifact 仍以 timeline/source provenance 为主，独立 artifact sidecar 计数等待后续 artifact importer。                                                                                                                                                                                                                     |
| done | per-item source provenance                                    | `ConversationImportSourceProvenance` 已覆盖 preview message/event、timeline runtime event、turn metadata 和 assistant delta；source path hash 可在后续 evidence sidecar 中按隐私策略追加。                                                                                                                                                                                                            |
| done | imported session timeline projection                          | `runtime/thread_item_projection.rs` 统一从 `StoredSession.events` 生成 `detail.items`，让导入历史和 live runtime 使用同一 GUI 消息恢复路径。                                                                                                                                                                                                                                                          |
| done | imported session continue Electron fixture                    | 真实 Electron preload bridge 已验证导入会话的 `agentSession/read.detail.items` 还原与同 session `agentSession/turn/start` 续聊，backend ledger 覆盖 imported metadata 和 cwd 继承。                                                                                                                                                                                                                   |
| done | imported session click-through Electron fixture               | 补完整用户点击证据：从侧边栏导入弹窗确认进入会话页，观察导入细节，再从输入框发送 follow-up；不再用 API fixture 直接替代用户路径。                                                                                                                                                                                                                                                                     |
| done | imported session route guard                                  | commit 后在目标 session 尚未完成水合前阻止 task-center fallback 恢复旧 tab，保证确认导入后稳定停在导入会话页。                                                                                                                                                                                                                                                                                        |
| done | imported Codex timeline default visibility                    | Codex imported process block 在完成态历史中默认/强制展开，并保留 tool metadata，保证导入的 command、patch、web search、approval 细节默认可见。                                                                                                                                                                                                                                                        |
| done | message timeline contentParts projection                      | `messageListTimelineContentParts.ts` 已承接 historical timeline 到 assistant 正文 parts 的构造，覆盖 text / thinking / command / web_search / action / patch；导入 patch 只有路径时保留路径级文件变更卡，不伪造 diff。                                                                                                                                                                                |
| done | commentary timeline text ordering                             | `phase=commentary` 的 agent_message 已按 thread item 事件序进入 inline 正文过程，修复工具前首句被丢弃 / 积压到后面的显示问题；final answer 仍是 `actionContent` 唯一来源。                                                                                                                                                                                                                            |
| done | imported turn ordering and incomplete lifecycle normalization | commit 阶段按来源用户消息 timestamp 写入 turn，并双向合并 `event_msg` / `response_item` 的同一句用户输入；未闭合工具 / 命令 / patch lifecycle 以 `importedIncomplete` 诊断 metadata 收口，不再显示为当前执行失败。                                                                                                                                                                                    |
| done | imported reasoning display metadata                           | 导入来源 `reasoning` 的 item metadata 已进入 `thinking` content part，渲染层只对导入 provenance 或同组导入工具轨默认展开并保留原始思考过程；普通非导入短 reasoning 继续保持安全折叠。                                                                                                                                                                                                                 |
| done | imported update_plan projection                               | 来源 `update_plan` 额外投影为 App Server `plan.final` 与 thread `plan` item，assistant inline 过程复用现有 proposed plan 渲染，任务轨从结构化 metadata 恢复多步骤计划；普通工具轨不重复展示该计划工具。                                                                                                                                                                                               |
| done | imported preview artifact selection                           | `thread_read.tool_calls` 保留 `tool.started.arguments`，前端文件 preview artifact 打开统一发送 `artifact:<id>` selection request；真实 Electron 点击闭环已验证图片附件、Markdown、HTML、DOCX 和同 session 续聊。                                                                                                                                                                                      |
| done | imported runtime lazy detail pagination                       | 已按 Codex rollout/reconstruction 口径把完整规范化 runtime event 写入 import sidecar，并在 `importedRuntimeProjection` 记录默认物化窗口；新增 current `conversationImport/thread/runtimeEvents/read`，协议、App Server handler、`packages/app-server-client`、`src/lib/api/conversationImport.ts` 和 schema manifest 已同步，支持按 session / offset / limit / turnIndex / eventType 分页读取未默认物化的命令、工具、reasoning、patch 和 approval 细节。默认会话页仍只展示可承载窗口，完整细节由 GUI 下钻懒加载。 |
| done | imported runtime detail GUI drilldown                         | 后端/API 已能分页读取完整导入事件；GUI 已在 Task Center 来源区接入懒加载下钻，默认只显示中性来源事实，展开后可按 session / offset / limit 读取默认窗口外的完整记录，并补了定向前端回归。默认会话页仍不承诺“任意历史细节全量可见”，完整记录需在来源区手动展开。                                                                                                                                                                             |
| done | imported specialized runtime item projection               | 已把 `mcp_tool_call_*`、`dynamic_tool_call_*`、`view_image_tool_call`、`image_generation_*`、`context_compacted`、`entered_review_mode`、`exited_review_mode`、`subagent_activity` 和 collab begin/end 事件映射进 current runtime 投影；前端 `messageListTimelineContentParts` 已补 `context_compaction` / `subagent_activity` 导入回归，Task Center 来源区完整记录下钻也能读取这些默认窗口外的运行事件。真实 Electron 点击闭环已覆盖导入细节、文件预览和同 session 续聊。 | `cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server runtime::conversation_import::tests::runtime_events::commit_projects_codex_runtime_specialized_items_into_existing_timeline_types --lib`、`npx vitest run "src/components/agent/chat/components/TaskCenterUtilityToolbar.integration.test.tsx" "src/components/agent/chat/components/ImportedRuntimeEventDetailPanel.test.tsx" "src/components/agent/chat/components/messageListTimelineContentParts.unit.test.ts" --silent=passed-only --disableConsoleIntercept`、`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000` |
| done | imported runtime detail ordering stability                  | 详情面板合并后再次按 `sourceEventIndex -> turnIndex -> eventIndex -> eventType` 稳定排序，避免分页拼接或未来接口变化把最先的历史事件压到后面；完整记录从原始 JSON 列表升级为语义卡片，按 `kind / status / facts / raw payload` 展示，并隐藏 source path / thread id / session id 等来源标识。摘要分母改用 `sourceRuntimeEvents`，不再把当前页 `totalEvents` 误显示成完整来源规模。 | `npx vitest run "src/components/agent/chat/components/importedRuntimeEventDetailViewModel.test.ts" "src/components/agent/chat/components/ImportedRuntimeEventDetailPanel.test.tsx" "src/components/agent/chat/components/TaskCenterUtilityToolbar.integration.test.tsx" --silent=passed-only --disableConsoleIntercept`、`git diff --check`、`npm run smoke:codex-import-click-through-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000` |
| done | real sample runtime detail drilldown guard                  | `smoke:local-history-import-real-sample-visual-audit` 已扩展真实大样本下钻守卫：从真实 content-studio 样本导入后会在 GUI 中展开“完整记录”，断言语义事件卡片、`data-event-kind`、facts、payload preview 和来源规模摘要可见，同时继续扫描 source path / source thread / raw 字段泄漏。2026-06-19 已用真实 `/Users/coso/Documents/dev/ai/limecloud/content-studio` 样本复跑通过：read model 92 messages / 1901 items，完整记录下钻首屏 10 events，probe 保留完整事件总量，包含 `message/tool/command` kind，facts / payload / source summary 均存在，未发现 source/raw 泄漏。 | `APP_SERVER_BIN="/tmp/lime-app-server-projection-target/debug/app-server" npm run smoke:local-history-import-real-sample-visual-audit -- --timeout-ms 300000 --commit-timeout-ms 240000`、`npx vitest run "scripts/electron/local-history-import-real-sample-visual-audit-smoke.test.mjs" --silent=passed-only --disableConsoleIntercept`、`git diff --check -- "scripts/electron/local-history-import-real-sample-visual-audit-smoke.mjs" "scripts/electron/local-history-import-real-sample-visual-audit-smoke.test.mjs"` |
| done | imported support boundary in workspace surface                | `imported-source` 已从消息主线、环境信息 / 运行控制面板 surface 撤回；导入来源只在导入预览、timeline、只读工具 / 审批记录和 evidence/provenance 中展示。                                                                                                                                                                                                                                              |
| done | imported session visual audit guard                           | `smoke:local-history-import-visual-audit` 已把 Electron 点击闭环的三视口结构化截图、输入框 / 消息列表 / 导入细节可见性和 GUI 可见文本来源品牌边界变成可重复守卫。                                                                                                                                                                                                                                     |
| done | imported source metadata signal                               | `importedSourceMetadata.ts` 集中判断导入来源 metadata；timeline 过程组、工具详情和工具批次标题复用同一事实源，不再各自写死单一来源客户端，后续 Claude Code 导入可复用同一只读展示边界。                                                                                                                                                                                                                 |
| done | real sample visual audit                                      | `smoke:local-history-import-real-sample-visual-audit` 已覆盖真实 content-studio 最复杂样本：真实 Electron + 隔离 App Server + 九张多视口/滚动截图，验证长 rollout 导入后输入框、消息列表、timeline 细节、泄漏边界和 console error。                                                                                                                                                                   |
| done | click-through fixture helper split                            | `codex-import-click-through-fixture-smoke.mjs` 已降到 850 行；fixture 数据、GUI 点击 / 视觉审计和通用 App Server / renderer helper 已拆到 `scripts/electron/lib/`，后续扩展继续按场景编排 + helper 分层。                                                                                                                                                                                             |
| done | UI modal view model                                           | `conversationImportDialogViewModel.ts` 已承接导入弹窗纯展示投影和来源标签防泄漏规则；后续批量导入、Claude Code、批量附件筛选应继续先扩展 view model / projection，不把状态机和展示规则塞回弹窗。                                                                                                                                                                                                      |
| done | Sidebar feature split                                         | 已先把导入弹窗状态和导入成功副作用抽到 `useAppSidebarConversationImport`，并把会话导航、新建、搜索结果点击、重命名、归档、删除抽到 `useAppSidebarConversationActions`；项目会话分组继续拆到 `AppSidebarProjectConversationGroups.tsx`，`AppSidebarConversationShelf.tsx` 已降到 858 行。后续如新增批量导入 / 多来源导入，只允许扩展对应 feature controller / view model，不再把状态机塞回侧栏 shelf。 |
