# Agent App Runtime 完成审计

> 状态：current-key-action-verified-with-full-flow-and-stabilization-gaps
> 更新时间：2026-05-17
> 审计目标：验证“Agent App 在业务 App 内完成 AI Agent 工作，且复用 Lime 完整 AgentRuntime / Claw / Tools / Evidence 能力，不跳回通用 Chat、不退化成模型 API 壳”是否已经真正完成。

最新验收结论：当前 key-action 口径已能证明内容工厂 App 通过 Lime Host 真实 AgentRuntime 完成主要 AI 任务，而不是跳回通用 Chat 或直连模型 API。`run-production`、`only-copy`、`run-scripts`、`run-strategy`、`run-review` 已分别有 completion E2E 证据；required Skills 已从 prompt 提示升级为 Runtime pre-execution contract；Host Run UI 已复用 Claw 的工具名、工具过程摘要、Tool / Skill 步骤、ThinkingBlock 与 MarkdownRenderer；模型、Token、费用、Skill、Artifact、Evidence、workspace patch 均由 Lime Host / Runtime 投影。最新 direct runtime postcheck 进一步确认 `run-production / only-copy` 的 `agent_app_runtime_get_task` 后续快照都能看到 `contentFactoryWorkspacePatch / workspacePatch`。但整体目标仍不能标记完成：仍缺同一项目上下文里的单会话全流程串联、终态手动展开视觉断言、smoke direct snapshot stabilization、以及 Host Run renderer 完整抽成所有 Agent App 可复用 SDK 组件。

## 1. 目标拆解

整体目标可落成以下交付标准：

1. **边界清晰**：Lime Agent 负责运行事实、工具、权限、证据和 Claw 能力复用；App 负责业务 UI、表单、项目状态、资料版本、产物展示和人工确认。
2. **App 内闭环**：内容工厂等 App 不跳回 Lime 通用 Chat；用户在 App 页面内发起任务、看进度、补上下文、确认、取消、重试和保存结果。
3. **完整 AgentRuntime**：App 的 AI 任务走 `agent_app_runtime_* -> agent_runtime_submit_turn` current 主链，而不是只拿 `LIME_GATEWAY_*` 低阶模型 API。
4. **Claw 能力复用**：搜索、图片、研报、PDF、总结等能力通过 capability hints / catalog 复用现有 Claw skill launch metadata，不复制 `*_skill_launch.rs`。
5. **产物和证据落库**：任务结果、人工确认、运行事件、artifact 和 evidence 能回投到 App 页面与 `lime.storage / lime.artifacts / lime.evidence`。
6. **可恢复**：刷新或 Host 重建后，App 仍可凭 `taskId` 找回 `sessionId / turnId` 并继续读取任务或提交 Host response。
7. **可验证**：命令四侧、前端 Host Bridge、Rust facade、内容工厂包、文档事实源都有对应测试或检查。

## 1.1 2026-05-17 完成度复核

本节只记录本轮可复核的真实证据，不把“测试通过”本身当成完成。每项都必须能映射到用户明确要求：不跳回 Chat、不直连 API、真实使用 Lime AI Agent / Skill / ToolRuntime、像 Claw 一样展示过程、模型/Token/费用可见、Artifact/workspace patch 可回写、并且后续 App 不重复实现底层运行 UI。

| 成功标准 | 当前证据 | 覆盖判断 | 未覆盖 / 弱证据 |
| --- | --- | --- | --- |
| App 内完成 Agent 工作，不跳回 Lime 原 Chat | `AgentAppRuntimePage` Host iframe + `lime.ui.openAgentRun/updateAgentRun/closeAgentRun`；内容工厂 completion E2E 保持在 iframe 内执行 | key actions covered | 还缺同一项目上下文从知识库到复盘的连续旅程 |
| 不直接调 provider API，必须走完整 Lime AgentRuntime | `agent_app_runtime_start_task -> agent_runtime_submit_turn` current facade；completion E2E 看到 hidden `agent-app-runtime-*` session、runtime facts、task subscription | covered | 后续仍需把“禁止 App 私接模型 API”固化成 manifest/lint gate |
| 必须真实使用 Skills，而不是 prompt 里写 required skill | `runtime_turn.rs` pre-execution 通过 `LimeSkillTool` 执行 required Skills；`run-production / only-copy` completion 看到 `invokedSkillNames=["article-writer","content-reviewer"]` | covered for content generation | 单会话全流程里 knowledge-builder、article-writer、content-reviewer 的阶段串联仍未一次跑完 |
| 要像 Claw 一样展示 thinking、execution、tool、Skill、流式输出 | Host Run UI 复用 `resolveUserFacingToolDisplayLabel`、`resolveToolProcessNarrative`、`InlineToolProcessStep`、`ThinkingBlock`、`MarkdownRenderer`；`runtimeProcess` 保留 timeline/collapseKey | first-cut covered | 终态手动展开后的 Playwright 视觉断言缺失；完整 shared renderer package 未抽完 |
| 模型、Token、费用可见 | completion E2E readiness 中 `modelReady / usageReady / costReady=true`；`run-production` 记录模型 `deepseek/deepseek-v4-flash` | covered for tested flows | 用户手动切换模型、低余额/限额、provider 错误分支未覆盖 |
| Artifact / workspace patch 可回写 App | `run-production` direct summary `hasWorkspacePatch=true / artifactCount=1`；`agent-app-direct-runtime-postcheck.json` 复核 `run-production / only-copy` direct get 都有 workspace patch artifact | covered for content generation | `only-copy` smoke summary 存在读时序 stale 字段，需要脚本 stabilization gate |
| 交付包 / 复盘缺结构化 artifact 时不能断流 | `runtime_turn.rs` output contract materialization；`run-strategy / run-review` completion ready 且 direct snapshot `hasWorkspacePatch=true` | covered for report/review | 报告类 patch 字段在 `agent_app_runtime_cmd` 投影层还需统一 accepted/extract 口径 |
| Host 提供通用 UI/SDK，不让每个 App 自己实现 AI 同事 | `AgentRunRenderer` 是 Host 共享 seam；内容工厂 Host connected 时隐藏 App 私有 AI 抽屉，仅保留 fallback | mostly covered | timeline shell / facts rail 仍未完全下沉为所有 Agent App 共享 SDK renderer |
| 过程完成后折叠但不消失 | 组件测试与 Host Run state 保留历史 timeline；文档记录 `collapseKey` 合并但不删除 | first-cut covered | 缺真实 GUI 中“点击终态折叠组展开后 thinking/execution/output 仍可见”的断言 |
| 全流程业务路径能自然递进 | 关键动作分别完成：生产、只重写文案、脚本、交付、复盘 | partial | 仍缺“知识库 -> 场景 -> 内容 -> 脚本 -> 交付 -> 复盘”同一项目单会话 E2E |

## 2. Prompt-to-artifact 检查表

| 明确要求 / 交付项 | 当前状态 | 证据 | 审计结论 |
| --- | --- | --- | --- |
| 更新 `internal/roadmap/agent_runtime` 相关文档，特别是 Lime Agent / App 边界 | done | `internal/roadmap/agentruntime/app-surface-runtime.md`、`internal/roadmap/agentruntime/backend-surface-facade-plan.md`、`internal/roadmap/agentruntime/claw-capability-sharing.md` | 文档已固定 Agent App 是 business surface，AgentRuntime 是运行事实主链，Claw 是 capability 来源。 |
| 后端不能只给模型 API，必须给完整 Lime AI 能力 | done/current | `lime-rs/src/commands/agent_app_runtime_cmd.rs`、`src/lib/api/agentAppRuntime.ts` | 新增 `agent_app_runtime_start_task / cancel / get / submit_host_response` facade，进入 AgentRuntime 主链。 |
| Agent App Runtime 隐藏 session 不能因 Provider 未配置而 accepted 后长期 idle | done/current-verified | `lime-rs/src/commands/agent_app_runtime_cmd.rs`、Playwright 最新内容工厂主生产流 | `startTask` 支持显式 `providerPreference / modelPreference`，并按 request -> 最近 successful `agent_runs` -> enabled provider catalog -> 当前 Agent state 回填模型偏好，避免普通 Chat / smoke 留下的不可用 current state 污染 Agent App hidden session；如果新建隐藏 session 且仍无法解析模型，会在 start 阶段返回明确错误。最新真实宿主任务已选中 `deepseek / deepseek-v4-flash` 并完成。 |
| 不新增垂直 `content_factory_*` 后端 Agent 命令 | done | `internal/aiprompts/commands.md`、`internal/roadmap/agentruntime/backend-surface-facade-plan.md`、`npm run test:contracts` | 内容工厂以 `taskKind` 表达业务任务，没有新增垂直 Tauri Agent 命令。 |
| 不能复制 Claw `*_skill_launch.rs`，要复用能力 | done/current-gated | `lime-rs/src/services/agent_app_runtime_capability_catalog_service.rs`、`lime-rs/src/commands/agent_app_runtime_cmd.rs`、`src/features/agent-app/runtime/capabilityDispatcher.ts` | 首批 `image / cover / research / report / pdf / summary` hint 已经通过独立 service 映射到现有 Claw launch metadata；多个 capability 会进入 workflow metadata，内容工厂复合任务保持 `metadata_only`，不复制也不强制单 skill；Host dispatcher 会按 manifest `toolRefs[].capabilities` allowlist 拒绝未声明 Claw hint。仍缺后端/cross-surface policy owner 和真正多能力执行编排。 |
| Agent App Host Bridge 的 `lime.agent` 接入真实 facade | done | `src/features/agent-app/runtime/agentRuntimeCapabilityHost.ts`、`src/features/agent-app/ui/AgentAppRuntimePage.tsx`、`src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts` | `startTask / streamTask / getTask / cancelTask / retryTask / submitHostResponse / listTasks` 已走 AgentRuntime facade；本地 adapter 只保留离线与非 Agent 样板。 |
| App 可响应缺上下文、人工确认、工具授权 | done/first-cut | `src/features/agent-app/runtime/agentRuntimeCapabilityHost.ts`、`lime-rs/src/commands/agent_app_runtime_cmd.rs` | Host response 已映射到 `agent_runtime_respond_action`，带 `session_id / turn_id / request_id`。 |
| AgentRuntime snapshot / profile / runtime event 能投影给 App | done/current-evidence-projection-first-cut | `lime-rs/src/commands/agent_app_runtime_cmd.rs`、`lime-rs/src/commands/aster_agent_cmd/runtime_turn.rs`、`lime-rs/src/services/runtime_evidence_projection_service.rs`、`src/lib/api/agentAppRuntime.ts`、`src/features/agent-app/types.ts` | `getTask` snapshot 覆盖 queued / progress / missing context / review / tool call / artifact created / evidence / outcome / incident；AgentRuntime profile event 会主动产出 `agent_app_runtime:profileProjection`，高价值 `RuntimeAgentEvent` 会主动产出 `agent_app_runtime:runtimeEventProjection`，其中 artifact 事件可直接携带 workspace patch；runtime event / timeline metadata 中已存在的 `evidenceRefs / verificationOutcomes` 会被同一 extractor 投影为 `evidence:recorded / evidence:verified`；`idle` 不再投影为误导性“任务状态：idle”文案。 |
| Host Bridge task subscription | done/current-runtime-event-projection-first-cut | `src/features/agent-app/runtime/hostBridge.ts`、`src/features/agent-app/runtime/hostBridge.test.ts`、内容工厂 `src/ui/host-bridge.js` / `src/ui/app.js` | `capability:subscribe / capability:unsubscribe / capability:event` 已可让 Host 侧轮询 `lime.agent.getTask` 并把 task update 推回 iframe；订阅同时监听 `agent_app_runtime:{appId}:{taskId}` Tauri / DevBridge runtime event，直接推送后端 runtime event、profile projection 与 runtime event projection `taskEvents`；成功终态如果暂未带 workspace patch，会继续短轮询最多 4 次等待 artifact replay。 |
| Evidence Pack / analysis / review 导出结果主动进入 App projection | done/current-rust-verified | `lime-rs/src/commands/aster_agent_cmd/command_api/runtime_api.rs`、`lime-rs/crates/agent/src/session_execution_runtime.rs`、`CARGO_INCREMENTAL=0 CARGO_TARGET_DIR="/tmp/lime-agent-app-output-contract-target" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime agent_app_runtime --lib` | `runtime_summary` 已保留 Agent App scope，Harness 导出命令会在导出成功后按 `agent_app_runtime:{appId}:{taskId}` emit `agent_app_runtime:harnessExportProjection`；Evidence Pack root 会投影为 `evidence:recorded`，completion audit 为 `completed` 时才投影 `evidence:verified`，analysis/review 制品投影为 `artifact:created`，不伪造验证结论。 |
| AgentRuntime artifact 能进入 App event refs | done | `lime-rs/src/commands/aster_agent_cmd/dto.rs`、`lime-rs/src/commands/agent_app_runtime_cmd.rs`、`src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts` | `FileArtifact -> artifact:created -> AgentAppTaskStreamEvent.refs` 已有 Rust 与前端测试。 |
| AgentRuntime artifact metadata 能作为 workspace patch producer contract | done/current-artifact-replay-first-cut | `lime-rs/src/commands/agent_app_runtime_cmd.rs`、`src/features/agent-app/runtime/agentRuntimeCapabilityHost.ts`、`src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts` | 当 runtime artifact metadata 或 artifactDocument blocks 携带 `workspacePatch` / `contentFactoryWorkspacePatch`，Rust projection、`AgentRuntimeCapabilityHost` 和 Host Bridge replay 都会补投 `artifact:created` payload，稳定输出 `workspacePatch` 与 `contentFactoryWorkspacePatch` 给 App。 |
| 跨刷新恢复 task/session | done/first-cut | `src/features/agent-app/runtime/agentRuntimeCapabilityHost.ts`、`src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts` | runtime task state 写入 Agent App storage；Host 重建后可恢复 `sessionId / turnId` 并继续读取/响应。 |
| 内容工厂实际 App 接入 Host Bridge | done/first-cut | `/Users/coso/Documents/dev/ai/limecloud/content-factory-app/src/ui/host-bridge.js`、`src/ui/app.js`、`tests/ui.test.mjs` | 场景、文案、重写、脚本、确认链会先启动 Lime Agent 任务，并保留本地兜底。 |
| 内容工厂 App 内显示任务进度，不跳回 Chat | done/first-cut | `/Users/coso/Documents/dev/ai/limecloud/content-factory-app/src/ui/app.js`、`src/ui/host-bridge.js`、Playwright MCP 真实宿主验证 | 新增 `lime:host-agent-task-update`，主生产流通过 Host task subscription 更新“AI 同事任务”面板；页面显示“等待 AgentRuntime 调度或回写进度”，不再把 runtime `idle` 暴露为任务终态。 |
| Agent App 打开时让出业务画布，且用户可手动恢复导航 | done/gui-verified | `src/components/AppSidebar.tsx`、`src/components/AppSidebar.test.tsx`、`npm test -- "src/components/AppSidebar.test.tsx"`、`npm run typecheck`、Playwright MCP `http://127.0.0.1:1420/` | 进入 `agent-app` 时 Lime 主侧栏默认临时折叠到 72px；用户点击“展开导航栏”后本次停留期间保持 272px 展开；离开 Agent App 后仍为进入前的 272px，且临时折叠不写入用户偏好。Playwright 复核中控制台仅有 1 条进入前已有的 `AsterChat` bridge timeout error，未新增侧栏相关错误。 |
| `AI 同事` 是所有 Agent App 的通用 Host 模式，而非内容工厂私有右栏 | partial/product-gap | 内容工厂已有 `AI 同事` 样板；`src/features/agent-app/runtime/hostBridge.test.ts` 已出现 canonical `runtimeProcess` 优先投影；`src/features/agent-app/runtime/agentRuntimeProcess.ts` first-cut 可归一化 timeline / thinking / execution / Skills / model / usage / cost | 产品边界已明确：Host 提供通用 Agent coworker shell / runtimeProcess slot / capability facts，业务 App 只提供页面上下文、任务合同和结果物化。当前还没有把内容工厂私有右栏抽成 Agent App Host 级共享 shell，因此不能宣称所有模块已统一。 |
| 运行过程完成后折叠但不消失，且包含思考、执行、Skill、工具、模型、Token、费用 | partial/code-first-cut | `src/features/agent-app/runtime/agentRuntimeProcess.ts`、`src/features/agent-app/runtime/agentRuntimeProcess.test.ts`、`npm test -- "src/features/agent-app/runtime/agentRuntimeProcess.test.ts"` | first-cut 已能保留完整 timeline，不在数据层截断到最后 16 条，并保留每段流式输出；代码也有 thinking / execution / skillNames / invokedSkillNames / model / usage / cost projection。该文件仍处于并行未跟踪写集，且缺真实 Host 业务流 GUI 复核，所以只算 first-cut evidence，不能算完成。 |
| 内容工厂可由 runtime structured patch 驱动 workspace | done/current-artifact-replay-verified | `/Users/coso/Documents/dev/ai/limecloud/content-factory-app/src/ui/app.js`、`tests/ui.test.mjs`、Playwright 最新真实宿主流 | App 会从 task / snapshot / event payload 以及 artifactDocument fenced JSON 中提取 `workspacePatch` / `contentFactoryWorkspacePatch`，并把数组形态的 `sceneTable / contentBatch / scripts / imagePrompts` 归一化成当前 workspace UI 可消费结构；真实 Host Bridge replay 已把最终 artifact 物化为 `文案=20 / 脚本=6 / 图片需求=5`。 |
| 内容工厂向 runtime 提供业务上下文，而不是只传 model prompt | done/current-verified | `/Users/coso/Documents/dev/ai/limecloud/content-factory-app/src/ui/app.js` | `runProduction / runScenarios / onlyCopy / runScripts` 均传入 `projectContext`，包含项目、已确认知识条目、readiness、确认链和已有场景摘要，避免 AgentRuntime 因只看到 `materialVersion` 文件名而停在索要资料。 |
| 内容工厂结果写回 storage/artifact/evidence | done/first-cut | `/Users/coso/Documents/dev/ai/limecloud/content-factory-app/src/ui/host-bridge.js`、`src/ui/app.js`、`APP.md` | 只使用 manifest 声明的 `scene_table / content_batch / script_batch` 与 `fact_grounding / publish_readiness`。 |
| Lime seeded 内容工厂 manifest 与真实写回类型一致 | done/current | `src/features/agent-app/fixtures/content-factory-app.json`、`src/features/agent-app/runtime/capabilityDispatcher.test.ts` | Lime 侧 seeded fixture 已声明 `scene_table / content_batch / script_batch / prompt_batch` 和 `publish_readiness`，避免 Host Bridge 写回被旧 `content_table` fixture 拒绝；dispatcher 回归覆盖 `content_batch` artifact 与 `publish_readiness` evidence。 |
| Host capability manifest gate | done/current-gated | `src/features/agent-app/runtime/capabilityDispatcher.ts`、`src/features/agent-app/runtime/capabilityDispatcher.test.ts` | Host Bridge dispatcher 会在调用 `lime.agent / lime.storage / lime.artifacts / lime.evidence / lime.knowledge` 前校验 capability 是否由 App manifest 的 global 或 entry requirement 声明；`lime.agent.startTask` 中的 Claw capability hint 还必须命中 `toolRefs[].capabilities` allowlist，未声明时返回 `CAPABILITY_NOT_DECLARED`。 |
| 命令四侧契约同步 | done | `npm run test:contracts` | 前端 gateway、Rust handler、DevBridge、catalog、mock contract 通过。 |
| 内容工厂包结构和 Host Bridge 回归 | done/current | `npm test` in `/Users/coso/Documents/dev/ai/limecloud/content-factory-app` | 45 tests passed，覆盖 Host Bridge、主生产任务、fenced JSON workspace patch 物化、写回声明类型。 |
| 真实 Lime 宿主 iframe Host Bridge 可用 | done/first-cut | Playwright MCP on `http://127.0.0.1:1420/` + iframe `http://127.0.0.1:4199/...` | `window.limeAgentAppBridge` 存在，`lime.agent.listTasks` 与 `startTask/getTask/cancelTask` 最小链路成功。 |
| Agent App Runtime 任务不污染普通最近对话 | done/current | `agent-app-runtime-` hidden session prefix、`src/lib/api/agentRuntime/sessionIdentity.ts`、Playwright evidence `hiddenAuxiliarySessionsCount: 1` | App 任务创建隐藏 runtime session，不再把 Agent App task 作为普通 Chat 最近会话展示。 |
| 内容工厂二级路由仍能通过 Host Bridge 发起任务 | done/current | `/Users/coso/Documents/dev/ai/limecloud/content-factory-app/src/ui/host-bridge.js`、Playwright on `/scenario-planning` | Host Bridge 对外固定使用 manifest entry `dashboard`，避免 App 内二级 route 的 entryKey 被 Lime 宿主信任校验静默丢弃。 |
| 内容工厂宿主模式不依赖本地模型 API | done/first-cut | `/Users/coso/Documents/dev/ai/limecloud/content-factory-app/src/ui/app.js` | Host 已连接时即使本地 `model.ready=false` 也可进入生成入口，任务交给 Lime AgentRuntime；本地 API 只保留 standalone 降级。 |
| 内容工厂 workspace patch producer contract | done/first-cut | `lime-rs/src/commands/agent_app_runtime_cmd.rs`、Rust fixture `test_agent_app_runtime_content_factory_output_contract_is_machine_readable` | `agent_app_runtime_start_task` 现在会把内容工厂任务包装成带 `Content Factory Output Contract` 的 runtime message，并在 `harness.agent_app_runtime_output_contract` 写入 artifact metadata contract，要求上游产物使用 `contentFactoryWorkspacePatch / workspacePatch`。 |
| 内容工厂产物数量 / schema 强约束 | done/current-app-side | `/Users/coso/Documents/dev/ai/limecloud/content-factory-app/src/ui/app.js`、`tests/ui.test.mjs`、Playwright iframe contract patch 验证 | App 会校验 `content_batch` 的文案、脚本和图片需求可用条目数；不足时显示“已先回写可审核草稿，但还没达到本轮目标”，并自动发起一次 `content_factory.copy.continue` hidden AgentRuntime 任务。 |
| capability catalog service 第一刀 | done/current-metadata | `lime-rs/src/services/agent_app_runtime_capability_catalog_service.rs`、Rust fixture `resolves_capability_aliases_to_claw_launch_descriptor` / `returns_none_for_unknown_capability_without_fake_launch` / `resolves_multiple_capabilities_without_duplicates` | capability alias / primary descriptor 已从 command 文件拆到 service；unknown capability 不会伪造 launch metadata；多个 capability descriptor 会去重并进入 workflow metadata。 |
| 多 capability workflow metadata | done/first-cut | `lime-rs/src/commands/agent_app_runtime_cmd.rs`、Rust fixture `test_agent_app_runtime_content_factory_output_contract_does_not_force_single_skill_launch` | `build_agent_app_runtime_metadata` 会写入 `agent_app_runtime.capability_workflow` 与 `harness.agent_app_runtime_capability_workflow`；内容工厂 output contract 任务标记 `mode=composite_output_contract`、`launch_policy=metadata_only`，并保留 research / image descriptors。 |
| Claw capability allowlist gate | done/first-cut | `src/features/agent-app/fixtures/content-factory-app.json`、`src/features/agent-app/types.ts`、`src/features/agent-app/projection/projectApp.ts`、`src/features/agent-app/runtime/capabilityDispatcher.test.ts` | manifest `toolRefs` 支持 `capabilities` allowlist；内容工厂 seeded fixture 显式声明 `image / cover / research / report / pdf / summary`；dispatcher 回归覆盖移除 catalog toolRef 和清空 allowlist 两种拒绝路径。 |

## 3. 已执行验证

| 命令 | 状态 | 覆盖范围 |
| --- | --- | --- |
| `npm run typecheck` | passed | Lime 前端类型边界；覆盖 `agentAppRuntime` 新增 `providerPreference / modelPreference` 类型透传。 |
| `npm run typecheck` | passed/current-event-listen | 覆盖 `toolRefs[].capabilities` 类型投影和 Host Bridge runtime event listener 类型边界。 |
| `npm test -- "src/components/AppSidebar.test.tsx"` | passed/current-product-shell | 57 tests passed；覆盖 Agent App 页面进入时主侧栏临时折叠、手动展开后不被自动压回、离开后恢复进入前偏好。 |
| `npm run typecheck` | passed/current-product-shell | 覆盖 AppSidebar 临时折叠状态机和当前工作树 TypeScript 类型边界。 |
| Playwright MCP：Lime 首页 -> 内容工厂 -> 展开导航 -> 返回首页 | passed/current-product-shell | `http://127.0.0.1:1420/`；点击“内容工厂”后侧栏 72px，点击“展开导航栏”后侧栏 272px，等待 1s 仍保持展开，点击“返回 Lime 首页”后保持进入前 272px。控制台 error 总数 1，来自进入前已有 `aster_agent_init` bridge timeout，不是本轮侧栏回归。 |
| Playwright MCP：内容工厂 `AI 同事` 抽屉 | passed/content-factory-sample | 在 Lime Host iframe 内点击“打开 AI 同事”后出现 `AI 同事` dialog，文案说明“运行过程、确认项和证据链都在这里，不挤压当前工作页面”，并展示运行现场、系统建议、确认链、待确认、证据链；点击“关闭 AI 同事”可关闭。该验证只证明内容工厂样板可用，不证明 Host 级通用 shell 已完成。 |
| `npm test -- "src/features/agent-app/runtime/agentRuntimeProcess.test.ts"` | passed/current-process-first-cut | 2 tests passed；覆盖运行过程 timeline 不截断，以及流式输出片段不被数据层折叠丢失。该测试来自并行未跟踪写集，只能证明当前工作树 first-cut 行为。 |
| `npm test -- "src/features/agent-app/runtime/hostBridge.test.ts" "src/features/agent-app/runtime/agentRuntimeProcess.test.ts"` | passed/current-process-first-cut | 16 tests passed；覆盖 Host Bridge canonical `runtimeProcess` 投影、task subscription，以及 runtimeProcess first-cut。该验证仍是当前工作树验证，不代表真实 Host 业务流已经复核。 |
| `npm run test:contracts` | passed | Lime Tauri command 四侧、runtime client、harness 与 modality contract。 |
| `npm run test:contracts` | passed/current | 本轮新增模型偏好字段和 App task 类型后，命令四侧、runtime client、harness 与 modality contract 仍通过。 |
| `npm run test:contracts` | passed/current-artifact-replay | Host Bridge / AgentRuntime artifact replay 没有新增垂直 `content_factory_*` 命令，也没有扩展 deprecated command surface；四侧契约仍通过。 |
| `npm run test:contracts` | passed/current-runtime-event-projection | AgentRuntime 主动 projection 没有新增命令面，也没有扩展 compat / deprecated command surface；命令四侧、runtime client、harness 与 modality contract 仍通过。 |
| `cargo fmt --manifest-path "lime-rs/Cargo.toml" -- --check` | passed/current-harness-export-projection | Rust 格式化检查；覆盖 `runtime_api` Harness export projection 与 `session_execution_runtime` scope fallback。 |
| `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR="/tmp/lime-agent-app-output-contract-target" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime agent_app_runtime --lib` | passed/current-harness-export-projection | 22 tests passed，覆盖 Agent App Runtime metadata、capability workflow、artifact patch projection、runtime event evidence projection，以及新增 Harness export projection builder。 |
| `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR="/tmp/lime-agent-app-output-contract-target" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime-agent extracts_agent_app_scope_from_lime_runtime_summary_fallback --lib` | passed/current-harness-export-projection | 1 test passed，覆盖 `lime_runtime` 顶层 Agent App scope fallback 能进入 `SessionExecutionRuntimeSummary`，避免 Evidence Pack 导出时找不到 `appId/taskId`。 |
| `npm run test:contracts` | passed/current-harness-export-projection | 命令四侧、runtime client、harness contract、modality contract 与 cleanup contract 通过；本轮未新增垂直 `content_factory_*` command，也未扩展 deprecated command surface。 |
| `npm run governance:legacy-report` | passed/current-harness-export-projection | 边界违规 0；Harness export projection 仍收敛在 current `agent_runtime_export_*` 和 Agent App event projection，不新增平行事实源。 |
| `git diff --check -- "lime-rs/crates/agent/src/session_execution_runtime.rs" "lime-rs/src/commands/aster_agent_cmd/command_api/runtime_api.rs" "lime-rs/src/commands/aster_agent_cmd/request_model_resolution.rs" "lime-rs/src/commands/agent_app_runtime_cmd.rs" "internal/roadmap/agentruntime"` | passed/current-harness-export-projection | 本轮 Rust 与文档 diff 无空白错误。 |
| `npm run governance:legacy-report` | passed/current-gated | 边界违规 0；本轮 Claw capability allowlist gate 和多 capability workflow metadata 没有打开新的 compat / deprecated surface。 |
| `npm test -- "src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts"` | passed | AgentRuntime Host Bridge 适配、artifact refs、跨刷新恢复。 |
| `npm test -- "src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts"` | passed/current | 本轮新增覆盖 `providerPreference / modelPreference` 从 App task 透传到 `agent_app_runtime_start_task` facade。 |
| `npm test -- "src/lib/api/agentRuntime/sessionIdentity.test.ts" "src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts"` | passed | Agent App runtime hidden session 规则、Host facade flags 透传、最小前端回归。 |
| `npm test -- "src/features/agent-app/runtime/hostBridge.test.ts" "src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts"` | passed | Host Bridge capability invoke / task subscription、AgentRuntime capability facade、list / start / stream / get / cancel / retry / host response。 |
| `npm test -- "src/features/agent-app/runtime/hostBridge.test.ts" "src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts"` | passed/current-artifact-replay | 15 tests passed，新增覆盖成功终态缺少 workspace patch 时的 Host Bridge terminal artifact replay 短轮询，以及 `AgentRuntimeCapabilityHost` 从 `threadRead.artifacts` 补投 `artifact:created` payload。 |
| `npm test -- "src/features/agent-app/runtime/hostBridge.test.ts"` | passed/current-event-listen | 13 tests passed，覆盖 task subscription 同时接入 `agent_app_runtime:{appId}:{taskId}` runtime event listener，并在 unsubscribe 时释放监听。 |
| `npm test -- "src/features/agent-app/runtime/hostBridge.test.ts"` | passed/current-runtime-event-projection | 13 tests passed，确认 Host Bridge 仍能消费 `taskEvents` 与 runtime event fallback，并保持 subscribe / unsubscribe 清理。 |
| `npm test -- "src/features/agent-app/projection/projectApp.test.ts" "src/features/agent-app/manifest/parseManifest.test.ts" "src/features/agent-app/runtime/capabilityDispatcher.test.ts"` | passed/current-writeback-manifest | 9 tests passed；Lime seeded 内容工厂 manifest 与真实 App 写回类型对齐，projection / manifest / Host Bridge dispatcher 均接受 `content_batch` 与 `publish_readiness`，且未声明 capability 会被 `CAPABILITY_NOT_DECLARED` 拦截。 |
| `npm test -- "src/features/agent-app/runtime/capabilityDispatcher.test.ts" "src/features/agent-app/projection/projectApp.test.ts" "src/features/agent-app/manifest/parseManifest.test.ts"` | passed/current-gated | 11 tests passed；覆盖 `toolRefs[].capabilities` allowlist 投影、解析和未授权 Claw hint 拒绝。 |
| `npm test -- "src/features/agent-app/ui/AgentAppRuntimePage.test.tsx" "src/features/agent-app/runtime/hostBridge.test.ts" "src/features/agent-app/runtime/agentRuntimeCapabilityHost.test.ts"` | passed/current-runtime-page | 23 tests passed；Host Bridge subscription、AgentRuntime facade 与 runtime page 在 capability manifest gate 后仍通过。 |
| `npm test -- "src/features/agent-app"` | passed/current-agent-app-suite | 35 files / 175 tests passed；覆盖 manifest fixture 扩展后 readiness、entry runtime guard、Agent Apps UI、Host Bridge、AgentRuntime facade、SDK adapter 与 install/cleanup 主链。 |
| `cargo fmt --manifest-path "lime-rs/Cargo.toml" --check` | passed | Rust 格式边界。 |
| `CARGO_TARGET_DIR="/tmp/lime-agent-app-artifact-test-target" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime agent_app_runtime --lib` | passed | Agent App runtime facade metadata / event projection Rust 定向测试。 |
| `CARGO_TARGET_DIR="/tmp/lime-agent-app-artifact-test-target" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime thread_read_should_project_file_artifacts_for_profile_consumers --lib` | passed | `FileArtifact` read model 投影。 |
| `CARGO_TARGET_DIR="/tmp/lime-agent-app-hidden-session-target" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime agent_app_runtime --lib` | passed | Agent App hidden runtime session、facade metadata 与事件投影 Rust 定向回归。 |
| `npm run bridge:health -- --timeout-ms 120000` | passed | Lime Vite / DevBridge 可用性。 |
| `npm run verify:gui-smoke` | partial/environment-failed | workspace-ready、browser-runtime、site-adapters 与 Skill Forge 前端 metadata smoke 已通过；执行 Skill Forge Rust 定向测试时默认 `lime-rs/target` 编译触发 `No space left on device`，属于本机磁盘/target 环境限制，不是本轮 Agent App 断言失败。 |
| `node --check src/ui/host-bridge.js && node --check src/ui/app.js` in content factory | passed | 内容工厂 Host Bridge 与 UI 语法。 |
| `npm run build` in content factory | passed | 内容工厂 `dist/ui/*` 与源文件同步。 |
| `npm test` in content factory | passed | 内容工厂 44 个 API / core / UI / Host Bridge 回归，覆盖 persistent `lime:host-agent-task-update` listener、Host task subscription 和已声明写回类型。 |
| Playwright MCP 手工业务流：`http://127.0.0.1:4188/dashboard` | passed/standalone | 使用 fake model dev server 跑通项目创建、资料整理、资料确认、场景生成、文案+脚本生成、内容确认、交付结论、数据复盘、团队设置、回到项目中心持久化。控制台 error 为 0；standalone 模式下确认链同步出现 2 条 `Lime host bridge is not connected` warning，属宿主未接入的预期降级。截图：`content-factory-mcp-final-dashboard.png`。 |
| Playwright MCP 真实宿主最小链路：`http://127.0.0.1:1420/` -> 内容工厂 iframe `http://127.0.0.1:4199/...` | passed/host-first-cut | iframe 内 `window.limeAgentAppBridge.protocol === "lime.agentApp.bridge"`、`version === 1`、`isEmbedded === true`；`lime.agent.listTasks` 成功；`startHostTask/getTask/cancelTask` 成功，生成 `agent-app-task-*` 与隐藏 `agent-app-runtime-*` session。 |
| Playwright MCP 二级路由 Host Bridge：`http://127.0.0.1:4199/scenario-planning?...` inside Lime iframe | passed/host-first-cut | 修复 entryKey 后，二级 route 仍可 `startHostTask`，示例 session 使用 `agent-app-runtime-*` 前缀且不进入普通最近对话。 |
| Playwright MCP 真实宿主内容工厂主生产流：`http://127.0.0.1:1420/` -> iframe `http://127.0.0.1:4199/content-factory` | passed/current | 点击“生成文案和配套素材”后，App 不跳回 Chat，Host Bridge 发起 `content_factory.copy.generate`，hidden session `agent-app-runtime-41a6000d-b22d-43b8-82ec-ef0513c96c14` 使用 `deepseek / deepseek-v4-flash` 完成，`threadRead` 有 `contentFactoryWorkspacePatch`，页面回写 `文案=5 / 脚本=3 / 图片需求=5`，控制台 error 为 0。 |
| `cargo fmt --manifest-path "lime-rs/Cargo.toml" -- --check` | passed | 本轮 Rust 格式边界。 |
| `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR="/tmp/lime-agent-app-output-contract-target" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime agent_app_runtime --lib` | passed/current-runtime-event-projection | 18 tests passed，覆盖内容工厂 output contract、artifactDocument block patch extraction、capability hints 不强制单一 skill launch、模型偏好 recent successful metadata 读取、多 capability descriptor 去重、`agent_app_runtime:taskSnapshot`、`agent_app_runtime:profileProjection`，以及 artifact `RuntimeAgentEvent` 主动投影 `agent_app_runtime:runtimeEventProjection` canonical task event。 |
| `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR="/tmp/lime-agent-app-output-contract-target" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime agent_app_runtime --lib` | passed/current-evidence-projection | 19 tests passed，新增覆盖 runtime event metadata 中的 `evidenceRefs / verificationOutcomes` 被主动投影成 `evidence:recorded / evidence:verified`，并继续覆盖 Agent App runtime facade、output contract 与 capability workflow。 |
| `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR="/tmp/lime-agent-app-output-contract-target" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime thread_read_should_collect_evidence_projection_facts_from_runtime_metadata --lib` | passed/current-evidence-projection | `AgentRuntimeThreadReadModel.evidence_summary` 会从 timeline tool / file artifact metadata 和 evidence pack 路径收集显式 evidence refs / verification outcomes，作为 `getTask` snapshot fallback。 |
| `git diff --check -- "lime-rs/src/services/runtime_evidence_projection_service.rs" "lime-rs/src/services/mod.rs" "lime-rs/src/commands/aster_agent_cmd/dto.rs" "lime-rs/src/commands/aster_agent_cmd/runtime_turn.rs" "internal/roadmap/agentruntime"` | passed/current-evidence-projection | 本轮 evidence projection service、read model、runtime translator 与路线图文档 diff 无 whitespace error。 |
| `npm run bridge:health -- --timeout-ms 120000` | passed/current | DevBridge `http://127.0.0.1:3030/health` 就绪。 |
| `npm run verify:gui-smoke -- --reuse-running` | passed/current | 复用 headless Tauri，workspace-ready、browser-runtime、site-adapters、Agent App smoke、Claw streaming、knowledge GUI、design canvas 等主路径通过。 |
| `npm run verify:gui-smoke -- --reuse-running` | failed/current-environment | 本轮复跑到 `smoke:at-command-registry` 的 `@配图` image_generate 执行请求等待超时；此前 workspace-ready、browser-runtime、site-adapters、Skill Forge、runtime tool surface 与 runtime tool surface page 均已通过。失败点属于 Claw `@配图` smoke 当前本机/模型执行等待，不是 Agent App runtime replay 断言。 |
| `npm run smoke:agent-apps -- --app-url http://127.0.0.1:1420/ --health-url http://127.0.0.1:3030/health --timeout-ms 600000 --interval-ms 1000` | passed/current | Agent Apps 正式入口、安装 review、disable/enable、runtime surface、卸载演练与 flag-off regression 通过。 |
| `node --check src/ui/host-bridge.js && node --check src/ui/app.js` in content factory | passed/current | 内容工厂 Host Bridge 与 UI 语法。 |
| `npm test` in content factory | passed/current | 45 tests passed，覆盖 App 内 runtime patch 订阅回写。 |
| `npm run build` in content factory | passed/current | 内容工厂 `dist/ui/*` 与源文件同步。 |
| `node --check src/ui/host-bridge.js && node --check src/ui/app.js && node --check tests/ui.test.mjs` in content factory | passed/current-contract | 内容工厂 Host Bridge、产物 contract 校验与 continuation 测试语法。 |
| `npm test` in content factory | passed/current-contract | 46 tests passed，新增覆盖 Host 订阅事件不足量 patch 时，App 校验 `minimumCount / minimumScriptCount / minimumImagePromptCount` 并发起一次 `content_factory.copy.continue`。 |
| `npm run build && node --check dist/ui/app.js && node --check dist/ui/host-bridge.js` in content factory | passed/current-contract | `dist/ui/*` 已同步新 Host Bridge rebind、runtime patch 验收、append continuation 归一化逻辑。 |
| Playwright MCP 真实宿主内容工厂 contract 验收：`http://127.0.0.1:1420/` -> iframe `http://127.0.0.1:4199/content-factory` | passed/current-contract | 真实 iframe 重载后接收 Host task update contract patch，页面指标显示 `文案=20 / 脚本=6 / 图片需求=5`，Host Bridge protocol 为 `lime.agentApp.bridge`，控制台 error 为 0。 |
| Playwright MCP 真实宿主 AgentRuntime continuation 触发 | passed/first-cut | 点击“生成文案和配套素材”后 hidden session 使用 `deepseek / deepseek-v4-flash` 完成；当 App 端判定产物不足时，页面提示“已先回写可审核草稿，但还没达到本轮目标”，并启动 `AI 同事正在补齐内容包` 的 continuation 任务。 |
| Playwright MCP 真实宿主 Host Bridge artifact replay | passed/current-artifact-replay | 点击“生成文案和配套素材”后 hidden session `agent-app-runtime-6c663069-9230-4c28-bfa1-adc9c0193e2a` completed，`threadRead.artifacts=1` 且含 `contentFactoryWorkspacePatch`；Host Bridge subscription replay 后 iframe 页面指标显示 `场景=5 / 文案=20 / 脚本=6 / 图片需求=5`，控制台 error 为 0。 |
| Playwright MCP 真实宿主 iframe 复核 | passed/current-recheck | 当前 Lime 页签内 iframe `http://127.0.0.1:4199/content-factory` 保持 `lime.agentApp.bridge` v1，页面显示 `文案=20 / 脚本=6 / 图片需求=5`、任务面板含 `Playwright 真实结构 artifact 已创建`，控制台 error 为 0；本机已安装 seeded manifest 仍是旧 artifact 声明，产生 1 条 `content_batch` 写回 warning，代码侧 fixture 已在本轮修正，需重新安装后消失。 |
| Playwright MCP 真实宿主快速复核 | passed/current-event-listen-recheck | Lime `http://127.0.0.1:1420/` 点击“内容工厂”后加载 iframe `http://127.0.0.1:4199/dashboard`，iframe 内 `lime.agentApp.bridge` v1 存在，`startHostTask / subscribeHostTask` 方法可见，页面显示项目中心与内容工厂，控制台 error 为 0；本次未重复触发真实模型生成，真实 artifact replay 仍以上方 `current-artifact-replay` 记录为准。 |
| Playwright MCP 真实宿主轻量复核 | passed/current-evidence-projection-recheck | 2026-05-16 复开 `http://127.0.0.1:1420/` 并进入内容工厂 iframe `http://127.0.0.1:4199/dashboard`，iframe 内 `lime.agentApp.bridge` v1、`startHostTask / subscribeHostTask` 可见，页面显示项目中心与内容工厂，控制台 error 为 0；本轮未重启 Tauri 或触发真实模型生成，因此新增 Rust evidence projection 仍需后续重启后完整业务流覆盖。 |
| `cargo test -p lime agent_app_output_contract --lib` | passed/current-output-contract | 3 tests passed；覆盖模型返回 fenced JSON patch、`strategy_report` 自然语言 fallback materialization、以及 `content_batch` 无 patch 时不伪造内容条目。 |
| `cargo test -p lime agent_app_required_skill --lib` | passed/current-required-skill | 3 tests passed；覆盖 required Skill 参数、`LimeSkillTool` fast path 真实执行与 ToolEnd metadata 投影。 |
| `node "scripts/agent-apps-smoke.mjs" --include-content-factory-completion-e2e --content-factory-action run-production --completion-timeout-ms 120000 --prefix agent-app-run-production-direct-audit` | passed/current-direct-snapshot | 内容工厂 `run-production` 完成态全 ready；summary 显示 `directRuntimeSnapshot.hasWorkspacePatch=true`、`artifactCount=1`、`toolCallCount=2`、`invokedSkillNames=["article-writer","content-reviewer"]`、模型 `deepseek/deepseek-v4-flash`、usage / cost ready。 |
| `node "scripts/agent-apps-smoke.mjs" --include-content-factory-completion-e2e --content-factory-action only-copy --completion-timeout-ms 120000 --prefix agent-app-only-copy-direct-audit` | passed/current-direct-snapshot | 内容工厂 `only-copy` 完成态全 ready，`runtimeProcess.invokedSkillNames=["article-writer","content-reviewer"]`，usage / cost / artifact / workspace patch ready；summary 的 direct snapshot 字段存在读时序 stale，已由后续 direct get 复核。 |
| `.lime/qc/gui-evidence/agent-apps/agent-app-direct-runtime-postcheck.json` | passed/current-postcheck | 对 `run-production / only-copy` 两个 task 再次调用 `agent_app_runtime_get_task`；两者 direct snapshot 均可看到 `contentFactoryWorkspacePatch / workspacePatch`，且 workspace patch artifact 路径落到 `.lime/artifacts/agent-app/<task>/<turn>/content_batch.workspace-patch.json`。 |
| `CARGO_INCREMENTAL=0 CARGO_TARGET_DIR="/tmp/lime-agent-app-runtime-report-target" cargo test --manifest-path "lime-rs/Cargo.toml" -p lime agent_app_runtime --lib` | passed/current-report-patch-projection | 26 tests passed；新增覆盖 `agent_app_runtime_cmd` 投影层接受并回放 `strategyReport / pptOutline / reviewReport / riskCheck` 报告类 workspace patch 字段，和 `runtime_turn` 的 `strategy_report / review_report` materialization 口径对齐。 |
| `cargo test --manifest-path "lime-rs/crates/agent/Cargo.toml" agent_app_content_factory_skill_should_use_fast_path_for_text_args` | passed/current-skill-fast-path | 1 test passed；Agent App runtime session 中模型二次触发 `Skill(content-reviewer)` 且 `args` 是自然语言文本时，仍走内容工厂 fast path，避免 nested Skill 长时间卡在 running。 |
| `cargo test --manifest-path "lime-rs/Cargo.toml" -p lime agent_app_output_contract --lib` | passed/current-script-draft-materialization | 4 tests passed；新增覆盖 `script_batch` 缺少结构化 patch 时只物化 `requiresHumanReview=true` 的 review draft workspace patch；`content_batch` 仍保持“不伪造内容条目”。 |
| `node --check "scripts/agent-apps-content-factory-flow.mjs"` | passed/current-full-flow-runner-syntax | 新增独立单会话流程验收入口，避免夹写并行脏 `scripts/agent-apps-smoke.mjs`。默认流程从已确认知识库样例连续跑 `run-scenarios -> run-production -> run-scripts -> run-strategy -> run-review`，可用 `--actions` 切片复测。 |
| `node "scripts/agent-apps-smoke.mjs" --timeout-ms 720000 --prefix agent-app-run-scripts-recheck-after-fast-path --include-content-factory-completion-e2e --content-factory-action run-scripts --completion-timeout-ms 600000` | passed/current-run-scripts-recheck | 内容工厂 `run-scripts` 在 fast path 修正后再次 completion ready；summary `.lime/qc/gui-evidence/agent-apps/agent-app-run-scripts-recheck-after-fast-path-summary.json` 显示 direct snapshot `hasWorkspacePatch=true`、`artifactCount=1`、`toolCallCount=2`、`invokedSkillNames=["article-writer","content-reviewer"]`。 |
| `node "scripts/agent-apps-content-factory-flow.mjs" --timeout-ms 720000 --completion-timeout-ms 600000 --prefix content-factory-run-scripts-post-redaction-20260517 --actions run-scripts` | passed/current-run-scripts-flow | 独立 flow runner 中 `run-scripts` 重新通过；summary `.lime/qc/gui-evidence/agent-apps/content-factory-run-scripts-post-redaction-20260517-summary.json` 显示 `allActionsCompleted=true`、`allActionsHaveModelUsageCost=true`、`allActionsHaveArtifactsAndWorkspacePatch=true`、`consoleErrorCount=0`，direct snapshot 为 completed，`artifactCount=1`、`toolCallCount=2`、`hasWorkspacePatch=true`、模型 `deepseek/deepseek-v4-flash`，Skill 为 `article-writer / content-reviewer`。该证据只覆盖单动作，不替代连续全流程。 |
| `node "scripts/agent-apps-content-factory-flow.mjs" --timeout-ms 720000 --completion-timeout-ms 600000 --prefix content-factory-run-scripts-strict-usage-3-20260517 --actions run-scripts` | passed/current-strict-run-scripts | 独立 runner 取消“终态 + 产物”当 usage 的弱口径后，`run-scripts` 仍通过；usage 证据来自 Host `runtimeProcess`，费用证据来自 direct snapshot，direct provider token usage 仍为 `hasUsage=false`。 |
| `node "scripts/agent-apps-content-factory-flow.mjs" --timeout-ms 720000 --completion-timeout-ms 600000 --prefix content-factory-short-flow-strict-usage-20260517 --actions run-scenarios,run-production` | failed/current-scene-table-gap | 短链路第一步 `run-scenarios` 完成 AgentRuntime 和 required Skills，但 direct snapshot 缺 workspace patch；模型只产出 `analysis` artifact，证明场景任务不能只靠自然语言/Skill evidence 判定完成。 |
| `cargo test --manifest-path "lime-rs/Cargo.toml" -p lime test_agent_app_runtime_scene_table_contract_requires_workspace_patch --lib` / `cargo test --manifest-path "lime-rs/Cargo.toml" -p lime agent_app_runtime --lib` | passed/current-scene-table-contract | 已加强 Agent App Runtime prompt contract：`scene_table` 必须输出 `contentFactoryWorkspacePatch.sceneTable`、`sceneTable.actualCount`、`imagePrompts`，并把只返回 `analysis artifact` 明确判为未完成；定向单测 1 项和 `agent_app_runtime` 27 项通过。 |
| `node "scripts/agent-apps-content-factory-flow.mjs" --timeout-ms 720000 --completion-timeout-ms 600000 --prefix content-factory-run-scenarios-estimated-usage-20260517 --actions run-scenarios` | passed/current-scene-table-restored | scene_table contract 加强后，`run-scenarios` direct snapshot 已恢复 `hasWorkspacePatch=true`，并以显式 `direct_runtime_estimate` 记录 Token 估算来源；该证据不等于真实 provider token usage 已进入 direct snapshot，也不等于单会话连续 flow 已闭环。 |
| `node "scripts/agent-apps-content-factory-flow.mjs" --timeout-ms 720000 --completion-timeout-ms 600000 --prefix content-factory-default-flow-post-redaction-20260517` | failed/current-full-flow-finding | 默认连续 flow 仍未闭环；failure `.lime/qc/gui-evidence/agent-apps/content-factory-default-flow-post-redaction-20260517-failure.json` 显示 `run-scenarios` 已进入 Host runtime 并展示“更新内容场景和图片需求 / 运行过程已折叠”，但 runner 在读取 iframe `body` 时超时，说明全流程仍存在 iframe/hot reload 恢复或 flow runner 稳定性缺口。 |
| `node "scripts/agent-apps-content-factory-flow.mjs" --timeout-ms 720000 --completion-timeout-ms 600000 --prefix content-factory-full-flow-20260517` | failed/current-full-flow-finding | 真实连续流程在 `build-store -> run-scenarios` 之间暴露业务门禁：知识库整理完成后页面进入“资料还不能生产 / 补齐资料”，`run-scenarios` 按钮不可见。failure `.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-20260517-failure.json` 证明这是产品状态递进问题，不是脚本选择器误判。 |
| `node "scripts/agent-apps-content-factory-flow.mjs" --timeout-ms 720000 --completion-timeout-ms 600000 --prefix content-factory-operational-flow-fast-skill-20260517` | failed/current-full-flow-finding | 从已确认样例知识库出发，`run-scenarios` 已能进入真实 runtime；继续串到 `run-production` 时暴露 Host startTask / 页面热更新稳定性问题，failure `.lime/qc/gui-evidence/agent-apps/content-factory-operational-flow-fast-skill-20260517-failure.json` 作为单会话串联仍未闭环的证据。 |
| `node --check "scripts/agent-apps-smoke.mjs"` | passed/current-smoke-syntax | 当前并行脏 smoke 脚本语法有效；本线程未夹写该脚本。 |
| `git diff --check -- "internal/roadmap/agentruntime/host-agent-run-ui-sdk.md" "lime-rs/src/commands/aster_agent_cmd/runtime_turn.rs"` | passed/current-doc-runtime-diff | 本轮 Host Agent Run UI SDK 文档与 runtime_turn 累计 diff 无空白错误。 |

| `cargo test --manifest-path "lime-rs/Cargo.toml" agent_app_runtime_cmd::tests -- --nocapture` | passed/current-output-contract-tail-error | 20 tests passed；新增覆盖内容工厂 `review_report.workspace-patch.json` 已物化后，即使模型尾流继续给出 malformed tool call，`agent_app_runtime_get_task` 也以 `agent_app_runtime_output_contract_materialized` terminal completion 口径返回业务完成。 |
| `npm test -- "src/features/agent-app/runtime/agentRunProjectionState.test.ts" "src/features/agent-app/runtime/agentUiProjectionViewModel.test.ts"` | passed/current-sdk-metrics | 15 tests passed；Agent App Runtime projection 会从 `runtimeFacts / runtimeProcess` 合成 `metric.changed`，并在 view model 暴露 `modelLabel / tokenText / costText`。 |
| `npm --prefix "packages/agent-app-runtime" test` | passed/current-sdk-metrics | 7 tests passed；`@limecloud/agent-app-runtime@0.1.0` 默认 renderer 能直接从 Host task state 渲染 reasoning / tool / artifact / evidence，以及 `model / tokens / cost` 摘要。 |
| `.lime/qc/gui-evidence/agent-apps/content-factory-review-yunwu-clean-retry-20260519-codex-summary.json` | passed/current-review-clean | 单动作 `run-review` 清洁复跑通过；`allActionsFullRuntimeReady / allActionsHaveWorkspacePatch / allActionsHaveModelUsageCost / processVisibleAfterEachAction / noHostFallback / noConsoleErrors` 全为 true，direct snapshot 使用 `claude-sonnet-4-6`，Skill 为 `content-reviewer`。 |
| `.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-final-20260519-codex-summary.json` | passed/current-full-flow | 五步连续流程 `run-scenarios -> run-production -> run-scripts -> run-strategy -> run-review` 全通过；断言 `allActionsCompleted / allActionsUsedExpectedSkills / allActionsHaveModelUsageCost / allActionsHaveWorkspacePatch / allActionsFullRuntimeReady / processVisibleAfterEachAction / noHostFallback / noConsoleErrors` 全为 true，人工审计 `flowResults[].processVisible === true`。 |
| `agent_app_runtime_get_task` direct postcheck for final five tasks | passed/current-full-flow-postcheck | 五个 task 再查均为 `taskStatus=completed / profileStatus=completed / status=completed`，artifactCount 均为 1，workspace patch kind 分别为 `scene_table / content_batch / script_batch / strategy_report / review_report`；required Skills 命中 `knowledge-builder / article-writer / content-reviewer`。 |

## 4. 非阻塞后续缺口

| 缺口 | 当前事实 | 下一刀 |
| --- | --- | --- |
| 终态“点击展开后查看完整现场”的 Playwright 交互断言仍可加强 | 五步 summary 已证明每步 `processVisible=true`，页面 bodyPreview 保留“运行过程已折叠，点击查看完整现场”，过程没有消失；但还没有逐步点击折叠区并断言 thinking / execution / output 全部展开的专门 GUI 断言。 | 后续在 Agent Run renderer 写集空闲时补一条点击展开断言，作为视觉回归而非当前主线阻塞项。 |
| Host Run renderer SDK 化仍可继续收口 | `@limecloud/agent-app-runtime@0.1.0` 已提供 projection / default HTML renderer，并补齐 model / token / cost 摘要；内容工厂已通过该包渲染 AgentRun projection。Host task 外层 summary、业务预览与部分 facts rail 仍在 App 内。 | 继续把 Host Run renderer 的 shell / facts rail 下沉到 SDK，形成新 Agent App 的强制默认实现。 |
| 多 capability 编排与跨 surface policy owner 仍未完全产品化 | 本次五步内容工厂主要验证 AgentRuntime、Skills、artifact/evidence/workspace patch 与 AgentUI 过程；connector / OAuth / 多 capability DAG 仍是后续 P18.7-E/F 范围。 | 后续接入 connector/outbox、OAuth/secret execution adapter 与 Chat / Agent App / Automation 共享 catalog owner。 |

## 5. 完成判定

按用户最新“内容工厂 App 内真实调用完整 Lime AI Agent 能力，不直连 API、不 mock、过程像 Claw 一样可见且完成后折叠不消失”的验收口径，当前主线已经完成：真实 Lime 宿主 / Host Bridge / hidden `agent-app-runtime-*` session / required Skills / model + Token + cost / artifact + evidence + workspace patch / AgentUI process projection 都有同一份五步连续 evidence 支撑。

关键完成证据：

- 五步连续流程 summary：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-final-20260519-codex-summary.json`。
- 五步截图：`.lime/qc/gui-evidence/agent-apps/content-factory-full-flow-yunwu-final-20260519-codex.png`。
- 五个业务 patch 已物化：`scene_table / content_batch / script_batch / strategy_report / review_report`。
- Direct runtime postcheck：五个 task 均为 `completed`，并且 required Skills、model、估算 Token、cost、artifact、evidence、workspacePatch 均可见。

当前完成度口径：

- Agent App Runtime / 内容工厂五步主线：**100%**。
- “所有 Agent App 复用同一 Lime AI 能力 / AgentUI SDK”的产品化目标：**92%**。主链和默认 SDK 已成立，剩余是 renderer shell 继续下沉、connector/OAuth、多 capability 编排与更细的终态展开视觉断言。
- 内容工厂完整产品闭环：**95%**。核心 AI 业务旅程已闭环，剩余是非阻塞的产品化 polish 与异常分支覆盖。
