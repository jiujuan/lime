# Soul Style Profile 输出面与 i18n 治理

> 状态：current planning source
> 更新时间：2026-07-08
> 上游路线图：[personal-style-profiles.md](personal-style-profiles.md)
> 目标：持续维护所有会影响用户“个性化口吻感知”的输出面、工具生命周期、i18n 边界和验收矩阵，避免把细节清单塞回主路线图。

## 1. 适用范围

本文件只回答一个问题：**用户在哪里会感知到 Soul Style Profile 是否真的生效。**

它覆盖：

1. 模型正文、欢迎语、缺参数追问和结尾建议。
2. 工具调用前、调用中、调用后、工具折叠条和单个工具卡片。
3. 图片生成块、报告、表格、artifact、导出和 copy。
4. 输入框、slash command、mention、toast、modal、confirm、错误恢复。
5. i18n 五语言资源和不能人格化的高风险场景。

它不建立新的 PersonalStyle 系统。唯一事实源仍是 `memory.soul` / Soul `Style Pack Registry` / `Style Profile` / `memory_soul_prompt_context` / Agent Runtime facts。

硬验收：

1. Soul 风格必须铺满整轮对话细节：工具调用前说明、工具调用中状态、工具完成承接、正文小标题、段落转折、失败恢复、风险提示和结尾建议。
2. 欢迎语、首句或少量状态条有变化，不等于 Soul 完成。
3. i18n 不得按风格提供固定句库；`agentChat.soulInteraction.<tone>.*` 属于 forbidden-to-restore。
4. 每个 Style Pack 只能提供规则、反例、surface contract、anti-repetition 和 few-shot anchor；few-shot 不能被组件当终稿句子渲染。

## 2. i18n 与文案事实源

Style Profile 不是一组硬编码中文口头禅。所有本地 UI 文案必须走 Lime current 五语言资源，模型生成文本才由 Soul prompt 影响。

```text
src/i18n/resources/{zh-CN,zh-TW,en-US,ja-JP,ko-KR}/settings.json
  settings.memory.soul.styleProfile.*        # 风格名称、描述、选择器、保存提示、aria/title

src/i18n/resources/{zh-CN,zh-TW,en-US,ja-JP,ko-KR}/agent namespace
  agent.runtime.status.*                     # 等待态、停止/继续、运行中、失败恢复
  agent.runtime.toolLifecycle.*              # 工具前、中、后、折叠条、工具卡片 label
  agent.runtime.collaboration.*              # deferred：仅限未来 App Server / Runtime locale copy service，不作为当前前端 owner
  agentChat.toolCall.processSummary.*        # 单工具过程摘要 descriptor 可渲染 key，不按 profile 分句库；runtime-generated / external append tool events 已产出 key-based descriptor 与 subject/family/operation facts
  agentChat.toolCall.siteResult.*            # 站点 / 保存结果过程摘要 descriptor 可渲染 key
  agentChat.collaboration.*                  # 当前聊天 timeline 协作执行、子任务标题、状态、预览、打开详情按钮
  agentChat.collaboration.runtime.*          # Workspace runtime team ready / preparing / failed preview
  agentChat.runtime.failure.prefix           # 通用 runtime 失败前缀；旧 soulInteraction.neutral.failure.prefix 已下线
  agentChat.threadTimeline.*                 # timeline / block 基础状态、审批、提醒、压缩、process mix label
  agentChat.streamingRenderer.mediaReference.* # media reference 卡片 label / kind / open aria
  agentChat.mediaReferencePreview.*          # Canvas Workbench media reference source preview / fallback preview

src/i18n/resources/{zh-CN,zh-TW,en-US,ja-JP,ko-KR}/workspace.json
  workspace.input.*                          # 输入框 placeholder、mention、slash command、busy line
  workspace.artifact.*                       # artifact title、copy/export、正式产物框架文案
  workspace.imageGeneration.*                # 图片生成块标题、状态、参数摘要、caption

src/lib/soul/
  interactionCopy.ts                         # 只允许返回 i18n key + facts，不写固定中文终稿
  style-profiles/
    packs/*.json                             # 内置 Style Pack seed manifest，承载 profile id、tone、禁忌、surface contract、few-shot
    builtInProfiles.ts                       # registry loader + manifest 校验，不承载四套 profile 大对象

lime-rs/crates/app-server/src/runtime/soul/
  locale_copy.rs                             # Rust/App Server 需要 presentation 文案时的 locale copy service
  prompt_context.rs                          # 模型生成正文的 Soul directive，不承载 UI 文案翻译
```

i18n 边界：

1. Profile registry 只存 `nameKey`、`descriptionKey`、`ariaKey` 等稳定 key，不存展示文案。
2. React / Workspace / Agent Chat 中的按钮、标题、空态、toast、confirm、prompt、placeholder、aria/title、错误提示、导出 Markdown、copy prompt、artifact title 必须覆盖 `zh-CN / zh-TW / en-US / ja-JP / ko-KR`。
3. `SoulInteractionCopy`、runtime status、协作执行、工具卡片、本地图片生成状态等用户可见本地 copy，必须改为 i18n key 或 locale copy adapter，不能只中文硬编码。
4. Rust / App Server 输出 presentation 文案时，只能传稳定 enum + facts 给前端 i18n 渲染，或通过统一 locale copy service 生成；不能在业务逻辑里拼接中文句子。
5. 模型生成正文不走前端 i18n，由 `memory_soul_prompt_context` 影响；但模型外框架文案必须 i18n。
6. 工具名、provider 名、模型名、URL、文件名、task id、stable enum、trace id 不翻译；presentation 层只翻译 label、状态和说明。
7. 高风险确认、权限审批、生产 API、删除、支付、法律、医疗、财务等文案不做强人格化，只允许专业、清晰、可审计的 locale copy。
8. 未来 Cloud / local 风格包也必须提供五语言资源或稳定 i18n key 映射；缺失 locale 时不能回退到硬编码中文。
9. 禁止新增 `agentChat.soulInteraction.cheeky_sassy.*`、`agentChat.soulInteraction.warm_supportive.*`、`agentChat.soulInteraction.cool_confident.*`、`agentChat.soulInteraction.calm_professional.*` 这类 profile 句库；本地渲染只能用 neutral copy + descriptor metadata。
10. 旧共享 pack id `com.lime.builtin.default` 已判为 `dead / forbidden-to-restore`；fixture、prompt snapshot、host-managed generation 测试和运行时代码都不得把它作为兼容 fallback。
11. 旧 `agentChat.soulInteraction.neutral.subagents.*` 与 `agentChat.soulInteraction.neutral.failure.prefix` 已判为 `dead owner`；协作 ready / preparing / failed preview 只能走 `agentChat.collaboration.runtime.*`，通用失败前缀只能走 `agentChat.runtime.failure.prefix`。

## 3. 个性化等级

口吻治理不能只看 assistant 最终回复。用户看到的是一条完整时间线：发送前 preview、等待态、工具调用前说明、工具调用中状态、工具完成折叠条、工具卡片、结果承接、正式正文、图片块、表格、结尾建议、输入框与错误恢复共同组成“人格感”。

| 等级 | 名称             | 规则                                                                            |
| ---- | ---------------- | ------------------------------------------------------------------------------- |
| `L0` | 产品 i18n        | UI label、按钮、placeholder、aria、toast 等只走五语言 i18n，不做明显人格化。    |
| `L1` | Soul 薄适配      | 工具进度、协作执行等短文案可消费 lifecycle / collaboration facts 做低强度变化；runtime status 本地 copy 保持 neutral，不按 profile id 改标题。 |
| `L2` | Soul 正文        | assistant 聊天正文、工具前后自然语言承接、缺参数追问走 Interaction Soul。       |
| `L3` | Generation Brief | 正式 artifact、导出正文、报告正文默认不受 Product Soul，只受用户显式创作声线。  |
| `L4` | 专业强制         | 危险确认、权限、生产 API、删除、支付、法律、医疗、财务等强制专业口吻。          |

## 4. 用户可见 surface 矩阵

| Surface                                      | 示例                                            | owner                                                                                                                     | 个性化等级        | i18n owner                                                                | 约束                                                                                                                                                                                                                                                                 |
| -------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 会话欢迎语 / 首轮 assistant 自述             | “我在，今天先处理哪件事？”                      | `Agent Chat runtime prompt` + welcome renderer                                                                            | `L2`              | `agent.json` 负责框架文案                                                 | 不能绕过 Soul；不能固定“我是专业友好助手”；用户设置贱兮兮或拽酷后第一屏就要能感知差异，但验收不能停在欢迎语。                                                                                                                                                        |
| 模型普通正文                                 | 回答、解释、建议、复盘                          | `session_config.rs` + `runtime/soul/prompt_context.rs`                                                                    | `L2`              | 模型输出不前端翻译                                                        | 由 Soul directive 影响；事实、引用、数字和结论必须来自上下文或工具。                                                                                                                                                                                                 |
| fast-response / direct answer                | 轻量问答、无需工具                              | `session_config.rs` light branch                                                                                          | `L2`              | 模型输出不前端翻译                                                        | 可跳过重型 memory 和 skills body，但不能跳过 Interaction Soul。                                                                                                                                                                                                      |
| 用户消息确认 / initial dispatch              | 发送后短暂显示“准备处理”                        | `workspaceSendHelpers.ts` / `agentRuntimeStatus.ts`                                                                       | `L0`              | `workspace.json`                                                          | 这是系统状态，不伪装 assistant；不得写“我已经理解你了”这类模型口吻。                                                                                                                                                                                                 |
| Home pending preview                         | 从 Home 输入后进入 Workspace 前的 pending 文案  | `homePendingPreview.ts`                                                                                                   | `L0`              | `workspace.json`                                                          | 保持中性系统态，避免与正式 assistant 首句重复或冲突。                                                                                                                                                                                                                |
| 发送后等待态 / busy line                     | “正在生成回复...”                               | `AgentMessageList` / runtime status view                                                                                  | `L0-L1`           | `agent.json`                                                              | 本地 UI 只渲染 neutral status；profile identity 只能作为 metadata / facts 进入后续 projection，不再按 profile id 改固定标题。                                                                                                                                       |
| 停止 / 继续 / 重新生成                       | 按钮和短提示                                    | Workspace controls                                                                                                        | `L0`              | `agent.json` / `workspace.json`                                           | 操作 label 必须稳定清晰，不跟随贱兮兮或拽酷口吻大幅变化。                                                                                                                                                                                                            |
| 工具调用前说明                               | “我先去查资料，避免凭感觉编。”                  | model assistant text / tool planner narrative                                                                             | `L2`              | 模型输出不前端翻译                                                        | 只说明要调用什么、为什么需要工具、预期拿什么证据；不能承诺结果。                                                                                                                                                                                                     |
| 工具调用中进度                               | “正在搜索”“正在读取网页”“正在生成图片”          | neutral `runtime_status.rs` diagnostics + tool event renderer / lifecycle facts                                           | `L1`              | `agent.runtime.toolLifecycle.*`                                           | 只表达阶段、等待原因和可观察进度；不得编工具尚未返回的内容；`runtime_status.rs` 已删除 profile-specific title rewrite。                                                                                                                                              |
| 多工具折叠条                                 | “3 个工具调用完成”                              | `toolBatchGrouping.ts` / tool group renderer                                                                              | `L0-L1`           | `agentChat.toolBatch.*`                                                   | 已迁到五语言 neutral i18n，并透出 `tool_lifecycle` metadata；当前优先消费 App Server `tool_process_facts.operationKind / operation_kind` 与 `subject` 聚合网页搜索、网页读取、探索和浏览器轨迹，未知工具才回退本地分类。                                                                                                            |
| 单个工具卡片标题                             | “Web Search”“Image Generation / Nanobanana Pro” | `ToolCallDisplay.tsx` / `InlineToolProcessStep.tsx`                                                                       | `L0`              | `agent.runtime.toolLifecycle.card.*`                                      | 工具名、provider、模型名不翻译；当前已消费 `ToolProcessNarrative` lifecycle metadata，并以 `data-soul-*` 暴露给样式 / GUI evidence；不得把 provider 名改成人格化昵称。                                                                                               |
| 单个工具卡片状态                             | pending / running / success / failed            | `ToolCallDisplay.tsx` / `InlineToolProcessStep.tsx`                                                                       | `L0-L1`           | `agent.json`                                                              | 状态必须可审计；失败态不能用玩笑掩盖原因；高风险后续应基于 `riskLevel` 进入 L4。                                                                                                                                                                                     |
| 单个工具过程摘要                             | “Searching X first”“2 sources found”            | `runtime_backend/tool_events.rs` + `tool_process_metadata.rs` + `tool_process_external_metadata.rs` + `tool_process_risk_metadata.rs` + `tool_process_kind_metadata.rs` + `runtime/event_store.rs` + `packages/agent-runtime-projection/src/toolLifecycleMetadata.ts` + `toolSurface.ts` + `toolProcessSummary.ts` / `toolProcessSummaryMetadata.ts` | `L0-L1`           | `agentChat.toolCall.processSummary.*` / `agentChat.toolCall.siteResult.*` | runtime-generated、external append 与 package projection 工具生命周期已产出 / 保留 key-based `tool_process_summary` / `tool_process_facts`；`profileId / packId / toneVariant` 已从 active config、payload 或 active tool state 进入 facts；`tool.progress` / `tool.output.delta` 已补 lifecycle facts；危险命令、权限 / sandbox、生产 / infra / 数据库影响已由 App Server risk classifier 统一写入 `riskLevel/styleLevel`；`tool_process_kind_metadata.rs` 下发 `toolFamily/tool_family` 与 `operationKind/operation_kind`；前端优先消费 descriptor，其次消费 facts subject/family/operation，raw string 不得作为 UI 文案渲染。 |
| Timeline / block 框架文案                    | “当前进展”“等你确认”“收到提醒”                  | `timeline-utils/timelineCopy.ts` + timeline renderer                                                                      | `L0-L1`           | `agentChat.threadTimeline.*`                                              | 当前第一刀已迁到五语言 neutral i18n；基础 label 不按 profile 写句库，后续由 lifecycle facts / `riskLevel/styleLevel` 决定是否允许 L1 轻适配或 L4 降级。                                                                                                              |
| 工具卡片操作按钮                             | 查看、展开、复制、重试                          | tool card renderer                                                                                                        | `L0`              | `agent.json`                                                              | 操作词稳定，不随 profile 变成难懂俚语。                                                                                                                                                                                                                              |
| 工具成功后的自然承接                         | “好了，数据抓回来了。先看结论。”                | model assistant text / result summarizer                                                                                  | `L2`              | 模型输出不前端翻译                                                        | 必须基于工具 result/read model；可以有口吻，但不能新增工具没有的事实。                                                                                                                                                                                               |
| 工具部分失败承接                             | “A 成了，B 没回来；我先基于 A 给你可用版本。”   | model assistant text                                                                                                      | `L2` 降级         | 模型输出不前端翻译                                                        | 清楚区分已完成、未完成、影响和下一步；贱兮兮和拽酷风格都只能轻微缓和。                                                                                                                                                                                               |
| 工具失败恢复                                 | “这次失败原因是权限不足，需要你授权后再试。”    | runtime + model assistant text                                                                                            | `L4` 或 `L2` 降级 | `agent.json` + 模型输出                                                   | external `action.required` / `permission.denied` / `sandbox.blocked` 已补 `riskLevel=high` + `styleLevel=L4` facts；runtime-generated / external tool process 已通过 `tool_process_risk_metadata.rs` 识别危险命令、生产 / infra / 数据库影响和权限风险；涉及权限、生产环境、危险操作时强制专业，不能甩锅。                                                                                       |
| WebSearch / WebFetch 总结                    | 来源数量、时间、证据摘要                        | search result presenter + assistant text                                                                                  | `L2`              | 模型输出不前端翻译                                                        | 口吻可变，引用、日期、来源、排序必须事实保真。                                                                                                                                                                                                                       |
| 图片生成块标题                               | “Image Generation / Nanobanana Pro”             | `runtime_backend/image_command/presentation_soul.rs` + `imageTaskPreviewRuntimePayload.ts` / `imageTaskPreviewRuntimeSnapshot.ts` + `ImageWorkbenchMessagePreview.tsx` | `L0`              | `workspace.imageGeneration.*`                                             | 标题和 provider 是产品/工具事实，不套 Soul；后端 presentation payload 标注 `styleLevels.title=L0`，前端只作为 facts / DOM evidence 透传。                                                                                                                            |
| 图片生成中状态                               | 排队、生成中、上传中、完成                      | `presentation_soul.rs` + image workbench preview/runtime projection                                                        | `L1`              | `workspace.imageGeneration.*`                                             | 可低强度随 profile 调整过程说明，但不描述未生成画面；payload 标注 `runningStatus=L1`，GUI 暴露 `data-soul-style-level`。                                                                                                                                              |
| 图片参数摘要                                 | 尺寸、风格、模型、prompt 摘要                   | image generation task payload + `ImageGenerationSoulMetadata`                                                             | `L0`              | `workspace.imageGeneration.*`                                             | 参数事实不可人格化改写；payload 标注 `parameterSummary=L0`，用户 prompt 可原样或安全摘要。                                                                                                                                                                            |
| 图片产物 caption                             | 产物下方一句总结                                | `image_command/presentation.rs` + `presentation_soul.rs` + image workbench preview                                         | `L2`              | 模型输出或 `workspace.imageGeneration.*`                                  | assistant intro / completion caption 可受 Interaction Soul；payload 标注 `assistantIntro=L2`、`completionCaption=L2`。media artifact / 正式 artifact body 标注 `L3` 与 `generation_brief_only`，前端不得按 Product Soul 改写产物正文。 |
| 图片再迭代建议                               | “可以再调色、换构图、加留白。”                  | assistant text                                                                                                            | `L2`              | 模型输出不前端翻译                                                        | 建议可以有个性，但要可执行，不刷固定口头禅。                                                                                                                                                                                                                         |
| Media reference 卡片 / source preview        | 图片 / 音频 / 视频引用、sidecar URI、MIME、大小、source owner | App Server `agent_message.contentParts` + `StreamingMediaReferenceCard` + `MessagePreviewTarget.kind="media_reference"` + `workspace/mediaReferencePreviewArtifacts.ts` + `workspace/mediaReferencePreviewPagination.ts` + `workspace/mediaReferencePreviewToolbarState.ts` + `workspace/mediaReferencePreviewToolbarActions.tsx` + `workspace/useWorkspaceMediaReferencePreviewRuntime.ts` + Canvas Workbench sourcePath media preview / metadata fallback + App Server `agentSession/media/read` | `L0-L1`           | `agentChat.streamingRenderer.mediaReference.*` / `agentChat.mediaReferencePreview.*` | 已由 `media-reference` current Electron fixture 验证 `agent_message.contentParts.media -> source owner facts -> GUI media card -> Canvas Workbench sourcePath image preview` 骨架；卡片和 preview 只展示 read model reference/source facts，不展开 inline `data:` payload，不从 provider wire 或正文文案反推媒体。App Server `agentSession/media/read` 已完成已知 session `sidecarRef` 的 bounded base64 / range window 读取、full-file `sha256` / `maxBytes` / `offset/length` 校验和 `totalBytes/contentRange/hasMore` response contract；GUI 点击无 source owner 但可读的 `sidecar://` media reference 时，由 `mediaReferencePreviewArtifacts.ts` 按 bounded range window 分片读取并校验 offset 连续、chunk length、sha256、MIME、totalBytes 和总上限后生成 object URL media preview artifact，读取失败、partial range、不连续、digest 漂移仍 fail-closed 到 metadata fallback；超过前端 preview 预算的大媒体只读取首段 range facts，返回带 `mediaPreviewPolicySchema`、`sidecar_preview_budget_exceeded`、budget / loaded / next offset / total bytes facts 和 `mediaPreviewRequiresPagination=true` 的 fallback artifact，不继续读取、不创建 object URL；page-window 读取执行骨架已由 `mediaReferencePreviewPagination.ts` 承接，可按指定 `offset/length` 读取并校验单页 range，生成 `sidecar_page_window` fallback artifact 与 previous / next / page index facts；Workbench 分页按钮骨架已由 `mediaReferencePreviewToolbarState.ts` / `mediaReferencePreviewToolbarActions.tsx` 承接，从 artifact metadata 还原 page request，并通过五语言 neutral icon button 触发上一段 / 下一段读取；同一 artifact meta 也写入 `mediaReferenceSoulSchema` 和 L0/L1/L2/L3 source-owned media boundary，避免 Product Soul 改写媒体 payload；chunked read 首段有效 range 后会先用同一 artifact id 写入 loading/progress markdown artifact，完整 object URL 成功后替换为 media artifact；App Server client lazy read、object URL registry、同一 preview artifact 替换释放旧 URL、request token、组件卸载 fail-closed、迟到读取不写 UI、object URL 数量 / bytes 预算、`AbortSignal` wait-detach、progress artifact 和 Canvas Workbench preview 接线由 `useWorkspaceMediaReferencePreviewRuntime.ts` / App Server client current owner 承接；`$/cancelRequest` 已进入 App Server client / renderer bridge / Electron Host / sidecar App Server，`agentSession/media/read` 会通过 transport task dispatch + request id cancel probe 中断 sidecar range read 与 full-file digest loop；`agentSession/media/read stream=true` 已复用 App Server JSON-RPC `agentSession/event` notification 输出 `media.read.chunk` / `media.read.completed` transient events，且不写入 session read model；`mediaTaskArtifact/image|audio/complete` 已补当前 session data/file/workspace-local/受控远程 URL 输出的 sidecar write/store 骨架；图片 worker 直接执行 provider 返回 `data:` 或 remote URL 输出时已能写入同一 sidecar store 并刷新 `sidecarRef` owner facts；tool result 已携带 completed media task owner facts时，App Server read model 会生成 synthetic `agent_message.contentParts.media`；worker 异步完成后的 task store enrich 已在 session read detail 返回前按 `sessionId/threadId/turnId` owner facts 补齐。progressive media renderer、其它非 sidecar/sourcePath owner 完整读取和非 media 长 IO / 子进程级 transport kill 仍是后续项。 |
| 表格 / 报告标题                              | “竞品功能对比”“本周趋势摘要”                    | artifact/report renderer                                                                                                  | `L3` 默认         | `workspace.artifact.*`                                                    | 正式报告标题默认走 Generation Brief；聊天中的导读句走 Soul。                                                                                                                                                                                                         |
| 表头 / 单元格 / 链接按钮                     | 来源、日期、结论、打开链接                      | artifact/table renderer                                                                                                   | `L0` / `L3`       | `workspace.artifact.*`                                                    | 表头和按钮稳定清晰；数据单元格不为口吻改写。                                                                                                                                                                                                                         |
| 报告分段标题                                 | 结论、证据、风险、下一步                        | model artifact generator                                                                                                  | `L3`              | 由生成语言决定                                                            | 默认不套 Product Soul；用户显式要求“用某个风格写报告”才进入 Generation Brief。                                                                                                                                                                                       |
| 聊天正文 bullet / 结论 / 下一步              | 对工具结果的解释和建议                          | assistant text                                                                                                            | `L2`              | 模型输出不前端翻译                                                        | 可体现 profile；结论和建议必须被事实或用户需求支撑。                                                                                                                                                                                                                 |
| 正文小标题 / 段落转折                        | “先看结论”“风险在这里”“下一步”                  | assistant text                                                                                                            | `L2`              | 模型输出不前端翻译                                                        | 小标题和转折也必须受 profile 指令约束；不能首句有风格、后文退回默认报告腔。                                                                                                                                                                                          |
| 结尾建议 / follow-up chips                   | “继续细化”“生成图片”“导出报告”                  | assistant text + suggestion renderer                                                                                      | `L1-L2`           | `agent.json` / 模型输出                                                   | 按钮 label 走 i18n；assistant 自然语言建议走 Soul；不得诱导无关消费。                                                                                                                                                                                                |
| 缺参数追问                                   | 尺寸、平台、范围、账号、时间                    | assistant text                                                                                                            | `L2`              | 模型输出不前端翻译                                                        | 只追问真正阻塞的 1-3 个参数；语气可爱但问题必须清楚。                                                                                                                                                                                                                |
| 权限审批 / 危险确认                          | 删除、覆盖、生产 API、支付                      | confirm / permission gate                                                                                                 | `L4`              | `agent.json` / host locale copy                                           | 强制专业，不卖萌；必须列影响范围、风险、确认方式。                                                                                                                                                                                                                   |
| 法律 / 医疗 / 财务提示                       | 高风险建议、免责声明、就医/咨询建议             | safety policy + assistant text                                                                                            | `L4`              | 模型输出不前端翻译                                                        | 不使用贱兮兮、拽酷或玩笑口吻；保持谨慎和可审计。                                                                                                                                                                                                                     |
| Subagents / 协作执行形成                     | “已分头推进 3 个子任务”                         | `packages/agent-runtime-projection/src/collaborationFacts.ts` + `subagentStatusEvents.ts` + `subagents.ts` + `appServerFacts.ts` + `timeline-utils/collaborationCopy.ts` + `workspace/runtimeTeamCollaborationCopy.ts` + `packages/agent-runtime-ui/src/subagents.tsx` + `importedRuntimeEventDetailViewModel.ts` + `AgentRuntimeStrip.tsx` | `L1`              | `agentChat.collaboration.*` + `agentChat.collaboration.runtime.*` + workbench task rail neutral fact labels | Chat timeline 已统一标题、状态、预览和打开详情按钮；Workspace 发送前 runtime team ready / preparing preview 已统一到 `agentChat.collaboration.runtime.*`，由 `runtimeTeamCollaborationCopy.ts` 负责五语言 locale copy，`workspaceSendHelpers.ts` 只组装 facts；package / App Server replay 已产出 `collaborationFacts`、`collaborationSurface`、`collaborationPhase`、`styleLevel`、`riskLevel` 与 `profileId/packId/toneVariant`；`AgentUiSubagentsModel`、`SubagentsView`、`AgentWorkbenchSurface`、workbench task rail 和 `AgentRuntimeStrip` 已消费同一 facts 第一刀；叫法统一为协作执行/分头推进，不把 Codex `/subagents` 命令误写成产品人格系统。 |
| Subagents / 协作执行失败                     | 子任务失败、取消、超时                          | `packages/agent-runtime-projection/src/collaborationFacts.ts` + `subagentStatusEvents.ts` + `subagents.ts` + `appServerFacts.ts` + collaboration renderer + `workspace/runtimeTeamCollaborationCopy.ts` + `packages/agent-runtime-ui/src/subagents.tsx` + `importedRuntimeEventDetailViewModel.ts` + `AgentRuntimeStrip.tsx` | `L1` 降级         | `agentChat.collaboration.*` + `agentChat.collaboration.runtime.*` + `agentChat.runtime.failure.prefix` + workbench task rail neutral fact labels | Chat timeline 失败/暂停状态已走五语言 neutral copy；Workspace runtime team failed preview 的失败前缀已从 `soulInteraction.neutral.failure.prefix` 迁到 `agentChat.runtime.failure.prefix`，协作详情仍走 `agentChat.collaboration.runtime.*`；package / App Server replay、标准 Subagents runtime/workbench surface、workbench task rail 与 `AgentRuntimeStrip` 已消费 `collaborationFacts` 第一刀；后续只补业务协作入口，并视需要补 `agent.runtime.collaboration.*` locale copy service。 |
| Plugin host-managed generation 过程说明      | 插件正在生成 Markdown / 图片 / 文件             | `runtime_backend/plugin_worker_generation.rs` + `features/plugin/runtime/hostManagedGenerationProcess.ts` + `AgentRunHostDrawerFallback.tsx` | `L1-L2`           | `agent.json`                                                              | 过程说明可受 Soul，并通过 `presentation` / `soul_lifecycle` / `data-soul-*` 暴露证据；插件产物正文仍走 `Generation Brief`，`generationBriefBoundary.formalArtifactVoiceSource=generation_brief_only`。 |
| 输入框 placeholder                           | “输入消息或 @ 工具”                             | Workspace input                                                                                                           | `L0`              | `workspace.input.*`                                                       | 只做产品 i18n；不把 placeholder 写成角色台词，避免长期噪音。                                                                                                                                                                                                         |
| slash command / mention 建议                 | `/image`、`@web`、工具说明                      | command palette / mention menu                                                                                            | `L0`              | `workspace.input.*`                                                       | 命令名称和工具能力说明稳定清晰，不跟随 profile 变化。                                                                                                                                                                                                                |
| Toast / snackbar                             | 保存成功、复制成功、网络失败                    | shared UI feedback                                                                                                        | `L0` 或 `L4`      | shared / `agent.json`                                                     | 默认产品 i18n；失败和危险场景不人格化。                                                                                                                                                                                                                              |
| Modal / confirm / prompt                     | 设置保存、覆盖确认、导入 SOUL.md                | shared modal renderer                                                                                                     | `L0` 或 `L4`      | settings/shared resources                                                 | 操作风险越高越专业；不得让可爱口吻降低警觉。                                                                                                                                                                                                                         |
| 历史会话 hydrate / 回放                      | 旧消息重新展示                                  | transcript renderer                                                                                                       | `L0` 包装         | `agent.json`                                                              | 历史事实按当时内容展示，不用当前 Soul 重写；只允许当前 UI label 包装状态。                                                                                                                                                                                           |
| 导出 Markdown / copy prompt / artifact title | 导出文件、复制 prompt、产物标题                 | export/copy pipeline；Harness current `evidence/export` UI；App Server `runtime/soul/locale_copy.rs`                       | `L3` / `L0`       | `workspace.artifact.*`；`agentChat.harness.export.*` / `agentChat.harness.evidence.*`；App Server locale copy service | Harness 问题证据包导出 L0 操作层已完成：按钮、badge、toast、路径 label、artifact preview/open aria 和 projection summary 走五语言产品 i18n；旧分析 / 回放 / 人工审核派生卡和“一键复制给 AI”旧 UI surface 已删除。正式导出正文、后端 `copy_prompt` 与 artifact title 属 L3 payload：App Server current `handoff/replay/analysis/review` export 已通过 locale copy service 覆盖五语言 title / Markdown / checklist，并在正文写入 `Generation Brief` 边界；前端只透传 locale，不按 Style Pack 写句库。 |
| 系统错误 / 崩溃恢复                          | bridge 断开、App Server 不可用                  | error boundary / runtime gateway                                                                                          | `L0-L4`           | `agent.json` / shared error copy                                          | 以可恢复动作和诊断信息为主；不使用调侃掩盖严重问题。                                                                                                                                                                                                                 |

## 5. 工具生命周期口吻合同

工具调用前后是用户最容易感知“设置了风格但没变化”的位置。每个工具事件必须按阶段输出，且每个阶段只允许表达该阶段已经知道的事实。

| 阶段                         | 可以说                                                           | 禁止说                                             | 风格强度                              |
| ---------------------------- | ---------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------- |
| `before_tool`                | 要调用什么工具、为什么需要、预期拿哪类证据、用户还需补什么参数。 | 承诺工具一定成功、提前给结论、编造来源或图片细节。 | `L2`，可明显体现 profile。            |
| `tool_queued`                | 已进入队列、等待原因、可取消或继续。                             | 把排队说成已经执行，或展示不存在的进度百分比。     | `L0-L1`。                             |
| `tool_running`               | 当前阶段、正在读取/搜索/生成/上传、已知 checkpoint。             | 描述尚未返回的数据、图片画面、搜索结果。           | `L1`。                                |
| `tool_group_completed`       | 完成数量、失败数量、可展开查看。                                 | 用人格化文案替代数量和状态。                       | `L0-L1`。                             |
| `single_tool_success`        | 工具名、完成状态、关键 metadata。                                | 私自总结未读取的结果。                             | 卡片 `L0`，承接句 `L2`。              |
| `after_tool_success`         | 基于 result/read model 的自然承接、结论入口、下一步。            | 新增来源、数字、图像元素或工具没有返回的事实。     | `L2`。                                |
| `after_tool_partial_failure` | 哪些完成、哪些失败、对结论的影响、可执行补救。                   | 把部分失败包装成全成功，或让用户自己猜影响。       | `L2` 降级。                           |
| `after_tool_failure`         | 失败原因、责任边界、下一步恢复动作。                             | 甩锅、卖萌盖过错误、隐藏权限/网络/配额问题。       | 普通失败 `L2` 低强度；风险失败 `L4`。 |
| `after_artifact`             | 聊天 caption、如何查看/导出/继续迭代。                           | 用 Product Soul 改写正式 artifact 正文。           | caption `L2`；正文 `L3`。             |
| `after_image`                | 基于产物事实的说明、可调参数建议。                               | 描述不存在的图像细节，或把 provider 名人格化。     | 图片块 `L0-L1`；caption `L2`。        |

实现合同：

1. 每个工具事件进入 UI 前必须携带稳定 `phase`、`status`、`toolName`、`facts`、`riskLevel`、`styleLevel`、`profileId`、`packId`、`toneVariant`，并尽量携带 `toolFamily` 与 `operationKind`，文案层不得反推状态或硬猜当前风格；runtime-generated、external append 与 package projection 工具生命周期已通过 `tool_process_summary` / `tool_process_facts` 落地第一刀，其中 external append helper 独立在 `tool_process_external_metadata.rs`，risk helper 独立在 `tool_process_risk_metadata.rs`，kind helper 独立在 `tool_process_kind_metadata.rs`，package helper 独立在 `toolLifecycleMetadata.ts` / `toolSurface.ts`；`tool.progress` / `tool.output.delta` 先补 facts，external `action.required` / `permission.denied` / `sandbox.blocked` 以及危险命令 / 生产影响 / 数据库破坏已进入 L4 risk facts，不新增本地句库。
2. `before_tool` 和 `after_tool_*` 可以由模型生成，并通过 Soul directive 体现 profile。
3. `tool_queued`、`tool_running`、`tool_group_completed`、工具卡片标题和按钮默认由 i18n 模板渲染；只允许低强度 profile 变体。
4. 多工具折叠条必须优先准确表达数量和状态，例如“3 个工具调用完成 / 2 个完成，1 个失败”；不能为了风格牺牲扫描效率。
5. 工具结果承接句必须引用 read model 或 tool result，不允许从 UI label、折叠条、工具名推断业务结论。
6. 同一 turn 内禁止固定口头禅复读。贱兮兮风格应体现为轻微吐槽、短句节奏和执行感；拽酷风格应体现为克制、锋利、少废话，不是每句都加同一后缀。
7. 高风险或危险操作一旦命中，工具前说明、确认弹窗、失败恢复和结尾建议全部降级到 `calm_professional_partner`；当前第一刀覆盖 sandbox / permission、递归删除、破坏性 VCS、数据库破坏、生产 / infra mutation、sudo / 权限变更，后续继续扩展业务域 risk taxonomy。
8. Style Pack 的 few-shot anchor 只能帮助模型理解节奏和边界，不得被 UI renderer、i18n 或工具 summary 复制为固定文案。
9. 工具 lifecycle descriptor 必须携带 `profileId / packId / toneVariant / styleLevel / riskLevel` 作为 metadata；本地 UI 仍只渲染 neutral key。
10. 工具卡片和内联工具过程节点必须消费 lifecycle metadata，并暴露稳定 `data-soul-*` contract，避免 GUI evidence / 后续样式再从工具名或原始 metadata 旁路反推。
11. 工具过程摘要只接受结构化 i18n descriptor，例如 `tool_process_summary.pre.key` / `completed.key`；无 descriptor 时只允许读取 `tool_process_facts.subject`、`toolFamily`、`operationKind` 这类结构化事实作为主体和泛化分类；`process_summary: "固定中文终稿"` 这类 raw string 只能作为不可渲染数据处理，不能进入 UI，也不能阻止 runtime 注入新的 key-based descriptor。
12. 协作执行事件进入 projection 前后必须保留 `collaborationFacts` 与 `profileId / packId / toneVariant / styleLevel / riskLevel`；前端协议 parser 不得丢弃 `subagent_status_changed.metadata`；标准 Subagents runtime/workbench surface、workbench task rail 与 `AgentRuntimeStrip` 必须消费同一 facts，并暴露稳定 `data-collaboration-*` / `data-soul-*` 或结构化 fact label，不得从协作标题、中文状态或局部 UI state 反推风格。Workspace runtime team preview 只能由 `runtimeTeamCollaborationCopy.ts` 消费 facts 后选择 `agentChat.collaboration.runtime.*` / `agentChat.runtime.failure.prefix` key，不得在 `workspaceSendHelpers.ts` 或 `soulInteraction.neutral.*` 重新拼中文终稿。

## 6. i18n 策略

i18n 的目标不是把同一句中文翻译成五份，而是把“产品 UI 文案”和“模型生成口吻”分层，避免出现中文硬编码导致英文、日文、韩文界面里突然冒出固定中文人格。

| 文案来源                                     | 翻译方式                            | 是否受 Soul | 说明                                                                                                                                |
| -------------------------------------------- | ----------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| Profile 名称 / 描述 / 设置项                 | key-based i18n                      | 否          | `builtInProfiles.ts` 只保存 key。                                                                                                   |
| 设置保存 toast / aria / title                | key-based i18n                      | 否          | 必须覆盖五语言。                                                                                                                    |
| 输入框 placeholder / slash command / mention | key-based i18n                      | 否          | 保持产品一致性，不人格化。                                                                                                          |
| runtime status 短句                          | key-based i18n + 低强度 variant     | 可低强度    | variant 只影响节奏，不改变事实。                                                                                                    |
| 工具卡片标题 / 按钮 / 状态                   | key-based i18n                      | 基本否      | provider、模型、URL、文件名原样展示。                                                                                               |
| 单工具过程摘要                               | key-based i18n descriptor + facts   | 可低强度    | runtime-generated、external append 与 package projection 工具生命周期已传 / 保留 `tool_process_summary` / `tool_process_facts`；`progress/output_delta` 只补 facts；危险命令 / 权限 / 生产影响进入 `riskLevel` / `styleLevel` metadata；无 summary descriptor 时可读取 `tool_process_facts.subject` 与 `toolFamily`，raw string 不能渲染。 |
| 多工具折叠条                                 | key-based i18n + lifecycle metadata | 可低强度    | `agentChat.toolBatch.*` 承载 neutral label；`profileId / packId / toneVariant / riskLevel / styleLevel` 只作为 descriptor metadata；聚合类别优先来自 `tool_process_facts.operationKind / operation_kind`。 |
| Timeline / block 基础框架                    | key-based i18n                      | 基本否      | `agentChat.threadTimeline.*` 承载 neutral label；不承接 profile-specific 句库，后续只通过 facts / descriptor 做低强度变化。         |
| 协作执行 timeline / runtime 框架             | key-based i18n + collaboration facts | 基本否      | `agentChat.collaboration.*` 承载 chat timeline 的 neutral label；`agentChat.collaboration.runtime.*` 承载 Workspace runtime team ready / preparing / failed preview；`agentChat.runtime.failure.prefix` 承载通用失败前缀；package / App Server replay、标准 Subagents runtime/workbench surface、workbench task rail 与 `AgentRuntimeStrip` 已消费 `collaborationFacts` 第一刀；业务协作入口继续消费同一 facts + locale copy。 |
| 工具前后 assistant narrative                 | 模型生成                            | 是          | 由 `memory_soul_prompt_context` 控制，不走前端翻译。                                                                                |
| 图片生成块标题 / 参数摘要                    | key-based i18n + facts              | 否          | 参数事实稳定展示。                                                                                                                  |
| 图片 caption / 迭代建议                      | 模型生成或 locale copy + facts      | 是          | 只基于产物事实。                                                                                                                    |
| 正式 artifact 正文                           | Generation Brief                    | 默认否      | 用户显式要求创作声线时才受 Generation Brief 控制。                                                                                  |
| 危险确认 / 高风险提示                        | key-based i18n 或专业模型输出       | 强制专业    | external action / permission / sandbox policy events 与 tool process risk classifier 已写入 `riskLevel=high` + `styleLevel=L4` facts；不允许贱兮兮、拽酷、卖萌、调侃。 |
| 导出 / copy / artifact title                 | key-based i18n 或 Generation Brief  | 默认否      | 产品操作 label 与正式内容分离；Harness `evidence/export` 操作层已落到 `agentChat.harness.export.*` / `agentChat.harness.evidence.*`，旧派生 copy prompt UI 已删除，后端 `copy_prompt` payload 不做前端风格化。 |

落地规则：

1. 五语言 key completeness 是验收门槛；新增任何用户可见 key 必须同时补 `zh-CN / zh-TW / en-US / ja-JP / ko-KR`。
2. 本地 copy helper 只返回 `{ key, values, toneVariant? }`，禁止返回已拼好的中文终稿。
3. Rust/App Server 如果必须直接生成 presentation copy，应通过 locale copy service；更推荐返回 stable enum + facts，由前端渲染。
4. 测试 fixture 不能用“固定中文欢迎语”当成功标准；应断言 Soul directive、profile id、i18n key 和安全降级是否正确。
5. 模型输出语言跟随用户会话语言；UI 框架语言跟随 app locale。两者不一致时，不能用 UI i18n 去重写模型正文。
6. Locale copy 不负责创造人格。真正的人格表达来自 Soul directive、允许动作、禁忌、surface contract 和工具生命周期合同。
7. 任何新增 profile-specific i18n key 都必须失败：风格差异来自模型 narrative 和 descriptor metadata，不来自本地句库。
8. Runtime / App Server 传给前端的工具过程摘要只能是 stable key + values；如果只能给自然语言观察，应交给模型承接或 artifact，不要塞进本地 UI copy。
9. 协作 preview 新增文案不得再挂到 `soulInteraction.neutral.*`；聊天 timeline 走 `agentChat.collaboration.*`，Workspace runtime team preview 走 `agentChat.collaboration.runtime.*`，通用 runtime 失败前缀走 `agentChat.runtime.failure.prefix`。

## 7. 细分回归矩阵

| 场景                        | 验收方式                                                              | 必须证明                                                                                                                                                         |
| --------------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 四种风格同一普通对话        | `style-profiles` 纯函数测试 + prompt snapshot                         | 同一输入下 profile id、allowed moves、forbidden moves 不同，且都进入 prompt context。                                                                            |
| 四种风格同一工具生命周期    | runtime event fixture + UI snapshot                                   | 四个 built-in Style Pack seed 都必须覆盖 `before_tool / tool_running / after_tool_success / after_tool_partial_failure / after_tool_failure / body_detail / closing_suggestion` surface contract 与 few-shot anchors；工具事实不变。当前 `styleProfiles` / App Server Soul 单测已证明 anchors 不会坍缩成同一模板；`claw-chat-current-fixture-soul-style-transcript-golden.mjs` 已证明同一工具 facts 下四风格 transcript 文本不同且 required fact tokens 不漂移；`soul-style` Electron fixture 已通过 `--soul-style-profile` 跑通四个 profile，证明 current pack id、强度、surface contract 和 profile-specific transcript 经真实 GUI/read model 到达用户可见输出，assistant text 去重为 `4/4`。 |
| 同一 turn 的正文细节        | golden transcript                                                     | 工具前说明、工具后承接、小标题、段落转折、风险提示、结尾建议都跟随 profile，不能只有欢迎语不同。                                                                 |
| 贱兮兮风格防复读            | 20 turn golden transcript                                             | 不出现固定口头禅刷屏；吐槽对象只限任务、工具或抽象情况，不攻击用户。                                                                                             |
| 拽酷风格不过度装腔          | 20 turn golden transcript                                             | 短句、克制、有推进感，但不命令用户、不轻蔑、不牺牲信息密度。                                                                                                     |
| 欢迎语 / 首轮 assistant     | Electron Claw 真实会话                                                | 设置 `cheeky_sassy_executor` 或 `cool_confident_operator` 后，第一条 assistant 开场不能仍是旧“专业友好助手”。                                                    |
| 发送前 preview              | React unit / screenshot                                               | preview 是中性系统态，不伪装 assistant 口吻。                                                                                                                    |
| 工具调用前说明              | agent transcript assertion                                            | 说明工具目的和证据需求，不提前承诺结论。                                                                                                                         |
| 工具调用中状态              | event renderer unit                                                   | 只展示阶段和 checkpoint，不编未返回结果。                                                                                                                        |
| 多工具完成折叠条            | Playwright screenshot                                                 | 数量、成功/失败状态清晰；风格不破坏扫描效率。                                                                                                                    |
| 单工具卡片                  | component test                                                        | 标题、provider、模型名、按钮 label 来自事实和 i18n，不被 Soul 改名。                                                                                             |
| 单工具过程摘要 descriptor   | Rust event mapper + App Server event stream + projection + utils unit | runtime-generated 与 external append 的 `tool.started` / `tool.progress` / `tool.output.delta` / `tool.result` / `tool.failed` 能产出并保留 lifecycle facts；危险命令、权限、sandbox、生产 / infra / 数据库影响能写入 L4 risk facts；终态能保留 key-based `tool_process_summary` 到 thread item；无 descriptor 时能消费 `tool_process_facts.subject / toolFamily / operationKind`；raw `process_summary` 不显示且不阻止新 descriptor。 |
| Timeline / block copy       | timeline utils unit + component regression                            | 基础状态、审批、提醒、上下文压缩和 process mix 覆盖五语言；英文/日文/韩文界面不混入前端硬编码中文。                                                              |
| Collaboration timeline copy | collaboration utils unit + component regression                       | 子任务协作标题、状态、预览 fallback 和打开详情按钮覆盖五语言；旧 `threadTimeline.subagent.*` owner 不回流。                                                      |
| Collaboration runtime preview copy | workspace send helper unit + i18n completeness                         | Workspace runtime team ready / preparing / failed preview 的标题、详情、checkpoint 和成员 fallback 覆盖五语言；旧 `soulInteraction.neutral.subagents.*` / `soulInteraction.neutral.failure.prefix` owner 不回流；`workspaceSendHelpers.ts` 不重新拥有中文终稿。 |
| Collaboration facts         | projection package + App Server replay + frontend parser unit + runtime UI snapshot + workbench task rail unit | subagent / handoff / worker notification 事件保留 `collaborationFacts`、`collaborationSurface`、`collaborationPhase`、`styleLevel`、`riskLevel` 与 `profileId/packId/toneVariant`；GUI projection 不丢 `subagent_status_changed.metadata`；`AgentUiSubagentsModel`、`SubagentsView`、`AgentWorkbenchSurface`、workbench task rail 与 `AgentRuntimeStrip` 都能消费同一 facts，并暴露 `data-collaboration-*` / `data-soul-*` 或中性 fact label。 |
| 工具成功承接句              | transcript fact-check                                                 | 承接句只使用 result/read model 中存在的事实。                                                                                                                    |
| 工具部分失败                | fixture + transcript assertion                                        | 明确已完成、失败、影响和补救；不包装成全成功。                                                                                                                   |
| 图片生成块                  | Playwright screenshot + metadata assertion                            | 标题、provider、参数摘要稳定；caption 可有 Soul，但不虚构图像元素。                                                                                              |
| Media reference / contentParts | Electron current fixture + `mediaReferencePreviewArtifacts` unit + `mediaReferencePreviewPagination` unit + `mediaReferencePreviewToolbarState` helper + `mediaReferencePreviewToolbarActions` unit + `useWorkspaceMediaReferencePreviewRuntime` hook + component / helper unit + App Server / client contract + GUI smoke | `agent_message.contentParts.media` 从 App Server read model 进入 GUI 卡片；有 `sourcePath` owner 时能经 Electron `asset://` 打开 Canvas Workbench 图片预览；没有 source owner 但可读的 `sidecar://` media reference 会经 `agentSession/media/read` bounded range window 分片组装 object URL media preview artifact，partial range、读取失败、不连续 offset、digest 漂移才 fail-closed 到 metadata fallback；超过前端 preview 预算的大媒体会返回 `sidecar_preview_budget_exceeded` fallback artifact，并暴露 `mediaPreviewRequiresPagination`、loaded / next offset / total bytes 和 Soul L0-L3 source-owned boundary metadata；page-window 读取执行骨架已能按指定 offset / length 生成 `sidecar_page_window` fallback artifact 与 previous / next / page index facts；Workbench toolbar 分页按钮已能从 artifact metadata 还原 page request，并调用 `openMediaReferencePreviewPage(...)` 读取上一段 / 下一段；chunked sidecar read 首段有效 range 后先写 loading/progress artifact，完整 object URL 成功后用同一 artifact id 替换；media preview helper owner 已从泛化 workspace helper 拆到 `mediaReferencePreviewArtifacts.ts`，policy / pagination owner 拆到 `mediaReferencePreviewPolicy.ts` / `mediaReferencePreviewPagination.ts`，toolbar state / action owner 拆到 `mediaReferencePreviewToolbarState.ts` / `mediaReferencePreviewToolbarActions.tsx`，preview runtime lifecycle / request token / 迟到读取 fail-closed / object URL budget / progress artifact / policy metadata / `AbortSignal` 接线 owner 已从 `AgentChatWorkspace.tsx` 拆到 `useWorkspaceMediaReferencePreviewRuntime.ts`，App Server client / renderer client 已能在 abort 后 detach late response；App Server / Electron sidecar 已能把 `$/cancelRequest` 传到 `agentSession/media/read` 并中断 range read / digest loop；`agentSession/media/read stream=true` 已通过 App Server current transport 发出 `media.read.chunk` / `media.read.completed` notification；media task completion 的 data/file/workspace-local/受控远程 URL 输出已能回写 sidecar owner facts；worker 直写 `data:` 或 remote URL 输出已能写入 sidecar 并刷新 task / attempt 的 `sidecarRef`；tool result carried owner facts 和 worker task store owner facts 都已能 synthetic 投影为 `agent_message.contentParts.media`；不展示 inline `data:` payload，不从 provider wire 或正文解析媒体。progressive media renderer、其它非 sidecar/sourcePath owner 完整读取和非 media 长 IO / 子进程级 transport kill 仍需后续验证。 |
| 表格 / 报告                 | artifact snapshot                                                     | 表头、链接、数据不人格化；正式正文默认走 Generation Brief。                                                                                                      |
| 结尾建议 / follow-up chips  | UI test                                                               | 按钮 label 走 i18n，assistant 自然语言建议可走 Soul。                                                                                                            |
| 输入框 / slash / mention    | i18n completeness + screenshot                                        | placeholder 和工具建议不人格化，五语言都有 key。                                                                                                                 |
| toast / modal / confirm     | i18n completeness + interaction test                                  | 普通操作是产品 i18n；危险确认强制专业。                                                                                                                          |
| 高风险场景降级              | prompt snapshot + transcript + risk classifier unit                   | 法律/医疗/财务/权限/删除/支付全部降级到 `calm_professional_partner`；工具事件里的删除 / 生产 / 权限风险必须先落成 `riskLevel=high` + `styleLevel=L4` facts。                                                                                            |
| 历史会话 hydrate            | replay snapshot                                                       | 历史消息不被当前 Soul 改写，只更新 UI label。                                                                                                                    |
| i18n 五语言完整性           | resource key test                                                     | 新增 key 在 `zh-CN / zh-TW / en-US / ja-JP / ko-KR` 全覆盖，无中文硬编码。                                                                                       |
| Style Pack manifest         | schema / unit test                                                    | `built_in` profile 从 `packs/*.json` 正常解析，并由前端 registry loader 与 App Server `style_profile.rs` 校验 / 消费；`cloud_download` / `local_import` 只作为 deferred source，不绕过 resolver / guard。 |
| Electron CDP Claw 真实测试  | `lime-playwright-e2e` CDP 复用会话                                    | 设置风格、发送真实任务、观察 `electron-ipc` 与 `agentSession/turn/start`，确认页面完整完成。                                                                     |
| Electron fixture Soul 回归  | `claw-chat-current-fixture --scenario soul-style --soul-style-profile <id>` | 不依赖手工状态，验证 `memory.soul` 配置、当前 built-in pack id、tool lifecycle surface contract、profile-specific transcript、GUI/read model 完成、current JSON-RPC trace；GUI evidence 不保存完整 system prompt。 |

## 8. Playwright / Claw 真实验收要求

仅靠 unit test 不能证明用户感知变化。每次改动 Soul prompt、runtime status、工具卡片、图片生成块或 Workspace 输入体验，都需要至少跑一个真实 Claw 路径：

1. 通过设置页切到 `cheeky_sassy_executor`，再切到 `cool_confident_operator` 做对比。
2. 在 Claw 发送一个会触发工具的真实任务，例如搜索资料、生成图片或读取网页。
3. 用 CDP 连接现有 Electron / Chrome 会话，避免重启丢失登录态和 App Server 连接。
4. 捕获页面截图和 runtime trace，至少包含 `electron-ipc`、`agentSession/turn/start`、工具事件和 assistant 最终回复。
5. 验证工具调用前、中、后都有合理文案，且最终回复不是旧默认口吻。
6. 切换到 `calm_professional_partner` 后跑高风险确认或失败恢复场景，确认不会继续卖萌。
7. 验证正式 artifact、表格、导出内容没有被 Product Soul 污染。
8. 若后续接入 Cloud / local 风格包，必须验证 manifest 校验失败时回退到 built-in，不展示半安装状态。

这些验收应沉淀到 `.codex/skills/lime-playwright-e2e` 或相关测试技能文档中，作为后续 GUI 变更的固定测试路径。

当前可重复的自动化入口：

```bash
node scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs \
  --scenario soul-style \
  --soul-style-profile cheeky_sassy_executor \
  --timeout-ms 180000 \
  --prefix soul-style-cheeky-sassy-transcript-fixture-smoke
```

四风格验收必须分别替换 `--soul-style-profile` 为 `cheeky_sassy_executor`、`warm_supportive_companion`、`cool_confident_operator`、`calm_professional_partner`。该入口证明真实 Electron / Claw / App Server 主链与 Soul 配置、pack id、surface contract、profile-specific transcript 同时生效；`memory_soul_prompt_context.v2` 的 system prompt 正文由 Rust prompt 单测证明。GUI evidence、support bundle 和 trace export 必须保持 summary-only，不允许把完整 system prompt、provider request/response、API key 或用户私密 prompt 落盘。

## 9. 维护规则

1. 新增任何用户可见输出面，先判断它属于 `L0-L4` 哪一级，再补到第 4 章矩阵。
2. 新增工具、插件、图片、artifact 或协作执行 surface 时，必须同时判断工具生命周期阶段和 i18n owner。
3. 如果某个文案既可能由模型生成、也可能由 UI 本地生成，必须拆成“模型 narrative”和“UI label”两行，不混成一个 owner。
4. 如果发现用户设置风格后“看起来没变化”，优先检查本文件是否漏了 surface，而不是先加口头禅。
5. 主路线图只保留架构、事实源和边界摘要；本文件负责持续增加细节清单。
