# Clawstream Codex 场景账本

> 状态：active ledger
> 更新时间：2026-07-07
> 上游索引：`/Users/coso/Documents/dev/rust/codex`
> 绑定：`./README.md`、`./codex-derived-index.md`

## 1. 账本口径

本文件把 Codex 里的具体测试文件和测试函数索引成 Lime 必须补齐的 Clawstream 场景。它不是泛泛参考清单，而是后续补 fixture、删旧实现和评审回归时的最小账本。

每条场景必须最终落到三层中的至少两层；触达用户主路径、history hydrate、MCP、Skills、Multi-Agent、artifact 的场景必须三层都具备：

| 层级 | Lime 目标 |
| --- | --- |
| Event / Item oracle | `packages/agent-ui-contracts` 或 `packages/agent-runtime-projection` 能 replay 出标准 Item / ContentPart / read model |
| Projection / DOM oracle | `MessageList`、`StreamingRenderer`、Inputbar、right surface、artifact / markdown / diff snapshot 有结构化断言 |
| Electron fixture evidence | `scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs --scenario ...` 或聚合 smoke 产生真实 Electron + App Server sidecar evidence |

状态说明：

| 状态 | 含义 |
| --- | --- |
| `covered-electron` | 已有 Electron fixture，但还缺 item-level / projection oracle 时仍不能删旧实现 |
| `partial` | 有局部 unit / DOM / fixture，缺至少一层标准化证据 |
| `partial+guard` | 有局部 oracle，且已补防回流 guard / regression test，但仍缺至少一层标准化证据 |
| `missing` | Lime 还没有标准 Clawstream 场景 |
| `guard-needed` | 需要先补防回流守卫，再删除旧 helper / fallback |

## 2. P0：首字、reasoning、terminal、输入恢复

这些场景直接对应首字慢、reasoning 顺序、输出状态丢失和停止继续，是下一刀优先级最高的护栏。

| scenarioId | Codex source / test | Lime 标准事件项 | Projection / GUI oracle | 清理目标 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `startup-prewarm-first-output` | `core/src/session/tests.rs::regular_turn_emits_turn_started_with_trace_id_without_waiting_for_startup_prewarm`、`interrupting_regular_turn_waiting_on_startup_prewarm_emits_turn_aborted`；`tui/src/chatwidget/tests/mcp_startup.rs::app_server_mcp_startup_lag_settles_startup_and_ignores_late_updates` | `turn.started` 先于 prewarm 完成；首个 `reasoning` / `agent_message` item 不被 startup note 阻塞；`turn.aborted` 能终结等待态 | projection 已证明 startup/prewarm 不进入 visible message；首字 latency marker 已覆盖 `streamRequestStartToFirstTextPaintMs` / `submitAcceptedToFirstTextPaintMs` / `firstTextDeltaToFirstTextPaintMs`，Electron current fixture 已实跑并通过 text-stream first visible paint marker 断言；保留“正在输出 / 正在生成回复”运行态；禁止“启动处理流程 / 已接收请求”闪现 | 删除 startup 说明闪现、固定 timeout 合成首字、prewarm 阻塞 active stream | `covered-electron` |
| `reasoning-first-visible` | `tui/src/chatwidget/tests/status_and_layout.rs::final_reasoning_then_message_without_deltas_are_rendered`、`multiple_agent_messages_in_single_turn_emit_multiple_headers`；`history_replay.rs::replayed_reasoning_item_shows_raw_reasoning_when_enabled` | `reasoning` item 带 `itemId / sequence / status / summaryText`；final message 是独立 `agent_message` | reasoning 可先于 text 出现；Electron current fixture 已捕获 final/done 前 `思考中 + 正在输出` 中间态，完成态 `reasoningIndex < finalAnswerIndex`，read model 中 reasoning item sequence 早于 final message；hydrate 后不重复；raw reasoning 默认不泄露，展开后才显示 raw 内容；summary item 不被 raw 开关复制 | 删除把 reasoning 拼进 final text、靠文案识别“已完成思考”的 helper | `covered-electron` |
| `stream-parser-boundary` | `core/src/session/tests.rs::assistant_message_stream_parsers_can_be_seeded_from_output_item_added_text`、`assistant_message_stream_parsers_seed_buffered_prefix_stays_out_of_finish_tail`、`assistant_message_stream_parsers_seed_plan_parser_across_added_and_delta_boundaries` | `output_item_added` / delta / completed 任意拆分都生成同一 item lineage | `ContentPart[]` 按 sequence 排序；completion suffix 不追加到 process boundary 前的 text；已覆盖 message seed/delta/completed full text 不重复，且 `<proposed_plan>` 跨边界 materialize 为 plan part | 删除 added/delta 特例字符串拼接和 legacy tail 盲合并 | `partial+guard` |
| `plan-parser-boundary` | `app-server/tests/suite/v2/plan_item.rs::plan_mode_uses_proposed_plan_block_for_plan_item`、`plan_mode_without_proposed_plan_does_not_emit_plan_item`；`tui/src/chatwidget/tests/plan_mode.rs::plan_update_renders_history_cell` | `<proposed_plan>` materialize `plan` item；plan delta 绑定 item id / revision / source | Projection oracle 已覆盖 `<proposed_plan>` 跨 message added/delta/completed 边界与结构化 `plan.final`；仍缺 Plan rail、decision drawer、history hydrate DOM / Electron 同构，以及无 proposed_plan 不生成 Plan 的 UI guard | 删除旧 `UpdatePlanTool/update_plan` UI owner 和 plan 文案猜测 | `partial+guard` |
| `terminal-contract-after-answer` | `tui/src/chatwidget/tests/app_server.rs::live_app_server_turn_completed_clears_working_status_after_answer_item`、`live_app_server_failed_turn_consolidates_streamed_answer`、`live_app_server_failed_turn_does_not_duplicate_error_history`；`core/src/session/tests.rs::turn_complete_flushes_terminal_event_after_delivery` | terminal 只认 `turn.completed / turn.failed / turn.canceled / turn.aborted`；terminal 不合成正文 | projection 已证明 `turn.completed` 只清 running、不合成正文；hook guard 已证明 answer 已显示后 terminal 不覆盖正文，`turn_failed` 保留 partial answer 且只补一个 error；Electron current `terminal-failed-after-answer` 已实跑，证明 partial 保留、失败说明可见且只出现一次、输入框恢复、read model 标记 `failed`；Electron current `terminal-canceled-after-answer` 已实跑，证明 stop 前 partial 与 running 同屏，`turn.canceled` 后 partial 保留且只出现一次、输入框恢复、read model 标记 `canceled` | 删除 `final_done` grace timer、terminal 事件合成 final text、重复 error 分支 | `covered-electron` |
| `inputbar-restore-matrix` | `tui/src/chatwidget/tests/composer_submission.rs::output_free_interrupted_turn_requests_prompt_restore`、`visible_output_prevents_cancelled_turn_prompt_restore`、`thinking_status_keeps_cancelled_turn_prompt_restore_eligible`、`patch_activity_prevents_cancelled_turn_prompt_restore` | interrupted/canceled turn 记录 visible output、thinking、patch activity、queued input | 当前已覆盖 pure policy：output-free / thinking-only 可恢复，visible output / patch-active fail closed；stop 不再本地抢先清空 queued turns；rich restore 已接到 Inputbar，组件 guard 覆盖 text / images / pathReferences / inputCapabilityRoute；Electron Gate B fixture 已覆盖 text/image/path/skill rich draft 取消恢复；App Server queued read model 与前端 normalizer 已保留 pending steer 的 attachments / pathReferences / textElements / skill route；devserver Electron Gate B fixture 已覆盖正在输出时排队富输入、取消 active turn 后恢复 text/image/path/skill | 删除多套 input restore fallback 和“只看 running flag”的恢复逻辑 | `partial+guard` |
| `pending-steer-queue` | `tui/src/chatwidget/tests/review_mode.rs::steer_enter_during_final_stream_preserves_follow_up_prompts_in_order`、`manual_interrupt_restores_pending_steers_to_composer`、`item_completed_only_pops_front_pending_steer`、`restore_thread_input_state_restores_pending_steers_without_downgrading_them` | `turn.steer` 绑定 active turn；queued steer 保留 order、textElements、attachments、skill bindings | current read model / normalizer guard 已证明 queued snapshot 不降级为普通字符串；frontend normalizer 已按 explicit `position` 恢复多 queued turn 顺序，同 position 或 legacy 缺 position 时保持输入稳定顺序；App Server oracle 已证明多 queued read model 顺序、pop-front resume 后剩余 queued reindex 为 `position=0`，且 top-level `queued_turns` 与 `thread_read.queued_turns` hydrate 同构；devserver Electron Gate B fixture 已证明单个富输入在 active output 期间进入 queued read model，stop active turn 后恢复到 Inputbar 且 text/image/path/skill 不丢；仍缺 Electron 多 pending steer 证据和 packaged dist 版 pending steer rich restore evidence | 删除 queue / steer / draft 三套状态并行维护 | `partial+guard` |
| `cancel-then-continue` | `app-server/tests/suite/v2/turn_interrupt.rs::turn_interrupt_aborts_running_turn`、`turn_interrupt_resolves_pending_command_approval_request`；`core/src/session/tests.rs::abort_empty_active_turn_preserves_pending_input` | cancel 产生 `turn.canceled/aborted`，后续 turn 使用同一 session/thread；pending approval resolve | 现有 Electron fixture 证明 stop 后同 session 第二轮完成；还需 projection oracle 覆盖 stale terminal | 删除 stop 后靠新 session 重启、pending action 残留、active stream 泄漏 | `covered-electron` |
| `stale-terminal-does-not-stop-new-turn` | `core/src/session/tests.rs::stale_defer_mailbox_delivery_does_not_override_steered_input`、`tool_calls_reopen_mailbox_delivery_for_current_turn`；`tui/src/chatwidget/tests/mcp_startup.rs::app_server_mcp_startup_next_round_discards_stale_terminal_updates` | terminal 必须匹配 active `turnId`；旧 turn terminal 只能更新旧 item | 当前已覆盖 pure guard：terminal 缺 `turnId` fail closed；只有 queued 线索时必须命中 queued ID；已有 handler oracle 证明旧 turn terminal 不能清新 active stream；Electron current `terminal-stale-guard` 骨架实跑通过，证明同 session 两轮输出均经 GUI/read model 完成，第二轮期间记录旧 terminal marker 且 stale done marker 不污染 UI | 删除全局 terminal listener 和无 turnId terminal fallback | `covered-electron` |
| `running-status-preserved` | `tui/src/chatwidget/tests/status_and_layout.rs::streaming_final_answer_keeps_task_running_state`、`final_answer_completion_restores_status_indicator_for_pending_steer`、`commentary_completion_restores_status_indicator_before_exec_begin` | running/status item 与 answer item 分离；commentary/final completion 各自有 owner | 输出中必须保留“正在输出”状态；final completion 后再恢复 status indicator；当前已覆盖首字前 placeholder、首字后正文仍 running、completed 后 reasoning/tool/text 不丢；Electron current `cancel` fixture 已证明 stop 前同一 turn 的 scoped text 同时包含 assistant 正文和“正在输出”，且 startup note 不可见 | 删除把“有正文”当成 turn 完成的 UI 判断 | `covered-electron` |
| `no-natural-language-lifecycle-regex` | Codex TUI / app-server 都按 item/status 断言，没有用“已完成思考 / Finding / 搜索结果”等正文识别 lifecycle | lifecycle 只来自结构化 event class / item status / terminal event | projection guard 已扫描核心投影 owner，禁止正文正则判断 reasoning/search/final answer；剩余细节是扩大扫描面并删除旧 helper | 删除自然语言正文 / 展示文案 lifecycle helper | `partial+guard` |

S1 evidence：

- `packages/agent-runtime-projection/tests/clawstreamP0.test.mjs` 已覆盖 `startup-prewarm-first-output`、`reasoning-first-visible`、`terminal-contract-after-answer` 的 projection 层最小 oracle。
- `src/lib/agentUiPerformanceMetrics.test.ts` 与 `scripts/agent-runtime/claw-chat-current-fixture-*.mjs` 已覆盖 `startup-prewarm-first-output` 的 latency marker guard：front-end summary 汇总 `providerWaitMs`、`serverToRendererFirstTextDeltaMs`、`rendererApplyFirstTextDeltaMs`、`clientLocalOutputMs`、`streamRequestStartToFirstTextPaintMs`、`submitAcceptedToFirstTextPaintMs`、`firstEventToFirstTextPaintMs`、`homeInputToFirstTextPaintMs` 和 `firstTextDeltaToFirstTextPaintMs`；Electron current fixture 的 text-stream 公共断言必须看到 first visible output marker 且不导出 raw payload。
- `src/components/agent/chat/components/MessageList.reasoningFlow.test.tsx` 与 `AgentThreadTimeline.reasoning.test.tsx` 已覆盖 `reasoning-first-visible` 的 DOM 层 guard：首字前 reasoning 时间线先于答案出现且不展示启动说明；history hydrate 后 inline thinking 不重复生成 timeline；完成态 reasoning 默认只展示 summary，展开后才显示 raw reasoning，summary/raw 各只出现一次。
- `npm run smoke:claw-chat-current-fixture -- --scenario reasoning-first-visible --timeout-ms 240000` 已覆盖 `reasoning-first-visible` 的 Electron current Gate B 证据，session=`claw-chat-current-1783403517680-48086`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`：中间态 `hasFinalText=false / hasDoneText=false / startupNoteVisible=false` 且可见 `思考中 / 正在输出`；完成态 `reasoningIndex=38 < finalAnswerIndex=63`；read model `reasoningSequence=1 < finalSequence=9`。
- `packages/agent-runtime-projection/tests/clawstreamP0.test.mjs` 已覆盖 `stream-parser-boundary` / `plan-parser-boundary` 的 projection oracle：message seed + delta + completed full text 只保留一个 final text part，不把 completed 全量正文追加成重复 finish tail；`<proposed_plan>` 即使跨 added / delta / completed 边界也 materialize 为 `plan` part；结构化 `plan.final` payload 会投影为 `plan` part。
- `src/components/agent/chat/hooks/agentStreamTerminalTurnGuard.unit.test.ts` 与 `agentStreamRuntimeHandler.unit.test.ts` 已覆盖 `stale-terminal-does-not-stop-new-turn` 的第一段 hook oracle：无 `turnId` terminal 不再收尾，旧 turn terminal 不误停新 current turn。
- `src/components/agent/chat/hooks/agentStreamInputRestorePolicy.unit.test.ts`、`agentStreamFlowControl.test.ts`、`agentStreamSubmissionLifecycle.test.ts`、`useInputbarSend.test.tsx`、`agentStreamUserInputSendPreparation.test.ts`、`src/lib/api/queuedTurn.test.ts`、`Inputbar/index.test.tsx`、`lime-rs/crates/app-server/src/runtime/tests/queue.rs::read_session_projects_queued_turn_input_snapshot` 与 `lime-rs/crates/app-server/src/runtime/tests/queue_order.rs::queued_turn_read_model_reindexes_after_pop_front_resume` 已覆盖 `inputbar-restore-matrix` / `pending-steer-queue` 的 hook + component + read model guard：output-free / thinking-only 恢复，visible output / patch-active 不恢复，queued steer 按 position 保留，manual stop 不再本地抢先清空 queued turns；stop 时提交的 draft 会随 active stream 保存，Inputbar 可恢复 text、images、pathReferences 与 installed skill route；queued snapshot 保留 attachments、pathReferences、textElements 与 skill route，不降级成普通字符串；多 queued read model 按 FIFO 输出，pop-front resume 后剩余 queued reindex 为 `position=0`，top-level `queued_turns` 与 `thread_read.queued_turns` hydrate 同构；纯空白输入不会被恢复为无意义 whitespace。
- `src/components/agent/chat/components/streamingProjectionGuard.unit.test.ts` 已禁止核心投影文件重新引入“启动处理流程 / 已接收请求”启动说明文案。
- `src/components/agent/chat/components/messageListItemProjection.unit.test.ts`、`MessageList.runtimeStatus.test.tsx` 与 `MessageList.streamingTurns.test.tsx` 已覆盖 `running-status-preserved` 的 W4 guard：首字前启动态不投影为 assistant 正文，首字后正文输出仍保持 current streaming，completed/stale runtimeStatus 不删除 reasoning/tool/text 结构，DOM 保留 inline running indicator 且禁止启动说明闪现。
- `running-status-preserved` Electron current evidence：`npm run smoke:claw-chat-current-fixture -- --scenario cancel --timeout-ms 240000` 通过，session=`claw-chat-current-1783399367181-64093`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`；`commonAssertions.guiRunningStatusPreservedBeforeStop=true`，`stopClick.beforeClick.scopedText` 为同一 turn 的 `整理今天的国际新闻`、`以下是今日国际新闻简要整理：`、`正在输出`，且 `hasVisibleAssistantOutput=true`、`hasRunningStatus=true`、`startupNoteVisible=false`。
- `terminal-contract-after-answer` 追加 canceled Electron current evidence：`LIME_ELECTRON_FIXTURE_BUILD_READY=1 npm run smoke:claw-chat-current-fixture -- --scenario terminal-canceled-after-answer --timeout-ms 240000` 通过，session=`claw-chat-current-1783417713111-42454`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`；stop 前 `hasVisibleAssistantOutput=true / hasRunningStatus=true / startupNoteVisible=false`，GUI 取消后 `hasPartialText=true / stopButtonVisible=false`，read model `latestTurnStatus=canceled / includesPartialText=true`，backend ledger 记录 `turn.canceled`。直接未设 `LIME_ELECTRON_FIXTURE_BUILD_READY` 的首次运行在 packaged renderer/assets build 阶段以 `143/SIGTERM` 退出，未进入场景执行。
- `src/components/agent/chat/hooks/agentStreamThreadItemController.test.ts`、`agentStreamToolItemMessageSync.unit.test.ts`、`src/components/agent/chat/components/timeline-utils/itemConverters.unit.test.ts` 与 `ToolCallDisplay.test.tsx` 已补 `mcp-structured-content` 的 current item / converter / display guard：`tool_end` 与 read model `tool_call` 的 `structuredContent` 进入 `toolCall.result`，GUI 展示 answer + reference id，transport envelope 不外露。
- Electron evidence：`npm run smoke:claw-chat-current-fixture -- --scenario complete --timeout-ms 240000` 通过，session=`claw-chat-current-1783395755343-32569`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`，首字 trace 证据包含 `providerWaitMs=90`、`serverToRendererFirstTextDeltaMs=29`、`rendererApplyFirstTextDeltaMs=1`、`clientLocalOutputMs=58`、`streamRequestStartToFirstTextPaintMs=307`、`submitAcceptedToFirstTextPaintMs=183`、`firstEventToFirstTextPaintMs=179`、`firstTextDeltaToFirstTextPaintMs=28`；`npm run smoke:claw-chat-current-fixture -- --scenario mcp-structured-content --timeout-ms 180000` 通过；`APP_SERVER_BIN="/Users/coso/Documents/dev/ai/aiclientproxy/lime/dist-electron/app-server/darwin-arm64/app-server" npm run smoke:claw-chat-current-fixture -- --scenario inputbar-rich-restore --timeout-ms 180000` 通过，session=`claw-chat-current-1783381197658-93694`；`npm run smoke:claw-chat-current-fixture -- --app-url "http://127.0.0.1:1420/" --scenario inputbar-pending-steer-rich-restore --prefix claw-chat-current-fixture-inputbar-pending-steer-rich-restore-devserver-text-element-assertion --timeout-ms 180000` 通过，session=`claw-chat-current-1783389967237-36808`，summary=`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-inputbar-pending-steer-rich-restore-devserver-text-element-assertion-summary.json`；`npm run smoke:agent-runtime-current-fixture` 完整通过并覆盖 `cancel-then-continue`、Plan hydrate、Skills Runtime、Multi-Agent Team、MCP structuredContent、Expert Skills / Plaza / Panel、Content Factory Article Editor，`liveProviderUsed=false`。
- 仍缺：plan DOM / Electron hydrate oracle、Electron 多 pending steer 证据、packaged dist 版 pending steer rich restore evidence，以及 stale terminal 的真实 event 注入 owner 投影细节。

## 3. P1：Thread / Turn / History / Compaction

| scenarioId | Codex source / test | Lime 标准事件项 | Projection / GUI oracle | 清理目标 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `thread-read-page-isomorphic` | `thread_read.rs::thread_read_can_include_turns`、`thread_turns_list_can_page_backward_and_forward`、`thread_turns_list_supports_requested_items_view`、`thread_resume_initial_turns_page_matches_requested_turns_list_page` | Thread read/list/resume 返回同一 turn/item page shape | live `ContentPart[]` 与 read model hydrate 同构；分页不重排 | 删除 timeline 二次拼装和 legacy paginated history path | `partial` |
| `thread-resume-running-stream` | `thread_resume.rs::thread_resume_keeps_in_flight_turn_streaming`、`thread_resume_rejoins_running_thread_even_with_override_mismatch`、`thread_resume_rejects_history_when_thread_is_running` | running thread resume 返回 active turn 和 live stream binding | 切回运行会话时继续显示 running status、live tail、stop button | 删除重连后新建 shadow session / orphan stream | `missing` |
| `thread-inject-items` | `thread_inject_items.rs::thread_inject_items_adds_raw_response_items_to_thread_history`、`thread_inject_items_adds_raw_response_items_after_a_turn` | external/injected response item 保留 insertion order、turn linkage、raw response metadata | 插入 item hydrate 后不被当成用户输入或 assistant final tail | 删除 ad hoc “补消息”路径 | `missing` |
| `thread-status-runtime-update` | `thread_status.rs::thread_status_changed_emits_runtime_updates`、`thread_status_changed_can_be_opted_out` | thread status update 是独立 runtime notification，不伪造 message item | sidebar/status/header 与 transcript 解耦；opt-out 不污染 read model | 删除 status 从 DOM 文案反推会话状态 | `missing` |
| `thread-fork-lineage` | `thread_fork.rs`；`thread_read.rs::thread_read_returns_forked_from_id_for_forked_threads`；`history_replay.rs::forked_thread_history_line_includes_name_and_id_snapshot` | forked thread 记录 `forkedFromThreadId / forkedAtTurnId / parent item ids` | sidebar、history banner、evidence export 都显示 lineage | 删除 fork 后复制成无 parent 的普通会话 | `missing` |
| `thread-rollback-projection` | `thread_rollback.rs`；`core/src/session/tests.rs::thread_rollback_drops_last_turn_from_history`、`thread_rollback_recomputes_previous_turn_settings_and_reference_context_from_replay`、`thread_rollback_persists_marker_and_replays_cumulatively` | rollback marker 是 thread item；被回滚 turn/item 不再投影 | read model、history window、token/context metadata 同步回滚 | 删除只在 UI 隐藏、存储仍保留的 rollback 伪实现 | `missing` |
| `thread-archive-delete-hydrate` | `thread_archive.rs`、`thread_unarchive.rs`、`thread_delete.rs`、`thread_read.rs::thread_read_can_return_archived_threads_by_id` | archive/unarchive/delete 是 thread metadata state，不改 item 内容 | sidebar/list/read/restore 一致；deleted 不可 resume | 删除只改导航列表不改 read model 的归档路径 | `missing` |
| `thread-settings-live-update` | `thread_settings_update.rs`、`thread_metadata_update.rs`；`tui/src/chatwidget/tests/app_server.rs::thread_settings_updated_updates_visible_state_without_transcript` | settings/model/cwd/environment 更新产生 settings item 或 metadata patch | active turn 不被污染；future turn 使用新 setting；GUI 不插入伪 transcript | 删除设置变更写进 assistant 消息的旧分支 | `missing` |
| `token-usage-replay` | `thread_resume.rs::thread_resume_emits_restored_token_usage_before_next_turn`、`thread_resume_token_usage_replay_ignores_stale_interrupted_tail_turn`；`core/src/session/tests.rs::record_initial_history_seeds_token_info_from_rollout` | token usage / context window 是 turn metadata，不是正文 | status line/header/chart 与 resumed turn 对齐；stale interrupted tail 不覆盖 current | 删除从最后一条消息猜 token usage 的 fallback | `missing` |
| `context-compaction-item` | `compaction.rs::auto_compaction_local_emits_started_and_completed_items`、`auto_compaction_remote_emits_started_and_completed_items`、`thread_compact_start_triggers_compaction_and_returns_empty_response`；`core/src/session/tests.rs::reconstruct_history_matches_live_compactions` | compaction started/completed 是 item；replacement history verbatim；旧 item 不 rewrite | compact 后继续 turn；history hydrate 和 live stream 同构 | 删除 rewrite history、压缩摘要替换旧消息正文 | `missing` |
| `goal-lifecycle-hydrate` | `thread_resume.rs::thread_goal_set_persists_resumable_stopped_statuses`、`thread_goal_lifecycle_emits_analytics_and_clear_deletes_goal`；`status_and_layout.rs::goal_status_indicator_formats_statuses_and_budgets` | goal status/budget/usage 与 thread 绑定 | goal UI、status footer、history hydrate 同构 | 删除 goal 只存在 GUI local state 的路径 | `partial` |

## 4. P1：Tool / Approval / Patch / Sandbox

| scenarioId | Codex source / test | Lime 标准事件项 | Projection / GUI oracle | 清理目标 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `command-execution-item` | `command_exec.rs`、`process_exec.rs`；`tui/src/chatwidget/tests/app_server.rs::live_app_server_command_execution_strips_shell_wrapper`；`exec_flow.rs::exec_history_cell_shows_working_then_completed` | `CommandExecution` item 含 command/cwd/processId/stdout/stderr/exitCode/duration | shell wrapper 隐藏；working -> completed/failed 同构；hydrate 不重复 | 删除命令输出直接拼 assistant text、wrapper 展示泄漏 | `missing` |
| `apply-patch-filechange` | `turn_start.rs::turn_start_streams_apply_patch_change_updates_v2`、`core/src/tools/handlers/apply_patch_tests.rs::diff_consumer_streams_apply_patch_changes`；`exec_flow.rs::apply_patch_events_emit_history_cells` | `FileChange` item 含 patch diff、streamed changes、environmentId、approval state | diff snapshot、artifact workbench、approval modal 共用同一 item | 删除 patch fake artifact、diff 文本二次解析 | `missing` |
| `approval-request-and-resume` | `approval_requests.rs::exec_approval_emits_proposed_command_and_decision_history`、`app_server_exec_approval_request_preserves_permissions_context`；`thread_resume.rs::thread_resume_replays_pending_command_execution_request_approval` | approval request/resolution 绑定 owner tool item、approvalId、permissions context | pending approval hydrate 后仍可操作；resolve 后继续 turn，不伪造 terminal | 删除 action_required 与 tool timeline 双写 | `partial` |
| `request-permissions-turn-scoped` | `request_permissions.rs::request_permissions_round_trip`；`core/src/session/tests.rs::record_granted_request_permissions_for_turn_uses_originating_turn`、`request_permission_grants_are_environment_keyed`、`notify_request_permissions_response_ignores_unmatched_call_id` | request permission item 含 originatingTurnId/environmentId/grants | 权限弹层、历史、后续 tool 使用同一 grant；跨环境不串 | 删除全局 permission granted flag | `missing` |
| `network-approval-item` | `core/src/tools/network_approval_tests.rs::pending_approvals_are_deduped_per_host_protocol_and_port`、`session_approved_hosts_are_scoped_by_environment`、`pending_waiters_receive_owner_decision` | network approval item 含 host/protocol/port/environment/decision/ownerCall | GUI 显示网络请求范围；allow once/session/deny 可 hydrate | 删除靠命令自然语言说明网络批准的路径 | `missing` |
| `guardian-review-status` | `guardian.rs::app_server_guardian_review_started_sets_review_status`、`app_server_guardian_review_denied_renders_denied_request_snapshot`、`guardian_parallel_reviews_render_aggregate_status_snapshot` | guardian review started/approved/denied/timedOut 是独立 item/status | parallel review 聚合状态稳定；denied/timed out 不吞 tool output | 删除 guardian warning 与 approval request 混写 | `missing` |
| `hook-lifecycle-events` | `status_and_layout.rs::pre_tool_use_hook_events_render_snapshot`、`post_tool_use_hook_events_render_snapshot`、`completed_hook_output_precedes_following_assistant_message`、`running_hook_does_not_displace_active_exec_cell` | hook started/completed/blocked/failed item 含 hookId/runId/entries/output | hook live cell 和 history cell 同构；parallel quiet hooks collapse | 删除 hook 输出插入 assistant text 的 fallback | `missing` |

## 5. P1：MCP / Resources / Elicitation

| scenarioId | Codex source / test | Lime 标准事件项 | Projection / GUI oracle | 清理目标 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `mcp-structured-content` | `mcp_tool.rs::mcp_server_tool_call_returns_tool_result`；`core/src/session/tests.rs::prefers_structured_content_when_present`、`falls_back_to_content_when_structured_is_null` | `McpToolCall` item 含 `content / structuredContent / isError / meta / wallTime` | structuredContent 可见；transport envelope 隐藏；large result truncation 有标记 | 删除 MCP result 普通文本化、envelope 直显 | `covered-electron` |
| `mcp-resource-read` | `mcp_resource.rs::mcp_resource_read_returns_resource_contents`、`mcp_resource_read_returns_resource_contents_without_thread`；`mcp_resource_tests.rs::serialize_function_output_caps_read_resource_payload` | resource read item 含 server/uri/mime/content/truncation/thread scope | resource/evidence 面板可见；不混入 ordinary tool text | 删除 resource read 走裸 tool output 的路径 | `missing` |
| `mcp-elicitation-resume` | `mcp_server_elicitation.rs::mcp_server_form_elicitation_round_trip`、`mcp_server_openai_form_elicitation_round_trip`、`openai_form_capability_follows_the_turn_starting_connection`；`mcp_tool.rs::mcp_server_tool_call_round_trips_elicitation` | elicitation request/resolution 绑定 thread/turn/client capability/form schema | 表单显示、提交、恢复后继续 tool call；无 capability 时 fail closed | 删除 MCP ask-user 用普通 prompt / fake action_required 表示 | `missing` |
| `mcp-inventory-status` | `mcp_server_status.rs::mcp_server_status_list_returns_raw_server_and_tool_names`、`mcp_server_status_list_tools_and_auth_only_skips_slow_inventory_calls`、`mcp_server_status_list_keeps_tools_for_sanitized_name_collisions` | inventory item 保留 raw name、sanitized name、collision group、auth-only status | UI 展示规范命名，详情保留 raw name；碰撞不丢 tool | 删除 naked `server__tool`、mock inventory success | `missing` |
| `mcp-thread-scope` | `executor_mcp.rs::selected_executor_plugin_exposes_its_mcps_only_to_that_thread` | selected executor/plugin MCP 只暴露给对应 thread/environment | Thread A 的 tool 不出现在 Thread B；scope hydrate 稳定 | 删除全局 MCP registry 注入到所有会话 | `missing` |
| `mcp-startup-status` | `mcp_startup.rs::mcp_startup_ignores_status_for_other_thread`、`mcp_startup_complete_does_not_clear_running_task`、`turn_start_preserves_active_mcp_startup_header`、`mcp_startup_failure_restores_running_status_header` | startup status 是 thread-scoped runtime status，不是 assistant item | startup header 与 running task/status 共存；late/stale update 被忽略 | 删除启动状态覆盖输出状态、启动失败闪屏 | `missing` |

## 6. P1：Skills / Plugins / Capability Stack

| scenarioId | Codex source / test | Lime 标准事件项 | Projection / GUI oracle | 清理目标 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `skills-runtime-search-read-gate` | `executor_skills.rs::selected_executor_root_exposes_plugin_skill`；`skills_list.rs::skills_list_uses_cached_result_until_force_reload`、`skills_changed_notification_is_emitted_after_skill_change` | skill search/read/gate/invoke 形成 evidence item；skill body/ref 有 provenance | natural、explicit `$skill`、manual enable 都必须 search -> read -> gate -> invoke | 删除 expert skillRefs 直连、自动 tool 注入 | `covered-electron` |
| `skills-catalog-scope` | `skills_list.rs::skills_list_skips_cwd_roots_when_environment_disabled`、`skills_list_preserves_requested_cwd_order`、`skills_extra_roots_set_updates_process_runtime_roots` | catalog/list item 带 cwd roots、source、cache generation | workspace/cwd 切换后 skill list 和 runtime request 一致 | 删除全局 skill cache 污染工作区 | `missing` |
| `skill-mention-input-restore` | `composer_submission.rs::submission_prefers_selected_duplicate_skill_path`、`blocked_image_restore_preserves_mention_bindings`、`submit_user_message_emits_structured_plugin_mentions_from_bindings` | user input item 保留 structured skill mentions、textElements、attachments | blocked/queued/cancel restore 后 skill mention 不丢、不降级成普通文本 | 删除 mention binding 临时字符串路径 | `missing` |
| `selected-capability-stack` | `selected_capability_stack.rs::selected_capability_stack_tracks_environment_availability_and_resume`、`selected_capabilities_become_available_between_samples_in_one_turn` | selected capability update 是 request metadata / runtime item，含 availability/resume state | 能力可用性变化不重写历史；next turn / resume 使用最新 stack | 删除插件/skill capability 只存在 UI local state | `missing` |
| `plugin-capability-runtime` | `plugin_read.rs::plugin_skill_read_reads_remote_skill_contents_when_remote_plugin_enabled`、`plugin_list.rs::plugin_list_returns_plugin_interface_with_absolute_asset_paths`、`plugin_install.rs::plugin_install_makes_bundled_mcp_servers_available_to_followup_requests` | plugin read/install/update 只改变 capability catalog，不绕过 Claw turn/start | plugin capability 出现在 skill/MCP catalog；聊天仍走 current turn chain | 删除插件中心 / skill runtime 双轨 | `missing` |
| `expert-skills-hint-only` | Lime `expert-skills-runtime`、`expert-plaza-skills-runtime`、`expert-panel-skills-runtime`；Codex `selected_capability_stack` | expert skillRefs 是 hint/allowlist，不是直接 invoke result | Electron 已覆盖 declared/selected/invoked；还需 contract guard 证明仍 search/gate | 删除专家卡直接注入工具结果 | `covered-electron` |

## 7. P1：Multi-Agent / Review / Subagent Lineage

| scenarioId | Codex source / test | Lime 标准事件项 | Projection / GUI oracle | 清理目标 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `multi-agent-item-taxonomy` | `protocol/src/items.rs` 的 `CollabAgentToolCallItem / SubAgentActivityItem`；`multi_agents_spec_tests.rs::spawn_agent_tool_v2_requires_task_name_and_lists_visible_models`、`list_agents_tool_status_schema_includes_interrupted` | spawn/send/followup/wait/list/interrupt 都是 item；状态含 running/completed/interrupted | Team transcript、worker card、review lane 都来自 item taxonomy | 删除 agent-first orphan history、文本摘要式 Team timeline | `partial` |
| `multi-agent-resume-lineage` | `core/src/session/tests.rs::record_inter_agent_communication_sets_turn_id_in_rollout_and_resume`、`record_inter_agent_communication_preserves_item_id_in_rollout_and_resume`、`resumed_subagent_session_restores_persisted_session_id` | parentThreadId、childSessionId、turnId、itemId 持久化 | hydrate 后 handoff/worker notification 仍指向 parent Thread/Turn/Item | 删除子代理恢复后丢 parent binding | `covered-electron` |
| `multi-agent-tool-schema` | `multi_agents_tests.rs::multi_agent_v2_spawn_requires_task_name`、`multi_agent_v2_send_message_accepts_root_target_from_child`、`multi_agent_v2_wait_agent_returns_summary_for_mailbox_activity`、`multi_agent_v2_interrupt_agent_accepts_task_name_target` | Team tool schema 与 UI 展示同源；legacy field 被拒绝 | schema 错误可见；wait/interrupt/resume 对应 status UI | 删除 legacy `SubAgentTask` / `items` field 回流 | `missing` |
| `multi-agent-visual-snapshot` | Codex TUI `snapshots/*multi_agents*.snap`；`app_server.rs::live_app_server_collab_wait_items_render_history`、`collab_spawn_end_shows_requested_model_and_effort` | Team item 含 requested model/effort/path/agent state | Team transcript screenshot / DOM snapshot 稳定 | 删除只用 evidence JSON 证明 GUI 正确 | `missing` |
| `review-start-item` | `review.rs::review_start_runs_review_turn_and_emits_code_review_item`、`review_start_exec_approval_item_id_matches_command_execution_item`、`review_start_with_detached_delivery_returns_new_thread_id`；`review_mode.rs::live_review_prompt_item_is_not_rendered` | review prompt、code review item、command approval item 绑定同一 review turn | review prompt 不污染 transcript；detached delivery 有新 thread id | 删除 review mode 与普通 chat 混用 prompt item | `missing` |

## 8. P2：Image / Web / Dynamic Tools / Structured Output / Realtime

| scenarioId | Codex source / test | Lime 标准事件项 | Projection / GUI oracle | 清理目标 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `local-remote-image-input` | `turn_start.rs::turn_start_defaults_local_image_detail_to_high`、`turn_start_forwards_custom_local_image_detail`；`composer_submission.rs::submission_with_remote_and_local_images_keeps_local_placeholder_numbering` | user input item 含 local/remote image refs、detail、placeholder map | input queue/cancel/restore/hydrate 图片顺序稳定 | 删除图片 placeholder 临时编号和普通文本占位 | `missing` |
| `image-generation-item` | `imagegen_extension.rs`；`exec_flow.rs::image_generation_call_adds_history_cell`；`core/src/session/tests.rs::handle_output_item_done_records_image_save_history_message` | `ImageGeneration` item 含 task/model/provider/result image/audit log/save history | 现有 image-command/plain-image-intent 还需 item taxonomy oracle | 删除前端 fake completion、模板 task id、旧图片旁路 | `covered-electron` |
| `web-search-item-sequence` | `web_search.rs`；`status_and_layout.rs::completed_hook_output_precedes_following_assistant_message` | WebSearch item 含 query/result/source sequence；WebFetch/tool output 与 final text 分离 | search 默认折叠、展开 sources、final markdown sequence 稳定 | 删除搜索过程文案合并进 final answer | `covered-electron` |
| `dynamic-tool-call-item` | `dynamic_tools.rs`；`router_tests.rs::specs_filter_deferred_dynamic_tools`、`extension_tool_executors_are_model_visible_and_dispatchable` | dynamic/extension tool item 含 namespace/tool/arguments/content/success/error | tool inventory 与 call output 同构；deferred tools 不提前显示 | 删除无 namespace 的工具名匹配 | `missing` |
| `structured-output-message` | `output_schema.rs`；`context_tests.rs::custom_tool_calls_should_roundtrip_as_custom_outputs`、`function_payloads_remain_function_outputs` | structured answer/tool output 保留 typed payload，不降级成 markdown text | JSON/table/card renderer 稳定；copy/export 保留结构 | 删除 structured output 普通文本化 | `missing` |
| `realtime-conversation-item` | `realtime_conversation.rs::realtime_conversation_streams_v2_notifications`、`websocket_v2_text_input_is_append_only_while_response_is_active`、`websocket_v2_tool_call_does_not_block_sideband_audio` | realtime start/end/audio/text/tool output 都是 item/update；append-only active response | 如 Lime 暂无 realtime 主路径，必须显式 guard 为 not-current；启用时走同一 Thread/Turn/Item | 防止未来 realtime 绕过 Claw projection | `missing` |

## 9. P2：Visual Snapshot / Layout / Status Surface

| scenarioId | Codex source / test | Lime 标准事件项 | Projection / GUI oracle | 清理目标 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `markdown-render-snapshot` | Codex `tui/src/snapshots/*markdown*`；`status_and_layout.rs::chatwidget_markdown_code_blocks_vt100_snapshot` | final text、tool output、artifact preview 使用同一 markdown render input | headings/table/file link/code block/CJK wrap snapshot | 删除散落 markdown renderer 和 pageText includes 断言 | `missing` |
| `diff-artifact-snapshot` | Codex `tui/src/snapshots/*diff*`；`exec_flow.rs::apply_patch_manual_flow_snapshot` | file change / review / artifact diff 共享 diff item | add/delete/update/rename/multiple files/line wrap snapshot | 删除 artifact fake card / raw patch string 渲染 | `missing` |
| `electron-resize-reflow` | `tui/tests/suite/resize_reflow.rs::tmux_split_preserves_fresh_session_composer_row_after_resize_reflow`、`tmux_repeated_resizes_do_not_push_composer_down`、`tmux_width_resize_restore_keeps_visible_content_anchored` | layout state 不写进 item，但 snapshot 绑定 active Thread/Turn | Electron resize 后 MessageList/Inputbar/right surface 锚定 | 删除只用文案证明复杂布局正确的 E2E | `missing` |
| `live-tail-commit` | `tui/tests/suite/vt100_live_commit.rs::live_001_commit_on_overflow`；`status_and_layout.rs::flush_answer_stream_requests_scrollback_reflow_for_live_table_tail` | live tail commit 与 history item 分离；overflow commit 不改 sequence | 长输出首字可见，滚动/commit 不抖动，table tail reflow 稳定 | 删除 overlay 充当全局助手文本缓冲 | `missing` |
| `history-replay-visual` | `history_replay.rs::resumed_initial_messages_render_history`、`replayed_user_message_preserves_text_elements_and_local_images`、`replayed_in_progress_mcp_tool_call_stays_active`、`live_reasoning_summary_is_not_rendered_twice_when_item_completes` | replayed item 保留 original type/status，不二次生成 | hydrate 后 user/image/reasoning/MCP active 状态同构 | 删除 live/hydrate 两套 renderer | `partial` |
| `status-surface-matrix` | `status_surface_previews.rs::*_snapshot`、`status_and_layout.rs::status_widget_active_snapshot`、`renamed_thread_footer_title_snapshot` | status/header/title 来自 thread/session metadata 与 runtime status | footer/header/title、rate limit、model/reasoning/goal/status preview 稳定 | 删除 status surface 各组件重复命名 | `missing` |
| `right-surface-screenshot-baseline` | Codex resize/reflow + status snapshot 口径；Lime `right-surface-visual-matrix` | right surface active owner 与 workspace request 绑定 | 现有 Electron fixture 需沉淀稳定 screenshot baseline，不只检查 rootVisible | 删除右侧面板互斥逻辑散落各组件 | `covered-electron` |

## 10. 下一批落地顺序

1. 先建 P0 fixture schema：`startup-prewarm-first-output`、`reasoning-first-visible`、`stream-parser-boundary`、`terminal-contract-after-answer`、`inputbar-restore-matrix`。
2. 再把已有 Electron scenarios 补 item/projection oracle：`cancel-then-continue`、`plan`、`mcp-structured-content`、`skills-runtime`、`multi-agent-team`、`image-command`、`web-tools-rendering`。
3. 然后补 P1 capability/runtime：`mcp-resource-read`、`mcp-elicitation-resume`、`selected-capability-stack`、`multi-agent-tool-schema`、`command-execution-item`、`apply-patch-filechange`。
4. 最后补 P2 snapshot/reflow：`markdown-render-snapshot`、`diff-artifact-snapshot`、`electron-resize-reflow`、`status-surface-matrix`。

删除旧 Claw 实现前，必须在对应行把状态推进到 `covered-electron` 或至少 `partial + guard-needed resolved`，否则只能登记为待清理项，不能动刀。
