# Lime 测试分层路线图

## 专题路线图

- [Clawstream 全链路护栏与旧实现清理](./clawstream/README.md)：绑定 `internal/research/refactor/v1` 的 Thread / Turn / Item 主线，覆盖 Claw 输入到输出的 streaming fixture、projection、GUI evidence 与旧 fallback 清理。

## 背景

当前 `npm test` 同时承载纯单元、React/jsdom 组件、DevBridge/Tauri 契约、脚本集成和少量 live-gated 测试。按当前工作区实测，前端完整 Vitest 分批跑完约 `306.95s`，Rust 后端 `cargo test --manifest-path "lime-rs/Cargo.toml"` 约 `11.00s`。这说明 Lime 的主要反馈瓶颈在前端测试分层，而不是 Rust 后端。

本路线图目标是把本地 TDD 反馈环从“全量前端 Vitest”收敛到“快速纯单元 + 定向相关测试”，同时保留 `verify:local`、`test:contracts`、`verify:gui-smoke` 对交付风险的覆盖。

## 原则

1. **单元测试优先测业务逻辑边界** - 前端复杂逻辑应优先沉淀到 View Model / projection / presentation / selector / command planner 等纯函数边界，再用纯单元测试覆盖。
2. **组件测试只测必要契约** - React Testing Library / jsdom 测试只保留关键渲染、可访问性、事件接线和回归点，不承担大段业务状态机验证。
3. **核心用户流交给 GUI smoke / E2E** - 用户必须真实完成的主路径，用 `verify:gui-smoke`、Playwright 或已有产品 E2E 验证，不把完整流程塞进单个组件测试。
4. **不引入重 MVVM 框架** - Lime 继续使用现有 React + projection/helper 模式；View Model 是测试边界，不是新的运行时框架。
5. **先分层入口，再逐步迁移** - 第一阶段不批量重命名 800+ 测试文件，先用 runner 分类和显式脚本形成反馈环；后续再按热点迁移。

## 分层定义

| 层级            | 命令                        | 覆盖范围                                                                                    | 本地 TDD 默认         |
| --------------- | --------------------------- | ------------------------------------------------------------------------------------------- | --------------------- |
| Unit            | `npm run test:unit`         | 纯函数、parser、formatter、projection、presentation、selector、View Model 状态转换          | 是                    |
| Component       | `npm run test:component`    | React/jsdom 组件与 hook 渲染、事件接线、关键 UI 断言                                        | 按 UI 改动定向跑      |
| Contract        | `npm run test:contract`     | `safeInvoke`、DevBridge、Tauri mock、command catalog、schema 契约                           | 按命令/桥接改动跑     |
| Integration     | `npm run test:integration`  | 文件系统、子进程、本地 fixture server、多模块脚本流程                                       | CI 或本地按需         |
| E2E             | `npm run test:e2e`          | Vitest 内显式 E2E / smoke / live-gated 测试；真实产品主路径仍以 GUI smoke / Playwright 为准 | 默认不进 TDD          |
| Frontend All    | `npm run test:frontend:all` | 现有前端 Vitest 全量兼容入口                                                                | 交付前/CI             |
| Layer Stats     | `npm run test:layers:stats` | 按同一分类事实源输出分层统计、默认可运行数和 live-gated 数                                  | 统计 / 治理           |
| Rust            | `npm run test:rust`         | Rust workspace 测试                                                                         | Rust 改动定向后再全量 |
| GUI Smoke / E2E | `npm run verify:gui-smoke`  | Tauri 壳、DevBridge、Workspace、主产品路径                                                  | GUI 主路径改动/交付前 |

## 治理后统计

最后统计时间：2026-06-03。

| 范围                  | 命令                                                                            | 文件 / 用例                                                | 实测耗时          | 备注                                                                                                                                                                                     |
| --------------------- | ------------------------------------------------------------------------------- | ---------------------------------------------------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 前端分层统计          | `npm run test:layers:stats`                                                     | Vitest 总 `1012` 个文件；默认可运行 `1011`；live-gated `1` | 统计脚本级        | 同一事实源来自 `scripts/lib/vitest-layer-classifier.mjs`；unit `485`、component `397`，component VM 迁移候选当前发布基线为 `22` 个；后续继续降回 `12` / `8` 或更低                              |
| 前端 Unit             | `npm run test:unit`                                                             | 最近有效默认复测 `483` 个文件，`2851` 个 case              | `161.62s`（通过） | 本地 / AI TDD 默认第一轮信号保持绿色；unit runner 默认 Node + threads pool，`--single-fork` / `--pool=forks` 可回退；高负载下的 `293.89s` / `366.78s` 只作为环境噪声记录，不作为稳定基准 |
| 前端全量 Vitest       | `npm run test:frontend:all`                                                     | 默认可运行 `891` 个文件；最近完整跑完 `63/63` 批次         | 约 `306.95s`      | 交付前 / CI 全量入口；runner 不输出聚合 case 总数                                                                                                                                        |
| 前端 Integration 定向 | `npm run test:integration -- src/lib/layered-design/export.integration.test.ts` | `1` 个文件，`16` 个 case                                   | `29.83s`（通过）  | `layered-design` ZIP / PSD 导出属于重二进制打包验证，已移出 unit                                                                                                                         |
| Rust Unit             | `npm run test:rust:unit`                                                        | `1551` passed，`1` ignored                                 | `61.76s` wall     | 后端 TDD 默认第一轮信号；另有 `make tdd-rust-filter FILTER=...` 做单测试名定向回路                                                                                                       |

### 2026-06-03 速度优先恢复点

如果后续 Agent 从本文件恢复本任务，先处理速度，再处理候选数量治理。当前有效证据如下：

- 现有 `npm run test:unit` 绿色；调整前由于 `scripts/run-vitest-layer.mjs` 强制 `--poolOptions.forks.singleFork`，486 个 unit 文件全层单 fork 跑完约 `213.05s`。
- 用同一批 unit 文件直接调用 Vitest、去掉 `singleFork` 后，486 files / 2849 tests 全部通过，耗时 `175.46s`；这说明优先去掉 unit 默认单 fork 预计可直接节省约 `37s`。
- 已完成第一刀：`test:unit` 默认不再强制 `singleFork`，`--single-fork` 与 `LIME_VITEST_SINGLE_FORK=1` 可作为排查回退；真实入口复测 486 files / 2853 tests 全部通过，耗时 `183.71s`，相比 `213.05s` 快约 `29s`。
- 进一步加 `--environment node` 后，耗时降到 `137.44s`，且只有 `4` 个文件、`6` 条断言失败；这说明大多数 unit 已经能在 Node 环境运行，当前最大的速度收益来自把少数浏览器依赖 unit 隔离出去。
- 已完成第二刀：unit 默认追加 `--environment node`；以下 4 个 browser-dependent 测试已显式移到 component 层：
  - `src/lib/crashReporting.component.test.ts`
  - `src/lib/workspaceHealthTelemetry.component.test.ts`
  - `src/lib/utils/scheduleMinimumDelayIdleTask.component.test.ts`
  - `src/components/agent/chat/hooks/agentStreamSubmitDraft.component.test.ts`
- 真实入口复测 `npm run test:unit` 482 files / 2841 tests 全部通过，耗时 `138.63s`，已达到当前 `140s` 级目标；Vitest summary 中 `environment 241ms`，说明全层 jsdom 成本已从 unit 移除。
- 已完成第三刀：unit 默认追加 `--pool threads`，`--pool=forks`、`--single-fork`、`LIME_VITEST_SINGLE_FORK=1` 可回退。实测证据：
  - `/usr/bin/time -p npm run test:unit -- --pool=threads`：483 files / 2848 tests 通过，`real 115.59`。
  - `/usr/bin/time -p npm run test:unit`：483 files / 2851 tests 通过，`real 161.62`。
  - `/usr/bin/time -p npm run test:unit -- --pool=forks`：484 files / 2858 tests 通过，`real 223.28`；该轮已包含并行新增的 `toolNameFamily.unit.test.ts`。
  - `/usr/bin/time -p npm run test:unit`（默认 threads 后高负载复测）：484 files / 2858 tests 通过，`real 293.89`。
  - `/usr/bin/time -p npm run test:unit`（临时取消显式 pool 后同一高负载复测）：484 files / 2859 tests 通过，`real 366.78`。
  - 当时机器 `load averages: 95.67 82.56 71.54`，且存在两个 `tsc --noEmit` 和多个高 CPU GUI / browser 进程；因此这两轮只证明当前机器负载会严重放大 wall time，不应作为回退 threads 默认的稳定证据。
  - 结论：threads pool 已验证绿色，且同阶段快于显式 forks；当前保留 unit 默认 threads。后续速度基准必须在工作树和机器负载安静后复测。
- 已完成第四刀：新增 `src/test/fastCheckRuns.ts`，把本地 / AI TDD 的 fast-check 属性测试 runs 默认降到 `25`，CI 保持原始 `50/100` runs，`LIME_FAST_CHECK_RUNS=100` 可在本地强制满量；当前 unit 层 fast-check 文件已全部接入 helper，覆盖 `src/lib/artifact/streaming.test.ts`、`store.test.ts`、`registry.test.ts`、`parser.test.ts`、`src/lib/api/importExport.test.ts`、`src/lib/config/providers.test.ts`、`src/lib/utils/apiKeyMask.test.ts`、`src/components/artifact/ArtifactRenderer.test.ts`、`ArtifactToolbar.test.ts`、`src/components/api-key-provider/ApiKeyProviderSection.test.ts`、`ProviderSetting.test.ts`、`providerConfigUtils.test.ts`、`src/icons/providers/providers-icons.test.ts`。
  - `npm run test:unit -- src/test/fastCheckRuns.unit.test.ts src/lib/artifact/streaming.test.ts src/lib/artifact/store.test.ts src/lib/artifact/registry.test.ts src/lib/artifact/parser.test.ts src/lib/api/importExport.test.ts src/lib/utils/apiKeyMask.test.ts`：7 files / 63 tests 通过，Vitest duration `3.95s`。
  - `npm run test:unit -- src/components/api-key-provider/ApiKeyProviderSection.test.ts src/components/api-key-provider/ProviderSetting.test.ts src/components/api-key-provider/providerConfigUtils.test.ts src/components/artifact/ArtifactRenderer.test.ts src/components/artifact/ArtifactToolbar.test.ts src/icons/providers/providers-icons.test.ts src/lib/config/providers.test.ts`：7 files / 105 tests 通过，Vitest duration `11.66s`。
  - `CI=1 npm run test:unit -- src/lib/utils/apiKeyMask.test.ts`：1 file / 7 tests 通过，证明 CI 环境路径可执行；`fastCheckRuns.unit.test.ts` 机械锁住 CI 仍返回原始 runs。
- 已开始治理 `useWorkspaceSendActions.test.tsx` 候选：`src/components/agent/chat/workspace/workspaceModelSkillLaunchRequestContext.ts` 承载 model skill launch request context、session binding 和 metadata wrapper 的纯 current helper；`workspaceModelSkillLaunchRequestContext.unit.test.ts` 进入 unit 层，覆盖播报、素材、转写、文本转换、URL、排版、网页、PPT、表单和 session 绑定。`useWorkspaceSendActions.ts` 从约 5475 行降到约 5039 行；component 候选数暂未下降。

下一次重启优先级：

1. **先稳基准**：工作树和机器负载安静后复跑一次 `/usr/bin/time -p npm run test:unit`，确认默认 Node + threads + 本地 fast-check 降采样后的稳定耗时；如果仍明显慢于 `138.63s`，优先做 transform / collect 慢文件画像，而不是继续试错 pool 默认。
2. **再治理候选数**：当前发布基线剩余 22 个 component migration candidates 仍是下一优先级；优先继续 `useWorkspaceSendActions.test.tsx`，把已由 `workspaceModelSkillLaunchRequestContext.unit.test.ts` 覆盖的重复 metadata 挂载断言删除或拆成 focused component suites，先降回 `12` / `8`。
3. **最后看 Rust**：`npm run test:rust:unit` 当前约 `61.76s`，后端后续优化重点应是定向 crate / filter 回路，而不是先全量重构 Rust 测试。

前端 Vitest 当前分层：

| 层级        | 文件数 | 默认可运行 | Live-gated |
| ----------- | -----: | ---------: | ---------: |
| Unit        |  `485` |      `485` |        `0` |
| Component   |  `397` |      `397` |        `0` |
| Contract    |   `78` |       `78` |        `0` |
| Integration |   `51` |       `51` |        `0` |
| E2E         |    `1` |        `0` |        `1` |

## 前端 View Model 策略

### 适合抽到 View Model 的逻辑

- 从 runtime/session/thread/state 计算 UI 展示模型
- 从用户输入、选中项、Provider 能力推导按钮状态和提交参数
- 消息、工具调用、artifact、task preview 的分组、排序、去重、状态折叠
- 表单草稿、筛选、分页、空态、错误态、loading 态的状态转换
- GUI 事件映射为 command/action 的规划逻辑

### 不应放进 View Model 的内容

- DOM 测量、滚动、焦点、快捷键监听等真实浏览器行为
- Tauri / DevBridge / 文件系统 / 网络调用本身
- 纯视觉布局、CSS、动画细节
- 需要真实壳或真实用户流才能证明的行为

### 测试分配

- VM / projection / selector：大量纯单元测试，作为 TDD 默认反馈环。
- 组件：少量接线测试，证明 VM 输出被正确渲染、关键按钮事件会触发对应 action。
- E2E / smoke：覆盖“用户能完成任务”的主路径，不重复测试 VM 的所有分支。

## 阶段计划

### P0：建立测试分层入口

- 新增 Vitest 分层 runner，支持 `unit/component/contract/integration/e2e`。
- 保留 `npm test` 原语义，新增分层命令，不破坏现有 CI。
- 分类规则先基于文件名和静态特征，避免第一刀批量迁移测试文件。
- 完成标准：`npm run test:unit -- --list` 能快速列出纯单元候选，`npm run test:unit -- <file>` 能运行指定纯单元测试，`npm run test:layers:stats` 能输出治理后统计。

### P1：把 TDD 默认入口切到 Unit

状态：第一刀已完成。

- 在工程文档中明确：AI / 本地 TDD 默认先跑 `npm run test:unit` 和相关文件。
- `verify:local` 继续作为交付入口，不被 `test:unit` 替代。
- 完成标准：普通纯逻辑改动无需跑完整 `npm test` 就能得到第一轮信号。

### P2：热点组件 VM 化

状态：已开始，先从 `AgentChatPage` shell 路由抽取小型 View Model 作为模板。

优先迁移当前耗时和复杂度最高的前端测试：

1. `src/components/agent/chat/index.test.tsx`
2. `src/components/agent/chat/hooks/useAsterAgentChat.test.tsx`
3. `src/components/agent/chat/workspace/*PreviewRuntime.test.tsx`
4. `src/components/agent/chat/components/HarnessStatusPanel.test.tsx`

迁移方式：

- 从大组件/大 hook 中抽出纯 projection、selector、command planner。
- 新增 `*.unit.test.ts` 覆盖状态转换和边界分支。
- 原 `*.test.tsx` 降为少量组件接线测试，或改名为 `*.component.test.tsx`。
- 核心用户路径交给 `verify:gui-smoke` 或 Playwright 续测。

完成标准：`src/components/agent/chat/index.test.tsx` 单文件耗时从约 `55s` 降到可接受范围，且对应 VM 单测进入 `test:unit`。

当前模板：

- `src/components/agent/chat/agentChatPageShellViewModel.ts`：承载 `new-task` 直达工作区意图判断、`claw` 强制切换和聊天面板展示策略。
- `src/components/agent/chat/agentChatPageShellViewModel.unit.test.ts`：覆盖文本、图片、站点技能、服务技能、输入能力、资料包、项目文件和浏览器协助等纯路由分支。
- `src/components/agent/chat/agentChatWorkspaceShellViewModel.ts`：承载工作区 shell 的展示消息、聊天面板、侧栏切换和工作区图片任务恢复策略。
- `src/components/agent/chat/agentChatWorkspaceShellViewModel.unit.test.ts`：覆盖空白首页、执行态、任务中心草稿压制、画布/主题工作台、紧凑工作台和非 `new-task` 入口等纯布局分支。
- `src/components/agent/chat/index.shell-routing.test.tsx`：保留 React 接线层测试，证明 VM 输出被传给 `AgentChatWorkspace`。
- `src/components/agent/chat/components/harnessStatusPanelViewModel.ts`：承载 Harness 状态面板中的状态标签、Badge 变体、工具友好标签、子任务摘要、文件审阅、diff summary、输出信号展示、URL / path 文本识别、runtime 任务展示、工具库存 label / filter / stats / sort、handoff / evidence / replay / analysis 制品展示、browser replay artifact 构造、LimeCore policy 展示和人工确认风险 / 描述等纯 presentation / selector 逻辑。
- `src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts`：覆盖子任务 runtime/session type、工具标签、子任务汇总、文件审阅、active write 描述、diff 状态 / summary、输出信号状态 / 路径 / 卡片展示、URL 归一化 / 文本切分 / 文件路径识别 / 搜索输出识别、runtime phase / status / progress / task presentation、工具库存执行来源 / 策略标签 / 筛选统计 / runtime 工具排序、handoff / evidence / replay / analysis 制品 label / size / time、browser replay artifact metadata、LimeCore policy 引用 / 缺失输入 / 决策汇总、审核状态/风险/权限/限制和 replay 推广命令构造等纯展示分支。
- `src/components/agent/chat/components/HarnessStatusPanelPrimitives.tsx`：承载 Harness 面板通用展示 primitive，包括 diff mini panel、可交互文本、路径链接、可点击 badge、summary card 和库存统计卡片。
- `src/components/agent/chat/components/HarnessSearchOutputCards.tsx`：承载搜索输出单卡与批次卡，主面板只保留输出信号分组和打开详情的事件接线。
- `src/components/agent/chat/components/HarnessEvidenceSummarySections.tsx`：承载 Browser Assist replay 索引摘要和 LimeCore policy 索引摘要，主面板只保留证据包导出 / 预览状态。
- `src/components/agent/chat/components/harnessPanelText.ts`：承载 Harness 面板 generated key 的 `agent` namespace 文案 fallback helper，避免拆分组件复制 i18n fallback 逻辑。
- `src/components/agent/chat/hooks/agentChatAutoTitleViewModel.ts`：承载 `useAsterAgentChat` 自动标题中的占位标题判断、assistant 预览标题判断、用户文本存在性判断、标题生成上下文构造，以及生成标题回填 topic 的纯 reducer。
- `src/components/agent/chat/hooks/agentChatAutoTitleViewModel.unit.test.ts`：覆盖自动标题占位、预览派生标题、是否触发标题生成、用户文本过滤、生成上下文 `1000` 字符裁剪，以及生成标题回填命中 / 未命中 / 无变化复用。
- `src/components/agent/chat/utils/submitOpRuntimeCompaction.ts`：承载 turn submit 前的 provider/model、execution strategy、thinking、web_search、access mode 与 Harness metadata 去重事实源，避免通过重 hook 测试间接验证纯配置裁剪。
- `src/components/agent/chat/utils/submitOpRuntimeCompaction.test.ts`：覆盖 runtime / synced preference 已承接时裁掉重复字段、未同步时保留显式变更、fast response / image routing 特例、team selection 与 access mode metadata 裁剪。
- `src/components/agent/chat/hooks/agentSessionTopicViewModel.ts`：承载 `useAgentSession` 中 topic/session detail 映射、runtime thread 状态判定、topic upsert 排序、新建 session 草稿插入、远端校验 session 补入、execution strategy 写回、live snapshot 写回 reducer、transient messages/turns/items tail selector，以及 restore candidate 工作区隔离清洗计划。
- `src/components/agent/chat/hooks/agentSessionTopicViewModel.unit.test.ts`：覆盖自动恢复运行态、queued/waiting/failed 状态映射、排队预览、topic 本地 pin/unread/tag 保留、新建 session 草稿插入 / 去重、远端校验 session 补入 / 已存在复用、execution strategy 写回、live snapshot 写回 / 无变化复用、transient 历史窗口裁剪，以及 restore candidate 空值/辅助会话/旧默认工作区/跨工作区/合法映射清洗。
- `src/components/agent/chat/hooks/agentSessionRestoreViewModel.ts`：承载 `useAgentSession` 中工作区切换 / 恢复时从 transient storage 与 cached snapshot 推导首屏 session snapshot、timeline、历史窗口和缓存快照打点上下文的纯状态逻辑。
- `src/components/agent/chat/hooks/agentSessionRestoreViewModel.unit.test.ts`：覆盖直接使用 cached snapshot、合并 cached/transient messages、timeline 回退、current turn 优先级、scoped snapshot 规范化，以及 cached topic snapshot 的历史窗口和 metric context 派生。
- `src/components/agent/chat/hooks/agentSessionState.ts`：承载 session snapshot / hydration 状态转换、detail hydration 策略、restorable topic 选择，以及当前会话缺失于 topics 时清空 / 跳过 / 远程校验的纯决策。
- `src/components/agent/chat/hooks/agentSessionState.test.ts`：覆盖空会话快照、恢复目标选择、detail hydration 延后策略、同会话 hydration 合并，以及 missing session from topics 的 inactive / detached / auxiliary / remote verify 分支。
- `src/components/agent/chat/hooks/sessionSwitchSnapshotController.ts`：承载 topic switch 中 cached snapshot 加载 / 应用 / 刷新策略计划、defer hydration 状态计划、pending shell 状态计划、切换开始状态重置计划、切换指标上下文，以及 in-flight switch 复用的纯决策。
- `src/components/agent/chat/hooks/sessionSwitchSnapshotController.test.ts`：覆盖 cached snapshot 命中、pending shell、指标上下文、缓存读取 / 应用 / 立即刷新策略、defer hydration 直接/延迟加载模式、pending shell 空会话壳策略、切换开始状态计划，以及普通同 topic 切换可复用 / 强刷 / 自动恢复 / detached / session start hooks 禁止复用分支。
- `src/components/agent/chat/hooks/sessionFinalizeController.ts`：承载会话 detail finalize 阶段的 workspace restore 拒绝、执行策略 fallback / override，以及成功 finalize 后 restore/hydration 状态收尾计划。
- `src/components/agent/chat/hooks/sessionFinalizeController.test.ts`：覆盖 known workspace 解析、跨 workspace 拒绝上下文、shadow execution strategy fallback、最终 execution strategy override，以及 finalize 成功后的状态收尾计划。
- `src/components/agent/chat/hooks/sessionMetadataSyncController.ts`：承载 finalize 成功路径中的 metadata sync 输入选择、access mode / provider / execution strategy patch 规划、本地状态应用计划、metadata sync 成功应用计划、topic execution strategy 回填 reducer、switch success 指标上下文和 metadata sync 执行 fallback。
- `src/components/agent/chat/hooks/sessionMetadataSyncController.test.ts`：覆盖 runtime provider preference 优先级、session storage preference fallback、runtime metadata 无回填、session storage / workspace default patch、finalize 本地状态应用计划、metadata sync 成功应用计划、topic execution strategy 回填 reducer、switch success metric 和 metadata sync runtime fallback。
- `src/components/agent/chat/hooks/sessionPostFinalizePersistenceController.ts`：承载 finalize 成功后的 workspace 持久化、topic workspace 回填、provider preference 应用、副作用应用入口计划和 topic workspace 回填 reducer。
- `src/components/agent/chat/hooks/sessionPostFinalizePersistenceController.test.ts`：覆盖 topic workspace 解析顺序、持久化 workspace 解析、post finalize persistence plan、副作用 apply plan 和 runtime workspace 回填 reducer。
- `src/components/agent/chat/hooks/agentChatActionState.ts`：承载 action_required 写入 assistant 消息、确认提交后在 pending action / submitted in-flight / message actionRequests / contentParts / runtime status 之间同步状态、fallback ask 回答排队和真实 request 匹配，以及 replay request 结果到 ActionRequired 的纯 reducer / planner / mapper。
- `src/components/agent/chat/hooks/agentChatActionState.unit.test.ts`：覆盖 ask/elicitation 提交后保留 submitted 面板、tool confirmation 确认后移除请求、未命中 requestId 不污染消息、submitted in-flight upsert / 清理、fallback ask 排队、同 assistant 消息真实请求匹配、queued 状态同步，以及 replay request questions/options/scope 映射。
- `src/components/agent/chat/workspace/generalWorkbenchHelpers.ts`：承载通用工作台运行中 workflow steps 的工具调用到业务步骤标题 / 状态投影。
- `src/components/agent/chat/workspace/generalWorkbenchWorkflowSteps.unit.test.ts`：覆盖写文件、封面图、搜索、浏览器导航、点击、页面分析和媒体命令等工具调用到 workflow steps 的纯投影分支，组件层只保留 `workflowSteps` 传给 `Inputbar` 的接线断言。
- `src/components/agent/chat/utils/taskCenterTabs.ts`：承载任务中心标签排序 / 可见性 / fallback restore planner / detached 会话判断、route sync / fallback reconcile、topic 切换参数、失败回滚和关闭标签 fallback plan。
- `src/components/agent/chat/utils/taskCenterTabs.test.ts`：覆盖默认标签、workspace tab map、fallback topic / fallback restore planner、detached 隐藏、预览焦点、切换中状态、初始会话 detached / waiting / workspace_error 恢复参数、active topic 重开跳过、topic 切换选项、失败回滚和关闭标签 plan 等纯 selector 分支。
- `src/components/agent/chat/workspace/taskCenterDraftTabs.ts`：承载任务中心草稿标签构造、upsert/remove、running/failed 标记、active draft 解析、warmup 条件和关闭草稿 fallback plan。
- `src/components/agent/chat/workspace/taskCenterDraftTabs.unit.test.ts`：覆盖草稿标签去重插入、运行态/失败态标记、active draft 解析、预热条件和关闭草稿后的 fallback 选择。
- `src/components/agent/chat/utils/workflowInputState.ts`：承载 Inputbar 工作流快捷动作、队列、进度、摘要和生成面板显示状态的纯 builder，`useWorkflowInputState` 只保留 React 薄包装。
- `src/components/agent/chat/utils/workflowInputState.test.ts`：去掉 `createRoot` / `act` 挂载，直接覆盖非工作区场景、等待 gate、开放步骤队列 / 进度和发送态生成面板等纯状态分支。

后续迁移要求：

- 每次从重组件测试中抽出一个 VM / projection / selector，都要补一个 `*.unit.test.ts`。
- 原 `*.test.tsx` 只保留关键渲染和事件接线，不重复覆盖 VM 的所有分支。
- 若 unit 层测试依赖真实 `setTimeout`、idle callback、DOM 或浏览器全局，应优先改成可控调度器或移入 component / integration 层。

### P3：命名和 CI 收敛

状态：CI 分层口径已落地，PR 保持快速反馈，`main` push / 手动触发保留全量质量面。

- 新增或迁移测试时使用后缀：
  - `*.unit.test.ts`
  - `*.component.test.tsx`
  - `*.contract.test.ts`
  - `*.integration.test.ts`
  - `*.e2e.test.ts`
  - `*.live.test.ts`
- `*.live.test.ts` 归入 E2E 层，但默认受 live Provider gate 排除；必须显式设置 `LIME_ALLOW_LIVE_PROVIDER_SMOKE=1` / `LIME_REAL_API_TEST=1` 才会进入默认可运行集合。
- 显式后缀不能降低风险层级；`*.unit.test.*` 如果触碰 React/jsdom、DevBridge/Tauri、文件系统、网络或 Playwright，分类器会自动提升到 component / contract / integration / e2e，避免非纯单元进入本地 TDD 默认入口。
- PR 前端快速门禁跑 `lint`、`typecheck`、`test:unit`、`test:contract`，不再把全量 Vitest 作为默认 PR 前端反馈环。
- `main` push / 手动触发继续跑 `lint`、`typecheck`、`test:frontend:all`、`test:rust`、`verify:gui-smoke`。
- `Bridge & Contracts` 仍按 changed-path 单独跑 `test:bridge` 与 `test:contracts`；这里的 `test:contract` 是 Vitest contract layer，`test:contracts` 是命令 / 治理契约聚合入口。

## 风险与边界

- `test:unit` 不是交付证明，只是 TDD 快速反馈。
- GUI 产品改动仍必须按 `internal/aiprompts/quality-workflow.md` 补 GUI smoke / E2E。
- 迁移测试时不能为了追求纯单元而删除真实回归覆盖；应先把覆盖迁到 VM 或 E2E 后再删重组件断言。
- live Provider 测试继续保持显式 opt-in，不进入默认单元测试。

## 进度日志

- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且带 `large-component-suite` / `large-component-file` / `business-logic-keywords` 的 `src/components/agent/chat/index.test.tsx`，确认该文件覆盖任务中心标签、侧栏、项目恢复、通用工作台、自动引导、浏览器协助、服务技能 A2UI 和当前 A2UI 等真实 React DOM / hook 生命周期 / localStorage / mocked API 接线，不适合整体硬降为 pure unit；本刀只按行为边界拆小 component suite，并把公共 mock、React root 挂载、flush / wait helper、send call 解析、browser assist fixture 和默认 AgentChat state 下沉到 `index.testFixtures.tsx`。原 `index.test.tsx` 从 `6989` 行降到 `482` 行、保留任务中心基础 `6` 个用例；新增 `index.taskTabs02.test.tsx` `5` 个、`index.stopTeam.test.tsx` `1` 个、`index.projectRestore.test.tsx` `7` 个、`index.sidebar.test.tsx` `7` 个、`index.workbench01.test.tsx` `11` 个、`index.workbench02.test.tsx` `5` 个、`index.workbench03.test.tsx` `7` 个、`index.workbench04.test.tsx` `7` 个、`index.workbench05.test.tsx` `1` 个、`index.autoGuide01.test.tsx` `11` 个、`index.autoGuide02.test.tsx` `9` 个、`index.autoGuide03.test.tsx` `7` 个、`index.autoGuide04.test.tsx` `4` 个、`index.noVideo.test.tsx` `1` 个、`index.serviceSkillA2ui.test.tsx` `2` 个、`index.currentA2ui.test.tsx` `5` 个；fixture 为 `1480` 行且不进入测试收集。case 总数保持 `96`，不删回归。拆分中同步修正 Vitest hoist 边界：suite 直接从原模块导入 `buildHomeAgentParams`、task center events、fileBrowser、webview API 和 browser assist storage key，不再从 fixture re-export 外部模块；发送断言按当前 `react` execution strategy 事实源校准。验证：`npm run test:component -- --list --explain ...index*.test.tsx ...index.testFixtures.tsx` 只列出 `17` 个真实 suite，fixture 不被收集；`npm run test:component -- ...index*.test.tsx` 实测 `17` 个文件、`96` 个 case、总 `86.41s` 通过；`npx eslint ...index*.test.tsx ...index.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `989`、默认可运行 `988`、live-gated `1`，分层为 unit `477`、component `384`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `10` 降到 `9`，`index.test.tsx` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。

- 2026-05-31：记录测试分层目标、前端 VM 策略与阶段计划。当前第一刀目标是新增分层 runner 和 npm 入口，不改现有 `npm test` 语义。
- 2026-05-31：P0 第一刀完成。新增 `test:unit`、`test:component`、`test:contract`、`test:integration`、`test:frontend:all`；`npm run test:unit` 当前筛出 `384` 个文件、`2072` 个 case，实测 `20.62s` 通过。`npm run test:contract -- src/lib/dev-bridge/http-client.test.ts` 实测 `27` 个 case，`0.70s` 通过。当前 `test:unit` 为稳定入口仍沿用默认 jsdom 环境；纯 Node VM 单测收敛放到 P2 迁移阶段。
- 2026-05-31：P1 第一刀完成。`internal/aiprompts/quality-workflow.md` 与 `internal/test/unit-tests.md` 已把本地 / AI TDD 默认入口收敛到 `npm run test:unit`，同时明确它不是 `verify:local`、`test:contracts`、`verify:gui-smoke` 的替代品。
- 2026-05-31：P2 第一刀完成。抽取 `agentChatPageShellViewModel`，新增 `agentChatPageShellViewModel.unit.test.ts`，将 `AgentChatPage` 的 `new-task` 直达工作区 shell 路由判断落到纯 VM 单测；`npm run test:unit -- src/components/agent/chat/agentChatPageShellViewModel.unit.test.ts` 实测 `8` 个 case，约 `0.73s` 通过；`npm run test:component -- src/components/agent/chat/index.shell-routing.test.tsx` 实测 `6` 个 case，约 `0.74s` 通过。
- 2026-05-31：修复 unit 层稳定性缺口。`sessionMetadataSyncScheduler.test.ts` 去掉真实 `setTimeout(0)` 式 promise flush，改为纯 microtask flush，避免 timer 状态泄漏导致伪单元测试超时；完整 `npm run test:unit` 当前筛出 `385` 个文件、`2080` 个 case，实测 `21.43s` 通过。
- 2026-05-31：P0/P3 分层治理收口。新增 `test:e2e` 与 `test:layers:stats`，将 Vitest 分层事实源收敛到 `scripts/lib/vitest-layer-classifier.mjs` 与 `scripts/report-vitest-layers.mjs`。当前 `npm run test:layers:stats` 统计：总 `882` 个 Vitest 文件，默认可运行 `881`，live-gated `1`；分层为 unit `385`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：补齐最终治理统计。最近一次完整 `npm run test:unit` 实测 `385` 个文件、`2080` 个 case、`25.01s` 通过；最近一次 `cargo test --manifest-path "lime-rs/Cargo.toml"` 实测 `13.17s` total，`1528` passed，`3` ignored。
- 2026-05-31：P2 第二刀完成。抽取 `agentChatWorkspaceShellViewModel`，将 `AgentChatWorkspace` 内联的展示消息、聊天面板、侧栏切换和工作区图片任务恢复判断落到纯 VM 单测；`npm run test:unit -- src/components/agent/chat/agentChatWorkspaceShellViewModel.unit.test.ts` 实测 `8` 个 case，约 `0.96s` 通过；`npm run test:component -- src/components/agent/chat/index.test.tsx -t "showChatPanel=false|new-task 执行态"` 实际回归整文件 `120` 个 case，约 `56.43s` 通过。当前 `npm run test:unit` 单独复跑 `386` 个文件、`2088` 个 case，实测 `21.68s` 通过；`npm run test:layers:stats` 统计：总 `883` 个 Vitest 文件，默认可运行 `882`，live-gated `1`；分层为 unit `386`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：修复 unit 层 i18n 状态污染。`agentStreamErrorController.test.ts` 在测试级固定 `zh-CN` locale，避免完整 `test:unit` 中其他 locale 测试污染 402 Provider 友好提示断言；`npm run test:unit -- src/components/agent/chat/hooks/agentStreamErrorController.test.ts` 实测 `15` 个 case 通过。
- 2026-05-31：P2 第二刀 GUI 验证完成。`npm run verify:gui-smoke` 完整通过，包含 `workspace-ready`、`browser-runtime`、`site-adapters`、Service Skill entry、runtime tool surface、页面级 runtime tool surface、code runtime fixture、approval/sandbox、`@` command registry、Agent Apps、Knowledge GUI、design canvas；`claw-chat-ready-streaming` 仍按规则跳过真实 Provider 调用。
- 2026-05-31：P2 第三刀完成。抽取 `harnessStatusPanelViewModel`，将 `HarnessStatusPanel` 中可纯测的状态标签、Badge 变体、工具友好标签、子任务摘要、LimeCore policy 与人工审核展示标签迁移到 VM 单测；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `8` 个 case，约 `3.53s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case，约 `12.33s` 通过；`npm run typecheck`、`npm run lint` 均通过。当前 `npm run test:layers:stats` 统计：总 `884` 个 Vitest 文件，默认可运行 `883`，live-gated `1`；分层为 unit `387`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：P2 第四刀完成。抽取 `agentChatAutoTitleViewModel`，将 `useAsterAgentChat` 内联的自动标题占位判断、assistant 预览派生判断、用户文本过滤和标题生成上下文构造迁移到 VM 单测；`npm run test:unit -- src/components/agent/chat/hooks/agentChatAutoTitleViewModel.unit.test.ts` 实测 `6` 个 case，约 `0.89s` 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "自动标题"` 实测 `2` 个相关 case 通过；`npm run typecheck`、`npm run lint` 均通过。当前 `npm run test:layers:stats` 统计：总 `885` 个 Vitest 文件，默认可运行 `884`，live-gated `1`；分层为 unit `388`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：P2 第五刀完成。补强 `submitOpRuntimeCompaction` 纯单元覆盖，将 `useAsterAgentChat.test.tsx` 后半段反复间接验证的 synced `recent_preferences` 与 `access_mode` metadata 去重沉到 unit 层，并修复偏好全部裁掉后残留 `{ harness: {} }` 的空 metadata；`npm run test:unit -- src/components/agent/chat/utils/submitOpRuntimeCompaction.test.ts` 实测 `12` 个 case，约 `0.74s` 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "recent_preferences|access_mode"` 实测 `9` 个相关 case 通过；`npm run typecheck`、`npm run lint` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `885` 个 Vitest 文件，默认可运行 `884`，live-gated `1`；分层为 unit `388`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：P2 第六刀完成。抽取 `agentSessionTopicViewModel`，将 `useAgentSession` 中 topic/session detail 映射、runtime thread 状态、topic upsert 和 transient tail selector 迁移到纯 VM 单测；`npm run test:unit -- src/components/agent/chat/hooks/agentSessionTopicViewModel.unit.test.ts` 实测 `7` 个 case 通过；`npm run test:unit -- src/components/agent/chat/hooks/agentSessionState.test.ts src/components/agent/chat/hooks/agentSessionRefresh.test.ts` 实测 `22` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|排队会话|话题列表暂时"` 实测 `23` 个相关 case 通过；`npm run typecheck`、`npm run lint` 均通过。当前 `npm run test:layers:stats` 统计：总 `886` 个 Vitest 文件，默认可运行 `885`，live-gated `1`；分层为 unit `389`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：P2 第七刀完成。抽取 `agentSessionRestoreViewModel`，将 `useAgentSession` 工作区恢复 effect 中的 transient/cached snapshot 选择、消息合并、timeline 回退、current turn 优先级和历史窗口推导迁移到纯 VM 单测；`npm run test:unit -- src/components/agent/chat/hooks/agentSessionRestoreViewModel.unit.test.ts` 实测 `4` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|排队会话|话题列表暂时"` 实测 `23` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计：总 `887` 个 Vitest 文件，默认可运行 `886`，live-gated `1`；分层为 unit `390`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：P2 第八刀完成。补强 `agentChatActionState`，将 `useAgentTools` 中确认提交后同步 message actionRequests、contentParts 与 runtime status 的状态转换抽成纯 reducer；`npm run test:unit -- src/components/agent/chat/hooks/agentChatActionState.unit.test.ts` 实测 `3` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "action_required|fallback ask|elicitation"` 实测 `29` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：P2 第九刀完成。继续补强 `agentChatActionState`，将 fallback ask 回答暂存、同 assistant 消息真实 request 匹配、queued 状态写回 pending action / message actionRequests / contentParts 的逻辑从 `useAgentTools` 抽成纯 planner / reducer；`npm run test:unit -- src/components/agent/chat/hooks/agentChatActionState.unit.test.ts` 实测 `6` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "fallback ask"` 实测 `2` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：P2 第十刀完成。继续补强 `agentChatActionState`，将 `useAgentTools.replayPendingAction` 中 replay response 到 `ActionRequired` 的字段映射、questions/options 归一化、scope 映射和非法 arguments 裁剪抽成纯 mapper；`npm run test:unit -- src/components/agent/chat/hooks/agentChatActionState.unit.test.ts` 实测 `8` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "replayPendingAction"` 实测 `1` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：P2 第十一刀完成。补强 `agentSessionRestoreViewModel`，将 `useAgentSession.applyCachedTopicSnapshot` 中 cached topic snapshot 到首屏 session snapshot、history window 与 metric context 的派生逻辑抽成纯 VM；`npm run test:unit -- src/components/agent/chat/hooks/agentSessionRestoreViewModel.unit.test.ts` 实测 `6` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-05-31：P2 第十二刀完成。补强 `agentSessionTopicViewModel`，将 `useAgentSession` 中 restore candidate 清洗逻辑抽成纯计划，覆盖空候选、辅助会话、旧默认工作区、跨工作区和合法映射；`npm run test:unit -- src/components/agent/chat/hooks/agentSessionTopicViewModel.unit.test.ts` 实测 `9` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "恢复态|切换话题|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第十三刀完成。补强 `agentSessionState`，将 `useAgentSession` 中当前会话缺失于 topics 时的 detached 跳过、辅助会话清理、无活动清空和远程存在性校验决策抽成纯 selector；`npm run test:unit -- src/components/agent/chat/hooks/agentSessionState.test.ts` 实测 `20` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "恢复态|切换话题|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第十四刀完成。补强 `sessionSwitchSnapshotController`，将 `useAgentSession.switchTopic` 中 in-flight switch 复用判断抽成纯 selector，避免 force refresh、自动恢复、detached session 和 session start hooks 分支继续靠重 hook 间接覆盖；`npm run test:unit -- src/components/agent/chat/hooks/sessionSwitchSnapshotController.test.ts` 实测 `6` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第十五刀完成。继续补强 `sessionSwitchSnapshotController`，将 `useAgentSession.switchTopic` 开始阶段的当前 session preference 持久化目标、detached session 写入、auto restore 清理和 hydration 重置抽成纯 state plan；`npm run test:unit -- src/components/agent/chat/hooks/sessionSwitchSnapshotController.test.ts` 实测 `7` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第十六刀完成。继续补强 `sessionSwitchSnapshotController`，将 `useAgentSession.switchTopic` 中 cached snapshot 是否读取、是否先应用、是否需要立即刷新这一组策略抽成纯 planner，存储读取副作用仍留在 hook 接线层；`npm run test:unit -- src/components/agent/chat/hooks/sessionSwitchSnapshotController.test.ts` 实测 `8` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第十七刀完成。继续补强 `sessionSwitchSnapshotController`，将 `useAgentSession.switchTopic` 的 defer hydration 分支中 Chrome 状态应用、restore candidate、auto restore / hydration 状态清理和 detail load mode 抽成纯状态计划，异步加载与错误重试仍留在 hook 接线层；`npm run test:unit -- src/components/agent/chat/hooks/sessionSwitchSnapshotController.test.ts` 实测 `9` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第十八刀完成。继续补强 `sessionSwitchSnapshotController`，将 `useAgentSession.switchTopic` 的 pending shell 分支中空 session snapshot、history window 清理、Chrome 状态应用、restore candidate 和 hydration 状态设置抽成纯状态计划，execution strategy 归一化与 metric 仍留在 hook 接线层；`npm run test:unit -- src/components/agent/chat/hooks/sessionSwitchSnapshotController.test.ts` 实测 `10` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第十九刀完成。补强 `sessionFinalizeController`，将 `useAgentSession.finalizeResolvedTopicDetail` 成功末尾的 auto restore / hydration 状态收尾抽成纯 plan，作为后续继续下沉 runtime preference / access mode / metadata sync 成功路径的模板；`npm run test:unit -- src/components/agent/chat/hooks/sessionFinalizeController.test.ts` 实测 `6` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第二十刀完成。补强 `sessionMetadataSyncController`，将 `useAgentSession.finalizeResolvedTopicDetail` 中 runtime preference / access mode / session storage preference 到 metadata sync 入参的选择抽成纯 input plan，保持 `buildSessionMetadataSyncPlan` 继续作为 patch 生成事实源；`npm run test:unit -- src/components/agent/chat/hooks/sessionMetadataSyncController.test.ts` 实测 `8` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第二十一刀完成。补强 `sessionPostFinalizePersistenceController`，将 `useAgentSession.finalizeResolvedTopicDetail` 中保存 session workspace、回填 topic workspace、应用 provider preference 的副作用入口抽成纯 apply plan，保留实际写 storage / setTopics / apply preference 在 hook 接线层；`npm run test:unit -- src/components/agent/chat/hooks/sessionPostFinalizePersistenceController.test.ts` 实测 `4` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `888` 个 Vitest 文件，默认可运行 `887`，live-gated `1`；分层为 unit `391`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第二十二刀完成。补强 `sessionMetadataSyncController`，将 `useAgentSession.finalizeResolvedTopicDetail` 成功路径中的 runtime execution strategy synced 标记、access mode 本地应用 / 持久化判断，以及 switch success metric 上下文组装抽成纯 local state plan，实际 setState / storage / metric 记录仍留在 hook 接线层；`npm run test:unit -- src/components/agent/chat/hooks/sessionMetadataSyncController.test.ts` 实测 `10` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过。
- 2026-06-01：P2 第二十三刀完成。继续补强 `sessionMetadataSyncController`，将 `scheduleSessionMetadataSync.onSynced` 中 provider preference synced 标记、execution strategy synced 标记和 topic execution strategy 回填抽成纯 success apply plan，实际 mark / setTopics 仍保留在 hook 接线层；`npm run test:unit -- src/components/agent/chat/hooks/sessionMetadataSyncController.test.ts` 实测 `11` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过。
- 2026-06-01：P2 第二十四刀完成。补强 `sessionPostFinalizePersistenceController`，将 `useAgentSession.finalizeResolvedTopicDetail` 中 runtime workspace 回填目标 topic 的 `setTopics(prev => prev.map(...))` 抽成纯 reducer，保留 React state 写入在 hook 接线层；`npm run test:unit -- src/components/agent/chat/hooks/sessionPostFinalizePersistenceController.test.ts` 实测 `5` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过。
- 2026-06-01：P2 第二十五刀完成。继续补强 `sessionMetadataSyncController`，将 `scheduleSessionMetadataSync.onSynced` 成功后 fallback execution strategy 回填目标 topic 的 `setTopics(prev => prev.map(...))` 抽成纯 reducer；`npm run test:unit -- src/components/agent/chat/hooks/sessionMetadataSyncController.test.ts` 实测 `12` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过。
- 2026-06-01：P2 第二十六刀完成。补强 `agentSessionTopicViewModel`，将 `useAgentSession.updateTopicSnapshot` 中 live snapshot 写回目标 topic 的 `setTopics(prev => prev.map(...))` 抽成纯 reducer，并固化未改变 / 未命中时复用原数组，避免组件层继续间接覆盖该性能语义；`npm run test:unit -- src/components/agent/chat/hooks/agentSessionTopicViewModel.unit.test.ts` 实测 `11` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint` 均通过。当前 `npm run test:layers:stats` 统计：总 `889` 个 Vitest 文件，默认可运行 `888`，live-gated `1`；分层为 unit `392`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第二十七刀完成。继续补强 `agentSessionTopicViewModel`，将 `useAgentSession` 中新建 session 草稿插入 / 去重、当前 session 缺失后远端校验成功补入 topic、以及 `updateTopicExecutionStrategy` 的 topic execution strategy 写回抽成纯 reducer；`useAgentSession` 继续保留 runtime 调用和 React state 接线。`npm run test:unit -- src/components/agent/chat/hooks/agentSessionTopicViewModel.unit.test.ts` 实测 `14` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "切换话题|恢复态|话题列表暂时"` 实测 `22` 个相关 case 通过；`npm run typecheck`、`npm run lint` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `889` 个 Vitest 文件，默认可运行 `888`，live-gated `1`；分层为 unit `392`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第二十八刀完成。阶段性审计后继续补强 `agentChatActionState`，将 `useAgentTools` 中 submitted action in-flight upsert、acknowledged requestIds 清理 pending / in-flight 集合，以及 ask/elicitation 是否保留 submitted 面板的判断抽成纯 reducer / selector；实际 runtime respond、read model refresh 和 toast 仍留在 hook 接线层。`npm run test:unit -- src/components/agent/chat/hooks/agentChatActionState.unit.test.ts` 实测 `11` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "action_required|fallback ask|elicitation"` 实测 `29` 个相关 case 通过；`npm run typecheck`、`npm run lint` 均通过。当前 `npm run test:layers:stats` 统计保持：总 `889` 个 Vitest 文件，默认可运行 `888`，live-gated `1`；分层为 unit `392`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第二十九刀完成。继续补强 `agentChatAutoTitleViewModel`，将 `useAsterAgentChat` 自动标题成功后回填目标 topic 标题的 `setTopics(prev => prev.map(...))` 抽成纯 reducer，并固化生成标题为空、未命中目标和标题未变化时复用原数组；实际 runtime title generation / rename 仍保留在 hook 接线层。`npm run test:unit -- src/components/agent/chat/hooks/agentChatAutoTitleViewModel.unit.test.ts` 实测 `8` 个 case 通过；`npm run test:contract -- src/components/agent/chat/hooks/useAsterAgentChat.test.tsx -- -t "自动标题"` 实测 `2` 个相关 case 通过；`npm run typecheck`、`npm run lint`、`npm run test:layers:stats`、`npm run i18n:unused -- --check`、`git diff --check` 均通过。同步删除五语言资源中已无生产引用的 `agentChat.fileChangesSummary.truncated` 后，`npm run verify:local` 完整通过，包含 `verify:gui-smoke` 的 workspace-ready、browser-runtime、site-adapters、Agent runtime / approval sandbox、`@` command registry、Agent Apps、Knowledge GUI 和 design canvas；真实 Provider smoke 仍按默认规则跳过。
- 2026-06-01：P3 CI 收敛完成。`.github/workflows/quality.yml` 将 PR 前端 job 明确拆为 `Frontend Quick`，执行 `lint`、`typecheck`、`test:unit`、`test:contract`；新增 `Frontend Full` 与 `Rust Full` 在 `main` push / 手动触发执行 `test:frontend:all` 与 `test:rust`，并让 `GUI Smoke` 在全量质量面继续运行；`results` 汇总区分 `frontend_quick`、`frontend_full`、`rust_full`，避免 skipped job 被误判为失败。`internal/aiprompts/quality-workflow.md` 已同步 CI 口径；最新 `npm run test:layers:stats` 统计为总 `889` 个 Vitest 文件、默认可运行 `888`、live-gated `1`，分层为 unit `392`、component `370`、contract `77`、integration `49`、e2e `1`；完整 `npm run test:unit` 实测 `392` 个文件、`2155` 个 case、`26.02s` 通过，`npm run test:contract` 实测 `77` 个文件、`992` 个 case、`16.79s` 通过，`npm run test:frontend:all` 完整跑完 `63/63` 个批次且退出码为 `0`，`npm run test:rust` 实测 `1528` passed、`3` ignored、`14.49s` wall 通过；workflow YAML parser、`npm run lint`、`npm run typecheck`、`npm run test:bridge`、`npm run test:contracts`、`npm run verify:app-version`、`npm run i18n:scan`、`npm run i18n:unused -- --check`、`git diff --check` 均通过。
- 2026-06-01：P3 纯单元边界补强完成。修正 `scripts/lib/vitest-layer-classifier.mjs` 的显式后缀优先级，`*.unit.test.*` 不再能掩盖 React/jsdom、DevBridge/Tauri、文件系统、网络或 Playwright 等更高风险信号；显式高风险后缀仍可把无外部边界的测试提升到 contract / integration / e2e。`scripts/lib/vitest-layer-classifier.unit.test.mjs` 增加显式 unit 被提升的自举用例，并避免测试样例字符串污染自身分类；`npm run test:unit -- scripts/lib/vitest-layer-classifier.unit.test.mjs scripts/lib/vitest-layer-report.unit.test.mjs` 实测 `2` 个文件、`13` 个 case、`1.72s` 通过；`npm run test:layers:stats` 统计保持总 `889` 个 Vitest 文件、默认可运行 `888`、live-gated `1`，分层为 unit `392`、component `370`、contract `77`、integration `49`、e2e `1`。
- 2026-06-01：P2 第三十刀完成。将 `index.test.tsx` 中 3 个工作区编排工具步骤映射分支迁入 `generalWorkbenchWorkflowSteps.unit.test.ts`，直接覆盖 `buildGeneralWorkbenchWorkflowSteps` 对写文件、封面图失败、搜索、浏览器导航、点击、页面分析和媒体命令的业务标题 / 状态投影；`index.test.tsx` 只保留“运行中应展示真实技能与工具步骤”的接线断言。`npm run test:unit -- src/components/agent/chat/workspace/generalWorkbenchWorkflowSteps.unit.test.ts` 实测 `3` 个 case 通过；`npm run test:component -- src/components/agent/chat/index.test.tsx` 实测 `117` 个 case、`60.19s` 通过；`npm run test:layers:stats` 统计为总 `890` 个 Vitest 文件、默认可运行 `889`、live-gated `1`，分层为 unit `393`、component `370`、contract `77`、integration `49`、e2e `1`。这刀证明纯投影已迁出，但 `index.test.tsx` 单文件仍约一分钟，瓶颈仍是整体挂载式测试。
- 2026-06-01：P2 第三十一刀完成。将 `AgentChatWorkspace` 初始会话恢复的 `allowDetachedSession`、`forceRefresh`、`resumeSessionStartHooks` 选择抽成 `resolveInitialTaskSessionSwitchOptions`，用 `taskCenterTabs.test.ts` 覆盖普通历史会话、等待态任务和 `workspace_error` 任务分支；`index.test.tsx` 删除 2 个重复挂载分支后降到 `115` 个 case。`npm run test:unit -- src/components/agent/chat/utils/taskCenterTabs.test.ts` 实测 `23` 个 case、`1.05s` 通过；`npm run test:component -- src/components/agent/chat/index.test.tsx` 实测 `115` 个 case、`55.81s` 通过；完整 `npm run test:unit` 实测 `393` 个文件、`2164` 个 case、`28.32s` 通过；`npm run typecheck`、`npm run lint`、`npm run test:layers:stats` 均通过。下一刀仍应优先拆任务中心 tab / draft / fallback restore 的状态计划，而不是继续只删低成本断言。
- 2026-06-01：P2 第三十二刀完成。继续补强 `taskCenterTabs.ts`，将任务中心 route sync、fallback topic reconcile、local session override 清理和 transition topic 清理等状态选择抽成纯 selector / planner；`index.test.tsx` 删除 5 个重复挂载分支后从 `115` 降到 `110` 个 case，`taskCenterTabs.test.ts` 扩到 `28` 个 case。该刀把“路由与任务中心标签状态如何同步”的判断从组件挂载测试下沉到纯 selector，组件层只保留必要接线回归。
- 2026-06-01：P2 第三十三刀完成。新增 `taskCenterDraftTabs.ts` 与 `taskCenterDraftTabs.unit.test.ts`，将任务中心草稿标签构造、upsert/remove、running/failed 标记、active draft 解析、warmup 条件和关闭草稿 fallback plan 从 `AgentChatWorkspace` 下沉到纯 reducer；同时扩展 `taskCenterTabs.ts` 的 active reopen skip、topic switch options、失败回滚和关闭标签 plan selector，并接回 `useTaskCenterDraftMaterializationRuntime.ts`、`useTaskCenterTopicNavigationRuntime.ts` 与 `AgentChatWorkspace.tsx`。`index.test.tsx` 再删除 3 个重复挂载分支，从 `110` 降到 `107` 个 case；`npm run test:unit -- src/components/agent/chat/workspace/taskCenterDraftTabs.unit.test.ts src/components/agent/chat/utils/taskCenterTabs.test.ts` 实测 `2` 个文件、`37` 个 case 通过；`npm run test:component -- src/components/agent/chat/index.test.tsx` 实测 `107` 个 case，tests `44.38s`、总 `50.18s` 通过；完整 `npm run test:unit` 实测 `394` 个文件、`2178` 个 case、`22.24s` 通过；`npm run test:layers:stats` 统计为总 `891` 个 Vitest 文件、默认可运行 `890`、live-gated `1`，分层为 unit `394`、component `370`、contract `77`、integration `49`、e2e `1`；`npm run typecheck`、`npm run lint`、`git diff --check`、`npm run verify:gui-smoke` 均通过。GUI smoke 覆盖 workspace-ready、browser-runtime、site-adapters、Skill Forge / runtime tool surface、页面级 runtime tool surface、code runtime fixture、approval/sandbox、`@` command registry、Agent Apps、Knowledge GUI 和 design canvas；真实 Provider smoke 按默认规则跳过。
- 2026-06-01：P2 第三十四刀完成。新增 `resolveTaskCenterFallbackRestorePlan`，把任务中心 fallback restore effect 中的草稿 surface、自动首条提交、detached session、未知会话、重复恢复冷却等条件下沉到 `taskCenterTabs.ts` 纯 planner；`AgentChatWorkspace` 只保留 planner 接线、detached 调试日志和实际打开任务 topic 的副作用。`taskCenterTabs.test.ts` 新增 4 个纯单元分支覆盖“新建草稿不恢复旧标签”“自动首条技能提交不恢复旧标签”“正常恢复第一个旧标签”和“2 秒内不重复恢复”；`index.test.tsx` 删除 2 个重复挂载分支，从 `107` 降到 `105` 个 case。验证：`npm run test:unit -- src/components/agent/chat/utils/taskCenterTabs.test.ts` 实测 `36` 个 case、总 `0.71s` 通过；`npm run test:component -- src/components/agent/chat/index.test.tsx` 实测 `105` 个 case，tests `44.31s`、总 `51.15s` 通过；完整 `npm run test:unit` 实测 `394` 个文件、`2182` 个 case、`23.68s` 通过；`npm run test:layers:stats` 统计保持总 `891` 个 Vitest 文件、默认可运行 `890`、live-gated `1`，分层为 unit `394`、component `370`、contract `77`、integration `49`、e2e `1`；`npm run typecheck`、`npm run lint`、`git diff --check`、`npm run verify:gui-smoke` 均通过。GUI smoke 覆盖 workspace-ready、browser-runtime、site-adapters、Skill Forge / runtime tool surface、页面级 runtime tool surface、code runtime fixture、approval/sandbox、`@` command registry、Agent Apps、Knowledge GUI 和 design canvas；`claw-chat-ready-streaming` 真实 Provider smoke 仍按默认规则跳过。
- 2026-06-01：P2 第三十五刀完成。继续从 `index.test.tsx` 迁出空白首页 / 草稿预热重复断言：`taskCenterDraftTabs.unit.test.ts` 增补 `new-task` 入口、空白 input、发送中三类不预热分支，`useWorkspaceCanvasLayoutRuntime.test.tsx` 保留空白 `new-task` 首页不自动生成 fallback 画布的组件层覆盖；`index.test.tsx` 删除 3 个重复挂载用例，从 `105` 降到 `102` 个 case。同时修复 `submitOpRuntimeCompaction` 默认关闭偏好裁剪：新会话默认 `web_search=false` / `thinking=false` 不再提交显式 request preference，也不残留空 metadata，只有开启或从已知开启状态切回关闭时才提交。验证：`npm run test:unit -- src/components/agent/chat/workspace/taskCenterDraftTabs.unit.test.ts` 实测 `5` 个 case 通过；`npm run test:component -- src/components/agent/chat/workspace/useWorkspaceCanvasLayoutRuntime.test.tsx` 实测 `8` 个 case、总 `4.84s` 通过；`npm run test:component -- src/components/agent/chat/index.test.tsx` 实测 `102` 个 case，tests `46.06s`、总 `53.25s` 通过；`npm run test:unit -- src/components/agent/chat/utils/submitOpRuntimeCompaction.test.ts` 实测 `13` 个 case、总 `0.91s` 通过；完整 `npm run test:unit` 实测 `394` 个文件、`2183` 个 case、`33.38s` 通过；`npm run test:layers:stats` 统计保持总 `891` 个 Vitest 文件、默认可运行 `890`、live-gated `1`，分层为 unit `394`、component `370`、contract `77`、integration `49`、e2e `1`；`npm run typecheck`、`npm run lint`、`git diff --check` 均通过。本刀未改 GUI 壳接线，沿用第三十四刀已通过的 `verify:gui-smoke` 证据作为主路径冒烟基线。
- 2026-06-01：P2 第三十六刀完成。继续从 `index.test.tsx` 迁出 Browser Assist artifact 默认选择与 scope 保留重复挂载断言：新增 `agentChatWorkspaceHelpers.unit.test.ts` 覆盖通用工作区不默认选浏览器协助 artifact、不默认选后台 `artifact_snapshot` 文档、非通用工作区仍选最后 artifact；新增 `browserAssistArtifact.unit.test.ts` 覆盖旧 scope 浏览器协助 artifact 不复用、同 scope 可保留、消息 artifact 与同 scope 浏览器协助 artifact 共存。`index.test.tsx` 删除 4 个重复挂载用例，从 `102` 降到 `98` 个 case，并按并行参数移除主线调整默认发送断言为不提交 `webSearch` / `thinking`。验证：`npm run test:unit -- src/components/agent/chat/workspace/agentChatWorkspaceHelpers.unit.test.ts src/components/agent/chat/workspace/browserAssistArtifact.unit.test.ts` 实测 `2` 个文件、`8` 个 case、总 `2.36s` 通过；`npm run test:component -- src/components/agent/chat/index.test.tsx` 实测 `98` 个 case、tests `41.58s`、总 `48.95s` 通过；完整 `npm run test:unit` 实测 `395` 个文件、`2189` 个 case、总 `26.59s` 通过；`npm run test:layers:stats` 统计为总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `395`、component `370`、contract `77`、integration `49`、e2e `1`。本刀未执行 `typecheck` / `lint` / `verify:gui-smoke`：同轮另一个进程正在收口输入栏 `webSearch` / `thinking` 参数移除，当前全局静态检查会被该中间态阻塞；GUI 主路径沿用第三十四刀 smoke 基线，下一刀待参数移除完成后补全静态检查。
- 2026-06-01：P2 第三十七刀完成。确认另一个进程正在移除 `webSearch` / `thinking` 两个参数后，本刀避开该写集，转而把 `useWorkflowInputState` 中 Inputbar 工作流展示状态抽成 `buildWorkflowInputState` 纯 builder；`workflowInputState.test.ts` 去掉 React `createRoot` / `act` 挂载测试，扩展为 4 个纯单元分支，覆盖非工作区场景、等待 gate、开放步骤队列 / 进度和发送态生成面板。验证：`npm run test:unit -- src/components/agent/chat/utils/workflowInputState.test.ts` 实测 `4` 个 case、总 `0.90s` 通过；`npm run test:layers:stats` 统计为总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；`git diff --check` 通过。完整 `npm run test:unit` 已尝试，当前 `396` 个文件 / `2196` 个 case 收集，`392` 文件通过、`4` 文件失败、`11` 个断言失败，失败集中在 `buildUserInputSubmitOp.test.ts`、`harnessRequestMetadata.test.ts`、`sessionExecutionRuntime.test.ts`、`submitOpRuntimeCompaction.test.ts` 的参数移除中间态；`npm run typecheck` 也失败于同一批 `webSearch` / `thinking` 类型和测试调用残留。本刀未执行 `verify:gui-smoke`，GUI 主路径仍沿用第三十四刀 smoke 基线，待参数移除进程收口后补全全局静态与 GUI smoke。
- 2026-06-01：P2 第三十八刀完成。继续补强 `harnessStatusPanelViewModel`，将 `HarnessStatusPanel` 中 Harness 文件名、文件动作 / 类型展示、文件 action 汇总、文件变更审阅条目构建、状态计数、输出信号路径和输出过滤器匹配等纯展示 / selector 逻辑下沉到 VM；组件层继续保留 DOM、交互和翻译接线断言。`harnessStatusPanelViewModel.unit.test.ts` 新增 3 个纯单元分支，覆盖文件名 / 动作 / 类型展示、active write 与 recent events 合并审阅、不可审阅事件过滤、状态计数，以及 path / offload / truncated / summary 输出过滤。验证：`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `11` 个 case、总 `4.06s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `10.56s` 通过；`npm run test:layers:stats` 统计保持总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；`git diff --check` 通过。`npm run typecheck` 当前仍失败于并行参数移除 / Inputbar props / i18n key 中间态，代表性错误包括残留 `webSearch` / `thinking`、`setExecutionStrategy` / `executionStrategy` prop 调用和 `agentChat.home.composer.generalContext` 类型引用；本刀不接管该写集。未执行 `verify:gui-smoke`，GUI 主路径仍沿用第三十四刀 smoke 基线，待参数移除进程收口后补全全局静态与 GUI smoke。
- 2026-06-01：P2 第三十九刀完成。继续补强 `harnessStatusPanelViewModel`，将 `HarnessStatusPanel` 中输出信号状态 badge 构造、输出路径列表、噪声 runtime 错误折叠判断和输出卡片 tone / preview 展示模型下沉到 VM；组件层只保留翻译、DOM、点击打开预览 / URL / 路径等接线。`harnessStatusPanelViewModel.unit.test.ts` 新增输出信号展示模型纯单元分支，覆盖 exit code / truncated / offload / sandbox / stdout / stderr / original chars / tokens 状态描述、output/offload/artifact 路径排序、JSON-RPC 噪声错误折叠、普通失败输出保留 preview 和成功输出默认 tone。验证：`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `2.50s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `8.30s` 通过；`npm run test:layers:stats` 统计保持总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；`git diff --check` 通过。全局 `typecheck` / `verify:gui-smoke` 仍等待并行参数移除和 Inputbar / i18n 写集收口后再补。
- 2026-06-01：P2 第四十刀完成。继续补强 `harnessStatusPanelViewModel`，将 `HarnessStatusPanel` 中 runtime phase 标签、workflow step status、status label、summary text、progress label 和 runtime task presentation 构造下沉到 VM；组件层只保留 `useMemo` 接线和 `ManagedObjectivePanel` 渲染。`harnessStatusPanelViewModel.unit.test.ts` 新增 runtime 任务展示模型纯单元分支，覆盖空闲 phase、运行中 detail / checkpoints 清洗、失败态 error、取消态 skipped / 已取消文案、空 runtime 返回 null，以及 diagnostics-only runtime status 不进入可见任务面板。验证：`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `13` 个 case、总 `2.03s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `6.89s` 通过；`npm run test:layers:stats` 统计保持总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；`npm run typecheck` 当前工作树通过；`git diff --check` 通过。完整 `npm run test:unit` 已恢复到 `396` 个文件 / `2201` 个 case 收集，但仍有 `buildUserInputSubmitOp.test.ts`、`sessionExecutionRuntime.test.ts`、`submitOpRuntimeCompaction.test.ts` 共 `9` 个断言失败，失败集中在并行 `webSearch` / `thinking` 偏好收口链路；本刀不接管该写集。未执行 `verify:gui-smoke`，待默认 unit 入口恢复绿色后再补 GUI 主路径冒烟。
- 2026-06-01：P2 第四十一刀完成。继续补强 `harnessStatusPanelViewModel`，将 `HarnessStatusPanel` 中工具库存的写入来源标签、执行策略来源标签 / badge 变体、warning / restriction / sandbox 策略标签、工具 lifecycle / source / permission / extension / runtime source 标签、catalog 筛选计数、执行来源统计、registry 执行来源收集和 runtime 工具可见性排序下沉到 VM；组件层保留 `toolInventoryFilter` 状态、点击筛选和 JSX 渲染。`harnessStatusPanelViewModel.unit.test.ts` 新增工具库存展示模型纯单元分支，覆盖来源 / 策略标签、runtime / persisted / default 筛选、三字段来源统计、registry source 收集和可见工具排序。验证：`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `15` 个 case、总 `2.59s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `8.11s` 通过；`npm run typecheck` 通过；`npm run test:layers:stats` 统计保持总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 和目标写集 `git diff --check` 通过。完整 `npm run test:unit` 当前为 `396` 个文件 / `2203` 个 case 收集、`394` 文件通过、`2` 文件失败、`4` 个断言失败，失败集中在并行 `webSearch` / `thinking` 参数移除链路；全局 `npm run lint` 当前失败于该链路的 `agentStreamRuntimeHandler.ts` 未使用 `webSearch`，本刀不接管该写集。未执行 `verify:gui-smoke`，待默认 unit / lint 入口恢复绿色后再补 GUI 主路径冒烟。
- 2026-06-01：P2 第四十二刀完成。继续补强 `harnessStatusPanelViewModel`，将 `HarnessStatusPanel` 中 handoff / evidence / replay / analysis 相关的时间格式、文件大小、状态标签、制品 kind label、LimeCore policy input / ref / missing input / decision 汇总、review regression facts、replay case slug / promotion context / eval command 构造等纯 presentation / selector 逻辑下沉到 VM；组件层继续保留 React state、导出请求、剪贴板、预览打开和 JSX 接线。`harnessStatusPanelViewModel.unit.test.ts` 新增 3 个纯单元分支，覆盖制品辅助展示、LimeCore policy 去重汇总和 replay 推广命令构造。验证：`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `18` 个 case、总 `6.10s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `18.19s` 通过；`npm run test:layers:stats` 统计保持总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 和目标写集 `git diff --check` 通过。完整 `npm run test:unit` 实测 `396` 个文件 / `2206` 个 case 收集、`394` 文件通过、`2` 文件失败、`4` 个断言失败，仍集中在并行 `webSearch` / `thinking` 参数移除链路；`npm run typecheck` 本轮启动后超过 `6` 分钟无输出，已终止本进程启动的 `npm run typecheck` / `tsc`，待参数移除写集收口后再补全量静态检查；未执行 `verify:gui-smoke`。
- 2026-06-01：P2 第四十三刀完成。继续补强 `harnessStatusPanelViewModel`，将 `HarnessStatusPanel` 中 URL 归一化 / 文本片段切分、首个 URL 提取、搜索输出信号识别、文件路径识别、approval path / command 提取、确认风险分类、动作 label key 和确认描述等纯 selector 下沉到 VM；组件层只保留链接打开、路径打开、确认表单和 JSX 接线。本刀明确避开并行进程正在移除的 `webSearch` / `thinking` 参数链路，不接管相关测试失败。验证：`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `21` 个 case、总 `2.52s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `7.31s` 通过；`npm run test:layers:stats` 统计保持总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 和目标写集 `git diff --check` 通过。完整 `npm run test:unit`、`npm run typecheck`、`npm run verify:gui-smoke` 本刀未执行；待参数移除写集收口后，再补默认 unit / 静态检查和 GUI 主路径冒烟。
- 2026-06-01：P2 第四十四刀完成。继续补强 `harnessStatusPanelViewModel`，将 `HarnessStatusPanel` 中 Harness 时间展示、active write 描述、diff review 状态 label key、文件变更 diff summary 和输出信号 diff summary 构造下沉到 VM；组件层只保留 diff mini panel DOM、CSS class、路径打开和翻译接线。`harnessStatusPanelViewModel.unit.test.ts` 新增纯单元分支，覆盖时间 fallback、写入阶段 / 来源 / 时间描述、diff 状态 key，以及文件审阅和输出信号两条 diff summary 构造。本刀继续避开并行 `webSearch` / `thinking` 参数移除链路。验证：`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `22` 个 case、总 `2.28s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `14.73s` 通过；`npm run test:layers:stats` 统计保持总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 和目标写集 `git diff --check` 通过。完整 `npm run test:unit`、`npm run typecheck`、`npm run verify:gui-smoke` 本刀未执行；待参数移除写集收口后，再补默认 unit / 静态检查和 GUI 主路径冒烟。
- 2026-06-01：P2 第四十五刀完成。继续补强 `harnessStatusPanelViewModel`，将 `HarnessStatusPanel` 中 browser action index 到 `browser_assist` replay artifact 的 metadata 映射下沉到 VM；组件层只保留打开预览 dialog 的状态和事件接线。`harnessStatusPanelViewModel.unit.test.ts` 新增纯单元分支，覆盖 replay artifact id/type/title/status、browserActionIndex 计数和 item 字段、viewer surface、evidence pack root、session/profile/target/url 投影以及 exported_at 时间戳。本刀继续避开并行 `webSearch` / `thinking` 参数移除链路。验证：`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `8.35s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `25.31s` 通过；`npm run test:layers:stats` 统计保持总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 和目标写集 `git diff --check` 通过。完整 `npm run test:unit`、`npm run typecheck`、`npm run verify:gui-smoke` 本刀未执行；待参数移除写集收口后，再补默认 unit / 静态检查和 GUI 主路径冒烟。
- 2026-06-01：P2 第四十六刀完成。响应 `HarnessStatusPanel.tsx` 过大问题，停止只做零散 VM 下沉，按“容器保留状态 / 副作用 / section 编排，展示组件按领域拆出”的边界拆分首批自包含组件：新增 `HarnessStatusPanelPrimitives.tsx`（diff mini panel、InteractiveText、PathTextLink、ActionableBadge、SummaryCard、InventoryStatCard）、`HarnessSearchOutputCards.tsx`（搜索输出单卡 / 批次卡）、`HarnessEvidenceSummarySections.tsx`（Browser Assist replay 索引和 LimeCore policy 索引摘要）和 `harnessPanelText.ts`（generated key i18n fallback helper）。`HarnessStatusPanel.tsx` 从约 `8791` 行降到约 `7782` 行，主文件继续保留状态、导出请求、预览 dialog、section 编排和真实副作用；同时把文件审阅 action / kind / phase / status、输出路径和 approval risk label key 的选择下沉到 VM，组件层只翻译 key 并渲染。验证：`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `3.94s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `27.39s` 通过；定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/HarnessStatusPanelPrimitives.tsx src/components/agent/chat/components/HarnessSearchOutputCards.tsx src/components/agent/chat/components/HarnessEvidenceSummarySections.tsx src/components/agent/chat/components/harnessPanelText.ts src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:layers:stats` 统计保持总 `892` 个 Vitest 文件、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；目标写集 `git diff --check` 通过。完整 `npm run test:unit` 本轮已尝试，`396` 个文件 / `2211` 个 case 收集、`394` 文件通过、`2` 文件失败、`4` 个断言失败，总 `43.82s`，失败仍集中在并行 `webSearch` / `thinking` 参数移除链路；本刀不接管该写集。未执行 `npm run typecheck` / `npm run verify:gui-smoke`，待参数移除链路收口后补全静态检查与 GUI 主路径冒烟。
- 2026-06-01：P2 第四十七刀完成。继续响应 `HarnessStatusPanel.tsx` 过大问题，按领域 section 拆出 `HarnessStatusSectionFrame.tsx`、`HarnessFileReviewSection.tsx`、`HarnessOutputSignalsSection.tsx`、`HarnessActiveWritesSection.tsx`、`HarnessHandoffExportSection.tsx`、`HarnessToolInventorySection.tsx` 和 `HarnessActivitySections.tsx`；父组件进一步收敛为 Harness 容器、状态 / 副作用编排、摘要卡、预览 dialog 与少量顶部 runtime 区块，文件行数从约 `7782` 降到 `2971`。本刀只触碰 Harness 面板拆分相关写集，继续避开并行 `webSearch` / `thinking` 参数移除链路。验证：定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/HarnessActivitySections.tsx src/components/agent/chat/components/HarnessToolInventorySection.tsx src/components/agent/chat/components/HarnessHandoffExportSection.tsx src/components/agent/chat/components/HarnessActiveWritesSection.tsx src/components/agent/chat/components/HarnessOutputSignalsSection.tsx src/components/agent/chat/components/HarnessFileReviewSection.tsx src/components/agent/chat/components/HarnessStatusSectionFrame.tsx` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case 通过；`npm run typecheck` 通过；目标写集 `git diff --check` 通过。未执行 `verify:gui-smoke`：本刀是普通前端组件结构拆分，不改 GUI 壳、DevBridge、Workspace 或命令边界，交互主路径冒烟留给下一次 GUI 主路径改动或全局收口门禁。
- 2026-06-01：P2 第四十八刀完成。继续拆 `HarnessHandoffExportSection.tsx` 的 2138 行二级大组件，按导出物类型拆成 `HarnessHandoffBundleCard.tsx`、`HarnessEvidencePackCard.tsx`、`HarnessReplayCaseCard.tsx`、`HarnessAnalysisHandoffCard.tsx`、`HarnessReviewDecisionCard.tsx`，并新增 `HarnessHandoffExportShared.tsx` / `HarnessHandoffExportTypes.ts` 收口导出卡片、目录展示、artifact 列表和 preview/path handler 类型；`HarnessHandoffExportSection.tsx` 降到 `159` 行，只保留 section 标题、badge 和五个卡片的 props 编排。当前最大剩余拆分对象变为 `HarnessToolInventorySection.tsx` 与 `HarnessActivitySections.tsx`。验证：定向 `npx eslint` 覆盖 handoff export 编排层、共享层和五个卡片通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `3.50s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `10.63s` 通过；`npm run typecheck` 通过；目标写集 `git diff --check` 通过。未执行 `verify:gui-smoke`：本刀仍是普通前端组件结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令或 mock 边界。
- 2026-06-01：P2 第四十九刀完成。继续把 `HarnessStatusPanel.tsx` 的顶部 runtime overview、预览弹窗和 handoff 导出状态机拆出：新增 `HarnessRuntimeOverviewSections.tsx` 承载 team config、runtime task、AgentUI projection、runtime facts 四个 section，并复用 VM 导出的 `RuntimeTaskPresentation` 类型，避免 UI 层复制 step status 枚举；新增 `useHarnessPreviewDialog.ts` 承载预览加载、Browser Assist replay artifact、复制路径 / 内容、定位文件和系统打开等 dialog 行为；新增 `HarnessPreviewDialog.tsx` 只负责预览弹窗 DOM；新增 `useHarnessHandoffExports.ts` 承载 handoff / evidence / replay / analysis / review 导出请求、剪贴板命令、人工审核保存和 AgentUI projection 记录副作用。`HarnessStatusPanel.tsx` 从第四十八刀后的约 `2451` 行降到 `1505` 行，继续保留容器状态、摘要卡、section 编排和跨 section 接线。当前最大剩余拆分对象为父级 `HarnessStatusPanel.tsx` `1505` 行、`HarnessToolInventorySection.tsx` `1113` 行、`HarnessActivitySections.tsx` `1050` 行。验证：定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/HarnessRuntimeOverviewSections.tsx src/components/agent/chat/components/HarnessPreviewDialog.tsx src/components/agent/chat/components/useHarnessPreviewDialog.ts src/components/agent/chat/components/useHarnessHandoffExports.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `3.23s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `9.00s` 通过；`npm run typecheck` 通过；目标写集 `git diff --check` 通过。未执行 `verify:gui-smoke`：本刀是普通前端组件结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-01：P2 第五十刀完成。继续拆 `HarnessToolInventorySection.tsx` 的 `1113` 行二级大组件，按工具库存领域拆成 `HarnessToolInventoryOverview.tsx`（请求面、刷新、统计卡、告警、runtime capability summary）、`HarnessRuntimeToolInventoryList.tsx`、`HarnessCatalogToolInventoryList.tsx`、`HarnessRegistryToolInventoryList.tsx`、`HarnessExtensionToolInventorySections.tsx` 和 `HarnessToolInventoryTypes.ts`；`HarnessToolInventorySection.tsx` 降到 `137` 行，只保留 inventory section badge、空态和五个子组件的 props 编排。`HarnessStatusPanel.tsx` 保持 `1505` 行，当前最大剩余拆分对象变为父级容器和 `HarnessActivitySections.tsx`。验证：定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/HarnessToolInventorySection.tsx src/components/agent/chat/components/HarnessToolInventoryOverview.tsx src/components/agent/chat/components/HarnessRuntimeToolInventoryList.tsx src/components/agent/chat/components/HarnessCatalogToolInventoryList.tsx src/components/agent/chat/components/HarnessRegistryToolInventoryList.tsx src/components/agent/chat/components/HarnessExtensionToolInventorySections.tsx src/components/agent/chat/components/HarnessToolInventoryTypes.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `2.45s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `8.37s` 通过；`npm run typecheck` 通过；目标写集 `git diff --check` 通过。未执行 `verify:gui-smoke`：本刀是普通前端组件结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-01：P2 第五十刀补充验证。补跑 `npm run verify:local`，统一入口在 `npm run i18n:unused -- --check` 阶段失败后停止，未进入前端全量、bridge、GUI 冒烟和 Rust 阶段；未引用 key 候选集中在 `agentChat.runtimeStrip.*` 与并行 `webSearch` / `thinking` 输入栏资源，本刀不接管该写集。第五十刀的可交付结论仍以 Harness Tool Inventory 定向 ESLint、VM 单测、组件回归、`typecheck` 和目标 `git diff --check` 为准。
- 2026-06-01：P2 第五十一刀完成。继续拆 `HarnessActivitySections.tsx` 的 `1050` 行二级大组件，新增 `HarnessActivityTypes.ts` 收口父子共享的 `FileFilterValue` / `FileDisplayMode` / `HarnessEnvironmentSummary` / 文件活动分组 / 预览请求类型，并按业务 section 拆成 `HarnessApprovalsSection.tsx`、`HarnessFileActivitySection.tsx`、`HarnessPlanSection.tsx`、`HarnessDelegationSection.tsx`、`HarnessContextCapabilitiesSections.tsx`；`HarnessActivitySections.tsx` 降到 `120` 行，只保留审批、文件活动、规划、子任务、上下文 / 能力五组 section 的顺序编排。`HarnessStatusPanel.tsx` 因移除重复类型降到 `1497` 行。验证：定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/HarnessActivitySections.tsx src/components/agent/chat/components/HarnessActivityTypes.ts src/components/agent/chat/components/HarnessApprovalsSection.tsx src/components/agent/chat/components/HarnessFileActivitySection.tsx src/components/agent/chat/components/HarnessPlanSection.tsx src/components/agent/chat/components/HarnessDelegationSection.tsx src/components/agent/chat/components/HarnessContextCapabilitiesSections.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `2.28s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `6.93s` 通过；`npm run typecheck` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；目标写集 `git diff --check` 通过。补跑 `npm run verify:local` 仍在 `npm run i18n:unused -- --check` 阶段失败，未进入 GUI 冒烟 / Rust 阶段；未引用 key 仍集中在 `agentChat.runtimeStrip.*` 与并行 `webSearch` / `thinking` 输入栏资源，本刀不接管该写集。
- 2026-06-01：P2 第五十二刀完成。继续拆 `HarnessStatusPanel.tsx` 父级容器，新增 `HarnessStatusPanelShell.tsx` 承载根容器、标题栏、lead content、摘要卡 grid、section jump nav 和 details scroll area，新增 `HarnessStatusPanelTypes.ts` 收口 layout / summary card 共享类型，新增 `harnessStatusPanelSummary.ts` 承载摘要卡与可跳转 section 的 UI view-model；父组件保留状态、hooks、副作用、跨 section 接线和少量派生输入，行数从第五十一刀后的 `1497` 降到 `1153`。本刀顺手修正抽取后暴露的 `toolInventory` 可选类型边界，传入 summary builder 前统一归一为 `null`。验证：定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/HarnessStatusPanelShell.tsx src/components/agent/chat/components/HarnessStatusPanelTypes.ts src/components/agent/chat/components/harnessStatusPanelSummary.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `4.75s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `7.02s` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；目标写集 `git diff --check` 通过。`npm run typecheck` 复跑后 Harness 相关错误已清零，当前仍失败于并行参数移除链路的 `AgentChatWorkspace.tsx` 8 处 `executionStrategy` 残留，本刀不接管该写集；未执行 `verify:gui-smoke`，因为本刀是普通前端结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-01：P2 第五十三刀完成。继续收敛 `HarnessStatusPanel.tsx` 中仍内联的纯 selector / view-model 逻辑，将文件筛选选项、输出筛选选项、文件事件过滤与分组、输出信号过滤与搜索批次分组、runtime facts 摘要和 runtime tool capability gaps 构造下沉到 `harnessStatusPanelViewModel.ts`，并在 `harnessStatusPanelViewModel.unit.test.ts` 补纯单元断言；父组件只保留 React state、hooks、副作用、section 编排和跨 section 接线，行数从第五十二刀后的 `1151` 降到 `976`。本刀继续避开并行参数移除链路，不接管 `AgentChatWorkspace.tsx` / Inputbar 写集。验证：定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `4.56s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `9.67s` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；目标写集 `git diff --check` 通过。完整 `npm run typecheck` / `npm run verify:local` 本刀未执行，等待隔壁参数移除写集收口后再补全量静态检查；未执行 `verify:gui-smoke`，因为本刀是普通前端结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-02：P2 第五十四刀完成。继续拆 `HarnessStatusPanel.tsx` 父级容器，将文件审阅状态机抽到 `useHarnessFileReviewState.ts`，将文件 / 输出活动筛选与分组状态抽到 `useHarnessActivityModel.ts`，将工具库存筛选、统计、runtime 可用性和 capability gaps 派生抽到 `useHarnessToolInventoryModel.ts`；父组件继续保留顶层布局、hook 编排、跨 section 事件接线和核心 JSX，行数从第五十三刀后的 `976` 降到 `850`。为兼容现有 `CodeReviewSummaryPanel` / `WorkspaceHarnessDialogs` 入口，`HarnessStatusPanel.tsx` 继续 re-export `HarnessFileChangeReviewSummary`，避免外部类型导入断裂。验证：定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/useHarnessActivityModel.ts src/components/agent/chat/components/useHarnessToolInventoryModel.ts src/components/agent/chat/components/useHarnessFileReviewState.ts src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `2.76s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `7.85s` 通过；`npm run typecheck` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；目标写集 `git diff --check` 通过。补跑 `npm run verify:local` 仍在 `npm run i18n:unused -- --check` 阶段失败，当前 20 个未引用 key 候选集中在 `agentChat.runtimeStrip.*` 和 Inputbar `webSearch` / `thinking` 资源，本刀不跨写集删除；未执行 `verify:gui-smoke`，因为本刀仍是普通前端结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-02：P2 第五十五刀完成。继续拆 `HarnessStatusPanel.tsx` 父级容器，新增 `HarnessStatusPanelSections.tsx` 承载 section 编排层，并将 `HarnessStatusPanelProps` / lead content 类型移入 `HarnessStatusPanelTypes.ts`；父组件现在聚焦翻译、状态 hooks、副作用 handler、摘要 / section models 和 shell/dialog 接线，行数从第五十四刀后的 `850` 降到 `718`。本刀继续避开隔壁正在修复的 execution strategy / Inputbar / i18n 写集。验证：定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/HarnessStatusPanelTypes.ts src/components/agent/chat/components/HarnessStatusPanelSections.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `2.83s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `8.20s` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`。`npm run typecheck` 已尝试，当前失败集中在隔壁参数移除链路的 `AgentChatWorkspace.tsx` / `useWorkspaceSendActions.ts` / `useTaskCenterDraftSendRuntime.ts` 对 `executionStrategy` 的 `"react"` 窄类型残留，本刀不接管该写集；未执行 `verify:gui-smoke`，因为本刀仍是普通前端结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-02：P2 第五十六刀完成。继续拆 `HarnessStatusPanel.tsx` 父级容器，新增 `harnessStatusPanelSectionModels.ts` 承载 section props / model 组装，把父组件内的大块 JSX props 对象移出；`HarnessStatusPanelSections.tsx` 继续只负责 section 顺序和渲染，`HarnessStatusPanel.tsx` 保留翻译、顶层 hooks、状态 / 副作用入口、shell/dialog 接线。父组件行数从第五十五刀后的 `718` 降到 `476`，新 section model 文件为 `447` 行，避免把复杂度继续堆回容器。验证：定向 `npx eslint src/components/agent/chat/components/HarnessStatusPanel.tsx src/components/agent/chat/components/HarnessStatusPanelSections.tsx src/components/agent/chat/components/HarnessStatusPanelTypes.ts src/components/agent/chat/components/harnessStatusPanelSectionModels.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `7.24s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `18.98s` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`。`npm run typecheck` 已复跑，本刀 Harness 相关错误已清零，当前仍失败于隔壁 execution strategy 写集的 `"auto"` / `"code_orchestrated"` 传给 `"react"` 窄类型残留，代表性文件包括 `agentSessionRefresh.test.ts`、`agentSessionTopicViewModel.unit.test.ts`、`sessionMetadataSyncController.test.ts`、`useAsterAgentChat.test.tsx`、`sessionExecutionRuntime.test.ts`、`submitOpRuntimeCompaction.test.ts` 和 `useWorkspaceSendActions.test.tsx`；本刀不接管该写集。未执行 `verify:gui-smoke`，因为本刀仍是普通前端结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-02：P2 第五十七刀完成。继续收敛 Harness runtime overview 拆分，将 `HarnessRuntimeOverviewSections.tsx` 中的 Team 配置、运行中任务、AgentUI 标准投影和运行时事实四块拆成 `HarnessTeamConfigSection.tsx`、`HarnessRuntimeTaskSection.tsx`、`HarnessAgentUiProjectionSection.tsx`、`HarnessRuntimeFactsSection.tsx`，原 `HarnessRuntimeOverviewSections.tsx` 退化为 5 行 barrel export；同时让运行时事实 section 直接复用 `harnessStatusPanelViewModel.ts` 导出的 `HarnessRuntimeFactSummary`，去掉 overview 层重复类型。该刀继续避开隔壁正在修复的 execution strategy / Inputbar / i18n 写集，不触碰 `AgentChatWorkspace.tsx`、`index.test.tsx` 和 `workspaceSendHelpers.ts`。验证：定向 `npx eslint src/components/agent/chat/components/HarnessRuntimeOverviewSections.tsx src/components/agent/chat/components/HarnessTeamConfigSection.tsx src/components/agent/chat/components/HarnessRuntimeTaskSection.tsx src/components/agent/chat/components/HarnessAgentUiProjectionSection.tsx src/components/agent/chat/components/HarnessRuntimeFactsSection.tsx src/components/agent/chat/components/HarnessStatusPanelSections.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `2.81s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `9.68s` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；目标 `git diff --check` 通过。`npm run typecheck` 已复跑，本刀 Harness 相关错误为零，当前仍失败于隔壁 execution strategy 测试写集的 `"code_orchestrated"` 传给 `"react"` 窄类型残留，代表文件为 `agentSessionRefresh.test.ts`、`agentSessionTopicViewModel.unit.test.ts`、`sessionMetadataSyncController.test.ts`、`chatToolPreferences.test.ts`、`sessionExecutionRuntime.test.ts` 和 `submitOpRuntimeCompaction.test.ts`；本刀不接管该写集。未执行 `verify:gui-smoke`，因为本刀仍是普通前端结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-02：P2 第五十八刀完成。继续收敛 Harness VM，将 `harnessStatusPanelViewModel.ts` 中 URL 识别、文本分段、文件路径启发式判断、approval 参数 path / command 提取等纯解析函数拆到 `harnessStatusPanelTextParsing.ts`，原 VM 保持统一 re-export，调用方和现有测试导入路径不变；`harnessStatusPanelViewModel.ts` 从 `2081` 行降到 `1986` 行，新的 text parsing helper 为 `113` 行。该刀仍属于纯 VM / selector 拆分，不触碰 execution strategy、Inputbar、i18n 或 Workspace 主路径写集。验证：定向 `npx eslint src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessStatusPanelTextParsing.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `1.97s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `7.31s` 通过；`npm run typecheck` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`。未执行 `verify:gui-smoke`，因为本刀是普通前端 VM / helper 结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-02：P2 第五十九刀完成。继续收敛 Harness VM，将工具库存执行来源 label / badge、warning / restriction / sandbox 策略 label、工具 lifecycle / permission / source / extension / runtime source label、catalog / registry 执行来源收集、runtime 工具排序、capability gaps、catalog filter/count 和 source stats 等纯函数从 `harnessStatusPanelViewModel.ts` 拆到 `harnessToolInventoryViewModel.ts`；原 VM 继续 re-export，现有组件和单测导入路径不变。`harnessStatusPanelViewModel.ts` 从 `1986` 行降到 `1683` 行，新的工具库存 VM 为 `308` 行。本刀继续避开隔壁正在修复的 execution strategy / Inputbar / Workspace 写集，只触碰 Harness VM 和测试路线图记录。验证：定向 `npx eslint src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessToolInventoryViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `3.58s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `8.87s` 通过；`npm run typecheck` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`。未执行 `verify:gui-smoke`，因为本刀是普通前端 VM / helper 结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-02：P2 第六十刀完成。继续收敛 Harness VM，将文件审阅 / 文件活动相关纯函数从 `harnessStatusPanelViewModel.ts` 拆到 `harnessFileReviewViewModel.ts`，包括文件筛选选项、文件活动过滤 / 分组、active write 描述、diff review 状态 key、文件变更 diff summary、文件审阅 action/kind/phase/status label key、文件审阅 action summary、可审阅事件判断、文件变更审阅条目构建和状态计数；原 VM 继续 re-export，现有组件和单测导入路径不变。`harnessStatusPanelViewModel.ts` 从第五十九刀后的 `1683` 行降到 `1204` 行，新的文件审阅 VM 为 `492` 行。本刀仍只触碰 Harness VM 和测试路线图记录，继续避开 execution strategy / Inputbar / Workspace / Rust 参数链路等并行写集。验证：定向 `npx eslint src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessFileReviewViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `2.74s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `7.84s` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`。`npm run typecheck` 已启动但在当前大量并行写集下长时间无输出，已终止本进程启动的 `npm run typecheck` / `tsc --noEmit`，本刀不把该项记为通过；未执行 `verify:gui-smoke`，因为本刀是普通前端 VM / helper 结构拆分，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-02：P2 第六十一刀完成。继续收敛 Harness VM，将输出信号相关纯函数从 `harnessStatusPanelViewModel.ts` 拆到 `harnessOutputSignalViewModel.ts`，包括输出筛选值 / 路径类型、搜索输出识别、输出 diff summary、路径 label key、路径选择、输出过滤选项、搜索批次分组、状态 badge、噪声 runtime 输出折叠、输出卡片 presentation 和 output/offload/artifact 路径列表；原 VM 继续 re-export，现有组件、hooks 和单测导入路径不变。`harnessStatusPanelViewModel.ts` 从第六十刀后的 `1204` 行降到 `928` 行，新的输出信号 VM 为 `275` 行。本刀仍属于普通前端 VM / helper 拆分，继续避开 execution strategy / Inputbar / Workspace / Rust 参数链路等并行写集。验证：定向 `npx eslint src/components/agent/chat/components/harnessStatusPanelViewModel.ts src/components/agent/chat/components/harnessOutputSignalViewModel.ts src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/harnessStatusPanelViewModel.unit.test.ts` 实测 `23` 个 case、总 `4.13s` 通过；`npm run test:component -- src/components/agent/chat/components/HarnessStatusPanel.test.tsx` 实测 `49` 个 case、总 `48.12s` 通过；`npm run test:layers:stats` 统计保持总 `892`、默认可运行 `891`、live-gated `1`，分层为 unit `396`、component `369`、contract `77`、integration `49`、e2e `1`；目标 `git diff --check` 通过。`npm run typecheck` 本轮启动后持续无输出，已终止本进程启动的 `npm run typecheck` / `tsc --noEmit`，本刀不把该项记为通过；未执行 `verify:gui-smoke`，因为本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-02：P3 防回流规则补强。为避免后续 Agent 在新增代码时继续把可纯化业务逻辑写进重组件挂载测试，已同步更新根 `AGENTS.md`、`internal/aiprompts/quality-workflow.md`、`internal/test/unit-tests.md` 与项目内 `lime-quality-workflow` skill / reference：新增或重写复杂 UI / hook 逻辑时，筛选、分组、formatter、request builder、runtime 参数投影、状态机和 reducer 必须优先抽到 View Model / projection / selector / helper，并用 `*.unit.test.ts` 覆盖；component 测试只保留渲染、DOM 事件、hook 生命周期和关键接线回归；暂时不能纯化的分支必须写明原因、风险层级和退出条件。
- 2026-06-02：P3 机械提示补强。`vitest-layer-classifier` 已禁止显式低风险后缀掩盖 React/jsdom、DevBridge/Tauri、文件系统、网络或 Playwright 等高风险边界；`test:layers:stats` 现在额外输出 `Component unit-migration candidates`，按用例数量、文件大小和筛选 / 分组 / formatter / request builder / runtime metadata / reducer / selector 等信号提示适合继续抽 VM 的 component 测试。当前实测候选 `57` 个，主要来自历史大 component 测试；该项只做治理提示，不作为当前失败门禁，避免一次性阻塞历史债务清理。
- 2026-06-02：P2 新一刀完成。将 `CodeReviewSummaryPanel` 的文件审阅聚合、失败输出排序、输出预览 / 详情裁剪、review 状态 / 焦点 / 主按钮决策下沉到 `CodeReviewSummaryPanelViewModel.ts`，并把 `codeFixPrompt` 的纯提示词构造移到独立 `codeFixPrompt.unit.test.ts`；`CodeReviewSummaryPanel.test.tsx` 现在只保留 React 渲染和事件接线回归。验证：`npx eslint src/components/agent/chat/components/CodeReviewSummaryPanel.tsx src/components/agent/chat/components/CodeReviewSummaryPanelViewModel.ts src/components/agent/chat/components/CodeReviewSummaryPanelViewModel.unit.test.ts src/components/agent/chat/components/CodeReviewSummaryPanel.test.tsx src/components/agent/chat/utils/codeFixPrompt.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/CodeReviewSummaryPanelViewModel.unit.test.ts src/components/agent/chat/utils/codeFixPrompt.unit.test.ts` 通过；`npm run test:component -- src/components/agent/chat/components/CodeReviewSummaryPanel.test.tsx` 通过；`npm run test:layers:stats` 统计更新为总 `894`、默认可运行 `893`、live-gated `1`，分层为 unit `398`、component `369`、contract `77`、integration `49`、e2e `1`；目标 `git diff --check` 通过。
- 2026-06-02：P2 新一刀完成。继续收敛 `ImageTaskViewer.tsx`，将 mode / status / layout / storyboard / runtime contract / empty state / source placeholder / follow-up command / output ordering 等纯展示与命令推导抽到 `ImageTaskViewerViewModel.ts`，并新增 `ImageTaskViewerViewModel.unit.test.ts` 覆盖中文文案、分镜选择、运行合同、策略标签和 follow-up 命令；`ImageTaskViewer.test.tsx` 只保留关闭按钮、资源管理器接线、结果图错误、缩略图切换、来源图预览和保存 / 应用动作等组件层回归。验证：`npx eslint src/components/agent/chat/components/ImageTaskViewer.tsx src/components/agent/chat/components/ImageTaskViewerViewModel.ts src/components/agent/chat/components/ImageTaskViewerViewModel.unit.test.ts src/components/agent/chat/components/ImageTaskViewer.test.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/ImageTaskViewerViewModel.unit.test.ts` 通过；`npm run test:component -- src/components/agent/chat/components/ImageTaskViewer.test.tsx` 通过；`npm run test:layers:stats` 统计更新为总 `895`、默认可运行 `894`、live-gated `1`，分层为 unit `399`、component `369`、contract `77`、integration `49`、e2e `1`；`git diff --check` 通过。
- 2026-06-02：P2 新一刀完成。继续收敛 `ToolCallDisplay.tsx`，将代码块渲染判定、结果图片 / metadata 归一化、命令摘要、stdout/stderr 分流、Skill invocation metadata、结果路径展示、工具分组和 ToolSearch 识别等纯展示 / selector 逻辑抽到 `ToolCallDisplayViewModel.ts`，并新增 `ToolCallDisplayViewModel.unit.test.ts` 覆盖命令结果代码块、命令摘要、输出分流、Skill 快照、图片 / 路径 / result notice 和分组；`ToolCallDisplay.test.tsx` 从 `28` 个组件 case 收窄到 `26` 个，删除 Windows 编码 metadata 纯分支，只保留搜索预览、分组展开、命令输出接线、diff 审阅、站点保存、图片预览、ToolSearch 和动作句等组件层回归。`ToolCallDisplay.tsx` 从 `2375` 行降到 `1778` 行。验证：`npx eslint src/components/agent/chat/components/ToolCallDisplay.tsx src/components/agent/chat/components/ToolCallDisplayViewModel.ts src/components/agent/chat/components/ToolCallDisplayViewModel.unit.test.ts src/components/agent/chat/components/ToolCallDisplay.test.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/ToolCallDisplayViewModel.unit.test.ts` 实测 `7` 个 case、总 `6.53s` 通过；`npm run test:component -- src/components/agent/chat/components/ToolCallDisplay.test.tsx` 实测 `26` 个 case、总 `13.01s` 通过；`npm run test:layers:stats` 统计更新为总 `896`、默认可运行 `895`、live-gated `1`，分层为 unit `400`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令或 mock，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `AgentThreadTimeline.tsx`，在既有 `timeline-utils` 基础上新增 `AgentThreadTimelineViewModel.ts`，将可见 item 过滤、block emphasis 判定，以及 artifact inline、历史单条 thinking 摘要壳、运行中 thinking inline、多工具分组和 detail materialization 等纯渲染计划抽到 VM；组件继续只负责 React state、DOM、滚动、回调和真实卡片渲染。新增 `AgentThreadTimelineViewModel.unit.test.ts` 覆盖权限等待内部错误 / 隐藏 artifact 过滤、focused / active / quiet emphasis、历史 reasoning 摘要壳、结构化 reasoning、运行中 thinking、多工具分组和多文件 artifact inline。`AgentThreadTimeline.tsx` 从 `1144` 行降到 `1114` 行。验证：`npx eslint src/components/agent/chat/components/AgentThreadTimeline.tsx src/components/agent/chat/components/AgentThreadTimelineViewModel.ts src/components/agent/chat/components/AgentThreadTimelineViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/AgentThreadTimelineViewModel.unit.test.ts` 实测 `5` 个 case、总 `11.94s` 通过；`npm run test:component -- src/components/agent/chat/components/AgentThreadTimeline.test.tsx` 实测 `35` 个 case、总 `19.48s` 通过；`npm run test:layers:stats` 统计更新为总 `897`、默认可运行 `896`、live-gated `1`，分层为 unit `401`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令或 mock，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `CanvasWorkbenchLayout.tsx`，将 workspace 路径归一化、展示 / 命令相对路径解析、已保存内容包根路径识别、工作区面板展示路径、内容包目录识别、目录项排序、entry 构造 / 去重、default preview / workspace file / artifact selection 解析、previous content 映射、preview 内容 / 路径解析、HTML 预览识别、coding 预览标签、变更项匹配、变更状态文案 / 样式等纯 selector / projection 抽到 `CanvasWorkbenchLayoutViewModel.ts`，并新增 `CanvasWorkbenchLayoutViewModel.unit.test.ts` 覆盖 macOS / Windows 分隔符、workspace 内外路径、`exports/<type>/<slug>` bundle root、结果目录识别、内容包 / 普通目录排序、preview target、HTML badge、change item 匹配、状态展示、entry 去重、default preview、workspace file 和 artifact previous content。组件继续只保留目录加载、React state、DOM 事件、图标渲染和文件预览接线；`CanvasWorkbenchLayout.tsx` 从 `3417` 行降到 `2559` 行。验证：`npx eslint src/components/agent/chat/components/CanvasWorkbenchLayout.tsx src/components/agent/chat/components/CanvasWorkbenchLayoutViewModel.ts src/components/agent/chat/components/CanvasWorkbenchLayoutViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/CanvasWorkbenchLayoutViewModel.unit.test.ts` 实测 `14` 个 case、总 `3.22s` 通过；`npm run test:component -- src/components/agent/chat/components/CanvasWorkbenchLayout.test.tsx` 实测 `17` 个 case、总 `8.24s` 通过；`npm run test:layers:stats` 统计更新为总 `898`、默认可运行 `897`、live-gated `1`，分层为 unit `402`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令或 mock，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `AgentThreadReliabilityPanel.tsx`，新增 `AgentThreadReliabilityPanelViewModel.ts`，将诊断剪贴板 JSON 序列化、状态 tone 样式、统计卡片样式、runtime fallback chain / decision reason 优先级、当前 turn prompt 和 team memory shadow key 等纯 selector 抽到 VM；组件继续保留 API 预取、clipboard 副作用、toast、文件快照对话框和实际渲染接线。新增 `AgentThreadReliabilityPanelViewModel.unit.test.ts` 覆盖 Date 序列化、tone 样式、fallback chain / decision reason 优先级、prompt fallback 和 memory shadow key。`AgentThreadReliabilityPanel.tsx` 从 `1243` 行降到 `1192` 行。验证：`npx eslint src/components/agent/chat/components/AgentThreadReliabilityPanel.tsx src/components/agent/chat/components/AgentThreadReliabilityPanelViewModel.ts src/components/agent/chat/components/AgentThreadReliabilityPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/AgentThreadReliabilityPanelViewModel.unit.test.ts` 实测 `6` 个 case、总 `1.18s` 通过；`npm run test:component -- src/components/agent/chat/components/AgentThreadReliabilityPanel.test.tsx` 实测 `21` 个 case、总 `4.20s` 通过；`npm run test:layers:stats` 统计更新为总 `899`、默认可运行 `898`、live-gated `1`，分层为 unit `403`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令或 mock，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `FileManagerSidebar.tsx`，新增 `fileManagerSidebarViewModel.ts`，将固定位置解析、应用 / Skill 包入口识别、右键菜单视口约束和菜单动作规划等纯 selector / planner 抽到 VM；组件继续保留 localStorage、clipboard、toast、文件系统 API、拖拽、图标映射和真实渲染接线。新增 `fileManagerSidebarViewModel.unit.test.ts` 覆盖畸形固定位置拒绝、应用 / Skill 包识别、右键菜单边界约束、普通文件预览 / 资料动作、Skill 包安装入口和资料整理禁用原因。`FileManagerSidebar.tsx` 从 `1133` 行降到 `1004` 行，新的 VM 为 `193` 行。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/FileManager/fileManagerSidebarViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/FileManager/FileManagerSidebar.tsx src/components/agent/chat/components/FileManager/fileManagerSidebarViewModel.ts src/components/agent/chat/components/FileManager/fileManagerSidebarViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/FileManager/fileManagerSidebarViewModel.unit.test.ts` 实测 `6` 个 case、总 `1.12s` 通过；`npm run test:component -- src/components/agent/chat/components/FileManager/FileManagerSidebar.test.tsx` 实测 `15` 个 case、总 `1.87s` 通过；`npm run test:layers:stats` 统计更新为总 `900`、默认可运行 `899`、live-gated `1`，分层为 unit `404`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令或 mock，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `ChatSidebar.tsx`，新增 `ChatSidebarViewModel.ts`，将任务卡片构造、当前任务状态预览、任务搜索 / active 过滤、运行 / 待处理 / 最近 / 归档分组、状态元信息、子任务优先级排序、子任务类型 / 时间解析和折叠摘要等纯 selector / formatter 下沉到 VM；组件继续保留 localStorage、React state、滚动、菜单、DOM 事件和性能打点副作用。新增 `ChatSidebarViewModel.unit.test.ts` 覆盖内部图片标题展示、当前任务运行态预览、工作区异常 / 待处理提示、关键词和 active 过滤、任务中心分组标题、当前 / 固定排序、子任务状态优先级、折叠摘要、类型 label 和 Unix 时间解析。`ChatSidebar.tsx` 从 `1881` 行降到 `1459` 行，新的 VM 为 `508` 行。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/ChatSidebarViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/ChatSidebar.tsx src/components/agent/chat/components/ChatSidebarViewModel.ts src/components/agent/chat/components/ChatSidebarViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/ChatSidebarViewModel.unit.test.ts` 实测 `6` 个 case、总 `5.90s` 通过；`npm run test:component -- src/components/agent/chat/components/ChatSidebar.test.tsx` 实测 `29` 个 case、总 `7.46s` 通过；`npm run test:layers:stats` 统计更新为总 `901`、默认可运行 `900`、live-gated `1`，分层为 unit `405`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。`npx tsc --noEmit --pretty false --skipLibCheck false` 作为探测启动后长时间无输出，已只终止本进程启动的该组 PID，本刀不把 typecheck 记为通过；未执行 `verify:gui-smoke`，因为本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议。
- 2026-06-02：P2 新一刀完成。继续收敛 `CuratedTaskLauncherDialog.tsx`，新增 `CuratedTaskLauncherDialogViewModel.ts`，将启动 readiness、已选 reference 解析、reference 勾选上限计划、outcome summary、starter contract、最新 review feedback signal 选择、推荐模板解析、sceneapp / 灵感 reference baseline snapshot / highlights / carry hint 等纯 selector / planner 下沉到 VM；组件继续保留订阅、Unified Memory API、React state、DOM、表单输入、按钮事件和真实渲染接线。新增 `CuratedTaskLauncherDialogViewModel.unit.test.ts` 覆盖必填项计数 / 禁用态、reference 缺失和选择上限、outcome / contract、最新 review signal / 推荐模板、baseline 摘要和 carry hint。`CuratedTaskLauncherDialog.tsx` 从 `903` 行降到 `820` 行，新的 VM 为 `317` 行。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/CuratedTaskLauncherDialogViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/CuratedTaskLauncherDialog.tsx src/components/agent/chat/components/CuratedTaskLauncherDialogViewModel.ts src/components/agent/chat/components/CuratedTaskLauncherDialogViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/CuratedTaskLauncherDialogViewModel.unit.test.ts` 实测 `5` 个 case、总 `5.47s` 通过；`npm run test:component -- src/components/agent/chat/components/CuratedTaskLauncherDialog.test.tsx` 实测 `12` 个 case、总 `2.53s` 通过；`npm run test:layers:stats` 统计更新为总 `902`、默认可运行 `901`、live-gated `1`，分层为 unit `406`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令或 mock，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `EmptyStateComposerPanel.tsx`，新增 `EmptyStateComposerPanelViewModel.ts`，将 active capability badge 派生、Tab 起手建议排序 / 展示条件 / 当前项选择、当前模型摘要、高级控件显示与高亮状态等纯 selector / projection 下沉到 VM；组件继续保留 React state/effect、Tab suggestion timer、textarea/file input、Team suggestion、DOM 事件和真实渲染接线。新增 `EmptyStateComposerPanelViewModel.unit.test.ts` 覆盖建议排序、空输入展示条件、上下文压制、index 取模、知识包命令过滤、curated task reference、provider/model 裁剪和高级控件状态。`EmptyStateComposerPanel.tsx` 从 `727` 行降到 `710` 行，新的 VM 为 `255` 行。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/EmptyStateComposerPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/EmptyStateComposerPanel.tsx src/components/agent/chat/components/EmptyStateComposerPanelViewModel.ts src/components/agent/chat/components/EmptyStateComposerPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/EmptyStateComposerPanelViewModel.unit.test.ts` 实测 `6` 个 case、总 `1.14s` 通过；`npm run test:component -- src/components/agent/chat/components/EmptyStateComposerPanel.test.tsx` 实测 `33` 个 case、总 `6.38s` 通过；`npm run test:layers:stats` 统计更新为总 `903`、默认可运行 `902`、live-gated `1`，分层为 unit `407`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `EmptyState.tsx`，新增 `EmptyStateViewModel.ts`，将首页默认 curated task 引用来源选择、推荐 prompt 裁剪、快速推荐项、引导帮助 label、最近会话入口 label/title/supplemental action，以及 composer 起手建议暴露条件等纯 selector / projection 下沉到 VM；组件继续保留 React state/effect、catalog / config 订阅、图片读取、toast、DOM 事件、模板启动和真实渲染接线。新增 `EmptyStateViewModel.unit.test.ts` 覆盖 prompt 规范化、显式引用优先于 creation replay、快速推荐前四项、引导帮助 starter label、最近会话入口和自动站点技能 / guide help 压制 input suggestion。`EmptyState.tsx` 从 `1684` 行降到 `1679` 行，新的 VM 为 `152` 行。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/EmptyStateViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/EmptyState.tsx src/components/agent/chat/components/EmptyStateViewModel.ts src/components/agent/chat/components/EmptyStateViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/EmptyStateViewModel.unit.test.ts` 实测 `6` 个 case、总 `2.20s` 通过；`npm run test:component -- src/components/agent/chat/components/EmptyState.test.tsx` 实测 `54` 个 case、总 `11.60s` 通过；`npm run test:layers:stats` 统计更新为总 `904`、默认可运行 `903`、live-gated `1`，分层为 unit `408`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchSidebar.test.tsx` 候选背后的纯逻辑，新增 `generalWorkbenchWorkflowPanelViewModel.ts`，将 `useGeneralWorkbenchWorkflowPanelState` 中 completed/progress、activity log 分组、creation task 分组、active run metadata summary/text/stage label 等纯 selector 下沉到 VM；hook 继续只保留 React 展开/折叠状态和 toggle。新增 `generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 覆盖进度计算、活动日志运行分组、任务类型 label、metadata 格式化和 stage label。`useGeneralWorkbenchWorkflowPanelState.ts` 从 `113` 行降到 `71` 行，新的 VM 为 `79` 行。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/useGeneralWorkbenchWorkflowPanelState.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts src/components/agent/chat/components/generalWorkbenchWorkflowData.test.ts` 实测 `7` 个 case、总 `5.33s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `38` 个 case、总 `6.54s` 通过；`npm run test:layers:stats` 统计更新为总 `905`、默认可运行 `904`、live-gated `1`，分层为 unit `409`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchSidebar.test.tsx` 候选背后的执行日志纯逻辑，新增 `generalWorkbenchExecLogData.unit.test.ts`，将消息 / thinking / tool / response 日志投影、工具展示标签、工具详情、技能详情、creation task 和清空时间过滤等纯 projection 覆盖迁移到 unit 层；`GeneralWorkbenchSidebar.test.tsx` 删除 1 个重复工具标签组件分支，组件 case 从 `38` 收窄到 `37`，继续只保留执行日志 UI 接线和侧栏交互回归。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchExecLogData.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/generalWorkbenchExecLogData.ts src/components/agent/chat/components/generalWorkbenchExecLogData.unit.test.ts src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchExecLogData.unit.test.ts` 实测 `4` 个 case、总 `2.73s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `37` 个 case、总 `9.87s` 通过；`npm run test:layers:stats` 统计更新为总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchSidebar.test.tsx` 候选背后的工作流面板纯逻辑，把 `GeneralWorkbenchWorkflowPanel.tsx` 内的产出记录摘要、result handoff 文案、curated task follow-up hint、当前模板 / 跨模板 follow-up payload、review feedback follow-up payload 和任务文件标题解析迁到 `generalWorkbenchWorkflowPanelViewModel.ts`；组件继续保留订阅、DOM 展示、展开切换和按钮回调接线。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 从 `2` 个 case 扩到 `6` 个 case，覆盖 creation summary、handoff、visible actions 去重、当前模板 payload、复盘跨模板 payload 和 sceneapp 基线 review payload；`GeneralWorkbenchSidebar.test.tsx` 删除 3 个重复 payload 细节挂载分支，组件 case 从 `37` 收窄到 `34`，保留“当前进展复盘建议点击”和“普通建议下一步点击”作为 UI 接线回归。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `6` 个 case、总 `5.56s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `7.41s` 通过；`npm run test:layers:stats` 统计保持总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将活动日志 gate/source/status label、run id 短标签、运行状态 label、primary activity log 选择、activity summary、activity section summary 和 run detail summary 等纯 formatter / projection 迁到 `generalWorkbenchWorkflowPanelViewModel.ts`；组件继续只保留 badge class、日志行渲染、展开区和运行 / 产物按钮接线。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 从 `6` 个 case 扩到 `8` 个 case，覆盖活动日志摘要、当前查看 run meta、运行详情 fallback 和组合摘要；`GeneralWorkbenchWorkflowPanel.tsx` 进一步降到约 `1876` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `8` 个 case、总 `3.82s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `4.90s` 通过；`npm run test:layers:stats` 统计保持总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将分支区的标题 / 新建按钮 / 主次动作 / empty 文案、状态 label、meta 文案、summary 文案、当前分支选择、相关分支计数和状态优先级排序迁到 `generalWorkbenchWorkflowPanelViewModel.ts`，组件继续只保留下拉菜单、切换显示、切换 topic、状态更新和删除按钮接线。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 从 `8` 个 case 扩到 `9` 个 case，覆盖 version 模式分支 projection、排序、当前分支、summary、状态 label 和 meta；`GeneralWorkbenchWorkflowPanel.tsx` 降到约 `1697` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `9` 个 case、总 `2.40s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `4.29s` 通过；`npm run test:layers:stats` 统计保持总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将当前工作流摘要、队列截断、隐藏队列计数、完成项提示、进度 pill / 百分比 clamp 等纯 projection 下沉到 `generalWorkbenchWorkflowPanelViewModel.ts`，组件继续只保留图标 / badge class、队列行渲染和 DOM 接线。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 从 `9` 个 case 扩到 `11` 个 case，覆盖 active / error / pending 排序后的当前焦点、队列隐藏数、完成提示、空工作流 fallback 和已完成工作流 fallback；`GeneralWorkbenchWorkflowPanel.tsx` 降到 `1585` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `11` 个 case、总 `4.08s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `4.38s` 通过；`npm run test:layers:stats` 统计保持总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将活动日志卡片标题、状态 / 来源 / 闸门 / 步骤 / 产物 badge、步骤 input/output summary、run 短标签和无 runId 时的 artifact action 目标下沉到 `buildGeneralWorkbenchActivityLogProjection`；组件继续只保留卡片 JSX、按钮回调和产物操作接线。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 继续保持 `11` 个 case，在活动日志用例中补充 projection 断言；`GeneralWorkbenchWorkflowPanel.tsx` 降到 `1559` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `11` 个 case、总 `4.09s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `4.98s` 通过；`npm run test:layers:stats` 统计保持总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将运行详情 header 的状态文案、source fallback、workflow / curated task / artifact badges、summary 和 artifact path 列表下沉到 `buildGeneralWorkbenchRunDetailProjection`；组件继续只保留运行详情 JSX、复制按钮和产物定位 / 打开接线。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 继续保持 `11` 个 case，在运行详情用例中补充 projection 断言；`GeneralWorkbenchWorkflowPanel.tsx` 降到 `1538` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `11` 个 case、总 `5.13s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `6.67s` 通过；`npm run test:layers:stats` 统计保持总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将最新 review feedback signal 选择、review feedback projection、sceneapp review baseline snapshot / highlights、curated task follow-up hint 和 follow-up 区域显隐下沉到 `buildGeneralWorkbenchFollowUpProjection`；组件继续只保留 signals 订阅 / 读取、卡片 JSX 和按钮回调接线，避免把 `localStorage` / window 边界带入 unit 层。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 从 `11` 个 case 扩到 `12` 个 case，新增覆盖最新 review feedback 选择、下游结果模板建议、sceneapp 基线字段和 follow-up payload；`GeneralWorkbenchWorkflowPanel.tsx` 降到 `1485` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `2.83s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `3.57s` 通过；`npm run test:layers:stats` 统计保持总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将 curated task follow-up actions 的去重、payload 构造和 aria label 投影下沉到 `buildCuratedTaskFollowUpActionItems`；组件的 `CuratedTaskFollowUpActions` 只保留按钮渲染和 `onApplyFollowUpAction` 接线，不再在 render map 内拼 payload。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 保持 `12` 个 case，在当前模板建议下一步用例中补充 action items 断言；`GeneralWorkbenchWorkflowPanel.tsx` 降到 `1472` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `2.82s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `3.59s` 通过；`npm run test:layers:stats` 统计保持总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将产出记录 group / row 的 count 文案、任务文件标题、复制目标和复制 aria label 下沉到 `buildGeneralWorkbenchCreationTaskGroupProjection`；组件继续只保留列表渲染和 `onCopyText` 接线。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 保持 `12` 个 case，在产出记录用例中补充绝对路径复制和普通路径复制 projection 断言；`GeneralWorkbenchWorkflowPanel.tsx` 降到 `1470` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `4.02s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `5.01s` 通过；`npm run test:layers:stats` 统计保持总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将队列行的序号 / 状态文案、当前步骤状态文案、分支行 status/meta/delete aria/hint/action items，以及 run detail 产物 copy/reveal/open 按钮 label / aria / target 下沉到 `generalWorkbenchWorkflowPanelViewModel.ts`；组件继续只保留图标、className、列表渲染和 handler 接线。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 保持 `12` 个 case，但在当前工作流、分支区和运行详情用例中补充 `queueItems`、`itemProjections`、`artifacts.actions` 的 projection 断言；`GeneralWorkbenchWorkflowPanel.tsx` 从 `1470` 行降到 `1426` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `2.79s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `3.25s` 通过；`npm run test:layers:stats` 统计保持总 `907`、默认可运行 `906`、live-gated `1`，分层为 unit `411`、component `369`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将 run detail 顶部复制运行 ID / 复制原始记录按钮的 label、aria 和 copy target 下沉到 `buildGeneralWorkbenchRunDetailActions`，并由 `buildGeneralWorkbenchRunDetailProjection` 统一暴露 `actions`；组件继续只按 action 渲染按钮并调用 `onCopyText`。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 保持 `12` 个 case，在运行详情用例中补充 `actions` projection 断言；`GeneralWorkbenchWorkflowPanel.tsx` 从 `1426` 行降到 `1417` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `3.47s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `3.59s` 通过；`npm run test:layers:stats` 统计为总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，将活动日志里的查看运行 action，以及无 runId 产物的定位 / 打开 action label、aria、target、sessionId 下沉到 `buildGeneralWorkbenchActivityLogProjection` / `buildGeneralWorkbenchActivityArtifactActionGroup`；组件继续只渲染 action 并分发到 `onViewRunDetail`、`onRevealArtifactInFinder`、`onOpenArtifactWithDefaultApp`。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 保持 `12` 个 case，在活动日志用例中补充 `runAction` 和 `artifactActions` projection 断言；`GeneralWorkbenchWorkflowPanel.tsx` 从 `1417` 行降到 `1407` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `3.23s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `3.73s` 通过；`npm run test:layers:stats` 统计保持总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，新增 `buildGeneralWorkbenchCreationTaskSectionProjection`，将产出记录 section 的 empty 文案、复制按钮 label 和 group projection 列表统一下沉到 VM；组件不再在 JSX map 内即时构造 group projection，也不再直接拼 `outputs.empty` / `outputs.copyPath`。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 保持 `12` 个 case，在产出记录用例中补充 section projection 断言；`GeneralWorkbenchWorkflowPanel.tsx` 从 `1407` 行降到 `1397` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `3.88s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `4.71s` 通过；`npm run test:layers:stats` 统计保持总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收敛 `GeneralWorkbenchWorkflowPanel.tsx`，新增 `buildGeneralWorkbenchActivitySectionProjection`，将 activity section 的 empty/loading/run detail title 文案和 activity log projection 列表统一下沉到 VM；`renderActivityLogItem` 改为消费 projection，不再接收原始 group 后在组件内构造。`generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 保持 `12` 个 case，在活动日志用例中补充 section projection 断言；`GeneralWorkbenchWorkflowPanel.tsx` 从 `1397` 行降到 `1390` 行，`GeneralWorkbenchSidebar.test.tsx` 保持 `34` 个组件接线 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `9.72s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `34` 个 case、总 `8.46s` 通过；`npm run test:layers:stats` 统计保持总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收窄 `GeneralWorkbenchSidebar.test.tsx`，删除已经由 `generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 覆盖的运行详情复制 ID / 原始记录、产出记录分组 / 复制路径两个组件细节用例；组件层保留运行产物复制 / 定位 / 打开、活动产物定位 / 打开和失败 toast 等真实 DOM/handler 接线回归。`GeneralWorkbenchSidebar.test.tsx` 从 `34` 个 case 收窄到 `32` 个 case，文件从 `1952` 行降到 `1818` 行；VM unit 保持 `12` 个 case。验证：`npx eslint src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx --max-warnings 0` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `32` 个 case、总 `6.25s` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `4.85s` 通过；`npm run test:layers:stats` 统计保持总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收窄 `GeneralWorkbenchSidebar.test.tsx`，将“切到下游结果模板后仍能显影 sceneapp 基线”的组合覆盖补到 `buildGeneralWorkbenchFollowUpProjection` unit 层，并删除下游模板基线展示、run detail 基线展示、run detail review banner 展示这 3 个组件 display-only 用例；组件层保留当前进展基线展示、review feedback action 点击回传和 curated task follow-up 点击回传等真实 DOM/handler 接线回归。`GeneralWorkbenchSidebar.test.tsx` 从 `32` 个 case 收窄到 `29` 个 case，文件从 `1818` 行降到 `1606` 行；VM unit 保持 `12` 个 case，新增下游模板 sceneapp fallback 断言。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `2.43s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `29` 个 case、总 `3.16s` 通过；`npm run test:layers:stats` 统计保持总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收窄 `GeneralWorkbenchSidebar.test.tsx`，删除“运行 metadata 带着 curated task 时应展示建议下一步”这个 display-only 用例；当前模板 follow-up 的去重、hint 文案、action payload 已由 `generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 覆盖，组件层继续保留“点击建议下一步应把 continuation prompt 回传给工作区”作为真实 DOM/handler 接线回归。`GeneralWorkbenchSidebar.test.tsx` 从 `29` 个 case 收窄到 `28` 个 case，文件从 `1606` 行降到 `1559` 行；VM unit 保持 `12` 个 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `4.10s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `28` 个 case、总 `5.44s` 通过；`npm run test:layers:stats` 统计保持总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收窄 `GeneralWorkbenchSidebar.test.tsx`，删除活动日志闸门 / 运行标识展示、按运行维度分组展示步骤、运行详情基础卡片展示这 3 个 display-only 用例；这些标题、状态、闸门、run label、step summary、run detail title / status projection 已由 `generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 的 activity / run detail projection 覆盖。组件层保留“点击运行标识”“运行产物复制 / 定位 / 打开”“活动产物定位 / 打开”和失败 toast 等真实 DOM/handler 接线回归。`GeneralWorkbenchSidebar.test.tsx` 从 `28` 个 case 收窄到 `25` 个 case，文件从 `1559` 行降到 `1420` 行；VM unit 保持 `12` 个 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `3.47s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `25` 个 case、总 `4.47s` 通过；`npm run test:layers:stats` 统计保持总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收窄 `GeneralWorkbenchSidebar.test.tsx`，删除“版本模式应展示可继续版本语义”组件用例；版本模式 section title、create label、primary action label、summary、排序和 action item 已由 `buildGeneralWorkbenchBranchSectionProjection` unit 覆盖，组件层保留 topic 分支状态点击作为 DOM/handler 接线回归。`GeneralWorkbenchSidebar.test.tsx` 从 `25` 个 case 收窄到 `24` 个 case，文件从 `1420` 行降到 `1378` 行；VM unit 保持 `12` 个 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `3.18s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `24` 个 case、总 `3.94s` 通过；`npm run test:layers:stats` 统计保持总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续收窄 `GeneralWorkbenchSidebar.test.tsx`，删除 review feedback banner 的纯文案展示用例；review feedback signal 选择、suggestion projection 和 follow-up payload 已由 `buildGeneralWorkbenchFollowUpProjection` unit 覆盖，组件层保留 review feedback action 点击回传作为真实 DOM/handler 接线回归。`GeneralWorkbenchSidebar.test.tsx` 从 `24` 个 case 收窄到 `23` 个 case，文件从 `1378` 行降到 `1310` 行；VM unit 保持 `12` 个 case。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npm run test:unit -- src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts` 实测 `12` 个 case、总 `6.27s` 通过；`npm run test:component -- src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx` 实测 `23` 个 case、总 `8.27s` 通过；清理未使用 import 后 `npx eslint src/components/agent/chat/components/GeneralWorkbenchSidebar.test.tsx src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.unit.test.ts src/components/agent/chat/components/generalWorkbenchWorkflowPanelViewModel.ts src/components/agent/chat/components/GeneralWorkbenchWorkflowPanel.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 统计保持总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选 `56`；目标 `git diff --check` 通过。当前该组件测试仍因 `>=20` case 和 `>=800` 行阈值留在候选列表，但剩余用例主要是折叠、添加上下文、日志展开 / 清空 / 筛选、搜索来源切换、结果详情跳转、分支点击、follow-up 点击、文件复制 / 定位 / 打开和失败 toast 等真实 UI 接线；下一刀不应为压阈值继续硬删，应转向其他已具备 VM 的候选文件。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 `FileManagerSidebar.test.tsx` 这个已具备 VM 的候选文件，删除“右键菜单靠近侧栏右侧时应向左避让”组件用例；右键菜单坐标避让已由 `fileManagerSidebarViewModel.unit.test.ts` 的 `resolveContextMenuPosition` 覆盖，组件层保留加载目录、右键加入对话、项目目录优先、路径脱敏、关闭侧栏、设为资料、普通点击文本文件、Skill 包安装、工作台预览、非文本资料禁用、应用图标渲染等真实 DOM/handler 接线回归。`FileManagerSidebar.test.tsx` 从 `822` 行降到 `773` 行，低于 `large-component-file` 的 `800` 行阈值；组件 case 保持 `14` 个。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/FileManager/fileManagerSidebarViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/FileManager/FileManagerSidebar.test.tsx src/components/agent/chat/components/FileManager/fileManagerSidebarViewModel.unit.test.ts src/components/agent/chat/components/FileManager/fileManagerSidebarViewModel.ts src/components/agent/chat/components/FileManager/FileManagerSidebar.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/FileManager/fileManagerSidebarViewModel.unit.test.ts` 实测 `6` 个 case、总 `1.42s` 通过；`npm run test:component -- src/components/agent/chat/components/FileManager/FileManagerSidebar.test.tsx` 实测 `14` 个 case、总 `2.17s` 通过；`npm run test:layers:stats` 统计为总 `909`、默认可运行 `908`、live-gated `1`，分层为 unit `411`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `56` 降到 `55`，`FileManagerSidebar.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向未被并行修改且已有 VM 的 `CodeReviewSummaryPanel.test.tsx`，删除“输出预览应裁剪长内容”组件 display-only 用例；裁剪规则已由 `CodeReviewSummaryPanelViewModel.unit.test.ts` 的 `buildOutputPreviewText` 覆盖，组件层继续保留文件变更、失败输出、主按钮跳转、继续修复 prompt、失败文件排序、审阅状态、快照入口和空态等真实 DOM/handler 回归。`CodeReviewSummaryPanel.test.tsx` 从 `803` 行降到 `768` 行，低于 `large-component-file` 的 `800` 行阈值；组件 case 从 `16` 个收窄到 `15` 个。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/CodeReviewSummaryPanelViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/CodeReviewSummaryPanel.test.tsx src/components/agent/chat/components/CodeReviewSummaryPanelViewModel.unit.test.ts src/components/agent/chat/components/CodeReviewSummaryPanelViewModel.ts src/components/agent/chat/components/CodeReviewSummaryPanel.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/CodeReviewSummaryPanelViewModel.unit.test.ts` 实测 `4` 个 case、总 `1.36s` 通过；`npm run test:component -- src/components/agent/chat/components/CodeReviewSummaryPanel.test.tsx` 实测 `15` 个 case、总 `3.09s` 通过；`npm run test:layers:stats` 统计为总 `910`、默认可运行 `909`、live-gated `1`，分层为 unit `412`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `55` 降到 `54`，`CodeReviewSummaryPanel.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 `InputbarCore.test.tsx`，新增 `inputbarCoreCopy.unit.test.ts` 覆盖 `buildInputbarCoreCopy` 的中英文输入区 chrome、action 和 dictation 文案构造，并删除“应支持使用 en-US copy 渲染输入区 chrome 文案”这个 display-only 组件用例；组件层保留“语音输入未启用反馈应跟随 en-US 资源”等真实 click / toast / 配置接线回归。`InputbarCore.test.tsx` 从 `824` 行降到 `794` 行，低于 `large-component-file` 的 `800` 行阈值；新增 unit 为 `47` 行，组件 case 为 `18` 个。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/Inputbar/components/inputbarCoreCopy.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/Inputbar/components/InputbarCore.test.tsx src/components/agent/chat/components/Inputbar/components/inputbarCoreCopy.unit.test.ts src/components/agent/chat/components/Inputbar/components/inputbarCoreCopy.ts src/components/agent/chat/components/Inputbar/components/InputbarCore.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/Inputbar/components/inputbarCoreCopy.unit.test.ts` 实测 `1` 个 case、总 `2.37s` 通过；`npm run test:component -- src/components/agent/chat/components/Inputbar/components/InputbarCore.test.tsx` 实测 `18` 个 case、总 `8.14s` 通过；`npm run test:layers:stats` 统计为总 `911`、默认可运行 `910`、live-gated `1`，分层为 unit `413`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `54` 降到 `53`，`InputbarCore.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 的 `CuratedTaskLauncherDialog.test.tsx`，删除两个已经由 `sceneAppCuratedTaskReference.test.ts` 与 `CuratedTaskLauncherDialogViewModel.unit.test.ts` 覆盖的 sceneapp 基线纯展示组件用例，并用 `findConfirmButton` 收敛重复确认按钮查询；组件层继续保留成果类灵感 reference 进入下游模板 / 内容主稿模板的真实预填、灵感回流刷新、手动勾选保留和 `onConfirm` payload 接线回归。`CuratedTaskLauncherDialog.test.tsx` 从 `897` 行降到 `798` 行，低于 `large-component-file` 的 `800` 行阈值；组件 case 从 `12` 个收窄到 `10` 个。验证：`npm run test:unit -- --list --explain src/components/agent/chat/components/CuratedTaskLauncherDialogViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/agent/chat/components/CuratedTaskLauncherDialog.test.tsx src/components/agent/chat/components/CuratedTaskLauncherDialogViewModel.unit.test.ts src/components/agent/chat/components/CuratedTaskLauncherDialogViewModel.ts src/components/agent/chat/components/CuratedTaskLauncherDialog.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/components/CuratedTaskLauncherDialogViewModel.unit.test.ts` 实测 `5` 个 case、总 `8.25s` 通过；`npm run test:component -- src/components/agent/chat/components/CuratedTaskLauncherDialog.test.tsx` 实测 `10` 个 case、总 `9.04s` 通过，最终复跑实测总 `8.13s` 通过；`npm run test:layers:stats` 统计为总 `911`、默认可运行 `910`、live-gated `1`，分层为 unit `413`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `53` 降到 `52`，`CuratedTaskLauncherDialog.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `KnowledgePage.test.tsx`，不删除业务场景，只做测试文件机械收敛：移除无用 `React` import、压缩 `IS_REACT_ACT_ENVIRONMENT` 赋值块并去掉首页断言里的冗余空行；项目资料首页、空态、状态说明、整理、确认、创作资料选择、保存、对话保存预填和回到创作等组件回归全部保留。`KnowledgePage.test.tsx` 从 `802` 行降到 `798` 行，低于 `800` 行阈值；组件 case 保持 `11` 个。验证：`npx eslint src/features/knowledge/KnowledgePage.test.tsx src/features/knowledge/KnowledgePage.tsx --max-warnings 0` 通过；`npm run test:component -- src/features/knowledge/KnowledgePage.test.tsx` 实测 `11` 个 case、总 `9.72s` 通过；`npm run test:layers:stats` 统计为总 `911`、默认可运行 `910`、live-gated `1`，分层为 unit `413`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `52` 降到 `51`，`KnowledgePage.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `DeveloperSettings` 测试，保持 14 个组件行为场景不变，只删除无复用价值的 `getBodyText` helper 并压缩 `IS_REACT_ACT_ENVIRONMENT` 赋值块；开发页分区、噪音清理、组件 debug、workspace harness、诊断复制、剪贴板权限、Service Skill Catalog、Site Script Catalog 和站点目录变更事件回归全部保留。`src/components/settings-v2/system/developer/index.test.tsx` 从 `804` 行降到 `798` 行，低于 `800` 行阈值。验证：`npx eslint src/components/settings-v2/system/developer/index.test.tsx src/components/settings-v2/system/developer/index.tsx --max-warnings 0` 通过；`npm run test:component -- src/components/settings-v2/system/developer/index.test.tsx` 实测 `14` 个 case、总 `3.97s` 通过；`npm run test:layers:stats` 统计为总 `911`、默认可运行 `910`、live-gated `1`，分层为 unit `413`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `51` 降到 `50`，该文件已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `AppPageContent.test.tsx`，这是页面路由接线测试，不适合为压阈值拆 VM；本刀只做机械去重：新增 `expectTestId` helper 统一页面挂载断言，并压缩重复 `vi.hoisted` props/lifecycle holder 初始化。agent 参数透传、实例重建、channels/settings/skills/memory/knowledge/experts/Agent Apps 路由接线回归全部保留。`AppPageContent.test.tsx` 从 `819` 行降到 `774` 行，低于 `800` 行阈值；组件 case 保持 `19` 个。验证：`npx eslint src/components/AppPageContent.test.tsx src/components/AppPageContent.tsx --max-warnings 0` 通过；`npm run test:component -- src/components/AppPageContent.test.tsx` 实测 `19` 个 case、总 `1.89s` 通过；`npm run test:layers:stats` 统计为总 `911`、默认可运行 `910`、live-gated `1`，分层为 unit `413`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `50` 降到 `49`，`AppPageContent.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且带 `business-logic-keywords` 的 `ApiKeyProviderSection.ui.test.tsx`，新增 `ApiKeyProviderSectionViewModel.ts`，将启用模型选中修正、未登录 Lime Hub 登录态判断和删除 Provider 配置计划下沉到纯 VM；新增 `ApiKeyProviderSectionViewModel.unit.test.ts` 覆盖登录态、保留未登录 Lime Hub 选中、空 / 不可见选中修正和系统 / 自定义 Provider 删除计划。组件测试删除已由 VM 覆盖的 Lime Hub 自动重定向纯决策用例，并把 fixture / DOM 查询 helper 外移到 `ApiKeyProviderSection.uiTestFixtures.ts`；组件层继续保留布局、登录按钮、聊天试跑、添加模型流程、删除配置、分类目录、SenseNova 修正和连接失败后仍激活等真实 DOM / handler 接线回归。`ApiKeyProviderSection.ui.test.tsx` 从 `936` 行降到 `692` 行，`unitMigrationHints` 清零，新增 unit 为 `206` 行；组件 case 从 `14` 个收窄到 `13` 个。验证：`npm run test:unit -- --list --explain src/components/api-key-provider/ApiKeyProviderSectionViewModel.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/api-key-provider/ApiKeyProviderSection.tsx src/components/api-key-provider/ApiKeyProviderSectionViewModel.ts src/components/api-key-provider/ApiKeyProviderSectionViewModel.unit.test.ts src/components/api-key-provider/ApiKeyProviderSection.ui.test.tsx src/components/api-key-provider/ApiKeyProviderSection.uiTestFixtures.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/api-key-provider/ApiKeyProviderSectionViewModel.unit.test.ts` 实测 `5` 个 case、总 `1.79s` 通过；`npm run test:component -- src/components/api-key-provider/ApiKeyProviderSection.ui.test.tsx` 实测 `13` 个 case、总 `4.25s` 通过；`npm run test:layers:stats` 统计为总 `912`、默认可运行 `911`、live-gated `1`，分层为 unit `414`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `49` 降到 `48`，`ApiKeyProviderSection.ui.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续在 clean 的 API Key Provider 区域收敛 `ProviderSetting.ui.test.tsx`，新增 `providerModelFetchHelpers.unit.test.ts` 覆盖模型接口返回 ID 去重、Responses 图片入口 `/models` unsupported、Fal provider / Fal 图片模型识别、Fal API Key 要求和 Fal unsupported 手动模型确认 / 手动添加提示；组件测试删除已由 helper unit 覆盖的 Responses / Fal display-only 获取模型状态用例，保留 API 获取失败不展示旧兜底模型、Fal 无 API Key 时仍可确认手动模型、API 成功建议列表点击加入、长列表筛选、图片 @命令绑定、API Host / API Key 保存、删除配置和连接测试等真实 DOM / handler 接线回归。新增 `ProviderSetting.uiTestFixtures.ts` 外移 provider fixture、翻译 stub、输入修改和建议标签 helper；`ProviderSetting.ui.test.tsx` 从 `1120` 行降到 `792` 行，`unitMigrationHints` 清零，组件 case 从 `24` 个收窄到 `18` 个。验证：`npm run test:unit -- --list --explain src/components/api-key-provider/providerModelFetchHelpers.unit.test.ts` 确认为 `# name:unit`；`npx eslint src/components/api-key-provider/ProviderSetting.ui.test.tsx src/components/api-key-provider/ProviderSetting.uiTestFixtures.ts src/components/api-key-provider/providerModelFetchHelpers.unit.test.ts src/components/api-key-provider/providerModelFetchHelpers.ts src/components/api-key-provider/ProviderSetting.tsx --max-warnings 0` 通过；`npm run test:unit -- src/components/api-key-provider/providerModelFetchHelpers.unit.test.ts` 实测 `4` 个 case、总 `1.72s` 通过；`npm run test:component -- src/components/api-key-provider/ProviderSetting.ui.test.tsx` 实测 `18` 个 case、总 `4.65s` 通过；`npm run test:layers:stats` 统计为总 `913`、默认可运行 `912`、live-gated `1`，分层为 unit `415`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `48` 降到 `47`，`ProviderSetting.ui.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `ModelSelector.test.tsx`，不删 provider/model 回填、后台预加载、失效模型回退、供应商切换、图片模型 fallback、能力标签、en-US chrome、云端 / 本地分组、Lime Hub 登录态、Prompt Cache 提示和无 Provider 引导等真实组件接线回归；本刀只新增 `ModelSelector.testFixtures.ts` 外移模型 metadata fixture、挂载清理和 DOM 点击 helper，收敛重复 `combobox` / body button 查询。`ModelSelector.test.tsx` 从 `988` 行降到 `696` 行，低于 `large-component-file` 阈值；组件 case 保持 `24` 个。验证：`npm run test:component -- --list --explain src/components/input-kit/ModelSelector.test.tsx` 确认为 `# browser-dom`；`npx eslint src/components/input-kit/ModelSelector.test.tsx src/components/input-kit/ModelSelector.testFixtures.ts --max-warnings 0` 通过；`npm run test:component -- src/components/input-kit/ModelSelector.test.tsx` 实测 `24` 个 case、总 `4.81s` 通过；`npm run test:layers:stats` 统计为总 `913`、默认可运行 `912`、live-gated `1`，分层为 unit `415`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `47` 降到 `46`，`ModelSelector.test.tsx` 已从候选列表移除。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `ProjectSelector.ui.test.tsx`，不删默认项目锁定、移除回退、workspace-tab 列表、当前行置顶、打开现有文件夹、创建工作区、延后加载摘要、被动展示延迟请求和默认项目回填等真实组件接线回归；本刀只新增 `ProjectSelector.uiTestFixtures.tsx` 与 `ProjectSelector.i18nTestStub.ts`，外移项目 fixture、hook 返回值、挂载清理、按钮查询、异步 flush 和 i18n 测试字典。`ProjectSelector.ui.test.tsx` 从 `987` 行降到 `780` 行，低于 `large-component-file` 阈值；组件 case 保持 `16` 个。验证：`npm run test:component -- --list --explain src/components/projects/ProjectSelector.ui.test.tsx` 确认为 `# extension:tsx`；`npx eslint src/components/projects/ProjectSelector.ui.test.tsx src/components/projects/ProjectSelector.uiTestFixtures.tsx src/components/projects/ProjectSelector.i18nTestStub.ts --max-warnings 0` 通过；`npm run test:component -- src/components/projects/ProjectSelector.ui.test.tsx` 实测 `16` 个 case、总 `2.71s` 通过；`npm run test:layers:stats` 统计为总 `913`、默认可运行 `912`、live-gated `1`，分层为 unit `415`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `46` 降到 `45`，`ProjectSelector.ui.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `src/components/settings-v2/agent/voice/index.test.tsx`，不删语音输入、语音模型下载 / 进度、缺模型跳转聚焦、默认模型、WAV 转写、开关保存、麦克风设备、默认润色指令和恢复默认等真实设置页接线回归；本刀只新增 `voiceSettingsTestFixtures.tsx`，外移 `VoiceSettings` 挂载清理、flush、deferred promise 和默认 `voice_input` config fixture。`index.test.tsx` 从 `854` 行降到 `773` 行，低于 `large-component-file` 阈值；组件 case 保持 `11` 个。验证：`npx eslint src/components/settings-v2/agent/voice/index.test.tsx src/components/settings-v2/agent/voice/voiceSettingsTestFixtures.tsx --max-warnings 0` 通过；`npm run test:component -- src/components/settings-v2/agent/voice/index.test.tsx` 实测 `11` 个 case、总 `6.14s` 通过；`npm run test:layers:stats` 统计为总 `913`、默认可运行 `912`、live-gated `1`，分层为 unit `415`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `45` 降到 `44`，该文件已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `src/components/settings-v2/system/chrome-relay/index.test.tsx`，不删浏览器列表、高级工具、扩展 / CDP 引导、复制配置、启动 Browser Assist、独立调试器、断开连接、Bridge 页、后端策略、浏览器动作配置和系统连接器切换等真实设置页接线回归；本刀只新增 `chromeRelaySettingsTestFixtures.tsx`，外移 `ChromeRelaySettings` 挂载清理、flush、按钮 / 页签查询和浏览器动作能力 fixture，并删除未被该测试触发的 dialog mock 样板。`index.test.tsx` 内部计数从 `899` 行降到 `792` 行，低于 `large-component-file` 阈值；组件 case 保持 `15` 个。验证：`npx eslint src/components/settings-v2/system/chrome-relay/index.test.tsx src/components/settings-v2/system/chrome-relay/chromeRelaySettingsTestFixtures.tsx --max-warnings 0` 通过；`npm run test:component -- src/components/settings-v2/system/chrome-relay/index.test.tsx` 实测 `15` 个 case、总 `5.54s` 通过；`npm run test:layers:stats` 统计为总 `913`、默认可运行 `912`、live-gated `1`，分层为 unit `415`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `44` 降到 `43`，该文件已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且带 `large-component-file` / `business-logic-keywords` 的 `src/components/settings-v2/agent/providers/index.test.tsx`，不删本地 Provider、云端登录 / 用户中心跳转、taxonomy 下线、本地 Offer 网格下线、桌宠桥接、安装引导和同步摘要等真实设置页接线回归；本刀新增 `cloudProviderSettingsTestFixtures.tsx`，外移云端 access state、offer、session、桌宠状态、API Key Provider fixture、i18n stub、挂载清理、tooltip 和按钮点击 helper。`index.test.tsx` 从 `1166` 行降到 `760` 行，低于 `large-component-file` 阈值；组件 case 保持 `17` 个。验证：`npx eslint src/components/settings-v2/agent/providers/index.test.tsx src/components/settings-v2/agent/providers/cloudProviderSettingsTestFixtures.tsx --max-warnings 0` 通过；`npm run test:component -- src/components/settings-v2/agent/providers/index.test.tsx` 实测 `17` 个 case、总 `6.14s` 通过；`npm run test:layers:stats` 统计为总 `913`、默认可运行 `912`、live-gated `1`，分层为 unit `415`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `43` 降到 `42`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `src/components/settings-v2/system/automation/index.test.tsx`，不删工作台 tips、遗留浏览器下线、Google Sheets 投递、settings / workspace 模式、详情弹窗、历史失败 toast、绑定目标审计 / 证据、概览 tab、模板预填、预选任务、首屏容错、服务型技能、目标摘要、旧目录兼容和旧 SceneApp 下线等真实设置页接线回归；本刀新增 `automationSettingsTestFixtures.tsx`，外移默认 API 响应、Automation job / run / delivery builder、挂载清理、tooltip、点击和场景 mock helper。`index.test.tsx` 从 `1319` 行降到 `754` 行，低于 `large-component-file` 阈值；组件 case 保持 `19` 个。验证：`npx eslint src/components/settings-v2/system/automation/index.test.tsx src/components/settings-v2/system/automation/automationSettingsTestFixtures.tsx --max-warnings 0` 通过；`npm run test:component -- src/components/settings-v2/system/automation/index.test.tsx` 实测 `19` 个 case、总 `5.04s` 通过；`npm run test:layers:stats` 统计为总 `913`、默认可运行 `912`、live-gated `1`，分层为 unit `415`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `42` 降到 `41`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `src/features/browser-runtime/BrowserRuntimeDebugPanel.test.tsx`，不删初始附着会话、英文外壳、启动 loading、高级调试审计、站点命令、附着 Chrome、中英文附着文案、existing_session 切换保护、标签页切换和 read_page race 等真实调试面板接线回归；本刀新增 `browserRuntimeDebugPanelTestFixtures.tsx`，外移默认 runtime state、Chrome bridge status / observer、附着 profile、site adapter fixture、挂载清理、flush、按钮点击和 deferred promise helper。`BrowserRuntimeDebugPanel.test.tsx` 从 `1056` 行降到 `642` 行，低于 `large-component-file` 阈值；组件 case 保持 `10` 个。验证：`npx eslint src/features/browser-runtime/BrowserRuntimeDebugPanel.test.tsx src/features/browser-runtime/browserRuntimeDebugPanelTestFixtures.tsx --max-warnings 0` 通过；`npm run test:component -- src/features/browser-runtime/BrowserRuntimeDebugPanel.test.tsx` 实测 `10` 个 case、总 `4.34s` 通过；`npm run test:layers:stats` 统计为总 `913`、默认可运行 `912`、live-gated `1`，分层为 unit `415`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `41` 降到 `40`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续转向 clean 且仅因 `large-component-file` 入候选的 `src/features/browser-runtime/BrowserSiteAdapterPanel.test.tsx`，不删站点结果保存、已保存内容导航、existing_session 资料优先、推荐适配器切换、目录变更刷新、站点不可达提示、初始脚本自动执行、附着会话门禁、写回当前主稿和导出结果导航等真实站点采集面板接线回归；本刀新增 `browserSiteAdapterPanelTestFixtures.tsx`，外移 site adapter / recommendation / catalog / profile / project / chrome bridge / run result builder、挂载清理、按钮点击、输入修改和 microtask flush helper。`BrowserSiteAdapterPanel.test.tsx` 从 `1053` 行降到 `583` 行，低于 `large-component-file` 阈值；组件 case 保持 `10` 个。验证：`npx eslint src/features/browser-runtime/BrowserSiteAdapterPanel.test.tsx src/features/browser-runtime/browserSiteAdapterPanelTestFixtures.tsx --max-warnings 0` 通过；`npm run test:component -- src/features/browser-runtime/BrowserSiteAdapterPanel.test.tsx` 实测 `10` 个 case、总 `2.16s` 通过；`npm run test:layers:stats` 统计为总 `913`、默认可运行 `912`、live-gated `1`，分层为 unit `415`、component `371`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `40` 降到 `39`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P3 分类事实源治理完成。修正 `scripts/lib/vitest-layer-classifier.mjs` 的 `browser-dom` 判定口径，将 `window` / `document` / `localStorage` 等浏览器全局对象匹配限定到去掉注释和字符串字面量后的源码，避免 i18n key、文案和普通注释中的 `workspace.document.*` / `renderFailed` 之类文本把纯逻辑测试误归为 component；真实 `window.localStorage` 与 `document.createElement` 代码引用仍保持 component。验证：`npm run test:unit -- scripts/lib/vitest-layer-classifier.unit.test.mjs` 实测 `14` 个 case 通过；`npm run test:unit -- --list --explain src/i18n/__tests__/loadNamespace.test.ts src/i18n/__tests__/types.test.ts src/lib/api/skillCatalog.test.ts src/lib/layered-design/analyzer.test.ts src/features/agent-app/runtime/capabilityDispatcher.test.ts` 只列出两个 i18n 文件为 `default:unit`；`npm run test:component -- --list --explain ...` 只列出真实浏览器全局对象引用的 3 个文件；`npx eslint scripts/lib/vitest-layer-classifier.mjs scripts/lib/vitest-layer-classifier.unit.test.mjs --max-warnings 0` 与目标 `git diff --check` 通过。`npm run test:layers:stats` 更新为总 `913`、默认可运行 `912`、live-gated `1`，分层为 unit `451`、component `335`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `39` 降到 `37`；本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理 clean 且带 `large-component-file` / `business-logic-keywords` 的 `src/lib/api/skillCatalog.test.ts`，将 seeded 目录 projection、旧版 site/browser assist 过滤、home-only command 解析、`cloud_scene` 兼容正规化、统一 command/scene 目录入口、Lime Cloud 图片模型命令和远端 scene entry 模板 / 占位 / request defaults 等纯目录断言迁到 `src/lib/api/skillCatalog.unit.test.ts`；新增 `skillCatalogTestFixtures.ts` 共享旧远端目录、Base Setup Package 和 legacy cloud scene fixture。原 component 文件只保留真实 `window.localStorage` / cache / Base Setup 持久化 / 本地图片模型 overlay 接线用例，从 `1041` 行降到 `210` 行，component case 从 `8` 个收窄到 `4` 个，新增 unit `7` 个 case。验证：`npm run test:unit -- --list --explain src/lib/api/skillCatalog.unit.test.ts src/lib/api/skillCatalog.test.ts` 确认为 unit 文件 `# name:unit`、component 文件不进入 unit；`npm run test:component -- --list --explain ...` 确认原 component 文件仍因 `browser-dom` 留在 component；`npx eslint src/lib/api/skillCatalog.test.ts src/lib/api/skillCatalog.unit.test.ts src/lib/api/skillCatalogTestFixtures.ts --max-warnings 0` 通过；`npm run test:unit -- src/lib/api/skillCatalog.unit.test.ts` 实测 `7` 个 case、总 `1.64s` 通过；`npm run test:component -- src/lib/api/skillCatalog.test.ts` 实测 `4` 个 case、总 `1.58s` 通过；`npm run test:layers:stats` 更新为总 `914`、默认可运行 `913`、live-gated `1`，分层为 unit `452`、component `335`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `37` 降到 `36`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理 clean 且仅因 `large-component-file` 入候选的 `src/lib/layered-design/analyzer.test.ts`，将 native OCR provider 映射、structured analyzer result 到 current extraction seam、外部 structured provider 替换 heuristic、native structured analyzer provider 代理 / unsupported、DesignCanvas adapter 包装、禁用 fallback 错误保留、structured result 创建 flat image draft、clean plate 失败输出标记等不依赖 DOM / canvas 的纯 adapter / projection 断言迁到 `src/lib/layered-design/analyzer.unit.test.ts`；原 component 文件只保留需要 `Image`、`document.createElement`、canvas、TextDetector 或 OCR runtime 的默认 heuristic / fallback / OCR 行为。`analyzer.test.ts` 从 `1373` 行降到 `720` 行，component case 从 `15` 个收窄到 `6` 个，新增 unit `9` 个 case。验证：`npm run test:unit -- --list --explain src/lib/layered-design/analyzer.unit.test.ts src/lib/layered-design/analyzer.test.ts` 确认为 unit 文件 `# name:unit`；`npm run test:component -- --list --explain ...` 确认原 component 文件仍因 `browser-dom` 留在 component；`npx eslint src/lib/layered-design/analyzer.test.ts src/lib/layered-design/analyzer.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/lib/layered-design/analyzer.unit.test.ts` 实测 `9` 个 case、总 `2.92s` 通过；`npm run test:component -- src/lib/layered-design/analyzer.test.ts` 实测 `6` 个 case、总 `2.92s` 通过；`npm run test:layers:stats` 更新为总 `915`、默认可运行 `914`、live-gated `1`，分层为 unit `453`、component `335`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `36` 降到 `35`，`analyzer.test.ts` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P3 分类事实源治理继续完成。继续收窄 `scripts/lib/vitest-layer-classifier.mjs` 的 `browser-dom` 判定口径，避免 `const document = createLayeredDesignDocument(...)`、artifact document、extraction document 等领域对象变量名把纯 projection / parser / export 测试误归为 component；真实 `window.localStorage`、`document.createElement`、`matchMedia(...)` 和 `new ResizeObserver(...)` 仍保持 component。`src/lib/layered-design/export.test.ts` 不需要拆文件，已从误判 component 归回 unit，并可由 `npm run test:unit` 直接执行。验证：`npm run test:unit -- scripts/lib/vitest-layer-classifier.unit.test.mjs` 实测 `15` 个 case 通过；`npm run test:unit -- src/lib/layered-design/export.test.ts` 实测 `16` 个 case、总 `9.65s` 通过；`npm run test:unit -- --list --explain src/lib/layered-design/export.test.ts src/lib/layered-design/document.test.ts src/lib/layered-design/extraction.test.ts src/features/agent-app/runtime/capabilityDispatcher.test.ts src/lib/api/skillCatalog.test.ts src/lib/layered-design/analyzer.test.ts` 只列出 `export/document/extraction` 3 个纯领域对象测试为 `default:unit`；对应 `npm run test:component -- --list --explain ...` 只列出 `capabilityDispatcher.test.ts`、`skillCatalog.test.ts`、`analyzer.test.ts` 这 3 个真实浏览器全局对象测试为 `browser-dom`；`npm run test:layers:stats` 更新为总 `915`、默认可运行 `914`、live-gated `1`，分层为 unit `468`、component `320`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `35` 降到 `34`。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理 clean 且带 `large-component-suite` / `large-component-file` 的 `src/features/agent-app/runtime/capabilityDispatcher.test.ts`，将不依赖浏览器全局对象的 Host capability discovery、v0.7 profile、AgentRuntime models / usage / skills / memory / context / tasks projection、ToolRuntime preview、connector handoff、storage、manifest allowlist、artifact / evidence 写回等 `18` 个 dispatcher 行为用例迁到 `src/features/agent-app/runtime/capabilityDispatcher.unit.test.ts`；新增 `capabilityDispatcherTestFixtures.ts` 共享 dispatcher / runtime projection / ToolRuntime handoff fixture。原 component 文件只保留 `4` 个真实 `window.__LIME_OEM_CLOUD__` / `window.__LIME_SESSION_TOKEN__` cloud session 接线用例，从 `3623` 行收窄到 `225` 行；新增 unit 文件 `2647` 行，fixture `763` 行。验证：`npm run test:unit -- --list --explain src/features/agent-app/runtime/capabilityDispatcher.unit.test.ts src/features/agent-app/runtime/capabilityDispatcher.test.ts` 只列出 unit 文件 `# name:unit`；`npm run test:component -- --list --explain ...` 只列出原 component 文件 `# browser-dom`；`npx eslint src/features/agent-app/runtime/capabilityDispatcher.test.ts src/features/agent-app/runtime/capabilityDispatcher.unit.test.ts src/features/agent-app/runtime/capabilityDispatcherTestFixtures.ts --max-warnings 0` 通过；`npm run test:unit -- src/features/agent-app/runtime/capabilityDispatcher.unit.test.ts` 实测 `18` 个 case、总 `2.32s` 通过；`npm run test:component -- src/features/agent-app/runtime/capabilityDispatcher.test.ts` 实测 `4` 个 case、总 `1.88s` 通过；`npm run test:layers:stats` 更新为总 `916`、默认可运行 `915`、live-gated `1`，分层为 unit `469`、component `320`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `34` 降到 `33`；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理 clean 且带 `large-component-suite` / `large-component-file` 的 `src/features/agent-app/ui/AgentAppsPage.test.tsx`，新增 `AgentAppsPageViewModel.ts` 承接 App Center item 投影、图标解析、搜索 / 来源 / 状态筛选、分页、计数、默认入口、更新态和按钮 disabled / label 推导；新增 `AgentAppsPageViewModel.unit.test.ts` 覆盖 `4` 个纯 VM case。原 component 测试删除已由 VM 覆盖的“大批量 Cloud catalog 搜索 / 来源筛选 / 分页”挂载用例，组件 case 从 `20` 降到 `19`，页面组件仍保留本地安装、安装审查、注册码阻断、Cloud 更新、hash 阻断、启停 / 卸载、runtime navigation、standalone shell、路径隐藏和自动打开等真实 DOM / API 接线回归；`AgentAppsPage.tsx` 从 `2288` 行降到 `1819` 行。验证：`npm run test:unit -- --list --explain src/features/agent-app/ui/AgentAppsPageViewModel.unit.test.ts src/features/agent-app/ui/AgentAppsPage.test.tsx` 只列出 VM 单测 `# name:unit`；`npx eslint src/features/agent-app/ui/AgentAppsPage.tsx src/features/agent-app/ui/AgentAppsPage.test.tsx src/features/agent-app/ui/AgentAppsPageViewModel.ts src/features/agent-app/ui/AgentAppsPageViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/features/agent-app/ui/AgentAppsPageViewModel.unit.test.ts` 实测 `4` 个 case、总 `1.58s` 通过；`npm run test:component -- src/features/agent-app/ui/AgentAppsPage.test.tsx` 实测 `19` 个 case、总 `2.42s` 通过；`npm run test:layers:stats` 更新为总 `917`、默认可运行 `916`、live-gated `1`，分层为 unit `470`、component `320`、contract `77`、integration `49`、e2e `1`，`large-component-suite` hint 从 `21` 降到 `20`，component 迁移候选总数仍为 `33`，`AgentAppsPage.test.tsx` 还因 `large-component-file` 留在候选中，下一刀应继续外移 test fixture 或迁移剩余非 DOM 断言；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 追加收口完成。继续收窄 `src/features/agent-app/ui/AgentAppsPage.test.tsx` 的组件测试体积，新增 `AgentAppsPage.testFixtures.tsx` 共享 API mock、React root 挂载、installed state builder、install review 断言和 flush / input helper；新增 `AgentAppsPage.runtime.test.tsx` 承接 runtime launch、standalone shell、路径隐藏、disabled launch gate、workflow entry 和 pageParams 自动打开等 `6` 个真实 runtime 接线用例。原 `AgentAppsPage.test.tsx` 从 `1469` 行降到 `760` 行，runtime suite 为 `231` 行，两个 component suite 均低于 `large-component-file` 阈值；component case 总数保持 `19`，不删回归。验证：`npm run test:unit -- --list --explain src/features/agent-app/ui/AgentAppsPageViewModel.unit.test.ts src/features/agent-app/ui/AgentAppsPage.test.tsx src/features/agent-app/ui/AgentAppsPage.runtime.test.tsx` 只列出 VM 单测 `# name:unit`；`npm run test:component -- --list --explain ...` 只列出两个 component suite `# extension:tsx`；`npx eslint src/features/agent-app/ui/AgentAppsPage.test.tsx src/features/agent-app/ui/AgentAppsPage.runtime.test.tsx src/features/agent-app/ui/AgentAppsPage.testFixtures.tsx src/features/agent-app/ui/AgentAppsPage.tsx src/features/agent-app/ui/AgentAppsPageViewModel.ts src/features/agent-app/ui/AgentAppsPageViewModel.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/features/agent-app/ui/AgentAppsPageViewModel.unit.test.ts` 实测 `4` 个 case、总 `3.10s` 通过；`npm run test:component -- src/features/agent-app/ui/AgentAppsPage.test.tsx src/features/agent-app/ui/AgentAppsPage.runtime.test.tsx` 实测 `19` 个 case、总 `2.46s` 通过；`npm run test:layers:stats` 更新为总 `918`、默认可运行 `917`、live-gated `1`，分层为 unit `470`、component `321`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `33` 降到 `32`，`AgentAppsPage.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `src/features/capability-drafts/components/WorkspaceRegisteredSkillsPanel.test.tsx`，确认该文件 `7` 个用例都是已保存技能 / runtime binding / Managed Job / completion audit / refreshSignal 的真实 React DOM 与 API mock 接线，不适合硬降为 unit；本刀只按行为边界拆小 component suite。原文件保留“无项目根目录”和“已注册能力来源 / runtime gate 展示”两条基础用例，从 `1468` 行降到 `570` 行；新增 `WorkspaceRegisteredSkillsPanel.runtime.test.tsx` 承接 runtime enable 与 Managed Job 草案 / audit 联动 `2` 个用例，`574` 行；新增 `WorkspaceRegisteredSkillsPanel.audit.test.tsx` 承接 completed audit、Prompt-to-Artifact gate 和 refreshSignal `3` 个用例，`600` 行；三个 component suite 均低于 `large-component-file` 阈值，case 总数保持 `7`，不删回归。验证：`npm run test:component -- --list --explain src/features/capability-drafts/components/WorkspaceRegisteredSkillsPanel.test.tsx src/features/capability-drafts/components/WorkspaceRegisteredSkillsPanel.runtime.test.tsx src/features/capability-drafts/components/WorkspaceRegisteredSkillsPanel.audit.test.tsx` 三个文件均为 `# react-dom, react-render, browser-dom`；`npm run test:component -- ...` 实测 `7` 个 case、总 `3.09s` 通过；`npx eslint ... --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `920`、默认可运行 `919`、live-gated `1`，分层为 unit `470`、component `323`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `32` 降到 `31`，`WorkspaceRegisteredSkillsPanel.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。转向 clean 且仅因 `large-component-file` 入候选的 `src/features/resource-manager/ResourceManagerPage.test.tsx`，确认该文件 `16` 个用例都是资源列表、预览切换、系统动作、业务来源菜单、Inspector、搜索 / 筛选、文本加载、预览查找和 Markdown 源码切换等真实 React DOM / browser API / 文件 API mock 接线，不适合硬降为 unit；本刀只按行为边界拆小 component suite。新增 `ResourceManagerPage.testFixtures.tsx` 共享文件系统 / file preview mock、Tauri capability mock、MarkdownRenderer stub、React root 挂载、输入更新和清理 helper；原 `ResourceManagerPage.test.tsx` 保留首屏资源列表和基础类型切换 `2` 个用例，从 `1213` 行降到 `173` 行；新增 `ResourceManagerPage.preview.test.tsx` 承接格式工具栏、Office、归档、系统优先格式、搜索 / 筛选、JSON、文本文件、预览查找和 Markdown 源码 `9` 个用例，`695` 行；新增 `ResourceManagerPage.actions.test.tsx` 承接本地文件动作、项目文件菜单、图片任务回跳、项目文稿原文入口和 Inspector `5` 个用例，`276` 行；三个 component suite 均低于 `large-component-file` 阈值，case 总数保持 `16`，不删回归。验证：`npm run test:component -- --list --explain src/features/resource-manager/ResourceManagerPage.test.tsx src/features/resource-manager/ResourceManagerPage.preview.test.tsx src/features/resource-manager/ResourceManagerPage.actions.test.tsx` 三个文件仍归 component；`npm run test:component -- ...` 实测 `16` 个 case、总 `2.94s` 通过；`npx eslint src/features/resource-manager/ResourceManagerPage.test.tsx src/features/resource-manager/ResourceManagerPage.preview.test.tsx src/features/resource-manager/ResourceManagerPage.actions.test.tsx src/features/resource-manager/ResourceManagerPage.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `922`、默认可运行 `921`、live-gated `1`，分层为 unit `470`、component `325`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `31` 降到 `30`，`ResourceManagerPage.test.tsx` 已从候选列表移除；目标 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理 clean 且带 `large-component-file` / `business-logic-keywords` 的 `src/components/skills/SkillsWorkspacePage.test.tsx`，将 Skills 工作台的搜索归一化、已安装本地 Skill 乐观合并、高亮排序、官方市场 / 本地服务 fallback、商店搜索、精选拆分、内置 / 用户安装切分等纯投影逻辑抽到 `SkillsWorkspacePageViewModel.ts`，新增 `SkillsWorkspacePageViewModel.unit.test.ts` 覆盖 `6` 个 VM case；组件测试只保留真实 React DOM / dialog / menu / Tauri API mock 接线。新增 `SkillsWorkspacePage.testFixtures.tsx` 共享 hoisted mock、React root 挂载、默认 Skill / marketplace / service catalog fixture 和菜单 helper；原 `SkillsWorkspacePage.test.tsx` 从 `1251` 行降到 `274` 行，保留分页、主题变量、详情和文件树预览 `6` 个用例；新增 `SkillsWorkspacePage.localActions.test.tsx` 承接卸载、导出、重命名 / 替换 / 显示文件夹、安装包预览和管理菜单 `6` 个用例；新增 `SkillsWorkspacePage.marketplace.test.tsx` 承接官方安装、详情、已安装使用 / 卸载和官方市场 fallback `5` 个用例；component case 总数保持 `17`，不删回归。验证：`npm run test:unit -- src/components/skills/SkillsWorkspacePageViewModel.unit.test.ts` 实测 `6` 个 case、总 `3.03s` 通过；`npm run test:component -- src/components/skills/SkillsWorkspacePage.test.tsx src/components/skills/SkillsWorkspacePage.localActions.test.tsx src/components/skills/SkillsWorkspacePage.marketplace.test.tsx` 实测 `17` 个 case、总 `4.53s` 通过；`npx eslint src/components/skills/SkillsWorkspacePage.tsx src/components/skills/SkillsWorkspacePageViewModel.ts src/components/skills/SkillsWorkspacePageViewModel.unit.test.ts src/components/skills/SkillsWorkspacePage.test.tsx src/components/skills/SkillsWorkspacePage.localActions.test.tsx src/components/skills/SkillsWorkspacePage.marketplace.test.tsx src/components/skills/SkillsWorkspacePage.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `932`、默认可运行 `931`、live-gated `1`，分层为 unit `473`、component `332`、contract `77`、integration `49`、e2e `1`，component 迁移候选从 `29` 降到 `28`，`SkillsWorkspacePage.test.tsx` 已从候选列表移除；目标写集 `git diff --check` 与当前 staged diff `git diff --cached --check` 均通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理 clean 且仅因 `large-component-file` 入候选的 `src/hooks/useOemCloudAccess.test.tsx`，确认该文件覆盖的是 hook 生命周期、浏览器全局对象、本地会话、OAuth 回调桥、系统浏览器打开和本地存储等真实接线，不适合硬降为 pure unit；本刀只拆出测试 fixture 并修正 Vitest hoisted mock 边界。新增 `src/hooks/useOemCloudAccess.testFixtures.tsx` 共享控制面 API mock、Tauri / 系统浏览器 / desktop auth mock、React root 挂载、deferred promise、opened window 和生命周期清理；原 component 测试从 `878` 行降到 `477` 行，fixture 为 `311` 行，`9` 个 hook 接线用例全部保留。验证：`npm run test:component -- src/hooks/useOemCloudAccess.test.tsx` 实测 `9` 个 case、总 `2.66s` 通过；`npm run test:component -- --list --explain src/hooks/useOemCloudAccess.test.tsx src/hooks/useOemCloudAccess.testFixtures.tsx` 只列出真实测试文件 `# browser-dom`，fixture 不被收集；`npx eslint src/hooks/useOemCloudAccess.test.tsx src/hooks/useOemCloudAccess.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `932`、默认可运行 `931`、live-gated `1`，分层为 unit `473`、component `331`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `27` 降到 `26`，`useOemCloudAccess.test.tsx` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理 clean 且仅因 `large-component-file` 入候选的 `src/features/agent-app/ui/AgentAppRuntimePage.test.tsx`，确认该文件 `18` 个用例都是 iframe runtime、Host Bridge postMessage、capability profile、AgentRuntime task、Host AI 运行面板和错误态的真实 React / browser 接线，不适合硬降为 pure unit；本刀按行为边界拆小 component suite。新增 `AgentAppRuntimePage.testFixtures.tsx` 共享 hoisted API / runtime / project mock、i18n / toast stub、React root 挂载、ready state builder、bridge message dispatch 和生命周期清理；原 `AgentAppRuntimePage.test.tsx` 保留启动、信息图标、升级红点、P14 guard、snapshot / cloud session / storage readiness 和主题 / Agent capability 接线 `8` 个用例，从 `2045` 行降到 `394` 行；新增 `AgentAppRuntimePage.agentRun.test.tsx` 承接 Host AI 运行面板 `3` 个用例，`613` 行；新增 `AgentAppRuntimePage.hostBridge.test.tsx` 承接 capability profile、Agent task、artifact / evidence writeback、origin 过滤和启动失败 `7` 个用例，`707` 行；fixture 为 `398` 行，四个文件均低于 `large-component-file` 阈值，case 总数保持 `18`，不删回归。验证：`npm run test:component -- src/features/agent-app/ui/AgentAppRuntimePage.test.tsx src/features/agent-app/ui/AgentAppRuntimePage.agentRun.test.tsx src/features/agent-app/ui/AgentAppRuntimePage.hostBridge.test.tsx` 实测 `18` 个 case、总 `11.25s` 通过；`npm run test:component -- --list --explain src/features/agent-app/ui/AgentAppRuntimePage.test.tsx src/features/agent-app/ui/AgentAppRuntimePage.agentRun.test.tsx src/features/agent-app/ui/AgentAppRuntimePage.hostBridge.test.tsx src/features/agent-app/ui/AgentAppRuntimePage.testFixtures.tsx` 只列出三个真实 suite，fixture 不被收集；`npx eslint src/features/agent-app/ui/AgentAppRuntimePage.test.tsx src/features/agent-app/ui/AgentAppRuntimePage.agentRun.test.tsx src/features/agent-app/ui/AgentAppRuntimePage.hostBridge.test.tsx src/features/agent-app/ui/AgentAppRuntimePage.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `934`、默认可运行 `933`、live-gated `1`，分层为 unit `473`、component `333`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `26` 降到 `25`，`AgentAppRuntimePage.test.tsx` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理最后一个非 chat 候选 `src/components/workspace/design/DesignCanvas.test.tsx`，确认该文件 `32` 个用例覆盖 DOM / canvas / FileReader / analyzer seam / project export / drag input / image task 等真实接线，不适合硬降为 pure unit；本刀只按行为边界拆小 component suite。新增 `DesignCanvas.testFixtures.tsx` 共享 React root 挂载、project export mock、document builders、button / drop helper 和生命周期清理；原 `DesignCanvas.test.tsx` 保留基础渲染、ZIP 导出和上传接线 `6` 个用例，从 `2727` 行降到 `540` 行；新增 `DesignCanvas.extraction.test.tsx` 承接拆层确认 / 质量元数据 / mask / clean plate / analyzer seam `11` 个用例，`460` 行；新增 `DesignCanvas.project.test.tsx` 承接项目保存 / 远程资产提示 / 最近工程恢复 `3` 个用例，`286` 行；新增 `DesignCanvas.editing.test.tsx` 承接图层编辑 / 拖拽 / 文本更新 `7` 个用例，`291` 行；新增 `DesignCanvas.generation.test.tsx` 承接图片生成任务 `5` 个用例，`235` 行；case 总数保持 `32`，不删回归。验证：`npm run test:component -- src/components/workspace/design/DesignCanvas.test.tsx src/components/workspace/design/DesignCanvas.extraction.test.tsx src/components/workspace/design/DesignCanvas.project.test.tsx src/components/workspace/design/DesignCanvas.editing.test.tsx src/components/workspace/design/DesignCanvas.generation.test.tsx` 实测 `32` 个 case、总 `8.32s` 通过；`npm run test:component -- --list --explain src/components/workspace/design/DesignCanvas.test.tsx src/components/workspace/design/DesignCanvas.extraction.test.tsx src/components/workspace/design/DesignCanvas.project.test.tsx src/components/workspace/design/DesignCanvas.editing.test.tsx src/components/workspace/design/DesignCanvas.generation.test.tsx src/components/workspace/design/DesignCanvas.testFixtures.tsx` 只列出五个真实 suite，fixture 不被收集；`npx eslint src/components/workspace/design/DesignCanvas.test.tsx src/components/workspace/design/DesignCanvas.extraction.test.tsx src/components/workspace/design/DesignCanvas.project.test.tsx src/components/workspace/design/DesignCanvas.editing.test.tsx src/components/workspace/design/DesignCanvas.generation.test.tsx src/components/workspace/design/DesignCanvas.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `938`、默认可运行 `937`、live-gated `1`，分层为 unit `473`、component `337`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `25` 降到 `24`，`DesignCanvas.test.tsx` 已从候选列表移除；`node scripts/report-vitest-layers.mjs --json` 确认剩余 `24` 个候选全部位于 `src/components/agent/chat/**`，本刀不跨并行 chat 写集处理；目标写集 `git diff --check` 通过。本刀不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。开始治理剩余 chat 候选里自身 clean 的 `src/components/agent/chat/utils/imageWorkbenchCommand.test.ts`，将不依赖浏览器存储的内置图片命令 parser、数量 / 比例 / 分镜 / 编辑 / 重绘解析、skill routing 和 slash command 构造等 `20` 个纯逻辑用例迁到 `imageWorkbenchCommand.unit.test.ts`；原 component 文件只保留本地动态图片模型绑定、同名命令覆盖、Lime Cloud catalog 注入和附件路由这 `4` 个真实 `window.localStorage` / Skill Catalog 接线用例。case 总数保持 `24`，不删回归，原 component 文件从 `420` 行降到 `156` 行，新增 unit 文件 `272` 行。验证：`npm run test:unit -- --list --explain src/components/agent/chat/utils/imageWorkbenchCommand.unit.test.ts src/components/agent/chat/utils/imageWorkbenchCommand.test.ts` 只列出 unit 文件 `# name:unit`；`npm run test:component -- --list --explain src/components/agent/chat/utils/imageWorkbenchCommand.unit.test.ts src/components/agent/chat/utils/imageWorkbenchCommand.test.ts` 只列出原 component 文件 `# browser-dom`；`npx eslint src/components/agent/chat/utils/imageWorkbenchCommand.test.ts src/components/agent/chat/utils/imageWorkbenchCommand.unit.test.ts --max-warnings 0` 通过；`npm run test:unit -- src/components/agent/chat/utils/imageWorkbenchCommand.unit.test.ts` 实测 `20` 个 case、总 `1.43s` 通过；`npm run test:component -- src/components/agent/chat/utils/imageWorkbenchCommand.test.ts` 实测 `4` 个 case、总 `1.43s` 通过；`npm run test:layers:stats` 更新为总 `939`、默认可运行 `938`、live-gated `1`，分层为 unit `474`、component `337`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `24` 降到 `23`，`imageWorkbenchCommand.test.ts` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理剩余 chat 候选里自身 clean 的 `src/components/agent/chat/utils/mentionCommandReplayText.test.ts`，将 replay formatter、prefill merge、capability / skill / MCP / shell / file / browser / image 等命令回放文本构造的 `44` 个纯逻辑用例迁到 `mentionCommandReplayText.unit.test.ts`；原 component 文件只保留动态本地图片模型绑定回放这 `1` 个真实 `window.localStorage` / Skill Catalog 接线用例。case 总数保持 `45`，不删回归。验证：`npm run test:unit -- src/components/agent/chat/utils/mentionCommandReplayText.unit.test.ts` 实测 `44` 个 case 通过；`npm run test:component -- src/components/agent/chat/utils/mentionCommandReplayText.test.ts` 实测 `1` 个 case 通过；`npm run test:unit -- --list --explain ...` 确认 unit 文件只进入 `# name:unit`；`npm run test:component -- --list --explain ...` 确认原 component 文件仍因 `browser-dom` 留在 component；`npx eslint src/components/agent/chat/utils/mentionCommandReplayText.test.ts src/components/agent/chat/utils/mentionCommandReplayText.unit.test.ts --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `940`、默认可运行 `939`、live-gated `1`，分层为 unit `475`、component `337`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `23` 降到 `22`，`mentionCommandReplayText.test.ts` 已从候选列表移除。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且仅因 `large-component-file` 入候选的 `src/components/agent/chat/components/TeamWorkbenchSummaryPanel.test.tsx`，确认该文件 `14` 个用例都覆盖 Team 记忆影子、Agent UI projection surface、work board action route、requested fix、历史正文、重指派 selector 和 artifact / evidence 追溯等真实 React DOM / runtime API mock 接线，不适合硬降为 pure unit；本刀只按行为边界拆小 component suite。新增 `TeamWorkbenchSummaryPanel.testFixtures.tsx` 共享 runtime / executionRun mock、React root 挂载、locale reset、projection store 清理和 technical details helper；原 `TeamWorkbenchSummaryPanel.test.tsx` 保留主视图 `3` 个用例，从 `1243` 行降到 `110` 行；新增 `TeamWorkbenchSummaryPanel.projection.test.tsx` 承接 projection / route / remote / handoff `5` 个用例，`511` 行；新增 `TeamWorkbenchSummaryPanel.workBoard.test.tsx` 承接 requested fix / teammate history / reassignment / evidence trace `6` 个用例，`530` 行。case 总数保持 `14`，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/components/TeamWorkbenchSummaryPanel.test.tsx src/components/agent/chat/components/TeamWorkbenchSummaryPanel.projection.test.tsx src/components/agent/chat/components/TeamWorkbenchSummaryPanel.workBoard.test.tsx src/components/agent/chat/components/TeamWorkbenchSummaryPanel.testFixtures.tsx` 只列出三个真实 suite，fixture 不被收集；`npx eslint src/components/agent/chat/components/TeamWorkbenchSummaryPanel.test.tsx src/components/agent/chat/components/TeamWorkbenchSummaryPanel.projection.test.tsx src/components/agent/chat/components/TeamWorkbenchSummaryPanel.workBoard.test.tsx src/components/agent/chat/components/TeamWorkbenchSummaryPanel.testFixtures.tsx --max-warnings 0` 通过；`npm run test:component -- src/components/agent/chat/components/TeamWorkbenchSummaryPanel.test.tsx src/components/agent/chat/components/TeamWorkbenchSummaryPanel.projection.test.tsx src/components/agent/chat/components/TeamWorkbenchSummaryPanel.workBoard.test.tsx` 实测 `14` 个 case、总 `3.94s` 通过；`npm run test:layers:stats` 更新为总 `942`、默认可运行 `941`、live-gated `1`，分层为 unit `475`、component `339`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `22` 降到 `21`，`TeamWorkbenchSummaryPanel.test.tsx` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且因 `large-component-suite` / `large-component-file` 入候选的 `src/components/agent/chat/components/AgentThreadTimeline.test.tsx`，确认该文件 `35` 个用例覆盖 timeline React DOM、mocked ToolCallDisplay / A2UI / MarkdownRenderer 接线、artifact 跳转、站点内容回调、process 聚合、pending / permission / provider error 状态、reasoning 展开与只读 A2UI 回显等组件行为，不适合硬降为 pure unit；本刀只按行为边界拆小 component suite。新增 `AgentThreadTimeline.testFixtures.tsx` 共享 mock、React root 挂载、locale reset、item / turn builder 和 render helper；原 `AgentThreadTimeline.test.tsx` 保留基础 reasoning、artifact、ToolSearch、审批 / 技术项 `12` 个用例，从 `1487` 行降到 `368` 行；新增 `AgentThreadTimeline.process.test.tsx` 承接 process ordering / grouped tools / active status / pending / permission / provider error `10` 个用例，`376` 行；新增 `AgentThreadTimeline.reasoning.test.tsx` 承接 A2UI、runtime summary、reasoning 展开、request_user_input 和 subagent activity `13` 个用例，`394` 行。case 总数保持 `35`，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/components/AgentThreadTimeline.test.tsx src/components/agent/chat/components/AgentThreadTimeline.process.test.tsx src/components/agent/chat/components/AgentThreadTimeline.reasoning.test.tsx src/components/agent/chat/components/AgentThreadTimeline.testFixtures.tsx` 只列出三个真实 suite，fixture 不被收集；`npx eslint src/components/agent/chat/components/AgentThreadTimeline.test.tsx src/components/agent/chat/components/AgentThreadTimeline.process.test.tsx src/components/agent/chat/components/AgentThreadTimeline.reasoning.test.tsx src/components/agent/chat/components/AgentThreadTimeline.testFixtures.tsx --max-warnings 0` 通过；`npm run test:component -- src/components/agent/chat/components/AgentThreadTimeline.test.tsx src/components/agent/chat/components/AgentThreadTimeline.process.test.tsx src/components/agent/chat/components/AgentThreadTimeline.reasoning.test.tsx` 实测 `35` 个 case、总 `2.65s` 通过；`npm run test:layers:stats` 更新为总 `944`、默认可运行 `943`、live-gated `1`，分层为 unit `475`、component `341`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `21` 降到 `20`，`AgentThreadTimeline.test.tsx` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且因 `large-component-suite` / `large-component-file` 入候选的 `src/components/agent/chat/components/ChatSidebar.test.tsx`，确认该文件 `29` 个用例覆盖任务中心侧栏、导航入口、归档点击、任务状态筛选、待处理继续动作、父 / 子线程子任务展示与折叠等真实 React DOM 接线，不适合硬降为 pure unit；本刀只按行为边界拆小 component suite。新增 `ChatSidebar.testFixtures.tsx` 共享 Badge / Dropdown mock、React root 挂载、locale reset、默认 topics 和 pending action message builder；原 `ChatSidebar.test.tsx` 保留基础侧栏 / 任务中心导航 `9` 个用例，从 `1232` 行降到 `273` 行；新增 `ChatSidebar.tasks.test.tsx` 承接任务切换 / 删除 / 运行中 / 待处理状态 `10` 个用例，`262` 行；新增 `ChatSidebar.subtasks.test.tsx` 承接父线程 / 子线程子任务列表、折叠、展开和图片任务标题 `10` 个用例，`563` 行。case 总数保持 `29`，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/components/ChatSidebar.test.tsx src/components/agent/chat/components/ChatSidebar.tasks.test.tsx src/components/agent/chat/components/ChatSidebar.subtasks.test.tsx src/components/agent/chat/components/ChatSidebar.testFixtures.tsx` 只列出三个真实 suite，fixture 不被收集；`npx eslint src/components/agent/chat/components/ChatSidebar.test.tsx src/components/agent/chat/components/ChatSidebar.tasks.test.tsx src/components/agent/chat/components/ChatSidebar.subtasks.test.tsx src/components/agent/chat/components/ChatSidebar.testFixtures.tsx --max-warnings 0` 通过；`npm run test:component -- src/components/agent/chat/components/ChatSidebar.test.tsx src/components/agent/chat/components/ChatSidebar.tasks.test.tsx src/components/agent/chat/components/ChatSidebar.subtasks.test.tsx` 实测 `29` 个 case、总 `3.51s` 通过；`npm run test:layers:stats` 更新为总 `946`、默认可运行 `945`、live-gated `1`，分层为 unit `475`、component `343`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `20` 降到 `19`，`ChatSidebar.test.tsx` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且仅因 `large-component-file` 入候选的 `src/components/agent/chat/components/ToolCallDisplay.test.tsx`，确认该文件 `26` 个用例主要覆盖 WebSearch 悬浮预览、命令结果折叠、diff 审阅、Skill 内容面板、站点保存内容回调、图片预览、ToolSearch 摘要、工具动作句和 write_file 产物路径等真实 React DOM / browser 接线，不适合硬降为 pure unit；本刀只按行为边界拆小 component suite。新增 `ToolCallDisplay.testFixtures.tsx` 共享 Tauri shell mock、React root 挂载、locale reset 和生命周期清理；原 `ToolCallDisplay.test.tsx` 保留 WebSearch `3` 个用例，从 `1452` 行降到 `163` 行；新增 `ToolCallDisplay.commandOutput.test.tsx` 承接命令 / diff / metadata / headline `11` 个用例，`538` 行；新增 `ToolCallDisplay.siteMedia.test.tsx` 承接 Skill、站点保存、图片预览和站点失败 `7` 个用例，`365` 行；新增 `ToolCallDisplay.toolSearchActions.test.tsx` 承接 ToolSearch、工具动作句和 write_file `5` 个用例，`285` 行；fixture 为 `66` 行。case 总数保持 `26`，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/components/ToolCallDisplay.test.tsx src/components/agent/chat/components/ToolCallDisplay.commandOutput.test.tsx src/components/agent/chat/components/ToolCallDisplay.siteMedia.test.tsx src/components/agent/chat/components/ToolCallDisplay.toolSearchActions.test.tsx src/components/agent/chat/components/ToolCallDisplay.testFixtures.tsx` 只列出四个真实 suite，fixture 不被收集；`npm run test:component -- src/components/agent/chat/components/ToolCallDisplay.test.tsx src/components/agent/chat/components/ToolCallDisplay.commandOutput.test.tsx src/components/agent/chat/components/ToolCallDisplay.siteMedia.test.tsx src/components/agent/chat/components/ToolCallDisplay.toolSearchActions.test.tsx` 实测 `26` 个 case、总 `9.49s` 通过；`npx eslint src/components/agent/chat/components/ToolCallDisplay.test.tsx src/components/agent/chat/components/ToolCallDisplay.commandOutput.test.tsx src/components/agent/chat/components/ToolCallDisplay.siteMedia.test.tsx src/components/agent/chat/components/ToolCallDisplay.toolSearchActions.test.tsx src/components/agent/chat/components/ToolCallDisplay.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `949`、默认可运行 `948`、live-gated `1`，分层为 unit `475`、component `346`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `19` 降到 `18`，`ToolCallDisplay.test.tsx` 已从候选列表移除。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且因 `large-component-suite` / `large-component-file` 入候选的 `src/components/agent/chat/components/TeamWorkspaceBoard.test.tsx`，确认该文件 `34` 个用例覆盖 team shell、主 / 子会话视角、embedded shell、live runtime props、runtime stream activity、自由画布拖拽 / 快捷键 / resize / 持久化、停止 / 恢复 / 等待 / SendMessage、Team wait / control 摘要和 runtime formation 等真实 React DOM、localStorage、鼠标 / 键盘事件接线，不适合硬降为 pure unit；本刀只按行为边界拆小 component suite。新增 `TeamWorkspaceBoard.testFixtures.tsx` 共享 agent runtime session mock、Button mock、React root 挂载、locale reset、localStorage 清理、session detail builder、effect flush、鼠标 / 键盘 helper 和 lane / viewport metric helper；原 `TeamWorkspaceBoard.test.tsx` 保留 shell / overview / embedded `8` 个用例，从 `2196` 行降到 `697` 行；新增 `TeamWorkspaceBoard.runtime.test.tsx` 承接 live runtime、stream activity、sticky header 和 runtime formation `6` 个用例，`273` 行；新增 `TeamWorkspaceBoard.canvas.test.tsx` 承接自由画布交互 `10` 个用例，`499` 行；新增 `TeamWorkspaceBoard.controls.test.tsx` 承接控制动作和 team 轨迹 `10` 个用例，`537` 行；fixture 为 `233` 行。case 总数保持 `34`，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/components/TeamWorkspaceBoard.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.runtime.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.canvas.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.controls.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.testFixtures.tsx` 只列出四个真实 suite，fixture 不被收集；`npm run test:component -- src/components/agent/chat/components/TeamWorkspaceBoard.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.runtime.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.canvas.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.controls.test.tsx` 实测 `34` 个 case、总 `6.30s` 通过；`npx eslint src/components/agent/chat/components/TeamWorkspaceBoard.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.runtime.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.canvas.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.controls.test.tsx src/components/agent/chat/components/TeamWorkspaceBoard.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `952`、默认可运行 `951`、live-gated `1`，分层为 unit `475`、component `349`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `18` 降到 `17`，`TeamWorkspaceBoard.test.tsx` 已从候选列表移除。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且仅因 `large-component-file` 入候选的 `src/components/agent/chat/components/CanvasWorkbenchLayout.test.tsx`，确认该文件 `17` 个用例覆盖画布标签壳、文件树聚焦、文稿 inspector、sessionView 回退 / 主稿切换、workspaceView 运行时文案、二进制文件、结果目录刷新、coding 模式、teamView 和 ResizeObserver 响应式状态等真实 React DOM、fileBrowser mock、clipboard / URL / ResizeObserver 接线，不适合硬降为 pure unit；本刀只按行为边界拆小 component suite。新增 `CanvasWorkbenchLayout.testFixtures.tsx` 共享 i18n / fileBrowser / sonner mock、React root 挂载、ResizeObserver stub、文件 / 文稿 builder、flush / click / resize helper 和生命周期清理；原 `CanvasWorkbenchLayout.test.tsx` 保留基础 session / document / responsive `7` 个用例，从 `2084` 行降到 `565` 行；新增 `CanvasWorkbenchLayout.workspace.test.tsx` 承接 workspace tabs `5` 个用例，`363` 行；新增 `CanvasWorkbenchLayout.coding.test.tsx` 承接 coding mode `2` 个用例，`274` 行；新增 `CanvasWorkbenchLayout.team.test.tsx` 承接 team view `3` 个用例，`231` 行；fixture 为 `706` 行且不进入测试收集。case 总数保持 `17`，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/components/CanvasWorkbenchLayout.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.workspace.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.coding.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.team.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.testFixtures.tsx` 只列出四个真实 suite，fixture 不被收集；`npm run test:component -- src/components/agent/chat/components/CanvasWorkbenchLayout.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.workspace.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.coding.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.team.test.tsx` 实测 `17` 个 case、总 `7.78s` 通过；`npx eslint src/components/agent/chat/components/CanvasWorkbenchLayout.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.workspace.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.coding.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.team.test.tsx src/components/agent/chat/components/CanvasWorkbenchLayout.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `955`、默认可运行 `954`、live-gated `1`，分层为 unit `475`、component `352`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `17` 降到 `16`，`CanvasWorkbenchLayout.test.tsx` 已从候选列表移除。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且仅因 `large-component-file` 入候选的 `src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.test.tsx`，确认该文件 `12` 个用例覆盖 React hook 生命周期、Agent 图片 skill launch、图片任务事件总线、toast、本地化、文稿插图 apply target 和 task file 重试 / 取消接线，不适合硬降为 pure unit；本刀只按行为边界拆小 component suite。新增 `useWorkspaceImageWorkbenchActionRuntime.testFixtures.tsx` 共享 sonner / agentRuntime title mock、React root 挂载、默认 hook props、locale reset、root cleanup 和 `renderHook` helper；原 `useWorkspaceImageWorkbenchActionRuntime.test.tsx` 保留 launch / skill request `4` 个用例，从 `907` 行降到 `207` 行；新增 `useWorkspaceImageWorkbenchActionRuntime.taskActions.test.tsx` 承接停止、聊天区 retry / cancel、草稿失败重试和跨根目录任务 `4` 个用例，`393` 行；新增 `useWorkspaceImageWorkbenchActionRuntime.documentApply.test.tsx` 承接文稿插图、应用结果、follow-up 回填和英文提示 `4` 个用例，`136` 行；fixture 为 `208` 行且不进入测试收集。case 总数保持 `12`，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.test.tsx src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.taskActions.test.tsx src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.documentApply.test.tsx src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.testFixtures.tsx` 只列出三个真实 suite，fixture 不被收集；`npm run test:component -- src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.test.tsx src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.taskActions.test.tsx src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.documentApply.test.tsx` 实测 `12` 个 case、总 `3.49s` 通过；`npx eslint src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.test.tsx src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.taskActions.test.tsx src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.documentApply.test.tsx src/components/agent/chat/workspace/useWorkspaceImageWorkbenchActionRuntime.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `957`、默认可运行 `956`、live-gated `1`，分层为 unit `475`、component `354`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `16` 降到 `15`，`useWorkspaceImageWorkbenchActionRuntime.test.tsx` 已从候选列表移除。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且仅因 `large-component-file` 入候选的 `src/components/agent/chat/workspace/ArtifactWorkbenchShell.test.tsx`，确认该文件 `16` 个用例覆盖 Artifact shell / workbench 的真实 React DOM、mock editor、`ArtifactWorkbenchDocumentController`、timeline 跳转、编辑事件、AI rewrite 按钮和轻量 renderer 注册接线，不适合硬降为 pure unit；本刀只按行为边界拆小 component suite。新增 `ArtifactWorkbenchShell.testFixtures.tsx` 共享 workbench canvas editor mock、React root 挂载、Artifact / structured document / timeline fixture、render helper 和 text control helper；原 `ArtifactWorkbenchShell.test.tsx` 保留概览、恢复稿、inspector 和来源 / diff 跳转 `6` 个用例，从 `1931` 行降到 `281` 行；新增 `ArtifactWorkbenchShell.editing.test.tsx` 承接保存回调、归档禁编辑、结构化摘要、转写 metadata 和 timeline jump `5` 个用例，`392` 行；新增 `ArtifactWorkbenchShell.rewrite.test.tsx` 承接 AI rewrite 触发与保存建议 `2` 个用例，`245` 行；新增 `ArtifactWorkbenchShell.structuredBlocks.test.tsx` 承接 callout、key points / table、checklist / metric / quote / code block `3` 个用例，`447` 行；fixture 为 `605` 行且不进入测试收集。case 总数保持 `16`，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/workspace/ArtifactWorkbenchShell.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.editing.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.rewrite.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.structuredBlocks.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.testFixtures.tsx` 只列出四个真实 suite，fixture 不被收集；`npm run test:component -- src/components/agent/chat/workspace/ArtifactWorkbenchShell.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.editing.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.rewrite.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.structuredBlocks.test.tsx` 实测 `16` 个 case、总 `7.69s` 通过；`npx eslint src/components/agent/chat/workspace/ArtifactWorkbenchShell.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.editing.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.rewrite.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.structuredBlocks.test.tsx src/components/agent/chat/workspace/ArtifactWorkbenchShell.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `960`、默认可运行 `959`、live-gated `1`，分层为 unit `475`、component `357`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `15` 降到 `14`，`ArtifactWorkbenchShell.test.tsx` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且带 `large-component-suite` / `large-component-file` 的 `src/components/agent/chat/hooks/agentStreamRuntimeHandler.test.ts`，确认该文件只有 `Skill launch` 缓存快照用例需要浏览器 `window.localStorage/sessionStorage` 与 session scoped storage 接线，其余 `19` 个用例都是 `handleTurnStreamEvent` 的事件状态机、projection envelope、tool delta、usage、message snapshot、text overlay flush、reasoning / thinking、empty final、permission wait 和 queued draft cleanup 纯逻辑断言；本刀将纯逻辑迁到 `src/components/agent/chat/hooks/agentStreamRuntimeHandler.unit.test.ts`，原 component 文件只保留 storage 接线回归。原 `agentStreamRuntimeHandler.test.ts` 从 `1989` 行降到 `144` 行、保留 `1` 个 component case；新增 `agentStreamRuntimeHandler.unit.test.ts` 为 `1878` 行、承接 `19` 个 unit case；case 总数保持 `20`，不删回归。验证：`npm run test:unit -- --list --explain src/components/agent/chat/hooks/agentStreamRuntimeHandler.unit.test.ts src/components/agent/chat/hooks/agentStreamRuntimeHandler.test.ts` 只列出 unit 文件 `# name:unit`；`npm run test:component -- --list --explain src/components/agent/chat/hooks/agentStreamRuntimeHandler.unit.test.ts src/components/agent/chat/hooks/agentStreamRuntimeHandler.test.ts` 只列出原 component 文件 `# browser-dom`；`npm run test:unit -- src/components/agent/chat/hooks/agentStreamRuntimeHandler.unit.test.ts` 实测 `19` 个 case、总 `5.17s` 通过；`npm run test:component -- src/components/agent/chat/hooks/agentStreamRuntimeHandler.test.ts` 实测 `1` 个 case、总 `5.06s` 通过；`npx eslint src/components/agent/chat/hooks/agentStreamRuntimeHandler.test.ts src/components/agent/chat/hooks/agentStreamRuntimeHandler.unit.test.ts --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `961`、默认可运行 `960`、live-gated `1`，分层为 unit `476`、component `357`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `14` 降到 `13`，`agentStreamRuntimeHandler.test.ts` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身 clean 且带 `large-component-suite` / `large-component-file` / `business-logic-keywords` 的 `src/components/agent/chat/skill-selection/CharacterMention.test.tsx`，确认大部分回归覆盖 textarea、Popover / dialog、真实按钮点击、recent usage 刷新和 slash / mention 面板 DOM 接线，不适合硬降为 pure unit；本刀只将可纯化的 `filterMentionableServiceSkills` 别名过滤断言迁到 `src/components/agent/chat/skill-selection/CharacterMention.catalog.unit.test.ts`，其余按 mention basics、service skill catalog、内建 `@` 命令、slash templates、slash recent 和 slash actions 拆小 component suite。原 `CharacterMention.test.tsx` 从 `2811` 行降到 `358` 行、保留 `10` 个 component case；新增 `CharacterMention.catalog.test.tsx` 为 `310` 行、承接 `8` 个 service skill catalog case；新增 `CharacterMention.catalogBuiltins.test.tsx` 为 `150` 行、承接 `14` 个内建 `@` 命令展示 case；新增 `CharacterMention.slashTemplates.test.tsx` 为 `534` 行、承接 `13` 个 slash template case；新增 `CharacterMention.slashRecent.test.tsx` 为 `311` 行、承接 `7` 个 slash recent case；新增 `CharacterMention.slashActions.test.tsx` 为 `346` 行、承接 `9` 个 slash selection / action case；新增 `CharacterMention.testFixtures.tsx` 为 `677` 行，共享 mock、React root 挂载、输入 helper、按钮查询和 fixture builders。case 总数保持 `62`，其中 component `61` 个、unit `1` 个，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/skill-selection/CharacterMention.test.tsx src/components/agent/chat/skill-selection/CharacterMention.catalog.test.tsx src/components/agent/chat/skill-selection/CharacterMention.catalogBuiltins.test.tsx src/components/agent/chat/skill-selection/CharacterMention.slashTemplates.test.tsx src/components/agent/chat/skill-selection/CharacterMention.slashRecent.test.tsx src/components/agent/chat/skill-selection/CharacterMention.slashActions.test.tsx` 只列出 6 个 component suite `# browser-dom`；`npm run test:unit -- --list --explain src/components/agent/chat/skill-selection/CharacterMention.catalog.unit.test.ts` 只列出 unit 文件 `# name:unit`；`npm run test:component -- ...CharacterMention...` 实测 `61` 个 case、总 `12.01s` 通过；`npm run test:unit -- src/components/agent/chat/skill-selection/CharacterMention.catalog.unit.test.ts` 实测 `1` 个 case、总 `5.35s` 通过；`npx eslint src/components/agent/chat/skill-selection/CharacterMention.test.tsx src/components/agent/chat/skill-selection/CharacterMention.catalog.test.tsx src/components/agent/chat/skill-selection/CharacterMention.catalogBuiltins.test.tsx src/components/agent/chat/skill-selection/CharacterMention.catalog.unit.test.ts src/components/agent/chat/skill-selection/CharacterMention.slashTemplates.test.tsx src/components/agent/chat/skill-selection/CharacterMention.slashRecent.test.tsx src/components/agent/chat/skill-selection/CharacterMention.slashActions.test.tsx src/components/agent/chat/skill-selection/CharacterMention.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `967`、默认可运行 `966`、live-gated `1`，分层为 unit `477`、component `362`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `13` 降到 `12`，`CharacterMention.test.tsx` 已从候选列表移除。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理自身测试写集中的 `src/components/agent/chat/components/AgentThreadReliabilityPanel.test.tsx`，确认剩余回归覆盖真实 React DOM、clipboard、localStorage、file checkpoint dialog、memory prefetch、按钮点击和 projection store 接线，不适合为降低统计而硬降 pure unit；本刀只按行为边界拆小 component suite，不碰隔壁正在修改的 `AgentThreadReliabilityPanel.tsx` / VM 生产写集。原 `AgentThreadReliabilityPanel.test.tsx` 从 `2106` 行降到 `440` 行，保留 routing / projection 基础 `7` 个用例；新增 `AgentThreadReliabilityPanel.fileCheckpoints.test.tsx` 为 `307` 行、承接 file checkpoint dialog `3` 个用例；新增 `AgentThreadReliabilityPanel.actions.test.tsx` 为 `267` 行、承接复制诊断 / follow-up / review / request telemetry / file checkpoint action `5` 个用例；新增 `AgentThreadReliabilityPanel.memory.test.tsx` 为 `751` 行、承接 memory insight / clipboard / prefetch / localStorage / projection store `6` 个用例；新增 `AgentThreadReliabilityPanel.testFixtures.tsx` 为 `387` 行，共享 API mock、React root 挂载、clipboard/localStorage/reset helper、默认 props 和断言工具。case 总数保持 `21`，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/components/AgentThreadReliabilityPanel.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.fileCheckpoints.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.actions.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.memory.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.testFixtures.tsx` 只列出四个真实 suite，fixture 不被收集；`npm run test:component -- src/components/agent/chat/components/AgentThreadReliabilityPanel.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.fileCheckpoints.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.actions.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.memory.test.tsx` 实测 `21` 个 case、总 `5.29s` 通过；`npx eslint src/components/agent/chat/components/AgentThreadReliabilityPanel.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.fileCheckpoints.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.actions.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.memory.test.tsx src/components/agent/chat/components/AgentThreadReliabilityPanel.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `970`、默认可运行 `969`、live-gated `1`，分层为 unit `477`、component `365`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `12` 降到 `11`，`AgentThreadReliabilityPanel.test.tsx` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
- 2026-06-02：P2 新一刀完成。继续治理测试文件本身 clean、且仅因 `large-component-suite` / `large-component-file` 入候选的 `src/components/agent/chat/components/EmptyStateComposerPanel.test.tsx`；生产侧 `EmptyStateComposerPanel.tsx` / VM 已有并行改动，本刀不触碰生产写集，只把真实 React DOM、textarea、Tab 起手建议、知识资料、路径 chip、Team selector、文件管理器、图片预览和高级设置等挂载回归按行为边界拆小 component suite，不为降低统计硬降 pure unit。新增 `EmptyStateComposerPanel.testFixtures.tsx` 为 `314` 行，共享组件 mock、i18n copy、React root 挂载、skill selection fixture、service skill builder、Team fixture、textarea / advanced toggle helper 和生命周期清理；原 `EmptyStateComposerPanel.test.tsx` 从 `1219` 行降到 `149` 行，保留 chrome / 高级入口基础 `5` 个用例；新增 `EmptyStateComposerPanel.references.test.tsx` 为 `286` 行，承接知识资料、路径引用、creation replay、curated task badge 和站点 service skill `9` 个用例；新增 `EmptyStateComposerPanel.inputFlow.test.tsx` 为 `211` 行，承接 Tab suggestion、guide help、paste、send、loading 和图片预览 `7` 个用例；新增 `EmptyStateComposerPanel.teamSettings.test.tsx` 为 `298` 行，承接 Team suggestion / selector、单代理回退、稳妥模式、模型只读摘要和权限模式 `12` 个用例。case 总数保持 `33`，不删回归。验证：`npm run test:component -- --list --explain src/components/agent/chat/components/EmptyStateComposerPanel.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.references.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.inputFlow.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.teamSettings.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.testFixtures.tsx` 只列出四个真实 suite，fixture 不被收集；`npm run test:component -- src/components/agent/chat/components/EmptyStateComposerPanel.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.references.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.inputFlow.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.teamSettings.test.tsx` 实测 `33` 个 case、总 `6.03s` 通过；`npx eslint src/components/agent/chat/components/EmptyStateComposerPanel.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.references.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.inputFlow.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.teamSettings.test.tsx src/components/agent/chat/components/EmptyStateComposerPanel.testFixtures.tsx --max-warnings 0` 通过；`npm run test:layers:stats` 更新为总 `973`、默认可运行 `972`、live-gated `1`，分层为 unit `477`、component `368`、contract `78`、integration `49`、e2e `1`，component 迁移候选从 `11` 降到 `10`，`EmptyStateComposerPanel.test.tsx` 已从候选列表移除；目标写集 `git diff --check` 通过。本刀只改测试分层，不改 GUI 壳、DevBridge、Workspace、Tauri 命令、mock 或后端协议，未执行 `verify:gui-smoke`。
