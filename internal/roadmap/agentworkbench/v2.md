# Agent Workbench v2 可执行协议内核

> 状态：active
> 创建时间：2026-06-12
> 更新时间：2026-06-12
> 范围：`@limecloud/agent-ui-contracts`、`@limecloud/agent-runtime-projection`、`@limecloud/agent-runtime-client`、`lime-agent-workbench` 文档站。

## 结论

v1 已经把 Lime AgentUI 标准从概念页推进到四包 seed、projection、React surface、Content Studio 接入和 Agent App smoke。v2 的目标不是继续补概念，而是补上 AG-UI 真正有价值的工程内核：**事件先经过可执行状态机验证，再进入投影和 UI**。

上一轮分析（Claude Code）方向基本正确，但停留在「清单式对比」，遗漏了两边架构形态的本质差异，也没有实施任何校验内核。v2 在重新精读两边核心管道后重写，以 Lime current 事实源为准，只吸收 AG-UI 的机制，不照搬 AG-UI 的事件名、Observable API 或 legacy compatibility 层。

## 重新精读后的本质差距

### 差距一（最大、上一轮漏掉）：Lime 曾是「全量重投影」，AG-UI 是「流式状态机 + 增量 apply」

v2.1 前，Lime 的 `createAgentUiProjector.apply()`（`packages/agent-runtime-projection/src/uiState.ts`）每来一个事件，就把历史全部事件重新跑一遍 `projectAgentUiState`（重建 messages / timeline / graph / runtimeStatus）。这是 O(N²)，且**没有任何流式合法性约束**——坏流照样能投影出「看起来正常」的 state。v2.1 已把 `apply()` 改为增量 projector，`hydrate()` / `projectAgentUiState()` 仍保留全量路径作为批量基准。

AG-UI 的 `runAgent`（`sdks/typescript/packages/client/src/agent/agent.ts`）是一条真正的流水线：`run -> transformChunks -> verifyEvents -> apply -> subscribers`。其中 `verifyEvents`（`verify/verify.ts`）是一个**有状态闭包状态机**，事件流过时实时维护 `activeToolCalls` / `activeMessages`，非法序列**当场短路**，坏流根本到不了 apply。

### 差距二：Lime 只有「逐事件 schema 校验」，没有「跨事件状态机」

Lime 的 `validation.ts` 全部是逐事件字段校验（某字段是否 string、是否带 scope id、payload 是否过大 / 含密钥）。唯一的跨事件检查是 `collectSequenceIssues`（sequence 是否连续）。文档 `contracts/runtime-event.md` 里写的配对规则（`tool.started -> tool.result/failed`、`action.required -> action.resolved`、`turn.completed` 前收口）**在代码里一行都不存在**，只能靠运行时提供方自觉遵守，违规时只能靠 projection 测试事后兜底。

### 不该当成「缺失」的部分（修正上一轮判断）

- `snapshot.updated` + hydration / repair cursor 是 Lime **比 AG-UI 更完整的恢复模型**，不是「缺少 JSON Patch」。字段级 patch 在 v2.2 先落 schema seed，v2.5 已进入 projection / read model apply 链。
- conformance 等级、fixture 矩阵、event 家族 taxonomy 比 AG-UI 文档更细，是 Lime 的资产，v2 要强化而不是另起炉灶。

## v2 主目标

把 Workbench 从「标准文档 + fixture replay」推进到「协议边界能自己拦截坏流」的工程标准：

```text
App Server / RuntimeCore / ExecutionBackend
  -> RuntimeEvent stream
  -> sequence verifier   <-- v2.0 新增的可执行内核
  -> optional adapter / middleware
  -> projection apply
  -> AgentUI read model / React surface
```

v2 完成后，运行时提供方不能只靠文档自觉遵守配对 / 收口 / 终态规则；这些规则必须可以在 contracts / conformance runner / runtime client 边界**机械执行**。

## AG-UI 深层参考的取舍

| AG-UI 机制 | Lime v2 取舍 | 原因 |
| --- | --- | --- |
| `verifyEvents` 流式状态机 | **吸收**，落到 `@limecloud/agent-ui-contracts` 的 sequence verifier。 | 这是协议能否自证的核心，也是本轮唯一的实施重点。 |
| `transformChunks` | **v2.4 吸收为 runtime event adapter / middleware pipeline**，不照搬 AG-UI Observable。 | Lime 保持现有 JSON-RPC / session gateway surface，只把兼容转换集中到 verifier 前。 |
| `apply` + subscribers Observable 管道 | **吸收核心机制**：runtime client 分发前先过 verifier，projector `apply()` 走增量 accumulator。 | 不照搬 AG-UI Observable API，但保留「先验证、再增量 apply、再通知 UI」的工程形态。 |
| Middleware / backward compatibility | **v2.4 已落最小接口**：adapter / middleware / schemaVersion compatibility middleware。 | 兼容逻辑集中在 runtime client pipeline，不再散落到 projector 或产品 gateway。 |
| JSON Patch `STATE_DELTA` | **v2.2 已落 schema seed，v2.5 已落 projection apply**。 | Lime 现有 `snapshot.updated` / hydration / repair cursor 已成主链，字段级 patch 先以 RFC 6902 语义进入 contracts / JSON Schema，再由 projection/read model apply 消费。 |
| Dojo 活体示例 | v2.3 文档站闭环。 | 活体 demo 必须跑 Lime fixture / App Server current fixture，不调用正式模型。 |

## v2.0 实施范围

本轮只实施最小可执行内核：

1. 在 `@limecloud/agent-ui-contracts` 新增 `sequenceVerifier.ts`，导出 sequence verifier。
2. verifier 双模式：
   - 批量 `verifyRuntimeEventSequence(events)`，供 fixture / conformance runner。
   - 增量 `createRuntimeSequenceVerifier()` 的 `push(event)` / `finalize()` / `getViolations()`，供未来 runtime client 在分发 listener 前逐事件流式拦截。
3. verifier 至少覆盖以下 violation：
   - `duplicate_event_id`：`id` 去重。
   - `tool_result_without_start` / `tool_failed_without_start`：tool 收口事件无对应 `tool.started`。
   - `tool_started_already_active`：同 `toolCallId` 重复开启。
   - `tool_unclosed_at_turn_end`：turn 终态时仍有未收口的 tool。
   - `action_resolved_without_request`：`action.resolved` 无对应 `action.required`。
   - `action_required_already_active`：同 `actionId` 重复开启。
   - `action_unresolved_at_turn_end`：turn 终态时仍有未解决的 action。
   - `model_unclosed_at_turn_end`：turn 终态时 model 流未收口。
   - `execution_after_turn_terminal`：turn 终态后仍出现同 turn 的 `tool.*` / `action.*` / `model.*` 等执行流事件。
   - `turn_terminal_repeated`：同 turn 重复进入终态。
4. `@limecloud/agent-runtime-projection` 的 `replayAgentUiFixture` 先通过 verifier；有 violation 时 **fail closed**，不把坏流继续投影成 state。
5. contracts / projection 包补定向测试，证明坏流被拦截、好流不被误伤。

### 兼容约束（实施时必须遵守）

verifier 检查的是「违反规则」，**不是「不完整」**。以下情形必须保持合法，否则会误伤现有 fixture：

- 未达 turn 终态的进行中流（例如 `tool-failure`、`hitl-action`、`artifact-evidence`、`subagent-handoff` 这些没有 `turn.completed` 的 fixture），其未闭合 tool / action **不算 violation**，因为流可能仍在进行或只截取片段。
- 收口检查只在 turn 终态事件（`turn.completed` / `turn.failed`）真实出现时触发。
- `snapshot.updated` / `session.*` / `state.*` 等帧外维护事件不算执行流，可在终态后出现，不触发 `execution_after_turn_terminal`。
- `stream-repair` 的 sequence gap 由现有 `collectSequenceIssues` 负责，verifier 不重复判定连续性。

## 非目标

- 本轮不新增 Electron IPC、App Server JSON-RPC 方法或生产 bridge。
- 本轮不把 AG-UI Observable / AbstractAgent API 搬进 Lime。
- v2.0 不把 projector 重写成增量 apply；该债已在 v2.1 完成。
- 本轮不新增 React UI surface，不改 Content Studio 或 Agent App 产品页面。
- v2.0 不实现 JSON Patch 字段级 delta；该 seed 已在 v2.2 完成。
- 本轮不做 npm registry 发布。

## v2.1 实施范围

1. Runtime client pipeline：`subscribeEvents` / `nextEvent` 在分发 listener 前可选接入 verifier，默认 fail closed，测试夹具可显式 collect diagnostics。
2. Projector 增量化：把 `createAgentUiProjector.apply` 的全量重投影改成增量 reducer，消除 O(N²)。
3. App Server event normalization：runtime client 在送入 verifier 前把 `message.delta` / `message.completed` / `turn.final_done` 等 current App Server 事件规整为 Lime 标准 `model.*` / `turn.completed`。
4. Adapter / middleware / versioning 留作后续 v2.x，不算 v2.1 完成判定。

## v2.2 实施范围

1. 设计 `state.delta` 字段级 patch 语义，优先评估 RFC 6902 JSON Patch。
2. 明确 snapshot / delta / hydration / repair cursor 的合并顺序。
3. 输出 JSON Schema，支撑 Rust App Server 侧机械校验。

## v2.5 实施范围

1. `state.delta` 进入 `@limecloud/agent-runtime-projection` 的 `projectAgentUiState()` 与 `createAgentUiProjector.apply()`，batch 与 incremental 共用同一 patch 归并逻辑。
2. patch 失败进入 `hydration.status = "stale"` 并写入 diagnostics，不污染目标 state。
3. `state.delta` 只 patch schema 已知 projection / readModel 子树，不得伪造 runtime fact。

## v2.6 实施范围

1. Rust App Server 在 `RuntimeCore` event 入库前，把 `AgentEvent` 规范化为 Workbench `AgentRuntimeExecutionEvent` 形状，并用 `@limecloud/agent-ui-contracts` checked-in JSON Schema 做机械校验。
2. `state.delta` payload 额外通过 `agent-runtime-state-delta.v0.1.schema.json`，非法 patch fail closed，不写入 session state，也不产生 outbound notification。
3. 契约守卫固定 App Server schema gate，防止后续只保留 TS / 前端下游校验。

## v2.7 实施范围

1. Workbench 文档站新增活体 Demo 矩阵页面，真实执行 `agentUiConformanceFixtures -> validation -> sequence verifier -> replayAgentUiFixture -> projection summary`。
2. 矩阵覆盖 text、tool success/failure、HITL、artifact/evidence、stream repair、Subagents 多事件族，不再只靠 `/subagents` 单点证明闭环。
3. 后续若继续加深，优先补 App Server 真实 fixture backend 与 bad-stream 负向可视化。

## v2.8 实施范围

1. Lime Rust/App Server 在 RuntimeCore event 入库前执行 AgentUI sequence gate，不只依赖 TS runtime client / renderer gateway。
2. gate 与 TS sequence verifier 保持同一核心语义：`tool.started -> tool.result/failed`、`action.required -> action.resolved`、turn terminal 前 active tool/action 必须收口，终态后执行流不得继续污染 state。
3. sequence violation fail closed：不写入 `StoredSession.events`，不提前改变 `AgentTurnStatus` / `AgentSessionStatus`，不发 outbound notification。
4. 保持兼容约束：未达终态的进行中流不算 violation；`message.delta -> turn.final_done` 仍合法；缺少 scope id 的历史兼容事件不被突然误伤。

## v2.9 实施范围

v2.9 补 AG-UI `transformChunks` 背后的承载机制，但不声称已经兼容 AG-UI wire protocol。

1. `@limecloud/agent-runtime-client` 的 event pipeline 支持 adapter / middleware 返回 `0..N` 个 `agentSession/event` notification，并支持 `flush()` / `flushSync()` 让 buffered transform 在流收尾时排空。
2. 根 runtime client 与 `sessionGateway` 的 `dispatchEvent()` / `nextEvent()` 都消费 `pipelineResult.notifications`，fan-out 后的后续事件先进入 pending queue，再读取下一条 transport message。
3. Lime 本体 current event gateway 不再只把 pipeline 当 boolean gate：App Server notification、本地 publish、bridge listener 都消费 pipeline 投影后的 `0..N` payload；未配对 `tool.result` 继续 fail-closed。
4. Agent App current runtime client 工厂透传 runtime-client options，让 adapters / middlewares / verifier mode 能接入 Agent App current 路径。
5. 只落最小兼容 fan-out：明确的 `tool.completed` / `tool.complete` 可展开为 `tool.started + tool.result`。具体 provider chunk family mapping 留给后续，不把临时兼容写成新协议事实源。

## v2.10 实施范围

v2.10 补 AG-UI 仍领先的运行时能力声明和 resume contract，但不新增平行 runtime owner，也不声称已经实现自动 negotiation：

1. `@limecloud/agent-ui-contracts` 新增 `AgentRuntimeCapabilityManifest` / `AgentRuntimeCapabilityEntry` 类型，声明 transport、tools、state.delta、reasoning、multimodal、HITL、subagents、evidence 等支持面。
2. `@limecloud/agent-ui-contracts` 新增 `AgentRuntimeResumeContract` / `AgentRuntimeResumeActionDecision` 类型，固定 open action 的 resume 覆盖规则。
3. contracts 包导出 capability manifest / resume contract JSON Schema constants，并写入 checked-in schema 文件。
4. validation API 机械校验 manifest 必填字段、capability entry 结构，以及 `all-open-actions` / `selected-actions` 模式必须覆盖所有 open action。
5. Lime 本体接入同一合同：App Server `capability/list` response 返回 `runtimeCapabilityManifest`，`agentSession/thread/resume` params 可带 `resumeContract`，RuntimeCore 在启动 queued turn 前校验 schema version、session match 和 open action coverage；前端 `AgentRuntime` current gateway 可读取 manifest，并在 resume 前构造/校验合同。
6. 保持边界：Lime 已有 App Server `capability/list` 和 `resumeAgentSessionThread` 主链继续作为 runtime owner；本轮不新增 App Server method，不实现自动 provider negotiation。

## v2.11 后续

补 projection reconciliation 与外部 transport compatibility 的深水区：

1. message snapshot 与 streaming text 的合并 / 替换规则。
2. tool result 必须邻接 owning assistant tool call；partial tool args / JSON args buffer 的归并规则。
3. encrypted reasoning continuity：只保留可展示 summary / encrypted ref，不泄露 raw chain of thought。
4. SSE / protobuf / Accept negotiation 只能作为外部 gateway compatibility 候选；Lime 本体 current transport 仍是 App Server JSON-RPC / Host bridge。

## v2.3 后续

1. 在 Workbench 文档站提供可运行最小面板，不只展示 Markdown。
2. 活体 demo 读取 Lime fixture backend 或 App Server current fixture，不调用正式模型。
3. GUI smoke 产出 evidence，证明 runtime event -> verifier -> projection -> UI surface 完整闭环。

## v2.4 实施范围

v2.4 补 AG-UI 对比后剩余杠杆最大的协议演进层：`normalize -> adapter -> middleware -> sequence verifier -> dispatch`。

1. `@limecloud/agent-runtime-client` 新增 browser-safe `AgentRuntimeEventPipeline`，统一承载 adapters / middlewares / sequence verifier。
2. 根入口 `createAgentRuntimeClient(...)` 与 `createAgentRuntimeClientFromSessionGateway(...)` 共用同一 pipeline；adapter / middleware 只能改写或丢弃 `agentSession/event` notification，不新增 transport owner。
3. 新增 `createSchemaVersionCompatibilityMiddleware()`，把缺失 `schemaVersion` 的兼容补丁集中在 middleware，不在 projector 或 runtime client 分散 `if/else`。
4. Lime 本体 `src/lib/api/agentRuntime/eventSequenceGate.ts` 不再直接持有裸 `AgentRuntimeEventSequenceGate`；current App Server fanout 与 bridge listener 进入同一 browser-safe pipeline。
5. Workbench 文档站补 Runtime Middleware / Adapter 页面，并在 roadmap 明确 `/subagents` 活体闭环已完成，v2.4 继续补协议演进机制。

## 完成判定

v2.0 完成必须同时满足：

1. `internal/roadmap/agentworkbench/README.md` 指向本文。
2. contracts 包导出 sequence verifier API（批量 + 增量双模式）。
3. 全部 conformance fixtures 通过 verifier（好流不被误伤）。
4. 至少四个负向测试覆盖：未配对 tool、孤立 tool.result、未配对 action、turn 终态前执行流未收口。
5. projection fixture replay 在 verifier 有 violation 时 fail closed，不继续投影坏流。
6. 定向包测试通过：`@limecloud/agent-ui-contracts` 与 `@limecloud/agent-runtime-projection`。
7. Workbench 文档站补 sequence verifier API 参考，并接入导航。

v2.1 完成必须同时满足：

1. `@limecloud/agent-runtime-client` 根入口和 `sessionGateway` 入口都默认启用 sequence verifier，invalid stream fail closed。
2. `collect-diagnostics` / `off` 模式有测试覆盖，保持测试和迁移场景可控。
3. App Server `message.delta` 在 runtime client 边界规整为 `model.delta` 后再进入 verifier。
4. `createAgentUiProjector.apply()` 对新事件只做增量 accumulator 更新；重复事件直接返回当前 state，不再重跑全量投影。
5. 增量 projector 与 `projectAgentUiState({ executionEvents })` 在 subagent fixture 和 action resolved 场景下输出等价 state。

v2.2 完成必须同时满足：

1. contracts 包导出 `AgentRuntimeStateDelta` / `AgentRuntimeJsonPatchOperation` 类型。
2. contracts 包导出 runtime event、state delta、projection state 三个 JSON Schema constants。
3. checked-in `packages/agent-ui-contracts/schemas/*.schema.json` 与 constants 有测试保证完全一致。
4. projection state schema 明确包含 `subagents` 标准模型。
5. Workbench 文档站说明 snapshot / delta / hydration / repair cursor 的合并顺序。

v2.3 完成必须同时满足：

1. Workbench 文档站 `/subagents` 顶层页以 `AgentUiProjectionState.subagents` / `SubagentsView` / `subagent-handoff` fixture 为事实源。
2. 文档站提供可渲染最小面板，展示 `subagent-handoff` replay 后的 `threads` / `delegationCalls` / `activities`。
3. Runtime client、schema、projection、fixture replay、Subagents UI 文档都不再把 `teamWorkbench` / `roster` / `workItems` 当 current owner。
4. `npm run docs:build` 通过。

v2.4 完成必须同时满足：

1. `@limecloud/agent-runtime-client` 导出 `AgentRuntimeEventPipeline`、adapter / middleware 类型、`withEvent()` 和 `createSchemaVersionCompatibilityMiddleware()`。
2. 根 runtime client 与 `sessionGateway` 子路径都支持 adapters / middlewares，且 verifier 仍在 dispatch 前 fail closed。
3. `sessionGateway` 子路径保持 browser-safe，不引入 `node:`、`@limecloud/app-server-client` 运行时代码或 Electron bridge。
4. Lime 本体 current event gateway 使用同一 pipeline，未配对 `tool.result` 继续被阻断，`message.delta -> turn.final_done` 不误伤。
5. `lime-agent-workbench` 文档站新增 Runtime Middleware / Adapter 页面并接入导航 / roadmap。

v2.6 完成必须同时满足：

1. `lime-rs/crates/app-server` 在 runtime event 入库前执行 Workbench runtime event schema gate。
2. `state.delta` 通过 Rust 侧 state delta schema gate，非法 patch 不能污染 `StoredSession.events`。
3. Rust 定向测试覆盖合法 runtime event、合法 state delta、非法 state delta fail closed。
4. `scripts/check-app-server-client-contract.mjs` 有结构守卫防止 Rust schema gate 退化成只生成 schema 文件。
5. Workbench 文档站明确 TS schema seed、Rust/App Server enforcement 和后续活体矩阵的边界。

v2.7 完成必须同时满足：

1. `lime-agent-workbench` 文档站新增活体 Demo 矩阵页，并接入侧栏。
2. 页面不能只列 Markdown 表格，必须在 build 时引入真实 fixture / replay API。
3. 矩阵至少覆盖 text、tool、HITL、artifact/evidence、stream repair、Subagents。
4. `npm run docs:build` 通过。

v2.8 完成必须同时满足：

1. `lime-rs/crates/app-server` 新增 Rust AgentUI sequence verifier，并由 `RuntimeCore` 入库路径调用。
2. schema gate 和 sequence gate 均发生在状态迁移 / event push 前。
3. Rust 测试覆盖合法 tool 配对、孤立 `tool.result` fail closed、终态前 active tool 未收口 fail closed、`message.delta -> turn.final_done` 兼容流。
4. 契约守卫固定 App Server sequence gate 文件、调用点和负向测试，防止后续只保留 TS / renderer 下游校验。
5. Workbench 文档站说明 App Server 已同时执行 schema + sequence gate。

v2.9 完成必须同时满足：

1. `@limecloud/agent-runtime-client` pipeline 支持 `0..N` fan-out 和 `flush()` / `flushSync()`，并有包内测试覆盖。
2. 根 runtime client 和 `sessionGateway` 的 `nextEvent()` 能先返回 fan-out pending events，再读取下一条 transport message。
3. Lime 本体 `src/lib/api/agentRuntimeEvents.ts` 与 `src/lib/api/agentRuntime/threadClient.ts` 消费 pipeline 输出，不再只用 boolean gate。
4. Agent App current runtime client 工厂可透传 adapters / middlewares / verifier options。
5. Workbench 文档站说明 v2.9 只是 fan-out / flush substrate 与 Lime 本体消费路径，不等同于完整 AG-UI `transformChunks` mapping。

v2.10 完成必须同时满足：

1. contracts 包导出 capability manifest / resume contract 类型。
2. contracts 包导出 capability manifest / resume contract 两份 JSON Schema constants，并有 checked-in schema 文件防漂移。
3. validation API 覆盖 capability entry 必填字段和 resume open action 覆盖规则。
4. 包测试证明新合同、schema 文件和声明文件全部可用。
5. Lime App Server protocol / generated schema / app-server-client 类型同步 `runtimeCapabilityManifest` 与 `resumeContract` 字段。
6. Lime RuntimeCore 在 `agentSession/thread/resume` 启动 queued turn 前校验 resume contract，coverage 失败 fail closed。
7. Lime 前端 `src/lib/api/agentRuntime` current gateway 消费 capability manifest，并在 resume 前构造/校验 resume contract。
8. Workbench 文档站说明 v2.10 是合同 + Lime current path 接入，不等同于自动 capability negotiation 已完成。

## 进度日志

| 时间 | 进度 | 证据 |
| --- | --- | --- |
| 2026-06-12 | 建立 v2 路线，固定「可执行协议内核」作为下一阶段主线。 | 本文件初版。 |
| 2026-06-12 | 重新精读两边核心管道，补「全量重投影」本质债与 verifier 双模式 / 兼容约束，重写实施范围与完成判定。 | 本文件（差距一/二、兼容约束、完成判定 1-7）。 |
| 2026-06-12 | **v2.0 完成**：sequence verifier 实现并通过全部测试（17 + 64 + 10 = 91 tests pass）。 | `packages/agent-ui-contracts/src/sequenceVerifier.ts`、`packages/agent-runtime-projection/src/fixtureReplay.ts`、`lime-agent-workbench/docs/sdk/typescript/contracts/sequence-verifier.md`。 |
| 2026-06-12 | **v2.1 完成**：runtime client / session gateway 默认 fail-closed 接入 verifier；App Server message events 规整后进入 verifier；projector `apply()` 改为增量 accumulator，重复事件不重投影。 | `packages/agent-runtime-client/src/eventVerifier.ts`、`packages/agent-runtime-client/src/runtimeClient.ts`、`packages/agent-runtime-client/src/sessionGateway.ts`、`packages/agent-runtime-projection/src/uiState.ts`、`packages/agent-runtime-projection/src/readModel.ts`、`packages/agent-runtime-projection/src/runtimeStatus.ts`；`agent-runtime-client` 12 tests pass，`agent-runtime-projection` 66 tests pass。 |
| 2026-06-12 | **v2.2 完成（主仓侧）**：`state.delta` RFC 6902 seed、三份 JSON Schema constants 和 checked-in schema 文件落地，并用测试防漂移。 | `packages/agent-ui-contracts/src/runtime.ts`、`packages/agent-ui-contracts/src/schemas.ts`、`packages/agent-ui-contracts/schemas/*.schema.json`；`agent-ui-contracts` 19 tests pass。 |
| 2026-06-12 | **v2.3 完成**：Workbench 文档站 `/subagents` 和 `Subagents 活体闭环` 页面收敛到 current subagents model；活体面板真实执行 `subagent-handoff -> replayAgentUiFixture -> state.subagents -> SubagentsView`，不再使用静态 mock 数据。 | `lime-agent-workbench/docs/subagents.md`、`docs/examples/subagents-live-loop.md`、`docs/.vitepress/theme/SubagentsLiveLoop.vue`、`lime-agent-workbench/package.json`；`npm run docs:build` 通过。 |
| 2026-06-12 | **Lime 本体接入补齐**：Claw / Agent App 共用的前端 runtime event gateway 在 App Server notification 发布和 bridge listener 分发前接入 browser-safe sequence gate；未配对 `tool.result` fail-closed，`message.delta -> turn.final_done` 当前 App Server 事实流不误伤。 | `src/lib/api/agentRuntime/eventSequenceGate.ts`、`src/lib/api/agentRuntimeEvents.ts`、`src/lib/api/agentRuntime/threadClient.ts`、`scripts/check-app-server-client-contract.mjs`；`agentRuntimeEvents` + `threadClient` 35 tests pass，`npm run test:contracts` pass。 |
| 2026-06-12 | **GUI current fixture 收口**：Claw Electron fixture 曾卡在侧栏“暂无聊天”，根因是 fixture 直连 App Server 创建 session 后 GUI recent shelf 没有明确刷新信号；已新增 `AGENT_RUNTIME_SESSIONS_CHANGED_EVENT`，session client mutation 和 fixture 外部 mutation 都触发侧栏重新读取 App Server current session list。 | `src/lib/api/agentRuntime/sessionClient.ts`、`src/components/app-sidebar/useAppSidebarSessions.ts`、`scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs`；`npm run smoke:agent-runtime-current-fixture` 通过，`sessionClient.current-boundary` + `AppSidebar.conversations` 30 tests pass。 |
| 2026-06-12 | **v2.4 完成**：runtime client 新增 browser-safe adapter / middleware pipeline；根 client、session gateway 与 Lime 本体 current event gateway 共用 `normalize -> adapter -> middleware -> verifier -> dispatch` 链；Workbench 文档站补 Runtime Middleware / Adapter 页面。 | `packages/agent-runtime-client/src/eventPipeline.ts`、`packages/agent-runtime-client/src/runtimeClient.ts`、`packages/agent-runtime-client/src/sessionGateway.ts`、`src/lib/api/agentRuntime/eventSequenceGate.ts`、`lime-agent-workbench/docs/sdk/typescript/runtime-client/middleware-adapter.md`；`agent-runtime-client` 16 tests pass，`agentRuntimeEvents` + `threadClient` 35 tests pass。 |
| 2026-06-12 | **v2.5 完成**：`state.delta` 真正接入 projection / read model apply 链；`projectAgentUiState()` 与 `createAgentUiProjector.apply()` 共享同一 patch 归并逻辑，batch 与 incremental 输出等价，patch failure 进入 stale diagnostics。 | `packages/agent-runtime-projection/src/stateDelta.ts`、`packages/agent-runtime-projection/src/uiState.ts`、`packages/agent-runtime-projection/tests/projection.test.mjs`；`agent-runtime-projection` 70 tests pass。 |
| 2026-06-12 | **v2.6 完成**：Rust/App Server 在 RuntimeCore event 入库前接入 AgentUI runtime event / state.delta JSON Schema gate；非法 `state.delta` fail closed，不写入 session state。 | `lime-rs/crates/app-server/src/agent_ui_event_schema.rs`、`lime-rs/crates/app-server/src/runtime.rs`、`lime-rs/crates/app-server/src/runtime/tests.rs`、`scripts/check-app-server-client-contract.mjs`；`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server agent_ui_event_schema --lib`、`append_external_runtime_events_rejects_invalid_state_delta_before_storage --lib`、`append_external_runtime_events_keeps_sequence_and_turn_scope --lib` 通过。 |
| 2026-06-12 | **v2.7 完成**：Workbench 文档站新增活体 Demo 矩阵，真实执行多个 conformance fixtures 的 validation / sequence verifier / replay / projection summary。 | `lime-agent-workbench/docs/examples/live-matrix.md`、`docs/.vitepress/theme/LiveMatrix.vue`、`docs/.vitepress/theme/index.ts`；`npm run docs:build` 通过。 |
| 2026-06-12 | **v2.8 完成**：Lime Rust/App Server 在 RuntimeCore event 入库前接入 AgentUI sequence gate；未配对 `tool.result` 和终态前未收口 tool/action fail closed，不污染 `StoredSession.events` 或 turn/session 状态。 | `lime-rs/crates/app-server/src/agent_ui_sequence_verifier.rs`、`lime-rs/crates/app-server/src/runtime.rs`、`lime-rs/crates/app-server/src/runtime/tests.rs`、`scripts/check-app-server-client-contract.mjs`。 |
| 2026-06-12 | **v2.9 完成**：runtime-client pipeline 支持 `0..N` fan-out 与 flush；Lime 本体 App Server notification、本地 publish、bridge listener 和 Agent App runtime client options 已消费同一 pipeline 输出，不再停留在包内 API。 | `packages/agent-runtime-client/src/eventPipeline.ts`、`packages/agent-runtime-client/src/runtimeClient.ts`、`packages/agent-runtime-client/src/sessionGateway.ts`、`src/lib/api/agentRuntime/eventSequenceGate.ts`、`src/lib/api/agentRuntimeEvents.ts`、`src/lib/api/agentRuntime/threadClient.ts`、`src/features/agent-app/runtime/agentRuntimeAppServerClient.ts`；`agent-runtime-client` 19 tests pass，相关 Lime 本体 41 tests pass。 |
| 2026-06-12 | **v2.10 完成（contracts seed + Lime 本体接入）**：新增 Runtime / Provider capability manifest 与 resume contract 类型、JSON Schema、checked-in schema 文件和 validation API；App Server `capability/list` 返回 `runtimeCapabilityManifest`，`agentSession/thread/resume` 承接 `resumeContract` 并在 RuntimeCore 启动 queued turn 前 fail-closed 校验；前端 `AgentRuntime` current gateway 消费/构造同一合同。 | `packages/agent-ui-contracts/src/capabilities.ts`、`lime-rs/crates/app-server-protocol/src/protocol/v0/core.rs`、`lime-rs/crates/app-server-protocol/src/protocol/v0/session_admin.rs`、`lime-rs/crates/app-server/src/runtime.rs`、`src/lib/api/agentRuntime/capabilityContract.ts`、`src/lib/api/agentRuntime/threadClient.ts`；`agent-ui-contracts` 24 tests pass，`app-server-client` 49 tests pass，`threadClient/clientFactory` 41 tests pass，Rust schema_export + resumeContract 定向测试 pass。 |
