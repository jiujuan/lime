# App Server 前端对接矩阵

> 状态：current planning source
> 更新时间：2026-06-08
> 作用：把 App Server JSON-RPC、Electron Desktop Host IPC、前端 API 网关、mock 和真实 UI caller 对齐到同一张表，避免继续出现“后端接口存在，但前端只接 mock / compat”的半闭环。

## 1. 当前结论

当前正确架构固定为：

`Frontend -> Electron Desktop Host bridge -> App Server JSON-RPC -> RuntimeCore / backend`

Electron main 是 Desktop Host bridge，不是后端适配层，也不是第二套后端；它只负责接收 renderer IPC / legacy command facade、转换 App Server JSON-RPC method、管理窗口 / 托盘 / Dock / updater / sidecar 生命周期，以及做少量参数和返回值投影。真正的后端事实源只有一个：App Server JSON-RPC。

如果某条路径使用 in-process App Server 或 typed channel，它也只是去掉进程边界，必须继续保持 App Server JSON-RPC result envelope 和 `agentSession/event` 事件合同；不得引入第二响应合同、第二 read model 或 Electron / App Server 业务层。

当前 App Server 已经具备 protocol / Rust processor / TypeScript client / Electron sidecar 通道的骨架；本轮开始把前端主 Agent lifecycle 切到上述 current 主链。

事实口径：

1. `packages/app-server-client/src/index.ts` 已列出并实现 App Server method builder / connection helper，覆盖 `agentSession/*`、workspace、skill、model、artifact、evidence 等方法。
2. `packages/app-server-client/src/protocol.ts` 是 renderer-safe 协议事实源，只放 JSON-RPC method 常量、wire DTO、method catalog、schema manifest 类型与 encode/decode / type guard；`index.ts` 只在此基础上增加 Node sidecar、stdio connection、release manifest 和 lifecycle helper。
3. `electron/appServerHost.ts` 已能启动 sidecar，并提供 `request(...)`、`handleJsonLines(...)`、`drainEvents(...)`。
4. `electron/hostCommands.ts` 已把部分 legacy 前端命令投影到 App Server method，例如 session list/read、workspace、skill、model 和 capability inventory。
5. `electron/ipcChannels.ts` 只把 `app_server_handle_json_lines` / `app_server_drain_events` 作为通用 App Server JSON-RPC IPC 暴露给 renderer。
6. `src/lib/api/appServer.ts` 是 renderer 侧 JSON-RPC client gateway，复用 `packages/app-server-client/src/protocol.ts` 的协议类型 / 常量 / helper，底层只通过 `app_server_handle_json_lines` / `app_server_drain_events` 进入 Electron Desktop Host IPC。
7. `src/lib/api/agentRuntime/threadClient.ts` 的 submit / cancel / respond 已改为 App Server current-only：`submitAgentRuntimeTurn -> agentSession/turn/start`、`interruptAgentRuntimeTurn -> agentSession/turn/cancel`、`respondAgentRuntimeAction -> agentSession/action/respond`；当 `app_server_handle_json_lines` 不可用时 fail closed，不再回退 `agent_runtime_*`。
8. `src/lib/desktop-host/agentRuntimeMocks.ts` 已移除 `app_server_handle_json_lines` / `app_server_drain_events` 与 submit / cancel / respond lifecycle 默认成功值；后续如需测试夹具必须显式注入，不得恢复 default mock。
9. `src/components/agent/chat/hooks/useAgentRuntimeSyncEffects.ts` 已订阅当前 active stream 的 App Server runtime event，并对 `runtime_status`、`artifact_snapshot`、`action_required`、`action_resolved`、`done` 等会影响 read model / timeline 的事件触发 `refreshSessionDetail(sessionId)`；`text_delta` 仍走流式渲染，不触发完整 read model 重拉。
10. `src/lib/api/agentRuntime/threadClient.ts` 的 `getAgentRuntimeThreadRead(...)` 已通过 `createAppServerReadModelClient()` 投影 `agentSession/read`，不再调用 `agent_runtime_get_thread_read`。
11. `src/lib/api/agentRuntime/sessionClient.ts` 的 create / list / get / update 已通过 `createAppServerSessionClient()` 投影 `agentSession/start`、`agentSession/list`、`agentSession/read`、`agentSession/update`，不再把 `agent_runtime_create_session`、`agent_runtime_list_sessions`、`agent_runtime_get_session`、`agent_runtime_update_session` 当作前端事实源。update 投影覆盖 rename / archive、provider / model、execution strategy 与 recent metadata；delete 和 title generation 仍是 compat 边界，不能混作 session update 完成证据。
12. `npm run smoke:electron` 已通过 smoke-only renderer build、Electron host build、App Server `initialize` 与 Claw 工作台 shell/composer ready 验证，输出 `app-server ready protocol=appserver.v0 version=1.59.0`、`app-server initialized protocol=appserver.v0 version=1.59.0` 与 `claw workbench shell ready`。该证据证明 Electron Desktop Host bridge 能带起真实 App Server sidecar，并能挂载 Claw / Agent 工作台入口与输入框；它仍不发送消息、不触发 `agentSession/turn/start`、不调用 Provider 连接测试，也不是完整 streaming / event refresh 业务 E2E。Electron packaged renderer build 固定 `base: "./"`，避免 `file://dist/index.html` 因绝对 `/assets/...` 只停在原生启动层。
13. 生产不能 mock，只有测试才 mock；`scripts/check-command-contracts.mjs` 必须扫描 `src / electron / packages` 的生产源码，禁止 `explicitMockFallback`、`invokeMockOnly`、`mockCommand`、`clearMocks`、`invokeExplicitMock`、`listenExplicitMock` 进入生产路径。
Rust wrapper 边界同样固定：`lime-rs/src/commands/**` 是旧 Tauri command wrapper 清理区，不是本矩阵的 current 实现目录。矩阵中仍列出的旧命令名只用于定位迁移来源、负向回归扫描或删除队列；新增后端能力进入 App Server crates / RuntimeCore / services，桌面壳能力进入 Electron Desktop Host。若某个前端入口只能靠 `commands/**` wrapper 才能成功，该入口应标为 current 缺口或下线候选，不能补 stub、compat wrapper 或 mock 来假成功。
14. `src/lib/api/agentRuntime/exportClient.ts` 的 `exportAgentRuntimeEvidencePack(...)` 已通过 `AppServerClient.exportEvidence(...) -> evidence/export` 进入 App Server current 主链，并把 `evidencePack` 投影成旧 UI 消费的 `AgentRuntimeEvidencePack` snake_case 形状；handoff / replay / analysis / review / save review 仍是 compat export command，不能算 evidence/export 全域迁完。
15. `src/lib/api/agent.test.ts` 的 facade 级 evidence pack 测试已改为断言 `app_server_handle_json_lines -> APP_SERVER_METHOD_EVIDENCE_EXPORT`，并要求 `includeEvents/includeArtifacts/includeEvidencePack`；`agent_runtime_export_evidence_pack` 只能作为 compat 残留，不能再作为正向完成证据。
16. `src/lib/api/agentRuntime/appServerArtifactClient.ts` 已把 timeline artifact 缺正文和 Workbench scoped artifact preview 的读取接到 `AppServerClient.readArtifacts(...) -> artifact/read`；`AgentThreadTimelineArtifactCard` 与 `useWorkspaceArtifactPreviewActions` 都只经该 gateway 请求 `includeContent: true` 并只接受 `contentStatus: "available"` 的内容。缺少 `sessionId` 或 App Server 无可用 content 时 fail closed，不回退 legacy command、文件预览旁路或 mock。该步仍只覆盖带 App Server scope 的 timeline / Workbench preview，真实 GUI artifact 打开证据和其它 artifact 旁路仍需继续盘点。
17. `src/lib/api/agentRuntime/inventoryClient.ts` 的 `listWorkspaceSkillBindings(...)` 已通过 `AppServerClient.request("workspaceSkillBindings/list", ...)` 进入 App Server current 主链，并把 `{ bindings }` 投影成旧 UI 既有 `AgentRuntimeWorkspaceSkillBindings` 形状；空 `workspaceRoot` 或缺少 `bindings` 时 fail closed，不再调用 `agent_runtime_list_workspace_skill_bindings`。`src/lib/api/capabilityDrafts.ts` 的 registered skills discovery 也已通过 `workspaceRegisteredSkills/list` 进入 App Server current 主链，不再调用 `capability_draft_list_registered_skills`。`scripts/workspace-skill-bindings-current-smoke.mjs` 已通过真实 Skills 用户安装页 / 已保存技能面板证明 `workspaceRegisteredSkills/list` 与 `workspaceSkillBindings/list` 均经 `app_server_handle_json_lines` 到达 App Server，返回有效数组且未观察到旧 registered skills discovery / workspace skill binding 命令。该证据覆盖 registered skills discovery 与 runtime binding readiness 两条读链，但不代表 Capability Draft create / verify / register 或完整 Skills 工作台执行闭环。`getAgentRuntimeToolInventory(...)` 暂保 compat command，因为 `capability/list` 只返回 capability descriptor，不能伪装成完整 catalog / registry / runtime / MCP inventory。
18. `src/lib/api/memory.ts` 的 `getProjectMemory(...)` 聚合读已通过 `AppServerClient.request("projectMemory/read", { projectId })` 进入 App Server current 主链；空 `projectId` 或 App Server 未返回 `memory` 时 fail closed，不再调用 `project_memory_get`、renderer mock 或 Electron legacy facade。`scripts/project-memory-current-smoke.mjs` 已通过真实 Agent 首页 / 工作区证明默认工作区解析后项目记忆读取经 `app_server_handle_json_lines` 观察到 `projectMemory/read`，且未观察到旧 `project_memory_get` 命令。角色 / 世界观 / 大纲 CRUD 仍暂走既有命令，因为 App Server 目前只覆盖项目记忆聚合读。
19. Agent App mock SDK 只允许测试环境显式使用；`mockSdkEnabled` 在生产构建中硬关闭，`buildMockCapabilityProfile`、`MockCapabilityHost`、`createMockLimeCapabilityTransport` 都必须通过 test-only 断言，且 mock transport 只允许测试夹具显式导入。public SDK / production runtime 不再导出或导入 mock transport、mock host、mock capability profile 或 mock handler 类型；生产 Agent App runtime 缺真实 App Server / Electron current method 时必须 fail closed，不能用 mock capability profile、mock host 或 mock transport 作为产品闭环。
20. `src/lib/api/project.ts` 的 workspace/project 读与目录就绪链路已通过 `AppServerClient.request(...)` 进入 App Server current 主链，并直接复用 `packages/app-server-client/src/protocol.ts` 的 `METHOD_WORKSPACE_*` 常量；`workspace/list`、`workspace/read`、`workspace/byPath/read`、`workspace/default/read`、`workspace/default/ensure`、`workspace/projectsRoot/read`、`workspace/projectPath/resolve`、`workspace/ensureReady` 缺少必需 result 时 fail closed，不再把 `workspace_get*`、`workspace_list`、`workspace_ensure_*` 或 `get_or_create_default_project` 当作前端事实源。`workspace_create/update/delete/set_default` 与 content 相关命令仍暂保 compat，因为 App Server 目前没有完整等价写 method。
21. `src/lib/api/modelRegistry.ts` 的模型 registry / preference / sync 读链，以及 provider / tier / alias scoped 读链的前端 gateway，已通过 `AppServerClient.request(...)` 进入 App Server current 主链，并直接复用 `packages/app-server-client/src/protocol.ts` 的 `METHOD_MODEL_*` / `METHOD_MODEL_PROVIDER_*` 常量；`model/list`、`modelPreferences/list`、`modelSyncState/read`、`modelProvider/fetchModels`、`modelProviderAlias/read`、`modelProviderAlias/list` 缺少必需 result 时 fail closed，不再把 `get_model_registry`、`get_model_preferences`、`get_model_sync_state`、`get_models_for_provider`、`get_models_by_tier`、`get_provider_alias_config`、`get_all_alias_configs` 或 `fetch_provider_models_auto` 当作前端事实源。`scripts/model-registry-current-smoke.mjs` 已通过真实设置页“添加模型”面板证明 `model/list`、`modelPreferences/list`、`modelSyncState/read` 经 `app_server_handle_json_lines` 到达 App Server，且未观察到旧 `get_model_*` 命令、模型写入 / refresh side-effect 或 `agentSession/turn/start`。本 GUI 证据不覆盖 `modelProviderAlias/read` / `modelProviderAlias/list` 的 alias GUI 触发，不覆盖 provider / tier scoped 查询或 provider ids 兼容查询。`refresh_model_registry`、`search_models`、`toggle_model_favorite`、`hide_model`、`record_model_usage` 与 provider ids 兼容查询仍暂保 compat，因为 App Server 目前没有完整等价写 / refresh method。
22. `src/lib/api/knowledge.ts` 的 Knowledge 生命周期网关已全量改走 App Server current method：`knowledgePack/list`、`knowledgePack/read`、`knowledgePack/source/import`、`knowledgePack/compile`、`knowledgePack/default/set`、`knowledgePack/status/update`、`knowledgeContext/resolve`、`knowledgeContextRun/validate`。空 `workingDir`、缺少必需 result 或 DTO 形状不对时 fail closed，不再把 `knowledge_*`、renderer mock、desktop-host mock 或 Electron legacy facade 当作前端事实源。App Server protocol / schema / TS client / Rust client / processor / RuntimeCore / `LocalAppDataSource -> lime_knowledge` 已覆盖同一组 method；`electron/hostCommands.ts` 与 `electron/ipcChannels.ts` 中的 `knowledge_*` compat facade 已退场，只保留通用 `app_server_handle_json_lines` / `app_server_drain_events` current bridge。`scripts/knowledge-current-smoke.mjs` 已证明项目资料页面列表读取经 `app_server_handle_json_lines` 观察到 `knowledgePack/list` 且无旧 `knowledge_list_packs`；Knowledge product / GUI / provider smoke 也已改为调用 `knowledgePack/*` current method。剩余缺口只限 live Builder Skill runtime binding：`knowledgePack/compile` 遇到 `builderRuntime.enabled=true` 时在 App Server current 路径 fail closed，后续必须把 Builder runtime binding 迁入 App Server current，不得回落到 `lime-rs/src/commands/knowledge_cmd.rs`。
23. `src/lib/api/skill-execution.ts` 的 `listExecutableSkills()` 与 `getSkillDetail(...)` 已通过 `AppServerClient.request(...)` 进入 App Server current 主链，并直接复用 `packages/app-server-client/src/protocol.ts` 的 `METHOD_SKILL_LIST` / `METHOD_SKILL_READ`；`skill/list` 或 `skill/read` 缺少必需 result 时 fail closed，不再把 `list_executable_skills`、`get_skill_detail`、renderer mock 或 Electron legacy facade 当作前端读事实源。`scripts/skills-current-smoke.mjs` 已通过真实 GUI slash skill preflight 证明 `skill/list` 经 `app_server_handle_json_lines` 返回 `result.skills` 数组，且未观察到旧 Skill 命令、`execute_skill` 或 `agentSession/turn/start`。`skill/read` 真实 GUI 触发与 `execute_skill` 仍暂保 compat side-effect，因为 App Server 当前没有完整等价执行 method。
24. `src/lib/api/agentApps.ts` 的 `listInstalledAgentApps(...)` 已通过 `AppServerClient.request("agentAppInstalled/list", {})` 进入 App Server current 主链；缺少 `states` 或 `issues` 时 fail closed，不再把 `agent_app_list_installed`、renderer mock 或 Electron legacy facade 当作前端事实源。`scripts/agent-app/installed-current-smoke.mjs` 已通过真实 GUI 点击路径证明 Agent Apps 管理页已安装列表读取经 `app_server_handle_json_lines` 观察到 `agentAppInstalled/list`，且未观察到旧 `agent_app_list_installed` 命令。`agent_app_save_installed_state`、install / uninstall / shell 等写入或 desktop side-effect 命令仍暂保 compat，因为 App Server 当前只覆盖已安装列表读链；UI runtime start / status / stop 已拆到 `agentAppUiRuntime/*` current method。`scripts/agent-app/ui-runtime-current-smoke.mjs` 已通过真实前端 gateway 证明 UI runtime `status` / `stop` / `start` 均经 `app_server_handle_json_lines -> agentAppUiRuntime/*` 到达 App Server，未观察到旧 `agent_app_*_ui_runtime` 命令，并用一次性不存在的 appId 验证 `start` fail closed、不写 installed state、不启动子进程；`scripts/agent-app/ui-runtime-stdio-fixture-smoke.mjs` 已进一步证明 App Server stdio / RuntimeCore 可用 fixture installed state 启动真实 npm dev 子进程，完成 `/api/bootstrap` ready、`entryUrl` 可达、status running 与 stop stopped 生命周期；`scripts/agent-app/ui-runtime-electron-fixture-smoke.mjs` 已证明真实 Electron-host GUI 能从正式侧栏进入 fixture Agent App runtime 页面，发出 `agentAppUiRuntime/start`，并让 iframe surface 打开 App Server 返回的 `entryUrl`。该证据只覆盖 Agent App UI runtime 生命周期，不覆盖 Agent 对话 turn、tool runtime、evidence 或 Claw/Aster 完整执行链。
25. `src/lib/api/automation.ts` 的 scheduler / job / schedule 网关已通过 `AppServerClient.request(...)` 进入 App Server current 主链，覆盖 `automationScheduler/config/read|update|status`、`automationJob/list|read|create|update|delete|runNow|health|runHistory` 与 `automationSchedule/preview|validate`；缺少必需 result 时 fail closed，不再把旧 `get_automation_*`、`*_automation_job`、renderer mock 或 Electron legacy facade 当作前端事实源。`scripts/automation-current-smoke.mjs` 已通过真实 GUI 点击路径证明持续流程任务列表读取经 `app_server_handle_json_lines` 观察到 `automationJob/list`，且未观察到旧 automation 命令族。旧 Tauri / Electron / DevBridge automation command surface 已判为 `dead` 并从 runner、dispatcher、Electron host 白名单和 mock priority 中移除；`automationJob/runNow` 已进入 current 协议，但 App Server 自动化执行器尚未迁完，当前实现 fail closed，不回退旧 Tauri 执行器。
26. `src/lib/api/apiKeyProvider.ts` 的 Provider list/catalog/read/create/update/delete/sort、API Key create/update/delete/next/usage/error、config import/export、UI state、连接测试与 chat test 已通过 `AppServerClient.request(...)` 进入 App Server current 主链，并直接复用 `METHOD_MODEL_PROVIDER_*`、`METHOD_MODEL_PROVIDER_KEY_*`、`METHOD_MODEL_PROVIDER_UI_STATE_*` 与 `METHOD_MODEL_PROVIDER_CONFIG_*`；缺少必需 result 时 fail closed，不再把 `get_api_key_providers`、`get_system_provider_catalog`、`get_provider_ui_state`、`set_provider_ui_state`、renderer mock 或 Electron legacy facade 当作前端事实源。`scripts/provider-settings-current-smoke.mjs` 已通过真实 GUI 点击路径证明 AI 服务商设置页和“添加模型”catalog 读取经 `app_server_handle_json_lines` 观察到 `modelProvider/list`、`modelProvider/catalog/list`，且未观察到旧 Provider 命令。单 Provider、secret 写入、UI state、导入导出和连接测试的 GUI 逐项证据仍缺，但协议、client、processor 与前端 gateway 已走 App Server current。
27. `src/lib/dev-bridge/safeInvoke.ts` 的 Electron IPC 分支已复用命令超时 profile；`app_server_handle_json_lines` 内的 `agentSession/list` 识别为 `agent-session-list` 8 秒 profile，超时后 fail closed 并记录 `electron-ipc` trace / error，不回退 legacy desktop host、DevBridge mock 或 renderer mock。侧栏最近对话 / 归档通过 `listAgentRuntimeSessions -> agentSession/list` 读取时，底层 App Server JSON-RPC 无返回也必须 settle，不能让 UI 永久 loading。`scripts/sidebar-sessions-current-smoke.mjs` 已通过真实侧栏 GUI 读取证明 `agentSession/list` 经 `app_server_handle_json_lines` 到达 Electron-host App Server，最近列表和归档列表都能退出 loading，且未观察到 `agent_runtime_list_sessions`；P3.197 进一步要求 seeded remembered workspace（默认 `workspace-1`）不得出现在任何 `agentSession/list.params.workspaceId`，避免本地记忆项目污染最近 / 归档列表；P3.198 已把 seeded workspace 禁污染、current App Server list path、legacy list 禁止和失败 summary options 固化为 `scripts/sidebar-sessions-current-smoke.test.mjs` 静态守卫。
28. `src/lib/api/agentRuntime/appServerSessionClient.ts`、`src/lib/api/agentRuntime/inventoryClient.ts` 与 `src/lib/api/memory.ts` 已把 `agentSession/list`、`workspaceSkillBindings/list`、`projectMemory/read` 的前端 method 引用收敛到 `packages/app-server-client/src/protocol.ts` 的 `METHOD_*` 常量；这些 current gateway 不再各自硬编码 method 字符串，避免 Electron / App Server / content-studio 等后续消费者复制出第二套协议事实源。
29. `lime-rs/crates/app-server/src/local_data_source.rs` 的 `agentSession/list` / `agentSession/read` 数据源已覆盖 current timeline 表与真实旧 `agent_messages` message-only 会话；列表去重合并后统一排序 / limit，详情对无 timeline entries 的既有会话调用 `lime_agent` 读取真实 runtime detail。该补齐发生在 App Server JSON-RPC 后端事实源内，不新增 Electron 后端逻辑、不回退旧 Tauri、不引入 mock。
30. MCP 管理与运行命令已接入 App Server JSON-RPC current 主链。`src/lib/api/mcp.ts` 只经 `AppServerClient.request(...)` 调用 `mcpServer/list|create|update|delete|enabled/set|importFromApp|syncAllToLive|start|stop`、`mcpServerStatus/list`、`mcpTool/list|listForContext|search|call|callWithCaller`、`mcpPrompt/list|get` 与 `mcpResource/list|read`；缺少 App Server result 时 fail closed，不回退旧 `get_mcp_servers` / `add_mcp_server` / `mcp_*` legacy command、Electron legacy facade 或 desktop-host mock。`runner.rs`、Rust DevBridge dispatcher 与 `commands/mod.rs` 已断开 `mcp_cmd` 可执行入口，`scripts/check-command-contracts.mjs` 已防止旧 MCP 命令回到 Electron Host、DevBridge truth、mock priority、runtime catalog、runner、dispatcher 或 module 暴露。`lime-rs/src/commands/mcp_cmd.rs` 只剩 `dead-file-candidate / delete-after-confirmation`，不是后端事实源；P4 最终 `done` 仍需要确认后物理删除该文件并补真实 MCP server fixture。
31. `scripts/settings-all-pages-current-smoke.mjs` 已补真实设置页 broad smoke：通过 Electron-host DevBridge 进入设置页，逐项点击 15 个设置入口并覆盖 24 个页面 / 子页快照，断言无 DevBridge 断连、无 unsupported command 文案、无 safeInvoke error buffer、无 console error / 未分类 warning、无 invoke JSON-RPC error response；最新证据位于 `.lime/qc/gui-evidence/settings-current/settings-current-summary.json`，显示 `ok=true`、`tabs=15`、`pages=24`、`invokeCount=97`、`consoleErrors=0`、`unexpectedConsoleWarnings=0`、`invokeResponseErrors=0`。About 页更新检查在开发态缺少 `app-update.yml` 时不再向 console 写 updater 诊断 warning，只保留用户可见的失败状态。该证据只证明设置页首屏点击不会再因桥接 unsupported / warning 断裂，并观察到 `app_server_handle_json_lines` 中的 `workspace/default/ensure`、`agentAppInstalled/list`、`modelProvider/list`、`model/list`、`modelPreferences/list`、`modelSyncState/read`、`knowledgePack/list`、`agentSession/list`、`automationJob/list`、`projectMemory/read`、`workspace/list` 等 current method。`get_experimental_config` / `save_experimental_config` 已接到 Electron `config.json` current 事实源；该历史网络证据曾包含旧 `get_provider_ui_state`，但 Provider UI state 现已迁入 `modelProviderUiState/read|write` current 主链，旧 Provider façade 不得继续作为 Desktop compat 证据；`get_usage_stats`、`unified_memory_stats`、browser connector / updater / voice / automation scheduler 等仍是 Desktop compat 或 degraded 桌面壳诊断命令，所以不能把 P3.177 / P3.192 误判为“设置页全部 current 化”。
32. `scripts/code-runtime-fixture-smoke.mjs` 的离线 code runtime fixture smoke 已把正向 runtime 证据迁到 App Server JSON-RPC：会话、provider/model update、自然语言 turn start、read model 轮询与 evidence export 均经 `app_server_handle_json_lines -> agentSession/start/update/turn/start/read -> evidence/export`；脚本禁止再把 `agent_runtime_create_session`、`agent_runtime_update_session`、`agent_runtime_submit_turn`、`agent_runtime_get_session`、`agent_runtime_get_thread_read` 或 `agent_runtime_export_evidence_pack` 当成完成证据。file checkpoint / diff 仍保留 `agent_runtime_list_file_checkpoints` 与 `agent_runtime_diff_file_checkpoint` compat，因为 App Server 目前没有等价 checkpoint method；该 smoke 仍是 localhost fixture 离线证据，不等同于完整 Claw live GUI streaming E2E，也不等同于 Codex 一等 `thread/*` 已实现。
33. `scripts/claw-chat-ready-streaming-smoke.mjs` 已作为 Chromium + DevBridge(electron-host transport) Claw live streaming E2E 当前入口收紧证据边界，不等同 Playwright `_electron.launch()` / 真实 Electron renderer IPC E2E：必须显式 `--allow-live-provider` 或 `LIME_ALLOW_LIVE_PROVIDER_SMOKE=1 / LIME_REAL_API_TEST=1` 才会进入 DevBridge / Provider 阶段；进入后必须先确认 DevBridge health 的 `transport=electron-host`，否则不能把结果当作 Electron current GUI 证据。该脚本已经通过网络捕获和 summary 断言覆盖 `agentSession/turn/start`、`agentSession/turn/cancel`、`agentSession/event`、`agentSession/read`、event 后 read model 回流、无 runtime mock fallback、provider/model preference 保持与恢复 turn 可见性；本轮只补了 electron-host 与未授权 gate 守卫，尚未获得 live provider 授权运行真实 streaming。
34. `AppSidebar` 与 `AgentChatWorkspace` 的 workspace 过滤投影已收口：本地记忆项目只保留导航上下文，不能在未验证前成为 App Server `agentSession/list.workspaceId` 过滤条件；Agent 工作台仅在 `projectSelectionSource=remembered` 且项目尚未加载为同一 ID 时暂缓 runtime workspace，显式外部项目、初始会话、话题绑定项目、默认项目和手动选择继续按 current 主路径进入 `useAgentChatUnified`。
35. Connect deep link 已切到 Electron Desktop Host URL bridge 与 App Server JSON-RPC current 主链。`electron/main.ts` 同时收集 macOS `open-url`、首启 `process.argv` 与 `second-instance` argv 中的 `lime://` URL，preload 只暴露 renderer-safe `deepLink.getCurrent/onOpenUrl`；业务解析、Relay registry 校验、API Key 保存和 callback 发送只允许经 `src/lib/api/connect.ts -> connectDeepLink/resolve`、`connectOpenDeepLink/resolve`、`connectRelayApiKey/save`、`connectCallback/send` 进入 App Server。`scripts/connect-deep-link-current-smoke.mjs` 已用真实 Electron 首启参数证明 `lime://connect` 触发确认弹窗，并观察到 `app_server_handle_json_lines -> connectDeepLink/resolve`；`scripts/connect-open-deep-link-current-smoke.mjs` 已用真实 Electron 首启参数证明 `lime://open` 官网入口经 `connectOpenDeepLink/resolve` 解析后进入 Agent 官网入口页面状态。旧 `deep-link://new-url`、`deep-link-connect`、`deep-link-error`、`handle_deep_link`、`handle_open_deep_link`、`save_relay_api_key`、`send_connect_callback`、`list_relay_providers`、`refresh_relay_registry` 不得回流。仍未覆盖可信 HTTPS webhook 网络投递或真实 Relay registry 网络刷新。
36. renderer 侧 App Server JSON-RPC result envelope 已统一收口到 `src/lib/api/appServer.ts`：`handleAppServerJsonLines(...)` 与 `drainAppServerEvents(...)` 同时兼容 Electron `safeInvoke` 返回的 `{ result: { lines } }` 和旧 `{ lines }`，避免设置页把真实 App Server response 误判成 `agentAppInstalled/list did not return states`、`modelProvider/list did not return providers`、`model/list did not return models`、`modelProviderAlias/list did not return configs`。`src/lib/dev-bridge/commandPolicy.ts` 也把 `workspace/default/read`、`workspace/default/ensure`、`workspace/list`、`workspace/read`、`workspace/byPath/read`、`workspace/projectsRoot/read`、`workspace/projectPath/resolve`、`workspace/ensureReady` 这组 App Server workspace 启动真相 method 归到 `startup-truth` 30 秒 profile，防止设置页 / 默认工作区准备态被 5 秒 `truth` 超时误杀。Claw 发送前的本地恢复 session 现在必须先经 `runtime.getSession(..., { historyLimit: 40 })` 被 App Server 确认；`session not found` 或 workspace mismatch 会丢弃本地恢复快照并新建 App Server session，普通桥接错误继续 fail closed。该修复不新增 App Server method，不把 `agentRuntime` 扩成业务 runtime，只修前端 thin gateway 和 session 恢复保护。
37. App Server `RuntimeBackend` 已成为 Claw/Aster 执行链 current backend。对照 Codex `app-server` 后，Lime 不再把长命名 `lime-rs/crates/app-server-agent-backend` 当默认事实源；旧 external 二进制已迁入 `lime-rs/crates/app-server/src/runtime_backend.rs` 的 `ExecutionBackend` implementation，并通过 `initialize_aster_runtime(db.clone()) -> AsterAgentState::new() -> configure_provider_from_pool(...) / configure_provider(...) -> stream_reply_with_policy(...)` 复用 Claw/Aster 流式执行链。`app-server --backend runtime`、`AppServerBackendMode::Runtime`、`AppServerRuntimeFactory::runtime_backend_core(...)`、Electron dev 默认 `APP_SERVER_BACKEND_MODE=runtime` 是 current；`lime-rs/crates/app-server-agent-backend` 已按 `dead` 删除，`Cargo.lock` 不再包含 `app-server-agent-backend` package，contract guard 同步禁止该目录或 lockfile package 回流。RuntimeBackend 读取 App Server `ExecutionRequest.session / turn`、扁平 `hostOptions.asterChatRequest` 与 `turn_config` 镜像，恢复 provider/model/system prompt/reasoning/sandbox/web search 等请求合同；runtime event 投影为 App Server current event type，最终输出 `turn.final_done`。provider/model 选择已按 Claw session default 语义收口：显式 runtime preference -> host provider_config / preference -> session `businessObjectRef.metadata` 中完整 provider/model default；不再扫描全局 enabled Provider `custom_models`，不再读取 `ModelRegistryService` cached models，避免 GUI 无显式偏好时误选无关 Provider。`useAgentSession.createFreshSession(...)` 在 `agentSession/start` 时同步写入 `providerSelector`、`modelName`、`executionRuntime` 与 `extensionData["lime_provider_routing.v0"]`，消除首回合等待异步 `agentSession/update` 的 race。显式 `web_search=false` 仍优先禁用，显式 `search_mode` 优先保留；无显式配置时默认 `RequestToolPolicyMode::Allowed`，把 WebSearch / WebFetch 作为模型可见工具交给模型按 tool choice auto 判断，禁止按“今天 / 新闻 / 最新”等自然语言关键词提升为 `Required`。该事实证明默认主链不再依赖独立 external glue crate，也不等同于 live provider 授权后的完整 Claw streaming GUI E2E。
38. Agent session messages Electron fixture 已把 `agentSession/turn/start -> agentSession/read.detail.messages` 证据收紧到 read model 收敛后再断言。`scripts/smoke/agent-session-messages-electron-fixture-smoke.mjs` 仍使用真实 Electron preload `app_server_handle_json_lines` 与一次性 `APP_SERVER_BACKEND_MODE=external` fixture backend，但 turn accepted 后会通过 `waitForReadModel()` 轮询同一 App Server JSON-RPC `agentSession/read`，直到 `detail.messages_count=2`、`detail.messages` 同时包含用户消息和 `message.delta` 投影的 assistant 消息。最新 summary 位于 `.lime/qc/gui-evidence/agent-session-messages-electron-fixture/agent-session-messages-electron-fixture-summary.json`，显示 `ok=true`、`electronPreloadBridge=true`、`backendKinds=["turnStart"]`、`detailMessagesCount=2`、`detailMessagesLength=2`、`consoleErrors=[]`。该证据用于区分真实后端缺口与异步收敛竞态；它不使用 legacy `agent_runtime_*`、renderer mock、default mock 或 App Server mock backend，也不等同 live provider 完整 Claw streaming GUI E2E。
39. 更新提醒窗口属于 Electron Desktop Host updater 壳能力，不进入 App Server JSON-RPC 或 RuntimeCore。`src/components/app-sidebar/AppUpdateEntry.tsx -> src/lib/api/appUpdate.ts -> safeInvoke("open_update_window", { anchorRect }) -> electron/updateHost.ts -> electron/main.ts` 已把侧边栏更新按钮锚点投影到 Electron 通知窗口定位；Electron Host 只管理窗口生命周期与位置，不新增后端业务 adapter，也不引入 mock fallback。最新 GUI evidence 位于 `.lime/qc/gui-evidence/update-notification-ui/update-notification-ui-summary.json` 与 `.lime/qc/gui-evidence/update-notification-ui/update-notification-ui.png`，summary 显示 `ok=true`、窗口 `232x128`、根背景透明、外层容器 `padding=0px`、toast 为唯一白色表面且内容未裁切；该证据证明更新弹层不再居中、不再露出双重背景，但不代表 release feed / signing / install session 全链路完成。

## 2. 状态定义

| 状态           | 含义                                                                                                                                                   |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `wired`        | UI 主路径经前端 gateway -> Electron Desktop Host bridge -> App Server JSON-RPC method 进入真实 sidecar 或 in-process App Server，并有契约或 GUI 证据。 |
| `bridge-wired` | UI 仍调用 legacy 命令名，但 Electron Desktop Host bridge 已把它同步投影到 App Server current method。                                                  |
| `client-only`  | protocol / TS client 存在，但没有真实 UI caller。                                                                                                      |
| `backend-only` | Rust method 存在，但 Electron / 前端 gateway 未接。                                                                                                    |
| `partial`      | 主链已有 API / hook 或局部 GUI 证据，但缺完整业务 E2E、完整投影或旧入口退场证据。                                                                      |
| `mock-only`    | 只有测试夹具 / mock 证据，没有真实 host / App Server 证据；生产交付一律视为未完成或阻塞，不得作为可用闭环。                                            |
| `blocked`      | 当前被已知编译、runtime 或并行写集阻塞。                                                                                                               |

## 3. Agent 主路径矩阵

| 用户能力             | App Server method             | TS client                                                | Electron host                                                                       | 前端主 caller                                                                                                                                                                                                                                                                                     | 当前状态       | 下一刀                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------- | ----------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 初始化 / handshake   | `initialize` / `initialized`  | `AppServerConnection` / `src/lib/api/appServer.ts`       | `ElectronAppServerHost.#start()` 自动握手                                           | 非测试 UI 不直接调用                                                                                                                                                                                                                                                                              | `client-only`  | 保持为 host 生命周期内部能力；GUI 只需要状态投影。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| capability inventory | `capability/list`             | `listCapabilities(...)`                                  | `agent_runtime_get_tool_inventory -> METHOD_CAPABILITY_LIST`                        | Agent runtime inventory client                                                                                                                                                                                                                                                                    | `bridge-wired` | 补 contract guard，证明 Electron truth bridge 不回退 mock。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| session list         | `agentSession/list`           | `createAppServerSessionClient()`                         | `app_server_handle_json_lines -> agentSession/list`；legacy facade 仅兼容投影       | `createSessionClient().listAgentRuntimeSessions`                                                                                                                                                                                                                                                  | `wired`        | 已迁到 App Server session facade；Electron IPC 已补 list 超时 fail-closed；真实侧栏 smoke 已覆盖最近 / 归档 GUI，并验证 recent / archived 请求都返回有效 `result.sessions` 数组；P3.197 smoke 还验证 seeded remembered workspace 不会出现在 `agentSession/list.params.workspaceId`，P3.198 已补静态守卫防回流；P3.221 已用真实 Electron fixture 证明 `agentSession/update` 后 `agentSession/list` 可读回更新标题；App Server 数据源已合并 current timeline 与旧 `agent_messages` 会话。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| session read         | `agentSession/read`           | `createAppServerSessionClient()`                         | `app_server_handle_json_lines -> agentSession/read`；legacy facade 仅兼容投影       | `createSessionClient().getAgentRuntimeSession`                                                                                                                                                                                                                                                    | `wired`        | 已迁到 App Server session facade；App Server 数据源已覆盖 current timeline detail 与旧 `agent_messages` detail；缺少 `detail` 时前端 fallback 已从 `businessObjectRef.title` / `metadata.title` 投影标题，并复用 `agentSession/read -> thread_read` projection，不再返回空壳 `thread_read=null`；P3.221 已用真实 Electron fixture 证明两次 `agentSession/read.detail` 都包含 history hydrate 所需的 `turns/items/queued_turns/thread_read` 数组；`resumeSessionStartHooks` 只保留前端日志语义，不再作为 RPC 参数发送。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| thread read model    | `agentSession/read`           | `createAppServerReadModelClient()`                       | `app_server_handle_json_lines -> agentSession/read`                                 | `createThreadClient().getAgentRuntimeThreadRead`                                                                                                                                                                                                                                                  | `wired`        | 已迁到 App Server read model projection；下一刀补真实 Agent 发送后的 read refresh 业务 E2E。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| session start        | `agentSession/start`          | `createAppServerSessionClient()`                         | `app_server_handle_json_lines -> agentSession/start`                                | `createSessionClient().createAgentRuntimeSession`                                                                                                                                                                                                                                                 | `wired`        | 已迁到 App Server session facade；legacy create command 只允许作为 compat 残留等待删除。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| session update       | `agentSession/update`         | `createAppServerSessionClient()`                         | `app_server_handle_json_lines -> agentSession/update`                               | `createSessionClient().updateAgentRuntimeSession`                                                                                                                                                                                                                                                 | `wired`        | rename、provider / model、execution strategy 与 recent metadata 已走 App Server current facade；P3.237 已补真实 Electron fixture 守卫，确认内存 session 的 `archived=true` 必须 fail closed，不能伪造归档成功；P3.239 新增 `AppSidebar.current-boundary.test.ts`，锁住侧栏归档 / 恢复 UI 只能经 `updateAgentRuntimeSession({ session_id, archived }) -> createAppServerSessionClient().updateSession`，不得回流 `agent_runtime_*`、direct `safeInvoke` / `invokeCommand` 或 mock；P3.240 新增 `sessionClient.current-boundary.test.ts`，进一步锁住 `createSessionClient(...)` 必须把归档 / 恢复 / 兼容 delete 投影成 `appServerClient.updateSession({ sessionId, archived })`，且不调用注入的 legacy `invokeCommand`；P3.241 / P3.243 新增并扩展 `lime-rs/crates/app-server/tests/session_archive_jsonrpc.rs`，证明 persisted session 经 App Server JSON-RPC `agentSession/update { archived: true/false }` 可正向驱动 recent / archived list 与 fake data source read detail，并在丢弃原 `AppServer + RuntimeCore` 后重建 server 仍能从 data source 读回归档 / 恢复状态；P3.245 用真实 `app-server --stdio --backend unavailable` 子进程、真实 `LocalAppDataSource` 与临时 SQLite app data 证明归档 / 恢复状态可经 `AgentSessionOverview.archivedAt` 跨进程重启读回；P3.246 进一步用真实 Electron Desktop Host preload `app_server_handle_json_lines` 与 App Server sidecar 多次重启证明 persisted timeline archive / unarchive / readback 可经 Electron 产品链路读回。当前 `LocalAppDataSource::agentSession/read.detail` 仍未投影 `archived_at`，归档事实仍以 update response 和 list `AgentSessionOverview.archivedAt` 为准；不得把内存 session update、前端边界守卫、App Server 实例级 reopen guard 或 stdio 子进程测试单独当作完整 Electron 产品闭环。`agent_runtime_update_session` / `agent_runtime_delete_session` 只允许作为禁回流扫描对象。`agent_generate_title` 仍是 compat 边界，不能作为 update current 完成证据。 |
| turn start           | `agentSession/turn/start`     | `startTurn(...)`                                         | `app_server_handle_json_lines -> agentSession/turn/start`                           | `createThreadClient().submitAgentRuntimeTurn`                                                                                                                                                                                                                                                     | `wired`        | 已经 fail-closed 到 App Server current-only；P3.219 已补 `npm run verify:gui-smoke` 真实回归，证明 Electron Host bridge、App Server sidecar initialize、renderer 加载与 Claw 工作台 shell/composer ready 仍可用；仍需 live provider 授权后的真实发送业务 E2E。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| turn cancel          | `agentSession/turn/cancel`    | `cancelTurn(...)`                                        | `app_server_handle_json_lines -> agentSession/turn/cancel`                          | `createThreadClient().interruptAgentRuntimeTurn`                                                                                                                                                                                                                                                  | `wired`        | 已经要求 `turn_id` 并 fail-closed；仍需真实 cancel 业务 E2E。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| action respond       | `agentSession/action/respond` | `respondAction(...)`                                     | `app_server_handle_json_lines -> agentSession/action/respond`                       | `createThreadClient().respondAgentRuntimeAction`                                                                                                                                                                                                                                                  | `wired`        | 已经 fail-closed 到 App Server current-only；仍需真实 action required/respond 业务 E2E。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| streaming event      | `agentSession/event`          | `AppServerAgentEventRouter` / `drainEvents(...)`         | `app_server_handle_json_lines` 同批 notifications 与 `app_server_drain_events` 可达 | `agentRuntimeEvents` 本地 fanout 已接 API 层；`useAgentRuntimeSyncEffects` 已把当前 turn 的 App Server runtime event 接到 `refreshSessionDetail`；已有 `createThreadClient -> agentSession/event -> listenAgentRuntimeEvent -> refreshSessionDetail` 前端业务闭环测试                             | `partial`      | 下一刀补真实 GUI 发送后的 event -> read model refresh / timeline 业务 E2E。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| artifact read        | `artifact/read`               | `createAppServerArtifactClient()` / `readArtifacts(...)` | 通用 JSONL 可达                                                                     | `AgentThreadTimelineArtifactCard` 缺正文打开路径与 `useWorkspaceArtifactPreviewActions` 带 App Server scope 的 Workbench preview 已通过 `appServerArtifactClient -> AppServerClient.readArtifacts(includeContent: true)` 补齐 content；无 scope 的本地 task/session/file preview 仍走既有本地路径 | `partial`      | 补真实 GUI artifact 打开证据，并继续盘点其它 artifact 读取旁路。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| evidence export      | `evidence/export`             | `exportEvidence(...)`                                    | 通用 JSONL 可达                                                                     | `exportAgentRuntimeEvidencePack -> AppServerClient.exportEvidence -> evidence/export` 已接入；handoff / replay / analysis / review / save review 仍走 compat export command                                                                                                                       | `partial`      | 继续迁 handoff / replay / analysis / review，或先补真实 GUI evidence export E2E。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## 4. Workspace / Skill / Model 矩阵

| 用户能力                                     | App Server method                                                                                                                                                                                                                                                                                                                                                             | Electron Host bridge                                                                                                                                                           | 前端 caller                                                                                       | 当前状态  | 备注                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| workspace list/read/default/ensure           | `workspace/*`                                                                                                                                                                                                                                                                                                                                                                 | 通用 JSONL 可达；legacy `workspace_*` facade 仅兼容投影                                                                                                                        | `src/lib/api/project.ts -> AppServerClient.request`                                               | `partial` | 读 / ensure gateway 已直连 App Server method，并有契约守卫；workspace 写命令与 content 仍是 compat，仍缺真实 GUI / E2E 证据。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| skill list/read                              | `skill/list` / `skill/read`                                                                                                                                                                                                                                                                                                                                                   | 通用 JSONL 可达；legacy `list_executable_skills` / `get_skill_detail` facade 仅兼容投影                                                                                        | `src/lib/api/skill-execution.ts -> AppServerClient.request`                                       | `partial` | list/read 已直连 App Server；`scripts/skills-current-smoke.mjs` 已补 slash skill preflight 真实 GUI / bridge 证据，确认 `skill/list` 返回 `result.skills` 数组且无旧 Skill 命令、`execute_skill` 或 `agentSession/turn/start`；`skill/read` GUI 触发和 `execute_skill` current 化仍缺。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| workspace registered skills / skill bindings | `workspaceRegisteredSkills/list` / `workspaceSkillBindings/list`                                                                                                                                                                                                                                                                                                              | `app_server_handle_json_lines -> workspaceRegisteredSkills/list` / `workspaceSkillBindings/list`                                                                               | `capabilityDraftsApi.listRegisteredSkills` / `createInventoryClient().listWorkspaceSkillBindings` | `partial` | 前端 registered skills discovery 与 runtime binding readiness gateway 均已直连 App Server method；`scripts/workspace-skill-bindings-current-smoke.mjs` 已补真实 Skills 用户安装页 / 已保存技能面板 GUI / bridge 证据，确认 `workspaceRegisteredSkills/list` 返回有效 `skills` 数组、`workspaceSkillBindings/list` 返回有效 `bindings` 数组，且无旧 `capability_draft_list_registered_skills` / `agent_runtime_list_workspace_skill_bindings`。Capability Draft create / verify / register 与完整 Skills 工作台执行闭环仍缺。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| project memory aggregate read                | `projectMemory/read`                                                                                                                                                                                                                                                                                                                                                          | `app_server_handle_json_lines -> projectMemory/read`                                                                                                                           | `getProjectMemory`                                                                                | `partial` | 聚合读已直连 App Server method；`scripts/project-memory-current-smoke.mjs` 已补真实 GUI / bridge 证据，确认 Agent 首页默认工作区记忆读取 `projectMemory/read` 且无旧 `project_memory_get` 命令；角色 / 世界观 / 大纲 CRUD 仍是 compat。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| model registry/preferences/sync read         | `model/list` / `modelPreferences/list` / `modelSyncState/read` / `modelProvider/fetchModels` / `modelProviderAlias/*`                                                                                                                                                                                                                                                         | 通用 JSONL 可达；legacy `get_model_*` / `fetch_provider_models_auto` facade 仅允许停留在负向守卫和历史记录                                                                     | `src/lib/api/modelRegistry.ts -> AppServerClient.request`                                         | `partial` | `scripts/model-registry-current-smoke.mjs` 已补设置页“添加模型”真实 GUI / bridge 证据，确认 `model/list`、`modelPreferences/list`、`modelSyncState/read` 均返回有效 result 且无旧 model 命令、模型写入 / refresh side-effect 或 Agent turn；alias/provider/tier scoped gateway 与 provider live fetch 已 current 化但 GUI 逐项证据仍缺，provider ids、favorite / refresh 等仍是 compat 或待补 App Server method。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| API key provider list/catalog                | `modelProvider/*` / `modelProviderKey/*` / `modelProviderUiState/*` / `modelProviderConfig/*`                                                                                                                                                                                                                                                                                 | 通用 JSONL 可达；legacy `get_api_key_providers` / `get_system_provider_catalog` / `get_provider_ui_state` / `set_provider_ui_state` facade 已判为 `dead`，不得回流             | `src/lib/api/apiKeyProvider.ts -> AppServerClient.request`                                        | `partial` | Provider list/catalog/read/write、API Key 写入 / 启停、UI state、导入导出、连接测试与 chat test gateway 已直连 App Server；`scripts/provider-settings-current-smoke.mjs` 已补真实 GUI / bridge 证据，确认设置页读取 `modelProvider/list`、添加模型 catalog 读取 `modelProvider/catalog/list` 且无旧 Provider 命令；单 Provider、secret 写入、UI state、导入导出和连接测试的 GUI 逐项证据仍缺。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| knowledge lifecycle / context                | `knowledgePack/list` / `knowledgePack/read` / `knowledgePack/source/import` / `knowledgePack/compile` / `knowledgePack/default/set` / `knowledgePack/status/update` / `knowledgeContext/resolve` / `knowledgeContextRun/validate`                                                                                                                                              | `app_server_handle_json_lines -> knowledgePack/* / knowledgeContext*`；旧 `knowledge_*` Electron facade 已退场，Rust Tauri wrapper / DevBridge dispatcher 只剩 dead file candidate | `src/lib/api/knowledge.ts -> AppServerClient.request`                                             | `wired-current-except-live-builderRuntime` | 前端网关、App Server protocol / schema / TS client / Rust client / processor / RuntimeCore / `LocalAppDataSource -> lime_knowledge` 已覆盖全量 Knowledge current method，desktop-host Knowledge 默认 mock 已清空，Electron `knowledge_*` 白名单与 facade 已撤。`scripts/knowledge-current-smoke.mjs` 已覆盖列表 GUI / bridge 证据；`scripts/knowledge-product-e2e.mjs`、`scripts/knowledge-gui-smoke.mjs`、`scripts/knowledge-provider-e2e.mjs` 已改为 current JSON-RPC method。`knowledgePack/compile` 的普通离线编译走 current；live Builder Skill runtime binding 尚未迁入 App Server，`builderRuntime.enabled=true` 当前 fail closed，不得复活 `lime-rs/src/commands/knowledge_cmd.rs`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Agent App installed list                     | `agentAppInstalled/list`                                                                                                                                                                                                                                                                                                                                                      | 通用 JSONL 可达；legacy `agent_app_list_installed` facade 仅兼容投影                                                                                                           | `src/lib/api/agentApps.ts -> AppServerClient.request`                                             | `partial` | 已安装列表读链已直连 App Server；`scripts/agent-app/installed-current-smoke.mjs` 已补真实 GUI / bridge 证据，确认 Agent Apps 页面读取 `agentAppInstalled/list` 且无旧 `agent_app_list_installed` 命令；保存、安装、卸载和 shell side-effect 仍是 compat。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| automation scheduler/job/schedule            | `automationScheduler/config/read` / `automationScheduler/config/update` / `automationScheduler/status` / `automationJob/list` / `automationJob/read` / `automationJob/create` / `automationJob/update` / `automationJob/delete` / `automationJob/runNow` / `automationJob/health` / `automationJob/runHistory` / `automationSchedule/preview` / `automationSchedule/validate` | `app_server_handle_json_lines -> automation*`；旧 `get_automation_*` / `*_automation_job` Tauri、Electron facade、DevBridge dispatcher 和 mock surface 已判为 `dead`，不得回流 | `src/lib/api/automation.ts -> AppServerClient.request`                                            | `partial` | 自动化 scheduler/job/schedule 网关已直连 App Server method，并有前端 no legacy fallback 测试；`scripts/automation-current-smoke.mjs` 已补真实 GUI / bridge 证据，确认持续流程任务列表读取 `automationJob/list` 且无旧 automation 命令族。`automationJob/runNow` 仍是 current 缺口：协议、client、前端和 processor 已接入，但 App Server 自动化执行器尚未迁完，当前 fail closed，不回退旧 Tauri 执行器；`scripts/managed-objective-automation-smoke.mjs` 也改为只走 `automationJob/create/runNow/runHistory`，遇到该执行器缺口时失败而不是调用旧命令。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Connect deep link resolve/open               | `connectDeepLink/resolve` / `connectOpenDeepLink/resolve`                                                                                                                                                                                                                                                                                                                     | Electron protocol/open-url / 首启参数 / `second-instance` -> preload `deepLink.getCurrent/onOpenUrl` -> `app_server_handle_json_lines`                                         | `useDeepLink -> src/lib/api/connect.ts`                                                           | `wired`   | `scripts/connect-deep-link-current-smoke.mjs` 已用真实 Electron 首启参数证明 `lime://connect` 经 preload bridge 到达 `connectDeepLink/resolve` 并出现确认弹窗；`scripts/connect-open-deep-link-current-smoke.mjs` 已用真实 Electron 首启参数证明 `lime://open?kind=prompt&slug=gemini-longform-master...` 经 `connectOpenDeepLink/resolve` 到达 Agent 官网入口 banner / prompt 状态。两条证据均未观察到旧 Tauri deep-link 事件或旧 Connect 命令。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Connect API Key save / callback              | `connectRelayApiKey/save` / `connectCallback/send`                                                                                                                                                                                                                                                                                                                            | Electron preload deep link -> `app_server_handle_json_lines -> connectRelayApiKey/save / connectCallback/send`；旧 `save_relay_api_key` / `send_connect_callback` 不得回流     | `useDeepLink` / `useConnectCallback -> src/lib/api/connect.ts`                                    | `wired`   | `scripts/connect-deep-link-save-current-smoke.mjs` 已用真实 Electron 首启参数、隔离 app data 与本地 registry fixture 点击“确认添加”，证明确认后弹窗关闭，`connectDeepLink/resolve`、`connectRelayApiKey/save`、`connectCallback/send` 均经 `electron-ipc` 的 `app_server_handle_json_lines` 到达 App Server，且未观察到旧 Tauri deep-link 事件或旧 Connect 命令；本证据只覆盖 callback JSON-RPC method path，外部 HTTPS webhook 网络投递仍未验证。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Agent App UI runtime                         | `agentAppUiRuntime/start` / `agentAppUiRuntime/status` / `agentAppUiRuntime/stop`                                                                                                                                                                                                                                                                                             | 通用 JSONL 可达；legacy `agent_app_start_ui_runtime` / `agent_app_get_ui_runtime_status` / `agent_app_stop_ui_runtime` facade 仅兼容投影                                       | `src/lib/api/agentApps.ts -> AppServerClient.request`                                             | `wired`   | protocol / schema / TS client / Rust processor / RuntimeCore helper / 前端 gateway 已成组接入 App Server current method，生产 gateway 不再 safeInvoke 旧 `agent_app_*` 或 mock；`scripts/check-app-server-client-contract.mjs` 与 `src/lib/api/agentApps.test.ts` 已守住 no legacy fallback。`scripts/agent-app/ui-runtime-current-smoke.mjs` 已证明真实前端 gateway 的 status / stop / start 经 `app_server_handle_json_lines -> agentAppUiRuntime/*` 到达 App Server，且未观察到旧 `agent_app_*_ui_runtime`；`scripts/agent-app/ui-runtime-stdio-fixture-smoke.mjs` 已用临时 app data 和 fixture installed state 证明 App Server stdio current 链路能真实 start -> `/api/bootstrap` ready -> entryUrl -> status running -> stop；`scripts/agent-app/ui-runtime-electron-fixture-smoke.mjs` 已证明真实 Electron-host GUI 能从正式侧栏进入 runtime page，发出 `agentAppUiRuntime/start`，iframe 打开 `entryUrl` 并加载“内容工厂 / 工作台状态”。该 `wired` 只针对 UI 子进程生命周期，不代表 Agent App 对话 turn 完成；后续对话仍必须复用 `agentSession/* -> RuntimeCore -> AsterBackend`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Agent App task facade                        | `agentSession/start` / `agentSession/turn/start` / `agentSession/read` / `agentSession/action/respond` / `agentSession/turn/cancel`                                                                                                                                                                                                                                           | `agent_app_runtime_*` Electron Desktop Host facade -> App Server JSON-RPC -> RuntimeCore；legacy `agent_runtime_*` 只作为负向回归扫描对象，不得作为完成证据                    | `src/lib/api/agentAppRuntime.ts -> safeInvoke -> electron/hostCommands.ts`                        | `wired`   | `electron/hostCommands.ts` 已把 `agent_app_runtime_start_task/get_task/cancel_task/submit_host_response` 投影到 `agentSession/start`、`agentSession/turn/start`、`agentSession/read`、`agentSession/action/respond` 与 `agentSession/turn/cancel`，并保留 `RuntimeOptions.hostOptions.asterChatRequest` 与 `turn_config` 镜像。`scripts/agent-app/runtime-electron-fixture-smoke.mjs` 已用真实 Electron preload bridge 和显式 external backend fixture 证明四条 facade 可进入同一 App Server RuntimeCore session；`scripts/agent-app/runtime-electron-task-fixture-smoke.mjs` 已进一步证明真实 Electron renderer bridge 可完成 start -> read blocked -> host response -> read running -> cancel -> read cancelled，summary / backend log 记录 `turnStart/actionRespond/turnCancel`，且 `RuntimeOptions.hostOptions.asterChatRequest` 与 `turn_config` 镜像抵达 external backend。`P3.216` 已补 Agent App iframe SDK Host Bridge 真实 Electron fixture，证明 iframe 内 `createLimeHostBridgeCapabilityInvoker -> lime.agent.startTask/getTask/submitHostResponse/cancelTask` 经正式 runtime page / Host Bridge / Electron IPC 进入 App Server current 主链；`P3.217` 已把 RuntimeCore `artifact.snapshot` 通过 `agentSession/read.detail.thread_read.artifacts` / `detail.artifacts` 回流到 SDK task record，并用同一 SDK fixture 断言 `artifact:created`、`evidence:recorded` 与 `contentFactoryWorkspacePatch`；`P3.218` 已把 App Server read model 的 `thread_read.tool_calls` / `tool_calls` replay 成 SDK task record 的 `task:toolCall`，真实 Electron SDK fixture summary 显示 `toolEvidence.toolEventType=task:toolCall`、`toolName=WebFetch`、`readModelStatus=completed`。该证据不使用 mock backend、不回退旧 `agent_runtime_*`，但仍不等同于 live provider 完整 Claw streaming，或真实模型执行完整 WebFetch。 |

## 5. 第一竖切交付标准

第一竖切只认 `Agent turn lifecycle`，不再用“接口已存在”作为完成依据。

边界纠偏：Claw / Aster 的完整执行链仍是当前真实执行事实源；App Server `agentSession/*` 是 Claw 与 Agent App 共用的协议门面；`src/lib/api/agentRuntime/*` 只是现阶段的前端 thin client gateway，不是新的运行时核心。Agent App 后续对话 turn 必须复用 `agentSession/start + agentSession/turn/start + agentSession/event + agentSession/read`，`agentAppUiRuntime/*` 只负责 Agent App UI 子进程 start/status/stop 与 entry URL 生命周期，不承接第二套对话 runtime。后续方向是把 Claw 原完整链直迁进 App Server `RuntimeCore -> AsterBackend -> backend host`，不是在前端或 Agent App runtime 里再做一层不完整胶水；`agentRuntime` 网关必须通过 `RuntimeOptions.hostOptions.asterChatRequest` 无损携带 Rust 可反序列化的扁平 `AsterChatRequest` 等价快照，并无损投影 Claw/Aster 事件。

必须同时满足：

1. 前端主发送路径必须经 `src/lib/api/appServer.ts -> app_server_handle_json_lines -> agentSession/turn/start`，不能直接把 `agent_runtime_submit_turn` 当事实源。
2. cancel / interrupt 路径必须经 `agentSession/turn/cancel`，不能只接 submit。
3. action respond 路径必须经 `agentSession/action/respond`，不能继续只由 mock 返回 `{}`。
4. App Server event 至少能被前端 API 层消费为一次 read model 刷新或 timeline append；当前已由 `agentRuntimeEvents` 本地 fanout + `useAgentRuntimeSyncEffects` 当前 turn event 订阅触发 `refreshSessionDetail`。
5. session create / list / read / update 必须经 App Server session facade，不能继续把 `agent_runtime_create_session`、`agent_runtime_list_sessions`、`agent_runtime_get_session`、`agent_runtime_update_session` 当成前端事实源；delete 与 title generation 仍按 compat 边界单独收口。
6. `src/lib/desktop-host/agentRuntimeMocks.ts` 不得恢复 `app_server_handle_json_lines` / `app_server_drain_events` 或 submit / cancel / respond lifecycle 默认成功值；测试必须显式注入 fake。
7. Contract guard 必须能防止前端 lifecycle 回流到 `agent_runtime_submit_turn`、`agent_runtime_interrupt_turn`、`agent_runtime_respond_action`，并锁住扁平 `hostOptions.asterChatRequest`、`turn.completed` 终态收口、`turn.canceled` / interrupt 快速收口、可选 `turn.final_done` 补充终态、`text_delta_batch`、`message.completed`、`item.completed(agent_message)` 等 Claw parity 关键合同。
8. GUI 证据以 Electron 为准：`npm run smoke:electron` 已证明 Electron renderer + Desktop Host bridge + App Server sidecar initialize + Claw 工作台 shell/composer ready 可用；Rust/TS 单测不能单独证明可交付。
9. 第一竖切已有前端业务闭环测试覆盖 App Server turn notification 触发 read model refresh；尚需补真实 GUI 发送 / event refresh / thread read refresh 的业务 E2E，不能把 initialize smoke 或 hook 测试误判成完整聊天闭环。
10. `agent_runtime_submit_turn` / `agent_runtime_interrupt_turn` / `agent_runtime_respond_action`、desktop-host mock、mock-only 测试或 legacy facade 投影只能作为负向回归扫描对象，不能作为 Agent turn lifecycle 的完成证据。
11. 完整 Claw streaming GUI E2E 必须显式传入 `--allow-live-provider` 或 `LIME_ALLOW_LIVE_PROVIDER_SMOKE=1`；未授权时 smoke 必须在 `wait-health`、`prepare-runtime`、`aster_agent_init`、`modelProvider/list`、`modelProvider/testChat` 之前 fail closed，不能用 mock、无模型探针或 Provider 自动探测替代真实业务闭环。
12. 离线 code runtime fixture smoke 可以作为 localhost fixture 下的 App Server JSON-RPC runtime 证据，但必须经 `agentSession/turn/start`、`agentSession/read` 与 `evidence/export`；file checkpoint compat 只能标记为剩余缺口，不能把旧 `agent_runtime_*` runtime facade 重新算作第一竖切完成证据。
13. 用户可见发送态必须能由 App Server current `turn.completed` 终态收口；前端收到 `turn.completed` 后不能继续等待 `turn.final_done` 或长 timeout。若 `turn.completed` 携带 assistant 正文则直接完成；若正文为空但已有工具 / 产物等真实完成信号则软完成；若正文为空且没有真实完成信号则立即落失败态并释放输入框。`turn.final_done` 只能作为兼容 / 补充终态，不得成为 current GUI 退出发送态的唯一条件。
14. 从本地存储恢复出的 session id 在发送前必须先被 App Server `agentSession/read` 确认存在且属于当前 workspace；确认失败时只能丢弃本地快照并创建新的 App Server session，不能继续轮询已不存在的 session 或向 stale session 提交 turn。
15. Agent App task facade 进入 `agentSession/turn/start` 时必须携带 `RuntimeOptions.hostOptions.asterChatRequest` 和 `turn_config` 镜像；前者恢复 Claw/Aster 扁平请求，后者供外部 App Server backend 读取 provider_config、system_prompt、reasoning、sandbox 等配置，不能再回到 `host_options: None`、`build_queued_turn_task` 或 `agent_runtime_submit_turn` 作为正向证据。
16. 任一前端命令组迁到 current 后，同轮清理旧 Tauri wrapper；`lime-rs/src/commands/**` 暂留对象只能是已登记 blocker 的 cleanup residue，不能作为 `wired` / `done` 的正向证据。
17. standalone external backend 必须读取 flat `hostOptions.asterChatRequest` 与 `turn_config` 镜像，必须复用 `stream_reply_with_policy(...)` 和请求级工具策略；禁止把 `.complete(&system, &messages, &[])`、空工具数组或只合成 `message.delta` 的 completion 路径重新作为 App Server Agent turn 完成证据。

## 6. 下一刀顺序

1. `P3.125`：已补 Electron GUI smoke 入口，证明 renderer + Desktop Host bridge + App Server sidecar initialize 可用，同时保留完整 `electron:build` 的 renderer typecheck。
2. `P3.126`：session create / list / read 已先迁到 App Server session facade。
3. `P3.128`：已补 `agentSession/event -> refreshSessionDetail -> thread read model` 的前端业务闭环测试；下一步仍要补真实 GUI 发送 E2E。
4. `P3.138`：evidence pack export 已迁到 App Server `evidence/export`；下一刀只迁有等价 App Server method 的 evidence/artifact UI，不把 handoff / replay / analysis / review 伪装成完成。
5. `P3.130`：继续收更宽 desktop-host / DevBridge compat facade，尤其是 session legacy command 与 default mock 残留，确保生产 `invoke` 不再用 default mock 伪成功。
6. `P3.149`：workspace/project 读与 ensure gateway 已直连 App Server；下一刀继续优先迁有完整 App Server 等价 method 的 model/provider、skill 或 knowledge 读链，或补真实 GUI workspace 证据。
7. `P3.150` / `P3.173`：model registry / preferences / sync / alias 读链已直连 App Server；P3.173 只补真实设置页“添加模型”面板读取 `model/list`、`modelPreferences/list`、`modelSyncState/read` 的 GUI / bridge 证据。下一刀继续补 alias/provider/tier scoped GUI 证据，或迁 model refresh / favorite / live fetch 等仍有完整 App Server method 缺口的写面。
8. `P3.151` / `P3.169`：knowledge pack list 读链已直连 App Server，且已补真实 GUI 项目资料列表证据；下一刀继续迁其它已有等价 method 的只读面，或补 skill / workspace skill bindings 等 current 读链 GUI 证据。
9. `P3.153` / `P3.172`：skill list/read 读链已直连 App Server，且已补真实首页 slash skill preflight 读取 `skill/list` 的 GUI / bridge 证据；下一刀继续补 `skill/read` 详情 GUI 或 `execute_skill` 的 App Server side-effect 主链。
10. `P3.154` / `P3.168`：Agent App installed list 读链已直连 App Server，且已补真实 GUI Agent App 列表证据；下一刀继续补 knowledge / skill 等 current 读链 GUI 证据。
11. `P3.155` / `P3.167`：automation job list 读链已直连 App Server，且已补真实 GUI automation 列表证据；下一刀继续迁有等价 method 的只读面，或补 knowledge / Agent App / skill 等 current 读链 GUI 证据。
12. `P3.156` / `P3.162` / `P3.166`：Electron IPC 已补 App Server `agentSession/list` 请求超时 fail-closed，真实侧栏 GUI 已证明最近对话 / 归档读取经 `app_server_handle_json_lines -> agentSession/list`，且 App Server data source 已覆盖 current timeline 与旧 `agent_messages` 会话；下一刀继续补 knowledge / Agent App / skill 等 current 读链 GUI 证据。
13. `P3.157` / `P3.160`：API key provider list/catalog 读链已直连 App Server，且已补真实 GUI provider 设置页读取证据；下一刀继续补其它 current 读链的 GUI 证据，或迁其它已有等价 method 的只读面。
14. `P3.158` / `P3.170` / `P3.174` / `P3.180`：已把 session list、workspace registered skills、workspace skill bindings 与 project memory current gateway 的 method 引用收敛到 protocol 常量；project memory 聚合读、registered skills discovery 与 workspace skill bindings readiness 读链均已补真实 GUI / bridge 证据。下一刀继续迁 Capability Draft create / verify / register，或把剩余已 current 化 gateway 的协议常量 / DTO 复用补齐。
15. `P3.177`：settings broad current smoke 已证明设置页 15 个入口 / 24 个首屏与子页在真实 Electron-host DevBridge 下不再出现 unsupported / safeInvoke / invoke response error；下一刀仍应按单能力继续迁剩余 Desktop compat 命令，不把 broad smoke 当成全设置页 current 完成证据。
16. `P3.191` / `P3.192`：默认 Electron smoke 已升级为 Claw / Agent 工作台 shell + composer ready 门禁，并修复 packaged renderer `file://` 资源相对路径；settings broad smoke 已确认 About updater 诊断 warning 不再污染 console。下一刀仍是完整 Claw streaming GUI E2E：必须显式 live provider 授权，验证真实发送、事件刷新和 `agentSession/read` 回流，不把默认入口 smoke 当成 streaming 完成证据。
17. `P3.193`：code runtime fixture smoke 的 session / update / turn / read / evidence 已改走 App Server JSON-RPC current 主链，并补 contract guard 禁止旧 runtime facade 回流；下一刀若继续补离线 fixture，应优先为 checkpoint/diff 补 App Server current method，或在 live provider 授权后跑完整 Claw streaming GUI E2E。
18. `P3.194`：完整 Claw streaming GUI E2E 入口已收紧到 Electron-host DevBridge 证据，未授权时在 `wait-health` 前 fail closed；下一刀仍是拿到 live provider 授权后运行 `scripts/claw-chat-ready-streaming-smoke.mjs --allow-live-provider`，按真实失败修复发送、streaming、event refresh 或 read model 回流。
19. `P3.196`：已修复 Claw 首 token 等待态被 `turn.completed` 误投影成“已完成”的 GUI 状态问题；任务卡与输入栏现在复用同一判定，在 assistant 仍等待最终输出时保持 `running` 且不展示 `completedAt`。下一刀仍是 live provider 授权后跑完整 Claw streaming GUI E2E，验证真实发送、streaming、event refresh 与 `agentSession/read` 回流。
20. `P3.197`：已收掉 stale `agent_last_project_id=workspace-1` 污染最近 / 归档列表的前端过滤问题；侧栏只在 active Agent project 显式存在时传 `agentSession/list.workspaceId`，Agent 工作台对未验证 remembered project 暂缓 runtime workspace，真实侧栏 smoke 已验证 `seededWorkspaceFilterRequestCount=0`。下一刀仍回到 live provider 授权后的完整 Claw streaming GUI E2E。
21. `P3.198`：已把 sidebar session current smoke 的 seeded workspace 禁污染、current App Server list path、legacy list 禁止和失败 summary options 固化为静态守卫；下一刀不继续打磨 session list，回到 live provider 授权后的完整 Claw streaming GUI E2E。
22. `P3.200` / `P3.203`：已补 Agent App UI runtime stdio fixture 子进程生命周期证据与 Electron-host GUI fixture 证据，证明 App Server `RuntimeCore -> agentAppUiRuntime/* -> fixture npm dev server` 可真实启动 / 探测 / 停止，且前端正式侧栏 / runtime page / iframe surface 能真实打开 `entryUrl`。下一刀不再继续打磨 UI runtime 生命周期，应回到 Agent App 后续对话 turn 复用 `agentSession/* -> RuntimeCore -> AsterBackend`，或在 live provider 授权后跑完整 Claw streaming GUI E2E。
23. `P3.199` / `P3.202` / `P3.204`：Connect deep link 首启参数、`lime://open` 官网入口、确认保存 API Key 与 callback method path 均已补真实 Electron GUI bridge 证据；`lime://connect` 现在经 Electron preload `deepLink.getCurrent/onOpenUrl` 到达 `app_server_handle_json_lines -> connectDeepLink/resolve -> connectRelayApiKey/save -> connectCallback/send`，`lime://open` 到达 `connectOpenDeepLink/resolve` 并落到 Agent 官网入口页面状态；contract guard 禁止旧 Tauri deep-link 事件 / Connect 命令回流。下一刀若继续 Connect，应补可信 HTTPS webhook 网络投递 fixture或真实 Relay registry 网络刷新证据，不要写回旧 Tauri command、renderer mock 或 Electron 业务 adapter。
24. `P3.201`：已修复 Electron `safeInvoke` result envelope 解包、App Server workspace 启动真相超时 profile 和 stale restored session 发送前确认；下一刀仍回到真实 settings / Electron smoke 与完整 Claw streaming GUI E2E，验证设置页 current response 不再误判、Claw 不再复用 App Server 中不存在的 session 后无输出。
25. `P3.207`：Agent App task facade 已完成 Claw/Aster request parity，`turnConfig` 会透传到 `hostOptions.asterChatRequest` 与 `turn_config` 镜像，避免 Agent App 对话 turn 只进入 App Server 半截链路。下一刀仍需真实 Agent App 对话 turn GUI / live provider 或 fixture E2E 证明 `agentSession/event`、`agentSession/read` 与 evidence 回流，不能把 UI runtime iframe 证据当成对话 runtime 完成。
26. `P3.212`：standalone external backend 已复用 `AsterAgentState + stream_reply_with_policy` 和 request tool policy，不再走空工具 completion；下一刀仍是 live provider 授权后的完整 Claw streaming GUI E2E，或 Agent App 对话 turn fixture，证明真实发送、tool events、`agentSession/event`、`agentSession/read` 与 evidence 回流。
27. `P3.213`：Agent App task facade 已补真实 Electron fixture 证据，证明 `agent_app_runtime_*` 四条 Desktop Host facade 可进入同一 App Server RuntimeCore session，并保留 `hostOptions.asterChatRequest.turn_config`。
28. `P3.214`：Agent App task facade 已补更严格的真实 Electron task fixture 证据，证明 start / read blocked / host response / read running / cancel / read cancelled 均经 Electron preload bridge -> App Server RuntimeCore -> external backend fixture 打通。下一刀应继续补 Agent App SDK / UI 自动触发 task、tool runtime / evidence 回流，或在 live provider 授权后跑完整 Claw streaming GUI E2E。
29. `P3.216`：Agent App iframe SDK Host Bridge 已补真实 Electron fixture 证据，证明 iframe 内 `createLimeHostBridgeCapabilityInvoker -> lime.agent.startTask/getTask/submitHostResponse/cancelTask` 经 `AgentAppRuntimePage Host Bridge -> AgentRuntimeCapabilityHost -> src/lib/api/agentAppRuntime.ts -> safeInvoke -> Electron Desktop Host IPC -> App Server JSON-RPC -> RuntimeCore/backend` 打通；summary 位于 `.lime/qc/gui-evidence/agent-app-runtime-electron-sdk-fixture/agent-app-runtime-electron-sdk-fixture-summary.json`，显示真实 runtime page / iframe 可见、动态 `agent-app-turn-*` turn id 贯穿 start / host response / cancel，backend log 记录 `turnStart / actionRespond / turnCancel`。该证据不使用 renderer direct invoke、legacy `agent_runtime_*`、DevBridge mock、default mock 或 App Server mock backend；下一刀继续补 tool runtime / evidence 回流，或在 live provider 授权后跑完整 Claw streaming GUI E2E。
30. `P3.217`：Agent App SDK artifact / evidence read model 回流已补真实 Electron fixture 证据。App Server `agentSession/read` 现在把 RuntimeCore artifact summaries 同步暴露到 `detail.thread_read.artifacts` 与 `detail.artifacts`，SDK task record 可从 current read model replay `artifact:created` 与 `evidence:recorded`；summary 位于 `.lime/qc/gui-evidence/agent-app-runtime-electron-sdk-fixture/agent-app-runtime-electron-sdk-fixture-summary.json`，显示 `artifactEvidence.artifactEventType=artifact:created`、`artifactEvidence.evidenceEventType=evidence:recorded`、`resultArtifactCount=1`。下一刀不再打磨 fixture lifecycle，应转向完整 tool runtime 执行证据或 live provider 授权后的 Claw streaming GUI E2E。
31. `P3.218`：Agent App SDK tool call read model replay 已补真实 Electron fixture 证据。`AgentRuntimeCapabilityHost` 现在从 App Server `agentSession/read` detail 的 `thread_read.tool_calls` / `tool_calls` 读取工具事实，并 replay 为 SDK task record 的 `task:toolCall`；summary 位于 `.lime/qc/gui-evidence/agent-app-runtime-electron-sdk-fixture/agent-app-runtime-electron-sdk-fixture-summary.json`，显示 `toolEvidence.toolEventType=task:toolCall`、`toolEvidence.toolName=WebFetch`、`toolEvidence.outputPreview=fetched https://example.com/agent-app-sdk-fixture`、`toolEvidence.readModelStatus=completed`、`toolEvidence.success=true`。下一刀应转向 live provider 授权后的完整 Claw streaming GUI E2E，或把真实 tool runtime 执行从 fixture evidence 扩到 live turn 证据。
32. `P3.219`：P3.218 后的默认 Electron GUI smoke 已通过，`npm run verify:gui-smoke` 覆盖 renderer smoke build、Electron main/preload build、`packages/app-server-client` build、Electron host typecheck、App Server sidecar build 与 `scripts/electron/smoke.mjs`，输出确认 `app-server initialized protocol=appserver.v0 version=1.59.0` 与 `claw workbench shell ready`。该证据只证明默认 Electron GUI 主路径仍可启动，不等同 live provider 完整 Claw streaming、真实发送、tool runtime live 执行或 `agentSession/event -> agentSession/read` 业务刷新。
33. `P3.220`：未授权 Claw streaming smoke gate 已回归验证，`npm run smoke:claw-chat-ready-streaming -- --timeout-ms 30000` 按预期在 live provider gate 快速失败，并提示必须显式传入 `--allow-live-provider` 或设置 `LIME_ALLOW_LIVE_PROVIDER_SMOKE=1 / LIME_REAL_API_TEST=1`；该运行没有进入 `wait-health`、DevBridge health、`aster_agent_init`、`modelProvider/list` 或 `modelProvider/testChat`。完整 Claw streaming GUI E2E 仍等待 live provider 授权，不能用 mock、无模型探针或 external backend fixture 代替。
34. `P3.221` / `P3.237`：Agent session history Electron fixture 已补真实 current 证据。`scripts/electron/session-history-fixture-smoke.mjs` 使用真实 Electron preload `app_server_handle_json_lines` 与 `APP_SERVER_BACKEND_MODE=unavailable`，执行 `initialize -> agentSession/start -> agentSession/read -> agentSession/update(title/provider/model) -> agentSession/update(archived=true) -> agentSession/read -> agentSession/list`，summary 位于 `.lime/qc/gui-evidence/agent-session-history-electron-fixture/agent-session-history-electron-fixture-summary.json`。P3.221 证明两次 `read.detail` 都包含 history hydrate 所需的 `turns/items/queued_turns/thread_read` 且 list 可读回更新标题；P3.237 进一步锁住内存 session 的 `archived=true` 必须 fail closed，错误文案包含 `agentSession/update archived is only supported for persisted current timeline sessions`，并确认 `listedSessionArchivedAt == null`。该证据不触发 `agentSession/turn/start`，不调用模型后端，不使用 legacy `agent_runtime_*`、mock backend 或 renderer mock fallback；persisted current timeline archive / unarchive / sidecar restart readback 已由 P3.246 补真实闭环。
35. `P3.239`：侧栏归档 / 恢复入口已补 current boundary 静态守卫。`src/components/AppSidebar.current-boundary.test.ts` 锁住 `handleToggleSessionArchive` 只能经 `updateAgentRuntimeSession({ session_id, archived })`，session gateway update / delete 只能委托 `createAppServerSessionClient()`，App Server gateway 必须使用 `agentSession/list/read/update` 事实源；该守卫只防回流，不证明 persisted timeline archive / unarchive 成功。
36. `P3.240`：session gateway 归档请求投影已补 current boundary 守卫。`src/lib/api/agentRuntime/sessionClient.current-boundary.test.ts` 锁住 `createSessionClient(...)` 对归档、恢复和兼容 delete 的请求必须投影为 `appServerClient.updateSession({ sessionId, archived })`，并确认注入的 legacy `invokeCommand` 不被调用；该守卫补齐侧栏 UI 与 App Server session gateway 之间的中间层证据，仍不证明 persisted timeline archive / unarchive 成功。
37. `P3.241`：persisted session archive / unarchive 已补 App Server JSON-RPC 正向守卫。`lime-rs/crates/app-server/tests/session_archive_jsonrpc.rs` 通过公开 `AppServer::handle_json_line(...)` 验证 `agentSession/update { archived: true }` 后 recent list 为空、`archivedOnly` list 出现、`agentSession/read.detail.archived_at` 可读，随后 `archived: false` 恢复 recent list 且 archived list 为空；该证据证明 App Server JSON-RPC / RuntimeCore / AppDataSource 边界打通，不等同真实 Electron sidecar 重启后 readback。
38. `P3.243`：persisted session archive / unarchive 已补 App Server 实例重建 readback 守卫。同一测试现在用可 reopen 的持久化 store seam，在 archive 后重建 `AppServer + RuntimeCore` 并验证 recent / archived list 与 `agentSession/read.detail.archived_at` 仍一致，恢复后再次重建并验证 recent list 恢复；该证据防止归档状态只留在 runtime 内存实例里，但仍不等同真实 Electron sidecar 进程重启后的 SQLite app data readback。
39. `P3.245`：persisted session archive / unarchive 已补真实 App Server stdio 子进程重启 readback 守卫。同一测试 seed 临时 SQLite current timeline session，启动真实 `app-server --stdio --backend unavailable`，执行 archive 后验证 recent list 为空、archivedOnly list 通过 `AgentSessionOverview.archivedAt` 读回；关闭并第二次启动同一 app data 后 archivedOnly list 仍保持归档状态，再 unarchive；第三次启动后 recent list 恢复、archivedOnly list 为空。该证据证明 `App Server JSON-RPC -> RuntimeCore -> LocalAppDataSource(SQLite)` 的进程级持久化链路，不使用 Electron、legacy `agent_runtime_*` 或 mock backend；真实 Electron Desktop Host sidecar restart 证据见 P3.246。
40. `P3.246`：persisted session archive / unarchive 已补真实 Electron Desktop Host sidecar restart readback 守卫。`scripts/electron/session-history-fixture-smoke.mjs` 在同一隔离 app data 中 seed persisted current timeline session 后，通过真实 Electron preload `app_server_handle_json_lines` 和 `APP_SERVER_BACKEND_MODE=unavailable` 依次验证 archive、重启后 archivedOnly readback、unarchive、再次重启后 recent readback；summary 显示 `sidecarRestartReadback=true`、`persistedArchiveReopenSummary.archivedAfterRestartSession.archivedAt` 有值、`persistedUnarchiveReopenSummary.recentAfterRestartSession.sessionId=agent-session-history-electron-persisted`。该证据不触发 `agentSession/turn/start`，不调用模型后端，不使用 external backend、App Server mock backend、renderer mock fallback 或 legacy `agent_runtime_*`。
41. `P3.249`：Claw current GUI 主链已在当前并行写集下完成复验。`npm run smoke:agent-runtime-current-fixture` 通过，覆盖 Agent Runtime current fixture 聚合 guard；真实 Electron `npm run smoke:claw-chat-current-fixture -- --timeout-ms 180000` 通过，summary 位于 `.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`，显示 `ok=true`、`consoleErrors=[]`、`readModelCompleted.latestTurnStatus=completed`、`includesPrompt=true` 与 `includesAssistantSummary=true`。该证据仍是 non-live external fixture backend，不等同 live Provider streaming / WebSearch 实际联网 E2E。
42. `P3.250`：Agent App iframe SDK / tool / evidence current 证据已在 `scripts/agent-app/` 迁移后完成复验。真实 Electron `node "scripts/agent-app/runtime-electron-sdk-fixture-smoke.mjs" --timeout-ms 180000` 通过，summary 位于 `.lime/qc/gui-evidence/agent-app-runtime-electron-sdk-fixture/agent-app-runtime-electron-sdk-fixture-summary.json`，显示 `ok=true`、`electronPreloadBridge=true`、`appServerHandleJsonLinesSeen=true`、`artifactEvidence.artifactEventType=artifact:created`、`artifactEvidence.evidenceEventType=evidence:recorded`、`toolEvidence.toolEventType=task:toolCall`、`toolEvidence.toolName=WebFetch`、`toolEvidence.readModelStatus=completed`、`toolEvidence.success=true` 与 `consoleErrors=[]`。该证据证明迁移后的 Agent App runtime page / iframe SDK 仍经 Electron Desktop Host IPC -> App Server JSON-RPC -> RuntimeCore/backend 回流 artifact / evidence / tool facts；external backend 只是一次性 fixture，不等同 live provider、真实 WebFetch 联网或完整 Claw streaming。
43. `P3.251`：Agent Runtime current 聚合回归已在当前并行写集下再次复验。`npm run smoke:agent-runtime-current-fixture` 通过，覆盖历史 / 缓存终态恢复、流式完成与运行态收尾、Claw MessageList 终态 UI，以及 Electron session history / code artifact / Claw GUI fixture 静态守卫；输出显示 `liveProviderUsed=false`。该证据证明非 live current fixture guard 未被 P3.247-P3.250 的 runtime backend、归档 GUI、Agent App SDK 与脚本迁移写集打断；它不等同 live Provider streaming / WebSearch 实际联网 E2E。
44. `P3.253`：Claw current GUI 主链已在 P3.252 发布/updater 脚本治理后再次复验。真实 Electron `npm run smoke:claw-chat-current-fixture -- --timeout-ms 180000` 通过，summary 位于 `.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`，显示 `ok=true`、`consoleErrors=[]`、`guiCompleted.hasPrompt=true`、`guiCompleted.hasDoneText=true`、`guiCompleted.textareaVisible=true`、`guiCompleted.textareaDisabled=false`、`readModelCompleted.latestTurnStatus=completed`、`includesPrompt=true` 与 `includesAssistantSummary=true`。该证据仍是 non-live external fixture backend，不等同 live Provider streaming / WebSearch 实际联网 E2E。
45. `P3.254`：contract / governance full gate 已在当前工作树下复验。`npm run test:contracts` 完整通过，App Server client contract 显示 `207 checks`，command contracts 显示 `frontend commands=389`、`Electron host commands=107`、`mock priority commands: 0`、`DevBridge truth commands=148`，scripts governance 显示 `rootFiles=119 retired=0 untrackedNew=0`，Harness cleanup 和 docs boundary 均通过。该证据证明命令契约、mock 禁回流、脚本治理与文档边界未漂移；它不等同 live Provider streaming / WebSearch 实际联网 E2E。
46. `P3.256`：默认 Electron GUI smoke 与未授权 live gate 已在当前并行工作树下复验。`npm run verify:gui-smoke` 通过，覆盖 Electron renderer smoke build、Electron host typecheck、`packages/app-server-client` build、App Server sidecar assets 与 `scripts/electron/smoke.mjs`，输出确认 renderer loaded、App Server initialized `protocol=appserver.v0 version=1.59.0`、Claw workbench shell ready；`npm run smoke:claw-chat-ready-streaming -- --timeout-ms 30000` 按预期在 live Provider 授权检查处 fail closed，提示必须显式 `--allow-live-provider` 或设置 `LIME_ALLOW_LIVE_PROVIDER_SMOKE=1 / LIME_REAL_API_TEST=1`。该证据证明默认 Electron Desktop Host + App Server sidecar + Claw composer 准备态未回退，并证明未授权时不会绕过 live Provider gate；它仍不等同完整 live Provider streaming、WebSearch/WebFetch 实际联网、tool runtime live 执行或 `agentSession/event -> agentSession/read` 业务刷新。
47. `P3.258`：code artifact workbench 工具轨迹 GUI 可见证据已在 current 源码和默认 dist 两条口径下复验。失败归因不是 App Server read model 缺工具事实，而是旧 `file://dist/index.html` 运行未重建 bundle时 GUI 仍停在“正在恢复生成会话...”，且 smoke 旧等待条件会只凭 workbench tab / message shell 过早进入工作台。`scripts/electron/code-artifact-workbench-fixture-smoke.mjs` 已收紧 hydrate 等待：恢复中状态不得通过，且必须看到用户 prompt、assistant 文本、tool evidence、artifact 或代码事实之一。真实 Electron dev server 口径 `npm run smoke:code-artifact-workbench-electron-fixture -- --app-url "http://127.0.0.1:1420/" --timeout-ms 180000` 通过；重建 `npm run build:renderer:electron:smoke` 后，默认 `file://dist/index.html` 口径 `npm run smoke:code-artifact-workbench-electron-fixture -- --timeout-ms 180000` 也通过，最终 summary 显示 `appUrl=null`、`toolTimelinePersisted=true`、`threadReadToolCallCount=1`、`fixtureToolCall.tool_name=WebFetch`、`guiToolTimelineEvidencePresent=true`、`assertions.toolTimelineEvidencePresent=true`、`isRestoringSession=false`、`consoleErrors=[]`、`pageErrors=[]`。该证据仍是 non-live external fixture backend，不证明 live Provider streaming 或真实 WebFetch 联网。
48. `P3.260`：Agent Runtime current 聚合和 Claw GUI current 主链已在 P3.258 等待收紧后复验。`npm run smoke:agent-runtime-current-fixture` 通过，覆盖 history/cache hydration、stream terminal 收尾、Claw MessageList 终态 UI 与 Electron/App Server fixture guard，`liveProviderUsed=false`；真实 Electron `npm run smoke:claw-chat-current-fixture -- --timeout-ms 180000` 通过，summary 显示 `usedCurrentSessionStart=true`、`usedCurrentSessionRead=true`、`usedCurrentSessionList=true`、`externalFixtureBackendUsed=true`、`fixturePromptReachedBackend=true`、`guiUserMessageVisible=true`、`guiAssistantOutputVisible=true`、`guiInputRemainsReady=true`、`guiNotStuckStreaming=true`、`readModelCompleted=true`、`noInvokeErrors=true`、`noConsoleErrors=true`、`noEpochFallbackTitle=true`。该证据证明 non-live external fixture 下普通 Claw GUI turn 仍经 Electron Desktop Host IPC -> App Server JSON-RPC -> RuntimeCore/backend 打通；它仍不等同 live Provider streaming / WebSearch-WebFetch 实际联网 E2E。
49. `P3.261`：contract / governance 与默认 Electron GUI smoke 已在 P3.258-P3.260 后复验。`npm run test:contracts` 完整通过，App Server client contract 显示 `207 checks`，command contracts 显示 `frontend commands=389`、`Electron host commands=107`、`mock priority commands: 0`、`DevBridge truth commands=148`，scripts governance 显示 `rootFiles=119 retired=0 untrackedNew=0`，docs boundary 通过；`npm run verify:gui-smoke` 也通过，覆盖 Electron renderer smoke build、Electron host typecheck、`packages/app-server-client` build、App Server sidecar assets 与真实 `scripts/electron/smoke.mjs`，输出确认 renderer loaded、App Server `protocol=appserver.v0 version=1.59.0` initialized、Claw workbench shell ready。该证据证明命令契约、mock 禁回流、Electron Desktop Host + App Server sidecar + Claw shell 准备态未漂移；它不发送 live turn、不调用正式 Provider，也不替代 live Provider streaming / WebSearch-WebFetch 实际联网 E2E。
50. `P3.262`：完整 Claw live streaming E2E 的 pass 条件已补 WebSearch/WebFetch 工具事实门禁。`scripts/claw-chat-ready-streaming-smoke.mjs` 现在在显式 live 授权后会新增一个 `submit-live-web-tools-turn`，要求模型使用 `WebSearch` 查询公开新闻并继续用 `WebFetch` 打开来源页面；verdict 只在 App Server `agentSession/read` 投影中同时观察到完成态 `WebSearch` 和 `WebFetch` tool call 时才可通过。`scripts/lib/live-provider-smoke-gate.test.mjs` 已补静态守卫，未授权 `npm run smoke:claw-chat-ready-streaming -- --timeout-ms 30000` 仍在 `wait-health` 前 fail closed。本证据只提升 live 完成标准，尚未运行真实 Provider；下一刀必须在明确授权后运行 `npm run smoke:claw-chat-ready-streaming -- --allow-live-provider --timeout-ms 180000`，并按真实失败修复 provider/tool/runtime/read model 问题。
51. `P3.265`：P3.262 的 live WebSearch/WebFetch 工具事实门禁已收紧为 turn-scoped。`scripts/claw-chat-ready-streaming-smoke.mjs` 现在从 App Server `agentSession/read` 投影中提取 `turn_id` / `turnId`，并只接受本次 `liveWebTurnId` 下完成态 `WebSearch` 与 `WebFetch`；`requiredForTurn` / `allRequiredCompletedForTurn` 是 verdict 条件，历史 session 中其他 turn 的工具调用不能让本次 live 工具 turn 误通过。`scripts/lib/live-provider-smoke-gate.test.mjs` 已补静态守卫。本证据仍不代表真实 Provider 已运行，下一刀仍是显式授权后跑 live streaming / WebSearch-WebFetch 实际联网 E2E。
52. `P3.268`：P3.265 的 turn-scoped live WebSearch/WebFetch 判定已从 smoke 内联逻辑抽到 `scripts/lib/claw-chat-live-web-tool-evidence.mjs`，并补 `scripts/lib/claw-chat-live-web-tool-evidence.test.mjs` 行为单测。测试明确证明旧 turn 的完成态工具调用不能让本次 `liveWebTurnId` 通过，且支持 App Server read model 中 `turn_id` / `turnId` 与 inherited turn scope。真实 live Provider 仍未运行，下一刀仍需授权后跑完整联网 E2E。
53. `P3.269`：live WebSearch/WebFetch helper 已补 App Server JSON-RPC / read model envelope 覆盖。`collectToolCallsFromValue(...)` 现在会递归 `result/detail/session/session_detail/sessionDetail`，行为测试锁住 `agentSession/read` 常见 `result.detail.thread_read.tool_calls` 形状，避免授权后因只传入 JSON-RPC response / read result 外壳而漏掉工具事实。真实 live Provider 仍未运行，下一刀仍需授权后跑完整联网 E2E。
54. `P3.271`：live Provider gate 已从根层脚本扫描扩展为递归扫描 `scripts/**/*.mjs`。`scripts/lib/live-provider-smoke-gate.test.mjs` 现在覆盖子目录 smoke，排除测试文件、`scripts/lib/**`、`check-*` 与测试夹具路径；递归扫描下当前脚本仍无未授权 `agent_runtime_submit_turn` / App Server `modelProvider/testChat` / `knowledge_compile_pack` 回流。真实 live Provider 仍未运行，下一刀仍需授权后跑完整联网 E2E。
55. `P3.272`：live WebSearch/WebFetch turn 已补自身 provider/model 与 fast routing verdict。`scripts/claw-chat-ready-streaming-smoke.mjs` 现在要求 Web 工具 turn 的 `turn_config` 同样命中 explicit provider/model，且 `metadata.harness.fast_response_routing` 未开启；这些断言已纳入最终 pass 条件。真实 live Provider 仍未运行，下一刀仍需授权后跑完整联网 E2E。
56. `P3.273`：`claw-chat-ready-streaming` 的 final `threadRead` summary 已保留 App Server current `detail.thread_read`。`appServerThreadReadFromSessionRead(...)` 现在优先展开 `detail.thread_read/threadRead`，避免把工具事实、artifact 和 routing 证据降级成由 turns 重建的最小投影。真实 live Provider 仍未运行，下一刀仍需授权后跑完整联网 E2E。
57. `P3.274`：live WebSearch/WebFetch verdict 已补工具输出非空证据。`scripts/lib/claw-chat-live-web-tool-evidence.mjs` 现在计算 `outputPresent` / `allRequiredOutputPresentForTurn`，`scripts/claw-chat-ready-streaming-smoke.mjs` 要求本次 live Web turn 下 WebSearch/WebFetch 均 completed 且有输出预览。真实 live Provider 仍未运行，下一刀仍需授权后跑完整联网 E2E。
58. `P3.276`：`claw-chat-ready-streaming` summary 已显式标注 live E2E 证据边界。`summary.e2eBoundary` 固定 runner 为 `playwright-chromium`，browser automation 为 `chromium.launchPersistentContext`，证明路径为 `Chromium GUI -> DevBridge -> Electron Desktop Host IPC -> App Server JSON-RPC -> RuntimeCore/backend`，并明确 `electronRendererIpcE2e=false` / `electronLaunch=false`。这防止把 DevBridge(electron-host transport) live smoke 误称为 Playwright `_electron.launch()` renderer IPC E2E；真实 Electron renderer IPC 证据仍应由独立 Electron smoke / fixture 承接。真实 live Provider 仍未运行，下一刀仍需授权后跑完整联网 E2E。
59. `P3.280`：live WebSearch/WebFetch 输出门禁已完成非 live 复验。`claw-chat-ready-streaming` 对本次 `liveWebTurnId` 的 read model settle 条件保持为 `allRequiredOutputPresentForTurn`，必须看到同一 turn 下 `WebSearch` 与 `WebFetch` 均 completed 且有非空输出预览才允许 live verdict pass；未授权 `npm run smoke:claw-chat-ready-streaming -- --timeout-ms 30000` 仍在 DevBridge / Provider 前 fail closed。真实 live Provider 仍未运行，完整目标下一刀仍是授权后跑 `npm run smoke:claw-chat-ready-streaming -- --allow-live-provider --timeout-ms 180000`。
60. `P3.282`：Claw current Electron IPC non-live 主链已在当前并行脏工作树下复验。`npm run smoke:agent-runtime-current-fixture`、`npm run smoke:claw-chat-current-fixture -- --scenario cancel --timeout-ms 180000`、`npm run smoke:claw-chat-current-fixture -- --timeout-ms 180000` 与 `npm run test:contracts` 均通过；完成态 fixture summary 显示真实 Electron preload bridge、App Server JSON-RPC、current session start/read/list、GUI 用户消息、assistant 输出、输入框恢复与 read model completed 均可用，且 `liveProviderNotUsed=true`。该证据证明 non-live external fixture 下 current Electron 主链未被版本 / release / WebSearch policy 并行写集打断；真实 live Provider / WebSearch-WebFetch 联网 E2E 仍未运行。
61. `P3.284`：默认 Electron GUI smoke 已在当前 `1.60.0` 并行工作树下通过。`npm run verify:gui-smoke` 覆盖 renderer Electron smoke build、Electron host typecheck、App Server sidecar assets、真实 Electron 启动、App Server initialize 和 Claw workbench shell/composer ready；输出确认 `app-server initialized protocol=appserver.v0 version=1.60.0` 与 `claw workbench shell ready`。该证据证明 Electron Desktop Host + packaged App Server sidecar current 边界仍可启动，不发送 live turn，也不替代 live Provider / WebSearch-WebFetch 联网 E2E。
62. `P3.286`：Agent session history / archive Electron current 复验已在当前并行工作树下通过。`npm run smoke:agent-session-history-electron-fixture -- --timeout-ms 180000` 通过，真实 Electron preload `app_server_handle_json_lines` 覆盖内存 fail-closed、persisted current timeline session seed、归档 / 恢复、sidecar restart readback 与侧栏 GUI 归档 / 恢复入口；summary 显示 `ok=true`、`consoleErrors=[]`、`electronPreloadBridge=true`、`sidecarRestartReadback=true`、`sidebarGuiArchive=true`，方法集合为 `initialize`、`agentSession/start`、`agentSession/read`、`agentSession/update`、`agentSession/list`，侧栏阶段只走 `agentSession/update/list` 且 `forbiddenMethodsSeen=[]`。该证据证明最近对话详情 hydrate、归档列表读取和恢复入口仍经 Electron Desktop Host IPC -> App Server JSON-RPC -> RuntimeCore / LocalAppDataSource current 主链打通；不调用模型后端，也不替代 live Provider / WebSearch-WebFetch 联网 E2E。
63. `P3.287`：code artifact workbench Electron current 复验已在当前并行工作树下通过。`npm run smoke:code-artifact-workbench-electron-fixture -- --timeout-ms 180000` 通过，真实 Electron preload `app_server_handle_json_lines` 创建带 `artifact.snapshot` / `tool.result` 的 current session，再从 GUI 侧栏打开会话和工作台；summary 显示 `ok=true`、`consoleErrors=[]`、`pageErrors=[]`、`artifactProjectionPersisted=true`、`toolTimelineProjectionPersisted=true`、`guiToolTimelineEvidencePresent=true`、`assertions.workbenchOpened=true`、`assertions.toolTimelineEvidencePresent=true`，并看到 Canvas workbench shell / panel / artifact summary。随后 `npm run smoke:agent-runtime-current-fixture` 通过，`liveProviderUsed=false`。该证据证明代码产物、工具轨迹和工作台 GUI 可见性仍经 Electron Desktop Host IPC -> App Server JSON-RPC -> RuntimeCore/backend -> read model -> GUI workbench current 主链打通；不调用正式模型，也不替代 live Provider / WebSearch-WebFetch 联网 E2E。
64. `P3.289`：Claw current Electron fixture 已补 Codex-style `agentSession/event -> agentSession/read` 同 turn 证据。`npm run smoke:claw-chat-current-fixture -- --prefix claw-chat-current-fixture-event-read --timeout-ms 180000` 通过，summary 位于 `.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-event-read-summary.json`，显示真实 Electron IPC `app_server_handle_json_lines` 发起 non-live `agentSession/turn/start` probe，`app_server_drain_events` 采集同一 `turnId` 下 `message.delta`、`message.delta`、`turn.completed`，并由 `agentSession/read` 读回同一 probe `turnId` 与 assistant delta 文本；断言 `eventReadProbeObserved=true`、`readModelEventReadAligned=true`。同轮 `npm run smoke:claw-chat-current-fixture -- --scenario cancel --timeout-ms 180000` 与 `npm run smoke:agent-runtime-current-fixture` 均通过。该证据补齐非 live event/read 对齐，但仍不等同 live Provider / WebSearch-WebFetch 联网 E2E。
65. `P3.290`：Claw current Electron fixture summary 已拆分 `commonAssertions` / `scenarioAssertions` / `notApplicableAssertions`，避免 complete 场景把 cancel-only 项、cancel 场景把 event-read-only 项误报成通过证据。`claw-chat-current-fixture-complete-scoped-assertions-summary.json` 只把 `noEpochFallbackTitle/readModelCompleted/eventReadProbeObserved/readModelEventReadAligned` 作为 complete 专属断言；`claw-chat-current-fixture-cancel-scoped-assertions-summary.json` 只把 `usedCurrentTurnCancel/externalFixtureCancelUsed/fixtureCancelReachedBackend/guiStopClicked/readModelCanceled` 作为 cancel 专属断言。该刀只提升 non-live Electron fixture 证据质量，不改变生产链路，也不替代 live Provider / WebSearch-WebFetch 联网 E2E。
66. `P3.291`：Claw current Electron fixture 已把 Codex-style event/read 证据扩到工具事实。`npm run smoke:claw-chat-current-fixture -- --prefix claw-chat-current-fixture-tool-event-read --timeout-ms 180000` 通过，summary 位于 `.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-tool-event-read-summary.json`，同一 non-live probe turn 通过真实 Electron IPC `app_server_handle_json_lines` 触发 `agentSession/turn/start`，`app_server_drain_events` 采集 `message.delta`、`tool.started`、`tool.result`、`message.delta`、`turn.completed`，并由 `agentSession/read` 在同一 `turnId` 下读回 `WebFetch` completed tool call 与输出；断言 `readModelToolCallAligned=true`。同轮 `npm run smoke:claw-chat-current-fixture -- --scenario cancel --timeout-ms 180000` 通过，证明新增 complete-only tool probe 不影响停止链路。该证据仍是 external fixture backend，不调用正式模型、不联网，也不替代 live Provider / WebSearch-WebFetch 实际联网 E2E。
67. `P3.293`：`claw-chat-ready-streaming` 授权后的 live WebSearch/WebFetch E2E pass 条件已收紧到本次 live Web turn 的 App Server event/read。脚本现在单独记录 `liveWebAppServerEvent`、`liveWebReadAfterEvent`、`liveWebSessionReadTurn` 与 `liveWebCurrentStreamingEvent`，最终 verdict 必须满足 `liveWebAppServerEventSeen=true` 与 `liveWebSessionReadAfterEventSeen=true`；其中 `liveWebAppServerEventSeen` 只认 `appServerEventRecords(invokes)` 中匹配本次 `liveWebSessionId/liveWebTurnId` 的 App Server JSON-RPC response / drain 事件，不接受 renderer console fallback。定向 `live-provider-smoke-gate` 16 项通过，未授权 `npm run smoke:claw-chat-ready-streaming -- --timeout-ms 30000` 仍 fail closed。该证据只是收紧 live 验收标准，仍未调用正式模型、不联网；完整目标仍需显式授权后跑 `npm run smoke:claw-chat-ready-streaming -- --allow-live-provider --timeout-ms 180000`。
68. `P3.294`：App Server cancel terminal projection 已对齐 current 语义。`turn.canceled` / `turn.cancelled` 不再由前端 App Server event gateway 投影成普通 `error`，而是投影为 terminal `turn_completed`，保留 `turn.status=canceled`；GUI stream listener 收到 canceled / interrupted / aborted 终态后立即收口为已停止，清 running tool、释放 active stream 与输入框，不进入空最终答复失败路径。验证通过：`threadClient` / `appServerSessionClient` / `agentStreamTurnEventBinding` 定向 38 项、真实 Electron `smoke:claw-chat-current-fixture` complete 与 cancel 两场景、`check-app-server-client-contract` 208 项和 `npm run test:contracts`。该证据仍是 non-live external fixture backend，不代表 live Provider / WebSearch-WebFetch 联网 E2E 已完成。
69. `P3.296`：`claw-chat-ready-streaming` 授权后的 live WebSearch/WebFetch E2E pass 条件已继续收紧到同一 live Web turn 的工具事件流。`scripts/lib/claw-chat-live-web-tool-evidence.mjs` 新增 `liveWebToolStreamEvidenceFromEvents(...)`，只从 App Server `agentSession/event` response / drain records 读取严格匹配本次 `liveWebSessionId + liveWebTurnId` 的事件，并要求 `WebSearch` 与 `WebFetch` 均有 `tool.started -> tool.result`、`tool.result` 非空输出、result 顺序晚于 started，且同 turn 看到 terminal event；缺 turn scope、跨 turn、无输出或乱序都不能通过。`scripts/claw-chat-ready-streaming-smoke.mjs` 的 verdict 新增 `liveWebSearchToolEventsSeen`、`liveWebFetchToolEventsSeen`、`liveWebRequiredToolEventsSeen`、`liveWebRequiredToolEventOutputsPresent`、`liveWebRequiredToolEventOrderValid` 与 `liveWebTurnCompletedEventSeen`，并把 `liveWebAppServerEvent` 改成严格 session / turn 匹配。定向 `live-provider-smoke-gate` + helper 行为单测 25 项通过，未授权 `npm run smoke:claw-chat-ready-streaming -- --timeout-ms 30000` 仍 fail closed；真实 live Provider 仍未运行，完整目标仍需显式授权后跑 `npm run smoke:claw-chat-ready-streaming -- --allow-live-provider --timeout-ms 180000`。
70. `P3.297`：`claw-chat-ready-streaming` 授权后的 live WebSearch/WebFetch read-after-event 证据已收紧为 strict session / turn scope。`appServerSessionReadAfterEventEvidence(...)` 新增 `strictEventScope` 选项，live Web turn 固定传 `{ strictEventScope: true }`；因此 `liveWebSessionReadAfterEventSeen` 只能由明确匹配本次 `liveWebSessionId + liveWebTurnId` 的 App Server `agentSession/event` 后续 `agentSession/read` 满足，缺 session / turn scope 的旧事件不能再让 live E2E 误通过。long/follow turn 普通诊断仍保持原宽松兼容口径。该刀只收紧授权后验收标准，真实 live Provider / WebSearch-WebFetch 联网 E2E 仍未运行。
71. `P3.298`：前端 Agent Runtime / Agent App UI runtime 边界已补 contract 守卫。`scripts/check-app-server-client-contract.mjs` 现在递归扫描 `src/lib/api/agentRuntime/**` production `.ts`，禁止生产 mock fallback、direct Provider/backend HTTP、parallel stream transport、Gateway credential 与 App Server mock backend，锁住该目录只能作为前端 thin client gateway / compat projection；同时对 `src/lib/api/agentApps.ts` 的 `requestAgentAppUiRuntimeAppServer`、`startAgentAppUiRuntime`、`getAgentAppUiRuntimeStatus`、`stopAgentAppUiRuntime` 做函数体级扫描，要求只经 App Server `agentAppUiRuntime/start|status|stop` current methods，并禁止 safeInvoke legacy facade、`agent_app_runtime_*` task facade、`agentSession/turn/start`、tool / evidence projection、Provider HTTP、Gateway credential 与 mock backend。`node "scripts/check-app-server-client-contract.mjs"` 通过，contract 扩为 `210 checks`；该守卫证明 `agentAppUiRuntime/*` 只管 UI 子进程生命周期，不能变成第二套对话 turn / tool runtime / evidence 后端。
72. `P3.299`：Claw 停止后继续输出回归已纳入 current 聚合 smoke。`npm run smoke:agent-runtime-current-fixture` 现在除 history/cache、stream terminal、Claw 终态 UI 与 Electron fixture guard 外，会真实运行 `scripts/agent-runtime/claw-chat-current-fixture-smoke.mjs --scenario cancel-then-continue`；该 Electron fixture 通过 GUI 发送“整理今天的国际新闻”、点击停止、确认 `agentSession/turn/cancel` / read model `canceled` / 输入框恢复，再在同一 current session 输入“继续输出”并完成第二轮。最新通过 summary 位于 `.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-cancel-then-continue-regression-summary.json`，显示 `turnStartCount=2`、`turnCancelCount=1`、`readModelCanceled.latestTurnStatus=canceled`、`readModelContinueCompleted.latestTurnStatus=completed`，所有 common/scenario assertions 为 true。该证据仍是 local external fixture backend，不调用正式模型、不联网，也不替代 live Provider / WebSearch-WebFetch 实际联网 E2E。
73. `P3.300`：contract / current fixture / 默认 Electron GUI smoke 已完成同轮复验收口。`npm run test:contracts` 完整通过，App Server client contract 为 `210 checks`，frontend commands `388`，Electron host commands `107`，mock priority commands `0`，DevBridge truth commands `148`，并包含 Electron release workflow guard、Harness cleanup 与 docs boundary；`npm run smoke:agent-runtime-current-fixture` 通过并实际运行 Claw 停止后同会话继续输出 Electron fixture，最新 summary session `claw-chat-current-1780875900810-29926`，`liveProviderUsed=false`；`npm run verify:gui-smoke` 通过，确认 Electron renderer loaded、App Server `protocol=appserver.v0 version=1.60.0` initialized、Claw workbench shell ready。该证据证明当前非 live 主链仍经 `Frontend -> Electron Desktop Host IPC -> App Server JSON-RPC -> RuntimeCore/backend` 打通，且命令契约 / mock 禁回流 / 默认 GUI 壳未漂移；它不发送 live turn、不调用正式 Provider，也不替代 live Provider streaming / WebSearch-WebFetch 实际联网 E2E。
74. `P3.301`：对话详情 / 归档 current Electron 复验已通过。`npm run smoke:agent-session-history-electron-fixture -- --timeout-ms 180000` 使用真实 Electron preload `app_server_handle_json_lines` 覆盖内存 session fail-closed、`agentSession/start/read/update/list`、persisted current timeline 归档 / 恢复、sidecar restart readback 和侧栏 GUI 归档 / 恢复入口；summary 位于 `.lime/qc/gui-evidence/agent-session-history-electron-fixture/agent-session-history-electron-fixture-summary.json`，显示 `ok=true`、`electronPreloadBridge=true`、`sidecarRestartReadback=true`、`sidebarGuiArchive=true`、`consoleErrors=[]`。该证据直接覆盖“对话 / 归档无法获取”风险，证明最近对话详情 hydrate、归档列表读取和恢复入口仍经 `Frontend -> Electron Desktop Host IPC -> App Server JSON-RPC -> RuntimeCore / LocalAppDataSource` current 主链打通；它不触发 live turn、不调用正式 Provider，也不替代 live Provider streaming / WebSearch-WebFetch 实际联网 E2E。
75. `P3.302`：当前脏工作树下 contract / GUI / Agent Runtime 聚合复验已通过。`npm run test:contracts` 显示 App Server client contract `210 checks`、frontend commands `388`、Electron host commands `107`、mock priority commands `0`、DevBridge truth commands `148`，并通过 scripts governance、Electron release workflow guard、Harness cleanup 与 docs boundary；`npm run verify:gui-smoke` 通过，确认 Electron renderer loaded、App Server `protocol=appserver.v0 version=1.60.0` initialized、Claw workbench shell ready；`npm run smoke:agent-runtime-current-fixture` 通过并实际运行 Claw 停止后同会话继续输出 Electron fixture，本轮 session `claw-chat-current-1780877737248-88730`，`liveProviderUsed=false`。该证据证明并行 Forge / updater / release / i18n 写集没有打断当前非 live `Frontend -> Electron Desktop Host IPC -> App Server JSON-RPC -> RuntimeCore/backend` 主链、命令契约或 mock 禁回流；它仍不调用正式 Provider、不联网，也不替代 live Provider streaming / WebSearch-WebFetch 实际联网 E2E。
76. `P3.303`：live Provider 未授权 gate 仍 fail-closed。`npm run smoke:claw-chat-ready-streaming -- --timeout-ms 30000` 按预期以退出码 1 停在授权检查，提示必须显式 `--allow-live-provider` 或设置 `LIME_ALLOW_LIVE_PROVIDER_SMOKE=1 / LIME_REAL_API_TEST=1`；该运行没有进入 DevBridge、Provider、`agentSession/turn/start`、WebSearch 或 WebFetch 阶段。该证据只证明未授权时不会偷偷调用正式模型或联网工具，不是 live Provider / WebSearch-WebFetch E2E 通过证据；完整目标仍需用户明确授权后运行 `npm run smoke:claw-chat-ready-streaming -- --allow-live-provider --timeout-ms 180000`。
